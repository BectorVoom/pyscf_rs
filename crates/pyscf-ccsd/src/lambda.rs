//! Closed-shell Λ-equations — `solve_lambda` / `update_lambda` (port of
//! `pyscf/cc/ccsd_lambda.py`; the concrete `CCSD.solve_lambda` at
//! `ccsd.py:1273` dispatches to `ccsd_lambda.kernel`, RESEARCH A6).
//!
//! **STUB (Wave 3).** Reserves the module. Same materialize-then-`oracle_*`
//! reduction discipline as [`crate::update_amps`].
#![allow(dead_code)]

// solve_lambda / update_lambda land in Wave 3.
