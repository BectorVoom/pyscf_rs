[package]
name = "pyscf-geomopt"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
description = "Geometry optimisation (BFGS+RFO redundant internals). Implemented in Phase 7."

[lib]
path = "src/lib.rs"

[dependencies]
