[package]
name = "pyscf-kernels"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
description = "Numerical kernels (DFT XC, integrals dispatch). Implemented in Phase 4."

[lib]
path = "src/lib.rs"

[dependencies]
