//! `Mp2OverrideHooks` trait + `NoMp2Overrides` default impl (D-08): the
//! subclass-override seam that DF-MP2 uses to swap the `ao2mo` ERI source
//! (mirrors the Phase-3 SCF `OverrideHooks` precedent). Body filled in plan
//! 05-05.
