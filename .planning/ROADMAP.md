# Roadmap: pyscf_rs

**Created:** 2026-05-10
**Granularity:** standard (5-8 target; landed at 8)
**Mode:** standard (Horizontal Layers — each phase is a complete chemistry module / cross-cutting concern, not a vertical user-story slice)
**Total v1 requirements:** 121 (10 FOUND + 8 ALG + 11 GTO + 14 SCF + 11 DFT + 8 MP2 + 11 CCSD + 10 GRAD + 7 GEOMOPT + 9 BIND + 9 ORACLE + 7 PERF + 6 DIST)
**Coverage:** 121 / 121 mapped

## Overview

pyscf_rs is a pure-Rust rewrite of PySCF that ships as a `pip install`-able wheel preserving the `from pyscf import gto, scf, dft, mp, cc, grad, geomopt` import surface. The architecture is locked: a 20-crate horizontal-layered façade workspace mirroring `cintx`/`xcfun_rs` (Phase 1 shipped 15 with `pyscf-algebra` owning all linear algebra; Phase 3 grew the workspace to 18 by adding `pyscf-chkfile`, `pyscf-diis`, `pyscf-df`; Phase 4 added `pyscf-grids` → 19; Phase 5 added `pyscf-ao2mo` → 20, D-01), cubecl 0.10.0 as the sole compute primitive (CPU SIMD/CUDA/WGPU/ROCm), faer 0.24 used only for host eigh/Cholesky/QR/SVD behind the algebra crate's surface, PyO3 0.28 for the Python boundary, and PySCF-as-live-oracle in CI. Backend selection is runtime-driven via `PYSCF_BACKEND`; the workspace `gpu` umbrella feature is OFF by default so the standard build is CPU-only. See `docs/manual/Cubecl/` for the cubecl runtime/ComputeClient/tensor-handle pattern that `pyscf-algebra` is built on.

The dependency DAG dictates phase ordering almost entirely: `core/runtime → kernels → gto → scf → {dft, mp2} → ccsd → grad → geomopt → wheel`. Phases 1–7 walk this critical path with the PyO3 contract folded into Phase 3 (SCF) so subclass-override / NumPy-boundary / GIL-release conventions lock on a small surface (RHF) before DFT's overrideable explosion. Phase 8 is the closing "ship readiness" phase combining GPU backend enable, oracle hardening, and wheel distribution because all three gate on the same artifact (a working CPU baseline across every method) and feed the same goal (validating the 2–5× speedup claim on a real benchmark suite, in a real wheel, on a real CI machine).

Five SHOWSTOPPER pitfalls and three MAJORs are addressed in Phase 1 and Phase 3 — this is by design. Foundational decisions (FMA-free profile, ordered-reduction primitives, panic policy, cubecl pin, scope-creep lint, sibling-crate ABI matrix) all land before any kernel touches a basis function. Bit-exact-with-PySCF and PyO3 drop-in fidelity are the two themes everything else inherits from.

## Phases

**Phase Numbering:**

- Integer phases (1, 2, 3): Planned milestone work
- Decimal phases (2.1, 2.2): Urgent insertions (marked with INSERTED) — none yet

- [x] **Phase 1: Foundation** — Workspace (15 crates including `pyscf-algebra`), core types, runtime + env-driven backend selection (`PYSCF_BACKEND`), workspace `gpu` feature (OFF by default → CPU is the default backend), single-owner cubecl algebra crate (GEMM/reduce/AXPY/dot via `cubecl-matmul`/`cubecl-reduce`/`#[cube]`), FMA-free oracle profile, ordered-reduction primitives, panic policy, cubecl pin, scope-creep + dependency-wall lints, nightly cross-crate matrix CI (completed 2026-05-23)
- [x] **Phase 2: GTO** — Mole, basis-set loading (5 atom-input × 11 basis-input forms), ECP, intor wrappers via cintx, eval_gto for grids (completed 2026-05-23)
- [x] **Phase 3: SCF + PyO3 bindings** — RHF/UHF/GHF + DIIS + chkfile + sign canonicalization + first end-to-end energy AND lock the entire PyO3 contract (subclass-override dispatch, NumPy contiguity, GIL release seam, abi3-py310 wheel skeleton, oracle harness bootstrap) (completed 2026-05-24)
- [x] **Phase 4: DFT** — RKS/UKS + Becke grids ported byte-for-byte + libxc/xcfun XC parser + range-separated hybrids + VV10 NLC + DF-DFT (all 10 plans executed 2026-05-22; verification = gaps_found 3/5 — 4 BLOCKERs in 04-VERIFICATION.md: UKS dead/closed-shell, f32 0.0-substitution, l>4 panic, non-injective XC-cache key → gap closure required before complete) (completed 2026-05-23)
- [x] **Phase 5: MP2** — RMP2/UMP2/DF-MP2 + frozen-core + AO→MO transformation kernel + helpers CCSD imports (completed 2026-05-23)
- [x] **Phase 6: CCSD** — RCCSD/UCCSD + amplitude DIIS + Lambda + RDMs + AO-direct + DF-CCSD with HDF5 spill + tensor-arena from day one + T1/D1/D2 diagnostics (completed 2026-05-25)
- [x] **Phase 7: Gradients + Geomopt** — Analytical gradients for HF/DFT/MP2/CCSD + ECP + CPHF/CPKS + native Rust BFGS+RFO in redundant internals + geomeTRIC/berny drop-in shims (completed 2026-05-26)
- [ ] **Phase 8: GPU enable + Oracle hardening + Distribution** — Per-backend regression suite (CPU/CUDA/WGPU/ROCm), 2–5× benchmark proof, abi3-py310 wheel for Linux/macOS/Windows × x86_64+aarch64, per-backend extras, drop-in audit (≥80% upstream tests pass against pyscf-rs as import target), full top-20-idiom shakedown

## Phase Details

### Phase 1: Foundation

**Goal**: The workspace exists, builds clean as a 15-crate horizontal-layered façade, the `pyscf-algebra` crate exposes a backend-agnostic linear-algebra surface dispatching to cubecl on the active runtime, and every cross-cutting convention that gates downstream numerical correctness is in place and CI-enforced before the first kernel lands.
**Depends on**: Nothing (first phase)
**Requirements**: FOUND-01, FOUND-02, FOUND-03, FOUND-04, FOUND-05, FOUND-06, FOUND-07, FOUND-08, FOUND-09, FOUND-10, ALG-01, ALG-02, ALG-03, ALG-04, ALG-05, ALG-06, ALG-07, ALG-08, ORACLE-01, ORACLE-05, ORACLE-09
**Success Criteria** (what must be TRUE):

  1. `cargo build --workspace` succeeds with no GPU features (CPU-only, the default); the workspace contains 15 members (`pyscf-{core,runtime,algebra,kernels,gto,scf,dft,mp2,ccsd,grad,geomopt,py,oracle,bench}` + top-level façade) and `pyscf-{core,runtime,algebra}` are non-stub (BackendKind enum, `select_backend()` env-driven resolver, WorkspacePool, Mole/Density/Energy types, `AlgebraClient` enum + `gemm`/`reduce_sum`/`axpy`/`dot` surface, all traits compile). `cargo build --workspace --features gpu` additionally compiles the `cuda` and `wgpu` cubecl runtimes.
  2. `cargo build --profile release-oracle --workspace` produces FMA-free machine code; CI runs `cargo-llvm-ir | grep llvm.fmuladd` over every numerical crate's object files under the oracle profile and finds **zero** matches (Pitfall 1 mitigation).
  3. A canary test using `oracle_sum`/`oracle_dot` reduction primitives produces **bit-identical** results on `RAYON_NUM_THREADS=1` and `RAYON_NUM_THREADS=8` runs of the same input vector (Pitfall 2 mitigation).
  4. `cubecl = "=0.10.0"` (and all `cubecl-*` crates including `cubecl-cpu`, `cubecl-wgpu`, `cubecl-cuda`, `cubecl-rocm`, `cubecl-matmul`, `cubecl-reduce`, `cubecl-std`) are pinned exactly via `[patch.crates-io]` in workspace `Cargo.toml`, matching cintx/libxc_rs/xcfun_rs; nightly cross-crate matrix CI rebuilds and tests cintx + libxc_rs + xcfun_rs + pyscf_rs together against the pin and reports green (Pitfall 3 + 15 mitigation, ORACLE-05).
  5. CI enforces four lints that block PR merge: (a) `unwrap()` in numerical modules → clippy deny, (b) `forbidden-paths` for upstream out-of-scope imports (pbc/x2c/mcscf/tdscf/adc/gw/eom/NAC/EPH) → custom lint deny (FOUND-08, Pitfall 21), (c) every `extern "C"` callback wrapped in `catch_unwind` → grep-based CI check (FOUND-07, Pitfall 14), (d) **algebra dependency-wall lint** (ALG-06): `cargo metadata` graph check fails the build if any crate other than `pyscf-algebra` or `pyscf-runtime` declares a `cubecl-*` dependency.
  6. **Backend resolution behaves**: a `pyscf-algebra` integration test sets `PYSCF_BACKEND` to each of `cpu`/`cuda`/`wgpu`/`rocm`/`metal`/`auto`/`unset`/`bogus` and asserts the resolved backend matches the documented FOUND-03 + ALG-04 rules — including the case where `PYSCF_BACKEND=cuda` is set but the `cuda` feature is not compiled in (must fall back to CPU + emit `tracing::warn!`). With no env var set on a CPU-only build, GEMM/reduce-sum/axpy on a 256×256 input agree with a `faer 0.24` host reference to 1e-12 (ALG-01..04, ALG-08).

**Plans**: 9 plans across 6 waves (7 shipped + 2 gap-closure)

Plans:

- [x] 01-01-PLAN.md — Workspace skeleton (root Cargo.toml, 12 stub crates, .cargo/config.toml, deny.toml; FOUND-01, FOUND-04, FOUND-10, ORACLE-01)
- [x] 01-02-PLAN.md — pyscf-core universal types and method traits (FOUND-02)
- [x] 01-03-PLAN.md — pyscf-runtime BackendKind, probes, WorkspacePool, tracing init (FOUND-03, FOUND-09, ALG-04, ALG-08-prep)
- [x] 01-04-PLAN.md — pyscf-algebra AlgebraClient + select_backend + 7 primitives + oracle_sum + 4 integration tests + façade (ALG-01..05, ALG-07, ALG-08, FOUND-06)
- [x] 01-05-PLAN.md — xtask 5 CI lint binaries (FOUND-05, FOUND-07, FOUND-08, ALG-06)
- [x] 01-06-PLAN.md — GitHub Actions ci.yml + nightly-cross-crate.yml (FOUND-05, FOUND-08, FOUND-10, ALG-06, ORACLE-05, ORACLE-09)
- [x] 01-07-PLAN.md — CONTRIBUTING.md + docs/upgrade-cubecl.md + README.md additions (FOUND-04, FOUND-09)
- [x] 01-08-PLAN.md — GAP CLOSURE: cintx clean-SHA repin + Cargo.lock commit (closes BLOCKER 1 + 3; FOUND-01, FOUND-04, FOUND-10)
- [x] 01-09-PLAN.md — GAP CLOSURE: check-cubecl-pin transitive version-skew reconciliation (closes BLOCKER 2; FOUND-04)

### Phase 2: GTO

**Goal**: A user can construct a molecule with any of upstream PySCF's atom-input or basis-input forms and run any 1e/2e integral upstream supports for in-scope methods, with byte-for-byte agreement on the internal `_atm`/`_bas`/`_env` arrays.
**Depends on**: Phase 1
**Requirements**: GTO-01, GTO-02, GTO-03, GTO-04, GTO-05, GTO-06, GTO-07, GTO-08, GTO-09, GTO-10, GTO-11
**Success Criteria** (what must be TRUE):

  1. `pyscf.M(atom='O 0 0 0; H 0 1 0; H 1 0 0', basis='cc-pvdz')` and the four other atom-input forms produce a `Mole` whose `_atm`, `_bas`, `_env`, `ao_loc_nr`, `nao_nr` arrays match upstream PySCF byte-for-byte on the test corpus (GTO-01, GTO-04).
  2. All 207 built-in basis-set files in `pyscf/gto/basis/` resolve correctly via `mol.basis = '<name>'`; `gto.parse(...)` accepts user-supplied Gaussian-94 and NWChem text; ECP via `mol.ecp = ...` loads and `mol.intor('int1e_ecp')` matches upstream bit-exact under `release-oracle` (GTO-02, GTO-03, GTO-05).
  3. `mol.intor('int2e')`, `mol.intor('int1e_ovlp_sph')`, and the integral families upstream PySCF supports for SCF/DFT/MP2/CCSD/grad all dispatch to `cintx` and produce arrays that match upstream within the cintx oracle tolerance; F-order layout is preserved on output where upstream returns F-order (Pitfall 8 mitigation).
  4. `eval_gto(mol, name, coords, ...)` for `GTOval`, `GTOval_sph`, `GTOval_deriv1`, `GTOval_deriv2`, `GTOval_ip`, `GTOval_ig` matches upstream values element-wise on a 1000-point grid (GTO-07).
  5. `Mole` exposes the ≥30 attribute floor (`atom`, `basis`, `charge`, `spin`, `nelectron`, `natm`, `nbas`, `nao_nr`, `nao_2c`, `ao_loc_nr`, `ao_labels`, `cart`, `verbose`, `max_memory`, `unit`, `output`, `_atm`, `_bas`, `_env`, …); `mol.dumps()`/`gto.Mole.loads()` JSON round-trip; `mol.copy()` deep-copies; `mol.set_geom_(new_atom)` mutates in place and returns self (GTO-08, GTO-09, GTO-10).

**Plans**: 10 plans across 10 waves (1 Wave 0 risk-buy-down + 8 implementation + 1 deferred gap-closure for cintx ECP)

Plans:

- [x] 02-01-PLAN.md — Wave 0 scaffolding: cintx round-trip smoke, cubecl-cpu kernel smoke, F/C-order layout table, algebra-wall allowlist update, oracle harness scaffold, PYSCF_BASIS_PATH docs (W0-T1..W0-T6)
- [x] 02-02-PLAN.md — Mole struct + ≥30-attribute floor + format_atom port (4-of-5 atom-input forms; 5th deferred to Phase 3) (GTO-01, GTO-08)
- [x] 02-03-PLAN.md — Basis loader (PYSCF_BASIS_PATH resolver + ALIAS table + NWChem/NWChem-ECP/CP2K parser dispatch + format_basis dispatcher) (GTO-02, GTO-03)
- [x] 02-04-PLAN.md — Mole↔cintx bridge (zero-copy BasisSet re-export + make_env flat-array projection) (GTO-04, GTO-11)
- [x] 02-05-PLAN.md — mol.intor(name) cintx dispatcher (with F-order layout preservation per Pitfall 8) (GTO-06)
- [x] 02-06-PLAN.md — eval_gto cubecl kernel in pyscf-kernels + algebra-wall-friendly user wrapper in pyscf-gto (GTO-07; s-shells fully implemented; l ≥ 1 deferred to Phase 4 DFT)
- [x] 02-07-PLAN.md — ECP loading parser + EcpEngine trait + EcpEngineNotAvailable stub + intor dispatcher routing (GTO-05 loading half)
- [x] 02-08-PLAN.md — mol.dumps()/Mole::loads() JSON round-trip + mol.copy() + mol.set_geom_() in-place mutation per Pattern 5 (GTO-09, GTO-10)
- [x] 02-09-PLAN.md — Phase 2 verification rollup: pytest oracle harness for byte-identity + intor + eval_gto + JSON interop + builtin basis sweep + STATE/VALIDATION updates (verifies GTO-01..11)
- [x] 02-10-PLAN.md — gap-closure: cintx ECP merged (Phase 19/20) → swapped EcpEngineNotAvailable for cintx-backed CintxEcpEngine; closes GTO-05 evaluation half (in-tree Cu/LANL2DZ gate green; upstream byte-identity pytest shipped, gated on oracle venv)

### Phase 3: SCF + PyO3 bindings

**Goal**: A Python user runs `from pyscf import scf; scf.RHF(mol).kernel()` from an unmodified existing PySCF script and gets the same total energy as upstream PySCF to ≤1 µHartree, while every PyO3 contract that downstream methods inherit (subclass-override dispatch, NumPy contiguity, GIL release seam, panic-to-exception, abi3 wheel) is locked and CI-enforced on this single small surface (RHF on H2O/cc-pVDZ).
**Depends on**: Phase 2
**Requirements**: SCF-01, SCF-02, SCF-03, SCF-04, SCF-05, SCF-06, SCF-07, SCF-08, SCF-09, SCF-10, SCF-11, SCF-12, SCF-13, SCF-14, BIND-01, BIND-02, BIND-04, BIND-05, BIND-06, BIND-07, BIND-09, ORACLE-02, ORACLE-08
**Success Criteria** (what must be TRUE):

  1. `scf.RHF(mol).kernel()` on the test corpus (H2O/cc-pVDZ, benzene/6-31G*, …) converges to upstream PySCF total energy to ≤1 µHartree under `release-oracle` (bit-exact when reduction order matches); `scf.UHF(mol).kernel()` matches upstream for open-shell systems; `scf.GHF(mol).kernel()` runs to completion (correctness only, perf parity not required) (SCF-01, SCF-02, SCF-03).
  2. **Cross-platform invariant**: running `scf.RHF(H2O).kernel()` under `release-oracle` on Linux x86_64 and macOS aarch64 produces total energies that agree to within 1 µHartree of each other (Pitfall 12 mitigation, depends on FOUND-05/06 and on `pyscf-core::lib::canonicalize_signs` (SCF-13) producing vendor-stable eigenvectors via the largest-|coefficient|-with-lowest-index sign-flip rule — Pitfall 4 mitigation).
  3. C-DIIS with `mf.diis_space=8`/`mf.diis_start_cycle=1` reproduces upstream DIIS extrapolation when reduction order is held; all five `init_guess` modes (`'minao'`, `'atom'`, `'1e'`, `'huckel'`, `'chkfile'`) plus user-supplied `dm0` produce upstream-matching first-iteration densities; `mf.density_fit(auxbasis=...)` solves DF-HF with upstream-matching aux defaults; `mf.chkfile = path` writes an HDF5 file that h5py reads with the upstream PySCF schema and `mf.from_chk(path)` reads upstream-h5py-written chkfiles (SCF-04..07, SCF-10, SCF-14, ORACLE-08, Pitfall 9 + 11 mitigation).
  4. **PyO3 subclass dispatch works**: a Python user defines `class MyHF(scf.RHF): def get_veff(self, mol, dm): return super().get_veff(mol, dm) + correction(dm)` and the Rust SCF driver calls the Python override (verified by an in-CI assertion that the override is invoked at least once per cycle); the same is true for every overrideable hook (`get_jk`, `get_hcore`, `get_init_guess`, `get_fock`, `get_occ`, `eig`, `make_rdm1`, `energy_elec`, `energy_tot`) — dispatched via `slf.call_method1(py, …)`, never via Rust MRO (BIND-07, SCF-08, Pitfall 7 mitigation).
  5. **PyO3 boundary discipline locked**: every public PyO3 entry point that takes a NumPy array calls `to_owned()` on input that is not `is_standard_layout()`, and a stride-fuzz CI test that calls each entry with `a`, `a.T`, `a[::2]`, `a[:, 1:5]` produces identical answers (BIND-04, Pitfall 5); long compute calls `Python::detach` and a `python3.13t` free-threaded CI build runs the SCF test corpus without deadlocks (BIND-05, Pitfall 6); `pyo3::sync::GILOnceCell` replaces every `lazy_static!` (BIND-06); a Rust panic in any kernel called via FFI surfaces as a Python exception with the original error chain preserved, **never** as a process abort or undefined behavior (BIND-09, Pitfall 14); `from pyscf import scf` works exactly as upstream via `_native.scf` PyO3 submodule + `python/pyscf/__init__.py` re-export shim (BIND-01, BIND-02).
  6. **Oracle harness bootstrap**: the `oracle_check!(method, tolerance, fixture)` macro is implemented in `pyscf-oracle` (dev-deps only); every SCF success-criterion above is asserted via this macro on a curated H2O/benzene/water-trimer corpus; chkfile round-trip oracle (PySCF writes → pyscf-rs reads asserts identical, pyscf-rs writes → PySCF reads runs downstream calc asserts agreement) is in CI (ORACLE-02, ORACLE-08); `mf.analyze()`, `mf.mulliken_pop()`, `mf.mulliken_meta()`, `mf.dip_moment()` produce upstream-matching numbers (SCF-09); cross-module dispatch helpers `mf.to_uhf()`, `mf.to_rhf()`, `mf.to_uks()`, `mf.to_rks()`, `mf.to_ghf()` work (SCF-11) because MP2/CCSD will depend on them; `mf.as_scanner()` returns a callable used by geomopt (SCF-12).

**Plans**: 15 plans across 10 waves (split per checker iteration 1 WARNING 3; + 4 gap-closure: 03-12 DF-HF lock-in, 03-13 minao, 03-14 atom/huckel, 03-15 mulliken_meta)

Plans:

**Wave 1**

- [x] 03-01-PLAN.md — Workspace scaffolding (+3 crates: pyscf-chkfile/diis/df, pyscf-algebra::solve_linear, pyscf-core::canonicalize_signs; SCF-13)
- [x] 03-02-PLAN.md — Wave-0 test stubs (pyproject.toml maturin config, python overlay shim, 19 pytest xfail stubs, oracle macro stub, forbid-lazy-static lint; BIND-02 scaffolding, BIND-06)

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 03-03-PLAN.md — pyscf-scf trait + struct scaffolding (OverrideHooks trait, RHF/UHF/GHF + 30-attribute floor, InitGuessMode declarations, kernel signature; SCF-01..03, SCF-05, SCF-06, SCF-14) — WARNING 3 split

**Wave 3** *(blocked on Wave 2 completion)*

- [x] 03-11-PLAN.md — pyscf-scf kernel internals (SCF cycle loop body, Fock build, eig+canonicalize_signs, occ+rdm+energy, '1e' init_guess body, analyze/mulliken/dip, convert helpers, as_scanner; SCF-01..03, SCF-05, SCF-06, SCF-09, SCF-11, SCF-12, SCF-13) — NEW, WARNING 3 split

**Wave 4** *(blocked on Wave 3 completion)*

- [x] 03-04-PLAN.md — pyscf-diis crate (CDIIS, SDF-FDS error vector, B-matrix via pyscf-algebra::solve_linear, FockSubspace impl DiisStorable; SCF-04, Pitfall 9 mitigation)

**Wave 5** *(blocked on Wave 4 completion)*

- [x] 03-05-PLAN.md — pyscf-df crate (DfIntegrals, cholesky_eri, DEFAULT_AUXBASIS, get_jk_df, RHF::density_fit; SCF-07)

**Wave 6** *(blocked on Wave 5 completion)*

- [x] 03-06-PLAN.md — pyscf-chkfile crate + pyscf-scf chkfile schema + 'chkfile' init_guess mode (D-05/D-06; SCF-10, DIST-05 baseline)

**Wave 7** *(blocked on Wave 6 completion)*

- [x] 03-07-PLAN.md — pyscf-py PyO3 bridge (#[pymodule] _native, PyRHF/UHF/GHF, PyOverrideBridge, NumPy converters, create_exception!, abi3-py310 + free-threading features, python/pyscf overlay; BIND-01, BIND-02, BIND-04, BIND-06, BIND-07, BIND-09, SCF-08)
- [x] 03-08-PLAN.md — pyscf-oracle macro body + chkfile round-trip oracle (ORACLE-08 empirical h5py↔hdf5-metno seal — STATE.md blocker; ORACLE-02, ORACLE-08)

**Wave 8** *(blocked on Wave 7 completion)*

- [x] 03-09-PLAN.md — CI jobs (maturin-smoke, stride-fuzz, xplat-uhartree Linux x86_64 + macOS aarch64 matrix, python313t-smoke NON-abi3 separate build per RESEARCH Pitfall (NEW); BIND-05, Pitfall 12)
- [x] 03-10-PLAN.md — Python test bodies (replace 19 xfail stubs with real ≤1 µHartree / element-wise / bit-identical assertions; verifies SCF-01..14 + BIND-02/04/07/09 + ORACLE-08)

**Wave 12** *(blocked on Wave 8 completion)*

- [x] 03-12-PLAN.md — DF-HF end-to-end lock-in (RHF::density_fit + DfHooks converge with int2e/int3c2e real; SCF-07) — gap-closure

**Wave 13** *(blocked on Wave 12 completion)*

- [x] 03-13-PLAN.md — minao default init guess (intor_cross + NRSRHF_CONFIGURATION/frac_occ; byte-matches upstream H2 dm; SCF-05) — gap-closure

**Wave 14** *(blocked on Wave 13 completion)*

- [x] 03-14-PLAN.md — atom + huckel init guesses (get_atm_nrhf per-element spherically-averaged atomic RHF; SCF-05 complete — all 5 modes) — gap-closure
- [x] 03-15-PLAN.md — mulliken_meta meta-Löwdin population analysis (orth_ao; SCF-09) — gap-closure

**UI hint**: yes

### Phase 4: DFT

**Goal**: A user runs `dft.RKS(mol, xc='b3lyp').run()` on the test corpus and gets the same total energy as upstream PySCF bit-exact under `release-oracle`; every DFT-specific overrideable hook (the largest single Python-override surface in the project) re-validates the Phase 3 PyO3 contract; and the integration of all three sibling crates (`cintx` + `libxc_rs` + `xcfun_rs`) into one consistent compute pipeline is proven on a real DFT cycle.
**Depends on**: Phase 3
**Requirements**: DFT-01, DFT-02, DFT-03, DFT-04, DFT-05, DFT-06, DFT-07, DFT-08, DFT-09, DFT-10, DFT-11
**Success Criteria** (what must be TRUE):

  1. `dft.RKS(mol, xc='b3lyp').kernel()` and `dft.UKS(mol, xc=...).kernel()` on the test corpus converge bit-exact to upstream PySCF under `release-oracle` (within 1 µHartree on every fixture in the test corpus); the XC string parser handles all upstream forms — single name (`'b3lyp'`), comma form (`'pbe,pbe'`), shorthands (`'lda'` → `'lda,vwn'`), explicit weights (`'.5*HF + .5*B88,LYP'`), and aliases from `XC_ALIAS` — with a parser-parity unit test against `pyscf/dft/libxc.py` (DFT-01, DFT-02).
  2. libxc functional evaluation routes through `libxc_rs` and produces numbers identical to upstream libxc on a 100-functional smoke; xcfun routes through `xcfun_rs` and produces numbers identical to upstream xcfun (DFT-03); `numint.NumInt` exposes `eval_xc`, `eval_rho`, `nr_rks`, `nr_uks` matching upstream signatures (DFT-10).
  3. **Grid weights bit-exact**: the `Grids` class with `level`, `atom_grid`, `prune`, `radi_method`, `becke_scheme`, `atomic_radii` controls produces grid points and weights byte-for-byte identical to upstream `pyscf/dft/gen_grid.py` for `level ∈ {0..9}` on the test corpus (DFT-04, DFT-09, Pitfall 10 mitigation); range-separated hybrids (`omega`, `alpha`, `beta`) use cintx's `int2e_lr_*`/`int2e_sr_*` integral families with a parity test on a CAM-B3LYP H2O fixture (DFT-05); VV10 non-local correlation produces upstream-matching energies via `mf.nlc='VV10'`/`mf.nlcgrids` (DFT-06); `dft.RKS(mol).density_fit()` solves DF-DFT and matches upstream (DFT-07).
  4. **Subclass-override re-validation at DFT scope**: a Python user defines `class MyKS(dft.RKS): def get_veff(...)` AND `def define_xc_(...)` and the Rust DFT driver invokes both Python overrides every cycle (DFT-08, re-asserts Pitfall 7 on the larger DFT overrideable surface).
  5. **WGPU f64 honesty**: the `wgpu` feature is gated on the `shader-f64` Vulkan extension being present at runtime; when the extension is missing, the runtime falls back to CPU with a clear warning rather than silently degrading to f32 — proven by a CI job on a `shader-f64`-less device that runs `dft.RKS(mol).run()` and prints the fallback warning while still producing CPU-correct numbers (DFT-11, Pitfall 3 mitigation).

*Planning note (2026-05-22): success criterion 3's "cintx `int2e_lr_*`/`int2e_sr_*`" framing is conceptual shorthand. RESEARCH corrected this: RSH (DFT-05) sets `PTR_RANGE_OMEGA = env[8]` (via a `with_range_coulomb`-equivalent on the pyscf-gto intor path) and calls the standard `int2e` — there are no distinct lr/sr intor symbols in cintx. The plans implement the env[8] mechanism.*
**Plans**: 14 plans across 7 waves (1 scaffold + 1 sibling-crate coordination + 8 implementation + 4 gap-closure)

Plans:
**Wave 1**

- [x] 04-01-PLAN.md — Workspace scaffold: register pyscf-grids (18→19 member), wire pyscf-dft deps + off-by-default `libxc` feature (D-03), 10 Wave-0 test scaffolds covering DFT-01..11 (D-05)
- [x] 04-02-PLAN.md — libxc_rs D-04 cross-crate gap-closure (status: PENDING_LIBXC_RS_FEATURE_GATE; per-functional `[features]` + cfg-gated dispatch in the sibling repo — cintx-ECP precedent) (DFT-03)
- [x] 04-03-PLAN.md — eval_gto l≥1 + GTOval_sph_deriv1 in pyscf-kernels (the Phase 2 D-04 deferral; ρ/∇ρ for non-H molecules) (DFT-01, DFT-10)

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 04-04-PLAN.md — pyscf-grids byte-exact: Lebedev generator + radial schemes + Becke partition (pure-Python port) — owns Pitfall 10; level 0..9 byte-for-byte (DFT-04, DFT-09)
- [x] 04-05-PLAN.md — XC parsers (libxc default + xcfun alternate, parse_xc port) + XcBackend cfg-gated seam + xcfun/libxc eval — 23 parser-parity + bit-exact Slater LDA; libxc path #[cfg(libxc)]-gated/CI-only, never compiled (DFT-02, DFT-03)

**Wave 3** *(blocked on Wave 2 completion)*

- [x] 04-06-PLAN.md — RKS/UKS core: algebra-orchestrated NumInt grid loop (nr_rks/nr_uks/eval_rho/eval_xc, D-07, no #[cube]) + KS get_veff = J+Vxc−hyb·K + KsOverrideHooks + RKS/UKS reusing the Phase 3 kernel<H>; D-08 PYSCF_DTYPE f32/f64 dispatch + read-only dtype() + below-bit-exact warn (f64 default unchanged); CI-only rks_energy/uks_energy oracle arms + always-on structural layer + f32 runs-end-to-end smoke; pyscf-dft stays pyo3-free + cubecl-free, libxc never compiled (DFT-01, DFT-08, DFT-10, DFT-11)

**Wave 4** *(blocked on Wave 3 completion)*

- [x] 04-07-PLAN.md — RSH via env[8] range-coulomb (NOT int2e_lr/sr symbols) + VV10 NLC double-loop port (DFT-05, DFT-06)

**Wave 5** *(blocked on Wave 4 completion)*

- [x] 04-08-PLAN.md — DF-DFT (reuse pyscf-df, D-10) + KsResult chkfile (Phase 3 D-06) (DFT-07)
- [x] 04-09-PLAN.md — PyO3 bridge: PyRKS/PyUKS + dft submodule + KsOverrideHooks bridge + overlay; subclass-override re-validation (DFT-08)
- [x] 04-10-PLAN.md — WGPU f64 honesty (delegate to xcfun_rs + Phase 1 resolver) + dedicated cached `--features libxc` bit-exact CI job + wgpu-no-f64 CI job + nightly cross-crate re-enable (DFT-03, DFT-11)

**Wave 6 — GAP CLOSURE** *(parallel: independent blockers from 04-VERIFICATION.md)*

- [x] 04-11-PLAN.md — GAP CLOSURE CR-03: c2s_coeff l>4 panic → Result; eval_gto_sph/deriv1 return Result<_, PyscfRsError> (DFT-01)
- [x] 04-12-PLAN.md — GAP CLOSURE CR-04: replaced non-injective Σ|D| cache fingerprint with u64 content hash (DefaultHasher over f64 bits) in hooks.rs + df_dft.rs; cache hit now exact `u64 ==` (DFT-01)
- [x] 04-13-PLAN.md — GAP CLOSURE CR-02: f32 numeric chain NumericOverflow error variant + ok_or propagation replacing unwrap_or(0.0) in numint.rs (DFT-11)

**Wave 7 — GAP CLOSURE** *(blocked on Wave 6: 04-13 touches numint.rs; 04-14 also touches numint.rs)*

- [x] 04-14-PLAN.md — GAP CLOSURE CR-01: UksXcOutput spin-polarized eval_uks + genuine open-shell nr_uks grid loop + UksKsHooks + UKS::kernel wiring + PyUKS::get_veff (DFT-01, DFT-10)

### Phase 5: MP2

**Goal**: A user runs `mp.RMP2(mf).kernel()` and `mp.DFMP2(mf).kernel()` on the test corpus and gets upstream-matching correlation energies bit-exact under `release-oracle`; the AO→MO transformation kernel is general enough to be reused by CCSD; the MP2 helpers CCSD will import (`get_nocc`, `get_nmo`, `get_frozen_mask`, `get_e_hf`, `_mo_without_core`) are exposed and contract-tested.
**Depends on**: Phase 3 (SCF + PyO3 bindings); Phase 4 (DFT) is parallelizable with this phase per the architecture's wave W5.
**Requirements**: MP2-01, MP2-02, MP2-03, MP2-04, MP2-05, MP2-06, MP2-07, MP2-08
**Success Criteria** (what must be TRUE):

  1. `mp.RMP2(mf).kernel()` on RHF references and `mp.UMP2(uhf_mf).kernel()` on UHF references reproduce upstream MP2 correlation energy bit-exact under `release-oracle` on the test corpus (MP2-01, MP2-02); `mf.MP2().run()` (the cross-module dispatch idiom) returns the same numbers (MP2-01).
  2. Frozen-core options accept `frozen=int`, `frozen=list`, `frozen='auto'`, and frozen-window forms; defaults match upstream on the test corpus (MP2-03).
  3. `mp.DFMP2(mf).kernel()` reproduces upstream DF-MP2 (MP2-04); SCS-MP2 via `mp.MP2(mf).set(emp2_ss_factor=..., emp2_os_factor=...)` works (MP2-06); `mp2.make_rdm1()` and `mp2.make_rdm2()` match upstream (MP2-05).
  4. `mp2.as_scanner()` returns a callable that takes a Mole and returns the energy — exercised by a geomopt smoke test in Phase 7 (MP2-07).
  5. MP2 helpers (`get_nocc`, `get_nmo`, `get_frozen_mask`, `get_e_hf`, `_mo_without_core`) are exported with upstream-matching semantics and contract-tested via a unit test that mimics CCSD's exact import call site (MP2-08).

**Plans**: 7 plans across 7 waves (1 scaffold + 1 AO→MO transform + 5 method/bridge plans)

Plans:

**Wave 1**

- [x] 05-01-PLAN.md — Scaffold: create `pyscf-ao2mo` (20th `pyscf-*` member, D-01) + register in workspace, wire `pyscf-mp2` deps (pyo3-free), Wave-0 test scaffolds + MP2 oracle arms + CI structural/cintx-gated jobs (MP2-01,02,04,05,08)

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 05-02-PLAN.md — AO→MO 4-index transform: `general`/`full` quarter-transform host-loop body through `oracle_sum` (D-02/D-03), F-order layout, always-on synthetic-ERI roundtrip (the one un-gated numeric assertion) (MP2-01,02,04)

**Wave 3** *(blocked on Wave 2 completion)*

- [x] 05-03-PLAN.md — In-core RMP2 headline: closed-form `rmp2_kernel` + SCS factors (MP2-06) + the five MP2-08 helpers (CCSD import contract) + frozen-core int/list/'auto'/window (MP2-03) + `Mp2OverrideHooks` trait (D-08) (MP2-01,03,06,08)

**Wave 4** *(blocked on Wave 3 completion)*

- [x] 05-04-PLAN.md — Open-shell UMP2 spin-block kernel + `UmpAmplitudes (t2aa/t2ab/t2bb)` container + `make_rdm1`/`make_rdm2` via `_gamma1_intermediates` (MP2-02,05)

**Wave 5** *(blocked on Wave 4 completion)*

- [x] 05-05-PLAN.md — Conventional DF-MP2 (D-06 primary): `DFRMP2`/`DFUMP2` swap ERI source to the `pyscf-df` B-tensor, `*-ri` mp2fit aux (`default_ri`), `(ia|jb)=Σ_Q B^Q·B^Q`; structural/synthetic always-on, numeric cintx#11-gated (MP2-04)

**Wave 6** *(blocked on Wave 5 completion)*

- [x] 05-06-PLAN.md — Native RI-MP2 fast path (D-06 additional): `emp2_rhf`/`emp2_uhf` on its own `dfmp2_native` module path, reuses the shipped 3c Cholesky; native↔conventional synthetic cross-check (MP2-04)

**Wave 7** *(blocked on Wave 6 completion)*

- [x] 05-07-PLAN.md — PyO3 bridge: `PyRMP2`/`PyUMP2`/`PyDFMP2` eager-snapshot (D-07) + `Mp2OverrideHooks` `call_method1` dispatch (D-08/Pitfall 7) + `mf.MP2()` factory + `as_scanner` (MP2-07) + `python/pyscf/mp` overlay (MP2-01,02,04,05,06,07)

### Phase 6: CCSD

**Goal**: A user runs `cc.RCCSD(mf).kernel()` on caffeine/cc-pVDZ within `PYSCF_MAX_MEMORY` and gets upstream CCSD correlation energy to ≤1 µHartree without OOMing or thrashing the heap; the tensor-arena/scratchpad pattern in `pyscf-runtime` is in place from the start (not retrofitted) so `Wabef` and other large intermediates do not allocate-and-drop per iteration; AO-direct and DF-CCSD modes both work.
**Depends on**: Phase 5 (CCSD imports MP2 helpers `get_nocc`, `get_nmo`, `get_frozen_mask`, `get_e_hf`, `_mo_without_core` directly per `cc/ccsd.py:35`)
**Requirements**: CCSD-01, CCSD-02, CCSD-03, CCSD-04, CCSD-05, CCSD-06, CCSD-07, CCSD-08, CCSD-09, CCSD-10, CCSD-11
**Success Criteria** (what must be TRUE):

  1. `cc.RCCSD(mf).kernel()` and `cc.UCCSD(uhf_mf).kernel()` return correlation energies matching upstream to ≤1 µHartree on the test corpus; T1 and T2 amplitudes converge to the same minimum (energy is the convergence target; amplitude paths may differ within tolerance) (CCSD-01..03); amplitude-DIIS with default `diis_space=6` converges within the same iteration count as upstream on the test corpus (CCSD-04).
  2. **Tensor-arena pattern in place from day one**: a CCSD iteration on caffeine/cc-pVDZ allocates `Wabef` and other large intermediates **once** at the start of the calculation (verified by a heap-allocation count assertion in CI); a `PYSCF_MAX_MEMORY` pre-flight check refuses to start a calculation that would exceed the budget rather than OOMing mid-iteration (CCSD-11, Pitfall 20 mitigation).
  3. `mycc.solve_lambda()` produces λ amplitudes for response densities (used by CCSD gradients in Phase 7); `mycc.make_rdm1()` and `mycc.make_rdm2()` match upstream (CCSD-05, CCSD-06).
  4. `mycc.direct = True` (AO-direct CCSD) works; DF-CCSD via `mf.density_fit().CCSD()` or `cc.dfccsd.RCCSD(mf)` works with bounded memory and spills `Wabef` to HDF5 when `PYSCF_MAX_MEMORY` is exceeded — proven by a benzene-dimer/cc-pVDZ DF-CCSD run on a deliberately constrained memory budget (CCSD-07, CCSD-08).
  5. T1/D1/D2 diagnostics expose `mycc.t1diagnostic()`, `mycc.d1diagnostic()` matching upstream values; frozen-core options match MP2 (`frozen=int`, `frozen=list`, `frozen='auto'`) (CCSD-09, CCSD-10).

**Plans**: 11 plans

Plans:
**Wave 1**

- [x] 06-01-PLAN.md — Crate scaffold: fill the pyscf-ccsd stub (17-module upstream-mirror skeleton + Cargo deps, no pyo3/cubecl/hdf5) + CcsdError/ChemistsEris/CcsdOverrideHooks/CcsdReference contracts + check_no_fma SCAN_TARGETS (CCSD-11)

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 06-02-PLAN.md — Tensor-arena from day one: WorkspacePool reserve/release free-list reuse + InMemory|Spilled backends + opaque Amplitudes handles (D-01/D-08) + heap-alloc-count & refusal gates (CCSD-11) (completed 2026-05-25)

**Wave 3** *(blocked on Wave 2 completion)*

- [x] 06-03-PLAN.md — In-core RCCSD headline (un-gated): rintermediates + update_amps (host-loop oracle, no gemm) + ccsd_kernel + init_amps MP2 seed + pre-flight arena + ≤1µH small-system oracle (CCSD-01/03/11) (completed 2026-05-25 — H2/STO-3G e_corr=-0.0205245 Ha, ~0.5µH vs FCI ref, RAYON 1==8 bit-identical)

**Wave 4** *(blocked on Wave 3 completion)*

- [x] 06-04-PLAN.md — In-core UCCSD (open-shell): uintermediates + uccsd_kernel + UccsdAmplitudes triple + e_aa+e_bb+e_ab + small open-shell smoke (CCSD-02) (completed 2026-05-25 — spin-orbital UCCSD; UCCSD(α==β) H2/STO-3G e_corr=-0.0205245 Ha bit-identical to 06-03 RCCSD; asymmetric α/β converges with e_aa≠e_bb; RAYON 1==8 bit-identical)
- [x] 06-05-PLAN.md — Amplitude-DIIS: AmplitudeSubspace: DiisStorable (dot via oracle_dot, byte-match amplitudes_to_vector) + Diis::new(6) wired into the kernel (D-06, Pitfall 9) (CCSD-04) (completed 2026-05-25 — H2/STO-3G converges in 8 iters with DIIS vs 12 without, same minimum within 2*CONV_TOL, e_corr=-0.020524527 matches published ref; one DIIS / second storable, no new DIIS body)

**Wave 5** *(blocked on Wave 4 completion)*

- [x] 06-06-PLAN.md — λ + RDMs incl. ao_repr (D-03): solve_lambda/update_lambda (port ccsd_lambda) + make_rdm1/make_rdm2 + the nmo⁴ AO back-transform (heaviest arena tenant) (CCSD-05/06) (completed 2026-05-25 — closed-shell solve_lambda (concrete CCSD.solve_lambda → ccsd_lambda.kernel, RESEARCH A6) converges λ on the 06-03 amplitudes; make_rdm1 Tr(γ)==nelec; make_rdm2(ao_repr=true) SHIPS the nmo⁴→nao⁴ AO back-transform numerically via pyscf-ao2mo over the heaviest arena tenant — NOT NotYetImplemented; host-loop oracle_sum, RAYON 1==8; ulambda/urdm documented open-shell deferral)
- [x] 06-07-PLAN.md — Diagnostics + frozen-core: t1/d1/d2 diagnostics (Frobenius/eigh) + the CCSD-10 frozen contract reusing the MP2 helpers (CCSD-09/10) (completed 2026-05-25 — get_t1/d1/d2_diagnostic port ccsd.py:748-776: T1=||t1||_F/sqrt(nelec) via oracle_sum, D1/D2=max sqrt(|eig|) over the ij/ab Gram blocks via eigh_gen against identity S; CCSD-10 frozen int/list/auto active space == MP2 helpers verbatim — validated on a real LiH/STO-3G CCSD run, frozen-core e_corr rises toward zero; Frozen::Auto through the CCSD helper path is element-blind == None; ccsd.rs unmodified; no new frozen logic)

**Wave 6** *(blocked on Wave 5 completion)*

- [x] 06-08-PLAN.md — AO-direct CCSD: the _contract_vvvv_t2 AO-direct branch + direct:bool flag; AO-direct == in-core e_corr equivalence, lower peak reservation (CCSD-07) (completed 2026-05-25 — contract_vvvv_t2_aodirect ports ccsd.py:473-570: Open Q4 resolved path-b, full AO int2e tiled per leading-virtual index, peak nv^3, never the full nv^4 MO vvvv; ccsd_kernel_direct routes the vvvv step + uses the lower nv^3 pre-flight; AO-direct e_corr == in-core e_corr bit-identical on LiH/STO-3G (-0.020449057574, 8 iters, ≤1e-9) + a budget the in-core path HARD-refuses but direct accepts; incidental clippy absurd_extreme_comparisons fixed)
- [x] 06-09-PLAN.md — DF-CCSD + HDF5 spill: DFRCCSD/DFUCCSD ERI swap (vvL B-tensor via the ao2mo hook) + dmax/vvblk sizing + Spilled backend (D-07, no new hdf5 dep) + the ao2mo outcore surface (CCSD-08) (completed 2026-05-25 — DfCcsdHooks::ao2mo builds the full ChemistsEris from the DF B-tensor ((pq|rs)=Σ_Q B^Q·B^Q via oracle_dot, the DFRMP2(RMP2) swap pattern) reusing the in-core ccsd_kernel verbatim; vvL spills to HDF5 via the 06-02 WorkspacePool Spilled backend (D-07) in a dedicated budget-matched pool so it's never wrongly reused for the in-core Wvvvv, RAII drop-deleted/no leftover scratch; block_sizing ports dfccsd.py:93-96 dmax/vvblk; pyscf-ao2mo gains the outcore/semi-incore HDF5-spilling AO→MO surface (D-04 deferral, bit-exact==in-core); 5 dfccsd_spill + 6 outcore always-on arms green; libxc stays 0)

**Wave 7** *(blocked on Wave 6 completion)*

- [x] 06-10-PLAN.md — PyO3 bridge (D-09): pyscf-py::cc PyRCCSD/PyUCCSD/PyDFCCSD + CcsdPyBridge (call_method1 / py.detach) + factory + scanner + solve_lambda/make_rdm + python/pyscf/cc overlay (CCSD-01/02/05/06/08) (completed 2026-05-25 — pyscf-py::cc copies pyscf-py::mp section-for-section: eager-snapshot CcsdReference, is_overridden __qualname__ MRO dispatch of the 5-hook set (ao2mo/update_amps/make_rdm1/make_rdm2/energy) via call_method1, each default under py.detach (BIND-05); the kernel does NOT py.detach at the top (hooks re-enter Python, mp.rs:359 — the update_amps default is the biggest py.detach region in the project); solve_lambda/make_rdm1/make_rdm2/as_scanner exposed; PyCcsdScanner Mole->energy callable (CCSD-07) + self-less ScannerDfBridge for the DF re-run; ccsd_factory UHF->UCCSD/with_df->DFCCSD/else RCCSD; python/pyscf/cc/__init__.py re-exports _native.cc + grafts mf.CCSD()/mf.density_fit().CCSD() onto the Rust SCF base classes; pyscf-ccsd stays pyo3-free; 6 always-on cc_bridge arms green; cargo check -p pyscf-py + -p pyscf-ccsd pass; libxc NEVER compiled; live numeric + python3.13t GIL smoke deferred to 06-11)

**Wave 8** *(blocked on Wave 7 completion)*

- [x] 06-11-PLAN.md — Oracle/CI close-out: pyscf-oracle CCSD fixtures (small always-on; caffeine/DF-spill/λ-RDM byte-identity gated) + ccsd-structural + heap-alloc-count + ccsd-oracle-upstream-manual + python3.13t CCSD smoke arms (D-04) (CCSD-01/02/05/06/08/11) (completed 2026-05-25 — 6 CCSD oracle method names registered in KNOWN_METHODS (24 total) + ccsd_oracle.rs with always-on dispatch-layer arms + #[cfg(feature=python)]/#[ignore]'d byte-identity arms (mirrors the MP2 register-but-defer-dispatch precedent); 4 CI arms: ccsd-structural + ccsd-heap-alloc-count ALWAYS-ON (scoped -p pyscf-ccsd/-p pyscf-oracle, no python/libxc), ccsd-oracle-upstream-manual + python313t-ccsd-smoke workflow_dispatch ONLY (caffeine/DF-spill on constrained PYSCF_MAX_MEMORY=500/λ/RDM byte-identity + mf.CCSD().run() GIL probe); YAML_OK; default cargo test -p pyscf-oracle pulls NO libxc (ORACLE_NO_LIBXC); 06-VALIDATION nyquist_compliant: true — phase Nyquist contract closed; live byte-identity + 3.13t GIL = the documented phase-gate human-verify run)

### Phase 7: Gradients + Geomopt

**Goal**: A user runs `mf.nuc_grad_method().kernel()` for any in-scope method (HF/DFT/MP2/CCSD) and gets upstream-matching analytical gradients; runs `pyscf.geomopt.optimize(mf)` (or the geomeTRIC/berny shims) and converges to the same stationary point as upstream within chemical accuracy — without a Python `geomeTRIC` or `pyberny` runtime dependency, because the optimizer is native Rust BFGS+RFO.
**Depends on**: Phase 6 (CCSD gradients need Λ-equations from `mycc.solve_lambda()`)
**Requirements**: GRAD-01, GRAD-02, GRAD-03, GRAD-04, GRAD-05, GRAD-06, GRAD-07, GRAD-08, GRAD-09, GRAD-10, GEOMOPT-01, GEOMOPT-02, GEOMOPT-03, GEOMOPT-04, GEOMOPT-05, GEOMOPT-06, GEOMOPT-07
**Success Criteria** (what must be TRUE):

  1. `mf.nuc_grad_method().kernel()` returns analytical gradients matching upstream to ≤1e-7 Hartree/Bohr for RHF, UHF, RKS (with `grid_response=True`), UKS, MP2 (Z-vector/CPHF), CCSD (Λ-equations), and ECP gradients on the test corpus (GRAD-01..07); atom-list subsetting via `grad.kernel(atmlst=[1,2,3])` returns just those rows of the gradient (GRAD-08).
  2. Every analytical gradient passes a finite-difference verification mode `grad.verify_fd(disp=1e-4)` to within 1e-6 Hartree/Bohr — this gates unit tests (GRAD-09).
  3. The CPHF/CPKS solver lives in `pyscf-grad` (or a shared module) and is reused by every method gradient that needs response equations (RKS-grad with `grid_response`, MP2-grad Z-vector, CCSD-grad Λ); a single CI test confirms there is one CPHF implementation, not N (GRAD-10).
  4. `pyscf.geomopt.optimize(mf)` runs a native Rust BFGS+RFO optimizer in redundant internal coordinates with **no** Python `geomeTRIC` or `pyberny` runtime dependency (verified by `pip uninstall geomeTRIC pyberny && python -c "import pyscf.geomopt; pyscf.geomopt.optimize(mf)"` succeeding) (GEOMOPT-01); `pyscf.geomopt.geometric_solver.optimize(mf)` and `pyscf.geomopt.berny_solver.optimize(mf)` are drop-in shims that delegate to the native optimizer, preserving the canonical PySCF import paths (GEOMOPT-02, GEOMOPT-03).
  5. Default convergence thresholds match geomeTRIC defaults (`gradient`, `displacement`, `energy`, `gradient_max`, `displacement_max`); Wilson B-matrix construction for redundant internals and RFO step with negative-eigenvalue tracking are ported from upstream/geomeTRIC; HDF5 checkpoint of optimizer state allows resuming a partially-converged optimization; optimization trajectories on the test corpus converge to the same stationary point as upstream within chemical accuracy (GEOMOPT-04..07).

**Plans**: 10 plans

Plans:
**Wave 1**

- [x] 07-01-PLAN.md — Wave-0 cintx grad-intor buy-down + gto guard removal / ECP-grad wiring (D-02) — GRAD-07 (2/8 cintx-ready families un-gated; 6/8 gated)
- [x] 07-02-PLAN.md — pyscf-grad skeleton + base Gradients trait + atmlst + verify_fd FD harness (D-01/D-09) — GRAD-08, GRAD-09

**Wave 2** *(blocked on Wave 1 completion)*

- [x] 07-03-PLAN.md — RHF analytical gradient (headline, FD-gated) (GRAD-01)

**Wave 3** *(blocked on Wave 2 completion)*

- [x] 07-04-PLAN.md — native BFGS+RFO redundant-internal optimizer on the RHF scanner loop, early (D-08; GEOMOPT-04/06/07)

**Wave 4** *(blocked on Wave 3 completion)*

- [x] 07-05-PLAN.md — UHF + RKS(+grid_response) + UKS gradients, no CPHF (GRAD-02/03/04, D-04)
- [x] 07-06-PLAN.md — geomopt HDF5 checkpoint + geometric_solver/berny_solver shims + constraints error (GEOMOPT-02/03/05, D-07)

**Wave 5** *(blocked on Wave 4 completion)*

- [x] 07-07-PLAN.md — single matrix-free Krylov CPHF (D-03) + MP2-grad Z-vector (GRAD-05/10)

**Wave 6** *(blocked on Wave 5 completion)*

- [x] 07-08-PLAN.md — CCSD-grad (consumes Phase-6 λ + RDMs) + ECP-grad (GRAD-06/07, D-04)

**Wave 7** *(blocked on Wave 6 completion)*

- [x] 07-09-PLAN.md — PyO3 bridge: mf.nuc_grad_method + geomopt submodule + python overlays (GEOMOPT-01/02/03)

**Wave 8** *(blocked on Wave 7 completion)*

- [x] 07-10-PLAN.md — pyscf-oracle grad fixtures + CI close-out (FD always-on + pip-uninstall proof + workflow_dispatch arms)

### Phase 8: GPU enable + Oracle hardening + Distribution

**Goal**: A Python user on a fresh container runs `pip install pyscf-rs[cuda]` and `python -c "from pyscf import gto, scf, dft, mp, cc, grad, geomopt"` and every one of the top-20 drop-in idioms succeeds; the 2–5× speedup claim against current PySCF + C extensions is proven on a defined benchmark suite; ≥80% of curated upstream PySCF unit tests for in-scope modules pass when run against pyscf-rs as the import target.
**Depends on**: Phase 7 (every CPU path must be correct on the oracle before GPU drift is introduced)
**Requirements**: PERF-01, PERF-02, PERF-03, PERF-04, PERF-05, PERF-06, PERF-07, DIST-01, DIST-02, DIST-03, DIST-04, DIST-05, DIST-06, ORACLE-03, ORACLE-04, ORACLE-06, ORACLE-07, BIND-03, BIND-08
**Success Criteria** (what must be TRUE):

  1. **GPU backends enabled and correct**: per-backend regression suite runs the full SCF/DFT/MP2/CCSD test corpus on CPU SIMD, CUDA, WGPU, and ROCm by setting `PYSCF_BACKEND` (no recompile per backend on a `--features gpu` build) — exercising the Phase 1 `pyscf-algebra` dispatch end-to-end (ALG-04, ALG-07); where hardware is available in CI, GPU backends pass at chemical accuracy with documented per-backend tolerance (CPU: bit-exact under oracle profile; CUDA: 1e-10 Hartree energy / 1e-8 gradient; WGPU: chemical accuracy 1e-6; ROCm: 1e-10) (ORACLE-07); cubecl autotune cache ships at `CUBECL_CACHE_DIR` so first-run overhead does not regress the benchmark (PERF-06); adaptive backend dispatch falls back to CPU when `nao < 200` to avoid GPU launch overhead (PERF-07, Pitfall 19 mitigation).
  2. **2–5× speedup claim proven**: criterion-based `pyscf-bench` crate covers RHF, RKS, MP2, CCSD on H2O/cc-pVDZ, benzene/6-31G*, 20-water cluster/cc-pVDZ, alanine dipeptide/def2-SVP, caffeine/cc-pVDZ; pyscf-rs achieves **≥2× speedup** vs current PySCF + C extensions on this suite on a fair-comparison machine (same CPU, same thread count, no GPU); **stretch**: ≥5× on at least one benchmark; CUDA backend demonstrates additional speedup on caffeine and alanine dipeptide; `mol.build()` is sub-second for 5000-AO molecules (PERF-01..05).
  3. **Drop-in audit passes**: the top-20 idioms from BIND-03 (from `pyscf.M(...)` through `mol.dumps()`/`gto.Mole.loads(s)`) all run unchanged against pyscf-rs as the import target on a representative existing PySCF user script; ≥80% of curated upstream PySCF unit tests for in-scope modules pass against pyscf-rs (ORACLE-04); nightly per-basis bit-exact sweep covers every basis-set name PySCF knows (ORACLE-06); test isolation uses subprocess-per-fixture for fixtures that mutate global state, persistent worker for stateless ones (ORACLE-03, Pitfall 16 mitigation) (BIND-03).
  4. **Wheel ships**: `pyscf-rs` published on crates.io with the workspace façade re-exporting in-scope methods (DIST-01); abi3-py310 PyPI wheel installs cleanly on Linux/macOS/Windows × x86_64 + macOS aarch64; `pip install pyscf-rs && python -c "from pyscf import gto, scf"` succeeds in a fresh container (DIST-02); per-backend optional extras `pyscf-rs[cuda]`/`pyscf-rs[wgpu]`/`pyscf-rs[rocm]` keep the base wheel under the PyPI 60 MB ceiling (DIST-03, Pitfall 13 mitigation); manylinux_2_28 baseline; `auditwheel show` clean (DIST-04); HDF5 ships statically linked via `hdf5-sys/static`, no system libhdf5 required (DIST-05); `python/pyscf/__init__.py` import shim makes `import pyscf` Just Work (DIST-06); `abi3audit` runs in CI on the produced wheel and fails on non-abi3 symbols (BIND-08).

**Plans**: TBD

## Progress

**Execution Order:**
Phases execute in numeric order: 1 → 2 → 3 → 4 → 5 → 6 → 7 → 8

| Phase | Plans Complete | Status | Completed |
|-------|----------------|--------|-----------|
| 1. Foundation | 9/9 | Complete    | 2026-05-23 |
| 2. GTO | 11/11 | Complete    | 2026-05-23 |
| 3. SCF + PyO3 bindings | 15/15 | Complete    | 2026-05-24 |
| 4. DFT | 14/14 | Complete    | 2026-05-23 |
| 5. MP2 | 7/7 | Complete    | 2026-05-23 |
| 6. CCSD | 11/11 | Complete    | 2026-05-25 |
| 7. Gradients + Geomopt | 10/10 | Complete    | 2026-05-26 |
| 8. GPU enable + Oracle hardening + Distribution | 0/TBD | Not started | - |

## Coverage Summary

All 121 v1 REQ-IDs are mapped to exactly one phase. No orphans. No duplicates.

| Category | Count | Phase(s) |
|----------|-------|----------|
| FOUND-01..10 | 10 | Phase 1 |
| ALG-01..08 | 8 | Phase 1 |
| GTO-01..11 | 11 | Phase 2 |
| SCF-01..14 | 14 | Phase 3 |
| DFT-01..11 | 11 | Phase 4 |
| MP2-01..08 | 8 | Phase 5 |
| CCSD-01..11 | 11 | Phase 6 |
| GRAD-01..10 | 10 | Phase 7 |
| GEOMOPT-01..07 | 7 | Phase 7 |
| BIND-01..09 | 9 | split: BIND-01,02,04,05,06,07,09 → Phase 3; BIND-03,08 → Phase 8 |
| ORACLE-01..09 | 9 | split: ORACLE-01,05,09 → Phase 1; ORACLE-02,08 → Phase 3; ORACLE-03,04,06,07 → Phase 8 |
| PERF-01..07 | 7 | Phase 8 |
| DIST-01..06 | 6 | Phase 8 |
| **Total** | **121** | **8 phases** |

## Cross-Cutting Concerns Threaded Through Every Phase

These are NOT separate phases — they are conventions established in Phase 1 + Phase 3 that every later phase re-validates as part of its success criteria:

- **Algebra-responsibility wall (`pyscf-algebra` is the only `cubecl-*` consumer)**: Phase 1 establishes (single-owner crate + dependency-graph lint); every method phase consumes algebra primitives only via `pyscf-algebra::{gemm,reduce_sum,axpy,dot,…}` and never imports `cubecl-*` directly. Reference: `docs/manual/Cubecl/`.
- **Backend selection at runtime via `PYSCF_BACKEND` (CPU-default)**: Phase 1 establishes (`select_backend()` resolver + workspace `gpu` feature OFF by default); every PyO3 entry point logs the resolved backend (ALG-08); Phase 8 GPU enable phase exercises the priority chain across CUDA/WGPU/ROCm hardware.
- **Bit-exact-with-PySCF**: Phase 1 establishes (`release-oracle` profile, `oracle_sum`/`oracle_dot`); every method phase asserts on the test corpus.
- **PyO3 subclass-override dispatch**: Phase 3 establishes (`slf.call_method1`); Phase 4 (DFT) re-asserts on the larger DFT overrideable surface; Phases 5/6/7 inherit by convention.
- **NumPy contiguity**: Phase 3 establishes (`to_owned()` on non-standard-layout); every PyO3 entry point in Phases 4–7 reuses the helper.
- **GIL-release seam**: Phase 3 establishes (`Python::detach` on long compute); Phase 6 (CCSD) is the heaviest re-validation under `python3.13t`.
- **Panic policy**: Phase 1 establishes (`catch_unwind`, no `unwrap()` in numerical code, `panic="abort"` release); every phase inherits via the workspace clippy config.
- **Scope-creep lint**: Phase 1 establishes (`forbidden-paths`); every PR in every phase passes through it.
- **cubecl pin lockstep with cintx/libxc_rs/xcfun_rs**: Phase 1 establishes; Phase 8 GPU enable phase is where a sibling-crate cubecl bump would force the most rework, so CONTRIBUTING.md upgrade ritual is documented in Phase 1.

## Pitfall-to-Phase Mapping (v1 — derived from research/PITFALLS.md)

| # | Pitfall | Severity | Primary Phase | Re-validated In |
|---|---------|----------|---------------|------------------|
| 1 | FMA contraction | SHOWSTOPPER | Phase 1 | every method phase |
| 2 | Parallel-reduction order | SHOWSTOPPER | Phase 1 | every method phase |
| 3 | cubecl pre-1.0 / WGPU f64 | MAJOR | Phase 1 | Phase 4 (DFT WGPU gating), Phase 8 |
| 4 | Eigenvector sign | SHOWSTOPPER | Phase 3 (canonicalize_signs) | Phases 5/6/7 |
| 5 | NumPy zero-copy | MAJOR | Phase 3 | every PyO3 entry point |
| 6 | GIL deadlock | MAJOR | Phase 3 | Phase 6 (CCSD heaviest), Phase 8 (3.13t CI) |
| 7 | PyO3 subclass override | SHOWSTOPPER | Phase 3 | Phase 4 (DFT large surface) |
| 8 | Loop / F-order layout | MAJOR | Phase 2 + Phase 3 | Phase 7 grad |
| 9 | DIIS path drift | MAJOR | Phase 3 | Phase 6 (amplitude DIIS) |
| 10 | DFT grid weights | MAJOR | Phase 4 | — |
| 11 | chkfile compatibility | SHOWSTOPPER | Phase 3 | every method phase via ORACLE-08 round-trip |
| 12 | Cross-platform libm | MAJOR | Phase 1 | Phase 3 (cross-platform µHartree assertion) |
| 13 | Wheel size / CUDA dist | MAJOR | Phase 8 | — |
| 14 | Panic across FFI | SHOWSTOPPER | Phase 1 (catch_unwind lint) | Phase 3 (panic→exception test) |
| 15 | Sibling-crate cubecl drift | SHOWSTOPPER | Phase 1 (nightly matrix CI) | Phase 8 |
| 16 | Test-oracle global state | MAJOR | Phase 8 (subprocess-per-fixture) | — |
| 17 | Off-by-one basis indexing | MAJOR | Phase 2 | Phase 8 nightly per-basis |
| 18 | Boys-function accuracy | MAJOR | Phase 2 (delegated to cintx) | — |
| 19 | GPU launch overhead small mol | MAJOR | Phase 8 (adaptive dispatch) | — |
| 20 | CCSD memory thrash | MAJOR | Phase 6 (tensor-arena from day one) | — |
| 21 | Scope creep | MAJOR | Phase 1 (forbidden-paths) | every PR in every phase |

## v2.0 Milestone — Periodic Boundary Conditions (Phases 9–20)

**Added:** 2026-08-22. **Detailed plan:** `.planning/pbc/PBC-MASTER-PLAN.md`.
**Per-module mapping:** `.planning/pbc/PBC-DRIVER-INVENTORY.md`.
**Scope:** all 186 modules / ~78,000 lines under `pyscf/pbc/` (see PROJECT.md "Current Milestone").
**Workspace growth:** 20 → 39 crates (19 new `pyscf-pbc-*` members).

- [x] **Phase 9: PBC Foundation + Complex Algebra** *(COMPLETE 2026-08-26 — see `.planning/phases/09-pbc-foundation/09-VERIFICATION.md`; gate met: `cell.ewald()` matches upstream to <1e-9 Ha on diamond/Si/LiF/He-fcc, `zeigh_gen` matches faer to <1e-12)* — 19 crate stubs, complex algebra in `pyscf-algebra` (`CTensor`, `zgemm`, `zeigh_gen`, `oracle_zsum`), `Cell` (owns `Mole` via `Deref`), reciprocal lattice, `Gv`/`SI` cubecl kernels, cutoff↔mesh, lattice `Ls`, k-point meshes, Ewald summation, supercell builders, forbidden-paths lint un-gate. **Gate:** `cell.ewald()` matches upstream to 1e-9 Ha; `zeigh_gen` matches faer to 1e-12.
- [x] **Phase 10: Periodic integrals + GTH pseudopotentials** *(COMPLETE 2026-08-26 — see `.planning/phases/10-periodic-integrals/10-VERIFICATION.md`; gate met with a 77x margin: `pbc_intor('int1e_ovlp', make_kpts([2,2,2]))` matches upstream PySCF 2.12.1 to **1.29e-14** on diamond, element-by-element over all 8 k-points)* — `pbc_intor`/`intor_cross` lattice-sum 1e integrals with Bloch phases (image-expansion route over `cintx` shell pairs, K-07 `bloch_phase`), neighbor list + screening, GTH PP data model with PP-adjusted `atom_charges`, `vloc` G-space factors (`get_gth_vlocG{,_part1}`, `get_alphas`, `get_coulG`) + the real-space `get_pp_loc_part2` 3-centre double lattice sum, `vnl` (`get_pp_nl`, matches upstream to 1.9e-15), periodic `eval_ao_kpts` (K-08, Bloch-periodicity gate), `get_ovlp`/`get_t`/`get_hcore_parts`. **cintx prerequisite RESOLVED:** Wave 0.5 has landed; the 10 moment-weighted families evaluate correctly behind `unstable-source-api` (this crate's default-on `gth-pp` feature). **A second cintx bug was found here and fixed upstream the same day:** both families mishandled GENERAL CONTRACTIONS (`nctr > 1`) — `origi` returned silently-wrong values, `origk` panicked in `c2s.rs:684` — which had blocked `get_pp_loc_part2` for LiF (`Li`/`gth-szv` is the sole general contraction among the §9.2 elements). cintx fixed it; all seven symbols now match libcint to <= 2.7e-15 on a general contraction (pinned by `origi/origk_matches_libcint_for_general_contractions`), the interim guard is deleted, and the LiF gate passes at **1.53e-10**. **No blockers remain.** `get_hcore` itself stays `NotYetImplemented{phase:11}`: the long-range local term needs FFT/AFT (upstream's own `get_pp_loc_part1` raises `NotImplementedError`).
- [x] **Phase 11: FFT + FFTDF + periodic HF** *(COMPLETE 2026-08-26 — see `.planning/phases/11-fft-fftdf-periodic-hf/11-VERIFICATION.md`)* — complex 3-D FFT (upstream's `_fftn_blas` GEMM engine as the reference + a host mixed-radix/Bluestein engine as the default, pinned to each other at **1e-13** over 200 random meshes, D-PBC-06), `get_coulG` (every 3D/2D/0D branch + range separation)/`madelung`/exxdiv, `fft_jk` J and K builds, `FFTDF`, `KSCF` driver (BZ-global aufbau, k-stacked DIIS), `KRHF`/`KUHF`/`KROHF`/`KGHF` + gamma-point variants, smearing, addons, chkfile. **Gate met:** `KRHF(diamond, 2×2×2)` matches live upstream PySCF **2.12.1** to **4.0e-12 Ha** (3.7e-13 relative) at mesh 31 AND at the default mesh 47; the ALL-ELECTRON reference (`KRHF`/`KUHF` on He-fcc) meets **1e-12 outright at 2.2e-13**. The pseudopotential residual is upstream's own accumulation, not this port's: upstream's `fft.get_pp` builds the non-local half by summing ~1e5 planewaves through `ft_ao` (~1e-13 roundoff per matrix element), while this port uses Phase 10's exact real-space `get_pp_nl` — the two agree to 1.1e-13 element-wise at every converged mesh, and 64 elements x 8 k-points of that is the observed 4e-12. Closing it exactly means reproducing `ft_ao`, which is Phase 13. The **supercell-equivalence identity holds oracle-free at 1.6e-10** and caught a live Phase-10 defect on the way: `super_cell` built its `Mole` through the MOLECULAR `build_from` and so never re-applied the GTH valence-charge rewrite, making every supercell of a pseudopotential cell an all-electron system (diamond: `atom_charges` `[6,6,6,6]` instead of `[4,4,4,4]`). Fixed. **One upstream-version trap is now pinned:** PySCF **2.14** rewrote `fft_jk.get_k_kpts` to fold the `exxdiv='ewald'` correction into `get_coulG` instead of applying `_ewald_exxdiv_for_G0` analytically (a 1.7e-5 difference in `vk`), so every oracle test asserts `pyscf.__version__ == '2.12.1'` and pins `PYTHONPATH` to the vendored tree.
- [x] **Phase 12: Periodic DFT** *(COMPLETE 2026-08-27, gate re-measured 2026-08-28 against a default-configured upstream — see `.planning/phases/12-periodic-dft/12-VERIFICATION.md`)* — periodic `NumInt` (`eval_ao_kpts` block loop, complex `eval_rho`, `nr_rks`/`nr_uks`/fxc), uniform + Becke periodic grids, `KRKS`/`KUKS`/`KROKS`/`KGKS` + gamma variants, DFT+U, `numint2c`, cDFT, and the `dimension ∈ {0,1,2}` low-dimension closure (plan 12-08: `inf_vacuum` Gauss-Chebyshev `Gv` base, the 2-D Sundararaman-Arias Ewald sum, `vcut_sph`/`vcut_ws`). **Gate met:** `KRKS(Si, 2×2×2, PBE)` matches live upstream PySCF **2.12.1**, both sides on their DEFAULT XC library (libxc — no `_numint.libxc` override, §1e), to **6.45e-12 Ha**, sitting on the **4.16e-12 pseudopotential floor** the `KRHF` no-XC control measures on the identical cell; the ALL-ELECTRON reference (`KRKS` on He-fcc/`sto-3g`) meets **1e-12 outright at 9.81e-14**. The 2-D Ewald branch hits its **Phase-9-pre-recorded** graphene target at **2.84e-14**. **A 1e-15 Ha gate was considered and is not reachable:** one f64 ulp at `|E| ≈ 7.79` is 8.88e-16, so 1e-15 is ~1.1 ulp — it demands bit-identical arithmetic to numpy/FFTW/LAPACK through a whole SCF, while the best result anywhere in this port is 221 ulp (§1d). **Three defects were found and fixed OUTSIDE this phase**, all live in Phase-4 code: the closed-shell `vsigma` returned `∂f/∂γ_aa` instead of `∂f/∂σ` (a factor of 4.7 on PBE), the GGA back-contraction halved the gradient rows that upstream leaves alone (4.8% error in molecular GGA `V_xc`), and MINAO could not be loaded at all. **The Phase-12 source had never been compiled**: `lib.rs` declared only `mod error` and there were zero tests — see §6, and the new `xtask check-orphan-modules` lint that now makes that state a CI failure. **2026-08-28: `XcBackend`'s default flipped from xcfun to libxc**, matching upstream, after `libxc_rs` (an external dependency) was fixed across five rounds of remediation to evaluate the full functional corpus bit-exactly against C libxc (≤2.14e-16). This closed the 4.71e-7 Ha libxc/xcfun library gap the original gate had to work around; §1e has the full history.
- [ ] **Phase 13: ft_ao + AFTDF** *(IMPLEMENTED 2026-08-29 for plans 13-01…13-05 + 13-07; 13-06 NOT shipped — see `.planning/phases/13-ft-ao-aftdf/13-VERIFICATION.md`)* — K-15 `ft_aopair` McMurchie–Davidson cubecl kernel (concrete `f64`: `cube_math::double` IS the f64 libm, as in every sibling PBC kernel), host MD recursion, `estimate_rcut` + upstream's three screens, single-centre `ft_ao` + `_fake_nuc`, `FtKernel`, `AFTDF` (`ft_loop`/`weighted_coulG`/`get_nuc`/`get_pp`), `aft_jk`, and the `Box<dyn PeriodicDf>` refactor across all 8 drivers. **Gate 1 (no oracle) MET, in three parts** — the flat 1e-10 was unmeasured and unreachable: `ft_ao.estimate_rcut` (20.420 Bohr) is LOOSER than `cell.rcut` (21.319), so upstream's own `ft_aopair[G=0]` misses `int1e_ovlp` by 1.554e-9, and past `rcut` ×1.5 the FT sum is converged (×2.0 identical to four digits) while the residual sticks at 1.472e-10 because it is then `pbc_intor`'s OWN truncation. So: **1a ≤2e-9** vs `pbc_intor` at `estimate_rcut`, **1b ≤2e-10** at 1.5×`cell.rcut`, **1c ≤1e-13** vs `intor_cross_with_images` over the SAME image list — 1c passes on diamond, He-fcc and all 8 k-points and is the real gate on the algebra. **Gate 2 restated as a `(rcut, mesh)` ladder** — upstream measured: the KRHF energy difference plateaus at **2.607e-11 Ha**, BIT-IDENTICAL at mesh 31 and 41 (all 14 digits), and separately plateaus at `rcut` ×1.5; two independent floors, so both "1e-13" and any monotone-in-mesh restatement would fail a CORRECT implementation. Also measured: with `exxdiv='ewald'` the R-15 G=0 asymmetry is ~96% of `dvk` at mesh 31, yet the ENERGY floors with `dvj` — the G=0 term is a near-uniform shift that cancels in `Tr(D·vk)`, so matrices and energies need DIFFERENT tolerances. **Gate 2 MET in the port:** `KRHF` on diamond at gamma, both builders, `conv_tol=1e-11` — 9.309e-5 → 5.066e-8 → **2.378e-10** over meshes 15/21/27, the same ~1000×-per-6-mesh rate as upstream. **Gate 3 MET for 4 of 5 quantities:** `get_nuc` **2.755e-12**, `vj` **3.733e-12**, `vk` **2.116e-12**, `get_eri` **4.172e-12** — all beating the 1e-11 bar, which disproves the obvious hypothesis that `ft_aopair`'s screening residual propagates broadly. `get_pp` is the exception at 1.806e-9, and **the cause is upstream, not the port**: `aft.get_pp` builds part 2 with `_IntPPBuilder` while `pp_int.get_pp_loc_part2` is the reference route Phase 10 ported and `fft.get_pp` agrees with, and **those two upstream routes disagree with EACH OTHER by 1.7933e-9** — 99.3% of the gap. Substituting the `pp_int` route into upstream's own `AFTDF.get_pp` collapses the deviation to **3.982e-11** (45×), and that substitution is asserted in the test suite so the attribution cannot rot. `ft_aopair`'s own 5.121e-10 vs upstream is separate and is screening (three upstream screens ported: `strip_basis`, `get_ovlp_mask` over the `_RangeSeparatedCell` per-primitive grouping, libcint `PTR_EXPCUTOFF`, taking it 1.553e-9 → 5.121e-10); oracle-free evidence it is truncation and not algebra is Gate 1c at 1e-13 plus the `get_pp` anti-Hermitian residue falling from 5.133e-11 at upstream `rcut` to **2.665e-15** at a converged one. **Four defects caught by the phase's own tests** (a per-record screen reapplied at the image-list threshold, worth 1.66e-7; `estimate_rcut`'s `cs` being the libcint coefficient rather than `gto_norm`; `Γ(1.5)` returning 1; Phase-10's F-order outputs added raw). **13-06 partially shipped:** `get_eri` + `get_ao_pairs_G` for both builders through one shared contraction (oracle 4.172e-12; AFT-vs-FFT converges 4.250e-5 → 5.427e-10 over meshes 11/21; 8-fold symmetry to 1.966e-12), but NOT `general`/`get_mo_pairs_G`/`ao2mo_7d` — `ao2mo_7d`'s index order is a contract with Phase 15's KMP2 and was deliberately not guessed. **Carry-overs:** `ao2mo_7d` still blocks Phase 15; the two Gate-3 residuals both land naturally in Phase 14 (`gdf_builder` needs the `ExtendedMole` machinery, and `incore.aux_e2` IS `_IntPPBuilder`); `kk_adapted_iter`/`group_by_conj_pairs` still unported; AFTDF band k-points deferred. **Worth reporting upstream:** `pp_int.get_pp_loc_part2` and `_IntPPBuilder.get_pp_loc_part2` disagree by 1.79e-9 in PySCF 2.12.1.
- [x] **Phase 14: GDF / MDF / RSDF / RSJK** *(CLOSED 2026-08-29; Gate 3 MET 2026-08-30 — see `.planning/phases/14-gdf-mdf-rsdf-rsjk/14-VERIFICATION.md` §5. 14-01 … 14-06 SHIPPED; 14-07 7a + 7b + 7c-partial SHIPPED on D-PBC-24, 7d held; 14-08 Tasks 2-partial and 3 shipped, Task 2-builder shipped as `Rsdf`, Task 4 (`rsjk`) still unwritten)* — `incore` 3-centre double lattice sum, `gdf_builder` compensating charge, `GDF` + HDF5 `_cderi`, `df_jk`, `df_ao2mo`, `outcore`, `MDF`, the ω machinery of `rsdf_builder`, `get_aux_chg`, one shared `density_fit`. ~~**Gate:** every DF builder gives the same KRHF energy to 1e-15 with GDF under 20% of FFTDF memory.~~ **THAT GATE WAS WRONG IN BOTH HALVES AND IS REPLACED BY THE FIVE BELOW** (`14-CONTEXT.md`; the corrections were measured BEFORE implementation, `measurements/README.md`). GDF is an *approximation*: `|E_FFTDF − E_GDF|` on diamond `gth-szv` 2×2×2 is **1.222e-03 Ha**, the DF **fitting error**, a property of the auxiliary basis, present in upstream and reachable by no implementation; upstream's OWN two GDF builders disagree by up to **4.502e-06**; and one f64 ulp at |E| ≈ 10.9 is 1.78e-15, so "1e-15" is under one ulp of the compared quantity. The memory half is **k-mesh dependent** — `_cderi` is O(nkpts²) against the AO table's O(nkpts) — so upstream itself is at 6.17 % (2×2×2) and 20.95 % (3×3×3) at mesh [40,40,40]. **THE FIVE GATES, AS MEASURED:** **Gate 1 (MET)** — the algebra, on the ALL-ELECTRON He-fcc/`sto-3g` 2×2×2 control where D-PBC-23's `exclude_dd_block` deferral is provably inert (measured **exactly 0**): converged `KRHF` on GDF **−2.80842508692377** vs upstream **−2.80842508664874**, |dE| = **2.750e-10**; `fuse(j3c)` **1.412e-12**; `j2c` **7.105e-14**; `df_ao2mo.get_eri` **1.667e-12**; `ao2mo_7d` **1.984e-12**; `KRHF` on MDF **2.827e-10**. The DF fitting error is reproduced and ASSERTED (6.0023e-05 vs upstream 6.006e-05) so nobody can "fix" it. **Gate 1b (PARTIAL)** — diamond's `make_j3c` is a single screening group of ~77 M cintx shell triples (a release run showed `user 280m59s` / `real 22m20s`, terminated before finishing), so its wall time is **unmeasured** and the oracle is an `#[ignore]`d acceptance run. Everything on diamond that needs no 3-centre build IS gated at ≤1e-11. **Gate 2 (MET)** — MDF converges to FFTDF and GDF does not: 6.002e-05 (GDF) → **1.695e-06** (mesh 7) → **3.433e-09** (mesh 11) → **3.245e-08** (mesh 15), on upstream's CC ladder to within 1 %, **including the non-monotone bounce** (two floors: MDF's own fit and the mesh-31 FFTDF reference's truncation — a monotone gate would fail a correct implementation). Note `measurements/mdfladder.out` measured the WRONG builder (`MDF._prefer_ccdf` is `False`, so it is `_RSMDFBuilder`); `mdfladder_cc.out` was added. **Gate 3 (was UNREACHABLE at close; MET 2026-08-30)** — `|E(GDF) − E(RSDF)|` needed RSDF, and RSDF was blocked on a `cintx` gap, not on this port: range separation is libcint's `PTR_RANGE_OMEGA` (`env[8]`) toggle around the STANDARD `int3c2e`/`int2c2e`/`int2e`, and cintx's safe API had no `range_omega`. `14-07-PLAN.md` Task 7b named that failure mode in advance and required it be REPORTED rather than worked around with a numerically different kernel, which is what the phase did. **D-PBC-24 then closed the gap in cintx** (`ExecutionOptions::range_omega`, part of the WORKSPACE query because short range doubles the Rys roots), and the second obstruction recorded here — that the periodic 3-centre driver builds its `BasisSet` without an `_env`, so `range_coulomb.rs`'s workaround was unreachable — **turned out never to matter: ω rides in the OPTIONS, not in the basis.** Plan 14-07 sub-tasks 7b/7c then ported `_RSGDFBuilder` as `rsdf_builder::{j2c, RsGdfBuilder}` + `gdf_builder::j3c::Scheme::RangeSeparated` — one fitting pipeline, three schemes. **MEASURED** vs vendored 2.12.1, two systems: He-fcc `sto-3g` 2×2×2 — **RSDF 2.325e-10** (−2.80842508693849 vs −2.80842508717097), **GDF 2.750e-10**; diamond `gth-szv` gamma — **RSDF 1.615e-8**, **GDF 2.074e-8** (the GTH floor). Gated by `crates/pyscf-pbc-scf/tests/gate3_rsdf.rs`. **The ORIGINAL criterion — the port's `|E(CC) − E(RS)|` landing on upstream's own gap within 2× — IS MET, on diamond: 4.465597e-6 against 4.502481e-6, ratio 0.9918.** Two independent implementations reproducing upstream's inter-route disagreement to under 1 % is the strongest single piece of evidence in the phase. It does NOT discriminate on He-fcc (ratio 0.028), and that is not a defect: upstream's two routes differ there almost entirely through `exclude_d_aux`/`exclude_dd_block`, which this port has in NEITHER route, so removing them from both leaves the two converging to the same quantity. The gate therefore asserts per-route agreement on both systems and REPORTS the ratio; gating on the ratio alone would have produced a false negative on the all-electron control. `_RSNucBuilder`'s absence does not show at 1e-8 even on the pseudopotential cell — RSDF's diamond error is SMALLER than GDF's. **One deliberate divergence, priced**: `_guess_omega` takes the ORBITAL cell here where upstream passes the auxcell — upstream affords the coarser `[7,7,7]` because its two splits route around the grid what this port must resolve on it; `[7,7,7]` gives 8.670e-7, `[11,11,11]` gives 1.97e-10, and `[11,11,11]` is what `measurements/omega.out` already recorded. **One real bug caught**: the SR 2-centre lattice sum was truncated because `auxcell.rcut` is an ORBITAL radius while the metric sums a two-centre COULOMB interaction `erfc(ωR)/R` — upstream sets `estimate_rs_2c2e_rcut` at `rsdf_builder.py:274`; without it the metric was 1.25e-4 off, worth 8.57e-5 Ha. **ALSO SHIPPED on the same foundation**: `_RSMDFBuilder` (`mdf.py:238-353`, expressed as a `mixed` flag on the RS builder rather than a subclass) gated **at matched meshes** at **3.209e-10 / 1.897e-11 / 7.808e-12** (meshes 11/15/21) against upstream's `df.MDF()` default — two bugs caught there: `_RSMDFBuilder` ends with `weighted_coulG = MDF.weighted_coulG`, so its SHORT-range kernel also carries MDF's `±Gmax±0.5` edge screen (worth **1.176e-4 Ha**; it fires at every k-difference on a 2×2×2 mesh), and `mdf.py:265`'s looser `estimate_rs_2c2e_rcut` precision measured WORSE than `rsdf_builder.py:274`'s, so both keep the tighter one. **For MDF the mesh is DEFINITIONAL** — the plane-wave set is part of the basis — so MDF energies are only comparable at a matched mesh, which is why that gate forces it on both sides. And **Task 7d: `GDF._prefer_ccdf` flipped to `false`**, matching upstream; `df_swap.rs` now pins BOTH routes against their own upstream numbers, which mattered because they differ by 5.222e-10 — INSIDE that test's 1e-9 bar — so the pre-flip test would have kept passing while silently measuring the other route. The default route is also the faster one (1.3 s vs 6.6 s, He-fcc 2×2×2). **Still NOT ported**: `_RSNucBuilder` — a PERFORMANCE carry-over, not a fidelity gap, and the same one 14-03 opened for `_CCNucBuilder`: this port uses NEITHER split nuclear builder, sending `get_nuc`/`get_pp` straight to AFTDF at the cell's converged mesh where it is oracle-gated at 2.755e-12, strictly better than either split (which buys a `[9,9,9]` mesh instead of `[43,43,43]`; 14-04 measured that dropping the split at that mesh costs 0.0743 Ha). Consistent with that, the flipped default shows no nuclear penalty — the RS route's diamond-gamma error is SMALLER than the CC route's. Also still open: `rsdf_helper`'s prescreen, and **`rsjk`, which is BLOCKED ON PHASE 17 and not on cintx**: `range_omega` was necessary but NOT sufficient, because `rsjk.py:150-200` builds its SR half over `ft_ao._RangeSeparatedCell` + `ExtendedMole.strip_basis` (Phase 17, D-PBC-21/23) and `_get_jk_sr` then drives a SCREENED periodic 4-centre `int2e` (`PBCVHF_direct_drv1`) of which this port has NO implementation at all. Unlike RSDF there is no all-compact fallback — for `rsjk` the screening IS the algorithm, so an unscreened BvK sweep is infeasible rather than merely slow. Sequence it after Phase 17 and size it as its own plan. **7a DID ship**: all twelve ω estimators + `weighted_coulG_LR/_SR` + `_gaussian_int`, every number gated at 1e-12 against `measurements/omega.out`, which `rsjk`, RSH functionals and Phase 17 all need regardless. **Gate 4 (MET)** — `_cderi` at mesh [40,40,40], k-mesh PINNED at 2×2×2: **6.08 %** of the FFTDF AO table on diamond (upstream 6.17 %), **0.29 %** payload on He-fcc, where upstream's 1.48 % is a 0.12 MiB HDF5 FILE around a 0.02 MiB payload and is not comparable; the 3×3×3 number is **20.50 %** (20.95 %) and is why the gate names its mesh. The size formula is validated against a `_cderi` file this port actually wrote. **SIX DEFECTS the phase's own tests caught.** (1) `decompose_j2c` read `zeigh_gen`'s COLUMN-MAJOR eigenvectors row-major — worth **+6 306 866.73 Ha** on He-fcc 2×2×2. **No gate had ever reached the eigen branch**: `j2ctag` is `CD` on every measured system, and MDF (`j2c_eig_always = True`) was its first consumer. New regression test: `V j2c Vᴴ = I` on the retained subspace, the identity that DEFINES the factor. (2) Two missing devices from `gen_uniq_kpts_groups`, both invisible on the Cholesky route: `if self_conj: j2c = j2c.real` (a complex eigensolver is free to return an arbitrary phase, and `cderi` is contracted with NO conjugate, so it survives as `e^{2iθ}`) and the conjugate pass at `−kpt` with `_conj_j2c` instead of an independent decomposition. (3) `get_naoaux` was STRICTER than upstream — it raised on per-k rank disagreement where upstream takes one arbitrary block; the ranks legitimately differ per k-difference on the eigen route (MDF keeps 10 vectors for one group and 11 for another at mesh 15). (4) **`ExtendedMole.strip_basis` is worth 1.054e-09 in `j3c` / 2.750e-09 in the ERI, and 14-02's gate could not see it** — it compared against a standalone `incore.Int3cBuilder`, which strips nothing. Localised by six measurements in which every INPUT matched (`weighted_ft_ao` 6.9e-18, `auxbar` 2.8e-17, `int1e_ovlp` 2.9e-15, `L` 1.1e-13, real-space `fuse(j3c)` 1.4e-12) and the assembly did not. Flattening upstream's per-shell-pair radius array to its own maximum — what this port does — collapses it to 7.333e-13: **the port is the MORE converged of the two.** (5) Both of 14-06's stated premises were wrong (MDF's default mesh is [11,11,11]/[9,9,9], not [7,7,7]; `mdfladder.out` measures `_RSMDFBuilder`). (6) `_ao2mo.r_e2`'s two source comments both say `^*` and its arithmetic conjugates the BRA ALONE (measured 2.512e-15 vs 12.227). **PHASE 15 IS UNBLOCKED**: `ao2mo_7d`'s index order is fixed and asserted — `eri[ki,kj,kk][i,j,k,l]` with `kl = kconserv[ki,kj,kk]`, chemists' notation — which is KMP2's `eri[ki,ka,kj][i,a,j,b]` = `(ia|jb)` with `kb = kconserv[ki,ka,kj]` under two index namings; and `sr_loop`/`get_naoaux`/the HDF5 store give KMP2 a GDF it can read. **CARRY-OVER, and it is ONE piece of work not four**: `ft_ao._RangeSeparatedCell` + `ExtendedMole` closes D-PBC-23's `exclude_dd_block` (1.835e-08 Ha), `strip_basis` (1.054e-09) and Phase 13's `ft_aopair` residual (5.121e-10) together, and feeds Phase 17. The cintx `range_omega` gap is separate and is a cintx change.
- [x] **Phase 15: Periodic AO2MO + KMP2** *(COMPLETE 2026-09-05 — restricted KMP2 and all eight plan surfaces shipped; the nine-part opt-in oracle matrix is green; FFTDF clears its `2e-6` gate by five orders at `5.418e-11` (diamond) / `2.719e-11` (He/6-31g); the staggered-mesh energy oracle now runs and CAUGHT THREE DEFECTS, one worth a factor of 26.6; the GDF route's `1.289e-1` upstream residual is the only NOT-MET row and belongs to Phase 14 — the **mean field itself** is `1.523e+1 Ha` out on diamond `[1,1,2]` (`-23.8828` vs `-8.6553`) while FFTDF is `4.772e-11`, and `Lov` vs forced AO2MO agree to `2e-15` on that same mean field. `14-VERIFICATION` never measured this: its GDF gates are a one-AO `sto-3g` He cell and **diamond at gamma only**, already PARTIAL. D-PBC-28 measured: MO-first is `9.784x` the AO-ERI route, against a `16x` prediction. See `15-VERIFICATION.md`.)* — `KptsHelper`, the k-point padding/frozen-core surface, the `PeriodicDf` ao2mo dispatch + `Lov`, `KMP2` + its density matrices, the `KUMP2` surface, `kmp2_stagger`. **THREE OF `PBC-MASTER-PLAN §8.7`'s FIVE PLANS WERE WRONG ABOUT THE STARTING STATE, in both directions, and the corrections were made BEFORE any code** (`15-CONTEXT.md §1`). (1) **`kconserv`/`kconserv3` shipped in plan 09-07** (`crates/pyscf-pbc-lib/src/kpts_helper.rs`, already consumed by `df_ao2mo::ao2mo_7d`) and **`ktensor.py` is not Phase-15 work at all** — it is the `KsymmArray` container for k-point SYMMETRY whose only consumers are the `*_ksymm` modules, so it moves to Phase 17 rather than being built here with no caller and no way to test it. What remains of 15-01 is `KptsHelper` itself (`kpts_helper.py:544-632` — `symm_map`, `_operation`, `transform_symm`), of which `KMP2` reads only `kconserv` and Phase 16 reads the rest. (2) **Plan 14-05 already shipped most of §8.7's 15-02**: `general`/`get_eri`/`ao2mo_7d` exist for AFTDF, FFTDF, GDF and MDF, and **the `ao2mo_7d` index contract is settled and asserted** — `eri[ki,kj,kk][i,j,k,l]` with `kl = kconserv[ki,kj,kk]`, chemists' notation (`df_ao2mo.rs:34-70`), which KMP2 reads as `eri[ki,ka,kj][i,a,j,b]` under two index namings. Phase 13's carry-over is closed and nothing in Phase 15 re-derives it. The real remaining work is an INTERFACE problem: `kmp2.py:92` is `fao2mo = mp._scf.with_df.ao2mo`, and this port's `PeriodicDf` trait (`traits.rs:66-100`) has `get_nuc`/`get_pp`/`get_jk` and no ao2mo at all — plus RSDF, which has no ao2mo surface and must delegate to its inner `Gdf` rather than grow a second fitting pipeline. (3) **`KUMP2`'s ENERGY KERNEL DOES NOT EXIST UPSTREAM** — `kump2.py:38`, `:384` and `:402` all `raise NotImplementedError` in PySCF 2.12.1. What ships there is the unrestricted padding surface; RULE 2 makes the Python authoritative, so this port ships the same surface and the same refusal, with an oracle-gated test asserting upstream still raises so the refusal cannot outlive its reason. ~~**Gate:** `KMP2` `e_corr` matches upstream to 1e-14.~~ **SUPERSEDED — the measured gate is `2e-6 Ha` per DF route and the FFTDF route lands at `5.418e-11`; see `15-VERIFICATION.md §1`. THE ORIGINAL GATE WAS UNMEASURED AND CONTRADICTS THE MASTER PLAN, WHICH SAYS 1e-8 FOR THE SAME NUMBER** — six orders apart, in two documents, neither derived from a measurement. This is the identical failure Phase 14 already paid for (`§8.6`'s "1e-15 across every DF builder" was wrong in both halves), and `1e-14` is very likely unreachable: `e_corr` is quadratic in integrals gated at 1.7e-12…4.2e-12, divided by orbital energies from an SCF converged to `conv_tol`, and the port's own KRHF energies land at 2.3e-10…6.5e-12. **Plan 15-01 therefore measures the floor before the gate is written** — upstream's run-to-run and thread-count spread, the mesh ladder, and the four-way DF-route table — and restates the gate in `ROADMAP`, `PBC-MASTER-PLAN §8.7` and `15-CONTEXT.md §2` together. **AND THE GATE MUST NAME ITS DF ROUTE**: `kmp2.py:69` branches the entire `(ia|jb)` computation on `isinstance(with_df, df.GDF)`, so upstream computes KMP2 two different ways, and Phase 14 measured the analogous SCF-level split at 4.5e-6 Ha on diamond with upstream's own two routes apart by the same amount — a single "matches upstream" assertion that does not say which backend it used is untestable. **Traps recorded in advance** (`15-CONTEXT.md §3`), each with the line that proves it: `KScfResult.mo_coeff` is COLUMN-MAJOR (`types.rs:119`) while `df_ao2mo::MoCoeff` is ROW-MAJOR (`df_ao2mo.rs:362`) — the same shape of defect as 14-05's `decompose_j2c` misread, which was worth **+6 306 866.73 Ha** and invisible to every gate then existing, so the conversion gets ONE implementation and a `CᴴSC = I` test rather than a round-trip; `1/nkpts` appears at three sites and is two distinct divisions (`:96`/`:104` and `:116-117`); `LARGE_DENOM = 1e14` is load-bearing arithmetic, not a guard — padded orbitals must contribute `~1e-28`, and skipping them is a different program; the padding convention is occupied-bottom-aligned and **virtual-TOP-aligned** (`:262-263`), and **no §9.2 reference cell exercises it**, so 15-01 must build a k-dependent-`nocc` fixture or nothing tests it; `get_nocc` refuses fractional occupations and this port has live smearing; and `kernel`'s two separate `ka` loops are not a style choice — the exchange term at `ka` reads the block at `kb`, so a fused loop is wrong for every `ka` with `kb > ka`. **Gates (final numbers set by 15-01):** the committed upstream anchor `e_corr = -0.204721432828996` (`kmp2.py:820`, diamond Bohr-specified, `[1,1,2]`, `exxdiv=None`, FFTDF) reproduced tier-2 with no interpreter; `e_corr` vs upstream **per DF route** on diamond and the all-electron He-fcc control; the port's own `|E(FFTDF) − E(GDF)|` against upstream's, reported as a ratio the way 14-VERIFICATION Gate 3 does; **supercell equivalence** `KMP2([1,1,2]) == KMP2(1×1×2 supercell)/2` with no oracle, which alone catches a wrong `kconserv` argument order, a wrong `t2` index order and both misplaced `1/nkpts`; `Tr(rdm1)` and Hermiticity; and §9.3 bit-identity of `e_corr` and `t2` at `RAYON_NUM_THREADS` 1 and 8 (the `nkpts³·nocc²·nvir²` accumulation is exactly the D-PBC-17 shape, so it goes through `oracle_sum`/`oracle_dot` from the first version, not as a retrofit). **A pre-implementation REVIEW + SPEED pass on 2026-09-02 (`15-REVIEW.md`) found six plan defects and added one plan, all before any code.** Three matter: (a) the phase's speed ruling was numbered **D-PBC-27, which 17-10 already owns** — it is now **D-PBC-28**; (b) `pyscf-algebra`'s only bit-deterministic complex inner product `oracle_zdot` is **`zdotc`**, and **all three** of KMP2's hot contractions (`Lov·Lov` at `kmp2.py:96`, `edi`/`exi` at `:113-114`, whose `t2` is already conjugated at `:110`) are **`zdotu`** — following the plan literally would have computed `Σ (oovv/e)·oovv`, a plausible wrong number that only the final `e_corr` gate would catch, so 15-04 now ships `oracle_zdotu` first; (c) **this port's `fft_general` materialises the full `nao²×nao²` AO ERI and then transforms** (`pbc_ao2mo.rs:449-457`), while upstream's `fft_ao2mo.general` transforms AO→MO **on the real-space grid first** and never builds it (`fft_ao2mo.py:145-152`) — an unrecorded RULE-2 deviation shipped by 14-05, worth a derived `(nao²/(nocc·nvir))²` = **16×** (diamond `gth-szv`) to **59×** (`gth-dzvp`) in flops and `nao⁴` complex in memory, and KMP2 calls it `nkpts³` times on the phase's own FFTDF anchor; **new plan 15-08** ports the MO-first path at wave 0. Also corrected: the `symm_map` corollary's arithmetic was backwards (the assembly is `naux`/`Ng` times the `edi`/`exi` contraction, not cheaper than it) though its conclusion survives — the usable symmetry inside the `(ov|ov)` pattern is **2-fold, not 8-fold**, because ops 2/3 of `transform_symm` yield `(vo|vo)` blocks KMP2 never asks for. **No tolerance was touched**: 15-01 still measures the gate before it is written.
- [ ] **Phase 16: Periodic CC + CI** *(PLANNED + REVIEWED 2026-09-02 — **fourteen** plans in `.planning/phases/16-periodic-cc-ci/`; read `16-CONTEXT.md` then `16-REVIEW.md` first)* — `KRCCSD`/`KUCCSD`/`KGCCSD` + k-intermediates, `KCCSD(T)`, `EOM-KCCSD`, `KCIS`, `kuccsd_rdm`. **`PBC-MASTER-PLAN §8.8` SIZED THIS PHASE AT TEN PLANS AND WAS WRONG IN SEVEN WAYS, all found before any code** (`16-CONTEXT.md §1`). Its line arithmetic is exact (13,675 + 852 reproduces `wc -l`, and the 1,399-line balance is Phase 17's `*_ksymm` plus `kccsd_t_rhf_slow.py`), but: (1) **the phase is HARD-BLOCKED on Phase 15** — all nine k-point CC/CI modules import `padding_k_idx`/`padded_mo_coeff`/`padded_mo_energy`/`get_nocc`/`get_nmo`/`get_frozen_mask` from `pbc.mp.kmp2`/`kump2` (`kccsd_rhf.py:31`, `kccsd.py:29`, `kccsd_uhf.py:36`, `kccsd_t.py:31`, `kccsd_t_rhf.py:32`, `eom_kccsd_rhf.py:30`, `eom_kccsd_ghf.py:35`, `eom_kccsd_uhf.py:34`, `kcis_rhf.py:31`) and `crates/pyscf-pbc-mp` is a **13-line stub**; this is the same block that stopped 17-09, and the padding convention is virtual-**TOP**-aligned (`kmp2.py:262-263`) so a second implementation of it is how a plausible wrong number ships. (2) **Four molecular prerequisites are costed at zero and do not exist**: `cc/gccsd.py` (`kccsd.py:332`/`:339`/`:352`/`:477`), `cc/rccsd.py` (`pbc/cc/ccsd.py:24`), `cc/eom_rccsd.py`'s `EOM`/`EOMIP`/`EOMEA` bases (`eom_kccsd_ghf.py:29`/`:684`/`:1201`/`:1691`), and **`lib.davidson_nosym1`** — an iterative NON-symmetric Davidson required by `eom_kccsd_ghf.py:128`/`:1352` and `kcis_rhf.py:97`, of which this workspace has none (`eigh_gen` is symmetric; 17-02's `faer` path is dense). Four plans are dead without it. Each is ported NARROWLY, to the entry points the k-point subclasses actually consume. (3) **`§8.8`'s Reuse note cannot be followed literally**: `WorkspacePool` is not in `pyscf-ccsd` (it is `pyscf-runtime/src/workspace_pool.rs`), `pyscf-ccsd` contains **zero** complex arithmetic, and the pool is f64-typed all the way down — `shape_bytes = product * 8` (`:278-280`), `InMemory(Box<[f64]>)`, `as_slice -> Vec<f64>`. Every k-point CC tensor is `complex128`. **Reinterpreting the f64 buffer would halve the number reaching the HARD `MemoryLimitExceeded` refusal**, on the machine where 17-12's host suite already exit-137s. (4) **`§8.8` builds the EOM base class LAST**: `eom_kccsd_rhf.py:25` and `eom_kccsd_uhf.py:29` both `import eom_kccsd_ghf as eom_kgccsd` and inherit its `EOMIP`/`EOMEA`/`EOMEE`, so GHF ships FIRST here. (5) **EOM-EE does not exist for UHF and is SINGLET-ONLY for RHF** — `eom_kccsd_uhf.py` declares no `EOMEE` at all and its `_IMDS.make_ee` (`:1120`) raises; `EOMEETriplet` (`eom_kccsd_rhf.py:1483`) and `EOMEESpinFlip` (`:1489`) are shells whose only body is `vector_size -> None`, and `EOMEE.vector_size` (`:1417`) raises. **This ROADMAP line's own "IP/EA/EE (RHF/UHF/GHF)" claim was the error**, and the port ships upstream's surface and upstream's refusals with oracle-gated tests, per RULE 2 — the discipline `15-CONTEXT §1.3` set for `KUMP2`'s absent kernel. (6) **`pbc/ci/cisd.py` is not k-point CI**: it is a Γ-only shim (`:24`, `:47` refuse `kpt != 0`) over molecular RCISD/UCISD/GCISD, and **this port has no molecular CI crate at all**. It is DEFERRED EXPLICITLY with the reason recorded (16-13 Task 1), never guessed; `kcis_rhf.py` (k-point CI **singles**) ships. (7) **`kccsd_t_rhf.py:236` runs on a C kernel** `_ccsd.libcc.CCsd_zcontract_t3T` (24 raw pointers, `:229-245`); the zero-C constraint is this project's core value, so it is ported to Rust and gated against **`kccsd_t_rhf_slow.py` (271 l) — the one `pbc/cc` file `§8.8`'s table omits entirely**. ~~**Gate:** `KRCCSD` `e_corr` matches upstream to 1e-14.~~ **THAT GATE IS UNMEASURED AND CONTRADICTS THE MASTER PLAN, WHICH SAYS 1e-8 FOR THE SAME NUMBER** (`§7` row 16, "on He 1×1×2") — six orders apart, in two documents, neither derived from a measurement. This is the **fourth** time this project has found the identical defect (Phases 14, 15, 17), and **upstream's own test suite settles it**: `pyscf/pbc/cc/test/` asserts `KRCCSD` `e_corr` at **6 decimals** (`test_krccsd.py:180`/`:226`/`:232`/`:338`/`:356`; `test_kgccsd.py`, 37 assertions; `test_eom_krccsd.py`, 44) and EOM roots at **3** (`test_krccsd.py:359-366`). `1e-14` is **eight orders tighter than upstream's own suite**; `1e-8` is two. `e_corr` is a quadratic functional of integrals gated at 1.7e-12…4.2e-12, divided by orbital energies from an SCF converged to `conv_tol`, then **iterated through a DIIS path that differs between two independently converged runs** — which is exactly why upstream drops from `KMP2`-ksymm's 5e-11 to `KRCCSD`-ksymm's 6 decimals. **Plan 16-01 measures the floor before the gate is written** and restates it in `ROADMAP`, `PBC-MASTER-PLAN §7`/`§8.8` and `16-CONTEXT §2` together; **and the gate must name its DF route** — `kccsd_rhf.py:37` imports `GDF, RSGDF` and branches the whole ERI build on the mean field's DF class, the same split Phase 14 measured at **4.5e-6 Ha** on diamond. Two upstream numbers worth keeping because they are the shape of gate that DOES work: incore-vs-outcore ERIs at **12 decimals** (`test_krccsd.py:250-256`, same code both sides, no convergence noise) and supercell equivalence at **4** (`:478`, oracle-free, and it alone catches a wrong `kconserv` order or a misplaced `1/nkpts`). **A speed + memory review (`16-REVIEW.md`) ran with the planning and produced D-PBC-29, four clauses, all derived with the line that proves them.** (a) **The complex arena is a new type, never a reinterpretation** — `ZWorkspacePool` with `shape_bytes = product * 16`; the f64 pool also **copies the whole tensor on every `as_slice`** (`:397`, `Ok(b.to_vec())`) and **holds its single global `Mutex` across the caller's closure in `with_mut_slice`** (`:461-483`), which would serialise every rayon k-loop in the phase at one core. (b) **Contractions are host rayon loops with `oracle_*` accumulators, not `zgemm_dense`** — the standing measurement records `zgemm_dense` at **6-12× slower** on the CPU backend (the default here) **and 1.35e-10 off**, outside the 1e-11 gate; re-measured at 16-14 against this phase's own shapes. Every site names its primitive: `oracle_zdot` is `zdotc` and `15-REVIEW.md D-15-R-02` showed a plan saying only "route through `oracle_dot`" yields a plausible wrong number. (c) **`symm_map` is a genuine ~4× and `§8.8` never mentions it** — `kpts_helper.py:583-630` puts each of the `nkpts³` triples in an orbit of ≤4 and `kccsd_rhf.py:783`/`:798-805`/`:909` transforms `≈nkpts³/4` blocks and transposes the rest. **`15-REVIEW.md D-15-R-04`'s ≤2× ruling does NOT carry over**: it held because KMP2 only wants `(ov|ov)`, whereas KCCSD's `_ERIS` wants the full general block (`:789-794`), so all four operations land inside the set it needs. Measured, not asserted, by 16-01 Task 6. (d) **Storage tiers come from an exact per-tensor byte count, never from upstream's `_mem_usage`** — which is `nkpts³·nmo⁴·4·16` carrying its own `# TODO` (`:1100-1107`) and **over-estimates by 9.1× (szv) to 6.2× (dzvp)**, i.e. it would make this port's HARD refusal reject jobs that fit. Derived `vvvv` footprints: diamond `gth-szv` 2×2×2 **2.0 MiB**, 4×4×4 **1.0 GiB**; `gth-dzvp` 2×2×2 **1.79 GiB**, 3×3×3 **68.7 GiB**; ×3 for KUCCSD's `Wvvvv`/`WvvVV`/`WVVVV` and **×16 for KGCCSD**, which doubles `nocc` and `nvir`. **Every `§9.2` fixture is `gth-szv`, where `vvvv` at 2×2×2 is 2 MiB** — so a gate on those alone would ship the HDF5 spill path never once executed, which is precisely 17-12's exit-137 shape; 16-01 Task 5 finds a fixture that crosses a tier and 16-05 test 4 asserts which tier each side used. On EOM, the wall is the **Davidson subspace, not the Hamiltonian** (the matvec is matrix-free): `2·max_space·nroots` vectors of `nocc + nkpts²·nocc²·nvir` (`eom_kccsd_rhf.py:412`) / `nvir + nkpts²·nocc·nvir²` (`:812`) — 16 MiB on `gth-szv` 2×2×2 but **5.4 GiB for EA on `gth-dzvp` 3×3×3**, making `max_space` a real knob rather than a constant to inline. And `(T)` is a **streaming** problem: `t3` is `nocc³·nvir³` **per k-triple** and `kccsd_t_rhf.py`'s `mo_offset`/`slices` blocking IS the algorithm, so 16-08 test 6 bounds peak live bytes by ONE block, not `nkpts³` of them. **IMPLEMENTATION STARTED AND PARTIALLY LANDED 2026-09-06 — see `16-VERIFICATION.md`.** Six of fourteen plans complete and measured, one partial, seven not started. **`KRCCSD e_corr` matches upstream PySCF 2.12.1 to `6.560e-9`** on diamond `gth-szv` `[1,1,2]` (FFTDF, mesh `[15,15,15]`, `conv_tol = 1e-9`), an order and a half inside the measured `1e-7` gate; `init_amps emp2` to `3.494e-9`; the seven `_ERIS` blocks and all nine `cc_*` intermediates to `3.5e-9 … 2.3e-7`, which is the FFT integral-transform floor at that mesh. **`KCCSD(T)` fast-vs-slow agrees to `8.363e-13` relative** (upstream's own two implementations: `2.946e-13`) and the energy is independent of the virtual block size to `2.17e-19`. Oracle-free: the incore and spilled `_ERIS` tiers are BIT-IDENTICAL with the tier asserted on each side, `t1`/`t2`/`e_corr` are bit-reproducible, and `init_amps`' MP2 agrees with Phase 15's `KMP2` to `2.166e-10`. Shipped: 16-01 (measure), 16-02 (`ZWorkspacePool` + `KTensor`), 16-03 (`davidson_nosym1`, cross-checked against upstream cycle-for-cycle including its stalls), 16-04 (`kintermediates_rhf`), 16-05 (`KRCCSD`), 16-08's RHF half. **NOT shipped and recorded with the reason**: 16-06 `KUCCSD`, 16-07 `KGCCSD` (and with it `scf.kghf.KGHF.CCSD`, the surface Phase 19 reads, and the spin-orbital (T)), 16-09/10/11 EOM — the plan set's own droppable half, whose one hard prerequisite (the Davidson) DID ship — 16-12 and 16-13. **17-09's Phase-16 dependency is satisfied** (`KRCCSD` ships and is oracle-green) but 17-09 is not thereby unblocked: its target is the k-SYMMETRY adapters, which also need Phase 17's `KPoints` IBZ machinery. **A finding for another phase:** this port's `KRHF` and upstream's differ by `1.348e-5 Ha` on diamond `[1,1,2]` when `cell.mesh` is PINNED at `[15,15,15]`, while Phase 15 measured them agreeing to `4.772e-11` on the same cell at the DEFAULT mesh — a coarse-mesh property of FFTDF that Phase 16 neither owns nor caused, which is why every CC gate here is driven from upstream's own `fock`/`mo_energy`/`mo_coeff` and the mean-field residual is printed beside every result. **GATE MEASURED AND RESTATED, 2026-09-06 (plan 16-01, `.planning/phases/16-periodic-cc-ci/measurements/README.md`).** Eleven gates, each naming its DF route, fixture, mesh and `cell.precision`; the two struck-through numbers above stay visible. **`KRCCSD e_corr` vs upstream: 1e-7, stated SEPARATELY per DF route** — measured, upstream's plane-wave pair (FFTDF, MDF) agrees with itself to `2.59e-7` and its Gaussian pair (GDF, RSDF) to `6.82e-9`, but the two PAIRS are **`9.22e-4 Ha`** apart on diamond `gth-szv` `[1,1,2]`, three orders worse than the `4.5e-6` the standing memory records at SCF level. Upstream's run-to-run spread is `1.94e-16` and its `OMP_NUM_THREADS` 1/4/8 spread `6.94e-17` — so the DIIS-path worry does not materialise and the floor is the ROUTE, not the iteration. `e_corr` plateaus at `conv_tol = 1e-9` / `conv_tol_normt = 1e-7` (Δ `6.3e-10`); tightening to 1e-11 costs **46× the wall clock** for under `1e-9`. **(T) fast-vs-slow is the tight one at 1e-13 relative** (upstream's own two implementations agree to `3.27e-16` absolute); **EOM roots 1e-5** (upstream's own Davidson-`conv_tol` and `nroots` spread reaches `5.1e-7`, so its 3-decimal assertion is necessity, not pessimism); **spin-orbital-vs-RHF (T) 1e-9** and **`KGCCSD` vs `KRCCSD` 1e-8** — both LOOSER than the plans asked for, because upstream's own two routes differ by `2.86e-10` and `4.95e-9` on the same fixture. **Three corrections to the plan set fell out of the measurement.** (i) **`symm_map` is `2.10×`, not the derived `~4×`** — 176 representatives of 512 triples at 2×2×2 (count ratio 2.91, reaching 3.87 only at `nkpts = 64`), and `vvvv` is built by `ao2mo_7d` in BOTH paths so it saves nothing; D-PBC-29 clause 3 is amended to `~2×` and `16-REVIEW.md §3`'s own "report it if materially below 4" clause is discharged. (ii) **the symmetry loop is NOT bit-identical to an all-triples loop** — upstream's own two paths differ by up to `1.32e-7`, so 16-05 test 5 gates at `1e-6`. (iii) **`_mem_usage` over-estimates by a measured `9.143×` (szv) / `6.058×` (dzvp)**, confirming the derived 9.1×/6.2×, and the incore/spill window a test can set exists only at 2×2×2 (`130.7 … 264.9 MB`) — at `[1,1,2]` it is EMPTY, so the tier-crossing gate is stated at 2×2×2. **Three upstream anchors no longer reproduce**: `test_krccsd.py::test_frozen_n3` FAILS on the vendored 2.12.1 tree (`ehf_bench` off by `1.56e-6`, `ecc_bench` by `5.56e-7`, both asserted at 6 decimals), the fourth instance of `15-VERIFICATION §7`'s standing caveat; and every `cu_metallic` anchor (`:338`, `:356`, `:359-366`) comes from a test upstream has ITSELF disabled with `@unittest.skip('Results not match')` at `:403` — run anyway, all eight diverge by `2e-2 … 7e-2`. No Phase-16 gate cites any of them. **Droppable half if the phase overruns:** 16-09/10/11/13 (EOM + KCIS) — excited-state properties nothing in Phases 17-20 needs for correctness, ordered last. Do **not** drop 16-05 or 16-07: `PBC-MASTER-PLAN §8.9` blocks 17-09's CC half on `KRCCSD` by name (+1071 l), and `scf.kghf.KGHF.CCSD` (`kccsd.py:805`) is a surface Phase 19 reads.
- [ ] **Phase 17: k-point symmetry + multigrid** *(PLANNED 2026-08-31 — thirteen plans in `.planning/phases/17-ksymm-multigrid/`; read `17-CONTEXT.md` first)* — `pbc/symm` (lattice/space-group detection, point groups, tables), `KPoints` IBZ machinery, every `*_ksymm` adapter (SCF/DFT/MP2/CCSD), `dft/multigrid`. **`PBC-MASTER-PLAN §8.9` SIZED THIS PHASE AT EIGHT PLANS AND WAS WRONG IN FOUR WAYS, all found before any code** (`17-CONTEXT.md §1`). (1) **An entire workstream is missing from its table**: `ft_ao._RangeSeparatedCell` + `ExtendedMole` (~600 of `ft_ao.py`'s 790 lines), which D-PBC-21 and D-PBC-23 assign to Phase 17 by name and which **seven live Rust sites already promise to `phase: 17`** — `gdf_builder/mod.rs:96`, `rsdf_builder/mod.rs:190`, `gdf_builder/eta.rs:196`, `gdf/jk.rs:36` and `:243`, `mdf/mdf_jk.rs:80`, `scf/rsjk.rs`. It closes `exclude_dd_block` (**1.835e-08 Ha** diamond 2×2×2, **2.900e-08** gamma, **exactly 0** on He-fcc, which has no smooth shell), `strip_basis` (**1.054e-09** in `j3c`, **2.750e-09** in the ERI) and Phase 13's `ft_aopair` residual (**5.121e-10**) together — "ONE piece of work not four" — and it is plan 17-10. It does **not** unblock `rsjk`: `rsjk.rs:41-52` names two blockers and this removes the first; the second is a screened periodic 4-centre `int2e` (`PBCVHF_direct_drv1`) of which this port has no implementation, and for which there is no correct-but-slow fallback because the screening IS the algorithm. (2) **`symm/basis.py` (161 l) is omitted entirely**, yet `khf_ksymm.eig` (`khf_ksymm.py:104-119`) reads `cell.symm_orb`/`cell.irrep_id` directly and `ksymm_scf_common_init` (`:142`) defaults `use_ao_symmetry = True` — the DEFAULT branch, gated upstream by `test_krhf_symorb`/`test_krks_symorb`. Plan 17-04. (3) **`ktensor.py` (386 l) is omitted**, although `15-CONTEXT §1.1` explicitly moved it OUT of Phase 15 and INTO this one. Plan 17-06. (4) **`multigrid` is two independent programs, not one**: `MultiGridNumInt` needs **two** C entry points (`multigrid.py:146, 280`), `MultiGridNumInt2` needs **twelve** via `_backend_c.py`, plus `pp.py` and `utils.py` — and it is **v2, not v1**, that `grad/rhf.py:28,44` and `grad/uhf.py:28,40` assert on, so Phase 18 inherits the larger half. Plans 17-11 and 17-12. Also unrecorded: **§8.9's 17-06 is blocked on two phases that have shipped no code** — `pyscf-pbc-mp` and `pyscf-pbc-cc` are 13-line stubs, Phase 15 is planned-only and Phase 16 unplanned; 17-09 ships the MP2 half and **defers the CC half explicitly rather than guessing**, the same ruling 15-CONTEXT §1.3 made for `KUMP2`'s non-existent kernel. ~~**Gate:** symmetry-restricted energies equal full-BZ energies to 1e-14.~~ **THAT GATE IS UNMEASURED, CONTRADICTS THE MASTER PLAN (`§7` line 446 says 1e-9 for the same number), AND IS SIX TO SEVEN ORDERS TIGHTER THAN UPSTREAM'S OWN TEST SUITE** — the identical failure Phases 14 and 15 both caught pre-implementation. Upstream asserts the same comparison at **7 decimals (5e-8) gamma-centred** and **6 (5e-7) Monkhorst** (`scf/test/test_khf_ksym.py:84, 92`; `dft/test/test_krks_ksym.py:86, 101`), because the two runs are **independently converged SCFs** taking different DIIS paths — the residual is convergence, not algebra. Note the shape of upstream's own table, which is itself evidence: **post-SCF is gated TIGHTER than the SCF** (`KMP2` ksymm at **5e-11**, `mp/test/test_ksym.py:56`) because it reads one SCF's orbitals through two index paths, while `KRCCSD` falls back to 6 decimals (`test_kccsd_ksymm.py:60`) as the amplitude iteration reintroduces a convergence path. **Plan 17-01 measures every floor before any gate is written** and restates them in `ROADMAP`, `PBC-MASTER-PLAN §7`/`§8.9` and `17-CONTEXT §2` together. **The replacement is five gates:** **A — the IBZ counts, exact integers, no tolerance and no oracle**: upstream pins Si fcc at `[16,16,16]` five ways — **145 / 145 / 245 / 408 / 816 / 2052** (`lib/test/test_kpts_ksymm.py:56-89`), where one wrong symmorphic branch or time-reversal fold moves an integer, and `2052 = 4096/2 + 4` independently pins the TRIM count; **B — the transforms against ONE converged SCF** (no second convergence, so no noise); **C — energy, port vs port**; **D — energy, port vs upstream, PER DF ROUTE** (upstream gates DFT-on-GDF at 8 decimals and DFT-on-FFTDF at 7, so one shared number is untestable); **E — multigrid is NOT held to any of the above**, because it is a different quadrature and upstream's own tests reach only 8 decimals on `get_pp`/`get_nuc`/`get_j` and 7 on `vxc`/`exc`/`ecoul` (`dft/test/test_multigrid.py:84-217`). **17-01 MEASURED all five against the vendored 2.12.1 tree (2026-09-01, `.planning/phases/17-ksymm-multigrid/measurements/README.md`) — every prediction above held, with one correction and two new numbers.** Gate A reproduced EXACTLY (145/145/245/408/816/2052) on upstream's own Si cell, AND on the port's own `si`/`diamond` fixtures at different lattice constants — confirming the integers travel with the space-group TYPE, not the lattice constant, so no extra fixture is needed to gate them; `finger(kpts_ibz)` does not travel and is not gated. **Correction to Gate B**: "≥1e-12" was too tight as an unconditional floor — measured at PySCF's DEFAULT `cell.precision` (1e-8), the linear-map transforms land at 4.5e-10 (`transform_dm`) down to 6.6e-13 (`symmetrize_density`), NOT uniformly ≥1e-12; tightening SCF `conv_tol` changes nothing, but tightening `cell.precision` to 1e-13 collapses every residual to machine epsilon (≤8e-15) — the floor is integral-screening-limited, not convergence-limited. Gate C/D measured on `si` (full grid) and `diamond`/`lif`/`he_fcc` (reduced, resource-scoped): FFTDF lands at 6.9e-14…5.985e-11, GDF at 9.4e-12…3.433e-09, both far inside upstream's 5e-8/5e-7. Gate E measured: v1 is exact to 1e-12…1e-14 against FFTDF/`numint` (mesh-dependent only when under-resolved); v2 carries a MESH-INDEPENDENT ~2e-8 (diamond)/1.5e-7 (si) floor against FFTDF — a definitional gap, not a convergence one. **Two NEW measured numbers with no prior prediction to check against**: (1) upstream's own multigrid (v1 AND v2) measured SLOWER than reference `numint`/FFTDF on these reference systems (wall-clock ratio 0.18x-0.49x) — 17-11/17-12 must not assume multigrid is a speed win at this scale, only at whatever larger scale they actually target; (2) a full-BZ-vs-IBZ-subset `get_jk` wall-clock bound for D-PBC-26's 17-07 fast path: 223x (FFTDF) / 40x (GDF) on `si [4,4,4]`, both far above the naive `nkpts/nkpts_ibz=8x` estimate because exact-exchange cost scales closer to quadratically than linearly in `nkpts` — target the GDF number (~40x) as the realistic floor since GDF is the port's default route. **Traps recorded in advance, each with the line that proves it** (`17-CONTEXT §3`): **never compare `mo_coeff` elementwise** — it is defined only up to a unitary within each degenerate subspace and upstream's own test compares the DM built from it (`test_kpts_ksymm.py:96-99`); the AO rotations act on a COLUMN-MAJOR `mo_coeff` (`pyscf-pbc-scf/src/types.rs:119`), the same shape as 14-05's `decompose_j2c` misread that was **worth +6 306 866.73 Ha and invisible to every gate then existing**, so `R S Rᴴ = S` is the test; **`check_mesh_symmetry` can ENLARGE `cell.mesh`** (`symmetry.py:96`, `cell.py:1529-1550` warns at 8×), so every two-run gate must pin the mesh on both sides or it measures the mesh — the same correction 14-VERIFICATION had to make for MDF; the Fermi level is computed over the **unfolded BZ** (`khf_ksymm.py:38-41`), so fusing it into the IBZ loop is the R-06 failure; `energy_elec` weights by **`weights_ibz`, not `1/nkpts`** (`:74-76`); the candidate-rotation enumeration order is observable through `stars_ops` and the pinned `finger` values, so a `HashSet` in `search_point_group_ops` is a defect; `geom.py:38-41`'s `np.clip` before `arccos` (upstream issue 3113) is load-bearing — unclipped it returns NaN, the comparison silently fails, and a **wrong rotation is admitted**; and `symmetrize_density` is a D-PBC-17 `nkpts × ngrids` accumulation that goes through `oracle_sum` from the first version, not as a retrofit. **One architectural decision that must be made before the first crate (D-PBC-25, recorded by 17-02):** upstream declares `class KPoints(symm.Symmetry, lib.StreamObject)` (`kpts.py:847`), so mirroring `lib/kpts.py` into `pyscf-pbc-lib` is a **dependency cycle** — that crate already sits below `pyscf-pbc-symm` and cannot see `Cell` at all. `KPoints` therefore lives in **`pyscf-pbc-symm`** and holds a `Symmetry` by composition, and `pyscf-pbc-df`/`pyscf-pbc-dft` gain a `pyscf-pbc-symm` dependency — forced by upstream's **seven** `isinstance(kpts, KPoints)` branches in `pbc/dft/numint.py` (`:328, 431, 647, 779, 859, 908, 956`) and every DF builder's `kpts` setter, which is the one place D-PBC-15's "add-on layer, never a fork" stops being free. Its corollary binds the multigrid plans: `pyscf-pbc-dft` may not name cubecl (ALG-06), so **every collocation kernel goes in `pyscf-kernels`**. **Scheduling:** 17-10 is an independent track that shares no code, fixture or gate with the symmetry half and starts in parallel at wave 1; **multigrid is the droppable half if the phase overruns** — it is a speed feature, nothing in Phases 18–20 needs it for correctness, and 17-11/17-12 are ordered last so dropping them costs nothing already built. Do not drop 17-10 instead: seven error messages point at it. **Deliberate non-ports:** `symm/pyscf_spglib.py` — the native detection IS upstream's default (`space_group.py:264`) and spglib cannot serve `dimension < 3` at all (`:288-290`), so it cannot cover the `graphene` reference cell; and `KROHF`/`KROKS`/`KGKS` ksymm adapters, which do not exist upstream. **17-10 status (2026-09-01): `RsCell`/`ExtendedMole` SHIPPED and gated (D-PBC-27); `exclude_dd_block`'s refusal CLOSED on both `CcGdfBuilder`/`RsGdfBuilder` as a fully-working opt-in — the crate's own DEFAULT was deliberately kept `false` rather than flipped to upstream's `true`, because a full-suite regression run to confirm the flip's safety against this crate's many pre-existing 1e-9-scale oracle gates did not finish inside the plan's time budget. He-fcc's exact-0 claim is CONFIRMED bit-identical; diamond's two Ha-level numbers (1.835e-08 2x2x2, 2.900e-08 gamma) have a written, oracle-gated `#[ignore]`d test whose live run did not complete this session (the oracle/target side was independently re-confirmed against vendored 2.12.1; this port's own number was not). Task 4 (band k-points, MO-factorised `get_k_kpts`) was NOT shipped by that session; **it LANDED in a follow-up session** whose test evidence was lost to a restart, and on 2026-09-02 its band route was proven BIT-IDENTICAL to the direct route (`max |dvj| = max |dvk| = 0e0`, 17-08's `gdf_band_route_matches_the_direct_route`). `grep -rn "phase: 17" crates/` now returns NO live refusal anywhere in the workspace. Full detail: `17-10-SUMMARY.md`, `STATE.md`'s 17-10 entry. **PHASE 17 STATUS ROLLUP (2026-09-02; 12 of 13 plans have shipped code, 1 is externally blocked, 0 are closed by 17-13 yet):** **17-01** MEASURED (five gates replace the guessed 1e-14). **17-02, 17-03, 17-04, 17-05, 17-06** SHIPPED and gated — Gate A EXACT (145/145/245/408/816/2052), Gate B **1e-12**, 17-04's Fock block-diagonality held at 1e-11 after the fixture (not the tolerance) was tightened per `17-04-MEASUREMENT.md`, 17-06's `KsymmArray` acceptance handoff CLOSED at 3.3e-12 from a real `khf_ksymm` SCF. **17-10** SHIPPED incl. Task 4 (band route bit-identical); `exclude_dd_block` default kept `false`, diamond Ha-level oracle run still unconfirmed on this port. **17-11** SHIPPED, v1 Gate E at machine precision on a matched coarse mesh. **17-07** PARTIAL — `KsymAdaptedKrhf` with the symmetry-adapted `eig`, `kscf.rs` diff EMPTY (D-PBC-15 holds literally), 4/4 green; `eig_trs`, KUHF/KGHF, fast-`get_jk` validation and Gates C/D carried over; **D-17-07-01** found a latent upstream `IndexError` (`little_cogroup_ops` indexes `k2opk`'s doubled columns, `basis.py:113` indexes `ops`) that gates upstream's DEFAULT `use_ao_symmetry`+time-reversal combination. **17-08** SHIPPED Tasks 1-4 + `kukspu_ksymm`, Task 5 partial — **TWO plan premises were wrong** (`17-08-FINDING-numint.md`): numint's seven `KPoints` sites UNFOLD, they do not symmetrize, and `symmetrize_density` has no upstream caller (D-17-08-01, which removes the phase's only DFT-side cost saving — the third speed assumption to fail in the same direction); DFT+U rotates NO projectors (D-17-08-02). Gate C FFTDF **3.1e-14**, DFT+U `E_U` **6.9e-18**, unfold identity **1.05e-13**; **GDF Gate C RUN and FAILS at 1.43e-6**, recorded not absorbed, band route exonerated, leading hypothesis a symmetry-broken full-BZ GDF solution (the **D-17-08-03** class: an IBZ-vs-full-BZ comparison is only valid if the full-BZ solution is itself symmetric — the KUKS gate hit exactly this, `alpha` symmetric / `beta` NOT, and is `#[ignore]`d on that asserted precondition rather than on a loosened tolerance). RSH stays blocked on the Phase-14 `omega` carry-over at `gdf/jk.rs:674`. **17-12** code SHIPPED, kernel gates 5/5 GREEN (adjoint identity, `gradient_gs` einsum), host-side Gate E **UNVERIFIED** — every SCF-bearing host test is SIGKILLed (exit 137, OOM class) on this machine, individually and in release; nothing observed to fail or pass. **17-09** BLOCKED EXTERNALLY — `pyscf-pbc-mp` and `pyscf-pbc-cc` are still 13-line stubs (Phase 15 planned-only, Phase 16 unplanned); per its own `autonomous: false` must-haves it is deferred explicitly, never guessed. **17-13** NOT RUN. Its inputs are now well-defined: the 17-12 host suite on a machine with memory; Gate D per DF route against 17-01's already-measured upstream numbers; the GDF-Gate-C symmetry check; 17-10's diamond oracle run; 17-07's carried-over tasks; and the formal deferral of 17-09. **Process finding, recorded for the phase:** the environment restarted every ~20-40 min and killed background agents — four consecutive 17-07 agents died in their READING phase; 17-07, 17-08 and this rollup were therefore written directly by the orchestrator session, with reconnaissance pre-extracted (`17-07-BLUEPRINT.md`) so any successor starts at "write code". Every plan that succeeded landed code incrementally and wrote its SUMMARY from the first task, not the last.
- [ ] **Phase 18: Periodic gradients + stress + geomopt** — derivative lattice-sum integrals, PP and Ewald nuclear gradients, `grad/{krhf,kuhf,krks,kuks,krkspu,kukspu}` + gamma variants, the four `*_stress` modules, periodic geometry/lattice optimization. **Gate:** always-on `verify_fd` central difference agrees to 1e-14 Ha/Bohr; stress agrees with `dE/da` finite difference to 1e-5. **cintx status (2026-08-22):** every gradient family this phase needs already ships and is oracle-covered — the v1.0 Phase-7 "6 of 8 missing" claim is stale. FFTDF J/K gradients are grid-based (no `int2e_ip1`) and stress needs no derivative integrals. Only the PP-gradient half (`int3c1e_ip1_r{2,4,6}_origk`, `int1e_r{2,4}_origi_ip2`) waits on cintx Wave 0.5.
- [ ] **Phase 19: Periodic response + relativistic** — CPHF (reusing the single Krylov solver, GRAD-10), `newton_ah`, `stability`, `tdscf` (k + gamma, HF and KS), `gw` (AC, CD, slow), `adc` (IP/EA), `x2c` (sfx2c1e, x2c1e), `eph`. **Gate:** KRHF-TDA lowest excitation matches upstream to 1e-14 eV.
- [ ] **Phase 20: MPI + tools + bindings + ship** — `k2gamma`, lattice DB, ASE bridge, optional wannier90 shim, `mpitools`/`mpicc` behind a default-OFF `mpi` feature, the full `pyscf.pbc.*` PyO3 surface with Phase-3 subclass-override semantics, upstream-test-suite oracle CI (≥80% pass), periodic benchmark suite. **Gate:** an unmodified upstream `pyscf.pbc` script runs on pyscf-rs.

**v2.0 architecture decisions:** D-PBC-01 … D-PBC-20, recorded in `.planning/pbc/PBC-MASTER-PLAN.md §3`. The load-bearing ones: `Cell` owns a `Mole` via `Deref` (D-PBC-01); complex numbers use a planar `CTensor { re, im }` split so every existing real cubecl kernel is reused (D-PBC-02); complex GEMM is four real GEMMs, never Karatsuba, to preserve bit-parity (D-PBC-03); complex eigh has a mandated real 2n×2n embedding fallback (D-PBC-04); the 3-D FFT ships as three batched complex GEMMs first and a Stockham kernel second (D-PBC-05); periodic 1e integrals are built by image expansion over `cintx` shell pairs, requiring no new cintx operator (D-PBC-07); DF lands strictly FFTDF → AFTDF → GDF/MDF → RSDF (D-PBC-09).

---

## Notes on the 12→8 Phase Compression (Judgment Calls)

The research's SUMMARY.md proposed a 12-phase structure (phases 0–11). Standard granularity targets 5–8. Compressed to 8 with the following deliberate merges and one deliberate non-merge:

- **MERGED `bindings` (research phase 3) into `scf` (phase 3 here)**: the PyO3 contract surface is small enough to lock alongside RHF, and locking it earlier than DFT is the entire point. Five SHOWSTOPPER-tier conventions (subclass dispatch, NumPy stride, GIL deadlock, panic across FFI, abi3 wheel) plus three MAJORs land here on a single small surface — easier than retrofitting onto DFT. The downstream methods inherit by convention with explicit re-validation in DFT (success criterion 4).
- **MERGED `geomopt` (research phase 8) into `grad` (phase 7 here)**: geomopt is small (BFGS+RFO + shims), depends only on grad, and the integration surface (`mf.as_scanner()` + analytical gradient pipeline) is shared. Combining keeps the gradient-driven workflow as one coherent unit.
- **MERGED `GPU enable` + `oracle hardening` + `distribution` (research phases 9+10+11) into one closing phase 8**: all three gate on the same prerequisite (CPU baseline correct on every method) and feed the same exit criterion (a wheel that demonstrably ships on PyPI and proves the 2–5× claim). Splitting them creates artificial boundaries: the benchmark suite (PERF) needs the GPU enable AND the wheel build AND the oracle harness running fairly. One closing phase, three sub-deliverables, all visible in success criteria 1/2/3/4.
- **NOT MERGED `infra` + `gto`** (despite the prompt's suggestion): Phase 1 locks 7 pitfall mitigations (FMA, reduction order, panic policy, cubecl pin, F-order conventions, scope-creep lint, sibling-crate ABI matrix) and produces 2 zero-compute substrate crates whose entire job is to gate later kernels. Phase 2 (gto) is the first kernel landing — a thin wrapper over cintx that exercises those conventions end-to-end on a real basis. Merging would conflate "establish conventions" with "first compute integration" and lose CI signal on the foundation phase.
- **NOT MERGED `dft`** (kept as its own phase, despite being the project's largest): ~9000 LOC upstream surface; integrates all three sibling crates simultaneously; owns the most overrideable methods; peaks cubecl-f64 risk via WGPU. Splitting would help; merging would silently bury the largest single-phase risk.
- **NOT MERGED `ccsd`** (kept as its own phase): largest memory pressure in the project (`Wabef ≈ nv⁴ ≈ 4 GB at caffeine size`); the tensor-arena pattern in `pyscf-runtime` must land here from day one (CCSD-11 explicitly), and combining with a smaller phase would create a dilutional retrofit risk.

The compression preserves clear delivery boundaries while landing inside the standard granularity window.

---
*Roadmap created: 2026-05-10*
*Updated 2026-05-10: added the `pyscf-algebra` crate (workspace 14 → 15), added ALG-01..08 (8 new REQ-IDs, all mapped to Phase 1, total 113 → 121), formalised the `gpu` workspace feature (OFF by default → CPU is default backend) and `PYSCF_BACKEND` env-driven runtime selection. Two new cross-cutting concerns documented (algebra-responsibility wall + backend selection). Reference: `docs/manual/Cubecl/`.*
*Updated 2026-05-10 (gap closure): added Phase 1 plans 01-08 (cintx clean-SHA repin + Cargo.lock commit) and 01-09 (check-cubecl-pin transitive version-skew reconciliation) closing 3 BLOCKERs from 01-VERIFICATION.md. Plan count 7 → 9; wave count 5 → 6. Plans 01-01..01-07 marked shipped.*
*Updated 2026-05-10 (revision iteration 1): per checker feedback, reassigned Plan 01-09 from Wave 6 to Wave 5 (a plan cannot share a wave with a declared dependency; 01-08 depends_on 01-09). Wave 6 retains only 01-08; Wave 5 now contains 01-07 (already shipped) plus 01-09. Total wave count unchanged at 6.*

*Updated 2026-08-22 (v2.0 PBC milestone): added Phases 9–20 covering full `pyscf/pbc/*` parity. Workspace grows 20 → 39 crates. Detailed per-plan breakdown, architecture decisions D-PBC-01..20, cubecl kernel inventory K-01..K-17, and the per-module driver inventory live in `.planning/pbc/`.*

*Updated 2026-08-22 (cintx audit): retired the stale "cintx is missing 6 of 8 gradient families" blocker — all gradient families ship and are oracle-covered. Identified the real cintx dependency for PBC: 10 moment-weighted `r^{2n}`-weighted 1e/3c1e families that gate GTH pseudopotentials in Phase 10. Recorded as `PBC-MASTER-PLAN.md` §2.4 (dependency matrix) and risks R-13/R-14; sequenced as Wave 0.5 in the cintx gap-closure plan.*
