---
phase: 04-dft
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - crates/pyscf-dft/src/parser/mod.rs
  - crates/pyscf-dft/src/parser/libxc.rs
  - crates/pyscf-dft/src/parser/xcfun.rs
  - crates/pyscf-dft/src/xc_backend.rs
  - crates/pyscf-dft/src/error.rs
  - crates/pyscf-dft/tests/parse_xc_parity.rs
  - crates/pyscf-dft/tests/xc_eval_bitexact.rs
  - crates/pyscf-dft/tests/libxc_functional_smoke.rs
autonomous: true
requirements: [DFT-02, DFT-03]
user_setup: []

must_haves:
  truths:
    - "The XC string parser handles single name ('b3lyp'), comma form ('pbe,pbe'), shorthands ('lda'->'lda,vwn'), explicit weights ('.5*HF + .5*B88,LYP'), and XC_ALIAS aliases, with a parity test against pyscf/dft/libxc.py:parse_xc"
    - "The default (libxc) parser routes to libxc_rs and the alternate (xcfun) parser routes to xcfun_rs; b3lyp routes to libxc_rs to match upstream numbers (D-01)"
    - "XC functional evaluation via xcfun_rs (default backend) and via libxc_rs (--features libxc, CI-only) produce numbers identical to the upstream C libraries"
    - "The XcBackend seam is a cfg-gated enum so the default build never references libxc_rs symbols"
  artifacts:
    - path: "crates/pyscf-dft/src/parser/libxc.rs"
      provides: "parse_xc + XC_CODES + XC_ALIAS port (default resolver -> libxc_rs); returns (hyb,alpha,omega),((id,fac),...)"
      contains: "parse_xc"
    - path: "crates/pyscf-dft/src/parser/xcfun.rs"
      provides: "xcfun-side parse_xc (alternate resolver -> xcfun_rs)"
      contains: "parse_xc"
    - path: "crates/pyscf-dft/src/xc_backend.rs"
      provides: "XcBackend cfg-gated enum { Xcfun, #[cfg(libxc)] Libxc } + eval dispatch + LdaInput/GgaInput/MggaInput family sizing"
      contains: "XcBackend"
  key_links:
    - from: "crates/pyscf-dft/src/xc_backend.rs"
      to: "xcfun_rs Functional::eval_vec / libxc_rs BatchEvaluator"
      via: "family-sized eval dispatch per backend"
      pattern: "eval"
    - from: "crates/pyscf-dft/src/parser/libxc.rs"
      to: "libxc_rs::lookup_by_name"
      via: "name -> FunctionalId resolution (gated)"
      pattern: "lookup_by_name"
---

<objective>
Port the two XC string parsers (D-01: `pyscf/dft/libxc.py:parse_xc` as the default resolver → `libxc_rs`, `pyscf/dft/xcfun.py:parse_xc` as the alternate → `xcfun_rs`) and build the `XcBackend` cfg-gated dispatch seam (D-03), wiring per-block functional evaluation to `xcfun_rs` (default) and `libxc_rs` (behind `--features libxc`). Covers DFT-02 (parser parity) and DFT-03 (bit-identical eval).

Purpose: This is the XC half of the DFT pipeline — string→spec resolution + functional evaluation. The libxc-default routing (D-01) is non-negotiable for bit-exact b3lyp (xcfun would match xcfun's different numbers).
Output: Two parsers, the XcBackend seam, and the xcfun + (gated) libxc eval paths.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-dft/04-CONTEXT.md
@.planning/phases/04-dft/04-RESEARCH.md
@.planning/phases/04-dft/04-PATTERNS.md

<interfaces>
XcBackend model (from crates/pyscf-algebra/src/client.rs:10-39 — AlgebraClient cfg-gated enum + match dispatch is the EXACT structural model):
- enum XcBackend { Xcfun, #[cfg(feature="libxc")] Libxc }
- fn eval(&self, spec: &XcSpec, rho: RhoBlock, order: DerivOrder) -> Result<XcOutput>
- the #[cfg(not(feature="libxc"))] arm returns PyscfRsError::NotYetImplemented / LibxcFeatureNotEnabled

parse_xc return shape (libxc.py:491-718): (hyb, alpha, omega), ((libxc_id, fac), ...)
Token sub-cases the parser MUST handle (04-RESEARCH Code Examples §1):
  '-' sign prefix; '*' factor either order ('0.5*B88' or 'B88*0.5'); 'E_'->'E-' exponent fixup;
  'RSH(alpha;beta;omega)'; 'HF'; 'SR_HF(omega)'/'LR_HF(omega)'; key.isdigit() raw integer ID;
  comma X/C split; compound-name expansion; '0.5*b3lyp' unit-scaling.

xcfun_rs eval (functional.rs, verified): Functional::new(); .set(name,weight); .is_gga()/.is_metagga();
  .eval_setup(Vars, Mode, order); .input_length()/.output_length(); .eval_vec(density, in_pitch, out, out_pitch, nr_points)
  (GPU dispatch >=64 pts, ERF auto-fallback to CPU). A4: confirm Vars/Mode variant spellings against xcfun-core/src/enums.rs.

libxc_rs eval (gated, verified API): lookup_by_name(name)->FunctionalId; lookup_by_id(u16)->&FunctionalMeta (.family()/.kind());
  FunctionalBuilder::new(id).spin(spin).build(); LdaInput/GgaInput/MggaInput::new(...); *Output::with_order(np,spin,DerivativeOrder);
  BatchEvaluator::new(spin,np_max).evaluate(&func,&input,DerivativeOrder,&mut out). A3: re-confirm exact ctor/field names against src/api/batch.rs + src/output/* (READ ONLY — never compile libxc_rs).

XC_CODES/XC_ALIAS tables: inline const slices, NO codegen/build.rs (auxbasis.rs DEFAULT_AUXBASIS convention).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port parse_xc (libxc default + xcfun alternate) with XC_CODES/XC_ALIAS</name>
  <files>crates/pyscf-dft/src/parser/mod.rs, crates/pyscf-dft/src/parser/libxc.rs, crates/pyscf-dft/src/parser/xcfun.rs, crates/pyscf-dft/src/error.rs, crates/pyscf-dft/tests/parse_xc_parity.rs</files>
  <behavior>
    - parse_xc('b3lyp') returns the hyb/alpha/omega + component (id,fac) list matching upstream libxc.py:parse_xc
    - parse_xc('pbe,pbe'), parse_xc('lda') (->'lda,vwn'), parse_xc('.5*HF + .5*B88,LYP'), and XC_ALIAS entries all match upstream
    - parse_xc rejects garbage strings with a clean error (NOT a panic, NOT an eval of arbitrary code) — pure string->spec mapping
    - the xcfun-side parser produces the xcfun functional-name/weight set for the same inputs
  </behavior>
  <read_first>
    - crates/pyscf-dft/src/lib.rs (the 04-01 skeleton; add `pub mod parser; pub mod xc_backend; pub mod error;` declarations here is OWNED by 04-06 which fills lib.rs — for THIS plan, create the parser/xc_backend/error modules and let 04-06 wire the lib.rs re-exports; do NOT edit lib.rs in this plan to avoid the wave conflict — instead add a minimal `mod parser; mod xc_backend; mod error;` ONLY if the crate won't compile otherwise, coordinating the final pub re-exports into 04-06)
    - pyscf/dft/libxc.py (parse_xc 491-718, XC_CODES 154, XC_ALIAS 217, hybrid_coeff 406, rsh_coeff 431; parse_token sub-cases 565+)
    - pyscf/dft/xcfun.py (the alternate parse_xc + code tables)
    - pyscf/dft/dft_parser.py (the 24-line shared XC-string preprocessing)
    - crates/pyscf-df/src/auxbasis.rs (DEFAULT_AUXBASIS — the inline-const-table + lookup-fn convention for XC_CODES/XC_ALIAS)
    - crates/pyscf-core/src/error.rs (PyscfRsError + the BasisLoad/EcpLoad #[from] variant pattern at 30-42 — add a DftError variant the same way)
    - crates/pyscf-gto/src/ (the Phase 2 format_atom/make_env port style — the no-Rust-analog port precedent)
  </read_first>
  <action>
    Create `parser/mod.rs` declaring `pub mod libxc; pub mod xcfun;` and a shared `XcSpec`/return type `((hyb,alpha,omega), Vec<(u32, f64)>)`. Port `pyscf/dft/libxc.py:parse_xc` into `parser/libxc.rs` as the DEFAULT resolver: handle every token sub-case (sign prefix, `*` factor either order, `E_`→`E-` fixup, `RSH(alpha;beta;omega)`, `HF`, `SR_HF`/`LR_HF`, raw integer ID, comma X/C split, compound expansion, `0.5*b3lyp` unit-scaling), with `XC_CODES` + `XC_ALIAS` as inline `const` slices (NO codegen). The parser MUST be a pure string→spec mapping — guard against panics, unbounded recursion (the `0.5*b3lyp` compound expansion must have a bounded depth / cycle guard), and never `eval` arbitrary code (security: untrusted user XC string). Port `pyscf/dft/xcfun.py:parse_xc` into `parser/xcfun.rs` mirroring the same shape but emitting xcfun functional names/weights. Add a `DftError` enum (or PyscfRsError variant) in `error.rs` for parse failures + `LibxcFeatureNotEnabled`. Fill `crates/pyscf-dft/tests/parse_xc_parity.rs` (remove its `#[ignore]`) with a parity test driving upstream `pyscf.dft.libxc.parse_xc` via the `pyscf-oracle` GIL harness for: single name, comma form, shorthand, explicit weights, several XC_ALIAS entries, AND at least 3 deliberately-malformed strings asserting a clean Err (no panic). Provenance doc-comment names libxc.py/xcfun.py + line ranges + Apache-2.0 + D-01/DFT-02.
  </action>
  <verify>
    <automated>cargo test -p pyscf-dft parse_xc_parity</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pyscf-dft parse_xc_parity` passes (parity vs upstream parse_xc for single/comma/shorthand/weights/alias) — DFT-02
    - malformed XC strings return `Err`, never panic (behavior assertion in the test)
    - `XC_CODES`/`XC_ALIAS` are inline `const` slices, no codegen/build.rs (source assertion)
    - the compound-expansion path has a bounded depth/cycle guard (source assertion — no unbounded recursion on adversarial input)
    - `parser/libxc.rs` is the default resolver; `parser/xcfun.rs` is the alternate (source assertion: both expose `parse_xc`)
  </acceptance_criteria>
  <done>Both parsers ported; the libxc-default parser matches upstream parse_xc on all forms; malformed input is rejected cleanly (no panic, no eval); tables are inline const.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: XcBackend cfg-gated seam + xcfun eval (default) + gated libxc eval</name>
  <files>crates/pyscf-dft/src/xc_backend.rs, crates/pyscf-dft/tests/xc_eval_bitexact.rs, crates/pyscf-dft/tests/libxc_functional_smoke.rs</files>
  <behavior>
    - XcBackend::Xcfun.eval(...) for LDA/GGA/MGGA functionals produces Exc/Vxc numbers identical to upstream xcfun on a block of grid-point rho values
    - (gated, CI-only) XcBackend::Libxc.eval(...) for the corpus functionals produces numbers identical to upstream libxc
    - the default build never references a libxc_rs symbol (the Libxc variant is #[cfg(feature="libxc")])
    - family dispatch sizes the rho-derivative input correctly: LDA(rho) / GGA(rho,sigma) / MGGA(rho,sigma,lapl,tau)
  </behavior>
  <read_first>
    - crates/pyscf-dft/src/parser/{libxc,xcfun}.rs (from Task 1 — the spec the backend consumes)
    - crates/pyscf-algebra/src/client.rs (lines 10-39 — AlgebraClient cfg-gated enum + match dispatch, the EXACT model for XcBackend)
    - ~/Documents/workspace/xcfun_rs/crates/xcfun-rs/src/functional.rs (Functional::new/set/is_gga/is_metagga/eval_setup/input_length/output_length/eval_vec — the default backend path; READ ONLY)
    - ~/Documents/workspace/xcfun_rs/crates/xcfun-core/src/enums.rs (Vars/Mode variant spellings — confirm A4)
    - ~/Documents/workspace/libxc_rs/src/{registry/mod.rs, api/builder.rs, api/batch.rs, input/mod.rs, output/mod.rs} (lookup_by_name/lookup_by_id/FunctionalBuilder/BatchEvaluator/*Input/*Output — confirm A3; READ ONLY, never compile)
    - .planning/phases/04-dft/04-RESEARCH.md (Pattern 1/2/3 — the three eval shapes; Pitfall 5 — never default-build with libxc)
  </read_first>
  <action>
    Implement the `XcBackend` enum in `xc_backend.rs` mirroring `AlgebraClient` (client.rs:10-39): `Xcfun` default-compiled, `#[cfg(feature = "libxc")] Libxc`, with a `match self` `eval(spec, rho_block, order)` dispatch and a `#[cfg(not(feature = "libxc"))]` arm returning `DftError::LibxcFeatureNotEnabled`. Implement the xcfun path: `Functional::new()` → `.set(name, weight)` per parsed spec → `.eval_setup(Vars, Mode, order)` (size by `.is_gga()`/`.is_metagga()`) → `.eval_vec(...)` over the block. Implement the libxc path (entirely inside `#[cfg(feature = "libxc")]` modules so default builds never name libxc_rs symbols): `lookup_by_name` → `lookup_by_id(.raw())` → `FunctionalMeta.family()`/`.kind()` to pick `LdaInput`/`GgaInput`/`MggaInput` + `DerivativeOrder`, then `BatchEvaluator::new(spin, np_max).evaluate(&func, &input, order, &mut out)`. Re-confirm the exact constructor/field names against the sibling source (A3/A4) — read only, never run `cargo build --features libxc` locally. Fill `crates/pyscf-dft/tests/xc_eval_bitexact.rs` and `libxc_functional_smoke.rs` (both `#[cfg(feature = "libxc")]`-gated, CI-only) with bit-identical-to-upstream-libxc assertions over the corpus functional subset. Add an xcfun-default eval unit test (NOT gated) asserting xcfun Exc/Vxc match upstream xcfun for an LDA + a GGA functional over a small rho block.
  </action>
  <verify>
    <automated>cargo test -p pyscf-dft xc_backend</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pyscf-dft` (the non-gated xc_backend unit test) passes: xcfun Exc/Vxc match upstream xcfun for LDA + GGA
    - the default build does NOT reference libxc_rs: `cargo build -p pyscf-dft` (default) succeeds and the `Libxc` variant + libxc eval code are `#[cfg(feature = "libxc")]`-gated (source assertion)
    - `xc_eval_bitexact.rs` + `libxc_functional_smoke.rs` are `#[cfg(feature = "libxc")]`-gated (source assertion) — the gated bit-exact run is CI-only and is NOT a per-task verify here (Pitfall 5)
    - family dispatch picks LdaInput/GgaInput/MggaInput by `family()`/`is_gga()`/`is_metagga()` (source assertion)
  </acceptance_criteria>
  <done>XcBackend is a cfg-gated seam (default never names libxc_rs); xcfun eval matches upstream xcfun; the gated libxc eval path is wired and CI-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → XC-string parser | `dft.RKS(mol, xc=<arbitrary user string>)` — untrusted string input crosses here (incl. arithmetic forms like '.5*HF + .5*B88,LYP') |
| build-system → dep graph | the libxc eval path must stay cfg-gated or it pulls libxc_rs (266 kernels) into the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05a | Elevation / Tampering | XC-string parser (untrusted input) | mitigate | parser is a pure string→spec mapping — NO `eval` of arbitrary code; bounded-depth/cycle guard on compound expansion (no unbounded recursion); malformed input returns `Err` not panic — asserted in `parse_xc_parity` |
| T-04-05b | Denial of Service | parser unbounded recursion on adversarial string | mitigate | explicit recursion-depth bound on `0.5*b3lyp`-style compound expansion; tested with adversarial inputs |
| T-04-05c | Denial of Service | accidental libxc_rs default-build | mitigate | the `Libxc` variant + all libxc eval code is `#[cfg(feature="libxc")]`; gated tests are CI-only; default `cargo build -p pyscf-dft` proves no libxc_rs reference (Pitfall 5) |
| T-04-SC | Tampering | sibling-crate eval (libxc_rs/xcfun_rs) | mitigate | path/`[patch.crates-io]` sibling deps under BectorVoom; no new registry installs (04-RESEARCH Package Legitimacy Audit); slopcheck N/A |
| — | Spoofing / Repudiation / Info Disclosure | n/a | accept | No auth/network/PII/secrets in XC parsing/eval — N/A |
</threat_model>

<verification>
- `cargo test -p pyscf-dft parse_xc_parity` passes (DFT-02)
- `cargo test -p pyscf-dft` xc_backend xcfun-default unit test passes (DFT-03 xcfun half)
- `cargo build -p pyscf-dft` (default) succeeds with no libxc_rs reference
- gated `cargo test --features libxc -p pyscf-dft xc_eval_bitexact` is CI-only (DFT-03 libxc half)
</verification>

<success_criteria>
The libxc-default + xcfun-alternate parsers match upstream parse_xc on all forms and reject malformed input cleanly; the XcBackend seam routes xcfun by default and libxc behind the gated feature, both bit-identical to the upstream C libraries.
</success_criteria>

<output>
Create `.planning/phases/04-dft/04-05-SUMMARY.md` when done.
</output>
