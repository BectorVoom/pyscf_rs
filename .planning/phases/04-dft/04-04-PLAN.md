---
phase: 04-dft
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - crates/pyscf-grids/src/lib.rs
  - crates/pyscf-grids/src/lebedev.rs
  - crates/pyscf-grids/src/radial.rs
  - crates/pyscf-grids/src/radii.rs
  - crates/pyscf-grids/src/levels.rs
  - crates/pyscf-grids/src/prune.rs
  - crates/pyscf-grids/src/partition.rs
  - crates/pyscf-grids/tests/grid_weights_level_sweep.rs
autonomous: true
requirements: [DFT-04, DFT-09]
user_setup: []

must_haves:
  truths:
    - "The Grids class with class-defaults (treutler radial, treutler_atomic_radii_adjust, original_becke, nwchem_prune, BRAGG_RADII, level=3) produces grid coords + weights byte-for-byte identical to upstream pyscf/dft/gen_grid.py on the test corpus"
    - "mf.grids.level = N for N in 0..9 produces the same number of grid points as upstream"
    - "Becke partition normalization uses ordered oracle_sum reductions (Pitfall 10) under release-oracle FMA-free"
  artifacts:
    - path: "crates/pyscf-grids/src/lebedev.rs"
      provides: "SphGenOh generator + per-order seed tuples + LEBEDEV_ORDER map (port of LebedevGrid.py)"
      contains: "SphGenOh"
    - path: "crates/pyscf-grids/src/radial.rs"
      provides: "treutler_ahlrichs (default) + gauss_chebyshev/becke/delley/mura_knowles radial schemes (port of radi.py)"
      contains: "treutler"
    - path: "crates/pyscf-grids/src/partition.rs"
      provides: "Becke partition (pure-Python get_partition fallback port, NOT the C VXCgen_grid) + atomic_radii_adjust"
      min_lines: 40
    - path: "crates/pyscf-grids/src/lib.rs"
      provides: "Grids struct (level/atom_grid/prune/radi_method/becke_scheme/atomic_radii) + gen_atomic_grids"
      contains: "Grids"
    - path: "crates/pyscf-grids/tests/grid_weights_level_sweep.rs"
      provides: "byte-for-byte weights vs upstream, level 0..9, corpus"
      contains: "grid_weights_level_sweep"
  key_links:
    - from: "crates/pyscf-grids/src/partition.rs"
      to: "pyscf_algebra::oracle_sum"
      via: "partition-weight normalization sum (byte-exactness hot spot)"
      pattern: "oracle_sum"
    - from: "crates/pyscf-grids/src/lib.rs"
      to: "lebedev + radial + partition + prune"
      via: "gen_atomic_grids composes radial x Lebedev then Becke-partitions"
      pattern: "gen_atomic_grids"
---

<objective>
Port `pyscf/dft/{gen_grid.py, radi.py, LebedevGrid.py}` + `pyscf/data/radii.py` into the `pyscf-grids` crate (D-05/D-06) so the `Grids` class produces grid points and weights byte-for-byte identical to upstream for `level ∈ {0..9}` on the test corpus. This is the highest-risk bit-exact target in the phase and the home of Pitfall 10 (grid-weight byte-exactness).

Purpose: DFT energy correctness rides entirely on grid weights matching upstream to the last ULP — any divergence here propagates to every DFT energy. Owning this in its own crate also serves VV10 nlcgrids (DFT-06) and Phase 7 grad-response.
Output: A byte-exact `pyscf-grids` crate.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-dft/04-CONTEXT.md
@.planning/phases/04-dft/04-RESEARCH.md
@.planning/phases/04-dft/04-PATTERNS.md

<interfaces>
From crates/pyscf-algebra/src/lib.rs:
- pub fn oracle_sum(&[f64]) -> f64   // ordered, FMA-free reduction (Pitfall 10 lives here)

From crates/pyscf-core (Mole):
- atom coordinates + atomic numbers Z (per-atom), available off pyscf-core::Mole

CRITICAL class-vs-function defaults (04-RESEARCH Pitfall 3, gen_grid.py:490-499): use the Grids CLASS defaults, NOT the gen_atomic_grids function defaults:
  radi_method = radi.treutler (Treutler-Ahlrichs, NOT gauss_chebyshev)
  radii_adjust = treutler_atomic_radii_adjust
  becke_scheme = original_becke
  prune = nwchem_prune
  atomic_radii = BRAGG_RADII
  level = 3
  ATOM_SPECIFIC_TREUTLER_GRIDS = True  (default; affects ~1e-6/atom — radi.py:142)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port Lebedev generator + radial schemes + const radius/level tables</name>
  <files>crates/pyscf-grids/src/lebedev.rs, crates/pyscf-grids/src/radial.rs, crates/pyscf-grids/src/radii.rs, crates/pyscf-grids/src/levels.rs, crates/pyscf-grids/src/prune.rs</files>
  <behavior>
    - SphGenOh(code, a, b, v) expands one seed into the correct Oh-symmetric point count (code 0→6, 1→12, 2→8, 3→24, 4→24, 5→48) with coords matching upstream
    - MakeAngularGrid_N for each Lebedev order N produces the LEBEDEV_NGRID[N] points matching upstream coords+weights
    - treutler_ahlrichs(n, Z) radial points+weights match upstream radi.py for several elements (H, C, O, a 3rd-row element)
    - LEBEDEV_ORDER map and RAD_GRIDS/ANG_ORDER level tables match upstream values for level 0..9
  </behavior>
  <read_first>
    - crates/pyscf-grids/src/lib.rs (the skeleton from 04-01 with the module declarations)
    - pyscf/dft/LebedevGrid.py (SphGenOh 113-, MakeAngularGrid_N seed tuples, LEBEDEV_ORDER/LEBEDEV_NGRID 4999-5033 — port the GENERATOR + seeds, NOT a 5047-line static table; D-06)
    - pyscf/dft/radi.py (treutler_ahlrichs 138 default, gauss_chebyshev 103, becke, delley, mura_knowles; treutler_atomic_radii_adjust 180, becke_atomic_radii_adjust 159; BRAGG/COVALENT/SG1RADII; ATOM_SPECIFIC_TREUTLER_GRIDS flag at 142)
    - pyscf/data/radii.py (BRAGG, COVALENT element-radius tables — small const tables to port)
    - pyscf/dft/gen_grid.py (RAD_GRIDS/ANG_ORDER tables 672-699, _default_rad/_default_ang period lookup; nwchem_prune default + sg1_prune/treutler_prune)
    - crates/pyscf-df/src/auxbasis.rs (the inline-`const`-table convention: `&[(...)]` const slice + lookup fn — D-06 forbids codegen/build.rs/runtime file reads)
    - crates/pyscf-kernels/src/eval_gto.rs (lines 1-12 — the port-the-reference-algorithm module-doc-header style)
  </read_first>
  <action>
    Port the Lebedev generator into `lebedev.rs`: `SphGenOh(code, a, b, v)` expanding the 6 octahedral-symmetry codes, the per-order `MakeAngularGrid_N` seed tuples as inline `const` arrays, and the `LEBEDEV_ORDER`/`LEBEDEV_NGRID` maps (port the generator + seeds, NEVER a static 5047-line table — D-06; const arrays only, no build.rs, no runtime file reads). Port `radi.py` radial schemes into `radial.rs`: `treutler_ahlrichs` (the class default), plus `gauss_chebyshev`, `becke`, `delley`, `mura_knowles`, and the `treutler_atomic_radii_adjust`/`becke_atomic_radii_adjust` functions; honor `ATOM_SPECIFIC_TREUTLER_GRIDS = True` (default, ~1e-6/atom). Port `pyscf/data/radii.py` BRAGG/COVALENT + radi.py SG1RADII into `radii.rs` as inline `const` slices with lookup-by-Z fns. Port the `RAD_GRIDS`/`ANG_ORDER` level tables + `_default_rad`/`_default_ang` period lookup into `levels.rs`. Port `nwchem_prune` (the class default) + `sg1_prune` + `treutler_prune` into `prune.rs`. Every module doc-comment names the upstream `.py` source + line range + Apache-2.0 provenance + D-06 (provenance convention). Any radial/angular product sum of > 2 terms uses `pyscf_algebra::oracle_sum`. Add the unit tests inline (per-module `#[cfg(test)]`) asserting SphGenOh point counts, a few MakeAngularGrid_N coordinate sets, and treutler_ahlrichs points for H/C/O vs hardcoded upstream values.
  </action>
  <verify>
    <automated>cargo test --profile release-oracle -p pyscf-grids lebedev radial levels</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --profile release-oracle -p pyscf-grids` (lebedev/radial/levels unit tests) passes
    - `lebedev.rs` contains `SphGenOh` and inline `const` seed arrays (source assertion); contains NO `build.rs`/`include_str!`/`include!`/runtime-file-read (source assertion: `grep -E 'include_str|include!|build.rs|read_to_string' crates/pyscf-grids/src/lebedev.rs` returns nothing)
    - `radial.rs` `treutler_ahlrichs` is the named default; SphGenOh code→count mapping is correct (unit assertion)
    - LEBEDEV_ORDER and RAD_GRIDS/ANG_ORDER level tables match upstream for level 0..9 (unit assertion)
  </acceptance_criteria>
  <done>Lebedev generator, radial schemes, const radius/level tables, and pruning are ported with inline const tables (no codegen/build.rs); unit tests pass under release-oracle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Becke partition + Grids struct + byte-exact level sweep</name>
  <files>crates/pyscf-grids/src/partition.rs, crates/pyscf-grids/src/lib.rs, crates/pyscf-grids/tests/grid_weights_level_sweep.rs</files>
  <behavior>
    - get_partition (pure-Python fallback port) produces Becke partition weights matching upstream byte-for-byte on a multi-atom molecule
    - gen_atomic_grids composes radial × Lebedev then applies the Becke partition + atomic_radii_adjust producing (coords, weights)
    - Grids::build() with class defaults on the corpus (H2O, benzene, a water trimer) produces coords + weights byte-for-byte identical to upstream gen_grid.py for level 0..9
    - mf.grids.level = N produces the upstream grid-point COUNT for each N in 0..9 (DFT-09)
  </behavior>
  <read_first>
    - crates/pyscf-grids/src/{lebedev,radial,radii,levels,prune}.rs (from Task 1 — the building blocks gen_atomic_grids composes)
    - pyscf/dft/gen_grid.py (Grids class defaults 490-499, get_partition 271-345 INCLUDING the pure-Python fallback 314-329, gen_atomic_grids 184-268; the C-kernel `libdft.VXCgen_grid` at line 307 is NOT in any sibling crate — port the pure-Python fallback, Pitfall 4)
    - crates/pyscf-df/src/df_jk.rs (lines 34-117 — the oracle_sum-per-output-element reduction convention; mirror it for `pbecke.sum(axis=0)` normalization at gen_grid.py:337)
    - .planning/phases/04-dft/04-RESEARCH.md (Pitfall 3 — the byte-exactness hot spot is the partition-sum order; use the CLASS defaults)
    - crates/pyscf-df/Cargo.toml (the test-harness oracle dev-dep wiring pattern)
  </read_first>
  <action>
    Port the pure-Python `get_partition` fallback (gen_grid.py:314-329) into `partition.rs` — NOT the C kernel `libdft.VXCgen_grid` (Pitfall 4: it lives in PySCF's own libdft, not in cintx/libxc_rs/xcfun_rs). Implement the `original_becke` scheme (class default) + `treutler_atomic_radii_adjust`. The normalization `pbecke.sum(axis=0)` (gen_grid.py:337) is THE byte-exactness hot spot — route it through `pyscf_algebra::oracle_sum` (build a per-element terms buffer then ordered-sum), mirroring `df_jk.rs:73-117`. In `lib.rs`, define the `Grids` struct exposing `level`, `atom_grid`, `prune`, `radi_method`, `becke_scheme`, `atomic_radii` controls (defaulting to the CLASS defaults: treutler radial, treutler_atomic_radii_adjust, original_becke, nwchem_prune, BRAGG_RADII, level=3) and `gen_atomic_grids` composing radial×Lebedev→Becke-partition→(coords,weights), plus a `build()` entry. Re-export `Grids`, `gen_atomic_grids` from lib.rs. Fill `crates/pyscf-grids/tests/grid_weights_level_sweep.rs` (the 04-01 scaffold, remove its `#[ignore]`) with a byte-for-byte oracle comparison vs upstream `Grids` weights+coords for level 0..9 on H2O/benzene/water-trimer under `RAYON_NUM_THREADS=1` + release-oracle.
  </action>
  <verify>
    <automated>cargo test --profile release-oracle -p pyscf-grids grid_weights_level_sweep</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --profile release-oracle -p pyscf-grids grid_weights_level_sweep` passes (byte-for-byte vs upstream, level 0..9, corpus) — DFT-04/09
    - `partition.rs` ports the pure-Python fallback and uses `pyscf_algebra::oracle_sum` for the normalization (source assertion: `grep oracle_sum crates/pyscf-grids/src/partition.rs`); it does NOT reference `VXCgen_grid` (source assertion)
    - `Grids` defaults are the CLASS defaults (treutler/treutler_adjust/original_becke/nwchem_prune/BRAGG/level=3) — source assertion
    - grid-point counts per level 0..9 match upstream (DFT-09 — asserted in the sweep test)
    - `cargo run -p xtask --bin check-dependency-wall` passes (pyscf-grids stays OUT of the carve-out)
  </acceptance_criteria>
  <done>Becke partition byte-exact via ordered reductions; Grids struct with class defaults; the level 0..9 byte-for-byte sweep passes on the corpus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (internal) | grid generator; inputs are Mole geometry + integer level — no untrusted external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure (numerical-correctness, Pitfall 10) | Becke partition normalization sum order | mitigate | `pyscf_algebra::oracle_sum` for the `pbecke.sum(axis=0)` normalization + release-oracle FMA-free; byte-for-byte oracle sweep level 0..9 |
| T-04-04b | Tampering | wrong Grids defaults (class vs function) | mitigate | hardcode the CLASS defaults (treutler/treutler_adjust/original_becke/nwchem_prune/BRAGG/level=3) per gen_grid.py:490-499; source-asserted |
| — | Spoofing / Repudiation / DoS / Elevation | n/a | accept | Pure deterministic generator; no auth/network/secrets; `level` is a small bounded int — no unbounded-recursion/DoS surface. No new packages. |
</threat_model>

<verification>
- `cargo test --profile release-oracle -p pyscf-grids grid_weights_level_sweep` passes (DFT-04/09)
- `cargo test --profile release-oracle -p pyscf-grids` unit tests pass
- `cargo run -p xtask --bin check-dependency-wall` passes
- no codegen/build.rs/runtime-file-read in pyscf-grids (source assertion)
</verification>

<success_criteria>
The Grids class with class-defaults produces grid coords + weights byte-for-byte identical to upstream for level 0..9 on the corpus; Pitfall 10 is owned via ordered oracle_sum reductions; the generator is a pure const-table port (no compile-freeze risk).
</success_criteria>

<output>
Create `.planning/phases/04-dft/04-04-SUMMARY.md` when done.
</output>
