---
phase: 04-dft
plan: 08
type: execute
wave: 5
depends_on: ["04-07"]
files_modified:
  - crates/pyscf-dft/src/df_dft.rs
  - crates/pyscf-dft/src/chkfile.rs
  - crates/pyscf-dft/src/lib.rs
  - crates/pyscf-dft/tests/df_dft_match.rs
  - crates/pyscf-dft/tests/ks_chkfile_roundtrip.rs
autonomous: true
requirements: [DFT-07]
user_setup: []

must_haves:
  truths:
    - "dft.RKS(mol).density_fit() solves DF-DFT routing the Coulomb-J build through pyscf-df::DfIntegrals/get_jk_df (Phase 3 D-10 reuse, no new DF crate) and matches upstream"
    - "KsResult impls Checkpointable: an RKS result writes an HDF5 chkfile (with xc/grids metadata) that round-trips and is h5py-readable on the upstream schema"
  artifacts:
    - path: "crates/pyscf-dft/src/df_dft.rs"
      provides: "RKS::density_fit + DfKsHooks routing get_jk J-part through pyscf-df"
      contains: "density_fit"
    - path: "crates/pyscf-dft/src/chkfile.rs"
      provides: "impl Checkpointable for KsResult + xc/grids schema metadata"
      contains: "Checkpointable"
  key_links:
    - from: "crates/pyscf-dft/src/df_dft.rs"
      to: "pyscf_df::DfIntegrals / get_jk_df"
      via: "RKS::density_fit precomputes B integrals; DfKsHooks routes the J build"
      pattern: "cholesky_eri|get_jk_df|DfIntegrals"
    - from: "crates/pyscf-dft/src/chkfile.rs"
      to: "pyscf_chkfile::primitives"
      via: "KsResult dump/load via the HDF5 primitives (no own hdf5 dep)"
      pattern: "primitives"
---

<objective>
Add DF-DFT (DFT-07) by reusing `pyscf-df::DfIntegrals`/`get_jk_df` for the Coulomb-J build (Phase 3 D-10 — no new DF crate), and implement `KsResult` chkfile persistence (`impl Checkpointable for KsResult` with `xc`/`grids` schema metadata, Phase 3 D-06 pattern). Both are integration-shape reuse of shipped Phase 3 crates.

Purpose: DF-DFT is a top-tier idiom (`dft.RKS(mol).density_fit()`); chkfile persistence makes KS results resumable and h5py-compatible (the project's chkfile-compatibility guarantee).
Output: DF-DFT energy match + KS chkfile round-trip.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-dft/04-CONTEXT.md
@.planning/phases/04-dft/04-RESEARCH.md
@.planning/phases/04-dft/04-PATTERNS.md

<interfaces>
From crates/pyscf-df (Phase 3 D-10):
- pub fn cholesky_eri(mol, auxbasis) -> Result<DfIntegrals, ...>
- pub fn get_jk_df(df: &DfIntegrals, dm) -> (Density, Density)
- DEFAULT_AUXBASIS / default_jkfit(basis_name)

From crates/pyscf-scf/src/df_scf.rs (the EXACT analog):
- RHF::density_fit(auxbasis: Option<&str>) -> Result<Self, PyscfRsError>  (lines 42-54): extracts basis name, picks aux default, cholesky_eri, stores in with_df as Box<dyn Any + Send + Sync>
- pub struct DfHooks<'a> { df: &'a DfIntegrals }  + impl OverrideHooks (lines 79-163): get_jk override routes through get_jk_df, other hooks delegate to default_*

From crates/pyscf-chkfile (Phase 3 D-06):
- pub trait Checkpointable { fn dump(&self, group: &hdf5::Group) -> Result<(),ChkfileError>; fn load(group) -> Result<Self,ChkfileError>; }
- pyscf_chkfile::primitives::{write_scalar_f64, write_dataset_1d, write_dataset_f_order, ...}
- checkpointable.rs:5 explicitly anticipates `pyscf-dft::KsResult (Phase 4)`

From crates/pyscf-scf/src/chkfile.rs (the EXACT analog):
- impl Checkpointable for ScfResult (lines 21-97): write e_tot scalar + mo_energy/mo_occ 1D + mo_coeff F-order; /mol group reuses mol.dumps() JSON
- dump_scf_to_file / load_scf_from_file (101-123)

For DFT, the K part of get_veff stays on the standard grid-loop/get_jk path; only the J (Coulomb) build routes through DF (df_scf.rs:96-98 is the J-build seam).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RKS::density_fit + DfKsHooks (DFT-07)</name>
  <files>crates/pyscf-dft/src/df_dft.rs, crates/pyscf-dft/src/lib.rs, crates/pyscf-dft/tests/df_dft_match.rs</files>
  <behavior>
    - dft.RKS(mol).density_fit().kernel() converges to upstream DF-DFT total energy within chemical accuracy on H2O/cc-pVDZ
    - the J (Coulomb) build routes through pyscf_df::get_jk_df; the Vxc/K parts stay on the standard grid-loop/get_jk path
    - auxbasis defaults match upstream (weigend/cc-pvdz-jkfit family)
  </behavior>
  <read_first>
    - crates/pyscf-scf/src/df_scf.rs (lines 26-163 — RHF::density_fit + DfHooks; THE exact analog to copy)
    - crates/pyscf-df/src/lib.rs (cholesky_eri/get_jk_df/DEFAULT_AUXBASIS public surface)
    - crates/pyscf-dft/src/{rks,veff,hooks,lib}.rs (from 04-06/04-07 — RKS struct, KS get_veff, KsOverrideHooks; add `pub mod df_dft;` + re-export in lib.rs)
    - pyscf/dft/rks.py (the density_fit shape on the KS side)
    - crates/pyscf-dft/tests/df_dft_match.rs (04-01 scaffold — remove #[ignore])
  </read_first>
  <action>
    Implement `df_dft.rs` mirroring `df_scf.rs`: `RKS::density_fit(auxbasis: Option<&str>) -> Result<Self, PyscfRsError>` extracting the basis name, defaulting the aux (df_scf.rs:42-54 via `default_jkfit`), pre-computing B integrals via `pyscf_df::cholesky_eri`, storing in `with_df` as `Box<dyn Any + Send + Sync>`. Implement a `DfKsHooks` `KsOverrideHooks` impl whose J-build (the Coulomb part of `get_veff`) routes through `pyscf_df::get_jk_df` while the Vxc/K parts stay on the standard grid-loop/get_jk path (df_scf.rs:96-98 J-build seam) and other hooks delegate to the KS `default_*`. Add `pub mod df_dft;` + re-export in `lib.rs`. Fill `crates/pyscf-dft/tests/df_dft_match.rs` (remove `#[ignore]`) with a DF-DFT energy-match oracle assertion vs upstream `dft.RKS(mol).density_fit()` on H2O/cc-pVDZ (CPU/xcfun default unless the XC is libxc-only). Provenance doc-comment names rks.py density_fit + DFT-07 + D-10 reuse.
  </action>
  <verify>
    <automated>cargo test --profile release-oracle -p pyscf-dft df_dft_match</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --profile release-oracle -p pyscf-dft df_dft_match` passes — DFT-07
    - `df_dft.rs` reuses `pyscf_df::cholesky_eri`/`get_jk_df` (source assertion — no new DF crate; D-10)
    - the J build routes through DF; Vxc/K stay on the grid-loop/get_jk path (source assertion vs df_scf.rs:96-98)
    - `RKS::density_fit` returns `Self` mirroring `RHF::density_fit` (source assertion)
  </acceptance_criteria>
  <done>DF-DFT works by reusing the Phase 3 DF crate for the J build; matches upstream on the corpus.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: impl Checkpointable for KsResult + chkfile round-trip</name>
  <files>crates/pyscf-dft/src/chkfile.rs, crates/pyscf-dft/src/lib.rs, crates/pyscf-dft/tests/ks_chkfile_roundtrip.rs</files>
  <behavior>
    - an RKS result dumps to an HDF5 group then loads back identical (e_tot, mo_energy, mo_occ, mo_coeff F-order, xc/grids metadata)
    - the chkfile is h5py-readable on the upstream PySCF schema (the /scf group + the added xc/grids metadata)
  </behavior>
  <read_first>
    - crates/pyscf-scf/src/chkfile.rs (lines 1-123 — impl Checkpointable for ScfResult + dump/load_scf_to/from_file; THE exact analog)
    - crates/pyscf-chkfile/src/checkpointable.rs (the trait + the line-5 anticipation of pyscf-dft::KsResult)
    - crates/pyscf-chkfile/src/lib.rs (the hdf5 alias re-export at 33-37 keeping pyscf-dft from adding its own hdf5-metno dep — D-05 sole-owner discipline)
    - crates/pyscf-dft/src/{rks,lib}.rs (the KsResult/RKS result type; add `pub mod chkfile;` + re-export)
    - pyscf/dft/rks.py / pyscf/scf/chkfile.py (the upstream chkfile schema; the xc/grids metadata to add)
  </read_first>
  <action>
    Define/confirm a `KsResult` type (or reuse `ScfResult` + DFT metadata) and `impl Checkpointable for KsResult` mirroring `ScfResult` (chkfile.rs:21-97): write e_tot scalar + mo_energy/mo_occ 1D datasets + the F-order `mo_coeff` via `primitives::write_dataset_f_order`, the `/mol` group reusing the Phase 2 `mol.dumps()` JSON, PLUS the DFT-specific `xc` (string) + `grids` (level/scheme) metadata. Use `pyscf_chkfile::primitives::*` via the crate's hdf5 alias (do NOT add an `hdf5-metno` dep to `pyscf-dft` — D-05 sole-owner discipline). Add `dump_ks_to_file`/`load_ks_from_file` analogous to the SCF helpers. Add `pub mod chkfile;` + re-export in `lib.rs`. Fill `crates/pyscf-dft/tests/ks_chkfile_roundtrip.rs` asserting an RKS result dump→load is identical AND (reusing the Phase 3 ORACLE-08 h5py round-trip harness) that h5py reads the written chkfile on the upstream schema. Provenance doc-comment names the upstream chkfile schema + Pitfall 8 (F-order mo_coeff) + D-06.
  </action>
  <verify>
    <automated>cargo test -p pyscf-dft ks_chkfile_roundtrip</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pyscf-dft ks_chkfile_roundtrip` passes: KsResult dump→load identical (behavior assertion)
    - the chkfile is h5py-readable on the upstream schema (ORACLE-08 round-trip assertion)
    - `chkfile.rs` uses `pyscf_chkfile::primitives` and `pyscf-dft/Cargo.toml` does NOT add `hdf5-metno` (source assertion — D-05 sole-owner)
    - the schema includes `xc` + `grids` metadata (source assertion)
  </acceptance_criteria>
  <done>KsResult round-trips through the Phase 3 chkfile primitives with xc/grids metadata and is h5py-readable on the upstream schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chkfile (HDF5) → KsResult::load | deserializing a (possibly untrusted) checkpoint file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | KsResult::load from untrusted chkfile | mitigate | bound/validate on read (dataset shapes/dims checked before allocation; reuse the Phase 3 ScfResult::load validation discipline); load failures return `ChkfileError`, never panic |
| T-04-08b | Information Disclosure (numerical-correctness) | DF-DFT J-build vs grid-loop K | mitigate | oracle energy match vs upstream; J via pyscf-df, K via standard path (source-asserted) |
| T-04-SC | Tampering | dependency surface | mitigate | reuses pyscf-df + pyscf-chkfile (shipped Phase 3 crates); no new registry installs; no new hdf5 dep on pyscf-dft (D-05 sole-owner) |
| — | Spoofing / Repudiation / DoS / Elevation | n/a | accept | No auth/network/secrets; chkfile path is user-local — N/A |
</threat_model>

<verification>
- `cargo test --profile release-oracle -p pyscf-dft df_dft_match` passes (DFT-07)
- `cargo test -p pyscf-dft ks_chkfile_roundtrip` passes (KS chkfile + ORACLE-08)
- pyscf-dft adds no hdf5-metno dep (source assertion)
- `cargo run -p xtask --bin check-dependency-wall` passes
</verification>

<success_criteria>
DF-DFT matches upstream by reusing the Phase 3 DF crate; KsResult round-trips via the Phase 3 chkfile primitives (with xc/grids metadata) and is h5py-readable, with bounded/validated deserialization.
</success_criteria>

<output>
Create `.planning/phases/04-dft/04-08-SUMMARY.md` when done.
</output>
