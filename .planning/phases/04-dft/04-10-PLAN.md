---
phase: 04-dft
plan: 10
type: execute
wave: 5
depends_on: ["04-05", "04-06"]
files_modified:
  - crates/pyscf-dft/src/xc_backend.rs
  - Cargo.toml
  - .github/workflows/ci.yml
  - .github/workflows/nightly-cross-crate.yml
  - crates/pyscf-dft/tests/wgpu_f64_fallback.rs
autonomous: false
requirements: [DFT-03, DFT-11]
user_setup: []

must_haves:
  truths:
    - "The wgpu backend is gated on the shader-f64 Vulkan extension at runtime; when absent, the pipeline falls back to CPU with a tracing::warn! rather than silently degrading to f32 — proven by a CI job on a shader-f64-less device that runs dft.RKS(mol).run() and prints the fallback warning while producing CPU-correct numbers"
    - "The XC-eval-portion f64 fallback delegates to xcfun_rs's existing auto_backend/must_fall_back_to_cpu (no re-implemented probe); the pipeline-level wgpu→CPU fallback reuses Phase 1's PYSCF_BACKEND resolver"
    - "The dedicated --features libxc DFT bit-exact CI job is the ONLY place the gated libxc compile runs (heavily cached); the [patch.crates-io] libxc_rs entry is re-enabled behind the feature; libxc_rs is re-enabled in the nightly cross-crate matrix"
  artifacts:
    - path: "crates/pyscf-dft/src/xc_backend.rs"
      provides: "WGPU f64 honesty: delegate XC-eval fallback to xcfun-gpu auto_backend; pipeline fallback via PYSCF_BACKEND + tracing::warn!"
      contains: "auto_backend"
    - path: ".github/workflows/ci.yml"
      provides: "the dedicated cached --features libxc DFT bit-exact job + the wgpu-no-f64-fallback job"
      contains: "libxc"
    - path: ".github/workflows/nightly-cross-crate.yml"
      provides: "libxc_rs re-enabled in the cross-crate matrix (-p libxc_rs)"
      contains: "libxc_rs"
  key_links:
    - from: "crates/pyscf-dft/src/xc_backend.rs"
      to: "xcfun_gpu::auto_backend / must_fall_back_to_cpu"
      via: "XC-eval f64 fallback delegated to xcfun_rs (DFT-11)"
      pattern: "auto_backend|must_fall_back"
    - from: "Cargo.toml"
      to: "libxc_rs [patch.crates-io] (behind the feature)"
      via: "re-enable the commented entry; nothing references it unless --features libxc"
      pattern: "libxc_rs"
---

<objective>
Land the WGPU f64 honesty path (DFT-11) — gate wgpu on the `shader-f64` Vulkan extension, falling back to CPU with a `tracing::warn!` rather than silently running f32 — by delegating the XC-eval-portion probe to `xcfun_rs`'s existing `auto_backend`/`must_fall_back_to_cpu` and the pipeline-level fallback to Phase 1's `PYSCF_BACKEND` resolver. Also wire the gated libxc bit-exact path's CI surface: re-enable the `[patch.crates-io] libxc_rs` entry behind the feature, add the dedicated heavily-cached `--features libxc` DFT bit-exact CI job (the ONLY place the gated libxc compile runs), add the `wgpu-no-f64-fallback` CI job, and re-enable `libxc_rs` in the nightly cross-crate matrix.

Purpose: DFT-11 closes the WGPU-f64-hole honesty gap (Pitfall 3 re-validation); the CI wiring is what makes the gated libxc bit-exact assertions (04-05/04-06/04-07) actually run in CI without ever risking a local ~6h freeze.
Output: WGPU honest fallback + the libxc + wgpu-no-f64 CI jobs.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-dft/04-CONTEXT.md
@.planning/phases/04-dft/04-RESEARCH.md
@.planning/phases/04-dft/04-PATTERNS.md

<interfaces>
From ~/Documents/workspace/xcfun_rs/crates/xcfun-gpu/src/lib.rs (the DFT-11 delegation target — READ ONLY):
- pub use auto_backend::auto_backend;   // priority-chain selector (env override → ...)
- pub use backend::Backend;             // runtime discriminator
- error_routing::must_fall_back_to_cpu  // ERF-bearing functionals on shader-f64-less wgpu/metal → CPU
- xcfun_rs's GPU-05: wgpu/metal + ERF deps silently override to CPU because range-separation needs f64

From crates/pyscf-runtime (Phase 1):
- PYSCF_BACKEND resolver + BackendKind; an unrecognised/uncompiled backend falls back to CPU + tracing::warn! (the pipeline-level fallback model)

From crates/pyscf-dft/src/xc_backend.rs (04-05):
- XcBackend::eval — the XC-eval seam where the f64 fallback delegation lands

From Cargo.toml (workspace):
- line 109: `# libxc_rs  = { path = "../libxc_rs" }   # disabled — pulls a ~6h libxc compile; re-enable when Phase 4 (DFT) needs it`  — re-enable BEHIND the feature (Pitfall 5)

From .github/workflows/nightly-cross-crate.yml (lines 39-40):
- the `-p libxc_rs` exclusion comment ("Re-add `-p libxc_rs` here ... Phase 4") — re-enable

DFT-11 is Manual-Only-verified per 04-VALIDATION.md (needs a shader-f64-less device/emulation); the CI job is the test instrument.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WGPU f64 honesty — delegate XC-eval fallback + pipeline PYSCF_BACKEND fallback</name>
  <files>crates/pyscf-dft/src/xc_backend.rs, crates/pyscf-dft/tests/wgpu_f64_fallback.rs</files>
  <read_first>
    - crates/pyscf-dft/src/xc_backend.rs (04-05 — the XcBackend::eval seam where the f64 fallback delegation lands)
    - ~/Documents/workspace/xcfun_rs/crates/xcfun-gpu/src/lib.rs + auto_backend.rs + error_routing (auto_backend/must_fall_back_to_cpu — the existing shader-f64/ERF probe to DELEGATE to, not re-implement; READ ONLY)
    - crates/pyscf-runtime/src/ (the PYSCF_BACKEND resolver + the unrecognised-backend → CPU + tracing::warn! model — the pipeline-level fallback)
    - docs/upgrade-cubecl.md (the four-crate cubecl-pin ritual, in case the wgpu path surfaces a bump)
    - .planning/phases/04-dft/04-RESEARCH.md (Pitfall 6 + the DFT-11 Claude's-discretion delegation note; the libxc_rs CPU-host-only fact)
  </read_first>
  <action>
    In `xc_backend.rs`, route the XC-eval-portion f64 capability decision through `xcfun_gpu::auto_backend()` / `must_fall_back_to_cpu()` — do NOT re-implement a `shader-f64` probe (DFT-11 delegation). For the pipeline-level wgpu→CPU fallback, reuse the Phase 1 `PYSCF_BACKEND` resolver: when the resolved backend is wgpu but `shader-f64` is unavailable, fall back to CPU and emit a single clear `tracing::warn!` (mirroring Phase 1's uncompiled-backend warning), then run the XC eval on CPU producing correct f64 numbers — NEVER silently degrade to f32. `libxc_rs` is CPU-host-only by construction, so its XC-eval path is always CPU (note this in the doc-comment). Write `crates/pyscf-dft/tests/wgpu_f64_fallback.rs` as a test that, when no `shader-f64` device is present (the common case / CI default), asserts the fallback path selects CPU and the warning is emitted (capture the `tracing` event), and that a small RKS XC-eval block produces CPU-correct numbers. Provenance doc-comment names the xcfun-gpu delegation + Phase 1 PYSCF_BACKEND + DFT-11 + Pitfall 6.
  </action>
  <verify>
    <automated>cargo test -p pyscf-dft wgpu_f64_fallback</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p pyscf-dft wgpu_f64_fallback` passes: on a shader-f64-less environment the fallback selects CPU + emits the warning + produces CPU-correct f64 numbers (behavior assertion)
    - `xc_backend.rs` delegates to `xcfun_gpu::auto_backend`/`must_fall_back_to_cpu` and does NOT re-implement a shader-f64 probe (source assertion)
    - the pipeline fallback reuses the Phase 1 `PYSCF_BACKEND` resolver + `tracing::warn!` (source assertion)
    - no silent f32 path: the fallback is to CPU-f64, never f32 (source/behavior assertion)
  </acceptance_criteria>
  <done>WGPU f64 honesty lands by delegating the probe to xcfun_rs + reusing the Phase 1 backend resolver; the shader-f64-less path falls back to CPU-f64 with a warning, never silent f32.</done>
</task>

<task type="auto">
  <name>Task 2: Re-enable libxc patch behind feature + libxc-bit-exact + wgpu-no-f64 CI jobs + nightly cross-crate</name>
  <files>Cargo.toml, .github/workflows/ci.yml, .github/workflows/nightly-cross-crate.yml</files>
  <read_first>
    - Cargo.toml (line 107-110 — the [patch.crates-io] block + the commented libxc_rs entry at 109; the comment is already written for re-enable)
    - .github/workflows/ci.yml (the existing job structure + the Phase 3 maturin-smoke / stride-fuzz / xplat job patterns to model the new jobs on)
    - .github/workflows/nightly-cross-crate.yml (lines 36-40 — the `-p libxc_rs` exclusion to re-enable; the cubecl-0.10.0-lockstep matrix)
    - crates/pyscf-dft/Cargo.toml (the `libxc = ["dep:libxc_rs"]` feature from 04-01)
    - .planning/phases/04-dft/04-RESEARCH.md (Pitfall 5 — gate the patch; the libxc job is the ONLY place the gated build runs, heavily cached) + 04-VALIDATION.md (the gated test commands + the DFT-11 Manual-Only CI job)
    - .planning/phases/04-dft/04-02-PLAN.md (the libxc_rs per-functional feature gate this CI job depends on for a tractable build)
  </read_first>
  <action>
    Re-enable the `# libxc_rs = { path = "../libxc_rs" }` line in `Cargo.toml` `[patch.crates-io]` — BEHIND the feature (Pitfall 5: uncommenting the patch is safe ONLY because nothing references libxc_rs unless `--features libxc` is on and the `pyscf-dft` dep is `optional = true` per 04-01; verify no default build pulls it). Add to `ci.yml`: (a) a dedicated `dft-libxc-bitexact` job that runs `cargo test --features libxc -p pyscf-dft xc_eval_bitexact libxc_functional_smoke cam_b3lyp_h2o_rsh` (+ the rks_uks_bitexact subset that needs libxc) under heavy caching (`Swatinem/rust-cache` or equivalent on the libxc_rs build artifacts) — this is the ONLY place the gated libxc compile runs, and it forwards ONLY the corpus-subset per-functional features from 04-02; (b) a `wgpu-no-f64-fallback` job on a shader-f64-less/emulated runner that runs the DFT-11 fallback test and asserts the warning is emitted + CPU-correct numbers (the 04-VALIDATION Manual-Only instrument). Re-enable `-p libxc_rs` in `nightly-cross-crate.yml` (lines 36-40) so the cubecl-0.10.0 lockstep matrix rebuilds libxc_rs + cintx + xcfun_rs + pyscf_rs together (ORACLE-05); cache the libxc build there too. Add `--features libxc` to the workflow env where the gated job needs it. Do NOT add `--features libxc` to any default/pre-merge job (only the dedicated cached job).
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | grep -c error; grep -c 'features libxc\|--features libxc' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace` (default, NO --features libxc) still succeeds and does NOT pull libxc_rs (the re-enabled patch is inert without the feature) — Pitfall 5
    - `ci.yml` has a dedicated `dft-libxc-bitexact` job that is the ONLY job using `--features libxc`, heavily cached, forwarding only the corpus-subset features (source assertion)
    - `ci.yml` has a `wgpu-no-f64-fallback` job running the DFT-11 fallback test (source assertion)
    - `nightly-cross-crate.yml` re-enables `-p libxc_rs` in the matrix (source assertion)
    - NO default/pre-merge CI job uses `--features libxc` (source assertion — only the dedicated cached job)
  </acceptance_criteria>
  <done>The libxc patch is re-enabled behind the feature (default build still libxc-free); the dedicated cached --features libxc bit-exact job + the wgpu-no-f64 job exist; libxc_rs is back in the nightly cross-crate matrix.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: verify default build stays libxc-free + fallback green</name>
  <action>Human verification — see what-built / how-to-verify / resume-signal below.</action>
  <what-built>The WGPU f64 honesty fallback (delegated to xcfun_rs + Phase 1 PYSCF_BACKEND) and the libxc/wgpu CI wiring. The `wgpu_f64_fallback` test asserts the CPU fallback automatically; the gated libxc bit-exact assertions now have their dedicated cached CI job.</what-built>
  <how-to-verify>
    1. Confirm `cargo build --workspace` (default) still completes WITHOUT a multi-hour libxc compile — i.e. the re-enabled `[patch.crates-io]` entry is inert without `--features libxc` (Pitfall 5 / user-memory hard constraint). If a default build suddenly takes hours, STOP — the gating is wrong.
    2. Confirm `cargo test -p pyscf-dft wgpu_f64_fallback` is green (CPU-fallback-with-warning, no silent f32).
    3. Review the new `dft-libxc-bitexact` + `wgpu-no-f64-fallback` CI jobs: the libxc job must be the ONLY `--features libxc` job and heavily cached; the wgpu job runs on a shader-f64-less/emulated runner.
    4. Do NOT run `cargo test --features libxc` locally — that is the dedicated CI job's job only (~6h freeze risk).
  </how-to-verify>
  <resume-signal>Type "approved" if the default build stays libxc-free and the fallback test is green, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime backend selection | a shader-f64-less wgpu device must not silently compute f32 DFT energies |
| build-system → dep graph | the re-enabled libxc patch must stay inert without the feature (~6h freeze risk) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10a | Tampering (silent precision degradation, Pitfall 6/3) | wgpu f32 fallback for f64 DFT | mitigate | gate on `shader-f64` via xcfun-gpu `must_fall_back_to_cpu`; fall back to CPU-f64 + `tracing::warn!`, NEVER f32; CI job on a shader-f64-less device asserts the warning + CPU-correct numbers |
| T-04-10b | Denial of Service (~6h freeze) | re-enabled libxc patch in default build | mitigate | patch inert without `--features libxc` (dep `optional = true`); default `cargo build --workspace` proves no libxc pull; `--features libxc` confined to ONE dedicated cached CI job (Pitfall 5, user-memory hard constraint) |
| T-04-SC | Tampering | CI dependency surface (libxc_rs in nightly matrix) | mitigate | libxc_rs is the pre-existing sibling path/`[patch.crates-io]` dep under BectorVoom, gated behind the corpus-subset features from 04-02; no new registry installs; cubecl 0.10.0 lockstep enforced by the nightly matrix |
| — | Spoofing / Repudiation / Info Disclosure / Elevation | n/a | accept | Backend selection + CI config; no auth/network-secrets/untrusted-input surface — N/A |
</threat_model>

<verification>
- `cargo test -p pyscf-dft wgpu_f64_fallback` passes (DFT-11 fallback)
- `cargo build --workspace` (default) stays libxc-free (Pitfall 5)
- ci.yml has the dedicated cached `--features libxc` job + the `wgpu-no-f64-fallback` job (source assertion)
- nightly-cross-crate.yml re-enables `-p libxc_rs` (source assertion)
- checkpoint: default build stays fast; fallback test green
</verification>

<success_criteria>
WGPU falls back to CPU-f64 with a warning on shader-f64-less devices (never silent f32, DFT-11) by delegating to xcfun_rs + the Phase 1 resolver; the gated libxc bit-exact path runs ONLY in a dedicated cached CI job with the patch inert in default builds; libxc_rs is back in the nightly cross-crate matrix.
</success_criteria>

<output>
Create `.planning/phases/04-dft/04-10-SUMMARY.md` when done.
</output>
