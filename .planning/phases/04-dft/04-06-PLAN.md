---
phase: 04-dft
plan: 06
type: execute
wave: 3
depends_on: ["04-03", "04-04", "04-05"]
files_modified:
  - crates/pyscf-dft/src/lib.rs
  - crates/pyscf-dft/src/numint.rs
  - crates/pyscf-dft/src/veff.rs
  - crates/pyscf-dft/src/hooks.rs
  - crates/pyscf-dft/src/rks.rs
  - crates/pyscf-dft/src/uks.rs
  - crates/pyscf-dft/tests/numint_signatures.rs
  - crates/pyscf-dft/tests/rks_uks_bitexact.rs
  - crates/pyscf-dft/tests/dtype_f32_smoke.rs
autonomous: true
requirements: [DFT-01, DFT-08, DFT-10, DFT-11]
decisions: [D-08]
user_setup: []

must_haves:
  truths:
    - "dft.RKS(mol, xc=...).kernel() and dft.UKS(mol, xc=...).kernel() on the corpus converge to upstream PySCF total energy within 1 µHartree under release-oracle for SVWN/PBE/B3LYP"
    - "NumInt exposes eval_xc/eval_rho/nr_rks/nr_uks with signatures matching upstream numint.py"
    - "the NumInt grid loop is orchestrated through pyscf-algebra gemm (no bespoke cubecl kernel); Exc accumulation uses oracle_sum (bit-exact)"
    - "KsOverrideHooks extends OverrideHooks with get_veff (KS form) + define_xc_ (string form); pyscf-dft stays pyo3-free"
    - "the NumInt grid loop reads DType::from_env() at entry and dispatches the AO-eval/ρ-contraction/Vxc back-contraction matmul chain over the active scalar (D-08); the default-f64 path is byte-stable + bit-exact, and PYSCF_DTYPE=f32 runs the same path end-to-end emitting a below-bit-exact tracing::warn!"
    - "NumInt/KS exposes a read-only dtype() accessor returning the active DType (no set_precision runtime override — deferred per D-08); kernel entry logs backend=… dtype=… (ALG-08 convention)"
  artifacts:
    - path: "crates/pyscf-dft/src/numint.rs"
      provides: "NumInt: eval_rho/eval_xc/nr_rks/nr_uks grid block-loop (port numint.py signatures, D-07) + DType::from_env-driven f32/f64 dispatch + read-only dtype() accessor (D-08)"
      contains: "nr_rks"
    - path: "crates/pyscf-dft/src/veff.rs"
      provides: "KS get_veff = J + Vxc - hyb*K (RSH branch deferred to 04-07)"
      contains: "get_veff"
    - path: "crates/pyscf-dft/src/rks.rs"
      provides: "RKS struct reusing Phase 3 kernel<H>, overriding get_veff"
      contains: "RKS"
    - path: "crates/pyscf-dft/src/hooks.rs"
      provides: "KsOverrideHooks trait + NoKsOverrides default-impl"
      contains: "KsOverrideHooks"
    - path: "crates/pyscf-dft/tests/dtype_f32_smoke.rs"
      provides: "D-08 f32 runs-end-to-end smoke (PYSCF_DTYPE=f32 RKS completes + warns; NO oracle/tolerance gate)"
      contains: "dtype_f32_smoke"
  key_links:
    - from: "crates/pyscf-dft/src/rks.rs"
      to: "pyscf_scf::kernel"
      via: "RKS::kernel reuses the Phase 3 generic kernel<H> with KS hooks"
      pattern: "kernel\\("
    - from: "crates/pyscf-dft/src/numint.rs"
      to: "pyscf_algebra::gemm + oracle_sum + pyscf_kernels::eval_gto"
      via: "rho = AOᵀ·D·AO via gemm; AO via eval_gto; Exc via oracle_sum"
      pattern: "gemm|oracle_sum|eval_gto"
    - from: "crates/pyscf-dft/src/numint.rs"
      to: "pyscf_runtime::DType::from_env"
      via: "active scalar type read at NumInt entry; f32/f64 enum-match dispatch of the matmul chain (D-08)"
      pattern: "DType::from_env|from_env\\(\\)"
---

<objective>
Build the core Kohn-Sham SCF: the `NumInt` grid loop (`nr_rks`/`nr_uks`/`eval_rho`/`eval_xc`, D-07/DFT-10), the KS `get_veff` (J + Vxc − hyb·K), the `KsOverrideHooks` trait (DFT-08), and the `RKS`/`UKS` structs that reuse the Phase 3 generic `kernel<H>` cycle wholesale (DFT-01). This is the phase headline: `dft.RKS(mol, xc='b3lyp').kernel()` bit-exact to upstream on SVWN/PBE/B3LYP. This plan also fills `pyscf-dft/src/lib.rs` (owning all module declarations + re-exports, including the parser/xc_backend modules created in 04-05). It is ALSO the primary owner of the D-08 precision seam through DFT: the grid loop reads the active scalar type from the existing `PYSCF_DTYPE`→`DType::from_env()` resolver and dispatches the AO-eval/ρ-contraction/Vxc back-contraction matmul chain over the active scalar (f64 default = bit-exact, f32 = opt-in fast / WGPU-f64 escape hatch).

Purpose: The end-to-end DFT energy path. Everything else (RSH, VV10, DF, PyO3) hangs off this. D-08 threads the already-shipped precision seam through this hot path so f32 is an honest opt-in (DFT-11 escape hatch) without disturbing the f64 bit-exact contract (DFT-01).
Output: Working bit-exact RKS/UKS on the local-XC corpus (f64), plus an f32 end-to-end path with a runs-end-to-end smoke test (no numeric-parity gate, per D-08).
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-dft/04-CONTEXT.md
@.planning/phases/04-dft/04-RESEARCH.md
@.planning/phases/04-dft/04-PATTERNS.md

<interfaces>
From crates/pyscf-scf/src/lib.rs (Phase 3 — reuse wholesale):
- pub fn kernel(&Mole, &impl OverrideHooks, KernelConfig) -> Result<ScfResult, PyscfRsError>
- pub trait OverrideHooks { get_hcore, get_ovlp, get_init_guess, get_jk, get_veff, get_fock, eig, get_occ, make_rdm1, energy_elec, energy_tot }
- pub struct NoOverrides; (delegates each hook to crate::<module>::default_<hook>)
- pub use fock::{default_get_jk, default_get_veff, ...};
- KernelConfig, ScfResult, InitGuessMode
- RHF::kernel body (rhf.rs:138-148): builds KernelConfig, calls kernel(&mol,&hooks,cfg), copies result back

From crates/pyscf-grids (04-04):
- Grids struct + gen_atomic_grids -> (coords, weights)

From crates/pyscf-kernels (04-03):
- eval_gto_sph (GTOval_sph + GTOval_sph_deriv1) — AO + ∇AO on grid

From crates/pyscf-dft/src/parser + xc_backend (04-05):
- parse_xc -> ((hyb,alpha,omega), Vec<(id,fac)>)
- XcBackend::eval(spec, rho_block, order)

From crates/pyscf-algebra:
- gemm, oracle_sum, reduce_sum

From the precision seam (Phase 1 D-08 + quick-260522-b06 — ALREADY SHIPPED; D-08 threads it):
- pyscf_runtime::DType { F32, F64 } + DType::from_env() (default F64, case-insensitive PYSCF_DTYPE) + DType::name() -> "f32"/"f64"
- pyscf_core::Scalar (sealed: f32/f64) + ScalarKind { F32, F64 } + Scalar::KIND/Scalar::NAME
- pyscf_algebra::DeviceScalar (Scalar + cubecl::Float) — bounds for the device matmul element type; the algebra gemm path is generic over the active scalar
- pyscf_kernels::KernelScalar = f64 — eval_gto is f64-only TODAY (D-08 f32 grid-loop boundary: f32 covers ONLY the algebra matmul chain, NOT eval_gto/XC eval)

nr_rks signature (numint.py:1074 — DFT-10 contract):
  nr_rks(ni, mol, grids, xc_code, dms, relativity=0, hermi=1, max_memory=2000, verbose=None) -> (nelec, excsum, vmat)
  companions: nr_uks(1192), eval_rho(116), eval_xc; NumInt class(2835); hybrid_coeff(2731), rsh_coeff(2737)

KS get_veff form (fock.rs:140-148 model): (j,k)=get_jk; veff[i] = j[i] + vxc[i] - hyb*k[i]. RSH branch is DEFERRED to 04-07 (this plan does the hyb-scaled-K standard-hybrid form only; leave a clearly-marked seam for omega!=0).

D-08 f32 grid-loop boundary (Claude's Discretion — finalized here): the f32 path is f32 ONLY for the AO-eval / ρ-contraction / Vxc back-contraction matmul chain (the pyscf-algebra gemm path). `libxc_rs` (f64 host-only) and `xcfun_rs` evaluate XC in f64, so ρ is cast to f64 at the XcBackend::eval boundary and v cast back to the active scalar before the Vxc back-contraction. Dispatch at NumInt entry mirrors the AlgebraClient enum-match shape (client.rs:10-39): match on DType::from_env() and run the f32 vs f64 matmul chain. The default-f64 path is the existing code path UNCHANGED (no behavioral diff, bit-exact). NO numeric-parity tolerance is committed for f32 (D-08); the f64 default stays the only oracle-validated path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NumInt grid loop (nr_rks/nr_uks/eval_rho/eval_xc) + DType-driven f32/f64 dispatch + read-only dtype() accessor + KS get_veff + KsOverrideHooks + lib.rs</name>
  <files>crates/pyscf-dft/src/lib.rs, crates/pyscf-dft/src/numint.rs, crates/pyscf-dft/src/veff.rs, crates/pyscf-dft/src/hooks.rs, crates/pyscf-dft/tests/numint_signatures.rs</files>
  <behavior>
    - eval_rho(ao, dm) produces ρ (and ∇ρ for GGA) at grid points matching upstream numint.eval_rho element-wise
    - nr_rks(ni, mol, grids, xc_code, dms, ...) returns (nelec, excsum, vmat) with the upstream signature; nelec integrates to the electron count within tolerance
    - get_veff returns J + Vxc - hyb*K matching upstream for an LDA functional (SVWN) on a small fixture
    - KsOverrideHooks::get_veff default delegates to the KS default; NoKsOverrides compiles and the SCF kernel accepts it
    - NumInt reads DType::from_env() at construction/entry and exposes a read-only dtype() accessor returning the active DType; with PYSCF_DTYPE unset the resolved dtype is F64 and the f64 matmul chain is used (default path, unchanged); with PYSCF_DTYPE=f32 the dtype() reads F32, the f32 matmul chain is selected, and a single below-bit-exact tracing::warn! is emitted at kernel entry
  </behavior>
  <read_first>
    - crates/pyscf-dft/src/lib.rs (the 04-01 skeleton; THIS plan fills it with ALL module declarations + re-exports, including `pub mod parser; pub mod xc_backend; pub mod error;` created in 04-05)
    - crates/pyscf-dft/src/parser/, xc_backend.rs, error.rs (the 04-05 modules to wire into lib.rs re-exports)
    - pyscf/dft/numint.py (nr_rks 1074, nr_uks 1192, eval_rho 116, NumInt 2835, hybrid_coeff 2731, rsh_coeff 2737; the block-loop + blksize chunking)
    - pyscf/dft/rks.py (get_veff 37-140 — the J + Vxc - K linear combination + energy_elec 226)
    - crates/pyscf-scf/src/hooks.rs (OverrideHooks trait 13-131 + NoOverrides delegation — KsOverrideHooks EXTENDS this shape)
    - crates/pyscf-scf/src/fock.rs (default_get_jk + default_get_veff 79-148 — the get_veff linear-combination template; note inline loop not axpy)
    - crates/pyscf-df/src/df_jk.rs (lines 34-117 — the block + per-element oracle_sum reduction discipline; the closest analog for the grid-loop accumulation)
    - crates/pyscf-algebra/src/lib.rs (gemm/oracle_sum re-exports) + docs/manual/Cubecl/cubecl_matmul_gemm_example.md (gemm call convention)
    - crates/pyscf-runtime/src/backend.rs (DType { F32, F64 } + DType::from_env() default-F64 case-insensitive + DType::name() — the resolver this plan reads at NumInt entry, D-08)
    - crates/pyscf-algebra/src/scalar.rs (DeviceScalar = Scalar + cubecl::Float; dtype_of::<T>() ScalarKind→DType bridge — the device matmul element bound for the f32 chain, D-08)
    - crates/pyscf-core/src/scalar.rs (Scalar sealed trait + ScalarKind + Scalar::KIND/NAME — the host precision-generic leaf, D-08)
    - crates/pyscf-kernels/src/scalar.rs (KernelScalar = f64 + DeviceScalar re-export — confirms eval_gto stays f64; D-08 f32 boundary excludes eval_gto/XC eval)
    - crates/pyscf-algebra/src/client.rs (lines 10-39 — AlgebraClient cfg-gated enum + match dispatch + log_resolution backend=…/dtype=… ALG-08 line; the EXACT structural model for the f32/f64 NumInt dispatch, D-08)
    - .planning/phases/04-dft/04-RESEARCH.md (D-07 grid-loop diagram lines 155-190; Claude's-discretion blksize/hybrid-coeff surface + the D-08 f32 grid-loop-boundary discretion note)
  </read_first>
  <action>
    Fill `lib.rs` with the `pyscf-scf` lib-root shape (crate-doc naming upstream sources + this plan, `#![forbid(unsafe_code)]`, `#![warn(clippy::unwrap_used)]`), declaring `pub mod` for: `error`, `parser`, `xc_backend` (created in 04-05), plus `numint`, `veff`, `hooks`, `rks`, `uks` (this plan) and grouped `pub use` re-exports (`KsOverrideHooks`, `NoKsOverrides`, `RKS`, `UKS`, `NumInt`, `XcBackend`, `DftError`). Implement `numint.rs` `NumInt` with the upstream signatures (DFT-10): `eval_rho(ao, dm, xctype)` (ρ for LDA, ρ+∇ρ for GGA using the eval_gto deriv1 components), `eval_xc(xc_code, rho, ...)` (routes to `XcBackend::eval`), `nr_rks(ni, mol, grids, xc_code, dms, relativity, hermi, max_memory, verbose) -> (nelec, excsum, vmat)` and `nr_uks` — orchestrating the grid block-loop per D-07: AO = `pyscf_kernels::eval_gto_sph`, ρ = AOᵀ·D·AO via `pyscf_algebra::gemm`, XC via `XcBackend::eval`, Vxc back-contraction via weighted `gemm`, Exc += `pyscf_algebra::oracle_sum(w·exc·ρ)`. NO bespoke cubecl kernel (D-07). Mirror upstream MEM-driven blksize chunking; log `PYSCF_MAX_MEMORY` at entry (Phase 3 convention); no hard pre-flight enforcement (Phase 6 owns that). Expose `hybrid_coeff`/`rsh_coeff(xc_code, spin) -> (omega, alpha, hyb)`.

    D-08 precision threading (this plan owns the DFT compute path): at NumInt construction/entry read the active precision via `pyscf_runtime::DType::from_env()` (NO new env var, NO new dep — `pyscf-runtime`/`pyscf-algebra` are already in the dep set) and store it on `NumInt`; add a read-only accessor `pub fn dtype(&self) -> DType` (and surface the active dtype on the KS object so 04-09 can expose it through the Python `#[getter]`). At `nr_rks`/`nr_uks` entry, log the ALG-08-convention `backend=<…> dtype=<…>` line (reuse the `AlgebraClient::log_resolution` shape from client.rs:38-45). Dispatch the AO-eval/ρ-contraction/Vxc back-contraction matmul chain over the active scalar by matching on the resolved `DType` (mirror the `AlgebraClient` enum-match dispatch, client.rs:10-39): the `DType::F64` arm is the existing f64 code path UNCHANGED (default, bit-exact — do NOT alter its numerics or call shape); the `DType::F32` arm runs the same `pyscf_algebra::gemm` chain with `f32`/`DeviceScalar` tensors (the f32 grid-loop boundary per D-08 Claude's Discretion). The f32 boundary covers ONLY the algebra matmul chain: `eval_gto` stays `KernelScalar = f64` and the XC eval stays f64, so cast ρ→f64 at the `XcBackend::eval` boundary (libxc_rs/xcfun_rs are f64) and cast the returned `v` back to the active scalar before the Vxc back-contraction. When the resolved dtype is `F32`, emit ONE `tracing::warn!` at kernel entry noting f32 is below the bit-exact bar (mirror the Phase 1 `select.rs` warn shape). Do NOT add a `set_precision(...)` runtime override (deferred per D-08) and do NOT add any f32 numeric-parity assertion here (deferred per D-08).

    Implement `veff.rs` `default_get_veff` = J + Vxc − hyb·K (fock.rs:140-148 template; query `hybrid_coeff`; reuse Phase 3 `get_jk` for K) — leave a clearly-marked `// RSH branch (omega != 0): DFT-05, filled by 04-07` seam (do NOT implement RSH here). Implement `hooks.rs` `KsOverrideHooks` extending `OverrideHooks` (add `get_veff` KS-form + `define_xc_`) + a `NoKsOverrides` default-impl delegating to the `default_*` fns (hooks.rs:63-131 pattern); the `define_xc_` body accepts the string-recombination form (feeds the parser) and the callable form returns `NotYetImplemented{deferred}` (D-02). `pyscf-dft` stays pyo3-free. Fill `crates/pyscf-dft/tests/numint_signatures.rs` (remove `#[ignore]`) asserting the eval_xc/eval_rho/nr_rks/nr_uks signatures + a numeric eval_rho element-wise check vs upstream + a `dtype()`-defaults-to-F64 assertion (PYSCF_DTYPE unset → F64). Every module doc-comment carries the upstream-source + D-number provenance (numint.rs names D-07 + D-08).
  </action>
  <verify>
    <automated>cargo test --profile release-oracle -p pyscf-dft numint_signatures</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --profile release-oracle -p pyscf-dft numint_signatures` passes — DFT-10
    - `nr_rks` signature is `(ni, mol, grids, xc_code, dms, relativity, hermi, max_memory, verbose) -> (nelec, excsum, vmat)` (source assertion vs numint.py)
    - the grid loop uses `pyscf_algebra::gemm` + `oracle_sum` and `pyscf_kernels::eval_gto`, with NO `#[cube]` kernel in pyscf-dft (source assertion — D-07)
    - `pyscf-dft` names no `pyo3` and no `cubecl-*` (source assertion); `cargo run -p xtask --bin check-dependency-wall` passes
    - `KsOverrideHooks` extends `OverrideHooks` with `get_veff` + `define_xc_`; the callable `define_xc_` form returns `NotYetImplemented{deferred}` (source assertion — D-02)
    - `numint.rs` reads `pyscf_runtime::DType::from_env()` at NumInt entry and matches on `DType { F32, F64 }` to select the matmul chain (source assertion — D-08); it adds NO new env var and NO new dependency (source assertion: pyscf-dft/Cargo.toml unchanged dep set)
    - `NumInt` exposes a read-only `dtype()` accessor and NO `set_precision`/mutable-precision method exists (source assertion — D-08 deferred override)
    - the `DType::F64` arm is the existing f64 path with no numeric change (the `numint_signatures` eval_rho element-wise check + the unchanged 04-06 Task-2 oracle energies confirm f64 is byte-stable); ρ is cast to f64 at the `XcBackend::eval` boundary and `v` cast back to the active scalar (source assertion — D-08 f32 grid-loop boundary)
    - a `tracing::warn!` below-bit-exact line is emitted when `DType::from_env()` resolves F32 (source assertion); kernel entry logs `backend=… dtype=…` (source assertion — ALG-08)
  </acceptance_criteria>
  <done>NumInt grid loop + KS get_veff + KsOverrideHooks land; the loop is algebra-orchestrated and bit-exact-reduction-disciplined; lib.rs wires all modules; pyscf-dft stays pyo3-free + cubecl-free. The grid loop reads DType::from_env() and dispatches f32/f64 over the algebra matmul chain (D-08), f64 default unchanged + bit-exact, f32 casting ρ→f64 at the XC boundary; a read-only dtype() accessor exists, no set_precision, no f32 tolerance gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RKS/UKS structs reusing kernel<H> + bit-exact corpus energies (f64) + f32 end-to-end smoke</name>
  <files>crates/pyscf-dft/src/rks.rs, crates/pyscf-dft/src/uks.rs, crates/pyscf-dft/tests/rks_uks_bitexact.rs, crates/pyscf-dft/tests/dtype_f32_smoke.rs</files>
  <behavior>
    - dft.RKS(mol, xc='svwn').kernel() converges to upstream PySCF total energy within 1 µHartree on H2O/cc-pVDZ under release-oracle (f64 default)
    - dft.RKS(mol, xc='pbe').kernel() (GGA) and xc='b3lyp' (hybrid; needs the hyb-scaled K) match upstream within 1 µHartree (f64 default)
    - dft.UKS(mol, xc=...).kernel() matches upstream for an open-shell fixture (f64 default)
    - the RKS attribute floor includes xc/grids/nlc/nlcgrids/_numint plus the inherited SCF attributes
    - with PYSCF_DTYPE=f32, dft.RKS(mol, xc='svwn').run() RUNS TO COMPLETION (a converged ScfResult, finite e_tot) and the below-bit-exact warning is emitted — NO comparison to the oracle, NO tolerance assertion (D-08)
  </behavior>
  <read_first>
    - crates/pyscf-dft/src/{numint,veff,hooks,lib}.rs (from Task 1 — incl. the DType::from_env dispatch + dtype() accessor)
    - crates/pyscf-scf/src/rhf.rs (RHF struct 23-178, RHF::kernel 138-148 reuse pattern, to_kernel_config 153-177, manual Debug 58-95, 30-attribute floor doc-header 4-11)
    - crates/pyscf-scf/src/uhf.rs (the alpha/beta open-shell variant — UKS analog)
    - pyscf/dft/rks.py (RKS class + the KS SCF driver layered on scf/hf.py) + pyscf/dft/uks.py (UKS)
    - crates/pyscf-dft/tests/rks_uks_bitexact.rs (the 04-01 scaffold — remove #[ignore], fill with oracle assertions)
    - crates/pyscf-dft/tests/dtype_f32_smoke.rs (the 04-01 Wave-0 scaffold for the D-08 f32 smoke — remove #[ignore], fill with the runs-end-to-end body)
    - crates/pyscf-runtime/src/backend.rs (DType::from_env reads PYSCF_DTYPE — the smoke test sets PYSCF_DTYPE=f32 around the run; D-08)
    - .planning/phases/04-dft/04-VALIDATION.md (the DFT-01 oracle-energy row + the matched-threading fixture requirement + the DFT-11 dtype_f32_smoke row)
  </read_first>
  <action>
    Implement `rks.rs` `RKS` mirroring `RHF` (rhf.rs:23-178) PLUS the DFT attributes (`xc`, `grids: Grids`, `nlc`, `nlcgrids`, `_numint: NumInt`); `RKS::kernel` reuses the Phase 3 generic `kernel<H>` verbatim (rhf.rs:138-148), passing a KS hooks impl whose `get_veff` adds Vxc + scales K by `hyb`. Copy `to_kernel_config` field-mapping + the manual `Debug` for opaque slots (grids/_numint). Implement `uks.rs` `UKS` analogously from `uhf.rs` (alpha/beta). Extend the 30-attribute floor with the DFT-specific fields. Surface the active precision read-only on the KS object by delegating to `_numint.dtype()` (so 04-09 can expose it through a Python `#[getter]`); do NOT add a precision setter (D-08 deferred). Fill `crates/pyscf-dft/tests/rks_uks_bitexact.rs` (remove `#[ignore]`) with `oracle_check!`-driven energy assertions: RKS SVWN + PBE + B3LYP on H2O/cc-pVDZ (and at least one larger corpus fixture) within 1 µHartree under `release-oracle` + `RAYON_NUM_THREADS=1`; UKS on an open-shell fixture — ALL f64 (PYSCF_DTYPE unset/default; these are the bit-exact oracle path, never run under f32). The B3LYP path exercises the hyb-scaled-K branch in `get_veff` (standard hybrid, omega=0). Fill `crates/pyscf-dft/tests/dtype_f32_smoke.rs` (remove `#[ignore]`) with the D-08 runs-end-to-end smoke ONLY: set `PYSCF_DTYPE=f32` (via env around the run, restoring after), run `dft.RKS(mol, xc='svwn').run()` on a small fixture, assert it COMPLETES (returns Ok with a finite `e_tot` and `converged == true`) and that the active `dtype()` reads F32 and the below-bit-exact `tracing::warn!` was emitted (capture the tracing event) — DO NOT compare `e_tot` to any oracle value and DO NOT assert a numeric tolerance (D-08 forbids an f32 tolerance gate in v1). Provenance doc-comments name rks.py/uks.py + DFT-01 (oracle test) and D-08/DFT-11 (the f32 smoke).
  </action>
  <verify>
    <automated>cargo test --profile release-oracle -p pyscf-dft rks_uks_bitexact; cargo test -p pyscf-dft dtype_f32_smoke</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --profile release-oracle -p pyscf-dft rks_uks_bitexact` passes: RKS SVWN/PBE/B3LYP + UKS within 1 µHartree of upstream under the DEFAULT f64 path — DFT-01 (bit-exact path unaffected by D-08)
    - `cargo test -p pyscf-dft dtype_f32_smoke` passes: with PYSCF_DTYPE=f32 the RKS run completes (finite converged e_tot), `dtype()` reads F32, and the below-bit-exact warning is captured — with NO oracle comparison and NO tolerance assertion (behavior assertion — D-08)
    - `RKS::kernel` calls `pyscf_scf::kernel` (source assertion — reuses the Phase 3 cycle, no new SCF machinery)
    - the RKS attribute floor includes `xc`/`grids`/`nlc`/`nlcgrids`/`_numint` (source assertion); the KS object surfaces a read-only active dtype delegating to `_numint.dtype()` and exposes NO precision setter (source assertion — D-08)
    - B3LYP energy match confirms the hyb-scaled standard-hybrid K branch works (omega=0); the RSH branch remains the marked `04-07` seam (source assertion — not yet implemented here)
    - the rks_uks_bitexact oracle suite runs ONLY f64 (no `--features` change, PYSCF_DTYPE unset): f32 is never compared to the oracle (source/behavior assertion — D-08)
  </acceptance_criteria>
  <done>RKS/UKS reuse the Phase 3 kernel<H> and converge bit-exact to upstream on SVWN/PBE/B3LYP under the default f64 path — the phase headline DFT-01 lands for local + standard-hybrid XC, unaffected by D-08. PYSCF_DTYPE=f32 runs the same path end-to-end with a below-bit-exact warning and a runs-to-completion smoke test only (no oracle comparison, no tolerance gate, no precision setter).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (internal) | numeric SCF driver; XC string already validated by the 04-05 parser; no new untrusted input crosses here |
| runtime precision selection (D-08) | PYSCF_DTYPE chooses the active scalar; f32 must be an explicit opt-in that is never silently compared to / mistaken for the f64 bit-exact path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure (numerical-correctness) | grid-loop Exc accumulation order | mitigate | `pyscf_algebra::oracle_sum` for Exc/ρ reductions + release-oracle FMA-free; ≤1 µHartree oracle assertions |
| T-04-06b | Tampering | bespoke kernel sneaking past the algebra wall | mitigate | no `#[cube]` kernel in pyscf-dft (D-07); `check-dependency-wall` passes; pyscf-dft cubecl-free + pyo3-free (source-asserted) |
| T-04-06c | Tampering (silent precision degradation, Pitfall 3/6) | f32 path mistaken for the f64 bit-exact result | mitigate | f32 is opt-in via PYSCF_DTYPE only (default is F64); a `tracing::warn!` fires at kernel entry when f32 is active; the oracle/bit-exact suite runs ONLY f64 and never compares f32 to upstream; read-only `dtype()` lets callers verify the active precision; ρ cast to f64 at the XC-eval boundary so XC numerics are unchanged (D-08) |
| — | Spoofing / Repudiation / DoS / Elevation | n/a | accept | Internal SCF compute; no auth/network/secrets/new-untrusted-input — N/A. No new packages, no new env var (reuses PYSCF_DTYPE), no new dependency (reuses pyscf-runtime/pyscf-algebra). |
</threat_model>

<verification>
- `cargo test --profile release-oracle -p pyscf-dft numint_signatures` passes (DFT-10)
- `cargo test --profile release-oracle -p pyscf-dft rks_uks_bitexact` passes (DFT-01, SVWN/PBE/B3LYP/UKS — f64 default)
- `cargo test -p pyscf-dft dtype_f32_smoke` passes (D-08 f32 runs-end-to-end smoke, no oracle compare)
- `cargo run -p xtask --bin check-dependency-wall` passes
- pyscf-dft names no pyo3 / no cubecl (source assertion); no new env var, no new dep (D-08 reuses the existing seam)
</verification>

<success_criteria>
dft.RKS/UKS converge bit-exact (≤1 µHartree) to upstream on SVWN/PBE/B3LYP via the algebra-orchestrated NumInt grid loop reusing the Phase 3 kernel<H>; NumInt signatures match upstream; KsOverrideHooks extends the SCF hook surface; pyscf-dft stays pyo3-free + cubecl-free. The grid loop reads DType::from_env() and dispatches the matmul chain over the active scalar (D-08): f64 default is the unchanged bit-exact path, f32 is an opt-in fast / WGPU-f64 escape hatch with a below-bit-exact warning, a read-only dtype() accessor, a runs-end-to-end smoke test, no set_precision override, and no f32 tolerance gate.
</success_criteria>

<output>
Create `.planning/phases/04-dft/04-06-SUMMARY.md` when done.
</output>
