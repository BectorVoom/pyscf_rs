---
phase: 04-dft
plan: 09
type: execute
wave: 5
depends_on: ["04-06"]
files_modified:
  - crates/pyscf-py/src/dft.rs
  - crates/pyscf-py/src/bridge.rs
  - crates/pyscf-py/src/lib.rs
  - crates/pyscf-scf/src/convert.rs
  - python/pyscf/dft/__init__.py
  - python/pyscf/__init__.py
  - python/tests/test_dft_override.py
autonomous: false
requirements: [DFT-08]
user_setup: []

must_haves:
  truths:
    - "from pyscf import dft; dft.RKS(mol, xc='b3lyp').run() works exactly as upstream via the _native.dft PyO3 submodule + the python/pyscf/dft overlay shim"
    - "a Python subclass `class MyKS(dft.RKS): def get_veff(...); def define_xc_(...)` has BOTH overrides invoked every SCF cycle by the Rust DFT driver (dispatched via slf.call_method1, never Rust MRO) — re-validating Pitfall 7 on the larger DFT hook surface"
    - "to_uks/to_rks (Phase 3 stubs returning NotYetImplemented{phase:4}) are wired to real KS targets"
    - "pyscf-dft stays pyo3-free; only pyscf-py names pyo3 (PyO3 wall)"
  artifacts:
    - path: "crates/pyscf-py/src/dft.rs"
      provides: "PyRKS/PyUKS pyclasses (subclass) + dft submodule register"
      contains: "PyRKS"
    - path: "crates/pyscf-py/src/bridge.rs"
      provides: "KsOverrideHooks impl extending PyOverrideBridge (get_veff DFT-form + define_xc_ string form)"
      contains: "define_xc_"
    - path: "python/pyscf/dft/__init__.py"
      provides: "overlay re-exporting RKS/UKS from pyscf._native.dft"
      contains: "from pyscf._native.dft import"
    - path: "python/tests/test_dft_override.py"
      provides: "subclass get_veff + define_xc_ invoked-every-cycle assertion (DFT-08)"
      contains: "get_veff"
  key_links:
    - from: "crates/pyscf-py/src/dft.rs"
      to: "pyscf_dft::RKS / pyscf_dft::KsOverrideHooks"
      via: "PyRKS wraps RksRust; the bridge supplies KsOverrideHooks"
      pattern: "RKS"
    - from: "crates/pyscf-py/src/bridge.rs"
      to: "slf.call_method1"
      via: "every KS hook dispatched via call_method1 (MRO, not Rust dispatch) — Pitfall 7"
      pattern: "call_method1|call_hook"
---

<objective>
Build the PyO3 boundary for DFT: `PyRKS`/`PyUKS` pyclasses + the `_native.dft` submodule + the `python/pyscf/dft` overlay, extend `PyOverrideBridge` to implement `KsOverrideHooks` (the `get_veff` DFT-form + `define_xc_` string form), and wire the Phase 3 `to_uks`/`to_rks` stubs to real KS targets. This re-validates the Phase 3 PyO3 subclass-override contract (Pitfall 7) on the project's largest Python-override surface (DFT-08).

Purpose: DFT-08 is a SHOWSTOPPER-tier re-validation — DFT adds the biggest overrideable hook set, and proving the `slf.call_method1` dispatch works here protects every downstream phase.
Output: Working `from pyscf import dft`, subclass overrides invoked every cycle, to_uks/to_rks wired.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-dft/04-CONTEXT.md
@.planning/phases/04-dft/04-RESEARCH.md
@.planning/phases/04-dft/04-PATTERNS.md

<interfaces>
From crates/pyscf-py/src/scf.rs (the EXACT analog for PyRKS/PyUKS):
- #[pyclass(subclass, name = "RHF", module = "pyscf._native.scf")] pub struct PyRHF { inner: RhfRust, py_mol: Py<PyAny> }
- pub(crate) fn register(py, m: &Bound<PyModule>) -> PyResult<()> { m.add_class::<PyRHF>()?; ... }

From crates/pyscf-py/src/lib.rs (submodule wiring, lines 51-54):
- let scf_mod = PyModule::new(py, "scf")?; crate::scf::register(py, &scf_mod)?; m.add_submodule(&scf_mod)?;

From crates/pyscf-py/src/bridge.rs (PyOverrideBridge — EXTEND, don't replace):
- pub struct PyOverrideBridge { slf: Py<PyAny>, py_mol: Py<PyAny>, ... }  (line 33)
- fn call_hook<'py,F,T>(slf, name, args, extract) (line 55) — the per-hook Python::attach + call_method1 helper
- impl OverrideHooks for PyOverrideBridge (line 72): get_hcore/get_ovlp/get_init_guess/get_jk/get_veff(150-163)/get_fock/...
- the get_veff impl (150-163) is the template for the KS get_veff override

From crates/pyscf-dft (04-06):
- pyscf_dft::{RKS, UKS, KsOverrideHooks, NoKsOverrides}
- KsOverrideHooks adds get_veff (KS form) + define_xc_ (string form; callable form is NotYetImplemented{deferred})

From crates/pyscf-scf/src/convert.rs (Phase 3 stubs to wire):
- to_uks_stub (line 82) / to_rks_stub (line 90) return NotYetImplemented{phase:4} — wire to real KS targets

From python/pyscf/scf/__init__.py + python/pyscf/__init__.py (overlay analogs to copy exactly).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PyRKS/PyUKS + dft submodule + KsOverrideHooks bridge + overlay + to_uks/to_rks</name>
  <files>crates/pyscf-py/src/dft.rs, crates/pyscf-py/src/bridge.rs, crates/pyscf-py/src/lib.rs, crates/pyscf-scf/src/convert.rs, python/pyscf/dft/__init__.py, python/pyscf/__init__.py</files>
  <read_first>
    - crates/pyscf-py/src/scf.rs (lines 1-160 — PyRHF/PyUHF pyclass + getter/setter floor + register; THE exact analog)
    - crates/pyscf-py/src/lib.rs (lines 24-57 — module decls + the _native pymodule + scf submodule registration)
    - crates/pyscf-py/src/bridge.rs (lines 33-291 — PyOverrideBridge + call_hook + the get_veff impl 150-163; EXTEND with KsOverrideHooks)
    - crates/pyscf-dft/src/{lib,hooks,rks,uks}.rs (RKS/UKS + KsOverrideHooks the bridge must satisfy)
    - crates/pyscf-scf/src/convert.rs (to_uks_stub/to_rks_stub at 82/90 — wire to real targets)
    - python/pyscf/scf/__init__.py + python/pyscf/__init__.py (the overlay shim shape to copy)
  </read_first>
  <action>
    Create `crates/pyscf-py/src/dft.rs` mirroring `scf.rs`: `#[pyclass(subclass, name = "RKS", module = "pyscf._native.dft")] PyRKS { inner: RksRust, py_mol: Py<PyAny> }` (and `PyUKS`), with `#[getter]/#[setter]` pairs for the DFT attribute floor (xc/grids/nlc/nlcgrids + inherited SCF attrs), a `kernel`/`run` method, and a `register(py, m)` fn adding both classes. Extend `bridge.rs` `PyOverrideBridge` to `impl KsOverrideHooks` (or impl the extended trait): the KS `get_veff` override reuses the get_veff template (bridge.rs:150-163 — `Python::attach` + `density_to_pyarray` + `call_hook(&self.slf, "get_veff", ...)`); the `define_xc_` override packages the string and calls `slf.call_method1("define_xc_", ...)` (string form per D-02; the callable form stays `NotYetImplemented{deferred}`). Every hook MUST dispatch via `slf.call_method1` (MRO), never Rust dispatch (Pitfall 7), each under per-hook `Python::attach`/`Python::detach` seam. Wire the `dft` submodule in `lib.rs` (mirror scf.rs registration at lib.rs:51-54: `let dft_mod = PyModule::new(py, "dft")?; crate::dft::register(py, &dft_mod)?; m.add_submodule(&dft_mod)?;`) and add `pub mod dft;`. Replace the `to_uks_stub`/`to_rks_stub` bodies in `convert.rs` with real RHF→RKS / UHF→UKS conversions returning the KS object. Create `python/pyscf/dft/__init__.py` re-exporting `RKS, UKS` from `pyscf._native.dft` (mirror scf overlay), and add `from pyscf._native import dft` to `python/pyscf/__init__.py` (mirror its `from pyscf._native import scf`). `pyscf-dft` stays pyo3-free — ONLY `pyscf-py` names pyo3.
  </action>
  <verify>
    <automated>cargo build -p pyscf-py 2>&1 | grep -c error; cargo run -p xtask --bin check-forbidden-paths 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p pyscf-py` succeeds (default features)
    - `crates/pyscf-py/src/dft.rs` defines `PyRKS` + `PyUKS` as `#[pyclass(subclass, ...)]` (source assertion)
    - the `_native.dft` submodule is registered in `lib.rs` (source assertion)
    - the KS bridge dispatches every hook via `call_method1`/`call_hook` (source assertion — Pitfall 7, no Rust MRO)
    - `pyscf-dft` names no `pyo3` (source assertion — PyO3 wall)
    - `to_uks_stub`/`to_rks_stub` no longer return `NotYetImplemented{phase:4}` (source assertion)
    - the `define_xc_` callable form still returns `NotYetImplemented{deferred}`; the string form dispatches (source assertion — D-02)
  </acceptance_criteria>
  <done>PyRKS/PyUKS + the dft submodule + overlay ship; the KsOverrideHooks bridge dispatches every hook via call_method1; to_uks/to_rks are wired; pyscf-dft stays pyo3-free.</done>
</task>

<task type="auto">
  <name>Task 2: pytest subclass-override-invoked-every-cycle assertion (DFT-08)</name>
  <files>python/tests/test_dft_override.py</files>
  <read_first>
    - python/tests/ (the Phase 3 SCF override test — the maturin-build + subclass-override-invoked assertion pattern to mirror)
    - crates/pyscf-py/src/dft.rs + bridge.rs (from Task 1 — the surface under test)
    - .planning/phases/04-dft/04-VALIDATION.md (the DFT-08 row + the Manual-Only/PyO3-dispatch-assertion notes)
  </read_first>
  <action>
    Fill `python/tests/test_dft_override.py` (remove the 04-01 `pytest.mark.skip`) with the DFT-08 assertion: define `class MyKS(dft.RKS)` overriding BOTH `get_veff` (calling super + a correction, with an invocation counter) AND `define_xc_` (string form), run `MyKS(mol, xc=...).kernel()`, and assert BOTH overrides are invoked at least once per SCF cycle (counter >= cycle count) — proving the Rust DFT driver calls the Python overrides via `slf.call_method1`, not Rust MRO. Build the extension via maturin (the Phase 3 maturin-smoke pattern). Keep the test CPU/xcfun-default (no `--features libxc`).
  </action>
  <verify>
    <automated>cd python && python -m pytest tests/test_dft_override.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_dft_override.py` passes (after the maturin build)
    - BOTH `get_veff` and `define_xc_` Python overrides are invoked >= once per SCF cycle (behavior assertion via invocation counter) — DFT-08, Pitfall 7 re-validated on the DFT surface
    - the test runs CPU/xcfun-default (no `--features libxc`)
  </acceptance_criteria>
  <done>The DFT-08 subclass-override re-validation passes: both get_veff and define_xc_ Python overrides fire every cycle through the call_method1 dispatch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: verify `from pyscf import dft` + override dispatch</name>
  <action>Human verification — see what-built / how-to-verify / resume-signal below.</action>
  <what-built>The DFT PyO3 surface: `from pyscf import dft; dft.RKS(mol, xc='b3lyp').run()` works, Python subclass overrides of `get_veff`/`define_xc_` are invoked every SCF cycle, and `to_uks`/`to_rks` cross-module dispatch is wired. The pytest in Task 2 already asserts the override-invocation automatically.</what-built>
  <how-to-verify>
    1. After the maturin build, run a real existing-PySCF-style script: `python -c "from pyscf import gto, dft; m=gto.M(atom='O 0 0 0; H 0 1 0; H 0 0 1', basis='cc-pvdz'); print(dft.RKS(m, xc='pbe').run().e_tot)"` and confirm an energy prints.
    2. Confirm `python -m pytest python/tests/test_dft_override.py` is green (the override-invoked-every-cycle assertion).
    3. Spot-check that a subclass overriding `get_veff` actually changes the result (proving the override is wired, not bypassed).
  </how-to-verify>
  <resume-signal>Type "approved" if `from pyscf import dft` works and the override test is green, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python callback → Rust driver (DFT-08) | Python subclass overrides invoked from Rust inside the SCF cycle; a panicking/exception-raising Python override must not cause UB |
| PyO3 NumPy boundary | arrays crossing into/out of the bridge hooks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09a | Elevation / Tampering | panicking Python override across the FFI seam | mitigate | per-hook `Python::attach`/`Python::detach` (Phase 3 D-03); a Python exception in a hook converts to a Rust `Err` (Phase 3 BIND-09 create_exception path), never a process abort/UB; reuse the Phase 3 catch_unwind discipline |
| T-04-09b | Tampering | NumPy non-contiguous input at the bridge boundary | mitigate | reuse the Phase 3 `to_owned()`-on-non-standard-layout converter (BIND-04) in the KS hooks |
| T-04-09c | Denial of Service | GIL deadlock under free-threaded Python | mitigate | per-hook GIL release seam (Phase 3 BIND-05); the string-only `define_xc_` (D-02) avoids a per-block Python callback inside the grid loop entirely |
| T-04-SC | Tampering | dependency surface | mitigate | reuses Phase 3 pyscf-py (pyo3 0.28 / rust-numpy 0.28, already pinned); no new registry installs |
| — | Spoofing / Repudiation / Info Disclosure | n/a | accept | Local Python interop; no auth/network/secrets — N/A |
</threat_model>

<verification>
- `cargo build -p pyscf-py` succeeds
- `python -m pytest python/tests/test_dft_override.py` passes (DFT-08)
- `cargo run -p xtask --bin check-forbidden-paths` passes; pyscf-dft names no pyo3 (source assertion)
- checkpoint: `from pyscf import dft; dft.RKS(mol, xc='pbe').run()` works
</verification>

<success_criteria>
`from pyscf import dft` works; Python subclass overrides of get_veff + define_xc_ fire every SCF cycle via call_method1 (DFT-08, Pitfall 7 re-validated); to_uks/to_rks wired; pyscf-dft stays pyo3-free; panicking overrides surface as exceptions not UB.
</success_criteria>

<output>
Create `.planning/phases/04-dft/04-09-SUMMARY.md` when done.
</output>
