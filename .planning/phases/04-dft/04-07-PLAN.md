---
phase: 04-dft
plan: 07
type: execute
wave: 4
depends_on: ["04-06"]
files_modified:
  - crates/pyscf-gto/src/intor.rs
  - crates/pyscf-dft/src/veff.rs
  - crates/pyscf-dft/src/vv10.rs
  - crates/pyscf-dft/src/lib.rs
  - crates/pyscf-gto/tests/range_coulomb_env.rs
  - crates/pyscf-dft/tests/cam_b3lyp_h2o_rsh.rs
  - crates/pyscf-dft/tests/vv10_energy_match.rs
autonomous: true
requirements: [DFT-05, DFT-06]
user_setup: []

must_haves:
  truths:
    - "Range-separated hybrids (omega, alpha, beta) compute long/short-range exact-exchange K by setting PTR_RANGE_OMEGA = env[8] around the standard int2e call (NOT distinct int2e_lr_*/int2e_sr_* symbols), restored after — CAM-B3LYP/H2O matches upstream"
    - "VV10 non-local correlation (mf.nlc='VV10' + a coarser nlcgrids) produces upstream-matching energies via the ported pure-Python _vv10nlc double-loop"
  artifacts:
    - path: "crates/pyscf-gto/src/intor.rs"
      provides: "range-coulomb env[8] set/restore on the int2e intor path (with_range_coulomb-equivalent)"
      contains: "omega"
    - path: "crates/pyscf-dft/src/veff.rs"
      provides: "RSH SR/LR branch in get_veff (omega != 0), mirroring rks.py:108-129"
      contains: "omega"
    - path: "crates/pyscf-dft/src/vv10.rs"
      provides: "VV10 NLC: nr_nlc_vxc + _vv10nlc double-loop port (Pitfall 4: pure-Python, not C VXC_vv10nlc) + nlcgrids"
      contains: "vv10"
  key_links:
    - from: "crates/pyscf-dft/src/veff.rs"
      to: "pyscf_gto range-coulomb env[8] setter"
      via: "get_k_with_omega sets env[8]=±omega around int2e (DFT-05)"
      pattern: "omega"
    - from: "crates/pyscf-dft/src/vv10.rs"
      to: "pyscf_algebra::oracle_sum"
      via: "inner-grid double-loop reductions (bit-exact)"
      pattern: "oracle_sum"
---

<objective>
Add range-separated hybrids (DFT-05) and VV10 non-local correlation (DFT-06) on top of the core RKS/UKS path. RSH is implemented via the range-coulomb `env[8]` mechanism (RESEARCH CORRECTION: NOT literal `int2e_lr_*`/`int2e_sr_*` symbols — those do not exist in cintx). VV10 ports the pure-Python `_vv10nlc` double-loop (Pitfall 4: the C `VXC_vv10nlc` is PySCF's own libdft, not in any sibling crate).

Purpose: CAM-B3LYP-class functionals and VV10-corrected functionals complete the v1 XC surface. RSH resolves the open question A5 (whether a small pyscf-gto env-slot setter suffices or a cintx gap-closure is needed).
Output: CAM-B3LYP/H2O RSH parity + VV10 energy match.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-dft/04-CONTEXT.md
@.planning/phases/04-dft/04-RESEARCH.md
@.planning/phases/04-dft/04-PATTERNS.md

<interfaces>
From crates/pyscf-gto/src/intor.rs:
- pub fn intor(mol: &Mole, name: &str) -> Result<IntorOutput, PyscfRsError>  (line 69)
- pub fn intor_with_auxmol(...) (line 424)
- the int2e path dispatches to cintx; cintx READS env[8] = PTR_RANGE_OMEGA (verified) but a safe-API omega setter was NOT found in cintx-rs/cintx-ops src (Open Question A5)

From crates/pyscf-dft/src/veff.rs (04-06):
- default_get_veff = J + Vxc - hyb*K with a marked `// RSH branch (omega != 0): DFT-05, filled by 04-07` seam
- hybrid_coeff/rsh_coeff(xc_code, spin) -> (omega, alpha, hyb) on NumInt

From crates/pyscf-scf get_jk / get_k (Phase 3): the K path RSH reuses.

RSH branch (rks.py:108-129, RESEARCH Pattern 4):
  (omega, alpha, hyb) = rsh_and_hybrid_coeff(xc, spin)
  omega == 0  -> standard hybrid: vk = hyb * K           (already done in 04-06)
  omega != 0  -> vk_full = hyb*K ; vk_lr = (alpha-hyb)*K_with_omega(+omega) ; vk = vk_full + vk_lr
  env[8] > 0 -> erf(ωr)/r (long-range); env[8] < 0 -> short-range complement; restore env[8] after.

VV10 (numint.py:526-538 commented pure-Python ref, RESEARCH Code Examples §4):
  Kvv=Bvv*1.5*π*(9π)^(-1/6); Beta=((3/Bvv²)^0.75)/32; default Bvv=5.9, Cvv=0.0093
  per outer grid point i, double-loop over inner (vv) grid: R2=|vvcoords-coords[i]|²; gp/g/gt; T=RpW/(g*gp*gt);
  F=-1.5*ΣT; U=ΣT(1/g+1/gt); W=ΣT(1/g+1/gt)R2 — use oracle_sum for inner reductions.
  A1: query libxc_rs NlcCoefficients per-functional; only the bare 'VV10' default is 5.9/0.0093.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Range-coulomb env[8] setter on pyscf-gto + RSH branch in get_veff (DFT-05)</name>
  <files>crates/pyscf-gto/src/intor.rs, crates/pyscf-dft/src/veff.rs, crates/pyscf-gto/tests/range_coulomb_env.rs, crates/pyscf-dft/tests/cam_b3lyp_h2o_rsh.rs</files>
  <behavior>
    - an int2e call with omega>0 produces the long-range erf(ωr)/r ERI; omega<0 the short-range complement; omega=0 the standard ERI — matching upstream mol.with_range_coulomb(omega) on a small fixture
    - env[8] is RESTORED to its prior value after the ranged int2e call (no leak across calls)
    - get_veff with omega!=0 forms vk_full + vk_lr per rks.py:108-129
    - dft.RKS(mol, xc='cam-b3lyp').kernel() on H2O/cc-pVDZ matches upstream within 1 µHartree
  </behavior>
  <read_first>
    - crates/pyscf-gto/src/intor.rs (the `pub fn intor` dispatcher at line 69 + the int2e/int3c2e dispatch; how _env is projected — find where env[8]/PTR_RANGE_OMEGA could be set/restored)
    - crates/pyscf-gto/src/projection.rs (the _env flat-array projection from make_env — the env[8] slot lives here)
    - ~/Documents/workspace/cintx/.planning/{STATE.md, research/PITFALLS.md} (the env[8]/PTR_RANGE_OMEGA mechanism — cintx reads it; confirm whether a safe setter exists; READ ONLY)
    - pyscf/dft/rks.py (get_veff RSH branch 108-129) + pyscf/gto/mole.py with_range_coulomb (the env[8] set/restore reference)
    - crates/pyscf-dft/src/veff.rs (the `// RSH branch ... filled by 04-07` seam from 04-06)
    - .planning/phases/04-dft/04-RESEARCH.md (Open Question A5 + Pattern 4 + Pitfall 1: NOT int2e_lr_*/int2e_sr_*)
  </read_first>
  <action>
    Implement a range-coulomb omega set/restore on the `pyscf-gto::intor` path (the `with_range_coulomb`-equivalent): set `_env[8] = ±omega` (`PTR_RANGE_OMEGA`) before the standard `int2e` call and restore the prior value after (RAII/guard so it restores on error too). Do NOT introduce `int2e_lr_*`/`int2e_sr_*` symbols (Pitfall 1 — they don't exist in cintx; the standard `int2e` reads env[8]). Expose this as a `get_k_with_omega(mol, dm, omega)`-style helper or an `intor` variant that takes an omega. RESOLVE Open Question A5: if the safe `pyscf-gto` path can set/restore env[8] (the omega slot is a single f64 in `_env`), do it here; if cintx's safe API blocks env mutation, file a cintx gap-closure (cintx#11-style status marker) and document the blocker in the SUMMARY + STATE.md, sequencing the CAM-B3LYP assertion behind it. Implement the RSH branch in `veff.rs` `get_veff` (replace the marked seam): when `omega != 0`, form `vk_full = hyb*K` + `vk_lr = (alpha-hyb)*K_with_omega(+omega)` per rks.py:108-129; adjust the exchange energy term accordingly. Write `crates/pyscf-gto/tests/range_coulomb_env.rs` asserting the ranged int2e matches upstream `with_range_coulomb` AND that env[8] is restored. Fill `crates/pyscf-dft/tests/cam_b3lyp_h2o_rsh.rs` (remove `#[ignore]`) with the CAM-B3LYP/H2O ≤1 µHartree oracle assertion (gate it `#[cfg(feature="libxc")]` if CAM-B3LYP is libxc-only on the corpus subset, CI-only; otherwise CPU/xcfun default).
  </action>
  <verify>
    <automated>cargo test --profile release-oracle -p pyscf-gto range_coulomb_env</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --profile release-oracle -p pyscf-gto range_coulomb_env` passes: ranged int2e matches upstream with_range_coulomb AND env[8] is restored after the call (behavior assertion)
    - `intor.rs` does NOT introduce `int2e_lr_`/`int2e_sr_` symbols (source assertion — Pitfall 1)
    - `veff.rs` RSH branch forms vk_full + vk_lr for omega!=0 (source assertion vs rks.py:108-129)
    - CAM-B3LYP/H2O matches upstream ≤1 µHartree (gated CI-only if libxc-only; else CPU default) — DFT-05; OR Open Question A5 resolved with a documented cintx gap-closure marker if env-mutation is blocked
  </acceptance_criteria>
  <done>RSH works via env[8] set/restore (not phantom intor symbols); CAM-B3LYP matches upstream; A5 is resolved (either in pyscf-gto or via a tracked cintx gap-closure).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VV10 non-local correlation (DFT-06)</name>
  <files>crates/pyscf-dft/src/vv10.rs, crates/pyscf-dft/src/lib.rs, crates/pyscf-dft/tests/vv10_energy_match.rs</files>
  <behavior>
    - the VV10 _vv10nlc double-loop produces the NLC energy/potential matching upstream for the bare 'VV10' default (Bvv=5.9, Cvv=0.0093) on a small fixture
    - mf.nlc='VV10' with a coarser nlcgrids (a second pyscf-grids instance) adds the NLC contribution to the RKS energy matching upstream
    - inner-grid reductions use oracle_sum (bit-exact)
  </behavior>
  <read_first>
    - crates/pyscf-dft/src/{numint,lib}.rs (from 04-06 — NumInt + the lib module wiring; add `pub mod vv10;` + re-export here)
    - pyscf/dft/numint.py (_vv10nlc 471-545 incl. the commented pure-Python reference 526-538; nr_nlc_vxc 1347)
    - crates/pyscf-grids/src/lib.rs (Grids — the nlcgrids is a second, coarser instance)
    - crates/pyscf-df/src/df_jk.rs (the nested-loop + oracle_sum inner-reduction pattern — the closest analog for the VV10 double-loop)
    - ~/Documents/workspace/libxc_rs/src/ (NlcCoefficients per-functional — A1: query, don't hardcode except the bare 'VV10' default; READ ONLY)
    - .planning/phases/04-dft/04-RESEARCH.md (Code Examples §4 + Pitfall 4 + A1)
  </read_first>
  <action>
    Implement `vv10.rs`: port the pure-Python `_vv10nlc` double-loop (numint.py:526-538) — NOT the C `VXC_vv10nlc` (Pitfall 4). Per outer grid point, double-loop over the inner (vv) grid computing R2/gp/g/gt/T and the F/U/W accumulators; use `pyscf_algebra::oracle_sum` for the inner reductions (bit-exact). Implement `nr_nlc_vxc` orchestrating it over a coarser `nlcgrids` (a second `pyscf-grids::Grids` instance). Constants: query `libxc_rs::NlcCoefficients` per-functional where available; hardcode only the bare `'VV10'` default Bvv=5.9/Cvv=0.0093 (A1). Wire `mf.nlc='VV10'` + `mf.nlcgrids` into the RKS path (the NLC energy adds to Exc; the NLC potential adds to Vxc). Add `pub mod vv10;` + re-export in `lib.rs`. Fill `crates/pyscf-dft/tests/vv10_energy_match.rs` (remove `#[ignore]`) with a VV10 energy-match oracle assertion (e.g. an explicit `nlc='VV10'` RKS run) ≤1 µHartree under release-oracle (CPU/xcfun default unless the functional is libxc-only). Provenance doc-comment names numint.py:471-545 + DFT-06 + Pitfall 4.
  </action>
  <verify>
    <automated>cargo test --profile release-oracle -p pyscf-dft vv10_energy_match</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --profile release-oracle -p pyscf-dft vv10_energy_match` passes — DFT-06
    - `vv10.rs` ports the pure-Python double-loop and does NOT reference `VXC_vv10nlc` (source assertion — Pitfall 4)
    - inner-grid reductions use `pyscf_algebra::oracle_sum` (source assertion)
    - the nlcgrids is a separate (coarser) Grids instance (source assertion)
  </acceptance_criteria>
  <done>VV10 NLC matches upstream via the ported pure-Python double-loop over a coarser nlcgrids; inner reductions are bit-exact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (internal) | env[8] mutation must be restored; otherwise a leaked omega corrupts subsequent integral calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07a | Tampering | env[8] omega leak across intor calls | mitigate | RAII/guard restores `_env[8]` after the ranged int2e even on error; tested in `range_coulomb_env` |
| T-04-07b | Information Disclosure (numerical-correctness) | VV10 inner double-loop reduction order | mitigate | `pyscf_algebra::oracle_sum` for inner reductions + release-oracle; oracle energy-match assertion |
| T-04-07c | Tampering | porting C kernel instead of pure-Python fallback | mitigate | port the documented pure-Python `_vv10nlc` (Pitfall 4); source-asserted no `VXC_vv10nlc` reference |
| — | Spoofing / Repudiation / DoS / Elevation | n/a | accept | Internal numeric paths; no auth/network/secrets; no new packages. |
</threat_model>

<verification>
- `cargo test --profile release-oracle -p pyscf-gto range_coulomb_env` passes (DFT-05 mechanism)
- `cargo test --profile release-oracle -p pyscf-dft vv10_energy_match` passes (DFT-06)
- CAM-B3LYP/H2O ≤1 µHartree (DFT-05; CI-gated if libxc-only)
- `cargo run -p xtask --bin check-dependency-wall` passes
</verification>

<success_criteria>
RSH works via env[8] set/restore (not phantom integral symbols) with CAM-B3LYP matching upstream; VV10 NLC matches upstream via the pure-Python double-loop port over a coarser nlcgrids; Open Question A5 is resolved.
</success_criteria>

<output>
Create `.planning/phases/04-dft/04-07-SUMMARY.md` when done.
</output>
