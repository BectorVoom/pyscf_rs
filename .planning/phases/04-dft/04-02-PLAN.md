---
phase: 04-dft
plan: 02
type: execute
wave: 1
depends_on: []
status: PENDING_LIBXC_RS_FEATURE_GATE
files_modified:
  - "~/Documents/workspace/libxc_rs/Cargo.toml"
  - "~/Documents/workspace/libxc_rs/src/kernel/lda.rs"
  - "~/Documents/workspace/libxc_rs/src/kernel/gga.rs"
  - "~/Documents/workspace/libxc_rs/src/kernel/mgga.rs"
  - "~/Documents/workspace/libxc_rs/src/eval/dispatch.rs"
  - "~/Documents/workspace/libxc_rs/tools/generate_kernel_reexports.py"
  - .planning/STATE.md
autonomous: false
requirements: [DFT-03]
user_setup: []

must_haves:
  truths:
    - "PLACEHOLDER / COORDINATION PLAN: do not execute as normal pyscf-rs work — this modifies the READ-ONLY sibling repo ~/Documents/workspace/libxc_rs and is structurally the Phase 2 cintx-ECP coordination (cintx#11) precedent"
    - "When libxc_rs grows a [features] block + cfg-gated per-functional dispatch: pyscf-rs's `libxc` feature pulls ONLY the corpus subset, keeping even the gated build to minutes instead of ~6h/266 crates"
    - "After execution: a libxc_rs build with only the corpus-functional features enabled compiles in minutes and exposes lda_x/lda_c_vwn/gga_x_pbe/gga_c_pbe/gga_x_b88/gga_c_lyp/hyb_gga_xc_b3lyp/hyb_gga_xc_cam_b3lyp + one meta-GGA + the VV10 gga deps"
  artifacts:
    - path: "~/Documents/workspace/libxc_rs/Cargo.toml"
      provides: "a [features] block making the 266 libxc-kernel-* deps optional + per-functional features (xcfun-rs [features] block is the canonical model)"
      contains: "[features]"
    - path: "~/Documents/workspace/libxc_rs/src/eval/dispatch.rs"
      provides: "cfg-gated per-functional match arms so only enabled kernels are referenced"
      contains: "cfg(feature"
  key_links:
    - from: "crates/pyscf-dft/Cargo.toml"
      to: "libxc_rs per-functional features"
      via: "the `libxc` feature forwards the corpus-subset functional features"
      pattern: "libxc_rs"
---

<objective>
Cross-crate coordination / gap-closure (D-04): add a `[features]` block + `cfg`-gated per-functional kernel dispatch to the sibling `libxc_rs` repo so consumers can enable only the functionals they need, instead of unconditionally path-depending on all 266 `libxc-kernel-*` crates (~6h compile). This is the libxc-side prerequisite for a tractable `--features libxc` build in `pyscf-rs`.

Purpose: Without this, even the off-by-default `libxc` feature (D-03) triggers the full 266-crate compile the moment it is enabled. This plan makes the gated build minutes, not hours.
Output: A `[features]`-gated `libxc_rs` (in the sibling repo) whose corpus-subset build is fast.

**STATUS MARKER — `PENDING_LIBXC_RS_FEATURE_GATE`.** This plan is the structural twin of the Phase 2 `02-10-PLAN.md` cintx-ECP coordination (`status: PENDING_CINTX_ECP_MERGE`). It modifies a READ-ONLY-during-research sibling repo. Per the Phase 2 precedent it is tracked with a status marker, and the libxc bit-exact path (04-09's CI gate + the `--features libxc` assertions in 04-05/04-06) MAY be sequenced behind it landing. The default-feature pyscf-rs build and the entire xcfun-default DFT path (04-04 through 04-08) do NOT depend on this plan and proceed regardless.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-dft/04-CONTEXT.md
@.planning/phases/04-dft/04-RESEARCH.md
@.planning/phases/02-gto/02-10-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: confirm libxc_rs feature-gate workstream readiness</name>
  <action>Coordination/sequencing gate — see what-built / how-to-verify / resume-signal below.</action>
  <what-built>Nothing yet — this is the coordination/sequencing gate. The executor must NOT modify the libxc_rs sibling repo nor run any `cargo build --features libxc` until the developer confirms the libxc_rs feature-gate workstream is ready to land (mirroring how 02-10 waited on the cintx ECP merge).</what-built>
  <how-to-verify>
    1. Confirm whether the libxc_rs `[features]` + cfg-gated-dispatch workstream is ready to be implemented in `~/Documents/workspace/libxc_rs` (its own repo, its own PR/issue, like cintx#11).
    2. If NOT ready: leave this plan `status: PENDING_LIBXC_RS_FEATURE_GATE`; the xcfun-default DFT path (04-04..04-08) ships independently and the `--features libxc` bit-exact assertions in 04-05/04-06/04-09 stay `#[cfg(feature="libxc")]`-gated and CI-only.
    3. If ready: confirm the corpus-functional closure to gate ON — read `~/Documents/workspace/libxc_rs/src/model/lda_functional.rs` + `generated_hybrid.rs`/`HybridTerm` metadata to enumerate the TRANSITIVE component IDs of each compound (b3lyp expands to LDA/B88/LYP/VWN components — all must be in the feature set). NEVER run `cargo build` on libxc_rs to discover this; read source only (~6h freeze, user memory).
  </how-to-verify>
  <resume-signal>Type "approved: implement libxc gate" to proceed with the sibling-repo edits, or "approved: keep pending" to leave the status marker and ship the xcfun-default path only.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Add [features] block + cfg-gated per-functional dispatch to libxc_rs (sibling repo)</name>
  <files>~/Documents/workspace/libxc_rs/Cargo.toml, ~/Documents/workspace/libxc_rs/src/kernel/lda.rs, ~/Documents/workspace/libxc_rs/src/kernel/gga.rs, ~/Documents/workspace/libxc_rs/src/kernel/mgga.rs, ~/Documents/workspace/libxc_rs/src/eval/dispatch.rs, ~/Documents/workspace/libxc_rs/tools/generate_kernel_reexports.py</files>
  <read_first>
    - ~/Documents/workspace/libxc_rs/Cargo.toml (the 266 unconditional `libxc-kernel-*` path-deps; confirm NO existing [features] block)
    - ~/Documents/workspace/xcfun_rs/crates/xcfun-rs/Cargo.toml (the CANONICAL [features]-block model D-04 must mirror — per-functional feature → optional dep mapping)
    - ~/Documents/workspace/libxc_rs/src/eval/dispatch.rs (the enum-based per-functional `match` arms, e.g. `LdaFunctional::LdaX => launch_lda_x(...)` — each arm gets cfg-gated)
    - ~/Documents/workspace/libxc_rs/src/kernel/{lda,gga,mgga}.rs (the auto-generated `pub use libxc_kernel_* as *` re-export façades — cfg-gate each)
    - ~/Documents/workspace/libxc_rs/tools/generate_kernel_reexports.py (the generator that emits the re-export façades — must emit `#[cfg(feature = ...)]` per functional)
    - ~/Documents/workspace/libxc_rs/src/model/lda_functional.rs + generated_hybrid.rs (compound-functional transitive component closure for the corpus subset)
  </read_first>
  <action>
    In `~/Documents/workspace/libxc_rs/Cargo.toml`: add `optional = true` to each of the 266 `libxc-kernel-*` path-deps, and add a `[features]` block with one feature per kernel (`lda_x = ["dep:libxc-kernel-lda-x"]`, etc.) plus convenience aggregate features for the corpus compounds (`b3lyp` forwarding its transitive component features `lda_x`/`lda_c_vwn`/`gga_x_b88`/`gga_c_lyp`/`hyb_gga_xc_b3lyp`; `pbe`; `svwn`; `cam_b3lyp`; the chosen meta-GGA; the VV10 gga deps). Mirror the `xcfun-rs/Cargo.toml` `[features]`-block shape exactly. Modify `tools/generate_kernel_reexports.py` so the generated `src/kernel/{lda,gga,mgga}.rs` re-export façades emit `#[cfg(feature = "<functional>")]` on each `pub use libxc_kernel_* as *;` line; regenerate those files. In `src/eval/dispatch.rs`, `#[cfg(feature = "<functional>")]`-gate each per-functional `match` arm (the dispatch is enum-based, so gating is mechanical) and provide a `#[cfg(not(...))]` fallback arm returning a clear "functional not compiled in" error. Do NOT change the public API surface (`lookup_by_name`/`lookup_by_id`/`BatchEvaluator`/`*Input`/`*Output`) — only the kernel availability. Track this work in the libxc_rs repo's own issue/PR (cintx#11 precedent). Do NOT run `cargo build`/`cargo test` on libxc_rs as a default verify — see the verify block for the corpus-subset-only build.
  </action>
  <verify>
    <automated>MISSING — sibling-repo build; the ONLY libxc compile is the corpus-subset-feature build, which must run on a heavily-cached dedicated machine, never the default verify. Confirm the gate via source assertion: grep -c 'cfg(feature' ~/Documents/workspace/libxc_rs/src/eval/dispatch.rs returns > 0</automated>
  </verify>
  <acceptance_criteria>
    - `~/Documents/workspace/libxc_rs/Cargo.toml` has a `[features]` block and every `libxc-kernel-*` dep is `optional = true` (source assertion)
    - `src/eval/dispatch.rs` per-functional match arms are `cfg(feature = ...)`-gated (source assertion: `grep -v '^//' src/eval/dispatch.rs | grep -c 'cfg(feature'` > 0)
    - A build of libxc_rs with ONLY the corpus-subset features enabled (`lda_x,lda_c_vwn,gga_x_pbe,gga_c_pbe,gga_x_b88,gga_c_lyp,hyb_gga_xc_b3lyp,hyb_gga_xc_cam_b3lyp` + one meta-GGA + VV10 gga deps) compiles in minutes (run ONLY on the dedicated cached machine; this is NOT a per-task verify)
    - The public API (`lookup_by_name`, `BatchEvaluator`, `LdaInput`/`GgaInput`/`MggaInput`) is unchanged — pyscf-dft's libxc backend code (04-05) compiles against it unmodified when `--features libxc` is on
  </acceptance_criteria>
  <done>libxc_rs has per-functional features + cfg-gated dispatch; a corpus-subset build is minutes not hours; the public API is unchanged so the pyscf-dft libxc path can target it.</done>
</task>

<task type="auto">
  <name>Task 2: Record coordination status in STATE.md</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (the "Blockers/Concerns" section has the "libxc_rs re-enable note"; the "Quick Tasks Completed" / cross-crate-coordination tracking style)
  </read_first>
  <action>
    Add a Blockers/Concerns or cross-crate-coordination entry recording the libxc_rs D-04 per-functional-feature workstream status (mirroring how the cintx ECP coordination (cintx#11) was tracked). State whether the gate landed or remains `PENDING_LIBXC_RS_FEATURE_GATE`, and note that the xcfun-default DFT path is independent of it.
  </action>
  <verify>
    <automated>grep -c -i 'libxc' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` records the libxc_rs feature-gate coordination status and that the xcfun-default path is independent
  </acceptance_criteria>
  <done>Coordination status is tracked in STATE.md like the Phase 2 cintx-ECP precedent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pyscf-rs → sibling libxc_rs repo | a cross-repo change; the only place the ~6h libxc compile risk lives |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Denial of Service | accidental full libxc_rs compile (~6h freeze) | mitigate | This plan NEVER runs a default `cargo build` on libxc_rs; the only build is the corpus-subset-feature build on a dedicated cached machine; all discovery is source-read-only (user-memory hard constraint) |
| T-04-SC | Tampering | sibling-repo dependency surface | mitigate | All 266 `libxc-kernel-*` are pre-existing path-deps in the sibling repo (no new registry installs); change is purely making them optional + cfg-gated; per 04-RESEARCH Package Legitimacy Audit slopcheck is N/A (path deps) |
| — | Spoofing / Repudiation / Info Disclosure / Elevation | n/a | accept | Build-config + dispatch-gating on a numeric library; no auth/network/secrets/untrusted-input surface — N/A |
</threat_model>

<verification>
- Source assertion: `[features]` block + `optional = true` deps present in libxc_rs Cargo.toml
- Source assertion: cfg-gating present in `src/eval/dispatch.rs`
- Corpus-subset build (dedicated cached machine only) compiles in minutes
- STATE.md records the coordination status
</verification>

<success_criteria>
libxc_rs gains per-functional features + cfg-gated dispatch (or stays cleanly `PENDING` with the xcfun-default path shipping independently); the public API is preserved so the pyscf-dft libxc backend targets it unchanged.
</success_criteria>

<output>
Create `.planning/phases/04-dft/04-02-SUMMARY.md` when done (or record PENDING status if the checkpoint defers it).
</output>
