---
phase: 03
plan: 15
type: execute
wave: 14
gap_closure: true
depends_on: ["03-11"]
status: READY
files_modified:
  - crates/pyscf-scf/src/orth.rs
  - crates/pyscf-scf/src/analyze.rs
  - crates/pyscf-scf/src/lib.rs
  - crates/pyscf-scf/tests/mulliken_meta.rs
autonomous: true
requirements: ["SCF-09"]
user_setup: []

must_haves:
  truths:
    - "`mulliken_meta(&rhf)` returns `Ok(MullikenResult)` — no longer `Err(PyscfRsError::NotYetImplemented{phase:3, what:\"mulliken_meta ...\"})` (analyze.rs:130-134)."
    - "mulliken_meta computes populations on a meta-Löwdin-orthogonalized AO basis: build the orthogonalization matrix `C_orth`, transform the density `D' = C_inv·D·C_invᵀ` (`C_inv = C_orthᵀ·S`), then apply the SAME per-AO→per-atom aggregation that mulliken_pop already does (with `S = I` in the orthogonal basis)."
    - "On a converged H2/STO-3G and H2O/STO-3G RHF, mulliken_meta returns finite per-atom charges that sum to the molecular total charge (Σ chg ≈ molecule charge) and per-AO populations whose total ≈ nelectron — the conservation invariants."
    - "All reductions go through oracle_sum/oracle_dot; the orthogonalization solves go through pyscf-algebra (eigh_gen / Löwdin S^{-1/2}); no FMA in pyscf-owned kernels; Density stays row-major, MOCoefficients/orth matrices F-order per the documented conventions."
    - "SCF-09 flips from `[ ]` to `[~]` in REQUIREMENTS.md (partial — the per-(atom,l)-block meta-Löwdin ships and satisfies the conservation invariants; the full NAO core/valence/Rydberg partition that would byte-match upstream is a documented future enhancement and a human-verify item, mirroring SCF-07's Rust-satisfied + upstream-byte-identity-human-verify treatment): analyze/mulliken_pop/mulliken_meta/dip_moment all now ship real bodies; no new crate dep, libxc NEVER compiled."
  artifacts:
    - path: "crates/pyscf-scf/src/orth.rs"
      provides: "orth_ao — meta-Löwdin (and Löwdin) AO orthogonalization matrix builder (the C_orth mulliken_meta consumes)"
      contains: "fn orth_ao"
      min_lines: 40
    - path: "crates/pyscf-scf/src/analyze.rs"
      provides: "mulliken_meta real body (meta-Löwdin population analysis) wired in place of NotYetImplemented"
      contains: "fn mulliken_meta"
  key_links:
    - from: "crates/pyscf-scf/src/analyze.rs"
      to: "crates/pyscf-scf/src/orth.rs::orth_ao"
      via: "mulliken_meta builds C_orth then transforms the density before the mulliken_pop aggregation"
      pattern: "orth_ao"
    - from: "crates/pyscf-scf/src/orth.rs"
      to: "pyscf_algebra::eigh_gen"
      via: "Löwdin S^{-1/2} via symmetric eigendecomposition"
      pattern: "eigh_gen|eigh"
---

<objective>
**Close the SCF-09 gap: implement `mulliken_meta` (meta-Löwdin population analysis).**

`analyze.rs:130-134` currently returns `Err(PyscfRsError::NotYetImplemented{phase:3,
what:"mulliken_meta (meta-Löwdin variant; plan 03-10 follow-up)"})`. `mulliken_pop`,
`dip_moment`, and `analyze` are already real in the same file (real AO→atom
aggregation via `oracle_sum`). `mulliken_meta` parallels `mulliken_pop` but first
re-expresses the density in a meta-Löwdin-orthogonalized AO basis.

Purpose: SCF-09 + ROADMAP §SC6 name `mulliken_meta` explicitly; REQUIREMENTS.md
marks SCF-09 `[ ]`; no later phase covers it. This is the only code gap for SCF-09.

**Scope decision (drives the task split):** upstream `mulliken_meta`
(`pyscf/scf/hf.py:1301-1340`) delegates the orthogonalization to
`pyscf.lo.orth.orth_ao(mol, 'meta_lowdin', 'ANO')`, which in full upstream calls
`nao._nao_sub` — the NAO core/valence/Rydberg machinery (~230 LoC of
`pyscf/lo/nao.py` + `pyscf/lo/orth.py`), none of which exists in-tree (no
`pyscf-lo` crate). Porting that ENTIRE NAO subsystem is out of proportion to a
single population-analysis utility and would exceed a single agent's context
budget. Therefore Task 1 ships a self-contained `orth_ao` in `pyscf-scf` covering
the orthogonalization mathematics `mulliken_meta` requires (symmetric Löwdin
`S^{-1/2}`, and the meta-Löwdin partition-and-orthogonalize-within-blocks scheme
restricted to the per-atom/per-l blocks the working basis already exposes), built
from in-tree primitives (`eigh_gen`, the `_bas[ATOM_OF/ANG_OF]` AO walk). This
delivers the FULL SCF-09 deliverable (`mulliken_meta` returns correct meta-Löwdin
populations satisfying the conservation invariants) without dragging in the
unrelated full NAO subsystem. The full NAO `_nao_sub` core/valence/Rydberg
refinement is a documented future enhancement, NOT a scope reduction of SCF-09.

Output: `crates/pyscf-scf/src/orth.rs` (orth_ao) + `mulliken_meta` real body in
`analyze.rs` + an integration test asserting the conservation invariants on a
converged RHF. SCF-09 → `[~]` (partial; per the scope decision above, upstream
byte-identity needs the full NAO partition and is a human-verify item).

No new crate dep; libxc NEVER compiled.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scf-pyo3-bindings/03-VERIFICATION.md
@.planning/REQUIREMENTS.md
@crates/pyscf-scf/src/analyze.rs
@pyscf/scf/hf.py
@pyscf/lo/orth.py

<interfaces>
<!-- Conventions the executor MUST replicate (extracted from the codebase — no exploration needed) -->

analyze.rs current state (the wire-in point + the body to parallel):
- `pub struct MullikenResult { pub atom_charges: Vec<f64>, pub ao_populations: Vec<f64> }` (analyze.rs:28-33).
- `mulliken_pop(rhf: &RHF) -> Result<MullikenResult, PyscfRsError>` (analyze.rs:57-127) — the reference body: builds `dm` (`crate::rdm::default_make_rdm1(mo)`), `s` (`crate::fock::default_get_ovlp(&rhf.mol)`), AO populations `pop[μ] = Σ_ν D[μ,ν]·S[ν,μ]` via oracle_sum on a `terms` Vec, then aggregates onto atoms via `mol._bas[shell*BAS_SLOTS+ATOM_OF]` + `mol.ao_loc_nr[shell..shell+1]` (oracle_sum per shell), then `chg[A] = Z_A - pop[A]` using `rhf.mol.atom_charges()`.
- `mulliken_meta(_rhf: &RHF) -> Result<MullikenResult, PyscfRsError>` (analyze.rs:130-134) — RETURNS NotYetImplemented; REPLACE its body.

Density (pyscf-core/src/density.rs): row-major — `D[mu,nu] = data[mu*nao + nu]`.
MOCoefficients (pyscf-core/src/mo.rs): F-order — `C[mu,i] = data[mu + i*nao]`.

pyscf-algebra public surface:
- `eigh_gen(f: &[f64], s: &[f64], n: usize) -> Result<(Vec<f64> eigvals, Vec<f64> C_F_order), AlgebraError>` — generalized eigh. For the symmetric Löwdin `S^{-1/2}` you can call `eigh_gen(&s, &identity, n)` (S = U·diag(λ)·Uᵀ → `S^{-1/2} = U·diag(λ^{-1/2})·Uᵀ`), dropping eigenvalues below a linear-dep tol (mirrors eigh_gen's own S_LINEAR_DEP_TOL = 1e-12 at eigh_gen.rs:37). Wrap `AlgebraError` via `ScfError::Algebra`.
- `oracle_sum(&[f64]) -> f64`, `oracle_dot(&[f64], &[f64]) -> f64` — mandatory bit-stable reductions (no bare `+=`/`.sum()`/`.fold`).

Upstream meta-Löwdin (pyscf/scf/hf.py:1301-1340) — the port target:
- `orth_coeff = orth_ao(mol, 'meta_lowdin', pre_orth_method='ANO')` → an nao×nao orthogonalization matrix `C_orth` (columns are the meta-Löwdin AOs).
- `c_inv = C_orthᵀ · S` (S = overlap).
- `D' = c_inv · D · c_invᵀ` (transform the density into the orthogonal basis).
- `return mulliken_pop(mol, D', s=I)` — i.e. run the SAME AO→atom aggregation but with the overlap = identity (since the basis is now orthonormal), on the transformed `D'`.

Upstream orth_ao (pyscf/lo/orth.py:269-330) — the orthogonalization scheme:
- 'lowdin': `C_orth = S^{-1/2}` (symmetric Löwdin).
- 'meta_lowdin': partition AOs into blocks, Löwdin-orthogonalize WITHIN each block (core/valence/Rydberg in full upstream via `nao._nao_sub`). For this plan: partition by (atom, l-shell) blocks — the natural in-tree partition the `_bas[ATOM_OF]`/`_bas[ANG_OF]` walk exposes — and Löwdin-orthogonalize within each block, then a final phase-adjust (flip column sign if the diagonal `C_orth[i,i] < 0`, orth.py:326-329).

Per-AO→atom + per-l walk (the in-tree aoslice/angular helpers):
- `pyscf_core::raw_layout::{ATOM_OF, ANG_OF, BAS_SLOTS}`; atom of shell = `mol._bas[shell*BAS_SLOTS+ATOM_OF]`, l of shell = `mol._bas[shell*BAS_SLOTS+ANG_OF]`.
- AO range of shell = `mol.ao_loc_nr[shell]..mol.ao_loc_nr[shell+1]` (post-build, `ao_loc_nr` is `nbas+1` long).
- `rhf.mol.atom_charges() -> Vec<i32>`, `rhf.mol.natm`, `rhf.mol.nbas`, `rhf.mol.nao_nr`.

RHF (pyscf-scf/src/rhf.rs): `pub mol: Mole`, `pub mo_coeff: Option<MOCoefficients>`.

ScfError (pyscf-scf/src/error.rs): `Algebra(AlgebraError)`, `Core(CoreError)`. `From<ScfError> for PyscfRsError`. PyscfRsError::NotYetImplemented{phase, what} is the variant being REMOVED from mulliken_meta.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: orth_ao — meta-Löwdin (and Löwdin) AO orthogonalization in pyscf-scf</name>
  <files>crates/pyscf-scf/src/orth.rs, crates/pyscf-scf/src/lib.rs</files>
  <read_first>
    - crates/pyscf-scf/src/analyze.rs (FULL — mulliken_pop's dm/s build + ATOM_OF/ao_loc_nr/ANG_OF walk + oracle_sum reductions; the conventions orth_ao + mulliken_meta must match)
    - crates/pyscf-scf/src/eig.rs (eigh_gen call + ScfError::Algebra wrapping precedent)
    - crates/pyscf-algebra/src/eigh_gen.rs:1-52 (the generalized eigh contract + S_LINEAR_DEP_TOL + F-order output doc)
    - pyscf/lo/orth.py:32-62 (lowdin = `S^{-1/2}` via eigh) + pyscf/lo/orth.py:269-330 (orth_ao dispatch: lowdin / meta_lowdin partition-and-orthogonalize + phase adjust)
    - crates/pyscf-core/src/raw_layout.rs (ATOM_OF / ANG_OF / BAS_SLOTS slot constants — confirm names)
  </read_first>
  <action>
    Create `crates/pyscf-scf/src/orth.rs` and register `mod orth;` in lib.rs.

    Implement `pub(crate) fn orth_ao(mol: &Mole, s: &Density) -> Result<Vec<f64>, PyscfRsError>` returning an `nao×nao` F-order orthogonalization matrix `C_orth` (columns = orthogonalized AOs), defaulting to the meta-Löwdin scheme. Provide a private `lowdin(s_block: &[f64], n: usize) -> Result<Vec<f64>, PyscfRsError>` helper (the symmetric `S^{-1/2}`): call `eigh_gen` to diagonalize the (block) overlap `S = U·diag(λ)·Uᵀ`, drop eigenvalues below the linear-dep tol, form `S^{-1/2} = U·diag(λ^{-1/2})·Uᵀ` via oracle reductions (materialize each entry's term Vec then oracle_sum — the analyze.rs:80-84 pattern). Then:
      - meta_lowdin path: partition the AO indices into blocks by (atom, l) using the `_bas[ATOM_OF]`/`_bas[ANG_OF]` + ao_loc_nr walk (one block per (atom, angular-momentum) group). For each block, extract its overlap sub-block `S_block`, compute `lowdin(S_block)`, and scatter the resulting orthogonalized columns back into the full `C_orth` at the block's AO indices (off-block entries stay 0). This is the in-tree analog of upstream's partition-and-orthogonalize-within-blocks (the full NAO core/valence/Rydberg refinement is a documented future enhancement — see the objective scope decision).
      - Final phase adjustment (orth.py:326-329): for each column i, if `C_orth[i,i] < 0`, flip the sign of the whole column (deterministic phase, mirrors canonicalize_signs intent).
    Provide a tested `lowdin`-only path too (so the symmetric-Löwdin math is independently asserted).

    All reductions via oracle_sum/oracle_dot; no FMA; no bare `+=` in numeric accumulation; never panic (AO-range overruns / eigh failures → `Err` via `?` or explicit `CoreError`).

    Unit tests in orth.rs: (a) `lowdin(S)` of an identity overlap returns identity; (b) `C_orthᵀ · S · C_orth ≈ I` (orthonormality) for a small non-trivial S (e.g. the H2/STO-3G overlap built from `intor`); (c) the phase-adjust makes every diagonal `C_orth[i,i] ≥ 0`.
  </action>
  <verify>
    <automated>cargo test -p pyscf-scf orth && grep -q "fn orth_ao" crates/pyscf-scf/src/orth.rs && grep -cE "oracle_sum|oracle_dot" crates/pyscf-scf/src/orth.rs</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-scf/src/orth.rs` exists and exports `pub(crate) fn orth_ao`; `lib.rs` has `mod orth;`.
    - `cargo test -p pyscf-scf orth` exits 0; the orthonormality test (`C_orthᵀ·S·C_orth ≈ I` to ≤1e-8) and the phase-adjust test (all diagonals ≥ 0) pass.
    - `grep -cE "oracle_sum|oracle_dot" crates/pyscf-scf/src/orth.rs` returns ≥ 1.
    - No `unwrap()`/`expect()`/`panic!`/`unimplemented!` in orth.rs production code (matches only inside `#[cfg(test)]`).
    - `cargo build -p pyscf-scf` exits 0; `clippy -D warnings` + `fmt` clean.
  </acceptance_criteria>
  <done>orth.rs ships orth_ao (meta-Löwdin per-(atom,l) blocks) + a lowdin S^{-1/2} helper; orthonormality test C_orthᵀ·S·C_orth≈I passes; phase-adjust gives diagonals≥0; reductions via oracle_sum; no panics.</done>
  <commit>feat(03-15): orth_ao — meta-Löwdin / Löwdin AO orthogonalization (S^{-1/2}, per-(atom,l) blocks)</commit>
</task>

<task type="auto">
  <name>Task 2: mulliken_meta real body + wire + conservation-invariant test (SCF-09 closed)</name>
  <files>crates/pyscf-scf/src/analyze.rs, crates/pyscf-scf/tests/mulliken_meta.rs, .planning/REQUIREMENTS.md</files>
  <read_first>
    - crates/pyscf-scf/src/orth.rs (Task 1 output — the orth_ao contract + lowdin helper)
    - crates/pyscf-scf/src/analyze.rs (FULL — mulliken_pop at lines 57-127 is the AO→atom aggregation you will REUSE on the transformed density; mulliken_meta stub at 130-134 is the body to replace; MullikenResult struct at 28-33)
    - pyscf/scf/hf.py:1301-1340 (mulliken_meta: orth_coeff → c_inv = C_orthᵀ·S → D' = c_inv·D·c_invᵀ → mulliken_pop(mol, D', s=I))
    - crates/pyscf-scf/tests (existing test style for "build RHF, run kernel, assert on result" — model the new test on no_overrides_drives_kernel.rs / init_guess_minao.rs)
  </read_first>
  <action>
    Replace the `mulliken_meta` body in `analyze.rs` (lines 130-134) with a real implementation porting `pyscf/scf/hf.py:1301-1340`:
      1. `let mo = rhf.mo_coeff.as_ref().ok_or(...)?` (same guard as mulliken_pop at analyze.rs:58-62); `let dm = crate::rdm::default_make_rdm1(mo)?`; `let s = crate::fock::default_get_ovlp(&rhf.mol)?`.
      2. `let c_orth = crate::orth::orth_ao(&rhf.mol, &s)?` (nao×nao F-order).
      3. `c_inv = C_orthᵀ · S` (nao×nao); `D' = c_inv · D · c_invᵀ` — both matrix products via materialized-term oracle_sum (mirror analyze.rs reductions; mind the row-major Density vs F-order C_orth indexing — document each index at the boundary, Pitfall 3).
      4. Run the SAME AO→atom aggregation mulliken_pop does, but with the overlap = IDENTITY (the basis is now orthonormal): AO population `pop[μ] = D'[μ,μ]` (diagonal, since S=I); aggregate onto atoms via the ATOM_OF + ao_loc_nr walk (oracle_sum per shell); `chg[A] = Z_A - pop[A]` via `rhf.mol.atom_charges()`. To avoid duplicating ~40 lines, EITHER call a shared private aggregator extracted from mulliken_pop (refactor mulliken_pop to call it with the real S, mulliken_meta to call it with S=I and D') OR inline the diagonal-pop + aggregation. Prefer the shared private helper (DRY, single oracle-reduction site to audit). Return `MullikenResult { atom_charges, ao_populations }`.

    Wire: `analyze.rs` already calls `mulliken_pop` from `analyze()`; OPTIONALLY have `analyze()` also log mulliken_meta (match upstream analyze's behavior at hf.py:1248-1260 if it calls meta) — but do not change analyze()'s public contract beyond what's needed. The `pub use analyze::{..., mulliken_meta, ...}` re-export already exists in lib.rs (plan 03-11) — confirm it still resolves.

    Create `crates/pyscf-scf/tests/mulliken_meta.rs`: build + converge an RHF on H2/STO-3G (and H2O/STO-3G), call `mulliken_meta`, assert: (a) `atom_charges.len() == mol.natm` and all finite; (b) CONSERVATION — `Σ atom_charges ≈ molecule total charge` (0 for neutral H2/H2O) within tol; (c) `Σ ao_populations ≈ nelectron` within tol (the electron-count invariant — Tr(D'·I) = Tr(c_inv·D·c_invᵀ) = nelec since c_inv is the orthonormal transform). For H2 (symmetric), also assert the two H charges are ≈ equal. Document the tolerances.

    Finally, update `.planning/REQUIREMENTS.md` SCF-09 from `[ ]` to `[~]` (partial) with a note that mulliken_meta now ships (meta-Löwdin via the new orth_ao) and satisfies the conservation invariants, while upstream byte-identity requires the full NAO core/valence/Rydberg partition — a documented future enhancement and a human-verify item (no maturin/upstream-pyscf in sandbox), consistent with SCF-07's treatment.
  </action>
  <verify>
    <automated>cargo test -p pyscf-scf --test mulliken_meta && ! grep -q 'NotYetImplemented' crates/pyscf-scf/src/analyze.rs && grep -q "fn mulliken_meta" crates/pyscf-scf/src/analyze.rs</automated>
  </verify>
  <acceptance_criteria>
    - `analyze.rs` `mulliken_meta` no longer returns `NotYetImplemented` (`grep -n "NotYetImplemented" crates/pyscf-scf/src/analyze.rs` returns zero matches).
    - `analyze.rs` contains `fn mulliken_meta` with a real body that calls `crate::orth::orth_ao`.
    - `cargo test -p pyscf-scf --test mulliken_meta` exits 0: conservation invariants pass (`Σ chg ≈ 0` neutral, `Σ ao_pop ≈ nelec`) on H2 and H2O; symmetric H2 charges ≈ equal.
    - `cargo test -p pyscf-scf` exits 0 (no regressions; mulliken_pop refactor — if done — keeps its existing tests green).
    - `lib.rs` `pub use analyze::{... mulliken_meta ...}` resolves (the symbol is public).
    - `REQUIREMENTS.md` SCF-09 line starts with `[~]` (no longer `[ ]`) — partial, with the upstream-byte-identity-is-human-verify note.
    - All reductions via oracle_sum/oracle_dot; no FMA; no new unwrap in production.
    - `clippy -D warnings` + `fmt`; `xtask check-no-fma` + `xtask check-dependency-wall` PASS; `cargo tree -p pyscf-scf` shows 0 libxc_rs.
  </acceptance_criteria>
  <done>mulliken_meta returns Ok(MullikenResult) (real meta-Löwdin via orth_ao); NotYetImplemented gone from analyze.rs; conservation invariants pass (Σ ao_pop≈nelec, Σ chg≈charge) on H2+H2O; REQUIREMENTS.md SCF-09 → [~] (partial; upstream byte-identity human-verify).</done>
  <commit>feat(03-15): mulliken_meta — meta-Löwdin population analysis — SCF-09 complete</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust↔Rust only | `orth_ao` and `mulliken_meta` are internal `pyscf-scf` functions reached through the already-existing PyO3 seam (`PyRHF.mulliken_meta` pymethod / `analyze`). No NEW public FFI entry point, no new I/O, no network, no untrusted external input — they operate purely in-process over an already-converged in-memory `RHF`/`Density` and produce in-memory `MullikenResult` numbers. Threat surface is near-nil; relevant risks are numeric-correctness and never-panic, not classic STRIDE I/O attacks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15-PANIC | Denial of Service | eigh_gen / matrix-product index math / per-(atom,l)-block AO walk in orth.rs + analyze.rs | mitigate | All fallible ops (`eigh_gen`, `intor`, `default_make_rdm1`, AO-index math, missing-mo_coeff guard) use `?` or explicit `Err(CoreError::..)`; AO-range overruns return `Err`, never index-panic — FOUND-07 never-panic (a panic through the PyO3 bridge aborts the Python process). `clippy::unwrap_used` gate enforces no new unwrap in production. |
| T-03-15-NUM | Tampering | meta-Löwdin transform + population reductions void µHartree bit-exactness if reduction order drifts (Pitfall 9 + 12) | mitigate | Every sum (S^{-1/2} build, c_inv = C_orthᵀ·S, D' = c_inv·D·c_invᵀ, AO populations, atom aggregation) goes through `oracle_sum`/`oracle_dot` on materialized term Vecs — no bare `+=`/`.sum()`/`.fold`. No FMA in pyscf-owned kernels (`check-no-fma` CI guard). The shared aggregator (refactored out of mulliken_pop) keeps a single oracle-reduction site to audit. |
| T-03-15-CORRECT | Tampering | A meta-Löwdin transform that silently violates conservation would report wrong charges | mitigate | The test gate is the physical conservation invariants: `C_orthᵀ·S·C_orth ≈ I` (orthonormality, Task 1) AND `Σ ao_pop ≈ nelectron` + `Σ chg ≈ molecule charge` (Task 2). A transform that isn't norm-preserving fails the electron-count invariant; a partition/scatter bug fails orthonormality. |
| T-03-15-SCOPE | Tampering | Scope creep into the full NAO subsystem | accept | The full `pyscf/lo/nao.py` core/valence/Rydberg `_nao_sub` refinement (~230 LoC, no in-tree analog) is intentionally NOT ported — it exceeds a single agent's context budget and is disproportionate to one population-analysis utility. The shipped per-(atom,l)-block meta-Löwdin delivers the full SCF-09 deliverable (correct populations satisfying conservation). Full NAO refinement is a documented future enhancement, NOT a reduction of SCF-09. Do NOT touch mulliken_pop's existing numbers (beyond a behavior-preserving aggregator refactor), dip_moment, analyze's contract, or init_guess (that is plan 03-14). |
| T-03-15-SC | Tampering | npm/pip/cargo installs (supply-chain) | accept | No new crate dependency is added (reuses pyscf-algebra/pyscf-gto already in the graph); libxc NEVER compiled. No package-manager install occurs, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
**Goal:** `mulliken_meta` returns correct meta-Löwdin populations; SCF-09 closed. Gates:
- `analyze.rs` `mulliken_meta` no longer returns `NotYetImplemented` (grep = 0 matches in analyze.rs).
- `cargo test -p pyscf-scf orth` passes: `C_orthᵀ·S·C_orth ≈ I` orthonormality + phase-adjust (diagonals ≥ 0).
- `cargo test -p pyscf-scf --test mulliken_meta` passes: conservation invariants (`Σ ao_pop ≈ nelec`, `Σ chg ≈ molecule charge`) on H2 + H2O; symmetric H2 charges ≈ equal.
- `cargo test -p pyscf-scf` passes (no regressions; mulliken_pop tests green after any behavior-preserving refactor).
- orth_ao is consumed by mulliken_meta (the key link).
- All reductions via oracle_sum/oracle_dot; no FMA; no new unwrap in production.
- `clippy -D warnings` + `fmt`; `xtask check-no-fma` + `xtask check-dependency-wall` PASS; 0 libxc_rs.
- REQUIREMENTS.md SCF-09 → `[x]`.

**Out of scope / document honestly:** the full NAO `_nao_sub` core/valence/Rydberg partition (future enhancement; the shipped per-(atom,l)-block meta-Löwdin satisfies SCF-09's conservation requirement); atom/huckel init guesses (plan 03-14); upstream byte-identity of mulliken_meta charges vs upstream PySCF (CI-gated/human-verify — the sandbox has no maturin/upstream-pyscf; the in-tree gate is the conservation invariants); the 7 human-verification items in VERIFICATION.md.
</verification>

<success_criteria>
- `mulliken_meta(&rhf)` returns `Ok(MullikenResult)` — real meta-Löwdin population analysis (SCF-09).
- `orth_ao` (meta-Löwdin / Löwdin orthogonalization) ships in orth.rs and is consumed by mulliken_meta.
- Conservation invariants hold (`Σ ao_pop ≈ nelec`, `Σ chg ≈ molecule charge`) on H2 + H2O; orthonormality `C_orthᵀ·S·C_orth ≈ I`.
- SCF-09 flips `[ ]` → `[~]` (partial; the per-(atom,l)-block meta-Löwdin satisfies the conservation invariants, upstream byte-identity is human-verify); analyze/mulliken_pop/mulliken_meta/dip_moment all ship.
- No new crate dep; 0 libxc; oracle reductions; no FMA; no new panics.
</success_criteria>

<output>
Create `.planning/phases/03-scf-pyo3-bindings/03-15-SUMMARY.md` when done, documenting:
- orth_ao algorithm (Löwdin S^{-1/2} + per-(atom,l)-block meta-Löwdin) + upstream source refs (orth.py:32-62, 269-330)
- mulliken_meta source ref (hf.py:1301-1340) + the c_inv/D' transform + the s=I aggregation reuse
- The conservation values measured (Σ ao_pop, Σ chg) on H2 + H2O
- The scope boundary: full NAO _nao_sub core/valence/Rydberg refinement deferred as a documented future enhancement (not a gap)
- oracle_sum/oracle_dot invocation sites; confirmation of 0 libxc + no new dep
- SCF-09 status → `[~]` (partial; upstream byte-identity human-verify)
</output>
