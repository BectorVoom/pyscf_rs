---
phase: 03
plan: 14
type: execute
wave: 14
gap_closure: true
depends_on: ["03-11", "03-13"]
status: READY
files_modified:
  - crates/pyscf-scf/src/atom_hf.rs
  - crates/pyscf-scf/src/init_guess.rs
  - crates/pyscf-scf/src/lib.rs
  - crates/pyscf-scf/tests/init_guess_atom_huckel.rs
autonomous: true
requirements: ["SCF-05"]
user_setup: []

must_haves:
  truths:
    - "`InitGuessMode::Atom` produces a real density (superposition of pre-tabulated spherically-averaged atomic-RHF densities) — no longer `Err(InitGuessNotYetImplemented(\"atom\"))`."
    - "`InitGuessMode::Huckel` produces a real density (extended-Hückel GWH guess built from the same per-element atomic-RHF orbitals) — no longer `Err(InitGuessNotYetImplemented(\"huckel\"))`."
    - "Both guesses satisfy the physical sanity check `Tr(D·S) ≈ nelectron` (within a documented tolerance) on H2/STO-3G and H2O/STO-3G, and produce symmetric row-major `Density` matrices of shape nao×nao."
    - "RHF with `init_guess='atom'` and `init_guess='huckel'` each converge to the SAME total energy as the already-shipped `'1e'`/`'minao'` guesses (within `conv_tol`) on H2/STO-3G — proving the guesses seed a correct SCF, not just produce a matrix."
    - "All five SCF-05 init_guess modes ('minao', 'atom', '1e', 'huckel', 'chkfile') plus user-supplied dm0 now return `Ok(Density)`; SCF-05 flips from `[~]` to `[x]` in REQUIREMENTS.md."
    - "No new crate dep; libxc NEVER compiled (scf/gto test scope excludes pyscf-dft); all reductions via oracle_sum/oracle_dot; no FMA in pyscf-owned kernels."
  artifacts:
    - path: "crates/pyscf-scf/src/atom_hf.rs"
      provides: "get_atm_nrhf — per-unique-element spherically-averaged atomic RHF (the shared engine atom + huckel both consume)"
      contains: "fn get_atm_nrhf"
      min_lines: 80
    - path: "crates/pyscf-scf/src/init_guess.rs"
      provides: "init_guess_by_atom + init_guess_by_huckel real bodies wired into default_get_init_guess"
      contains: "fn init_guess_by_atom"
  key_links:
    - from: "crates/pyscf-scf/src/init_guess.rs"
      to: "crates/pyscf-scf/src/atom_hf.rs::get_atm_nrhf"
      via: "atom + huckel both build per-element atomic-RHF orbital/occupation data once, then superpose / GWH-couple"
      pattern: "get_atm_nrhf"
    - from: "crates/pyscf-scf/src/init_guess.rs"
      to: "crates/pyscf-scf/src/atom_config.rs::frac_occ"
      via: "spherically-averaged fractional occupations per (element, l-shell)"
      pattern: "frac_occ"
    - from: "crates/pyscf-scf/src/init_guess.rs"
      to: "pyscf_algebra::eigh_gen"
      via: "angular-averaged per-l atomic Fock solve + GWH minimal-basis solve"
      pattern: "eigh_gen"
---

<objective>
**Close the SCF-05 gap: implement the `atom` and `huckel` init-guess modes (the
last 2 of 5 modes still stubbed).**

`init_guess.rs:11-13` (`InitGuessMode::Atom`) and `init_guess.rs:15-17`
(`InitGuessMode::Huckel`) currently return `Err(ScfError::InitGuessNotYetImplemented)`.
The other three modes (`minao`/`1e`/`chkfile`) plus user-`dm0` are already real
in the same file. This plan ports the two remaining modes from
`pyscf/scf/hf.py`, matching the existing `Result<Density, PyscfRsError>` /
row-major `Density.data` conventions.

Purpose: SCF-05 enumerates ALL five modes as required for Phase 3; REQUIREMENTS.md
marks it `[~]`; no later phase covers atom/huckel. This is the only code gap for
SCF-05.

**Key structural finding (drives the task split):** both `init_guess_by_atom`
(`pyscf/scf/hf.py:495-535`) and `init_guess_by_huckel` (`pyscf/scf/hf.py:537-555`,
via `_init_guess_huckel_orbitals` at `:577`) depend on the SAME upstream helper
`pyscf/scf/atom_hf.py:get_atm_nrhf` — a per-unique-element spherically-averaged
atomic RHF that yields each element's atomic MO coefficients, energies, and
fractional occupations. There is NO in-tree analog (no `pyscf-lo`/atom-HF crate),
so Task 1 ports that shared engine first; Tasks 2 and 3 consume it. Build the
contract first (interface-first ordering) so the two consumers receive it rather
than re-derive it.

Output: `crates/pyscf-scf/src/atom_hf.rs` (shared atomic-RHF engine) +
`init_guess_by_atom`/`init_guess_by_huckel` bodies in `init_guess.rs` + an
integration test asserting both seed a converging RHF. SCF-05 → `[x]`.

No new crate dep; libxc NEVER compiled.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scf-pyo3-bindings/03-VERIFICATION.md
@.planning/REQUIREMENTS.md
@crates/pyscf-scf/src/init_guess.rs
@crates/pyscf-scf/src/atom_config.rs
@pyscf/scf/hf.py
@pyscf/scf/atom_hf.py

<interfaces>
<!-- Conventions the executor MUST replicate (extracted from the codebase — no exploration needed) -->

Density (pyscf-core/src/density.rs): `pub struct Density { pub nao: usize, pub data: Vec<f64> }` — `data` is ROW-MAJOR: `D[mu,nu] = data[mu*nao + nu]`.

MOCoefficients (pyscf-core/src/mo.rs): `pub struct MOCoefficients { pub nao, pub nmo, pub data, pub energies, pub occupations }` — `data` is COLUMN-MAJOR (F-order): `C[mu,i] = data[mu + i*nao]`.

pyscf-scf/src/init_guess.rs current dispatcher (the wire-in points):
```
InitGuessMode::Atom   => Err(ScfError::InitGuessNotYetImplemented("atom", "03-03 follow-up").into()),
InitGuessMode::Huckel => Err(ScfError::InitGuessNotYetImplemented("huckel", "03-03 follow-up").into()),
```
Replace each with a call to the new free fn. Match `init_guess_by_minao` / `init_guess_by_1e` signature style: `pub(crate) fn ...(mol: &Mole) -> Result<Density, PyscfRsError>`.

pyscf-scf/src/atom_config.rs (already shipped, plan 03-13):
- `NRSRHF_CONFIGURATION: [[u32;4];119]` — `[n_s,n_p,n_d,n_f]` electrons per l-shell, indexed by Z (0=GHOST).
- `frac_occ(z: usize, l: usize) -> (u32 ndocc, f64 frac)` — `ndocc` doubly-occupied shells of this l, fractional `frac ∈ [0,2)` for the frontier; `(0,0.0)` if l>=4 or empty.

pyscf-algebra public surface:
- `eigh_gen(f: &[f64], s: &[f64], n: usize) -> Result<(Vec<f64> eigvals_nondecreasing, Vec<f64> C_F_order_flat), AlgebraError>` — generalized `F·C = S·C·diag(ε)`; input row-major n×n, output C column-major (F-order). Use for the angular-averaged per-l atomic Fock solve AND the GWH minimal-basis solve. Wrap `AlgebraError` via `ScfError::Algebra` (see eig.rs:31 precedent).
- `oracle_sum(&[f64]) -> f64`, `oracle_dot(&[f64], &[f64]) -> f64` — bit-stable reductions (mandatory; no bare `+=` / `.sum()` / `.fold` in numeric accumulation).
- `solve_linear(a: &[f64], b: &[f64], n: usize) -> Result<Vec<f64>, AlgebraError>` (faer LU) — available if a linear solve is cleaner than eigh for any sub-step.

pyscf-gto::intor surface (used to build atomic Fock + molecular S):
- `pyscf_gto::intor(mol, "int1e_kin") -> IntorOutput` (`.values`, F-order nao×nao)
- `pyscf_gto::intor(mol, "int1e_nuc") -> IntorOutput`
- `pyscf_gto::intor(mol, "int1e_ovlp_sph") -> IntorOutput`
- `pyscf_gto::intor(mol, "int2e_sph") -> IntorOutput` (arity-4, real since 05-08) for the atomic get_jk.
- `pyscf_gto::AtomInput`, `BasisInput`, `M(MoleBuildArgs{..})` to build a per-element single-atom Mole (the minao port at init_guess.rs:122-130 is the build template).

Per-atom AO walk (the in-tree `aoslice_by_atom` equivalent, used by mulliken_pop at analyze.rs:92-108):
- `pyscf_core::raw_layout::{ATOM_OF, BAS_SLOTS}`; atom of shell = `mol._bas[shell*BAS_SLOTS + ATOM_OF] as usize`.
- AO range of shell = `mol.ao_loc_nr[shell] as usize .. mol.ao_loc_nr[shell+1] as usize` (`ao_loc_nr` is `nbas+1` long post-build).
- element symbol/charge: `pyscf_gto::format_atom::charge_for_symbol(&elem)`; `mol._atom[ia].0` is the symbol string.

pyscf-scf reductions/eig precedent to mirror: eig.rs:31 (`eigh_gen` + `ScfError::Algebra`), init_guess.rs:200-211 (`dm[μν] = Σ_p occ_p·mo[μ,p]·mo[ν,p]` via oracle_sum on a materialized terms Vec), init_guess_by_1e at init_guess.rs:87-94.

ScfError (pyscf-scf/src/error.rs): `InitGuessNotYetImplemented(&'static str, &'static str)`, `Algebra(AlgebraError)`, `Core(CoreError)`, `ConvergenceFailure{cycles, last_diff}`. `From<ScfError> for PyscfRsError` exists (`.into()`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port get_atm_nrhf — per-element spherically-averaged atomic RHF (shared engine)</name>
  <files>crates/pyscf-scf/src/atom_hf.rs, crates/pyscf-scf/src/lib.rs</files>
  <read_first>
    - crates/pyscf-scf/src/init_guess.rs (FULL — current dispatcher + minao build template at lines 114-214 + 1e at 87-94; the Mole-build + occ-walk + oracle_sum-dm patterns you will reuse)
    - crates/pyscf-scf/src/atom_config.rs (FULL — NRSRHF_CONFIGURATION + frac_occ; the occupation source)
    - pyscf/scf/atom_hf.py:27-196 (get_atm_nrhf + AtomSphAverageRHF.eig/get_occ + AtomHF1e — the canonical algorithm: build a single-atom Mole per unique element, run an angular-averaged RHF where eig() block-diagonalizes the Fock by l-shell and averages over the (2l+1) m-components, get_occ() uses frac_occ for spherically-averaged fractional occupancy)
    - crates/pyscf-scf/src/eig.rs (default_eig — the eigh_gen + canonicalize_signs call precedent + ScfError::Algebra wrapping)
    - crates/pyscf-scf/src/fock.rs (default_get_hcore / default_get_ovlp / default_get_jk / default_get_veff — reuse for the atomic Fock build)
    - crates/pyscf-scf/src/analyze.rs:87-109 (the ATOM_OF + ao_loc_nr per-atom AO walk pattern)
  </read_first>
  <action>
    Create `crates/pyscf-scf/src/atom_hf.rs` and register `mod atom_hf;` in lib.rs.

    Port `pyscf/scf/atom_hf.py:get_atm_nrhf` as `pub(crate) fn get_atm_nrhf(mol: &Mole) -> Result<HashMap<String, AtomScfResult>, PyscfRsError>` where `AtomScfResult { mo_coeff: MOCoefficients (F-order, per-atom nao_atm×nao_atm), mo_energy: Vec<f64>, mo_occ: Vec<f64> }`. For each UNIQUE element symbol in `mol._atom`:
      1. Build a single-atom Mole via `pyscf_gto::M(MoleBuildArgs{ atom: AtomInput::Tuples(vec![that atom]), basis: mol's basis for that element, unit: Bohr, charge: 0, spin: Z%2, cart: false, .. })` — mirror the minao ANO-reference build at init_guess.rs:122-130 (here the basis is the WORKING basis restricted to this element, not 'ano'). Handle the GHOST / nao_atm==0 / nelectron==0 case by returning a zero MOCoefficients + zero occ (upstream atom_hf.py:58-61).
      2. Run a spherically-averaged atomic RHF: this is a SMALL self-consistent loop (reuse `fock::default_get_hcore`/`default_get_ovlp`/`default_get_veff` on the single-atom Mole) where the eig step is the ANGULAR-AVERAGED solve from `AtomSphAverageRHF.eig` — group AOs by angular momentum l (use the `_bas[shell, ANG_OF]` layout + ao_loc_nr to map each AO to its l and shell-within-l), reshape the per-l Fock/overlap blocks to (nsh, degen, nsh, degen), average over the diagonal m-index (`einsum('piqi->pq')/degen`), `eigh_gen` the (nsh×nsh) averaged block, then scatter the eigvecs back across the (2l+1) m-components (upstream atom_hf.py:107-138). The occupations come from `get_occ` = spherically-averaged fractional occupancy via `frac_occ(Z,l)` per l-shell, repeated over `degen` (upstream atom_hf.py:142-172). For the 1-electron element case, mirror `AtomHF1e` (single-electron, no 2e term). Loop until the energy delta < a tight tol or max cycles; on non-convergence return `Err(ScfError::ConvergenceFailure{..})` (NEVER panic — T-03-14-PANIC).
      3. Cache per unique element (upstream `if element in atm_scf_result: continue`); return the map.

    Reductions: every sum (Fock-block average, dm build, energy) goes through `oracle_sum`/`oracle_dot` (materialize terms into a Vec first — the init_guess.rs:205-209 pattern). No bare `+=` in numeric accumulation, no `.iter().sum()`, no FMA.

    Add focused unit tests in atom_hf.rs: (a) get_atm_nrhf on a 1-atom H/STO-3G Mole returns a result whose mo_occ sums to 1.0 (H spherically-averaged occ); (b) on O/STO-3G the per-l occupations match frac_occ(8,0)/(8,1); (c) the returned MOCoefficients is F-order shape nao_atm×nao_atm and finite.
  </action>
  <verify>
    <automated>cargo test -p pyscf-scf atom_hf && grep -q "fn get_atm_nrhf" crates/pyscf-scf/src/atom_hf.rs && grep -cE "oracle_sum|oracle_dot" crates/pyscf-scf/src/atom_hf.rs</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-scf/src/atom_hf.rs` exists and exports `pub(crate) fn get_atm_nrhf`; `lib.rs` has `mod atom_hf;`.
    - `cargo test -p pyscf-scf atom_hf` exits 0; the H and O unit tests pass (H atomic occ sums to 1.0; O per-l occ matches frac_occ).
    - `grep -cE "oracle_sum|oracle_dot" crates/pyscf-scf/src/atom_hf.rs` returns ≥ 1 (Pitfall 9: reductions are bit-stable).
    - No `unwrap()`/`expect()`/`panic!`/`unimplemented!` in atom_hf.rs production code (`grep -n "unwrap()\|expect(\|panic!\|unimplemented!" crates/pyscf-scf/src/atom_hf.rs` finds matches only inside `#[cfg(test)]`).
    - `cargo build -p pyscf-scf` exits 0; `clippy -D warnings` + `fmt` clean.
  </acceptance_criteria>
  <done>atom_hf.rs ships get_atm_nrhf (per-unique-element spherically-averaged atomic RHF) returning AtomScfResult{mo_coeff,mo_energy,mo_occ}; H/O unit tests pass; reductions via oracle_sum; no panics in production.</done>
  <commit>feat(03-14): port get_atm_nrhf — per-element spherically-averaged atomic RHF (shared atom/huckel engine)</commit>
</task>

<task type="auto">
  <name>Task 2: init_guess_by_atom — block-diagonal superposition of atomic densities (SCF-05)</name>
  <files>crates/pyscf-scf/src/init_guess.rs, crates/pyscf-scf/tests/init_guess_atom_huckel.rs</files>
  <read_first>
    - crates/pyscf-scf/src/atom_hf.rs (Task 1 output — the get_atm_nrhf contract + AtomScfResult)
    - crates/pyscf-scf/src/init_guess.rs (the dispatcher + init_guess_by_minao dm-build at lines 200-213 — the oracle_sum dm pattern + Density row-major convention)
    - pyscf/scf/hf.py:495-535 (init_guess_by_atom — atm_dms = [c*occ @ c.T per atom]; dm = block_diag(*atm_dms) placed onto the molecular AO layout via aoslice_by_atom)
    - crates/pyscf-scf/src/analyze.rs:87-109 (ATOM_OF + ao_loc_nr per-atom AO offset walk — how to place each atom's block into the molecular nao×nao Density)
  </read_first>
  <action>
    Add `pub(crate) fn init_guess_by_atom(mol: &Mole) -> Result<Density, PyscfRsError>` to `init_guess.rs`. Port `pyscf/scf/hf.py:495-535`:
      1. Call `crate::atom_hf::get_atm_nrhf(mol)?` to get each unique element's `(mo_coeff c, mo_occ occ)`.
      2. For each atom `ia` in `mol._atom`, look up its element's result; build the per-atom density block `atm_dm[μ,ν] = Σ_p occ[p]·c[μ,p]·c[ν,p]` (row-major, oracle_sum over p materialized into a Vec — mirror init_guess.rs:205-209). For an unassigned-basis atom, use a zero block sized to its AO count (upstream hf.py:516-518).
      3. Place each atom's block on the block-diagonal of the molecular `nao×nao` Density: the atom's molecular AO range is `[lo, hi)` from the ATOM_OF + ao_loc_nr walk (analyze.rs:104-105 pattern); write `dm[(lo+i)*nao + (lo+j)] = atm_dm[i*nao_atm + j]`. (`scipy.linalg.block_diag` in upstream = placing each atomic block at its atom's AO offset.)
    Wire `InitGuessMode::Atom => init_guess_by_atom(mol)` in `default_get_init_guess`, replacing the `InitGuessNotYetImplemented("atom", ...)` arm.

    The dm build must use oracle_sum (no bare `+=`), produce a symmetric row-major Density, and never panic on shape surprises (return `Err` via `?`/explicit `CoreError`). Document that cart-basis support (upstream's cart2sph branch at hf.py:531-534) is out of scope here if `mol.cart` (return a clear error) OR handle it — match whatever the working-basis tests exercise; H2/H2O STO-3G are spherical.
  </action>
  <verify>
    <automated>cargo test -p pyscf-scf --test init_guess_atom_huckel atom && ! grep -q 'InitGuessNotYetImplemented("atom"' crates/pyscf-scf/src/init_guess.rs && grep -q "fn init_guess_by_atom" crates/pyscf-scf/src/init_guess.rs</automated>
  </verify>
  <acceptance_criteria>
    - `init_guess.rs` no longer contains `InitGuessNotYetImplemented("atom"` (grep finds zero matches).
    - `init_guess.rs` contains `fn init_guess_by_atom` and the dispatcher arm `InitGuessMode::Atom => init_guess_by_atom(mol)`.
    - A new test in `crates/pyscf-scf/tests/init_guess_atom_huckel.rs` (created here, extended in Task 3) asserts: `init_guess_by_atom` on H2/STO-3G returns a symmetric 2×2 row-major Density with `Tr(D·S) ≈ 2.0` (the H2 electron count) within tol (document the tol; minao's inherent non-normalization caveat does NOT apply to the atom guess — atomic dm is built from normalized atomic orbitals, so the trace should be close to nelec).
    - The dm-build reduction uses `oracle_sum` (grep the new function body).
    - `clippy -D warnings` + `fmt` clean; `cargo build -p pyscf-scf` exits 0.
  </acceptance_criteria>
  <done>init_guess_by_atom returns a real symmetric row-major Density (block-diagonal superposition of atomic densities) with Tr(D·S)≈nelec on H2; InitGuessNotYetImplemented("atom") is gone; dispatcher arm wired.</done>
  <commit>feat(03-14): init_guess_by_atom — superposition of spherically-averaged atomic densities (SCF-05)</commit>
</task>

<task type="auto">
  <name>Task 3: init_guess_by_huckel (GWH) + wire + RHF-converges integration test (SCF-05 closed)</name>
  <files>crates/pyscf-scf/src/init_guess.rs, crates/pyscf-scf/tests/init_guess_atom_huckel.rs, .planning/REQUIREMENTS.md</files>
  <read_first>
    - crates/pyscf-scf/src/atom_hf.rs (Task 1 output — get_atm_nrhf)
    - crates/pyscf-scf/src/init_guess.rs (Task 2 state — init_guess_by_atom + the dispatcher)
    - pyscf/scf/hf.py:537-555 (init_guess_by_huckel) + pyscf/scf/hf.py:577-665 (_init_guess_huckel_orbitals — collect occupied atomic orbitals into the molecular AO basis, build the GWH Hückel matrix orb_H[io,jo] = 0.5·Kgwh(Ei,Ej)·orb_S[io,jo]·(Ei+Ej), solve eig(orb_H, orb_S), back-transform to AO) + pyscf/scf/hf.py:563 (Kgwh — the GWH constant, default 1.75)
    - crates/pyscf-scf/src/occ.rs (default_get_occ — Aufbau fill, to occupy the Hückel MOs)
    - crates/pyscf-scf/src/rdm.rs (default_make_rdm1 — C·diag(occ)·Cᵀ to form the final density)
    - crates/pyscf-scf/tests/no_overrides_drives_kernel.rs (the existing pattern for "RHF with config X converges" — model the integration test on it)
  </read_first>
  <action>
    Add `pub(crate) fn init_guess_by_huckel(mol: &Mole) -> Result<Density, PyscfRsError>` to `init_guess.rs`, porting `pyscf/scf/hf.py:537-555` + `_init_guess_huckel_orbitals` (`:577-665`):
      1. `let atm = crate::atom_hf::get_atm_nrhf(mol)?`.
      2. Collect the OCCUPIED atomic orbitals of every atom into the molecular AO basis: for each atom, for each atomic MO with occ>0, scatter its atomic coeff column into the molecular `orb_C[lo..hi, iocc]` slot (lo..hi = that atom's molecular AO range via ATOM_OF+ao_loc_nr) and record its atomic orbital energy in `orb_E[iocc]`. `nocc` = count of occupied atomic orbitals across all atoms.
      3. `orb_S = orb_Cᵀ · S · orb_C` (S = `intor(mol,"int1e_ovlp_sph")`), via materialized-terms oracle reductions.
      4. Build the GWH Hückel matrix: `orb_H[io,io] = orb_E[io]`; off-diagonal `orb_H[io,jo] = 0.5·Kgwh·orb_S[io,jo]·(orb_E[io]+orb_E[jo])` with `Kgwh = 1.75` (the non-updated GWH rule, `updated_rule=false` — the default `init_guess_by_huckel`, NOT mod_huckel). Symmetric.
      5. `(mo_E, atmo_C) = eigh_gen(orb_H, orb_S, nocc)`; back-transform to AO: `mo_C[μ,k] = Σ_o orb_C[μ,o]·atmo_C[o,k]` (F-order, oracle reductions).
      6. Aufbau-fill via `crate::occ::default_get_occ(&mo_E, mol.nelectron)`, then `dm = crate::rdm::default_make_rdm1(&MOCoefficients{ data: mo_C, energies: mo_E, occupations, nao, nmo: nocc })`.
    Wire `InitGuessMode::Huckel => init_guess_by_huckel(mol)` in `default_get_init_guess`, replacing the `InitGuessNotYetImplemented("huckel", ...)` arm.

    Extend `crates/pyscf-scf/tests/init_guess_atom_huckel.rs` with: (a) `init_guess_by_huckel` on H2/STO-3G returns a symmetric 2×2 Density with `Tr(D·S) ≈ 2.0`; (b) THE closing gate — build an RHF on H2/STO-3G with `init_guess='atom'`, run kernel, assert converged + `e_tot ≈ -1.117` (within conv_tol); same with `init_guess='huckel'`; assert both land within ~1e-6 of the `'1e'`-guess converged energy (proving the guesses seed a CORRECT SCF, mode-independent at convergence). Reuse the M(MoleBuildArgs{..}) + kernel pattern from no_overrides_drives_kernel.rs.

    Finally, update `.planning/REQUIREMENTS.md` SCF-05 from `[~]` to `[x]` and append a note that `'atom'`/`'huckel'` are now implemented (via `get_atm_nrhf` in atom_hf.rs), so all 5 modes + user-dm0 ship.
  </action>
  <verify>
    <automated>cargo test -p pyscf-scf --test init_guess_atom_huckel && cargo test -p pyscf-scf -p pyscf-gto && ! grep -qE 'InitGuessNotYetImplemented\("(atom|huckel)"' crates/pyscf-scf/src/init_guess.rs</automated>
  </verify>
  <acceptance_criteria>
    - `init_guess.rs` contains `fn init_guess_by_huckel` and the dispatcher arms `InitGuessMode::Atom => init_guess_by_atom(mol)` and `InitGuessMode::Huckel => init_guess_by_huckel(mol)`; grep for `InitGuessNotYetImplemented("atom"` AND `InitGuessNotYetImplemented("huckel"` returns zero matches.
    - `cargo test -p pyscf-scf --test init_guess_atom_huckel` exits 0: both `Tr(D·S)≈nelec` sanity checks pass AND the RHF-converges-with-atom/huckel-guess gate passes (e_tot ≈ -1.117 on H2/STO-3G, within ~1e-6 of the 1e-guess energy).
    - `cargo test -p pyscf-scf -p pyscf-gto` exits 0 (no regressions across the SCF + GTO suites).
    - `REQUIREMENTS.md` SCF-05 line starts with `[x]` (no longer `[~]`).
    - `parse_init_guess_mode` already maps "atom"/"huckel" (init_guess.rs:217-227) — confirm the dispatcher now returns Ok for both.
    - `clippy -D warnings` + `fmt` clean; `xtask check-no-fma` + `xtask check-dependency-wall` PASS; `cargo tree -p pyscf-scf` shows 0 libxc_rs.
  </acceptance_criteria>
  <done>init_guess_by_huckel (GWH, Kgwh=1.75) ships; both atom & huckel guesses seed an RHF that converges to the 1e-guess energy (≈-1.117 H2/STO-3G); all 5 SCF-05 modes return Ok; REQUIREMENTS.md SCF-05 → [x].</done>
  <commit>feat(03-14): init_guess_by_huckel (GWH) + atom/huckel converge RHF — SCF-05 complete (all 5 modes)</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust↔Rust only | `init_guess_by_atom`/`init_guess_by_huckel`/`get_atm_nrhf` are internal `pyscf-scf` functions reached through the already-existing PyO3 seam (`PyOverrideBridge::get_init_guess`, bridge.rs:95-125). No NEW public FFI entry point, no new I/O, no network, no untrusted external input — they operate purely in-process over an already-validated in-memory `Mole` and produce an in-memory `Density`. Threat surface is near-nil; the relevant risks are numeric-correctness and never-panic, not classic STRIDE I/O attacks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14-PANIC | Denial of Service | atomic-RHF loop / per-atom AO walk / eigh_gen calls in atom_hf.rs + init_guess.rs | mitigate | All fallible ops (`eigh_gen`, `intor`, `M`, AO-index math) use `?` or explicit `Err`; convergence failure returns `Err(ScfError::ConvergenceFailure)`; AO-range overruns return `Err(CoreError::InvalidMolecule)` — NEVER panic (FOUND-07 never-panic; through the PyO3 panic→exception bridge a panic would abort the Python process). `clippy::unwrap_used` gate enforces no new unwrap in production code. |
| T-03-14-NUM | Tampering | dm/Fock/GWH reductions void µHartree bit-exactness if reduction order drifts (Pitfall 9 + 12) | mitigate | Every sum (atomic Fock-block average, dm superposition, orb_S/orb_H build, AO back-transform) goes through `oracle_sum`/`oracle_dot` on materialized term Vecs — no bare `+=`/`.sum()`/`.fold`. No FMA in pyscf-owned kernels (`check-no-fma` CI guard). |
| T-03-14-CORRECT | Tampering | A wrong atom/huckel guess that still "looks like a density" could silently seed a wrong SCF | mitigate | The closing gate is behavioral, not structural: RHF seeded with `'atom'` and `'huckel'` must CONVERGE to the same e_tot as the `'1e'`/`'minao'` guesses (mode-independence at convergence) AND `Tr(D·S)≈nelec` — a guess that produces a plausible-but-wrong matrix fails one of these. |
| T-03-14-SC | Tampering | npm/pip/cargo installs (supply-chain) | accept | No new crate dependency is added (reuses pyscf-algebra/pyscf-gto already in the graph); libxc NEVER compiled. No package-manager install occurs in this plan, so no legitimacy checkpoint is required. |
| T-03-14-SCOPE | Tampering | Scope creep into adjacent SCF utilities | accept | Implement atom + huckel ONLY; do not touch minao/1e/chkfile, mulliken_meta (that is plan 03-15), or any other default. Cart-basis cart2sph branch handled only if a working-basis test needs it (STO-3G is spherical). |
</threat_model>

<verification>
**Goal:** all five SCF-05 init_guess modes return `Ok(Density)`; atom + huckel seed a converging RHF. Gates:
- `init_guess.rs` no longer contains `InitGuessNotYetImplemented("atom"` or `..("huckel"` (grep = 0 matches).
- `cargo test -p pyscf-scf --test init_guess_atom_huckel` passes: `Tr(D·S)≈nelec` for both guesses on H2 + the RHF-converges gate (atom & huckel each reach e_tot ≈ -1.117 on H2/STO-3G, within ~1e-6 of the 1e-guess energy).
- `cargo test -p pyscf-scf -p pyscf-gto` passes (no regressions).
- `atom_hf.rs` exports `get_atm_nrhf`, used by BOTH init_guess_by_atom and init_guess_by_huckel.
- All reductions via oracle_sum/oracle_dot; no FMA; no new unwrap in production code.
- `clippy -D warnings` + `fmt`; `xtask check-no-fma` + `xtask check-dependency-wall` PASS; 0 libxc_rs.
- REQUIREMENTS.md SCF-05 → `[x]`.

**Out of scope / document honestly:** `mulliken_meta` (plan 03-15); cart-basis cart2sph branch unless a test exercises it; upstream byte-identity of converged atom/huckel-seeded SCF energies (CI-gated/human-verify — the sandbox has no maturin/upstream-pyscf); the 7 human-verification items in VERIFICATION.md.
</verification>

<success_criteria>
- `InitGuessMode::Atom` and `InitGuessMode::Huckel` return real `Density` (SCF-05 — the last 2 of 5 modes).
- `get_atm_nrhf` (the shared per-element atomic-RHF engine) ships in atom_hf.rs and is consumed by both modes.
- Both guesses pass `Tr(D·S)≈nelec` and seed an RHF that converges to the mode-independent energy.
- SCF-05 flips `[~]` → `[x]`; all 5 modes + user-dm0 implemented.
- No new crate dep; 0 libxc; oracle reductions; no FMA; no new panics.
</success_criteria>

<output>
Create `.planning/phases/03-scf-pyo3-bindings/03-14-SUMMARY.md` when done, documenting:
- get_atm_nrhf algorithm + upstream source-of-truth line refs (atom_hf.py:27-196)
- init_guess_by_atom / init_guess_by_huckel source refs (hf.py:495-555, 577-665) + the GWH constant used (Kgwh=1.75, non-updated rule)
- The Tr(D·S) values measured for atom + huckel on H2 (and H2O if run)
- Confirmation that atom & huckel-seeded RHF converge to the 1e-guess energy
- oracle_sum/oracle_dot invocation sites; confirmation of 0 libxc + no new dep
- SCF-05 status: all 5 modes + user-dm0 now ship → `[x]`
</output>
