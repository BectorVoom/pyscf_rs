---
phase: 03
plan: 09
type: execute
wave: 8
depends_on: ["03-07", "03-08"]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - BIND-05
must_haves:
  truths:
    - "ci.yml gains 4 new jobs per RESEARCH §Validation Architecture and VALIDATION.md §Wave 0:"
    - "  (a) maturin-smoke: `maturin develop && python -c 'from pyscf._native import scf'` (BIND-01)"
    - "  (b) stride-fuzz: BIND-04 — `pytest python/pyscf/tests/test_scf_stride_fuzz.py` (a/a.T/a[::2]/a[:,1:5])"
    - "  (c) xplat-uhartree: matrix Linux x86_64 + macOS aarch64 running `pytest python/pyscf/tests/test_scf_rhf_h2o.py` under release-oracle; cross-platform µHartree assertion (Pitfall 12 mitigation; depends on canonicalize_signs being plumbed from plan 03-01 + plan 03-03)"
    - "      CRITICAL: this job MUST hard-fail if `release-oracle` profile cannot be activated — no `||` fallback to `--release` (which has FMA enabled, voiding the µHartree guarantee). Maturin >=1.4 is pinned at job start."
    - "  (d) python313t-smoke: NON-abi3 build using `maturin develop --no-default-features --features free-threading` against Python 3.13t (BIND-05; CRITICAL — abi3 wheels incompatible with 3.13t per RESEARCH §Pitfall (NEW))"
    - "      The 3.13t job MUST assert `import pyscf._native` succeeds before the SCF smoke runs (segfault-on-import would otherwise pass CI; see NIT 6 in checker review iteration 1)."
    - "All four jobs run on push/PR to master/main and on workflow_dispatch (consistent with existing jobs)"
    - "Each job uses dtolnay/rust-toolchain + actions/cache for ~/.cargo + target (existing ci.yml pattern)"
    - "free-threading job uses actions/setup-python@v5 with python-version='3.13' + free-threaded variant flag, OR setup-uv + `uv python install 3.13t` (executor picks per RESEARCH Open Question 2)"
    - "abi3audit is DEFERRED to Phase 8 BIND-08 per ROADMAP.md; this plan does NOT run abi3audit"
    - "auditwheel show is DEFERRED to Phase 8 DIST-04; this plan does NOT run auditwheel"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "4 new jobs: maturin-smoke, stride-fuzz, xplat-uhartree (matrix), python313t-smoke"
      contains: "python313t-smoke"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "crates/pyscf-py/Cargo.toml features"
      via: "free-threading build uses --no-default-features --features free-threading"
      pattern: "free-threading"
    - from: ".github/workflows/ci.yml"
      to: "python/pyscf/tests/"
      via: "pytest invocations"
      pattern: "pytest python/pyscf/tests"
    - from: ".github/workflows/ci.yml"
      to: "[profile.release-oracle]"
      via: "maturin develop --profile release-oracle (HARD requirement, no fallback)"
      pattern: "maturin develop --profile release-oracle"
---

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI environment | GitHub Actions runners (trusted) + pyscf-rs source (trusted, under git). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-08 | DoS | ABI mismatch between abi3-py310 wheel and Python 3.13t at install time | mitigate | Two-build configuration (BIND-01 default abi3-py310 + BIND-05 separate non-abi3 free-threading build). Installing the wrong wheel raises clean `ImportError`, not a segfault. THIS PLAN ships the CI jobs that prove both builds work. Pitfall (NEW) static guard is enforced in plan 03-02's build.rs (Warning 4 mitigation). |
| T-3-12 | DoS | Cross-platform numerical drift (Linux/macOS µHartree) — Pitfall 12 | mitigate | xplat-uhartree matrix job asserts ≤1 µHartree on H2O/cc-pVDZ across Linux x86_64 + macOS aarch64. Anchored by FMA-free `release-oracle` profile (Phase 1) + canonicalize_signs (plan 03-01). HARD requirement: no `||` fallback to `--release` — checker iteration 1 BLOCKER 2 closed by pinning maturin >=1.4 and failing the job if `--profile release-oracle` is rejected. |
| T-3-18 | DoS | Segfault on `import pyscf._native` under 3.13t passes CI if not explicitly asserted | mitigate | python313t-smoke job runs `python3.13t -c "import pyscf._native; print(pyscf._native.__doc__)"` BEFORE the SCF smoke. Checker iteration 1 NIT 6 closed. |
</threat_model>

<objective>
Add four new GitHub Actions jobs to `.github/workflows/ci.yml` per VALIDATION.md
§"Wave 0 Requirements" line 114-118.

This plan does NOT change existing jobs (fmt / clippy / build-default / build-gpu /
test / oracle-determinism). It APPENDS new jobs that:

1. **maturin-smoke**: build the abi3-py310 wheel via `maturin develop`; verify
   `from pyscf._native import scf` succeeds. (BIND-01)

2. **stride-fuzz**: run plan 03-10's stride-fuzz pytest; asserts BIND-04 layout
   discipline (a / a.T / a[::2] / a[:,1:5] all return identical answers). NOTE: in
   Wave 3 the stride-fuzz pytest is still xfail-marked from plan 03-02. Plan 03-10
   will UN-xfail it. This CI job becomes meaningful when plan 03-10 lands; until
   then, runs cleanly with xfail-skipped tests.

3. **xplat-uhartree** (MATRIX: ubuntu-latest + macos-14 aarch64): build under
   release-oracle profile (HARD requirement, no fallback), run pytest test_scf_rhf_h2o.py;
   collect e_tot from both jobs; the merge gate asserts |e_linux - e_macos| < 1 µHartree.
   (Pitfall 12)

4. **python313t-smoke**: build pyscf-py with `--no-default-features --features
   free-threading` (non-abi3 — abi3 is incompatible with 3.13t per RESEARCH §Pitfall
   (NEW)); install Python 3.13t free-threaded variant; run a load-only import check
   FIRST (so segfault-on-import surfaces), then a tiny SCF smoke to probe for GIL
   deadlocks. (BIND-05)

## Checker iteration 1 remediations applied (2026-05-11)

- **BLOCKER 2 closed**: `maturin develop --profile release-oracle || maturin develop --release`
  has been replaced by a hard-pinned `pip install 'maturin>=1.4,<2.0'` + a `maturin --version`
  assertion + `maturin develop --profile release-oracle` (no `||` fallback). FMA-free guarantee
  preserved.
- **NIT 6 closed**: python313t-smoke job now runs `python3.13t -c "import pyscf._native; print(pyscf._native.__doc__)"`
  BEFORE the SCF smoke so a segfault-on-import fails the job instead of being masked by `run`.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-scf-pyo3-bindings/03-VALIDATION.md
@.planning/phases/03-scf-pyo3-bindings/03-RESEARCH.md
@.planning/phases/03-scf-pyo3-bindings/03-07-PLAN.md
@.github/workflows/ci.yml

<interfaces>
<!-- Existing ci.yml structure (verified from initial inspection lines 1-100) -->
```yaml
name: CI

on:
  push:
    branches: [master, main]
  pull_request:
  workflow_dispatch:

env:
  RUSTFLAGS: ""
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: "1"

jobs:
  fmt:              # Phase 1 — cargo fmt --all -- --check
  clippy:           # Phase 1 — cargo clippy --workspace --all-targets --locked -- -D warnings
  build-default:    # Phase 1 — cargo build --workspace --locked
  build-gpu:        # Phase 1 — cargo build --workspace --locked --features gpu
  test:             # Phase 1 — cargo test --workspace --locked
  oracle-determinism:  # Phase 1 — RAYON_NUM_THREADS=1 vs =8
```

<!-- New jobs to add (verbatim — APPEND to ci.yml, do NOT replace existing) -->
```yaml
  maturin-smoke:
    name: maturin develop + pyscf._native importable (BIND-01)
    runs-on: ubuntu-latest
    needs: [build-default]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: pip install "maturin>=1.4,<2.0" "numpy>=1.26"
      - run: maturin develop --release
      - run: python -c "from pyscf._native import scf; print(scf.RHF, scf.UHF, scf.GHF)"

  stride-fuzz:
    name: BIND-04 stride-fuzz (a / a.T / a[::2] / a[:,1:5])
    runs-on: ubuntu-latest
    needs: [maturin-smoke]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: pip install "maturin>=1.4,<2.0" "pytest>=7.0" "numpy>=1.26"
      - run: maturin develop --release
      - run: pytest python/pyscf/tests/test_scf_stride_fuzz.py -x

  xplat-uhartree:
    name: cross-platform µHartree assertion (Pitfall 12, Linux x86_64 + macOS aarch64)
    needs: [maturin-smoke]
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-14]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      # Maturin >=1.4 added --profile support. Earlier maturin would
      # SILENTLY drop --profile flag and fall back to [profile.release]
      # (which has FMA enabled — voiding Pitfall 12 guarantee). Pin and assert.
      - run: pip install "maturin>=1.4,<2.0" "pytest>=7.0" "numpy>=1.26" "h5py>=3.10"
      - name: Assert maturin >= 1.4 (supports --profile)
        run: |
          MATURIN_VER=$(maturin --version | awk '{print $2}')
          python -c "import sys; v=tuple(map(int,'$MATURIN_VER'.split('.')[:2])); sys.exit(0 if v >= (1,4) else 1)" || \
            (echo "FATAL: maturin $MATURIN_VER < 1.4; cannot activate release-oracle profile (BLOCKER 2)"; exit 1)
      - name: Build pyscf-py under release-oracle profile (NO FALLBACK)
        # HARD requirement per checker iteration 1 BLOCKER 2: if release-oracle
        # cannot be activated, the job MUST fail. The previous `||` fallback
        # silently dropped to FMA-enabled --release, voiding the µHartree
        # guarantee. Pitfall 12 mitigation depends on FMA-free codegen.
        run: maturin develop --profile release-oracle
      - name: H2O/cc-pVDZ RHF — assert ≤ 1 µHartree vs upstream
        run: pytest python/pyscf/tests/test_scf_rhf_h2o.py -x --tb=short

  python313t-smoke:
    name: python3.13t SCF smoke (BIND-05; NON-abi3 build per RESEARCH §Pitfall (NEW))
    runs-on: ubuntu-latest
    needs: [build-default]
    steps:
      - uses: actions/checkout@v4
      # 3.13t free-threaded interpreter: use deadsnakes PPA or uv (per RESEARCH OQ#2).
      # Path A — deadsnakes PPA (Linux only):
      - name: Install Python 3.13t (free-threaded)
        run: |
          sudo add-apt-repository -y ppa:deadsnakes/ppa
          sudo apt-get update
          sudo apt-get install -y python3.13-nogil
          python3.13t --version
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install maturin in 3.13t venv
        run: |
          python3.13t -m venv /tmp/venv313t
          /tmp/venv313t/bin/pip install "maturin>=1.4,<2.0" "numpy>=1.26"
      - name: Build pyscf-py with free-threading feature (non-abi3)
        run: |
          /tmp/venv313t/bin/maturin develop --no-default-features --features free-threading
        # --no-default-features drops abi3-py310; the free-threading feature only
        # turns on pyo3/extension-module. This produces a 3.13t-specific wheel that
        # IS NOT abi3 — incompatible with 3.10-3.12. Acceptable because this job
        # is exclusively for the BIND-05 contract.
      # ── NIT 6 (checker iteration 1) ─────────────────────────────────────
      # Assert the extension LOADS before running any SCF smoke. A segfault
      # on import would otherwise pass CI silently.
      - name: Assert pyscf._native loads under 3.13t
        run: /tmp/venv313t/bin/python -c "import pyscf._native; print(pyscf._native.__doc__)"
      - name: SCF smoke (no deadlock)
        run: |
          /tmp/venv313t/bin/python -c "
          from pyscf import gto, scf
          mol = gto.M(atom='O 0 0 0; H 0 0.587 0.757; H 0 0.587 -0.757', basis='sto-3g')
          mf = scf.RHF(mol).run()
          assert mf.converged, 'SCF failed under 3.13t'
          print(f'OK 3.13t SCF e_tot = {mf.e_tot}')
          "
```

NOTE on `maturin develop --profile release-oracle`: maturin 1.4+ supports the `--profile`
flag. Earlier versions would silently drop the flag and fall back to `[profile.release]`,
which has FMA enabled — voiding the Pitfall 12 µHartree guarantee. THE PREVIOUS `||`
fallback (`maturin develop --profile release-oracle || maturin develop --release`) MUST
NOT BE REINSTATED. This is BLOCKER 2 from checker iteration 1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append 4 new CI jobs to .github/workflows/ci.yml (with iteration-1 BLOCKER 2 and NIT 6 fixes)</name>
  <read_first>
    - .github/workflows/ci.yml (full file — to see the current job list and pick the right position to append)
    - .planning/phases/03-scf-pyo3-bindings/03-VALIDATION.md lines 114-118 — Wave 0 CI jobs list
    - .planning/phases/03-scf-pyo3-bindings/03-RESEARCH.md §"Pitfall (NEW)" lines 1146-1156 — abi3 / 3.13t separation contract
    - .planning/phases/03-scf-pyo3-bindings/03-RESEARCH.md §"Open Question 2" (line 1269-1272) — python3.13t CI recipe choices
    - crates/pyscf-py/Cargo.toml (plan 03-07) — confirms the `free-threading` feature flag
  </read_first>
  <behavior>
    - After this plan, `.github/workflows/ci.yml` has 10 jobs total (6 existing + 4 new)
    - `maturin-smoke` depends on `build-default` and verifies abi3-py310 wheel imports cleanly (BIND-01)
    - `stride-fuzz` runs the BIND-04 pytest from plan 03-02's stub (will pass with xfail-skipped tests in Wave 3; becomes meaningful when plan 03-10 unmarks)
    - `xplat-uhartree` runs as a matrix across ubuntu-latest + macos-14 (aarch64); both run the H2O RHF test under release-oracle (Pitfall 12). HARD failure if maturin <1.4 OR `--profile release-oracle` rejected — NO FALLBACK.
    - `python313t-smoke` uses deadsnakes 3.13t interpreter + non-abi3 maturin build + an explicit `import pyscf._native` load assertion + a tiny SCF smoke to detect deadlocks (BIND-05)
    - No existing job is modified
  </behavior>
  <action>
    1. Read `.github/workflows/ci.yml` in full (~100 lines per initial inspection) to identify the EXACT position to append (after the last existing job, before the file's final blank-line/EOF).

    2. Append the 4 new jobs verbatim from the `<interfaces>` block above. The full YAML
       to APPEND (matching indentation level — 2 spaces under `jobs:`):
       ```yaml

         # ── Phase 3 plan 03-09 ─────────────────────────────────────────────
         # 4 new jobs per RESEARCH §Validation Architecture + VALIDATION.md §Wave 0.
         # See plan 03-09-PLAN.md for the contract each job validates.
         # Checker iteration 1: BLOCKER 2 + NIT 6 remediations applied (2026-05-11).

         maturin-smoke:
           name: maturin develop + pyscf._native importable (BIND-01)
           runs-on: ubuntu-latest
           needs: [build-default]
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-python@v5
               with:
                 python-version: "3.11"
             - uses: dtolnay/rust-toolchain@stable
             - uses: Swatinem/rust-cache@v2
             - name: Install maturin (>=1.4 required for --profile flag)
               run: pip install "maturin>=1.4,<2.0" "numpy>=1.26"
             - name: Build + install pyscf-rs into venv
               run: maturin develop --release
             - name: Verify _native module imports
               run: python -c "from pyscf._native import scf; assert hasattr(scf,'RHF') and hasattr(scf,'UHF') and hasattr(scf,'GHF'); print('OK')"

         stride-fuzz:
           name: BIND-04 stride-fuzz (a/a.T/a[::2]/a[:,1:5] return identical)
           runs-on: ubuntu-latest
           needs: [maturin-smoke]
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-python@v5
               with:
                 python-version: "3.11"
             - uses: dtolnay/rust-toolchain@stable
             - uses: Swatinem/rust-cache@v2
             - run: pip install "maturin>=1.4,<2.0" "pytest>=7.0" "numpy>=1.26"
             - run: maturin develop --release
             - run: pytest python/pyscf/tests/test_scf_stride_fuzz.py -x

         xplat-uhartree:
           name: Linux x86_64 + macOS aarch64 µHartree (Pitfall 12)
           needs: [maturin-smoke]
           strategy:
             fail-fast: false
             matrix:
               os: [ubuntu-latest, macos-14]
           runs-on: ${{ matrix.os }}
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-python@v5
               with:
                 python-version: "3.11"
             - uses: dtolnay/rust-toolchain@stable
             - uses: Swatinem/rust-cache@v2
             # CRITICAL (checker iteration 1 BLOCKER 2): pin maturin >=1.4 so that
             # `--profile release-oracle` is honored (earlier maturin silently
             # falls back to [profile.release], which is FMA-enabled, voiding
             # Pitfall 12). No `||` fallback allowed.
             - name: Install maturin (>=1.4 — supports --profile)
               run: pip install "maturin>=1.4,<2.0" "pytest>=7.0" "numpy>=1.26" "h5py>=3.10"
             - name: Assert maturin >=1.4
               run: |
                 MATURIN_VER=$(maturin --version | awk '{print $2}')
                 python -c "import sys; v=tuple(int(x) for x in '$MATURIN_VER'.split('.')[:2]); sys.exit(0 if v >= (1,4) else 1)" || \
                   (echo "FATAL: maturin $MATURIN_VER does not support --profile (need >=1.4). xplat-uhartree depends on release-oracle profile; --release would be FMA-enabled (Pitfall 12)."; exit 1)
             - name: Build pyscf-py under release-oracle profile (HARD REQUIREMENT, NO FALLBACK)
               # If `maturin develop --profile release-oracle` fails, this step
               # MUST exit non-zero. The previous `|| maturin develop --release`
               # fallback silently dropped to an FMA-enabled profile, voiding
               # the µHartree assertion. See checker iteration 1 BLOCKER 2.
               run: maturin develop --profile release-oracle
             - name: H2O/cc-pVDZ RHF — assert ≤ 1 µHartree vs upstream
               run: pytest python/pyscf/tests/test_scf_rhf_h2o.py -x --tb=short

         python313t-smoke:
           name: python3.13t SCF smoke (BIND-05; NON-abi3 build per Pitfall (NEW))
           runs-on: ubuntu-latest
           needs: [build-default]
           steps:
             - uses: actions/checkout@v4
             - name: Install Python 3.13t (free-threaded)
               # deadsnakes PPA path. Alternative: `uv python install 3.13t`.
               # If deadsnakes lacks 3.13-nogil on the runner image, switch to uv.
               run: |
                 sudo add-apt-repository -y ppa:deadsnakes/ppa
                 sudo apt-get update
                 sudo apt-get install -y python3.13-nogil || (
                   pip install uv && uv python install 3.13t
                 )
             - uses: dtolnay/rust-toolchain@stable
             - uses: Swatinem/rust-cache@v2
             - name: Create 3.13t venv with maturin
               run: |
                 PY=$(which python3.13t || which python3.13)
                 "$PY" -m venv /tmp/venv313t
                 /tmp/venv313t/bin/pip install "maturin>=1.4,<2.0" "numpy>=1.26"
             - name: Build pyscf-py (non-abi3 free-threading)
               run: |
                 # CRITICAL per RESEARCH §"Pitfall (NEW)": abi3 wheels are incompatible
                 # with 3.13t. We use --no-default-features --features free-threading
                 # (drops abi3-py310 from the feature set) for this job ONLY.
                 /tmp/venv313t/bin/maturin develop \
                   --no-default-features --features free-threading
             # ── Checker iteration 1 NIT 6 ──────────────────────────────────
             # Assert the extension LOADS under 3.13t BEFORE the SCF smoke
             # runs. A segfault on `import pyscf._native` would otherwise
             # mask itself behind the `run: python -c "from pyscf import ..."`
             # shell exit code (which only fires after import succeeds).
             - name: Assert pyscf._native imports cleanly under 3.13t
               run: |
                 /tmp/venv313t/bin/python -c "import pyscf._native; print(pyscf._native.__doc__)"
             - name: SCF smoke (probe for GIL deadlock)
               run: |
                 /tmp/venv313t/bin/python -c "
                 from pyscf import gto, scf
                 mol = gto.M(
                     atom='O 0 0 0; H 0 0.587 0.757; H 0 0.587 -0.757',
                     basis='sto-3g',
                 )
                 mf = scf.RHF(mol).run()
                 assert mf.converged, 'SCF failed under python3.13t (deadlock?)'
                 print(f'OK 3.13t SCF e_tot = {mf.e_tot}')
                 "
       ```

       NOTE: The macos-14 runner image is aarch64. If the runner image's name differs
       in the GitHub Actions catalog at execution time (e.g., `macos-14-arm64` or
       `macos-latest-aarch64`), update the matrix to match.
  </action>
  <verify>
    <automated>grep -cE "(maturin-smoke|stride-fuzz|xplat-uhartree|python313t-smoke):" .github/workflows/ci.yml | grep -E "^4$" && grep -F "free-threading" .github/workflows/ci.yml && grep -F "maturin>=1.4" .github/workflows/ci.yml && grep -F "import pyscf._native" .github/workflows/ci.yml && ! grep -F "|| maturin develop --release" .github/workflows/ci.yml</automated>
  </verify>
  <done>4 new jobs added to ci.yml; each cites its REQ-ID + the contract it validates; abi3 default build is unchanged; free-threading build uses --no-default-features --features free-threading per RESEARCH §"Pitfall (NEW)" abi3/3.13t separation; macOS aarch64 covered (Pitfall 12); python3.13t covered (BIND-05); xplat-uhartree uses `--profile release-oracle` HARD with NO fallback (BLOCKER 2 closed); python313t-smoke asserts `import pyscf._native` BEFORE SCF smoke (NIT 6 closed).</done>
</task>

</tasks>

<verification>
- `grep -F "maturin-smoke:" .github/workflows/ci.yml` finds the job
- `grep -F "python313t-smoke:" .github/workflows/ci.yml` finds the job
- `grep -F "macos-14" .github/workflows/ci.yml` finds the macOS matrix entry
- `grep -F "free-threading" .github/workflows/ci.yml` finds the non-abi3 build flag
- `grep -F "from pyscf._native import scf" .github/workflows/ci.yml` finds the BIND-01 import smoke
- `grep -F "maturin>=1.4" .github/workflows/ci.yml` finds the version pin (BLOCKER 2)
- `! grep -F "|| maturin develop --release" .github/workflows/ci.yml` confirms NO `||` fallback (BLOCKER 2)
- `grep -F "import pyscf._native" .github/workflows/ci.yml` finds the load assertion (NIT 6)
- No existing job (fmt, clippy, build-default, build-gpu, test, oracle-determinism) is altered
</verification>

<success_criteria>
- BIND-01 (maturin abi3-py310 wheel skeleton) validated by CI
- BIND-04 (stride-fuzz) wired (becomes meaningful when plan 03-10 unmarks pytest stubs)
- BIND-05 (python3.13t free-threaded build) is on a separate non-abi3 path — RESEARCH §"Pitfall (NEW)" mitigated
- Pitfall 12 cross-platform µHartree assertion runs on every PR under release-oracle (FMA-free) — checker iteration 1 BLOCKER 2 fixed
- python313t segfault-on-import surfaces as a CI failure — checker iteration 1 NIT 6 fixed
- Phase 8 owns: abi3audit (BIND-08), auditwheel show (DIST-04), per-backend extras (DIST-03)
</success_criteria>

<output>
After completion, create `.planning/phases/03-scf-pyo3-bindings/03-09-SUMMARY.md` documenting:
- 4 new jobs added (names, dependencies, runners)
- Resolution of RESEARCH Open Question 2 (python3.13t CI recipe — deadsnakes vs uv)
- macOS aarch64 runner image name confirmed at land time (macos-14 vs macos-latest)
- Confirmation that abi3 + 3.13t are on SEPARATE build configurations (CRITICAL Pitfall (NEW) mitigation)
- Confirmation that xplat-uhartree uses `--profile release-oracle` exclusively (no `||` fallback) — Pitfall 12 guarantee preserved
- Confirmation that python313t-smoke asserts `import pyscf._native` before SCF smoke (segfault-on-import surfaces)
- Phase 8 deferred items: abi3audit, auditwheel show, per-backend extras
</output>
