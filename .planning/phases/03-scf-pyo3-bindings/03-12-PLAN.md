---
phase: 03
plan: 12
type: execute
wave: 12
gap_closure: true
depends_on: ["03-05", "03-11", "05-08", "05-09"]
status: READY
files_modified:
  - crates/pyscf-scf/tests/no_overrides_drives_kernel.rs
  - crates/pyscf-scf/tests/dfhf_end_to_end.rs
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: ["SCF-07"]
user_setup: []

must_haves:
  truths:
    - "With int2e (05-08) + the robust DF metric (05-09), DF-HF runs end-to-end: a real RHF SCF converges and `density_fit` + DfHooks converges to a DF-HF energy that matches the non-DF RHF energy within DF accuracy."
    - "Plain RHF on H2/STO-3G converges to ≈ -1.1168 Hartree (the int2e_sph dispatch gap that `#[ignore]`d this is closed) — using the working `1e` init guess (the default `minao` guess lands in 03-13)."
    - "DF-HF[weigend] and DF-HF[cc-pvdz-jkfit] converge and match non-DF RHF to ≤ ~1 mHartree (observed weigend 45 µHa, jkfit 200 µHa) — `get_jk_df` + the rank-revealing metric fit are numerically correct."
    - "Upstream-PySCF byte-identity DF-HF energy stays the CI-gated/human-verify arm (sandbox lacks numpy/PySCF)."
  artifacts:
    - path: "crates/pyscf-scf/tests/dfhf_end_to_end.rs"
      provides: "Always-on DF-HF end-to-end convergence + DF-vs-RHF cross-check"
      contains: "density_fit"
  key_links:
    - from: "crates/pyscf-scf/tests/dfhf_end_to_end.rs"
      to: "pyscf_scf::DfHooks + pyscf_df::cholesky_eri"
      via: "kernel(&mol, &DfHooks{df}, cfg) converges"
      pattern: "DfHooks"
---

<objective>

**Lock in DF-HF end-to-end (SCF-07) with always-on tests — Option 1.**

DF-HF already works: with int2e (05-08) and the robust rank-revealing DF metric
fit (05-09), `RHF::density_fit(aux)` + `DfHooks` + the SCF kernel converge to a
correct DF-HF energy. An experiment confirmed (H2/STO-3G, `1e` guess):

| Calc | e_tot (Ha) | vs RHF |
|------|-----------|--------|
| RHF (exact) | -1.1167593 | — |
| DF-HF[weigend] | -1.1168048 | 45 µHa |
| DF-HF[cc-pvdz-jkfit] | -1.1165595 | 200 µHa |

This plan locks that in with always-on tests and un-ignores the long-gated RHF
convergence test (the int2e_sph dispatch gap that blocked it is closed).

**Scope note:** the working init guess here is `1e`. The DEFAULT `minao` guess is
still `NotYetImplemented` — implemented separately in 03-13. So this plan uses an
explicit `init_guess = OneElectron`; `kernel_propagates_jk_not_yet_implemented`
(which asserts the default `minao` path errors) stays valid until 03-13 flips it.

No new crate dep; libxc NEVER compiled (pyscf-scf/-df test scope excludes pyscf-dft).
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@crates/pyscf-scf/src/df_scf.rs
@crates/pyscf-scf/src/kernel.rs
@crates/pyscf-scf/tests/no_overrides_drives_kernel.rs

<interfaces>
- `pyscf_scf::kernel(&mol, &hooks, KernelConfig)` → `ScfResult { e_tot: Energy, converged, cycles, .. }`.
- `KernelConfig { init_guess: InitGuessMode::OneElectron, ..Default::default() }` — `1e` works; `Minao` (the default) is `NotYetImplemented` until 03-13.
- `pyscf_scf::NoOverrides` — default hooks (get_jk via int2e). `pyscf_scf::DfHooks { df: &DfIntegrals }` — routes get_jk/get_veff through `pyscf_df::get_jk_df`.
- `pyscf_df::cholesky_eri(&mol, aux)` — builds the B-tensor (rank-revealing metric fallback, 05-09).
- Aux names that load: `"weigend"`, `"cc-pvdz-jkfit"`, `"sto-3g"` (poor minimal aux — large DF error, do NOT use for the match assertion).
- `Energy` is a newtype: read `.0` for the f64.
</interfaces>
</context>

<tasks>

<task type="implement">
  <name>Task 1: un-ignore RHF-converges + add DF-HF end-to-end test</name>
  <files>crates/pyscf-scf/tests/no_overrides_drives_kernel.rs, crates/pyscf-scf/tests/dfhf_end_to_end.rs</files>
  <steps>
  1. `no_overrides_drives_kernel.rs`: remove the `#[ignore]` on `h2_no_overrides_converges`
     and set `init_guess: InitGuessMode::OneElectron` in its config (the default `minao`
     is NotYetImplemented until 03-13). Keep the converged + `e_tot ∈ (-2,-1)` assertions.
     Update the stale `#[ignore]`-rationale doc comment (int2e_sph gap is closed). Leave
     `kernel_propagates_jk_not_yet_implemented` as-is (default minao still errors).
  2. NEW `dfhf_end_to_end.rs`: build H2/STO-3G; run plain RHF (NoOverrides, 1e) → record
     e_rhf (assert converged, ≈ -1.117). For aux ∈ {"weigend","cc-pvdz-jkfit"}: build
     `cholesky_eri(&mol, aux)`, run `kernel(&mol, &DfHooks{df:&df}, 1e)` → assert converged,
     finite, and `|e_dfhf - e_rhf| < 1e-3` (DF accuracy). Assert the DF-HF energy is NOT
     the exact RHF (DF is an approximation) only loosely — the upper bound is the contract.
  3. Optional: assert the sto-3g-as-aux path also converges (no panic) but do NOT assert it
     matches RHF (minimal aux → large DF error, by design).
  </steps>
  <verify>cargo test -p pyscf-scf --test no_overrides_drives_kernel --test dfhf_end_to_end green; clippy -p pyscf-scf -D warnings; fmt; xtask check-no-fma + check-dependency-wall PASS; 0 libxc in pyscf-scf tree.</verify>
  <commit>test(03-12): lock in DF-HF end-to-end — RHF + DF-HF converge, DF matches RHF within DF accuracy</commit>
</task>

<task type="implement">
  <name>Task 2: tracking</name>
  <files>.planning/REQUIREMENTS.md</files>
  <steps>
  1. REQUIREMENTS.md: add a note under the SCF block that DF-HF (SCF-07) numeric is now
     proven end-to-end in-tree (converges, matches non-DF RHF within DF accuracy) via the
     05-08 int2e + 05-09 DF-metric robustness; default `minao` init guess lands in 03-13;
     upstream byte-identity is CI-gated/human-verify.
  2. (orchestrator writes STATE decision + SUMMARY after execution.)
  </steps>
  <verify>doc-only; no test impact.</verify>
  <commit>docs(03-12): record DF-HF end-to-end (SCF-07) numeric closure</commit>
</task>

</tasks>

<verification>
**Goal:** DF-HF (SCF-07) proven end-to-end in-tree. Gates:
- `cargo test -p pyscf-scf` green (incl. un-ignored h2_no_overrides_converges + new dfhf_end_to_end)
- RHF converges ≈ -1.117; DF-HF[weigend/jkfit] converges and matches RHF to < 1 mHartree
- clippy -D warnings + fmt; xtask check-no-fma + check-dependency-wall PASS; 0 libxc

**Out of scope:** the `minao` default init guess (03-13); upstream-PySCF byte-identity
(CI-gated/human-verify); UHF/GHF density-fit; DF-DFT (Phase 4, its own path).
</verification>

<threat_model>
- **T-03-12-NUM:** assert DF-HF matches RHF within a DF-accuracy bound (≤ 1 mHartree for weigend/jkfit), NOT bit-exact — DF is an approximation. Do not assert against the sto-3g minimal aux.
- **T-03-12-SCOPE:** use the `1e` guess (minao is 03-13); do not change the default init guess.
- **T-03-12-COMPILE:** pyscf-scf/-df test scope excludes pyscf-dft → 0 libxc. Keep it so.
</threat_model>
