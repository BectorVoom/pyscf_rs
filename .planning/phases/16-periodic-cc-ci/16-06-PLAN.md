---
phase: 16-periodic-cc-ci
plan: 06
type: build
wave: 3
depends_on: ["16-05"]
files_modified:
  - crates/pyscf-pbc-cc/src/kintermediates_uhf.rs
  - crates/pyscf-pbc-cc/src/kccsd_uhf.rs
  - crates/pyscf-pbc-cc/src/lib.rs
  - crates/pyscf-pbc-cc/tests/kccsd_uhf.rs
autonomous: false
requirements: []
must_haves:
  truths:
    - "KUCCSD's kernel DOES exist upstream — unlike KUMP2 (15-CONTEXT §1.3). kccsd_uhf.py:1022 is the only live refusal and it is NOT the kernel; port the kernel, do not refuse it"
    - "the padding surface comes from pbc.mp.kump2 (two spin channels), not kmp2 — kccsd_uhf.py:36"
    - "every contraction site names its primitive (16-CONTEXT §3.2)"
    - "the three-tier Wvvvv/WvvVV/WVVVV storage branch ships, not collapsed to incore"
  artifacts:
    - path: "crates/pyscf-pbc-cc/src/kccsd_uhf.rs"
      provides: "KUCCSD kernel + unrestricted k-point intermediates"
      contains: "e_corr"
---

<objective>
Port `pyscf/pbc/cc/kintermediates_uhf.py` (1225 l) and `kccsd_uhf.py` (1116 l)
— `§8.8`'s plan 16-03, unchanged in scope.

**One correction worth stating up front, because the sibling phase went the
other way.** `15-CONTEXT §1.3` found `KUMP2`'s energy kernel is
`raise NotImplementedError` upstream at three sites and ruled that this port
ships the same refusal. **`KUCCSD` is not that case.** `kccsd_uhf.py` has a
real, reachable kernel; its only live refusal is `:1022`, and `:486`, `:505`,
`:525` and `:842` are all commented out. Verify this at the top of the plan
and port the kernel.
</objective>

<context>
@.planning/phases/16-periodic-cc-ci/16-CONTEXT.md   # §1.1 the Phase-15 block, §1.5 what IS absent upstream
@.planning/phases/16-periodic-cc-ci/16-REVIEW.md    # §2, §5
@crates/pyscf-ccsd/src/uccsd.rs                     # the molecular sibling: mirror its shape
@crates/pyscf-ccsd/src/uintermediates.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: confirm what upstream actually refuses, before porting</name>
  `grep -n "NotImplementedError" pyscf/pbc/cc/kccsd_uhf.py`. Exactly one live
  site (`:1022`); four commented (`:486`, `:505`, `:525`, `:842`). Record what
  `:1022` is and port the same refusal with the upstream line in the payload,
  plus an oracle-gated test asserting upstream still raises there — the
  discipline `15-CONTEXT §1.3` set, so a refusal cannot outlive its reason.

  Confirm the padding surface is `pbc.mp.kump2`'s two-spin-channel version
  (`kccsd_uhf.py:36`), not `kmp2`'s. If 15-04 has not shipped it, stop and
  defer as 16-04 Task 1 does.
</task>

<task type="auto">
  <name>Task 2: `kintermediates_uhf`</name>
  The `aa` / `ab` / `bb` spin-block triplication of 16-04's intermediates.
  Mirror `crates/pyscf-ccsd/src/uintermediates.rs`'s module shape.

  `Wvvvv`/`WvvVV`/`WVVVV` (`kccsd_uhf.py:1110`) are three tensors of the
  `nkpts³ · nvir⁴` class, not one — the memory ceiling is **~3×** 16-04's
  single `Wvvvv` for the same cell, and the tier branch matters
  correspondingly more. `16-REVIEW.md §2.3`'s table gives the numbers.
</task>

<task type="auto">
  <name>Task 3: `KUCCSD` — `_ERIS`, `update_amps`, `energy`, `kernel`</name>
  Same structure as 16-05, per spin block. The `symm_map` loop applies
  identically (`16-CONTEXT §3.1`) and ships in the first version.
</task>

<task type="auto">
  <name>Task 4: tests</name>
  1. **Spin-restricted reduction, no oracle** — on a closed-shell fixture,
     `KUCCSD.e_corr` equals 16-05's `KRCCSD.e_corr` to 1e-11. The single
     highest-value test in the plan.
  2. **Supercell equivalence** — upstream has a dedicated file for this,
     `pyscf/pbc/cc/test/test_kuccsd_supercell_vs_kpts.py`; reproduce its
     comparison. Oracle-free.
  3. **Γ-point reduction** against `pyscf-ccsd`'s molecular UCCSD to 1e-12.
  4. **Open-shell** — reproduce `test_kuccsd_openshell.py`'s system; this is
     the only fixture in the phase where `nocca != noccb` and the k-dependent
     padding actually has something to pad.
  5. Tier crossing, symm_map equivalence, determinism on `t1`/`t2`/`e_corr`
     — as 16-05 Tasks 4.4, 4.5, 4.7.
  6. `e_corr` vs upstream per DF route at 16-01's measured tolerance.
</task>

</tasks>

<verification>
* `cargo test -p pyscf-pbc-cc --release` green.
* Tests 1-4 are oracle-free and pass before test 6 is written.
* The `:1022` refusal has a live oracle-gated test asserting upstream still
  raises.
* `check-orphan-modules` and `check-dependency-wall` PASS.
</verification>
