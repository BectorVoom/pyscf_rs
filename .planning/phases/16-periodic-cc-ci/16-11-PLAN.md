---
phase: 16-periodic-cc-ci
plan: 11
type: build
wave: 5
depends_on: ["16-09", "16-06"]
files_modified:
  - crates/pyscf-pbc-cc/src/eom_kccsd_uhf.rs
  - crates/pyscf-pbc-cc/src/lib.rs
  - crates/pyscf-pbc-cc/tests/eom_kccsd_uhf.rs
autonomous: false
requirements: []
must_haves:
  truths:
    - "UHF EE DOES NOT EXIST UPSTREAM: eom_kccsd_uhf.py declares EOMIP (:458) and EOMEA (:959) and NO EOMEE class at all; _IMDS.make_ee (:1120) is raise NotImplementedError (16-CONTEXT §1.5)"
    - "this port ships the same absence and the same refusal, with the upstream line, and an oracle-gated test asserting upstream still raises"
    - "the padding surface is kump2's two-spin-channel version (eom_kccsd_uhf.py:34)"
  artifacts:
    - path: "crates/pyscf-pbc-cc/src/eom_kccsd_uhf.rs"
      provides: "EOM-KCCSD-UHF IP/EA; EE refused as upstream refuses it"
      contains: "make_ee"
---

<objective>
Port `pyscf/pbc/cc/eom_kccsd_uhf.py` (1275 l) — `§8.8`'s plan 16-07, now
downstream of the GHF base.

**`ROADMAP.md`'s "EOM-KCCSD IP/EA/EE (RHF/UHF/GHF)" is wrong for UHF.** The
module has no `EOMEE` class, and its intermediates refuse:

```
pyscf/pbc/cc/eom_kccsd_uhf.py:458   class EOMIP(eom_kgccsd.EOMIP)
pyscf/pbc/cc/eom_kccsd_uhf.py:959   class EOMEA(eom_kgccsd.EOMEA)
pyscf/pbc/cc/eom_kccsd_uhf.py:1120      def make_ee(self): raise NotImplementedError
```

This is the same shape as `15-CONTEXT §1.3`'s finding for `KUMP2`. RULE 2 makes
the Python authoritative: ship the surface that exists and the refusal that
exists. Inventing a k-point UHF-EE kernel here is the one thing no oracle in
this workspace could check.
</objective>

<context>
@.planning/phases/16-periodic-cc-ci/16-CONTEXT.md   # §1.5
@.planning/phases/15-periodic-ao2mo-kmp2/15-CONTEXT.md  # §1.3 — the precedent for shipping a refusal
@crates/pyscf-pbc-cc/src/eom_kccsd_ghf.rs           # 16-09's base classes
</context>

<tasks>

<task type="auto">
  <name>Task 1: IP and EA, per spin block</name>
  `:458` `EOMIP` and `:959` `EOMEA`, on 16-06's `KUCCSD` amplitudes and
  `kintermediates_uhf`. `:509` is a live refusal — port it with its line.
</task>

<task type="auto">
  <name>Task 2: the EE absence, made explicit</name>
  Do **not** create an `EOMEE` type for UHF. `_IMDS::make_ee` returns
  `NotYetImplemented` whose payload names `eom_kccsd_uhf.py:1120` and states
  that upstream PySCF 2.12.1 has no kernel either.

  An oracle-gated test asserts upstream still raises there, so the refusal
  cannot outlive its reason — if a future PySCF implements it, the test fails
  and this port is told to catch up.
</task>

<task type="auto">
  <name>Task 3: tests</name>
  1. **Koopmans limit**, oracle-free, written first.
  2. **Spin-restricted reduction** — on a closed-shell fixture, UHF IP/EA
     roots equal 16-10's RHF roots to 1e-10. Oracle-free, and the main gate.
  3. **UHF == GHF roots** on the same fixture to 1e-10.
  4. Open-shell roots vs upstream (`test_eom_kuccsd.py`,
     `test_eom_kuccsd_diag.py`) at 16-01 Task 4's tolerance.
  5. The `make_ee` refusal test (Task 2).
  6. Determinism (§9.3); peak-memory bound per `16-REVIEW.md §4.1`.
</task>

</tasks>

<verification>
* `cargo test -p pyscf-pbc-cc --release` green.
* Tests 1-3 are oracle-free and pass before test 4 is written.
* `grep -rn "EOMEE" crates/pyscf-pbc-cc/src/eom_kccsd_uhf.rs` finds no type
  definition — the absence is the deliverable.
* `check-orphan-modules` and `check-dependency-wall` PASS.
</verification>
