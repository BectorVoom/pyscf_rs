---
phase: 16-periodic-cc-ci
plan: 14
type: verify
wave: 6
depends_on: ["16-05", "16-06", "16-07", "16-08", "16-09", "16-10", "16-11", "16-12", "16-13"]
files_modified:
  - .planning/phases/16-periodic-cc-ci/16-VERIFICATION.md
  - .planning/phases/16-periodic-cc-ci/16-REVIEW.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/pbc/PBC-MASTER-PLAN.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "every gate asserted here is one 16-01 MEASURED; no number in this plan was invented by it"
    - "each energy gate names its DF route, fixture, mesh and cell.precision"
    - "a gate that FAILS is recorded as a failure with its number, never absorbed by loosening the tolerance (17-08's GDF Gate C is the precedent)"
    - "the speed and memory claims of D-PBC-29 are re-measured against the shipped code, not restated from the review"
  artifacts:
    - path: ".planning/phases/16-periodic-cc-ci/16-VERIFICATION.md"
      provides: "the phase's measured close-out"
      contains: "e_corr"
---

<objective>
Close Phase 16 against the gates 16-01 measured — not against the gates
`ROADMAP.md` and `PBC-MASTER-PLAN §7` carried before it
(`1e-14` and `1e-8`, six orders apart, neither measured; `16-CONTEXT §2`).

This plan measures; it does not negotiate. A gate that fails is written down
with the number it reached and the hypothesis for why, the way 17-08 recorded
its GDF Gate C failure at 1.43e-6 rather than absorbing it.
</objective>

<context>
@.planning/phases/16-periodic-cc-ci/measurements/README.md   # 16-01's measured gates — the authority
@.planning/phases/16-periodic-cc-ci/16-CONTEXT.md
@.planning/phases/16-periodic-cc-ci/16-REVIEW.md             # §6 D-PBC-29 — the claims to re-measure
@.planning/phases/14-gdf-mdf-rsdf-rsjk/14-VERIFICATION.md    # the format to copy
</context>

<tasks>

<task type="auto">
  <name>Task 1: the correctness gates</name>
  Per DF route (FFTDF / GDF / MDF / RSDF), per fixture (`diamond`, `si`,
  `he_fcc` all-electron control, `graphene` for the `dimension = 2` /
  Madelung-overlap path), mesh and `cell.precision` pinned on both sides:

  * `KRCCSD` `e_corr` vs upstream;
  * `KUCCSD` and `KGCCSD` `e_corr` vs upstream;
  * `KCCSD(T)` `e_tot` vs upstream, and fast-vs-slow within this port;
  * EOM IP/EA/EE-singlet roots vs upstream;
  * `KCIS` roots vs upstream.

  Report the port's own `|E(FFTDF) − E(GDF)|` **as a ratio** to upstream's own
  inter-route gap, the way 14-VERIFICATION Gate 3 does — that ratio, not the
  absolute difference, is what says whether this port reproduces upstream's
  disagreement or adds its own.
</task>

<task type="auto">
  <name>Task 2: the oracle-free gates, collected</name>
  These need no PySCF and are the ones that survive an oracle drift:
  supercell equivalence (16-05), the Γ reductions against `pyscf-ccsd`
  (16-04/05/06/07/12), `KUCCSD == KRCCSD` closed-shell, `KGCCSD == KRCCSD`
  closed-shell, RHF/UHF/GHF EOM root agreement, Davidson-vs-dense (16-09,
  16-13), fast-vs-slow (T) (16-08), `Tr(rdm1) == nelec` (16-12), and the
  `MP2 init_amps == KMP2` cross-phase check (16-05).

  Table them with their measured residuals. **This table is the phase's real
  proof**; the upstream comparisons are the sanity check on it.
</task>

<task type="auto">
  <name>Task 3: determinism (§9.3)</name>
  Bit-identity at `RAYON_NUM_THREADS` 1 and 8 of `t1`, `t2`, `e_corr`, the (T)
  correction, and every EOM/CIS root — **`t1`/`t2` as well as the energy**
  (`16-CONTEXT §3.7`). Confirm each accumulation of the `nkpts³` class runs
  through `oracle_sum`/`oracle_zdot`/`oracle_zdotu`, and that the primitive
  chosen at each site matches the conjugation upstream uses (`§3.2`).
</task>

<task type="auto">
  <name>Task 4: re-measure D-PBC-29's four claims against the shipped code</name>
  The review derived these; this task measures them:

  1. the complex arena's byte accounting is exact (a reservation's reported
     size equals `product * 16`) and the HARD refusal fires at the boundary;
  2. host rayon loops beat `zgemm_dense` on this workspace's default CPU
     backend for the phase's actual contraction shapes — or they do not, in
     which case the ruling is amended and the measurement recorded;
  3. `symm_map` delivers the wall-clock ratio 16-01 Task 6 predicted;
  4. every storage tier is exercised by at least one green test, and the peak
     `ZWorkspacePool` live bytes for each shipped path is below the bound its
     plan asserted.
</task>

<task type="auto">
  <name>Task 5: close out, including what did not ship</name>
  Update `ROADMAP.md`, `STATE.md` and `PBC-MASTER-PLAN §7`/`§8.8` together with
  the measured numbers, and list the carry-overs explicitly with the reason and
  the unblocking phase for each:

  * `pbc/ci/cisd.py` — needs molecular RCISD/UCISD/GCISD (16-13 Task 1);
  * EOM-EE triplet / spin-flip (RHF) and EOM-EE (UHF) — **absent upstream**,
    not deferred by this port (`16-CONTEXT §1.5`);
  * `kuccsd_rdm`'s `with_frozen` path — absent upstream (16-12);
  * anything 16-04 Task 1 or 16-06 Task 1 had to defer because Phase 15 had
    not landed.

  Then unblock **17-09** by name: `PBC-MASTER-PLAN §8.9` defers its CC half
  "if Phase 16 has shipped `KRCCSD`" (+1071 l: `kccsd_rhf_ksymm.py` 806 +
  `kintermediates_rhf_ksymm.py` 265). Say plainly whether it is now unblocked.
</task>

</tasks>

<verification>
* `16-VERIFICATION.md` exists; every number in it is reproducible from a
  committed command, and every tolerance traces to `measurements/README.md`.
* No tolerance in the phase was loosened during this plan. If a gate failed,
  it is recorded as a failure with its measured number and a hypothesis.
* `ROADMAP.md`, `STATE.md`, `PBC-MASTER-PLAN §7` and `§8.8` agree with each
  other and with `16-VERIFICATION.md` on every gate number.
* The full workspace suite is green, or every non-green test is named with its
  reason.
</verification>
