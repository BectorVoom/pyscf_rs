---
phase: 20-pbc-python-bindings
plan: 06
type: execute
wave: 2
depends_on: [01]
files_modified:
  - crates/pyscf-pbc-scf/src/rsjk.rs
  - .planning/carryovers/D-PBC-24-cintx-range-omega-PLAN.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "DEFAULT ACTION is to re-assess and update the carryover; implementing rsjk is only in scope if Task 1 shows the machinery is now sufficient"
    - "a documented refusal is an acceptable outcome; a silently wrong number is not"
    - "rsjk was blocked on the Phase-17 supermole (ft_ao::RsCell + ExtendedMole::strip_basis), NOT on cintx range_omega — range_omega was necessary but not sufficient"
    - "get_jk_mpi (rsjk.rs:182) is a phase-19 refusal and stays refusing regardless of this plan's outcome"
  artifacts:
    - path: ".planning/carryovers/D-PBC-24-cintx-range-omega-PLAN.md"
      provides: "an accurate, current statement of what still blocks rsjk"
---

<objective>
**Decide whether `rsjk` is still blocked — and record the answer either way.**

```rust
// crates/pyscf-pbc-scf/src/rsjk.rs:149  build   -> NotYetImplemented { phase: 14, what: RS_BUILDER_GAP }
// crates/pyscf-pbc-scf/src/rsjk.rs:163  get_jk  -> same
```

The D-PBC-24 carryover is explicit that this is **not** the blocker the plan
was originally written about:

> `rsjk` (14-08 Task 4) — **BLOCKED ON PHASE 17, NOT ON CINTX.** `range_omega`
> was necessary but NOT sufficient. `rsjk.py:150-200` builds its short-range
> half from `ft_ao._RangeSeparatedCell` + `ExtendedMole.strip_basis` (the
> supermole this port defers to Phase 17 under D-PBC-21/23).

Phase 17 has since closed, and the Rust tree now has both pieces —
`RsCell` (`ft_ao/rs_cell.rs:56`) and `ExtendedMole` (`ft_ao/supmol.rs:57`). So
the premise may no longer hold.

**The default action of this plan is to re-assess and update the carryover.**
Implementation is in scope *only* if Task 1 shows the machinery is sufficient.
This is deliberately not a judgment call left open: do Task 1, then follow the
branch it selects.
</objective>

<context>
@.planning/carryovers/D-PBC-24-cintx-range-omega-PLAN.md      # the blocker as recorded
@crates/pyscf-pbc-scf/src/rsjk.rs                             # :94 builder, :128 guess_omega, :149/:163/:182 the three refusals
@crates/pyscf-pbc-df/src/rsdf_builder/mod.rs                  # :95 RS_BUILDER_GAP — the `what` string
@crates/pyscf-pbc-df/src/ft_ao/rs_cell.rs                     # :56 RsCell — was missing, now exists
@crates/pyscf-pbc-df/src/ft_ao/supmol.rs                      # :57 ExtendedMole — was missing, now exists
@pyscf/pbc/scf/rsjk.py                                        # :150-200 the SR build, :267-436 _get_jk_sr
</context>

<tasks>

<task type="auto">
  <name>Task 1: the sufficiency check — do this FIRST, it selects the branch</name>
  Read `rsjk.py:150-200` and `_get_jk_sr:267-436` and list every upstream
  construct they consume. For each, record whether a Rust equivalent exists
  today, with `file:line`, specifically:

  - `_RangeSeparatedCell` → `ft_ao/rs_cell.rs:56`?
  - `ExtendedMole.strip_basis` → `ft_ao/supmol.rs:57`?
  - `get_q_cond` / the Schwarz prescreen (`rsdf_helper.py`) → present or absent?
  - the screened periodic 4-centre `int2e` over the supermole → present or absent?

  **If every row has a Rust equivalent → branch A (Task 2). If any row is
  absent → branch B (Task 3).**
</task>

<task type="implement">
  <name>Task 2 (branch A only): implement build + get_jk</name>
  Wire `RangeSeparatedJkBuilder::build` and `get_jk` over `RsCell` +
  `ExtendedMole`, and gate against upstream `rsjk` on He-fcc 2×2×2 at the
  Phase-14 Gate-3 floor `1.353e-08`. Delete `:149` and `:163` only when the
  gate passes. Leave `:182` (`get_jk_mpi`, phase 19) refusing.
</task>

<task type="auto">
  <name>Task 3 (branch B only): update the carryover and stop</name>
  Rewrite the carryover's `status:` block to name **exactly** which construct is
  still missing, with the upstream `file:line` that needs it and the Rust file
  that would host it. Delete the now-stale claim that `RsCell`/`ExtendedMole`
  are the blocker if they are not.

  Leave the refusals in place. A refusal that says what is missing is a correct
  outcome for this plan.
</task>

<task type="auto">
  <name>Task 4: make the refusal message accurate either way</name>
  `RS_BUILDER_GAP` (`rsdf_builder/mod.rs:95`) is the `what` string users see.
  Whatever branch ran, ensure it names the real current blocker rather than the
  historical cintx one.
</task>

</tasks>

<verification>
- The carryover's `status:` block names the current blocker with `file:line`, or records `rsjk` as shipped.
- Branch A only: `PYSCF_ORACLE_VENV=1 cargo test -p pyscf-pbc-scf --release --test rsjk -- --ignored` exits `0` below `1.353e-08`.
- Branch B only: `grep -n 'NotYetImplemented' crates/pyscf-pbc-scf/src/rsjk.rs` still shows `:149` and `:163`, and `RS_BUILDER_GAP` mentions no closed blocker.
- `grep -n 'phase: 19' crates/pyscf-pbc-scf/src/rsjk.rs` still shows `get_jk_mpi` refusing.
- `cargo test -p pyscf-pbc-scf --release --test kscf --test gate3_rsdf` exits `0`.
</verification>
