---
phase: 20-pbc-python-bindings
plan: 05
type: implement
wave: 2
depends_on: [01]
files_modified:
  - crates/pyscf-pbc-df/src/gdf/jk.rs
  - crates/pyscf-pbc-df/src/mdf/mdf_jk.rs
  - crates/pyscf-pbc-df/tests/gdf_omega.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "an omega that is silently ignored returns a plain-Coulomb answer for a range-separated request — the refusal must become a correct result or stay a refusal, never a quiet fallback"
    - "range_omega threading already landed (D-PBC-24 stage 1, commit 789530b); _RSGDFBuilder and _RSMDFBuilder both ship"
    - "upstream MDF swaps its builder to AFTDF for the RSH branch — mirror that, do not invent an MDF-native short-range path"
    - "the gate is the Phase-14 Gate-3 floor 1.353e-08 (GDF vs RSDF), not a tighter invented number"
  artifacts:
    - path: "crates/pyscf-pbc-df/tests/gdf_omega.rs"
      provides: "the RSH get_jk gate for GDF and MDF against upstream"
---

<objective>
**Delete two refusals that block range-separated hybrids.**

```rust
// crates/pyscf-pbc-df/src/gdf/jk.rs:673      NotYetImplemented { phase: 14 }
// crates/pyscf-pbc-df/src/mdf/mdf_jk.rs:132  NotYetImplemented { phase: 14 }
```

Both refuse `get_jk` when `JkOpts::omega` is set. They were written when
`range_omega` (libcint `env[8]`) was missing from cintx. **That blocker is
closed**: the D-PBC-24 carryover records cintx stages 0-2 done, omega threading
landed (commit `789530b`), and both `_RSGDFBuilder` and `_RSMDFBuilder` ship.
Phase 14 Gate 3 is met — RSDF `2.325e-10` / GDF `2.750e-10` on He-fcc 2×2×2.

These two refusals are what still keeps Phase 4's CAM-B3LYP/H2O numerical RSH
assertion CI-gated. They are the cheapest of the three §2b items.

`RangeSeparatedJkBuilder` is **not** in scope — that is 20-06, and its blocker
is different.
</objective>

<context>
@.planning/phases/20-pbc-python-bindings/20-CONTEXT.md            # §2b
@.planning/carryovers/D-PBC-24-cintx-range-omega-PLAN.md          # what landed, what did not
@crates/pyscf-pbc-df/src/gdf/jk.rs                                # :673 the refusal; :53 get_j_kpts, :135 get_k_kpts
@crates/pyscf-pbc-df/src/mdf/mdf_jk.rs                            # :132 the refusal; :39/:68 the J/K builds
@crates/pyscf-pbc-df/src/rsdf_builder/mod.rs                      # :110 RsGdfBuilder, :95 RS_BUILDER_GAP, :201 make_j3c
@crates/pyscf-pbc-df/src/traits.rs                                # :26 JkOpts.omega
@crates/pyscf-pbc-scf/tests/gate3_rsdf.rs                         # the Gate-3 harness to copy
@pyscf/pbc/df/df_jk.py                                            # upstream's RSH branch
@pyscf/pbc/df/mdf_jk.py                                           # upstream swapping to AFTDF for omega
</context>

<tasks>

<task type="implement">
  <name>Task 1: GDF get_jk(omega)</name>
  Route the short-range build through `RsGdfBuilder` with the requested
  `omega`, mirroring upstream's `df_jk` RSH branch. Delete the `:673` refusal
  only once the path returns real values — a stub returning plain Coulomb is
  worse than the refusal.
</task>

<task type="implement">
  <name>Task 2: MDF get_jk(omega)</name>
  Upstream swaps the builder to **AFTDF** for the RSH branch rather than
  building an MDF-native short-range path. Mirror that exactly. Delete `:132`.
</task>

<task type="test">
  <name>Task 3: the gate</name>
  New `tests/gdf_omega.rs`, modelled on `gate3_rsdf.rs` (same venv gate, same
  `run_python` oracle, `pyscf==2.12.1` asserted). Compare `vj`/`vk` at a
  non-zero `omega` on He-fcc 2×2×2 against upstream's RSH branch, at the
  **1.353e-08** Phase-14 Gate-3 floor.

  Tier the test `T2` per 20-02's convention and add it to 20-03's nightly list.
</task>

<task type="auto">
  <name>Task 4: report, do not absorb</name>
  If either route exceeds 1.353e-08, record the measured number and leave the
  refusal in place. **Stop and report** rather than loosening the floor.
</task>

</tasks>

<verification>
- `grep -n 'NotYetImplemented' crates/pyscf-pbc-df/src/gdf/jk.rs crates/pyscf-pbc-df/src/mdf/mdf_jk.rs` shows no `omega` refusal.
- `PYSCF_ORACLE_VENV=1 CARGO_TARGET_DIR=$HOME/.cargo-target-gate cargo test -p pyscf-pbc-df --release --test gdf_omega -- --ignored` exits `0`.
- The test prints `|dvj|` and `|dvk|` and both are below `1.353e-08`.
- `cargo test -p pyscf-pbc-df --release --test gdf_builder --test band_kpoints` still exits `0` — no pre-existing DF gate regressed.
</verification>
