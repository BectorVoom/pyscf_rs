---
phase: 20-pbc-python-bindings
plan: 12
type: implement
wave: 4
depends_on: [09, 10, 11]
files_modified:
  - crates/pyscf-py/src/pbc/scf.rs
  - crates/pyscf-pbc-scf/src/addons.rs
  - python/pyscf/pbc/scf/__init__.py
  - python/pyscf/tests/test_pbc_scf.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "drivers consume KPyOverrideBridge from 20-11; this plan does NOT reimplement hook dispatch"
    - "only the RHF ksymm driver exists (KsymAdaptedKrhf, khf_ksymm.rs:136) — KUHF/KGHF with a KPoints argument must RAISE, never fall through to upstream Python"
    - "project_mo_nr2nr (addons.rs:131) is a `phase: 20` refusal — THIS phase owns it; do not leave a phase-20 marker unaddressed in phase 20"
    - "upstream's isinstance(arg, KPoints) dispatch belongs in the PYTHON shim (pbc/scf/__init__.py:40-106), not in Rust MRO"
    - "get_bands returns complex per-k mo_coeff — route through 20-07, and reproduce krhf_bands_oracle.rs's 1.68e-11"
  artifacts:
    - path: "crates/pyscf-py/src/pbc/scf.rs"
      provides: "KRHF/KUHF/KROHF/KGHF + ksymm + smearing + chkfile + get_bands"
---

<objective>
**Bind the four periodic SCF drivers over 20-11's bridge.**

All four share one shape — `new(cell, kpts)`, `from_df`, `kernel`, `run`:

| upstream | Rust | file:line |
|---|---|---|
| `KRHF` | `Krhf` | `krhf.rs:27` (`new:45`, `from_df:52`, `kernel:80`, `run:89`, `get_bands:106`) |
| `KUHF` | `Kuhf` | `kuhf.rs:29` (`nelec:86`, `kernel:108`, `get_bands:138`) |
| `KROHF` | `Krohf` | `krohf.rs:29` (`roothaan_fock:116`) |
| `KGHF` | `Kghf` | `kghf.rs:31` |
| `KRHF` + `KPoints` | `KsymAdaptedKrhf` | `khf_ksymm.rs:136` (`from_df:165`, `kernel:195`) |

**One asymmetry must be surfaced, not hidden.** Upstream ships
`kuhf_ksymm.py` and `kghf_ksymm.py`; this port has neither. Under the
`pkgutil` passthrough those names resolve silently to upstream Python — the
exact failure 20-17 exists to prevent. Raise here.

**And one refusal this phase owns:** `addons::project_mo_nr2nr`
(`addons.rs:131`) carries `phase: 20`. Phase 20 is now. Either implement it or
restate the refusal accurately; do not ship a `phase: 20` marker out of phase 20.
</objective>

<context>
@crates/pyscf-pbc-scf/src/krhf.rs        # :27,:45,:52,:80,:89,:106
@crates/pyscf-pbc-scf/src/kuhf.rs        # :29,:53,:86,:108,:138
@crates/pyscf-pbc-scf/src/krohf.rs       # :29,:43,:72,:116
@crates/pyscf-pbc-scf/src/kghf.rs        # :31,:43,:66
@crates/pyscf-pbc-scf/src/khf_ksymm.rs   # :136,:165,:182,:187,:195
@crates/pyscf-pbc-scf/src/types.rs       # :26 KInitGuess, :45 KScfConfig, :93 for_cell, :108 KScfResult
@crates/pyscf-pbc-scf/src/smearing.rs    # :24 SmearingMethod, :34 Smearing
@crates/pyscf-pbc-scf/src/addons.rs      # :26,:35 smearing_*, :131 the phase-20 refusal
@crates/pyscf-pbc-scf/src/chkfile.rs     # :37 dump, :103 KScfCheckpoint, :122 load
@crates/pyscf-py/src/pbc/kbridge.rs      # 20-11's bridge — consume it
@crates/pyscf-pbc-scf/tests/krhf_bands_oracle.rs   # the 1.68e-11 band gate to reproduce
@pyscf/pbc/scf/__init__.py               # :40-106 the Python-level dispatch to port into the shim
</context>

<tasks>

<task type="test">
  <name>Task 1: failing tests first</name>
  `python/pyscf/tests/test_pbc_scf.py`: `KRHF` on He-fcc 2×2×2 equals the Rust
  `kernel()` energy **bitwise**; `mf.conv_tol` round-trips; `get_bands`
  reproduces `krhf_bands_oracle.rs`; `KUHF(cell, kpts=KPoints(...))` **raises**;
  a chkfile dump/load round-trips.
</task>

<task type="implement">
  <name>Task 2: the four drivers</name>
  `#[pyclass(subclass)]` each, accepting a `PyPeriodicDf` (20-10) via `from_df`
  and supporting post-construction `with_df` assignment. Drive
  `kernel<H: KOverrideHooks>` (`kscf.rs:110`) with 20-11's bridge.
</task>

<task type="implement">
  <name>Task 3: config and result surface</name>
  `KScfConfig` (`types.rs:45`) as settable attributes — `conv_tol`,
  `conv_tol_grad`, `max_cycle` — matching upstream's `mf.conv_tol` idiom, with
  `for_cell:93` as the default. Expose `KScfResult` (`:108`): `e_tot`,
  `e_elec`, `e_nuc`, `mo_energy`, `mo_coeff`, `mo_occ`, `converged`, `cycles`,
  `fermi`. `mo_coeff` is complex per-k — route through 20-07.
</task>

<task type="implement">
  <name>Task 4: get_bands, smearing, chkfile, gamma shims</name>
  `get_bands` on `Krhf:106` / `Kuhf:138`. `Smearing` (`smearing.rs:34`) with
  `smearing_krhf`/`smearing_kuhf` (`addons.rs:26,35`). `dump_kscf_to_file:37` /
  `load_kscf_from_file:122`. The gamma shims `gamma.rs:27,35,43,51,59`.
</task>

<task type="implement">
  <name>Task 5: resolve project_mo_nr2nr</name>
  `addons.rs:131`. Implement it, **or** keep it raising with a message naming
  exactly what is missing. Remove the `phase: 20` marker either way.
</task>

<task type="implement">
  <name>Task 6: raise for the missing ksymm variants, and port the dispatch</name>
  `KUHF`/`KGHF` given a `KPoints` must raise a clear
  "not implemented in pyscf-rs; upstream has pbc/scf/kuhf_ksymm.py" error.
  Put upstream's `isinstance(arg, KPoints)` branching
  (`pbc/scf/__init__.py:40-106`) in `python/pyscf/pbc/scf/__init__.py`, not in
  Rust.
</task>

</tasks>

<verification>
- `.venv/bin/pytest python/pyscf/tests/test_pbc_scf.py -q` exits `0`.
- `KRHF` He-fcc 2×2×2 through the binding equals the Rust `kernel()` energy **bitwise**.
- `get_bands` matches `krhf_bands_oracle.rs` (band `1.68e-11`, `mo_energy` `6.10e-11`).
- `KUHF(cell, kpts=KPoints(...))` raises; `pyscf.pbc.scf.kuhf_ksymm` is not silently served.
- `grep -n 'phase: 20' crates/pyscf-pbc-scf/src/addons.rs` returns nothing.
- `cargo run -p xtask --bin check-catch-unwind` exits `0`.
</verification>
