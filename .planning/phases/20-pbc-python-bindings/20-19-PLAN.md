---
phase: 20-pbc-python-bindings
plan: 19
type: gap-closure
wave: 7
depends_on: [17, 18-PRE]
autonomous: true
---

<objective>
**Close the measured, cheap gaps between the overlay and the upstream PBC suite
before the 20-18 rollup — then re-measure. Written 2026-09-14 by the
orchestrator from `measurements/upstream-pbc-suite.md` (1 / 815 under the
overlay vs 808 / 815 control) and `20-18-PRE-SUMMARY.md`.**

| id | gap (measured) | tests | owner file(s) |
|---|---|---:|---|
| A | molecular overlay packages hide upstream names PBC modules import: `gto.basis` 204, `dft.radi` 86, `gto.moleintor` 81, `ATOM_OF` 51, … | 591 (class 4a) | `python/pyscf/{gto,dft,scf,…}/__init__.py` |
| C | `pbc/dft/gen_grid.py` shim lacks `gen_uniform_grids` | 44 (subset of A) | `python/pyscf/pbc/dft/gen_grid.py` |
| B | native `Cell` rejects upstream inputs: `cell.output=` 31, list-form atom entries 30, per-element pseudo dict 4, explicit basis shell lists 3 | 68 (class 5) | `crates/pyscf-py/src/pbc/gto.rs` |
| D | SCF `get_ovlp` integrates at `cell.precision`, upstream at `precision*1e-5` (`pbc/scf/hf.py:47-55`); 5.0e-3 Ha free-energy error on `23-smearing.py` σ=0.1 | examples + all SCF bits | `pyscf-pbc-scf` / `pyscf-pbc-gto` |

Rules: no gate loosened; identity gate stays 18/18; molecular suite may not
gain failures beyond `measurements/python-suite-triage.md`; any bit change from
D re-runs the affected T1 oracle gates from `measurements/pbc-oracle-tiers.md`.
</objective>

<verification>
- Upstream PBC suite re-run with the 20-18 harness (`target/p20-18-suite/`) — report passed/collected before vs after, same denominator.
- `examples/pbc/23-smearing.py` σ=0.1 free energy vs upstream after D.
- T1 gates of `pyscf-pbc-scf`, `pyscf-pbc-dft`, `pyscf-pbc-df`, `pyscf-pbc-mp`, `pyscf-pbc-cc` re-run after D, exit 0.
</verification>
