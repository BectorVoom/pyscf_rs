---
phase: 20-pbc-python-bindings
plan: 01
type: measure
wave: 0
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/pbc/PBC-MASTER-PLAN.md
  - .planning/pbc/PBC-DRIVER-INVENTORY.md
  - .planning/phases/20-pbc-python-bindings/measurements/README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "NO Rust source file is created or edited by this plan"
    - "the Phase-20 gate is restated so a no-op CANNOT pass it; the current wording is satisfied today by the pkgutil passthrough"
    - "every number written into ROADMAP.md is COPIED from a phase's measurements/ or *-VERIFICATION.md, never invented"
    - "no gate is tighter than one f64 ulp of the quantity it bounds"
    - "bitwise equality with upstream is recorded as unattainable so no later plan re-proposes it"
  artifacts:
    - path: ".planning/phases/20-pbc-python-bindings/measurements/README.md"
      provides: "the authoritative per-method floor table for the v2.0 milestone"
      contains: "1.432e-06"
---

<objective>
**Reconcile the gates before any binding is written.**

Four documents disagree with each other and with what the phases measured.

`ROADMAP.md:459-463` shows phases 13-17 as `[ ]` unchecked, though all five have
a `*-VERIFICATION.md`. The same rows carry superseded gates: Phase 15 says
`1e-14` where `15-VERIFICATION` records `2e-6` MET; Phase 16 says `1e-14` where
its measurements record **1e-7 per DF route**; Phase 17 says `1e-14` where
`17-VERIFICATION` records five gates. Phases 18-19 say `1e-15`, which
`12-VERIFICATION` proved is ~1.1 ulp — unreachable when the best result anywhere
in this port is **221 ulp**.

`STATE.md`'s front-matter claims `5/5` phases and `48/48` plans at `100%` while
its body says Phase 16 is in progress and the tree holds nine PBC phases.

`PBC-DRIVER-INVENTORY.md` marks **167 rows `[ ]`, zero `[x]`**, after nine
phases shipped.

Worst: `ROADMAP.md:466`'s Phase-20 gate — *"an unmodified upstream `pyscf.pbc`
script runs on pyscf-rs"* — **is already true with zero work done**, because
`python/pyscf/pbc/__init__.py` is a `pkgutil.extend_path` passthrough and
`import pyscf.pbc.scf` resolves to the vendored upstream Python tree.

This is the seventh gate-before-measurement defect this project has found.
**This plan writes no Rust.**
</objective>

<context>
@.planning/ROADMAP.md                                       # lines 455-466 — the rows to rewrite
@.planning/STATE.md                                         # front-matter counters vs body
@.planning/phases/20-pbc-python-bindings/20-CONTEXT.md      # §1.1, §1.2, §2 — the findings
@.planning/phases/13-ft-ao-aftdf/measurements/README.md
@.planning/phases/14-gdf-mdf-rsdf-rsjk/measurements/README.md
@.planning/phases/15-periodic-ao2mo-kmp2/15-VERIFICATION.md
@.planning/phases/16-periodic-cc-ci/measurements/README.md
@.planning/phases/17-ksymm-multigrid/17-VERIFICATION.md     # :126 — the NOT MET row
@.planning/phases/17-ksymm-multigrid/measurements/README.md # the format to copy
</context>

<tasks>

<task type="auto">
  <name>Task 1: build the floor table</name>
  Create `measurements/README.md` in the format of
  `17-ksymm-multigrid/measurements/README.md`. Copy each number from its source
  — **do not re-run anything, do not invent**:

  | quantity | floor | source |
  |---|---|---|
  | `KRHF` vs upstream, all-electron | 2.2e-13 | `11-VERIFICATION` |
  | `KRHF` vs upstream, `gth-pade` | 4.0e-12 | `11-VERIFICATION` |
  | `KRKS` vs upstream (Si, PBE) | 6.45e-12 | `12-VERIFICATION` |
  | FFTDF − AFTDF | 2.607e-11 | `13 measurements:53` |
  | GDF − RSDF | 1.353e-08 | `14 measurements:119` |
  | FFTDF − MDF | 1.124e-06 | `14 measurements:120` |
  | `KMP2 e_corr` | 2e-6 MET | `15-VERIFICATION §1a` |
  | `KRCCSD e_corr`, per DF route | 1e-7 | `16 measurements §1` |
  | ksymm Gate B | 1e-9 / 1e-13 | `17-VERIFICATION` |
  | ksymm Gate C, FFTDF | 1.703e-11 | `17-VERIFICATION` |
  | ksymm Gate C, **GDF** | **1.432e-06 NOT MET** | `17-VERIFICATION:126` |
  | `get_bands` vs upstream | 1.68e-11 | `krhf_bands_oracle.rs` |
  | band J/K (`kpts_band`) | 1.394e-09 | `band_kpoints.rs` |
</task>

<task type="auto">
  <name>Task 2: record the irreducible gaps in a SEPARATE table</name>
  So no later plan mistakes them for defects. FFTDF−GDF `1.222e-03` (the DF
  fitting error); upstream's own two GDF builders `4.502e-06`; the CC route
  split `9.22e-04 Ha`; `get_pp` route disagreement `1.79e-09`; band agreement
  `~1.7e-11`. Each row states **why no implementation reaches it**.
</task>

<task type="auto">
  <name>Task 3: record that bitwise equality is unattainable</name>
  He-fcc `sto-3g`, 1 AO, 2×2×2: `0/8` `mo_energy` and `0/2` `get_bands` values
  bitwise identical, at 6.10e-11 / 1.68e-11 — and the port is not bitwise
  reproducible against itself run-to-run (`6.102252e-11` vs `6.102218e-11`).
  State the rule: `to_bits()` assertions are for **same-implementation A/B
  only**.
</task>

<task type="auto">
  <name>Task 4: rewrite ROADMAP.md:455-466</name>
  Check the five shipped boxes (13, 14, 15, 16, 17). Replace each superseded
  gate with its measured floor plus provenance. Mark Phase 18 planned-only (15
  PLAN files, zero SUMMARYs) and Phase 19 nonexistent.
</task>

<task type="auto">
  <name>Task 5: restate the Phase-20 gate</name>
  In `ROADMAP.md:466` **and** `PBC-MASTER-PLAN §7` row 20, replace the current
  wording with the identity contract:

  ```python
  assert pyscf.pbc.scf.KRHF is pyscf._native.pbc.scf.KRHF
  ```

  plus a named upstream script running with that identity holding.
  Cross-reference 20-18.
</task>

<task type="auto">
  <name>Task 6: fix STATE.md counters and the inventory header</name>
  Correct `total_phases` / `completed_phases` / `total_plans` /
  `completed_plans` to match the tree. In `PBC-DRIVER-INVENTORY.md`, either
  refresh the 167 checkboxes or add a header stating the column is
  **unmaintained** and the document is a scope map only. Do not leave it
  silently wrong.
</task>

</tasks>

<verification>
- `grep -c '^- \[ \] \*\*Phase 1[3-7]' .planning/ROADMAP.md` returns `0`.
- `grep -n '1e-14\|1e-15' .planning/ROADMAP.md` returns no line inside the Phase 9-20 block.
- `measurements/README.md` contains `1.432e-06` and the string `NOT MET`.
- `grep -n 'unmodified upstream' .planning/ROADMAP.md` shows the restated gate naming `_native.pbc`.
- `git diff --stat -- crates/` is **empty** — this plan writes no Rust.
</verification>
