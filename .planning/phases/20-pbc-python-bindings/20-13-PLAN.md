---
phase: 20-pbc-python-bindings
plan: 13
type: implement
wave: 5
depends_on: [09, 10, 11, 12]
files_modified:
  - crates/pyscf-py/src/pbc/dft.rs
  - crates/pyscf-py/Cargo.toml
  - python/pyscf/pbc/dft/__init__.py
  - python/pyscf/tests/test_pbc_dft.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "DEFAULT: add pyscf-pbc-dft behind a `libxc` cargo feature on pyscf-py, default OFF. Escalate ONLY if the xcfun fallback changes a gate number — then stop and report"
    - "pyscf-pbc-dft has NO root re-exports (lib.rs:8-21); name pyscf_pbc_dft::krks::Krks explicitly"
    - "KGKS with a hybrid functional refuses (kgks.rs:124) — raise, never silently drop exact exchange"
    - "multigrid v2 carries a mesh-independent 2e-8...2e-7 floor (17-01 Gate E) and is NOT held to v1's 1e-12"
    - "the GDF ksymm arm is 1.432e-06 NOT MET (20-04 owns it); this plan must not paper over it"
  artifacts:
    - path: "crates/pyscf-py/src/pbc/dft.rs"
      provides: "KRKS/KUKS/KROKS/KGKS + 4 ksymm + DFT+U + numint backends"
---

<objective>
**Bind periodic KS-DFT, with the libxc posture already decided.**

`pyscf-pbc-dft` is 11,987 lines / 286 public items: `Krks` (`src/krks.rs:58`),
`Kuks` (`src/kuks.rs:38`), `Kroks` (`src/kroks.rs:34`), `Kgks`
(`src/kgks.rs:34`), four ksymm adapters (`krks_ksymm.rs:70,539,427,916`) and
DFT+U (`kspu.rs:653,705`).

**The libxc decision is made here, not left open.**
`crates/pyscf-pbc-dft/Cargo.toml:14` takes `pyscf-dft` with default features on,
and `pyscf-dft`'s default is `["libxc"]` (`crates/pyscf-dft/Cargo.toml:36-37`) —
266 kernels, ~6h, the reason `ci.yml:418` is `if: false`.

> **DEFAULT ACTION:** add `pyscf-pbc-dft` to `pyscf-py` behind a new `libxc`
> cargo feature, **default OFF**, forwarding to `pyscf-pbc-dft/libxc`. Developer
> builds stay fast; the release wheel is built with the feature ON.
>
> **ESCALATE (stop and report) only if:** with the feature OFF, the xcfun
> fallback changes any gate number in this plan's verification. The XC default
> is libxc and must stay libxc for numbers — a silent xcfun default cost a wrong
> floor attribution once already.

Do not deliberate further; run the default, escalate only on that trigger.
</objective>

<context>
@crates/pyscf-pbc-dft/src/lib.rs          # :8-21 modules only, NO root re-exports
@crates/pyscf-pbc-dft/src/krks.rs         # :58,:82,:92,:131,:141,:149,:158,:225 get_bands,:255 hooks
@crates/pyscf-pbc-dft/src/kuks.rs         # :38,:69,:110,:136,:186 get_bands,:220,:318 hooks
@crates/pyscf-pbc-dft/src/kroks.rs        # :34,:52,:93,:130 hooks
@crates/pyscf-pbc-dft/src/kgks.rs         # :34,:85,:118,:124 the hybrid refusal,:210 hooks
@crates/pyscf-pbc-dft/src/krks_ksymm.rs   # :70,:427,:539,:916 the four adapters
@crates/pyscf-pbc-dft/src/kspu.rs         # :91 HubbardU,:162 set_u,:653 Krkspu,:705 Kukspu
@crates/pyscf-pbc-dft/src/numint.rs       # :128 KsNumInt,:155 grid,:159 multigrid,:163 multigrid2
@crates/pyscf-pbc-dft/src/gen_grid.rs     # :214 PeriodicGrids,:268 uniform,:276 becke
@crates/pyscf-pbc-dft/src/numint2c.rs     # :104,:152,:256 the three refusals
@crates/pyscf-pbc-dft/Cargo.toml          # :14 the libxc inheritance
@pyscf/pbc/dft/__init__.py                # :37-119 the Python-level dispatch for the shim
</context>

<tasks>

<task type="auto">
  <name>Task 1: execute the libxc default</name>
  Add `libxc = ["pyscf-pbc-dft/libxc"]` to `crates/pyscf-py/Cargo.toml`,
  default OFF, with `pyscf-pbc-dft` taken `default-features = false`. Record the
  warm build time with the feature off; a jump means libxc leaked in.
</task>

<task type="test">
  <name>Task 2: failing tests first</name>
  `python/pyscf/tests/test_pbc_dft.py`: `KRKS(Si, 2×2×2, PBE)` equals the Rust
  `kernel()` **bitwise**; DFT+U `E_U` matches `6.939e-18`; each refusal raises;
  the three numint backends are selectable and `multigrid2` is asserted only at
  its own `2e-8…2e-7` floor.
</task>

<task type="implement">
  <name>Task 3: the four KS drivers</name>
  Naming modules explicitly (`pyscf_pbc_dft::krks::Krks`), each taking
  `(cell, kpts, xc)` plus a `PyPeriodicDf`, exposing `kernel`, `run`,
  `get_rho`, `get_bands` (`krks.rs:225`, `kuks.rs:186`), over 20-11's bridge.
</task>

<task type="implement">
  <name>Task 4: the four ksymm adapters and DFT+U</name>
  `KsymAdaptedKrks:70`, `KsymAdaptedKuks:539`, `KsymAdaptedKrkspu:427`,
  `KsymAdaptedKukspu:916`; `Krkspu:653` / `Kukspu:705` with `HubbardU:91`,
  `set_u:162`, `e_u`.

  **Do not paper over the GDF ksymm arm**: it is `1.432e-06` NOT MET and owned
  by 20-04. Any binding test on that route cites 20-04, not a loosened bound.
</task>

<task type="implement">
  <name>Task 5: numint backends and grids</name>
  `KsNumInt::grid:155` / `multigrid:159` / `multigrid2:163`, mirroring upstream's
  `mf.multigrid_numint(mesh)` (`pbc/dft/rks.py:428`). Document v2's
  mesh-independent floor. Bind `PeriodicGrids::uniform:268` / `becke:276`.
</task>

<task type="implement">
  <name>Task 6: refusals and the Python dispatch</name>
  Raise for KGKS + hybrid (`kgks.rs:124`), `collinear='mcol'`
  (`numint2c.rs:104`), `'ncol'` beyond LDA (`:152`), non-collinear `nr_fxc`
  (`:256`). Port `pbc/dft/__init__.py:37-119`'s `isinstance(KPoints)` dispatch
  into the Python shim.
</task>

</tasks>

<verification>
- `.venv/bin/pytest python/pyscf/tests/test_pbc_dft.py -q` exits `0`.
- `KRKS(Si, 2×2×2, PBE)` equals the Rust `kernel()` **bitwise** and sits on `12-VERIFICATION`'s `6.45e-12` upstream floor.
- `cargo tree -p pyscf-py | grep -c libxc` returns `0` with the feature off, and non-zero with `--features libxc`.
- `CARGO_TARGET_DIR=$HOME/.cargo-target-gate cargo build -p pyscf-py` stays under 2 minutes warm.
- Every listed refusal raises.
- If the xcfun fallback moved any number, the plan STOPPED and reported instead of adjusting the gate.
</verification>
