---
phase: 01-foundation
plan: 06
type: execute
wave: 4
depends_on: ["01-04", "01-05"]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/nightly-cross-crate.yml
autonomous: true
requirements:
  - FOUND-05
  - FOUND-08
  - FOUND-10
  - ALG-06
  - ORACLE-05
  - ORACLE-09
must_haves:
  truths:
    - "ci.yml runs on every push and PR: fmt, clippy -D warnings, build (default + --features gpu), test, and the 5 xtask gates"
    - "ci.yml has an oracle-determinism job pinning RAYON_NUM_THREADS=1 (ORACLE-09) running release-oracle profile suite"
    - "ci.yml includes a 2nd oracle-determinism job with RAYON_NUM_THREADS=8 to verify Roadmap criterion 3 across thread counts"
    - "ci.yml includes a `cargo deny check` job (FOUND-10)"
    - "ci.yml RUSTFLAGS env var is explicitly empty string (FOUND-05 + Pitfall 1: no inherited fast-math)"
    - "nightly-cross-crate.yml runs on cron schedule + workflow_dispatch (D-14)"
    - "nightly-cross-crate.yml runs `cargo update -p cintx -p libxc_rs -p xcfun_rs` then rebuilds + tests + check-cubecl-pin (D-14 lockstep enforcement)"
    - "Both workflows use Swatinem/rust-cache@v2 for dep-build cache (xcfun precedent)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Pre-merge CI: fmt, clippy, build matrix, test, 5 xtask gates, oracle-determinism, cargo-deny"
      contains: "check-no-fma"
      min_lines: 100
    - path: ".github/workflows/nightly-cross-crate.yml"
      provides: "Nightly cross-crate matrix per ORACLE-05 + D-14"
      contains: "cargo update -p cintx -p libxc_rs -p xcfun_rs"
      min_lines: 30
  key_links:
    - from: ".github/workflows/ci.yml jobs"
      to: "xtask binaries from Plan 05"
      via: "cargo run -p xtask --bin check-NAME"
      pattern: "check-no-fma|check-forbidden-paths"
    - from: ".github/workflows/ci.yml oracle-determinism job"
      to: "RAYON_NUM_THREADS=1"
      via: "env: { RAYON_NUM_THREADS: \"1\" }"
      pattern: "RAYON_NUM_THREADS"
    - from: ".github/workflows/nightly-cross-crate.yml"
      to: "cargo update + rebuild lockstep"
      via: "cron schedule"
      pattern: "cargo update -p cintx"
---

<objective>
Wire the five xtask gates from Plan 05 + the oracle-determinism guarantee from Plan 04 into GitHub Actions CI. Two workflows:

1. **`.github/workflows/ci.yml`** — runs on every push to master + every PR. Includes:
   - fmt check
   - clippy `-D warnings`
   - build matrix: default features AND `--features gpu`
   - test (workspace, locked)
   - 5 xtask gates (check-no-fma, check-forbidden-paths, check-catch-unwind, check-dependency-wall, check-cubecl-pin)
   - oracle-determinism: 2 jobs, one with RAYON_NUM_THREADS=1 and one with =8, both running tests under `--profile release-oracle`
   - cargo-deny check
2. **`.github/workflows/nightly-cross-crate.yml`** — runs nightly on cron + manual dispatch. Bumps the sibling-crate revs (`cargo update -p cintx -p libxc_rs -p xcfun_rs`), rebuilds the workspace with `--features gpu`, runs the tests, and runs check-cubecl-pin to confirm the cubecl 0.10.0 lockstep across the four-crate family (D-14 + ORACLE-05).

Purpose: ROADMAP success criterion 5 explicitly requires four lints to be CI-enforced; criterion 3 requires the rayon-1 vs rayon-8 bit-identical proof in CI; criterion 4 requires the nightly cross-crate matrix. This plan provides those CI gates.

Output: 2 workflow files. After Plan 06 merges, every PR runs through ~12 jobs (10 in ci.yml + 1 nightly + 1 manual), and any regression on FMA-free build, scope creep, dep-wall, cubecl pin, oracle determinism, or cross-crate ABI drift fails the merge.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/Documents/workspace/pyscf_rs/.planning/REQUIREMENTS.md
@/home/user/Documents/workspace/pyscf_rs/.planning/phases/01-foundation/01-CONTEXT.md
@/home/user/Documents/workspace/pyscf_rs/.planning/phases/01-foundation/01-RESEARCH.md
@/home/user/Documents/workspace/pyscf_rs/.planning/phases/01-foundation/01-PATTERNS.md
@/home/user/Documents/workspace/pyscf_rs/.planning/phases/01-foundation/01-VALIDATION.md

<interfaces>
<!-- xtask binaries from Plan 05 invoked from CI: -->
<!--   cargo run -p xtask --bin check-no-fma -->
<!--   cargo run -p xtask --bin check-forbidden-paths -->
<!--   cargo run -p xtask --bin check-catch-unwind -->
<!--   cargo run -p xtask --bin check-dependency-wall -->
<!--   cargo run -p xtask --bin check-cubecl-pin -->

<!-- Test invocations: -->
<!--   cargo test --workspace --locked --no-fail-fast -->
<!--   cargo test --profile release-oracle --workspace --locked --no-fail-fast (oracle-determinism jobs) -->
<!--   cargo build --workspace --locked (default features = cpu) -->
<!--   cargo build --workspace --locked --features gpu (cuda + wgpu probe arms) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/workflows/ci.yml — pre-merge CI matrix</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - /home/user/Documents/workspace/pyscf_rs/.github/workflows/ (existing GitHub workflows; verify no `ci.yml` already there or note collision)
    - ~/Documents/workspace/xcfun_rs/.github/workflows/ci.yml (PATTERNS row ".github/workflows/ci.yml" lines 789-860 — verbatim adoption + 5 xtask jobs added)
    - /home/user/Documents/workspace/pyscf_rs/.planning/phases/01-foundation/01-RESEARCH.md (lines 1466-1473 §13 CI matrix design)
    - /home/user/Documents/workspace/pyscf_rs/.planning/REQUIREMENTS.md (FOUND-05, FOUND-08, FOUND-10, ALG-06, ORACLE-09)
    - /home/user/Documents/workspace/pyscf_rs/.planning/ROADMAP.md (success criteria 1, 2, 3, 4, 5, 6 — what CI must prove)
  </read_first>
  <action>
    First check for existing CI workflow collision:
    ```
    ls /home/user/Documents/workspace/pyscf_rs/.github/workflows/ 2>&1 | head
    ```
    If `ci.yml` already exists (e.g. inherited from upstream PySCF), the new file goes at `.github/workflows/ci.yml` overwriting only if it's not the upstream Python CI (record collision in SUMMARY). The upstream PySCF python CI is fine to leave alongside; if it is named `ci.yml` we use a different name like `rust-ci.yml`. Most likely the existing dir holds python-only workflows with different names.

    Create `.github/workflows/ci.yml` with this content:

    ```yaml
    # Pre-merge CI for the pyscf-rs Rust workspace. Runs alongside any
    # upstream-PySCF Python workflows in .github/workflows/.
    #
    # Coverage: ROADMAP success criteria 1, 2, 3, 5, 6:
    #   1. cargo build --workspace --locked (default + --features gpu)
    #   2. xtask check-no-fma (release-oracle FMA grep)
    #   3. oracle-determinism × 2 (RAYON_NUM_THREADS=1 and =8)
    #   5. four lints: clippy unwrap deny + check-forbidden-paths +
    #      check-catch-unwind + check-dependency-wall (+ check-cubecl-pin)
    #   6. select_backend env-var truth table (covered by cargo test)
    name: CI

    on:
      push:
        branches: [master, main]
      pull_request:
      workflow_dispatch:

    env:
      # FOUND-05 + Pitfall 1: explicitly empty so no GitHub-Actions-default
      # fast-math sneaks in. The .cargo/config.toml [build] rustflags
      # supply -fp-contract=off + -target-feature=-fma,-fma4 (Plan 01).
      RUSTFLAGS: ""
      CARGO_TERM_COLOR: always
      RUST_BACKTRACE: 1

    jobs:
      fmt:
        name: cargo fmt --check
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: dtolnay/rust-toolchain@stable
            with: { toolchain: stable, components: rustfmt }
          - run: cargo fmt --all -- --check

      clippy:
        name: cargo clippy -D warnings (FOUND-07 unwrap deny)
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: dtolnay/rust-toolchain@stable
            with: { toolchain: stable, components: clippy }
          - uses: Swatinem/rust-cache@v2
          - run: cargo clippy --workspace --all-targets --locked -- -D warnings

      build-default:
        name: cargo build --workspace (default features = cpu)
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: dtolnay/rust-toolchain@stable
            with: { toolchain: stable }
          - uses: Swatinem/rust-cache@v2
          - run: cargo build --workspace --locked

      build-gpu:
        name: cargo build --workspace --features gpu (Pitfall 7 cuda+wgpu compile-only)
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: dtolnay/rust-toolchain@stable
            with: { toolchain: stable }
          - uses: Swatinem/rust-cache@v2
          # cuda+wgpu probe arms compile even without GPU hardware (RESEARCH
          # Pitfall 7 — type-check the gpu-gated arms early so a Phase 4
          # mistake doesn't go unnoticed for weeks).
          - run: cargo build --workspace --locked --features gpu

      test:
        name: cargo test --workspace
        runs-on: ubuntu-latest
        needs: [build-default]
        steps:
          - uses: actions/checkout@v4
          - uses: dtolnay/rust-toolchain@stable
          - uses: Swatinem/rust-cache@v2
          - run: cargo test --workspace --locked --no-fail-fast -- --test-threads=1

      # ROADMAP success criterion 3 — Pitfall 2 SHOWSTOPPER mitigation.
      # Two jobs in matrix: rayon-1 vs rayon-8. Both run release-oracle
      # profile (FOUND-05 FMA-free) and the oracle_determinism integration
      # test (Plan 04). The bit-identical contract is asserted in-process
      # by the test — these CI jobs prove it survives across thread-count
      # configurations.
      oracle-determinism:
        name: oracle-determinism (RAYON_NUM_THREADS=${{ matrix.rayon }})
        runs-on: ubuntu-latest
        needs: [build-default]
        strategy:
          matrix:
            rayon: ["1", "8"]
        env:
          RAYON_NUM_THREADS: ${{ matrix.rayon }}
        steps:
          - uses: actions/checkout@v4
          - uses: dtolnay/rust-toolchain@stable
          - uses: Swatinem/rust-cache@v2
          - run: cargo test --profile release-oracle -p pyscf-algebra --test oracle_determinism --locked -- --test-threads=1

      # FOUND-05: the FMA grep gate. Runs xtask check-no-fma which builds
      # pyscf-algebra + pyscf-core under release-oracle, scans the asm,
      # and fails on any FMA mnemonic.
      xtask-no-fma:
        name: xtask check-no-fma (FOUND-05 / Pitfall 1)
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: dtolnay/rust-toolchain@stable
          - uses: Swatinem/rust-cache@v2
          - run: cargo run --quiet -p xtask --bin check-no-fma

      # FOUND-08 / Pitfall 21: scope-creep gate.
      xtask-forbidden-paths:
        name: xtask check-forbidden-paths (FOUND-08 / Pitfall 21)
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: dtolnay/rust-toolchain@stable
          - uses: Swatinem/rust-cache@v2
          - run: cargo run --quiet -p xtask --bin check-forbidden-paths

      # FOUND-07 / Pitfall 14: catch_unwind on every extern "C" gate.
      xtask-catch-unwind:
        name: xtask check-catch-unwind (FOUND-07 / Pitfall 14)
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: dtolnay/rust-toolchain@stable
          - uses: Swatinem/rust-cache@v2
          - run: cargo run --quiet -p xtask --bin check-catch-unwind

      # ALG-06: cubecl-* containment gate.
      xtask-dependency-wall:
        name: xtask check-dependency-wall (ALG-06)
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: dtolnay/rust-toolchain@stable
          - uses: Swatinem/rust-cache@v2
          - run: cargo run --quiet -p xtask --bin check-dependency-wall

      # FOUND-04: cubecl 0.10.0 lockstep gate.
      xtask-cubecl-pin:
        name: xtask check-cubecl-pin (FOUND-04 / Pitfall 1)
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: dtolnay/rust-toolchain@stable
          - uses: Swatinem/rust-cache@v2
          - run: cargo run --quiet -p xtask --bin check-cubecl-pin

      # FOUND-10: license + advisory + bans gating.
      cargo-deny:
        name: cargo deny check (FOUND-10)
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: dtolnay/rust-toolchain@stable
          - uses: Swatinem/rust-cache@v2
          - run: cargo install --locked cargo-deny@0.19.5
          - run: cargo deny check
    ```

    Verify the YAML parses:
    ```
    python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" 2>&1
    ```
    If yaml.safe_load completes without error, syntax is valid. (Python is in the upstream-PySCF dev env; available even without ruby.)

    Push verification: this workflow doesn't run locally (it's GitHub Actions only). Local sanity-check by counting jobs:
    ```
    grep -c '^\s*\w[a-z-]*:\s*$' .github/workflows/ci.yml | head
    ```
  </action>
  <verify>
    <automated>test -f /home/user/Documents/workspace/pyscf_rs/.github/workflows/ci.yml && python3 -c "import yaml; yaml.safe_load(open('/home/user/Documents/workspace/pyscf_rs/.github/workflows/ci.yml'))" 2>&1 | (! grep -q -i 'error\|exception') && grep -q 'check-no-fma' /home/user/Documents/workspace/pyscf_rs/.github/workflows/ci.yml && grep -q 'check-forbidden-paths' /home/user/Documents/workspace/pyscf_rs/.github/workflows/ci.yml && grep -q 'check-catch-unwind' /home/user/Documents/workspace/pyscf_rs/.github/workflows/ci.yml && grep -q 'check-dependency-wall' /home/user/Documents/workspace/pyscf_rs/.github/workflows/ci.yml && grep -q 'check-cubecl-pin' /home/user/Documents/workspace/pyscf_rs/.github/workflows/ci.yml && grep -q 'RAYON_NUM_THREADS' /home/user/Documents/workspace/pyscf_rs/.github/workflows/ci.yml && grep -q 'release-oracle' /home/user/Documents/workspace/pyscf_rs/.github/workflows/ci.yml && grep -q 'cargo deny check' /home/user/Documents/workspace/pyscf_rs/.github/workflows/ci.yml && grep -q '--features gpu' /home/user/Documents/workspace/pyscf_rs/.github/workflows/ci.yml && grep -q 'RUSTFLAGS:' /home/user/Documents/workspace/pyscf_rs/.github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/ci.yml` exits 0
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (valid YAML)
    - `grep -q 'name: CI' .github/workflows/ci.yml`
    - `grep -q 'check-no-fma' .github/workflows/ci.yml` (FOUND-05 gate wired)
    - `grep -q 'check-forbidden-paths' .github/workflows/ci.yml` (FOUND-08 gate)
    - `grep -q 'check-catch-unwind' .github/workflows/ci.yml` (FOUND-07 gate)
    - `grep -q 'check-dependency-wall' .github/workflows/ci.yml` (ALG-06 gate)
    - `grep -q 'check-cubecl-pin' .github/workflows/ci.yml` (FOUND-04 gate)
    - `grep -q 'cargo deny check' .github/workflows/ci.yml` (FOUND-10 gate)
    - `grep -q 'release-oracle' .github/workflows/ci.yml` (oracle profile job)
    - `grep -q 'RAYON_NUM_THREADS' .github/workflows/ci.yml` (Roadmap criterion 3)
    - `grep -q '"1"' .github/workflows/ci.yml && grep -q '"8"' .github/workflows/ci.yml` (matrix rayon-1 vs rayon-8 — Roadmap criterion 3 documented contract)
    - `grep -q 'cargo build --workspace --locked --features gpu' .github/workflows/ci.yml` (Pitfall 7 + Roadmap criterion 1 GPU clause)
    - `grep -E 'RUSTFLAGS:\s*""' .github/workflows/ci.yml` (Pitfall 1 explicit empty)
    - `grep -q 'Swatinem/rust-cache' .github/workflows/ci.yml` (xcfun precedent for cache)
    - Job count: at least 10 named jobs (fmt, clippy, build-default, build-gpu, test, oracle-determinism, xtask-no-fma, xtask-forbidden-paths, xtask-catch-unwind, xtask-dependency-wall, xtask-cubecl-pin, cargo-deny)
  </acceptance_criteria>
  <done>.github/workflows/ci.yml exists, parses as valid YAML, contains all 5 xtask gate jobs, oracle-determinism matrix (rayon=1 + rayon=8), build-default + build-gpu, cargo-deny, fmt + clippy. RUSTFLAGS explicitly empty. Each gate maps to a Roadmap success criterion.</done>
</task>

<task type="auto">
  <name>Task 2: Create .github/workflows/nightly-cross-crate.yml — sibling-crate lockstep</name>
  <files>.github/workflows/nightly-cross-crate.yml</files>
  <read_first>
    - /home/user/Documents/workspace/pyscf_rs/.github/workflows/ci.yml (just created — confirm action versions reused)
    - /home/user/Documents/workspace/pyscf_rs/Cargo.toml (workspace root — confirm [patch.crates-io] sibling-crate refs match D-13)
    - ~/Documents/workspace/xcfun_rs/.github/workflows/ci.yml (PATTERNS row ".github/workflows/nightly-cross-crate.yml" — workflow shape only)
    - /home/user/Documents/workspace/pyscf_rs/.planning/phases/01-foundation/01-PATTERNS.md (lines 866-891 — cron + cross-crate matrix)
    - /home/user/Documents/workspace/pyscf_rs/.planning/REQUIREMENTS.md (ORACLE-05 nightly cross-crate; D-14 cargo update mechanism)
  </read_first>
  <action>
    Create `.github/workflows/nightly-cross-crate.yml` with this content:

    ```yaml
    # Nightly cross-crate matrix per ORACLE-05 + CONTEXT D-14.
    #
    # Mechanism: bump the pinned [patch.crates-io] sibling-crate revs to
    # current default-branch HEAD via `cargo update -p cintx -p libxc_rs
    # -p xcfun_rs`, rebuild + test the workspace under --features gpu,
    # and run check-cubecl-pin to confirm the cubecl 0.10.0 lockstep
    # survived. Failure blocks merge of any sibling-crate cubecl bump
    # until pyscf-rs is fixed in lockstep.
    #
    # Reference: ROADMAP success criterion 4 (Pitfall 3 + 15 mitigation).
    name: nightly-cross-crate

    on:
      schedule:
        # 06:00 UTC daily — after US/EU evening pushes settle.
        - cron: "0 6 * * *"
      workflow_dispatch:

    env:
      RUSTFLAGS: ""
      CARGO_TERM_COLOR: always
      RUST_BACKTRACE: 1

    jobs:
      cross-crate-matrix:
        name: cross-crate matrix (cubecl 0.10.0 lockstep)
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: dtolnay/rust-toolchain@stable
            with: { toolchain: stable }
          - uses: Swatinem/rust-cache@v2

          # D-14: bump the sibling-crate revs to current HEAD on each
          # default branch. cintx + libxc_rs default branch is "main";
          # xcfun_rs default branch is "master" (verified via
          # `gh api repos/BectorVoom/{name}` 2026-05-10).
          - name: bump sibling-crate revs to HEAD
            run: cargo update -p cintx -p libxc_rs -p xcfun_rs

          # Verify the cubecl pin still holds after the bump. If a sibling
          # bumped cubecl, this fails immediately with a clear diagnostic.
          - name: check-cubecl-pin
            run: cargo run --quiet -p xtask --bin check-cubecl-pin

          # Compile the workspace including gpu-feature arms. cubecl-cuda
          # + cubecl-wgpu have build-side dependencies (CUDA toolkit
          # detection, wgpu native libs) but the gpu-feature compile-only
          # path passes on stock ubuntu-latest.
          - name: cargo build --workspace --features gpu --locked
            run: cargo build --workspace --features gpu --locked

          # Run the test suite (default features — GPU runtime exercise
          # is Phase 8). The point is to catch sibling-crate API drift,
          # not GPU regression.
          - name: cargo test --workspace --locked
            run: cargo test --workspace --locked --no-fail-fast -- --test-threads=1

          # On failure, leave the updated Cargo.lock as a CI artifact so
          # a developer can `git checkout` the offending revs locally for
          # bisection.
          - name: upload Cargo.lock on failure
            if: failure()
            uses: actions/upload-artifact@v4
            with:
              name: cross-crate-cargo-lock
              path: Cargo.lock
    ```

    Verify YAML parses:
    ```
    python3 -c "import yaml; yaml.safe_load(open('.github/workflows/nightly-cross-crate.yml'))"
    ```
  </action>
  <verify>
    <automated>test -f /home/user/Documents/workspace/pyscf_rs/.github/workflows/nightly-cross-crate.yml && python3 -c "import yaml; yaml.safe_load(open('/home/user/Documents/workspace/pyscf_rs/.github/workflows/nightly-cross-crate.yml'))" 2>&1 | (! grep -q -i 'error\|exception') && grep -q 'cron:' /home/user/Documents/workspace/pyscf_rs/.github/workflows/nightly-cross-crate.yml && grep -q 'cargo update -p cintx' /home/user/Documents/workspace/pyscf_rs/.github/workflows/nightly-cross-crate.yml && grep -q 'cargo update -p cintx -p libxc_rs -p xcfun_rs' /home/user/Documents/workspace/pyscf_rs/.github/workflows/nightly-cross-crate.yml && grep -q 'check-cubecl-pin' /home/user/Documents/workspace/pyscf_rs/.github/workflows/nightly-cross-crate.yml && grep -q '--features gpu' /home/user/Documents/workspace/pyscf_rs/.github/workflows/nightly-cross-crate.yml && grep -q 'workflow_dispatch' /home/user/Documents/workspace/pyscf_rs/.github/workflows/nightly-cross-crate.yml</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/nightly-cross-crate.yml` exits 0
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/nightly-cross-crate.yml'))"` exits 0
    - `grep -q 'name: nightly-cross-crate' .github/workflows/nightly-cross-crate.yml`
    - `grep -q 'cron:' .github/workflows/nightly-cross-crate.yml` (scheduled)
    - `grep -q 'workflow_dispatch' .github/workflows/nightly-cross-crate.yml` (manual trigger)
    - `grep -q 'cargo update -p cintx -p libxc_rs -p xcfun_rs' .github/workflows/nightly-cross-crate.yml` (D-14 mechanism)
    - `grep -q 'check-cubecl-pin' .github/workflows/nightly-cross-crate.yml` (FOUND-04 lockstep verifier)
    - `grep -q '--features gpu' .github/workflows/nightly-cross-crate.yml` (compile-only GPU exercise)
    - `grep -q 'upload-artifact' .github/workflows/nightly-cross-crate.yml` (Cargo.lock on failure for bisection)
  </acceptance_criteria>
  <done>.github/workflows/nightly-cross-crate.yml exists, parses as valid YAML, has cron schedule + workflow_dispatch, runs cargo update -p cintx -p libxc_rs -p xcfun_rs followed by check-cubecl-pin and cargo build/test --features gpu. Cargo.lock uploaded as artifact on failure for bisection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → workspace build | Runner trusts checkout actions; xtask binaries run in CI environment with same trust as `cargo build`. |
| Sibling-crate git remotes (BectorVoom/cintx etc.) → nightly cross-crate matrix | `cargo update -p cintx` pulls latest commit from default branch; if remote is compromised, this CI run will exercise the malicious code (acceptable: D-13 documents BectorVoom-org sibling repos as the trust source). |
| `RUSTFLAGS` env var inheritance | Default empty per FOUND-05; `.cargo/config.toml` `[build] rustflags` provides FMA-off + the `[target.'cfg(all())']` duplication wins against any user-level override (Plan 01 Pitfall 2 mitigation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | T (Tampering / oracle drift) | RAYON_NUM_THREADS=1 vs =8 | mitigate | ci.yml has 2-job matrix; both run release-oracle profile + the oracle_determinism integration test (Plan 04). Bit-identity asserted in-process. |
| T-1-01 | T (FMA leak via inherited fast-math) | RUSTFLAGS env | mitigate | ci.yml sets `RUSTFLAGS: ""` explicitly per Pitfall 1; `.cargo/config.toml` (Plan 01) provides the FMA-off flags. |
| T-1-01 | S (Spoofing / sibling-crate ABI drift) | nightly cross-crate matrix | mitigate | nightly-cross-crate.yml runs `cargo update` then check-cubecl-pin; auto-issue creation could be added but Phase 1 ships the matrix only (RESEARCH §13 — auto-issue is Phase 8 nice-to-have). |
| T-1-02 | I (Information disclosure / scope creep) | every PR | mitigate | ci.yml runs check-forbidden-paths on every PR; merge blocked on out-of-scope import. |
| T-1-03 | E (Privilege escalation via unguarded FFI) | extern "C" blocks | mitigate | ci.yml runs check-catch-unwind; merge blocked on unguarded FFI. |
</threat_model>

<verification>
After Plan 06:

1. Both workflow files exist in `.github/workflows/`
2. Both YAML files parse via `python3 -c "import yaml; yaml.safe_load(...)"`
3. `ci.yml` has ≥10 jobs covering: fmt, clippy, build-default, build-gpu, test, oracle-determinism (matrix), all 5 xtask gates, cargo-deny
4. `ci.yml` env block has `RUSTFLAGS: ""` (Pitfall 1)
5. `ci.yml` oracle-determinism job has `matrix: rayon: ["1", "8"]` and `RAYON_NUM_THREADS` env (Roadmap criterion 3)
6. `nightly-cross-crate.yml` has cron schedule + `cargo update -p cintx -p libxc_rs -p xcfun_rs` + check-cubecl-pin (D-14)
7. **GitHub validation deferred until first push** — `act` (the local GitHub Actions runner) is not assumed available; the YAML correctness is verified locally via Python's yaml.safe_load
</verification>

<success_criteria>
- `.github/workflows/ci.yml` and `.github/workflows/nightly-cross-crate.yml` exist
- Both parse as valid YAML
- ci.yml covers Roadmap criteria 1, 2, 3, 5, 6 via separate jobs
- nightly-cross-crate.yml covers Roadmap criterion 4 (cubecl lockstep + sibling-crate ABI matrix)
- All 5 xtask gates from Plan 05 are wired in ci.yml
- All 6 REQ-IDs (FOUND-05, FOUND-08, FOUND-10, ALG-06, ORACLE-05, ORACLE-09) have observable evidence in CI
- D-14 (sibling-crate cargo update lockstep) implemented in nightly workflow
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-06-SUMMARY.md` with:
- File list (2 workflow files)
- Job count in ci.yml (per `grep -c '^\s\s\w*:\s*$'`)
- Note that initial workflow execution happens on first push to GitHub; the local validation via yaml.safe_load is the syntactic guarantee
- Reference to manual verification path: after Phase 1 merges, watch the first PR's CI run to confirm all 10+ jobs pass; record outcomes in next phase's RETROSPECTIVE
</output>
