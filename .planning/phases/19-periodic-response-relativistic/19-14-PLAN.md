---
phase: 19-periodic-response-relativistic
plan: 14
type: implementation
wave: 1
depends_on: ["19-02"]
files_modified:
  - crates/pyscf-pbc-adc/src/kadc_ao2mo.rs
  - crates/pyscf-pbc-adc/src/kadc_rhf.rs
  - crates/pyscf-pbc-adc/src/amplitudes.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "this is the ADC BASE: 966 lines that 19-15 and 19-16 both build on - it ships before either"
    - "kadc_ao2mo is an integral transform, not an ADC method; it reuses the shipped pbc-ao2mo machinery"
    - "free k-axis gathers are SHAPE-SILENT: oovv[:,km,ke] and oovv[km,:,ke] have the same shape, so index tests need an element-wise oracle"
    - "the complex arena recycles dirty, over-sized buffers - anything accumulating into a 'zeroed' KTensor must not trust it"
  artifacts:
    - path: "crates/pyscf-pbc-adc/src/kadc_rhf.rs"
      provides: "the ADC integral transform, driver base and amplitudes"
---

<objective>
**The ADC base - 966 lines that both the IP and EA plans build on.**

`kadc_ao2mo.py` (294), `kadc_rhf.py` (326), `kadc_rhf_amplitudes.py` (346).
Split out of `§8.11`'s single 3,437-line ADC row so the shared half is written
and gated once, before 19-15 and 19-16 consume it.

Two traps this repo has already paid for, both in the `must_haves`:
**shape-silent k-axis gathers** (`oovv[:,km,ke]` and `oovv[km,:,ke]` have
identical shapes, so only an element-wise oracle on synthetic amplitudes
catches a swap) and the **complex arena's dirty recycled buffers** (equal
`nocc`/`nvir` hid that for a whole phase).
</objective>

<context>
@.planning/phases/19-periodic-response-relativistic/19-CONTEXT.md   # 1.4
@pyscf/pbc/adc/kadc_ao2mo.py
@pyscf/pbc/adc/kadc_rhf.py
@pyscf/pbc/adc/kadc_rhf_amplitudes.py
@crates/pyscf-pbc-ao2mo/src/eris.rs   # the shipped transform to reuse
@crates/pyscf-pbc-cc/src/ktensor.rs   # KTensor and the arena
@.planning/phases/19-periodic-response-relativistic/measurements/README.md   # 19-01's floors
</context>

<tasks>

<task type="tdd">
  <name>Task 1: kadc_ao2mo over the shipped transform</name>
  Port the ADC integral transform by reusing `pyscf-pbc-ao2mo`, not by writing a new one.
</task>

<task type="tdd">
  <name>Task 2: the driver base and amplitudes</name>
  Port `kadc_rhf.py` and the amplitude builders.
</task>

<task type="test">
  <name>Task 3: element-wise index oracle</name>
  Build synthetic amplitudes with distinguishable values per k-index and assert every gather element-wise. A shape assertion catches nothing here.
</task>

<task type="test">
  <name>Task 4: the arena is not trusted to be zero</name>
  Assert any accumulation target is explicitly zeroed, on a fixture where `nocc != nvir` so an equal-dimension coincidence cannot hide a bug.
</task>

</tasks>

<verification>
- `cargo test -p pyscf-pbc-adc --test kadc_base` exits `0`.
- The element-wise index oracle fails when a free k-axis gather is transposed (demonstrated once, then reverted).
- The `nocc != nvir` fixture passes, proving no equal-dimension coincidence is masking an index error.
- Determinism at `RAYON_NUM_THREADS=1` and `8` under `release-oracle`.
</verification>
