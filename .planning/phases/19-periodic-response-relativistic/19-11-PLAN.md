---
phase: 19-periodic-response-relativistic
plan: 11
type: implementation
wave: 5
depends_on: ["19-10"]
files_modified:
  - crates/pyscf-pbc-gw/src/krgw_cd.rs
  - crates/pyscf-pbc-gw/tests/krgw_cd.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "CD is a different approximation from AC - it gets its OWN gate number (19-01 Task 3)"
    - "the contour parameters are part of the method and are pinned on both sides"
    - "CD has no Pade fit, so it is the better route to trust when the two disagree - record which is which"
  artifacts:
    - path: "crates/pyscf-pbc-gw/src/krgw_cd.rs"
      provides: "the contour-deformation G0W0 driver"
---

<objective>
**G0W0 by contour deformation - the route without a fit.**

`pbc/gw/krgw_cd.py` (704 l). Where AC (19-10) continues from the imaginary axis
by a Pade fit, CD deforms the integration contour and evaluates on the real
axis directly. **It has no fit**, which is why the AC-CD split is a meaningful
diagnostic rather than noise.

Reuse 19-10's self-energy machinery wherever the two share it; the contour is
the difference, not the whole method.
</objective>

<context>
@.planning/phases/19-periodic-response-relativistic/19-CONTEXT.md
@pyscf/pbc/gw/krgw_cd.py
@crates/pyscf-pbc-gw/src/krgw_ac.rs   # the shared self-energy machinery
@.planning/phases/19-periodic-response-relativistic/measurements/README.md   # 19-01's floors
</context>

<tasks>

<task type="tdd">
  <name>Task 1: the contour deformation</name>
  Port the contour build and the real-axis evaluation, sharing 19-10's self-energy code where it is genuinely shared.
</task>

<task type="test">
  <name>Task 2: Gate C (CD), and the AC-CD split recorded</name>
  Compare against upstream at its own decimals with the contour pinned. Then record `|E_AC - E_CD|` per quasiparticle against 19-01 Task 3's measured split - a disagreement larger than measured is a finding.
</task>

</tasks>

<verification>
- `cargo test -p pyscf-pbc-gw --test krgw_cd` exits `0`.
- **Gate C (CD)** passes at upstream's own decimals with the contour pinned.
- The AC-CD split is recorded and consistent with 19-01 Task 3's measurement.
- Determinism at `RAYON_NUM_THREADS=1` and `8` under `release-oracle`.
</verification>
