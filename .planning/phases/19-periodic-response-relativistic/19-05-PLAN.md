---
phase: 19-periodic-response-relativistic
plan: 05
type: implementation
wave: 2
depends_on: ["19-03"]
files_modified:
  - crates/pyscf-pbc-scf/src/stability.rs
  - crates/pyscf-pbc-scf/tests/stability.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "a stability analysis reports a SIGN (is the solution a minimum) - the gate is the sign and the instability direction, not a float to 8 decimals"
    - "internal and external stability are different questions; port both and keep their results distinct"
    - "it consumes 19-03's gen_response and must not rebuild it"
  artifacts:
    - path: "crates/pyscf-pbc-scf/src/stability.rs"
      provides: "the periodic SCF stability analysis"
---

<objective>
**Is the converged SCF solution actually a minimum?**

`pbc/scf/stability.py` (329 l), over 19-03's response seam.

The gate here is unlike every other in the phase: the primary output is a
**verdict** (stable / unstable) plus, when unstable, a direction. Gating a
verdict to 8 decimals is meaningless. Assert the verdict against upstream's on
the same cell, and gate the lowest eigenvalue of the stability matrix at
19-01's floor only as a secondary check.
</objective>

<context>
@.planning/phases/19-periodic-response-relativistic/19-CONTEXT.md
@pyscf/pbc/scf/stability.py
@crates/pyscf-pbc-scf/src/response.rs   # 19-03's seam
@.planning/phases/19-periodic-response-relativistic/measurements/README.md   # 19-01's floors
</context>

<tasks>

<task type="tdd">
  <name>Task 1: internal stability</name>
  Port the internal-stability path over `gen_response`.
</task>

<task type="tdd">
  <name>Task 2: external stability</name>
  Port the external path; keep its result a distinct value from the internal one.
</task>

<task type="test">
  <name>Task 3: verdict first, eigenvalue second</name>
  Assert the stable/unstable verdict matches upstream on a cell known to be each way. Only then compare the lowest stability eigenvalue at 19-01's floor.
</task>

</tasks>

<verification>
- `cargo test -p pyscf-pbc-scf --test stability` exits `0`.
- The verdict matches upstream on both a stable and an unstable fixture.
- Internal and external results are distinct values, not one flag.
- `cargo run -p xtask --bin check-single-cphf` exits `0` - no second CPHF solver was added.
</verification>
