---
phase: 19-periodic-response-relativistic
plan: 09
type: implementation
wave: 5
depends_on: ["19-08"]
files_modified:
  - crates/pyscf-pbc-tdscf/src/krks.rs
  - crates/pyscf-pbc-tdscf/src/kuks.rs
  - crates/pyscf-pbc-tdscf/src/rks.rs
  - crates/pyscf-pbc-tdscf/src/uks.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "these four total 205 lines because they are THIN subclasses - the work is in 19-06/07/08 and in the XC kernel, not here"
    - "the XC contribution enters through gen_response, which is NotImplemented on the PBC KohnShamDFT base (pbc/dft/rks.py:268) and rebound only on the concrete class (:411)"
    - "hybrid functionals change the response kernel; a hybrid must not silently take the pure-DFT path"
  artifacts:
    - path: "crates/pyscf-pbc-tdscf/src/krks.rs"
      provides: "the four Kohn-Sham TDA/TDHF subclasses"
---

<objective>
**Four thin subclasses - 205 lines total - over the drivers already built.**

`krks.py` (63), `kuks.py` (41), `rks.py` (52), `uks.py` (49). They are thin
because the work is in 19-06 through 19-08 and in the XC response kernel.

The one thing to get right is §1.6's seam: the XC contribution enters through
`gen_response`, which upstream leaves `NotImplemented` on the PBC
`KohnShamDFT` base and rebinds only on the concrete class. And a **hybrid**
functional changes the response kernel - assert that a hybrid does not take the
pure-DFT path.
</objective>

<context>
@.planning/phases/19-periodic-response-relativistic/19-CONTEXT.md   # 1.6
@pyscf/pbc/tdscf/krks.py
@pyscf/pbc/tdscf/kuks.py
@pyscf/pbc/tdscf/rks.py
@pyscf/pbc/tdscf/uks.py
@crates/pyscf-pbc-scf/src/response.rs   # 19-03's seam
@.planning/phases/19-periodic-response-relativistic/measurements/README.md   # 19-01's floors
</context>

<tasks>

<task type="tdd">
  <name>Task 1: the four subclasses</name>
  Port all four over the 19-06/07/08 drivers, taking the XC response through `gen_response`.
</task>

<task type="test">
  <name>Task 2: Gate A for KS, and the hybrid path</name>
  Compare against upstream at each test's own decimals, for a pure functional and a hybrid. Assert the hybrid takes the hybrid kernel - a test that passes with the pure path substituted is vacuous.
</task>

</tasks>

<verification>
- `cargo test -p pyscf-pbc-tdscf --test krks` exits `0`.
- **Gate A** passes for all four subclasses at their own upstream decimals.
- A hybrid functional demonstrably takes the hybrid response kernel (the test fails when the pure kernel is substituted).
- Determinism at `RAYON_NUM_THREADS=1` and `8` under `release-oracle`.
</verification>
