---
phase: 19-periodic-response-relativistic
plan: 13
type: implementation
wave: 5
depends_on: ["19-10"]
files_modified:
  - crates/pyscf-pbc-gw/src/kgw_slow.rs
  - crates/pyscf-pbc-gw/src/kgw_slow_supercell.rs
  - crates/pyscf-pbc-gw/src/gw_slow.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "these three are REFERENCE implementations - slow, explicit, and valuable precisely because they are easy to verify"
    - "kgw_slow_supercell and the k-point route describe the same crystal: their agreement is a supercell-equivalence identity needing no upstream"
    - "do not optimise them; their job is to be obviously correct"
  artifacts:
    - path: "crates/pyscf-pbc-gw/src/kgw_slow.rs"
      provides: "the explicit reference GW routes"
---

<objective>
**The slow, explicit reference routes - 328 lines that make the fast ones checkable.**

`kgw_slow.py` (169), `kgw_slow_supercell.py` (135), `gw_slow.py` (24).

These exist to be obviously correct rather than fast, and that is their value:
`kgw_slow_supercell` and the k-point route describe **the same crystal**, so
their agreement is a supercell-equivalence identity that needs no upstream at
all - the same oracle-free structure Phase 11 used to catch a live `super_cell`
defect and Phase 16 used at `2.97e-8`.

**Do not optimise them.**
</objective>

<context>
@.planning/phases/19-periodic-response-relativistic/19-CONTEXT.md
@pyscf/pbc/gw/kgw_slow.py
@pyscf/pbc/gw/kgw_slow_supercell.py
@pyscf/pbc/gw/gw_slow.py
@crates/pyscf-pbc-gto/src/supercell.rs   # :53 super_cell
@.planning/phases/19-periodic-response-relativistic/measurements/README.md   # 19-01's floors
</context>

<tasks>

<task type="tdd">
  <name>Task 1: the three reference routes</name>
  Port them plainly. Clarity over speed; no fusion, no blocking.
</task>

<task type="test">
  <name>Task 2: supercell equivalence, oracle-free</name>
  A k-mesh GW on the primitive cell and a gamma GW on the corresponding supercell describe the same crystal; assert they agree per quasiparticle at 19-01's floor.
</task>

<task type="test">
  <name>Task 3: the slow route checks the fast one</name>
  Assert `kgw_slow` agrees with 19-10's `krgw_ac` on a small cell.
</task>

</tasks>

<verification>
- `cargo test -p pyscf-pbc-gw --test kgw_slow` exits `0`.
- The supercell-equivalence identity holds at 19-01's floor, with no upstream involved.
- `kgw_slow` and `krgw_ac` agree on a small cell.
- Determinism at `RAYON_NUM_THREADS=1` and `8` under `release-oracle`.
</verification>
