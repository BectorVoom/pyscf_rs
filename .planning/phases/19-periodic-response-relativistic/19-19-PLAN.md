---
phase: 19-periodic-response-relativistic
plan: 19
type: verification
wave: 6
depends_on: ["19-01", "19-02", "19-03", "19-04", "19-05", "19-06", "19-07", "19-08", "19-09", "19-10", "19-11", "19-12", "19-13", "19-14", "19-15", "19-16", "19-17", "19-18"]
files_modified:
  - .planning/phases/19-periodic-response-relativistic/19-VERIFICATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - FEATURES
autonomous: true
requirements: []
must_haves:
  truths:
    - "every gate NOT met is recorded as not met WITH ITS MEASURED NUMBER, in the style of 17-VERIFICATION:126, and moved to the carryover ledger"
    - "no tolerance introduced anywhere in this phase is tighter than one ulp of the quantity it bounds"
    - "the ROADMAP Phase-19 row is rewritten with the measured gates, and the checkbox is only ticked if they are met"
    - "a gate that was never run is reported as NOT RUN, never folded into a pass"
  artifacts:
    - path: "19-VERIFICATION.md"
      provides: "the phase rollup with every gate's measured number"
---

<objective>
**Roll up the five gates, honestly.**

Phases 15, 16 and 17 each closed with at least one gate recorded as **NOT MET**
rather than absorbed - `17-VERIFICATION:126`'s `1.432e-06` is the model. This
plan follows it.

The specific risk here is Gate A: `ROADMAP:465` demanded `1e-15 eV`, 19-01
replaced it with a measured number, and the temptation at rollup is to quote the
old number as met. Quote the measured one, with its provenance.
</objective>

<context>
@.planning/phases/19-periodic-response-relativistic/19-CONTEXT.md
@.planning/phases/19-periodic-response-relativistic/measurements/README.md   # 19-01's floors
@.planning/phases/17-ksymm-multigrid/17-VERIFICATION.md   # :126 - how to record a gate NOT met
@.planning/ROADMAP.md   # :465 - the Phase-19 row
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: the gate matrix</name>
  One row per gate A-E per method, with: the measured value, the tolerance from 19-01, met/not-met, and the file:line of the test that produced it.
</task>

<task type="auto">
  <name>Task 2: the carryover ledger</name>
  Every not-met gate gets a row in `.planning/carryovers/` naming what is missing. Nothing is absorbed.
</task>

<task type="auto">
  <name>Task 3: the rollup edits</name>
  `19-VERIFICATION.md`; `STATE.md`'s position and counters; `ROADMAP.md`'s Phase-19 row (measured gates, checkbox only if met); `FEATURES`.
</task>

</tasks>

<verification>
- `19-VERIFICATION.md` has one row per gate per method, each with a measured number and a met/not-met verdict.
- `grep -n '1e-15' .planning/ROADMAP.md` returns nothing on the Phase-19 line.
- Every not-met gate has a carryover row; none is absorbed into a pass.
- Any gate not run is labelled **NOT RUN**, not folded into a pass.
- `cargo test -p pyscf-pbc-tdscf -p pyscf-pbc-gw -p pyscf-pbc-adc -p pyscf-pbc-x2c -p pyscf-pbc-eph` exits `0`.
</verification>
