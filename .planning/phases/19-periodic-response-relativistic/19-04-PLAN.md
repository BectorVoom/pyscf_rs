---
phase: 19-periodic-response-relativistic
plan: 04
type: implementation
wave: 2
depends_on: ["19-03"]
files_modified:
  - crates/pyscf-pbc-scf/src/newton_ah.rs
  - crates/pyscf-pbc-scf/tests/newton_ah.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "newton_ah is SECOND-ORDER SCF: it consumes 19-03's gen_response seam and must not rebuild it"
    - "it has no upstream counterpart anywhere else in the Rust tree - `newton_ah` was listed in the Phase-20 audit as having NO match"
    - "convergence-path differences are expected: second-order SCF reaches the same minimum by a different route, so gate the ENERGY, never the iteration count"
  artifacts:
    - path: "crates/pyscf-pbc-scf/src/newton_ah.rs"
      provides: "the periodic second-order SCF driver"
---

<objective>
**Second-order SCF over the response seam 19-03 built.**

`pbc/scf/newton_ah.py` (303 l). It is the largest consumer of `gen_response`
and the reason that seam exists. The Phase-20 audit recorded `newton_ah` as
having **no match anywhere in the Rust tree** - this plan is its first.

The trap is the gate. Second-order SCF converges to the same minimum along a
different path, so iteration counts and intermediate densities legitimately
differ from first-order SCF and from upstream. **Gate the converged energy
against the first-order driver's, not the trajectory.**
</objective>

<context>
@.planning/phases/19-periodic-response-relativistic/19-CONTEXT.md   # 1.6 the gen_response seam
@pyscf/pbc/scf/newton_ah.py
@crates/pyscf-pbc-scf/src/response.rs   # 19-03's seam
@crates/pyscf-pbc-scf/src/kscf.rs      # :110 the first-order driver to agree with
@.planning/phases/19-periodic-response-relativistic/measurements/README.md   # 19-01's floors
</context>

<tasks>

<task type="tdd">
  <name>Task 1: the augmented-Hessian driver</name>
  Port `newton_ah.py` over 19-03's `gen_response`. Route every inner product through `oracle_dot`/`oracle_sum`.
</task>

<task type="test">
  <name>Task 2: gate the energy, not the path</name>
  Converge the same cell with the first-order driver and with `newton_ah`; assert the **energies** agree at 19-01's Gate-A floor. Assert nothing about cycle counts.
</task>

<task type="test">
  <name>Task 3: upstream comparison</name>
  Against upstream `newton_ah` at the decimal count upstream's own test asserts.
</task>

</tasks>

<verification>
- `cargo test -p pyscf-pbc-scf --test newton_ah` exits `0`.
- The second-order and first-order converged energies agree at 19-01's measured floor; no assertion references an iteration count.
- `cargo run -p xtask --bin check-single-cphf` exits `0` - no second CPHF solver was added.
- Determinism at `RAYON_NUM_THREADS=1` and `8` under `release-oracle`.
</verification>
