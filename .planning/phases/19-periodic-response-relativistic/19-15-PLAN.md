---
phase: 19-periodic-response-relativistic
plan: 15
type: implementation
wave: 2
depends_on: ["19-14"]
files_modified:
  - crates/pyscf-pbc-adc/src/ip.rs
  - crates/pyscf-pbc-adc/tests/ip.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "1061 lines - the single largest file in the phase; it consumes 19-14's base and adds nothing to it"
    - "upstream's adc suite asserts at 4 DECIMALS in 58 of its 80 assertions - that is Gate D, not 1e-8"
    - "roots are Davidson roots: sort and count them, never index positionally"
  artifacts:
    - path: "crates/pyscf-pbc-adc/src/ip.rs"
      provides: "the ADC ionisation-potential solver"
---

<objective>
**ADC(n) for ionisation potentials - the largest single file in the phase.**

`pbc/adc/kadc_rhf_ip.py` (1,061 l) over 19-14's base.

The gate is looser than instinct suggests and that is upstream's own doing:
`pbc/adc`'s suite asserts at **4 decimals in 58 of its 80 assertions**. Gating
at 1e-8 would be four orders tighter than the reference implementation's own
tests and would fail on upstream's own numbers.
</objective>

<context>
@.planning/phases/19-periodic-response-relativistic/19-CONTEXT.md   # 2 Gate D
@pyscf/pbc/adc/kadc_rhf_ip.py
@pyscf/pbc/adc/test/
@crates/pyscf-pbc-adc/src/kadc_rhf.rs   # 19-14's base
@.planning/phases/19-periodic-response-relativistic/measurements/README.md   # 19-01's floors
</context>

<tasks>

<task type="tdd">
  <name>Task 1: the IP matrix-vector product</name>
  Port the sigma build over 19-14's amplitudes, element-wise-oracle tested as in 19-14.
</task>

<task type="tdd">
  <name>Task 2: the Davidson solve and root extraction</name>
  Sorted, with an explicit root count.
</task>

<task type="test">
  <name>Task 3: Gate D for IP</name>
  Against upstream at its own 4 decimals, sorted and counted.
</task>

</tasks>

<verification>
- `cargo test -p pyscf-pbc-adc --test ip` exits `0`.
- **Gate D** passes at the 4 decimals upstream's own suite asserts.
- Roots are compared sorted with an explicit count; no positional indexing.
- Determinism at `RAYON_NUM_THREADS=1` and `8` under `release-oracle`.
</verification>
