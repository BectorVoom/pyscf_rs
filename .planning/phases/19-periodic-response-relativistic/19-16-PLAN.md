---
phase: 19-periodic-response-relativistic
plan: 16
type: implementation
wave: 2
depends_on: ["19-14"]
files_modified:
  - crates/pyscf-pbc-adc/src/ea.rs
  - crates/pyscf-pbc-adc/tests/ea.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "1324 lines - the largest file in the phase; structurally the mirror of 19-15"
    - "IP and EA share 19-14's base and differ in the excitation manifold; do not fork the base"
    - "Gate D is upstream's own 4 decimals"
  artifacts:
    - path: "crates/pyscf-pbc-adc/src/ea.rs"
      provides: "the ADC electron-affinity solver"
---

<objective>
**ADC(n) for electron affinities - the mirror of 19-15.**

`pbc/adc/kadc_rhf_ea.py` (1,324 l), the largest file in the phase. It is
structurally 19-15 with a different excitation manifold, over the same 19-14
base. **Do not fork the base**; if a change is needed there, make it in 19-14's
files so IP and EA stay on one implementation.
</objective>

<context>
@.planning/phases/19-periodic-response-relativistic/19-CONTEXT.md
@pyscf/pbc/adc/kadc_rhf_ea.py
@pyscf/pbc/adc/test/
@crates/pyscf-pbc-adc/src/ip.rs   # 19-15's mirror structure
@.planning/phases/19-periodic-response-relativistic/measurements/README.md   # 19-01's floors
</context>

<tasks>

<task type="tdd">
  <name>Task 1: the EA matrix-vector product</name>
  Over 19-14's base, element-wise-oracle tested.
</task>

<task type="tdd">
  <name>Task 2: the Davidson solve and root extraction</name>
  Sorted, counted.
</task>

<task type="test">
  <name>Task 3: Gate D for EA</name>
  Against upstream at its own 4 decimals.
</task>

</tasks>

<verification>
- `cargo test -p pyscf-pbc-adc --test ea` exits `0`.
- **Gate D** passes at upstream's own 4 decimals.
- `git diff crates/pyscf-pbc-adc/src/kadc_rhf.rs` shows no fork of the shared base, or the change is shared with 19-15.
- Determinism at `RAYON_NUM_THREADS=1` and `8` under `release-oracle`.
</verification>
