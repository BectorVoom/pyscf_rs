---
phase: 19-periodic-response-relativistic
plan: 12
type: implementation
wave: 5
depends_on: ["19-10"]
files_modified:
  - crates/pyscf-pbc-gw/src/kugw_ac.rs
  - crates/pyscf-pbc-gw/tests/kugw_ac.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "784 lines, the largest single GW file - it is the unrestricted generalisation of 19-10, not a new method"
    - "alpha and beta self-energies couple through the screened interaction W, which is spin-summed"
    - "a closed-shell system run unrestricted must reproduce 19-10's restricted answer - that is the strongest available oracle-free check"
  artifacts:
    - path: "crates/pyscf-pbc-gw/src/kugw_ac.rs"
      provides: "the unrestricted analytic-continuation G0W0 driver"
---

<objective>
**The unrestricted generalisation of 19-10.**

`pbc/gw/kugw_ac.py` (784 l), the largest single file in the GW family. The
structure is 19-10's; what changes is that W is **spin-summed** while the
self-energies are per spin, so the two channels couple through the screening
even though their Green's functions are separate.

The strongest check here needs no upstream at all: **a closed-shell system run
unrestricted must reproduce 19-10's restricted answer.** Phase 16 used exactly
this shape (`KUCCSD` vs `KRCCSD` closed-shell at 1e-8, against upstream's own
4.95e-9 gap).
</objective>

<context>
@.planning/phases/19-periodic-response-relativistic/19-CONTEXT.md
@pyscf/pbc/gw/kugw_ac.py
@crates/pyscf-pbc-gw/src/krgw_ac.rs   # the restricted driver to reproduce
@.planning/phases/19-periodic-response-relativistic/measurements/README.md   # 19-01's floors
</context>

<tasks>

<task type="tdd">
  <name>Task 1: the unrestricted AC driver</name>
  Port it over 19-10's machinery, with W spin-summed and the self-energies per spin.
</task>

<task type="test">
  <name>Task 2: the closed-shell identity</name>
  On a closed-shell cell, the unrestricted result must equal 19-10's restricted result at 19-01's floor. This is oracle-free and is the primary check.
</task>

<task type="test">
  <name>Task 3: Gate C (unrestricted AC)</name>
  Against upstream at its own decimals.
</task>

</tasks>

<verification>
- `cargo test -p pyscf-pbc-gw --test kugw_ac` exits `0`.
- The closed-shell unrestricted result reproduces the restricted one at 19-01's floor.
- **Gate C** passes at upstream's own decimals.
- Determinism at `RAYON_NUM_THREADS=1` and `8` under `release-oracle`.
</verification>
