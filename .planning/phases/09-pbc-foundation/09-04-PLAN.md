---
phase: 09-pbc-foundation
plan: 04
type: execute
wave: 2
depends_on: [09-01, 09-03]
files_modified:
  - crates/pyscf-pbc-gto/src/cutoff.rs
  - crates/pyscf-pbc-tools/src/mesh.rs
  - crates/pyscf-pbc-gto/tests/cutoff.rs
autonomous: true
requirements: [PBC-GTO-03]
must_haves:
  truths:
    - "every function is a line-by-line port of the upstream Python named in the PORT block"
    - "tests live in crates/pyscf-pbc-*/tests/ — never inside a source file (AGENTS.md §2)"
    - "any cubecl kernel is generic over F: Float and launched via dispatch_backend!"
  artifacts:
    - path: "crates/pyscf-pbc-gto/tests/cutoff.rs"
      provides: "numeric acceptance gate for this plan"
      contains: "assert"
---

<objective>
Cutoffs, rcut, mesh.

**The authoritative task body for this plan is `.planning/pbc/PBC-MASTER-PLAN.md`
§8.1, plan 09-04.** It names the exact upstream file and line ranges to port
(the PORT block), the ordered STEPS, the TEST assertions, and the DONE command.
Open it and follow it literally. Do not improvise an algorithm.
</objective>

<context>
@.planning/pbc/PBC-MASTER-PLAN.md   # §0 rules, §8.1 plan 09-04, §11 formula appendix
@.planning/phases/09-pbc-foundation/09-CONTEXT.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: read the upstream source</name>
  For every entry in the PORT block of PBC-MASTER-PLAN §8.1 plan 09-04, run
  `sed -n '&lt;start&gt;,&lt;end&gt;p' &lt;file&gt;` and read it before writing any Rust.
  Also run `codegraph_explore` on the Rust symbols you will call, so you edit with
  the blast radius in view (RULE 3).
</task>

<task type="auto">
  <name>Task 2: implement the STEPS</name>
  Execute the numbered STEPS from PBC-MASTER-PLAN §8.1 plan 09-04 in order.
  Keep upstream variable names in comments so a reviewer can diff against the Python.
  Any branch the plan marks deferred returns
  `PyscfRsError::NotYetImplemented { phase: &lt;N&gt;, what: "&lt;reason&gt;" }` — never a
  silently wrong answer (D-PBC-20).
</task>

<task type="test">
  <name>Task 3: tests/cutoff.rs</name>
  Write exactly the assertions listed in the plan's TEST block. Tier-1 invariant tests
  (no upstream needed) come first and must pass unconditionally; tier-2 hard-coded
  reference values are generated once from upstream and pasted as literals with a
  comment recording the generating snippet (D-PBC-19).
</task>

</tasks>

<verification>
```bash
cargo test -p pyscf-pbc-gto --test cutoff
cargo test -p pyscf-pbc-tools
cargo clippy -p pyscf-pbc-gto -- -D warnings
```
Then write `09-04-SUMMARY.md`: what shipped, the green test command, deviations, carry-overs.
</verification>
