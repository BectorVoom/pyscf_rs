---
phase: 09-pbc-foundation
plan: 09
type: execute
wave: 4
depends_on: [09-01, 09-02, 09-03, 09-04, 09-05, 09-06, 09-07, 09-08]
files_modified:
  - .planning/phases/09-pbc-foundation/09-VERIFICATION.md
  - python/pyscf/pbc/__init__.py
  - crates/pyscf-pbc-gto/tests/oracle_phase9.rs
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [PBC-ORACLE-01]
must_haves:
  truths:
    - "all seven Phase 9 success criteria in 09-CONTEXT.md are demonstrated green"
    - "the oracle test is venv-gated (#[ignore] + PYSCF_ORACLE_VENV), never a hard CI dep"
    - "every reference number used in a tier-2 test is recorded in 09-VERIFICATION.md with its generating snippet"
---

<objective>
Close Phase 9: prove the seven success criteria, record every reference number, and
land the venv-gated upstream oracle comparison.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: oracle harness</name>
  `crates/pyscf-pbc-gto/tests/oracle_phase9.rs`, `#[ignore]` unless `PYSCF_ORACLE_VENV`
  is set — mirror the pattern already used in `crates/pyscf-gto/tests/`.
  Compare against upstream for all five reference systems: `Cell` scalars (`vol`, `rcut`,
  `mesh`), `get_Gv` (element-wise, 1e-12), `get_SI` (1e-12), `get_lattice_Ls` (count +
  values), `make_kpts` (1e-12), `get_kconserv` (exact ints), `ewald()` (1e-9 Ha).
</task>

<task type="auto">
  <name>Task 2: 09-VERIFICATION.md</name>
  One row per success criterion: criterion, the command proving it, the observed value,
  PASS/FAIL. Plus a "reference values" table: every hard-coded literal used in a tier-2
  test, the system it belongs to, and the exact Python snippet that generated it.
</task>

<task type="auto">
  <name>Task 3: bookkeeping</name>
  - `python/pyscf/pbc/__init__.py` — empty shim so the import path exists (no bindings yet, D-PBC-14).
  - Tick the Phase 9 checkbox in `.planning/ROADMAP.md`.
  - Append a Phase 9 row to `.planning/STATE.md` recording: the D-PBC-04 decision from
    09-02, the R-02 probe result from 09-01, and any carry-overs.
</task>

</tasks>

<verification>
```bash
cargo test --workspace
PYSCF_ORACLE_VENV=1 cargo test -p pyscf-pbc-gto --test oracle_phase9 -- --ignored
cargo run -p xtask --bin check_dependency_wall
cargo run -p xtask --bin check_forbidden_paths
```
</verification>
