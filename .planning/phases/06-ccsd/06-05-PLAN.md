---
phase: 06-ccsd
plan: 05
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - crates/pyscf-ccsd/src/diis_amps.rs
  - crates/pyscf-ccsd/src/ccsd.rs
  - crates/pyscf-ccsd/tests/diis_amps.rs
autonomous: true
requirements: [CCSD-04]

must_haves:
  truths:
    - "t1 + lower-triangular t2 pack into one flat vector byte-matching the upstream amplitudes_to_vector layout, and unpack round-trips"
    - "AmplitudeSubspace::dot routes through pyscf_algebra::oracle_dot (Pitfall 9)"
    - "the ccsd_kernel loop extrapolates amplitudes through Diis::<AmplitudeSubspace>::new(6) and converges in the same iteration count as without DIIS or fewer"
  artifacts:
    - path: "crates/pyscf-ccsd/src/diis_amps.rs"
      provides: "AmplitudeSubspace: DiisStorable + amplitudes_to_vector/vector_to_amplitudes (port ccsd.py:670/679)"
      contains: "impl DiisStorable for AmplitudeSubspace"
    - path: "crates/pyscf-ccsd/src/ccsd.rs"
      provides: "run_diis wired into the kernel loop with diis_space=6, diis_start_cycle=0"
      contains: "Diis"
    - path: "crates/pyscf-ccsd/tests/diis_amps.rs"
      provides: "vector packing byte-match + iter-count convergence (CCSD-04)"
      contains: "amplitudes_to_vector"
  key_links:
    - from: "crates/pyscf-ccsd/src/diis_amps.rs"
      to: "pyscf_diis::{Diis, DiisStorable}"
      via: "one DIIS, second storable (D-06)"
      pattern: "DiisStorable|Diis"
    - from: "crates/pyscf-ccsd/src/diis_amps.rs"
      to: "pyscf_algebra::oracle_dot"
      via: "dot inner product (Pitfall 9)"
      pattern: "oracle_dot"
---

<objective>
Implement amplitude-DIIS (CCSD-04, D-06): a new `AmplitudeSubspace: DiisStorable` in `pyscf-ccsd` that packs t1 + lower-triangular t2 into one flat error/solution vector (byte-matching upstream `amplitudes_to_vector`, `ccsd.py:670`) and routes `dot` through `pyscf_algebra::oracle_dot` (Pitfall 9 — DIIS path drift). Wire `Diis::<AmplitudeSubspace>::new(6)` into the 06-03 `ccsd_kernel` loop's `run_diis` step with the verified `diis_space=6` / `diis_start_cycle=0` defaults. ONE DIIS, second storable — reuses the entire Phase-3 `pyscf-diis` machinery, no new DIIS body.

Purpose: Land CCSD-04 (amplitude-DIIS converges in the same iteration count as upstream) and re-validate Pitfall 9 on the amplitude vector — the ROADMAP's mapped Phase-6 DIIS re-validation.
Output: A DIIS-accelerated kernel; vector-packing byte-match + iter-count tests green.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ccsd/06-CONTEXT.md
@.planning/phases/06-ccsd/06-RESEARCH.md
@.planning/phases/06-ccsd/06-PATTERNS.md
@.planning/phases/06-ccsd/06-VALIDATION.md

<interfaces>
<!-- The DiisStorable contract + the generic Diis<S> to reuse. From the codebase. -->

From crates/pyscf-diis/src/storable.rs (the trait — dot MUST use oracle_dot; the doc-comment already names pyscf-ccsd::AmpsSubspace):
  pub trait DiisStorable {
      fn as_flat(&self) -> &[f64];
      fn from_flat(&mut self, slice: &[f64]);   // &mut self in-place
      fn dot(&self, other: &Self) -> f64;       // MUST be pyscf_algebra::oracle_dot (Pitfall 9)
      fn len(&self) -> usize;
  }

From crates/pyscf-diis/src/cdiis.rs (the generic machinery to reuse, NO new body):
  pub struct Diis<S: DiisStorable + Clone> { ... }  // B[i,j]=oracle_dot(err_i,err_j); solve_linear; extrap via oracle_sum
  // Diis::<AmplitudeSubspace>::new(6)

VERIFIED upstream packing (ccsd.py:670 amplitudes_to_vector / :679 vector_to_amplitudes):
  size = nov + nov*(nov+1)//2          # nov = nocc*nvir
  vector[:nov] = t1.ravel()
  pack_tril(t2.transpose(0,2,1,3).reshape(nov,nov))   # symmetric t2[iajb]==t2[jbia]
  # unpack: lib.unpack_tril(filltriu=SYMMETRIC)

VERIFIED defaults: diis_space=6 (ccsd.py:926 — NOT SCF's 8), diis_start_cycle=0 (ccsd.py:928).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AmplitudeSubspace: DiisStorable — pack t1 + tril(t2) byte-matching ccsd.py:670, dot via oracle_dot</name>
  <files>crates/pyscf-ccsd/src/diis_amps.rs</files>
  <behavior>
    - amplitudes_to_vector(t1,t2) produces a flat vector of length nov + nov*(nov+1)/2 with vector[:nov]==t1.ravel() and the tail == pack_tril of t2.transpose(0,2,1,3).
    - vector_to_amplitudes round-trips: pack then unpack reproduces (t1,t2) to bit-identity (symmetric filltriu).
    - AmplitudeSubspace::dot equals pyscf_algebra::oracle_dot of the two packed vectors (not iter().sum()).
    - len() == packed.len() == nov + nov*(nov+1)/2.
  </behavior>
  <read_first>
    - crates/pyscf-ccsd/src/diis_amps.rs (the 06-01 stub returning NotYetImplemented{wave:2})
    - crates/pyscf-diis/src/storable.rs (the DiisStorable trait — the dot-via-oracle_dot mandate + the doc-comment naming pyscf-ccsd::AmpsSubspace)
    - crates/pyscf-diis/src/cdiis.rs lines 199-214 (the test `V` minimal flat-vector storable impl — the exact shape to copy)
    - crates/pyscf-scf/src/ (FockSubspace impl of DiisStorable — the SCF storable precedent, if present)
    - UPSTREAM pyscf/cc/ccsd.py lines 670-690 (amplitudes_to_vector / vector_to_amplitudes — the pack_tril/unpack_tril layout to byte-match)
    - 06-PATTERNS.md §"diis_amps.rs" (lines 219-237 — the storable impl shape + the diis_space=6 note)
  </read_first>
  <action>
Implement `AmplitudeSubspace { packed: Vec<f64>, nocc: usize, nvir: usize }` in `diis_amps.rs`. Write `amplitudes_to_vector(t1, t2, nocc, nvir) -> Vec<f64>` and `vector_to_amplitudes(v, nocc, nvir) -> (t1, t2)` byte-matching the upstream `ccsd.py:670/679` layout: `nov = nocc*nvir`; `vector[:nov] = t1.ravel()`; the tail is `pack_tril(t2.transpose(0,2,1,3).reshape(nov,nov))` (the symmetric `t2[iajb]==t2[jbia]` lower-triangular pack); unpack uses the symmetric `filltriu` fill. Doc-comment the transpose/reshape flat-index mapping at the boundary (the project flat-index discipline). Impl `DiisStorable for AmplitudeSubspace`: `as_flat`→`&self.packed`, `from_flat`→`copy_from_slice`, `dot`→`pyscf_algebra::oracle_dot(&self.packed, &other.packed)` (MANDATORY — Pitfall 9; copy the cdiis.rs:208-210 test-V shape), `len`→`self.packed.len()`. NO running `+=`/`iter().sum()` anywhere.

RED→GREEN: write the four `<behavior>` tests first (the byte-match against a hand-built reference vector, the round-trip, the dot==oracle_dot, the len) against the stub, then implement.
  </action>
  <verify>
    <automated>cargo test -p pyscf-ccsd --lib diis_amps --locked 2>&1 | tail -8; grep -q 'oracle_dot' crates/pyscf-ccsd/src/diis_amps.rs && echo "ORACLE_DOT_OK"</automated>
  </verify>
  <done>`AmplitudeSubspace` impls `DiisStorable` with `dot` via `oracle_dot` (prints ORACLE_DOT_OK); `amplitudes_to_vector`/`vector_to_amplitudes` byte-match the upstream pack_tril layout and round-trip; the four behavior tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire run_diis into the ccsd_kernel loop (Diis::<AmplitudeSubspace>::new(6)) + iter-count test</name>
  <files>crates/pyscf-ccsd/src/ccsd.rs, crates/pyscf-ccsd/tests/diis_amps.rs</files>
  <read_first>
    - crates/pyscf-ccsd/src/ccsd.rs (06-03 kernel loop — the `t1,t2 = t1new,t2new` step that DIIS replaces, and the existing diis_space/diis_start_cycle constants)
    - crates/pyscf-ccsd/src/diis_amps.rs (Task-1 AmplitudeSubspace + the packers)
    - crates/pyscf-diis/src/cdiis.rs lines 33-142 (the Diis<S> update/extrapolate API — how SCF calls it)
    - crates/pyscf-scf/src/ (the SCF run_diis call site, if present — the extrapolate-when-istep>=start pattern)
    - UPSTREAM pyscf/cc/ccsd.py lines 1206-1230 (run_diis — only extrapolate when istep >= diis_start_cycle)
    - 06-VALIDATION.md row CCSD-04 (the `cargo test -p pyscf-ccsd diis_amps` command)
  </read_first>
  <action>
Wire DIIS into the 06-03 `ccsd_kernel` loop: construct `Diis::<AmplitudeSubspace>::new(DIIS_SPACE)` where `DIIS_SPACE=6` (the verified constant — NOT SCF's 8). Replace the placeholder `t1,t2 = t1new,t2new` step with `run_diis`: build the current `AmplitudeSubspace` from `(t1new,t2new)` AND the error vector `(t1new−t1, t2new−t2)` (the amplitude residual), feed both to the `Diis` ring buffer, extrapolate only when `istep >= DIIS_START_CYCLE (0)` (port `ccsd.py:1206`), unpack the extrapolated vector back to `(t1,t2)` via `vector_to_amplitudes`. Add a kernel flag `diis: bool` (default true, per `mycc.diis=True`) so a no-DIIS run is testable for the iter-count comparison. Keep the dual-criterion convergence unchanged.

Add to `crates/pyscf-ccsd/tests/diis_amps.rs` (or extend Task-1's): an integration arm that runs the small-system `ccsd_kernel` with `diis=true` and asserts it converges in `<=` the iteration count of the `diis=false` run (CCSD-04 — "within the same iteration count as upstream"; the in-tree proxy is "DIIS does not increase iterations and reaches the same energy"). Same final `e_corr` within 1e-9 between the DIIS and non-DIIS runs (DIIS changes the path, not the minimum — CCSD-03).
  </action>
  <verify>
    <automated>cargo test -p pyscf-ccsd diis_amps --locked -- --test-threads=1 2>&1 | tail -10; grep -q 'AmplitudeSubspace' crates/pyscf-ccsd/src/ccsd.rs && echo "DIIS_WIRED"</automated>
  </verify>
  <done>`ccsd_kernel` extrapolates through `Diis::<AmplitudeSubspace>::new(6)` (prints DIIS_WIRED); the iter-count test shows DIIS converges in `<=` the non-DIIS iteration count to the same `e_corr` (≤1e-9); `diis_start_cycle=0` honored; `cargo test -p pyscf-ccsd diis_amps` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure internal extrapolation over amplitude vectors; no Python boundary, no untrusted input, no file I/O in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-FP | Tampering (result integrity / DIIS path drift, Pitfall 9) | AmplitudeSubspace::dot + the DIIS B-matrix | mitigate | `dot` routes through `pyscf_algebra::oracle_dot`; the B-matrix solve reuses the proven `pyscf-diis` `solve_linear`/`oracle_sum`; vector packing byte-matches upstream. RAYON-invariant. |

No new threat surface — pure internal reduction; the only risk is non-deterministic reduction (mitigated by oracle_dot).
</threat_model>

<verification>
- `cargo test -p pyscf-ccsd diis_amps -- --test-threads=1` passes (packing byte-match + round-trip + dot + iter-count).
- `cargo test -p pyscf-ccsd --test rccsd_numeric_smoke` still passes (DIIS does not change the converged energy).
- Per-wave: `cargo test --workspace --locked` + release-oracle determinism arms green.
</verification>

<success_criteria>
- AmplitudeSubspace impls DiisStorable with dot via oracle_dot (Pitfall 9) and byte-matching upstream amplitudes_to_vector packing.
- The kernel extrapolates through Diis::<AmplitudeSubspace>::new(6); diis_start_cycle=0.
- DIIS converges in ≤ the non-DIIS iteration count to the same minimum (CCSD-04/CCSD-03).
- One DIIS, second storable — no new DIIS body.
</success_criteria>

<output>
Create `.planning/phases/06-ccsd/06-05-SUMMARY.md` when done.
</output>
