---
phase: 06-ccsd
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - crates/pyscf-ccsd/src/uccsd.rs
  - crates/pyscf-ccsd/src/uintermediates.rs
  - crates/pyscf-ccsd/src/reference.rs
  - crates/pyscf-ccsd/tests/uccsd_smoke.rs
autonomous: true
requirements: [CCSD-02]

must_haves:
  truths:
    - "cc.UCCSD on a small open-shell system returns a CCSD correlation energy = e_aa + e_bb + e_ab matching the open-shell reference"
    - "each spin channel transforms its OWN alpha/beta orbital energies; the alpha-beta cross channel is direct-only"
    - "every spin-channel contraction is a host-loop oracle reduction (no +=, no gemm); RAYON 1==8"
  artifacts:
    - path: "crates/pyscf-ccsd/src/uccsd.rs"
      provides: "uccsd_kernel + UccsdAmplitudes triple (t2aa/t2ab/t2bb) + open-shell energy (port uccsd.py)"
      contains: "uccsd_kernel"
    - path: "crates/pyscf-ccsd/src/uintermediates.rs"
      provides: "spin-resolved intermediates (port uintermediates.py)"
      contains: "pub fn"
    - path: "crates/pyscf-ccsd/tests/uccsd_smoke.rs"
      provides: "small open-shell UCCSD energy structural + numeric (CCSD-02)"
      contains: "uccsd_kernel"
  key_links:
    - from: "crates/pyscf-ccsd/src/uccsd.rs"
      to: "crate::ccsd::ccsd_kernel pattern"
      via: "reuses the iterate-loop / pre-flight / arena discipline per spin channel"
      pattern: "try_reserve|update"
    - from: "crates/pyscf-ccsd/src/uccsd.rs"
      to: "pyscf_algebra::{oracle_sum, oracle_dot}"
      via: "per-channel reductions"
      pattern: "oracle_sum|oracle_dot"
---

<objective>
Port the open-shell UCCSD (CCSD-02, D-05): `uccsd.py` + `uintermediates.py`. Mirror the Phase-5 `UmpReference { alpha, beta }` two-channel snapshot and the `UmpAmplitudes { t2aa, t2ab, t2bb }` spin-resolved container; each spin channel transforms its OWN α/β orbital energies; `e_corr = e_aa + e_bb + e_ab` (same-spin aa/bb antisymmetrized, opposite-spin ab direct-only). Same materialize-then-`oracle_sum`/`oracle_dot` reduction discipline per channel; reuse the 06-03 kernel iterate-loop + pre-flight + arena.

Purpose: Land CCSD-02 (open-shell). Reuses the Phase-5 UMP2 two-channel structural contract verbatim and the 06-03 RCCSD kernel discipline — the genuinely new work is the spin-resolved intermediates.
Output: A converging UCCSD whose energy matches a small open-shell reference; structural + numeric smoke green.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ccsd/06-CONTEXT.md
@.planning/phases/06-ccsd/06-RESEARCH.md
@.planning/phases/06-ccsd/06-PATTERNS.md
@.planning/phases/06-ccsd/06-VALIDATION.md

<interfaces>
<!-- The two-channel spin contract to mirror. From the codebase. -->

From crates/pyscf-mp2/src/ump2.rs:40-90 (the spin-resolved container + two-channel reference to mirror):
  pub struct UmpAmplitudes { nocc_a, nvir_a, nocc_b, nvir_b, t2aa: Vec<f64>, t2ab: Vec<f64>, t2bb: Vec<f64> }
  pub struct UmpReference { alpha: Mp2Reference, beta: Mp2Reference }
  // e_corr = e_aa + e_bb + e_ab (ump2.py:10); aa/bb antisymmetrized 0.5*(direct-exchange); ab direct-only

From crate::ccsd (06-03): ccsd_kernel<H>, default_energy, the iterate loop + pre-flight + arena discipline to mirror per channel.

VERIFIED defaults (reuse from 06-03 ccsd.rs constants): max_cycle=50, conv_tol=1e-7, conv_tol_normt=1e-5, diis_space=6.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port uintermediates.py (spin-resolved) + the UccsdAmplitudes triple + UccsdReference two-channel snapshot</name>
  <files>crates/pyscf-ccsd/src/uintermediates.rs, crates/pyscf-ccsd/src/uccsd.rs, crates/pyscf-ccsd/src/reference.rs</files>
  <behavior>
    - UccsdAmplitudes carries (t2aa, t2ab, t2bb) with the documented flat C-order layout matching UmpAmplitudes; constructing from synthetic per-channel blocks round-trips by index.
    - The spin-resolved intermediates on a synthetic 2x2 per-channel input match a hand-computed longhand reference for the aa, bb, and ab channels.
    - Each channel uses ITS OWN orbital energies (an asymmetric alpha/beta test gives t2aa != t2bb).
  </behavior>
  <read_first>
    - crates/pyscf-ccsd/src/uintermediates.rs + uccsd.rs (the 06-01 stubs returning NotYetImplemented{wave:2})
    - crates/pyscf-ccsd/src/reference.rs (06-01 CcsdReference — add the two-channel UccsdReference)
    - crates/pyscf-mp2/src/ump2.rs lines 1-110 (the UmpAmplitudes triple + UmpReference + the e_aa/e_bb/e_ab spin decomposition + the per-channel materialize-then-oracle discipline + the documented flat-index layout)
    - crates/pyscf-ccsd/src/rintermediates.rs (06-03 closed-shell intermediates — the spin-resolved versions mirror these per channel)
    - UPSTREAM pyscf/cc/uintermediates.py (the spin-block intermediates — port aa/bb/ab channels)
  </read_first>
  <action>
Define `UccsdAmplitudes { nocc_a, nvir_a, nocc_b, nvir_b, t2aa, t2ab, t2bb }` mirroring `UmpAmplitudes` (ump2.rs:40-69) with the IDENTICAL documented flat C-order index layout (copy the doc-comments). Add `UccsdReference { alpha: CcsdReference, beta: CcsdReference }` to `reference.rs` mirroring `UmpReference` (ump2.rs:79-90). Port `uintermediates.py` into `uintermediates.rs`: the spin-resolved Foo/Fvv/Fov/Woooo/Wvvvv/Wvoov/Wovvo analogs for the αα, ββ, and αβ channels — each as host-loop materialize-then-`oracle_sum`/`oracle_dot` (NO gemm, NO `+=`), each channel using its OWN α or β orbital energies. Doc-comment flat-index offsets at every boundary. ShapeMismatch-validate per block.

RED→GREEN: write the `<behavior>` tests (synthetic 2x2 per-channel longhand references + the asymmetric t2aa!=t2bb check) first against the stub, then implement.
  </action>
  <verify>
    <automated>cargo test -p pyscf-ccsd --lib uintermediates uccsd_amps --locked 2>&1 | tail -8</automated>
  </verify>
  <done>`UccsdAmplitudes` + `UccsdReference` exist with the documented layout; spin-resolved intermediates ported and matching the longhand references for aa/bb/ab; asymmetric α/β gives distinct t2aa/t2bb; no gemm/no `+=` (grep clean).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: uccsd_kernel (open-shell iterate, e_aa+e_bb+e_ab) + small open-shell oracle smoke</name>
  <files>crates/pyscf-ccsd/src/uccsd.rs, crates/pyscf-ccsd/tests/uccsd_smoke.rs</files>
  <behavior>
    - uccsd_kernel on a small open-shell system (e.g. doublet, or a triplet H2-like) converges and returns e_corr = e_aa + e_bb + e_ab.
    - On a closed-shell system fed as alpha==beta, the UCCSD e_corr equals the RCCSD e_corr from 06-03 (the spin-restricted consistency cross-check, within bit-close tolerance).
    - RAYON 1==8 converged energy invariance.
  </behavior>
  <read_first>
    - crates/pyscf-ccsd/src/uccsd.rs (Task-1's intermediates + the kernel stub)
    - crates/pyscf-ccsd/src/ccsd.rs (06-03 ccsd_kernel — the iterate-loop / pre-flight try_reserve / arena reserve-once / dual-convergence discipline to mirror)
    - crates/pyscf-mp2/src/ump2.rs (ump2_kernel — the e_aa+e_bb+e_ab assembly + per-channel reduction)
    - crates/pyscf-mp2/tests/ (the open-shell UMP2 smoke test shape, if present) and crates/pyscf-ccsd/tests/rccsd_numeric_smoke.rs (06-03 — the small-system reference-build pattern)
    - UPSTREAM pyscf/cc/uccsd.py (the UCCSD kernel + energy)
    - 06-VALIDATION.md row CCSD-02 (the `cargo test -p pyscf-ccsd --test uccsd_smoke` command)
  </read_first>
  <action>
Port the `uccsd.py` kernel into `uccsd::uccsd_kernel<H>(refr: &UccsdReference, frozen: &Frozen, hooks: &H, pool: &WorkspacePool) -> Result<UccsdResult, PyscfRsError>`, mirroring the 06-03 `ccsd_kernel` discipline: pre-flight `pool.try_reserve(estimate)?` (per-channel vvvv estimate), init each channel's `t2 = (ia|jb)/Dijab` MP2 seed using its own orbital energies, reserve the vvvv arena buffers once before the loop, iterate `update_amps` per spin channel + the cross αβ channel, compute `e_corr = e_aa + e_bb + e_ab` via `oracle_sum`, dual-criterion convergence (`conv_tol=1e-7`, `conv_tol_normt=1e-5`, `max_cycle=50` — reuse the 06-03 constants), release buffers. Same-spin (aa/bb) antisymmetrized; opposite-spin (ab) direct-only (the ump2.rs:10 decomposition extended to CC). EVERY reduction via `oracle_sum`/`oracle_dot`, no `+=`, no gemm.

Create `crates/pyscf-ccsd/tests/uccsd_smoke.rs` (CCSD-02): a small open-shell reference run + assert convergence and `e_corr` vs a published open-shell reference (or, where no published small-open-shell value is in-tree, assert the closed-shell-consistency cross-check: feeding a closed-shell system as α==β yields the 06-03 RCCSD e_corr within bit-close tolerance — the structural+numeric always-on arm; live upstream open-shell byte-identity is the 06-08 workflow_dispatch arm). RAYON 1==8 invariance assertion.
  </action>
  <verify>
    <automated>cargo test -p pyscf-ccsd --test uccsd_smoke --locked -- --test-threads=1 2>&1 | tail -10</automated>
  </verify>
  <done>`uccsd_kernel` converges and returns `e_corr = e_aa+e_bb+e_ab`; the closed-shell-as-α==β cross-check matches 06-03 RCCSD e_corr; RAYON 1==8 invariant; pre-flight + reserve-once arena discipline mirrored; `cargo test -p pyscf-ccsd --test uccsd_smoke` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook returns / per-channel eris → kernel | wrong-shaped spin-block arrays must be validated before indexing. |
| in-core job → memory budget | per-channel vvvv reservation crosses PYSCF_MAX_MEMORY. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-SHAPE | DoS / Tampering | UccsdAmplitudes / spin-block lengths | mitigate | ShapeMismatch `?`-propagation per channel before indexing; `#![forbid(unsafe_code)]`. |
| T-06-04-OOM | DoS | uccsd_kernel pre-flight | mitigate | per-channel `try_reserve` HARD refusal (D-01), reuses the 06-03 contract. |
| T-06-04-FP | Tampering (integrity) | per-channel reductions | mitigate | `oracle_sum`/`oracle_dot`, no `+=`/gemm; RAYON 1==8 invariance test; check-no-fma scans pyscf-ccsd. |

No network/auth/session/crypto surface — pure internal contraction.
</threat_model>

<verification>
- `cargo test -p pyscf-ccsd --test uccsd_smoke -- --test-threads=1` passes.
- `cargo test -p pyscf-ccsd --lib uintermediates` passes.
- Per-wave: `cargo test --workspace --locked` + release-oracle determinism arms green.
</verification>

<success_criteria>
- UCCSD converges and returns e_aa+e_bb+e_ab matching the open-shell / closed-shell-consistency reference (CCSD-02).
- Spin channels use their own α/β energies; aa/bb antisymmetrized, ab direct-only.
- Reuses the 06-03 pre-flight + arena + dual-convergence discipline; all reductions oracle-based, thread-invariant.
</success_criteria>

<output>
Create `.planning/phases/06-ccsd/06-04-SUMMARY.md` when done.
</output>
