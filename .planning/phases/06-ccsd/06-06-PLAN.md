---
phase: 06-ccsd
plan: 06
type: execute
wave: 5
depends_on: ["06-03"]
files_modified:
  - crates/pyscf-ccsd/src/lambda.rs
  - crates/pyscf-ccsd/src/ulambda.rs
  - crates/pyscf-ccsd/src/rdm.rs
  - crates/pyscf-ccsd/src/urdm.rs
  - crates/pyscf-ccsd/tests/lambda.rs
  - crates/pyscf-ccsd/tests/rdm.rs
autonomous: true
requirements: [CCSD-05, CCSD-06]

must_haves:
  truths:
    - "mycc.solve_lambda() produces converged lambda amplitudes (l1,l2) for the response density"
    - "make_rdm1 builds the nmo x nmo MO 1-RDM; make_rdm2 builds the nmo^4 MO 2-RDM; both match the reference"
    - "make_rdm2(ao_repr=true) returns the AO back-transformed 2-RDM (D-03 ships ao_repr THIS phase)"
    - "Tr(make_rdm1) == nelec (the 1-RDM trace invariant)"
    - "every lambda/RDM contraction is a host-loop oracle reduction; the nmo^4 AO back-transform is an arena tenant"
  artifacts:
    - path: "crates/pyscf-ccsd/src/lambda.rs"
      provides: "solve_lambda + update_lambda + l1/l2 intermediates (port ccsd_lambda.py, concrete CCSD.solve_lambda)"
      contains: "solve_lambda"
    - path: "crates/pyscf-ccsd/src/rdm.rs"
      provides: "make_rdm1/make_rdm2 + _gamma1/_gamma2_intermediates + ao_repr back-transform (port ccsd_rdm.py)"
      contains: "make_rdm2"
    - path: "crates/pyscf-ccsd/tests/lambda.rs"
      provides: "lambda convergence + small-system reference (CCSD-05)"
      contains: "solve_lambda"
    - path: "crates/pyscf-ccsd/tests/rdm.rs"
      provides: "rdm1 trace invariant + rdm2 shape + ao_repr (CCSD-06)"
      contains: "ao_repr"
  key_links:
    - from: "crates/pyscf-ccsd/src/rdm.rs"
      to: "crate::lambda::solve_lambda"
      via: "RDMs consume the lambda amplitudes"
      pattern: "lambda|l1|l2"
    - from: "crates/pyscf-ccsd/src/rdm.rs"
      to: "pyscf_ao2mo + pyscf_runtime::WorkspacePool"
      via: "ao_repr nmo^4 AO back-transform is an arena tenant"
      pattern: "ao_repr|reserve"
---

<objective>
Port the CCSD response surface: λ-equations (CCSD-05, `ccsd_lambda.py`/`uccsd_lambda.py`) and reduced density matrices (CCSD-06, `ccsd_rdm.py`/`uccsd_rdm.py`) INCLUDING the `make_rdm2(ao_repr=True)` nmo⁴ AO back-transform (D-03 — a deliberate departure from Phase-5's ao_repr deferral, because CCSD RDMs are the heaviest arena tenant and Phase-7 gradients want a complete λ+RDM surface). `solve_lambda` ports the CONCRETE `CCSD.solve_lambda` (`ccsd.py:1273` → `ccsd_lambda.kernel`; the base `CCSDBase.solve_lambda` raises NotImplementedError — port the concrete-class behavior, RESEARCH A6). Every contraction host-loop oracle reduction; the AO back-transform routes the nmo⁴ contraction through `pyscf-ao2mo` over an arena-reserved buffer.

Purpose: Land CCSD-05 (λ for response densities, consumed by Phase-7 GRAD-06) and CCSD-06 (make_rdm1/make_rdm2 incl. ao_repr). Exercises the heaviest arena tenant (nmo⁴ AO back-transform) — the natural CCSD-11 stress test.
Output: Converged λ + MO/AO RDMs matching small-system references; trace invariant holds.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ccsd/06-CONTEXT.md
@.planning/phases/06-ccsd/06-RESEARCH.md
@.planning/phases/06-ccsd/06-PATTERNS.md
@.planning/phases/06-ccsd/06-VALIDATION.md

<interfaces>
<!-- From the codebase + verified upstream. -->

From crate::ccsd (06-03): ccsd_kernel produces CcsdResult { t1, t2, ... }; default_energy; the reduction discipline + arena reserve/release.
From crate::update_amps (06-03): the host-loop intermediate-build pattern lambda mirrors.
From crates/pyscf-mp2/src/rdm.rs: gamma1_intermediates / make_rdm1 / make_rdm2 — the build-RDM-from-amplitudes shape (MP2 deferred ao_repr; CCSD ships it).

VERIFIED upstream (RESEARCH A6): CCSDBase.solve_lambda (ccsd.py:1118) raises NotImplementedError; the concrete CCSD.solve_lambda (ccsd.py:1273) dispatches to ccsd_lambda.kernel — port the concrete behavior.

ao_repr (D-03): make_rdm2(ao_repr=true) = the nmo^4 AO back-transform via pyscf-ao2mo (the contraction MP2 deferred to Phase 7; CCSD does it now). This buffer is an arena tenant (reserve once).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port ccsd_lambda.py + uccsd_lambda.py — solve_lambda / update_lambda / l1,l2 intermediates</name>
  <files>crates/pyscf-ccsd/src/lambda.rs, crates/pyscf-ccsd/src/ulambda.rs, crates/pyscf-ccsd/tests/lambda.rs</files>
  <behavior>
    - solve_lambda on the small-system converged (t1,t2) from 06-03 converges (l1,l2) on the lambda dual criterion.
    - At convergence on a closed-shell system the lambda amplitudes satisfy the canonical l~t structural sanity check, and the small-system l2 matches an in-tree reference within the small-system tolerance.
    - update_lambda is a host-loop oracle reduction; RAYON 1==8 invariant.
  </behavior>
  <read_first>
    - crates/pyscf-ccsd/src/lambda.rs + ulambda.rs (the 06-01 stubs returning NotYetImplemented{wave:3})
    - crates/pyscf-ccsd/src/ccsd.rs (06-03 — the iterate loop + dual convergence + arena discipline solve_lambda mirrors)
    - crates/pyscf-ccsd/src/rintermediates.rs (06-03 — the l1/l2 intermediates reuse the cc_W* shapes)
    - UPSTREAM pyscf/cc/ccsd_lambda.py (kernel + update_lambda + make_intermediates — port the lambda iterate)
    - UPSTREAM pyscf/cc/uccsd_lambda.py (open-shell lambda)
    - UPSTREAM pyscf/cc/ccsd.py lines 1273-1290 (the concrete CCSD.solve_lambda dispatch — RESEARCH A6)
    - 06-VALIDATION.md row CCSD-05 (the `cargo test -p pyscf-ccsd lambda` command)
  </read_first>
  <action>
Port `ccsd_lambda.py` into `lambda.rs`: `solve_lambda(refr, t1, t2, eris, hooks, pool) -> Result<(l1, l2), PyscfRsError>` porting the CONCRETE `CCSD.solve_lambda` (`ccsd.py:1273` → `ccsd_lambda.kernel`), `update_lambda` (the λ iterate), and the l1/l2 `make_intermediates`. Mirror the 06-03 kernel discipline: pre-flight `try_reserve`, reserve arena buffers once before the λ loop, iterate to a λ dual criterion (`conv_tol`/`conv_tol_normt` — reuse the 06-03 constants), every einsum a host-loop materialize-then-`oracle_sum`/`oracle_dot` (NO gemm, NO `+=`). Port `uccsd_lambda.py` into `ulambda.rs` for the open-shell channels (the spin-resolved λ mirroring 06-04's uintermediates). ShapeMismatch-validate inputs.

RED→GREEN: write `<behavior>` tests (λ convergence on the 06-03 small-system (t1,t2); the structural l~t sanity check; RAYON invariance) against the stub, then implement.
  </action>
  <verify>
    <automated>cargo test -p pyscf-ccsd --test lambda --locked -- --test-threads=1 2>&1 | tail -8; cargo test -p pyscf-ccsd --lib lambda --locked 2>&1 | tail -3</automated>
  </verify>
  <done>`solve_lambda`/`update_lambda` ported (concrete-class behavior); `ulambda.rs` covers open-shell; λ converges on the small system; host-loop oracle reductions only (no gemm/`+=`); RAYON 1==8 invariant; `cargo test -p pyscf-ccsd --test lambda` green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port ccsd_rdm.py + uccsd_rdm.py — make_rdm1/make_rdm2 + _gamma1/_gamma2 + the ao_repr nmo^4 back-transform (D-03)</name>
  <files>crates/pyscf-ccsd/src/rdm.rs, crates/pyscf-ccsd/src/urdm.rs, crates/pyscf-ccsd/tests/rdm.rs</files>
  <behavior>
    - make_rdm1 on the small-system converged (t1,t2,l1,l2) builds the nmo x nmo MO 1-RDM with Tr == nelec (the conservation invariant) and matches an in-tree reference.
    - make_rdm2 builds the nmo^4 MO 2-RDM whose 1-RDM contraction reproduces make_rdm1 (the consistency invariant).
    - make_rdm2(ao_repr=true) returns a nao^4 AO 2-RDM (D-03 ships it THIS phase — NOT NotYetImplemented); its trace invariant holds in the AO metric.
    - every contraction is a host-loop oracle reduction; the ao_repr buffer is arena-reserved (no per-call alloc).
  </behavior>
  <read_first>
    - crates/pyscf-ccsd/src/rdm.rs + urdm.rs (the 06-01 stubs returning NotYetImplemented{wave:3})
    - crates/pyscf-ccsd/src/lambda.rs (Task-1 solve_lambda — RDMs consume l1/l2)
    - crates/pyscf-mp2/src/rdm.rs (the gamma1_intermediates / make_rdm1 / make_rdm2 build-from-amplitudes shape + the frozen oidx/vidx embedding; note MP2 ao_repr=true returned NotYetImplemented — CCSD does NOT)
    - crates/pyscf-ao2mo/src/transform.rs (the host-loop AO<->MO transform — the ao_repr nmo^4 back-transform routes through general/full)
    - crates/pyscf-runtime/src/workspace_pool.rs (06-02 reserve/release — the ao_repr buffer is an arena tenant)
    - UPSTREAM pyscf/cc/ccsd_rdm.py (make_rdm1/make_rdm2, _gamma1_intermediates/_gamma2_intermediates, the ao_repr=True AO back-transform)
    - UPSTREAM pyscf/cc/uccsd_rdm.py (open-shell RDM)
    - 06-VALIDATION.md row CCSD-06 (the `cargo test -p pyscf-ccsd rdm` command)
  </read_first>
  <action>
Port `ccsd_rdm.py` into `rdm.rs`: `_gamma1_intermediates`/`_gamma2_intermediates` (consuming t1/t2/l1/l2), `make_rdm1(..., ao_repr: bool)` (the nmo×nmo MO 1-RDM; `ao_repr=true` → `C·γ·Cᵀ` AO back-transform), `make_rdm2(..., ao_repr: bool)` (the nmo⁴ MO 2-RDM; `ao_repr=true` → the nao⁴ AO back-transform via `pyscf-ao2mo` — D-03, this is the heaviest arena tenant: `pool.reserve` the back-transform buffer ONCE, do NOT allocate per call). Mirror the `pyscf-mp2/src/rdm.rs` build-from-amplitudes shape + the frozen oidx/vidx fancy-index embedding (CCSD-10 frozen consistency). Port `uccsd_rdm.py` into `urdm.rs` for the spin-resolved RDMs. EVERY contraction host-loop materialize-then-`oracle_sum`/`oracle_dot` (no gemm, no `+=`). ShapeMismatch-validate.

CRITICAL D-03 departure: unlike Phase-5 MP2 (which returned `NotYetImplemented` for `make_rdm2(ao_repr=true)`), CCSD SHIPS the AO back-transform numerically THIS phase — do NOT stub it.

RED→GREEN: write the `<behavior>` tests (the Tr==nelec 1-RDM invariant; the 2-RDM-contraction-reproduces-make_rdm1 invariant; the ao_repr=true AO RDM trace invariant) against the stub, then implement.
  </action>
  <verify>
    <automated>cargo test -p pyscf-ccsd --test rdm --locked -- --test-threads=1 2>&1 | tail -10; grep -q 'ao_repr' crates/pyscf-ccsd/src/rdm.rs && grep -q 'reserve' crates/pyscf-ccsd/src/rdm.rs && echo "AO_REPR_ARENA_OK"</automated>
  </verify>
  <done>`make_rdm1`/`make_rdm2` ported with the `ao_repr: bool` parameter; `ao_repr=true` returns a real AO RDM (NOT NotYetImplemented — D-03); Tr==nelec + 2-RDM-reproduces-1-RDM invariants pass; the ao_repr buffer is arena-reserved (prints AO_REPR_ARENA_OK); host-loop oracle reductions only; `cargo test -p pyscf-ccsd --test rdm` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| amplitude inputs → λ/RDM build | wrong-shaped t1/t2/l1/l2 must be validated before indexing. |
| ao_repr back-transform → memory budget | the nmo⁴/nao⁴ AO RDM buffer is the heaviest tenant; crosses PYSCF_MAX_MEMORY. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-SHAPE | DoS / Tampering | l1/l2/gamma block lengths | mitigate | ShapeMismatch `?`-propagation before indexing; `#![forbid(unsafe_code)]`. |
| T-06-06-OOM | DoS | make_rdm2(ao_repr) nmo⁴ buffer | mitigate | `pool.try_reserve` pre-flight before the AO back-transform (D-01); arena-reserved once. |
| T-06-06-FP | Tampering (integrity) | λ + RDM reductions | mitigate | `oracle_sum`/`oracle_dot`, no `+=`/gemm; RAYON 1==8; check-no-fma scans pyscf-ccsd. |

No network/auth/session/crypto surface — pure internal contraction.
</threat_model>

<verification>
- `cargo test -p pyscf-ccsd --test lambda -- --test-threads=1` passes (λ convergence).
- `cargo test -p pyscf-ccsd --test rdm -- --test-threads=1` passes (trace invariant + ao_repr).
- Per-wave: `cargo test --workspace --locked` + release-oracle determinism arms green.
- λ/RDM byte-identity vs upstream is the 06-08 workflow_dispatch human-verify arm (sandbox has no PySCF).
</verification>

<success_criteria>
- solve_lambda produces converged λ (CCSD-05), consumed by make_rdm (and Phase-7 GRAD-06).
- make_rdm1/make_rdm2 match the small-system reference with Tr==nelec; ao_repr=true ships the AO back-transform numerically (D-03, NOT deferred).
- The nmo⁴ AO back-transform is the heaviest arena tenant, reserved once (CCSD-11 stress).
- All reductions oracle-based, thread-invariant.
</success_criteria>

<output>
Create `.planning/phases/06-ccsd/06-06-SUMMARY.md` when done.
</output>
