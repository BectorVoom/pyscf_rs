---
phase: 06-ccsd
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - crates/pyscf-ccsd/src/rintermediates.rs
  - crates/pyscf-ccsd/src/update_amps.rs
  - crates/pyscf-ccsd/src/eris.rs
  - crates/pyscf-ccsd/src/ccsd.rs
  - crates/pyscf-ccsd/src/hooks.rs
  - crates/pyscf-ccsd/tests/rccsd_numeric_smoke.rs
  - crates/pyscf-ccsd/tests/convergence.rs
autonomous: true
requirements: [CCSD-01, CCSD-03, CCSD-11]

must_haves:
  truths:
    - "cc.RCCSD on a small system returns a CCSD correlation energy matching the upstream reference to <=1 microHartree"
    - "the iteration converges on the dual criterion |dE|<conv_tol AND normt<conv_tol_normt"
    - "every contraction is a host-loop materialize-then-oracle_sum/oracle_dot (no bare +=, no gemm)"
    - "Wabef/vvvv is reserved once before the loop via the arena, not allocated per iteration"
    - "init_amps seeds t1=0, t2=(ia|jb)/Dijab and reports emp2 reusing the MP2 ovov path"
  artifacts:
    - path: "crates/pyscf-ccsd/src/rintermediates.rs"
      provides: "cc_Foo/cc_Fvv/cc_Fov/cc_Woooo/cc_Wvvvv/cc_Wvoov/cc_Wovvo/make_tau (port rintermediates.py)"
      contains: "cc_Wvvvv"
    - path: "crates/pyscf-ccsd/src/update_amps.rs"
      provides: "default_update_amps + _add_vvvv/_contract_vvvv_t2 (host-loop, port ccsd.py:104,362-490)"
      contains: "default_update_amps"
    - path: "crates/pyscf-ccsd/src/ccsd.rs"
      provides: "ccsd_kernel loop + init_amps(MP2 seed) + default_energy + defaults + pre-flight reserve"
      contains: "ccsd_kernel"
    - path: "crates/pyscf-ccsd/tests/rccsd_numeric_smoke.rs"
      provides: "small-system in-core RCCSD energy <=1uH (always-on, CCSD-01)"
      contains: "1e-6"
  key_links:
    - from: "crates/pyscf-ccsd/src/ccsd.rs"
      to: "pyscf_runtime::WorkspacePool::reserve"
      via: "pre-flight refusal + once-before-loop vvvv reservation"
      pattern: "reserve|try_reserve"
    - from: "crates/pyscf-ccsd/src/ccsd.rs"
      to: "pyscf_mp2 ovov / pyscf_ao2mo"
      via: "init_amps MP2 seed reuses the in-core ovov transform"
      pattern: "ao2mo|ovov"
    - from: "crates/pyscf-ccsd/src/update_amps.rs"
      to: "pyscf_algebra::{oracle_sum, oracle_dot}"
      via: "every einsum is materialize-then-oracle reduce"
      pattern: "oracle_sum|oracle_dot"
---

<objective>
Port the in-core closed-shell RCCSD — the UN-GATED NUMERIC HEADLINE (D-02). This is the proof that CCSD energy works in-tree with no external dependency: `int2e` has been real and bit-exact since plan 05-08, so the small-system RCCSD correlation energy is achievable and oracle-validatable THIS phase. Port `rintermediates.py` (`cc_Foo`/`cc_Fvv`/`cc_Fov`/`cc_Woooo`/`cc_Wvvvv`/`cc_Wvoov`/`cc_Wovvo`/`make_tau`), `update_amps` (`ccsd.py:104` + the `_add_vvvv`/`_contract_vvvv_t2` `'ijcd,acdb->ijab'` core, `ccsd.py:362-490`), the `ccsd_kernel` iteration loop with `init_amps` MP2 seed, `default_energy`, the verified convergence defaults, and the pre-flight arena reservation. Every contraction is a host-loop materialize-then-`oracle_sum`/`oracle_dot` (the gemm stub finding — NO gemm, NO bare `+=`).

Purpose: Land CCSD-01/CCSD-03 (in-core RCCSD ≤1 µHartree, dual convergence) and exercise the CCSD-11 arena under real math (`Wabef`/`vvvv` reserved once before the loop). This is the headline gate — everything after (UCCSD, DIIS, λ, RDM, AO-direct, DF) builds on this kernel.
Output: A converging in-core RCCSD whose energy passes the always-on small-system oracle to ≤1 µHartree.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ccsd/06-CONTEXT.md
@.planning/phases/06-ccsd/06-RESEARCH.md
@.planning/phases/06-ccsd/06-PATTERNS.md
@.planning/phases/06-ccsd/06-VALIDATION.md

<interfaces>
<!-- The reduction discipline + kernel + defaults contracts. From the codebase + verified upstream. -->

From crates/pyscf-mp2/src/mp2.rs:275-296 (the CANONICAL reduction discipline to replicate — NO gemm, NO +=):
  let edi = 2.0 * oracle_dot(&gi, &t2i);   // materialize gi*t2i then reduce
  let exi = -oracle_dot(&g_jba, &t2i);
  e_ss_terms.push(edi * 0.5 + exi);        // collect per-index terms
  let e_ss = oracle_sum(&e_ss_terms);      // single final reduction

From crates/pyscf-mp2/src/mp2.rs:191-205 (the ShapeMismatch ?-propagation to apply at every block):
  if eris.ovov.len() != expected_len {
      return Err(CcsdError::ShapeMismatch { expected: expected_len, got: eris.ovov.len() }.into());
  }

VERIFIED upstream convergence defaults — pyscf/cc/ccsd.py CCSDBase class attributes (these win at runtime over kernel-signature defaults):
  max_cycle        = 50      (ccsd.py:920)
  conv_tol         = 1e-7    (ccsd.py:921)   [energy]
  conv_tol_normt   = 1e-5    (ccsd.py:923)   [amplitude norm — NOT the 1e-6 in CONTEXT Discretion; the class attr wins]
  diis_space       = 6       (ccsd.py:926)   [NOT SCF's 8 — used in 06-04]
  diis_start_cycle = 0       (ccsd.py:928)

VERIFIED gemm stub (crates/pyscf-algebra/src/gemm.rs:16): returns NotYetImplemented{phase:2} — DO NOT call it.

Upstream init_amps MP2 seed (ccsd.py:1048): t1 = fock[:nocc,nocc:]/eia; t2 = ovov.transpose(0,2,1,3)/Dijab; emp2 = 2*einsum('ijab,iajb') - einsum('jiab,iajb'). The t2=(ia|jb)/Dijab form is already in mp2.rs:265-273.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port rintermediates.py + update_amps (host-loop, oracle reductions, arena-tenant vvvv)</name>
  <files>crates/pyscf-ccsd/src/rintermediates.rs, crates/pyscf-ccsd/src/update_amps.rs, crates/pyscf-ccsd/src/eris.rs</files>
  <behavior>
    - make_tau(t1,t2) reproduces tau = t2 + einsum('ia,jb->ijab', t1, t1) on a synthetic small (nocc,nvir) input, element-for-element vs an independent longhand reference.
    - cc_Foo/cc_Fvv/cc_Fov on synthetic eris match a hand-computed reference for nocc=1,nvir=1 and nocc=2,nvir=2.
    - The vvvv contraction _contract_vvvv_t2 ('ijcd,acdb->ijab') matches a longhand quadruple-loop reference on a 2x2 synthetic block (the heaviest einsum — proves the host-loop port is correct).
    - default_update_amps(t1,t2,eris) returns (t1new,t2new) whose values are thread-count invariant (RAYON_NUM_THREADS=1 vs 8 byte-identical) — the Pitfall 2 guard.
  </behavior>
  <read_first>
    - crates/pyscf-ccsd/src/rintermediates.rs + update_amps.rs (the 06-01 stubs returning NotYetImplemented{wave:1})
    - crates/pyscf-ccsd/src/eris.rs (the ChemistsEris blocks from 06-01 — confirm oooo/ovoo/oovv/ovov/ovvo/ovvv/vvvv/fock/mo_energy layout)
    - crates/pyscf-mp2/src/mp2.rs lines 253-313 (the materialize-then-oracle_dot/oracle_sum reduction template + the ShapeMismatch ?-propagation)
    - crates/pyscf-ao2mo/src/transform.rs lines 9-33 (the explicit "gemm is a stub so contractions are host loops" doc-note + the flat-index-offset discipline to doc-comment at every boundary)
    - UPSTREAM pyscf/cc/rintermediates.py lines 30-326 (cc_Foo/Fvv/Fov @30-60, cc_Woooo/Wvvvv/Wvoov/Wovvo, make_tau — port each einsum as a host loop)
    - UPSTREAM pyscf/cc/ccsd.py lines 104-360 (update_amps body) + lines 362-490 (_add_vvvv/_add_vvvv_tril/_contract_vvvv_t2 — the 'ijcd,acdb->ijab' core)
    - crates/pyscf-runtime/src/workspace_pool.rs (the reserve/release handle from 06-02 — the vvvv buffer is reserved by the kernel in Task 2 and passed in; update_amps does NOT allocate it)
  </read_first>
  <action>
Port `rintermediates.py` into `rintermediates.rs`: `make_tau`, `cc_Foo`, `cc_Fvv`, `cc_Fov`, `cc_Woooo`, `cc_Wvvvv`, `cc_Wvoov`, `cc_Wovvo`. EVERY einsum becomes an explicit host loop that materializes the elementwise products into a `Vec` then calls `pyscf_algebra::oracle_sum`/`oracle_dot` for the contracted axis — NO `pyscf_algebra::gemm` (verified stub, gemm.rs:16), NO bare `+=` accumulation (Pitfall 1/2 — breaks thread-count invariance). Doc-comment the flat C-order index offset at every block boundary (the `transform.rs:21-33` discipline). Validate every input block length against the expected `nocc*nvir*...` BEFORE indexing → `Err(CcsdError::ShapeMismatch{..})`, never panic/OOB (V5, the `#![forbid(unsafe_code)]` guarantee).

Port `update_amps` (`ccsd.py:104-360`) into `update_amps::default_update_amps(t1, t2, eris) -> Result<(Amplitudes, Amplitudes), PyscfRsError>` (or the handle types from 06-02). Build the intermediates via the `rintermediates.rs` fns, then assemble `t1new`/`t2new` with the same materialize-then-`oracle_*` discipline. Port `_contract_vvvv_t2` (`ccsd.py:362-490`) — the `'ijcd,acdb->ijab'` vvvv contraction (the `Wabef ≈ nv⁴` arena tenant, the heaviest FLOP in the project) — as a host loop over the (a,b) tiles, materializing each tile's products and reducing with `oracle_sum`. The vvvv buffer is PASSED IN (reserved once by the kernel, Task 2) — `update_amps` writes into the provided arena buffer, it does NOT `reserve`/allocate it per call (Pitfall 20 / CCSD-11).

Use the verified upstream constants when any default appears (`conv_tol_normt=1e-5` per the research correction — NOT the CONTEXT 1e-6). Reductions imported from `pyscf_algebra::{oracle_dot, oracle_sum}` (the mp2.rs:22 import).

RED→GREEN: write the four `<behavior>` tests first (synthetic small eris with hand-computed references, esp. the 2x2 vvvv longhand) — they fail against the 06-01 stub — then implement until green. The thread-count-invariance test runs the same `default_update_amps` under `RAYON_NUM_THREADS=1` and `=8` and asserts byte-identical output.
  </action>
  <verify>
    <automated>cargo test -p pyscf-ccsd --lib rintermediates update_amps --locked 2>&1 | tail -8; RAYON_NUM_THREADS=1 cargo test -p pyscf-ccsd --lib update_amps_thread_invariant --locked 2>&1 | tail -3 && RAYON_NUM_THREADS=8 cargo test -p pyscf-ccsd --lib update_amps_thread_invariant --locked 2>&1 | tail -3</automated>
  </verify>
  <done>`rintermediates.rs` exports all 8 intermediate fns; `update_amps::default_update_amps` is ported; the make_tau/cc_F*/vvvv-2x2/thread-invariance behavior tests pass; the vvvv 'ijcd,acdb->ijab' contraction matches the longhand reference; no `gemm` call, no bare `+=` in the contraction (grep clean); output is RAYON_NUM_THREADS 1==8 byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ccsd_kernel loop + init_amps MP2 seed + default_energy + defaults + pre-flight arena reserve + small-system oracle</name>
  <files>crates/pyscf-ccsd/src/ccsd.rs, crates/pyscf-ccsd/src/hooks.rs, crates/pyscf-ccsd/tests/rccsd_numeric_smoke.rs, crates/pyscf-ccsd/tests/convergence.rs</files>
  <behavior>
    - init_amps on a small system seeds t1=0 and t2=(ia|jb)/Dijab and reports an emp2 equal to the in-core RMP2 e_corr on the same system (reuses the MP2 ovov path).
    - default_energy(t1,t2,eris) on the converged amplitudes equals 2*einsum('ijab,iajb') - einsum('jiab,iajb') over the oovv/ovov blocks (host-loop oracle reduction).
    - ccsd_kernel converges (converged=true) on the dual criterion |dE|<1e-7 AND normt<1e-5 within max_cycle=50 on H2/STO-3G or a comparably small system.
    - ccsd_kernel calls pool.try_reserve BEFORE allocating vvvv; an over-budget in-core run returns MemoryLimitExceeded (no silent downgrade).
  </behavior>
  <read_first>
    - crates/pyscf-ccsd/src/ccsd.rs (the 06-01 stub: ccsd_kernel/default_ao2mo/default_energy/CcsdResult signatures)
    - crates/pyscf-ccsd/src/hooks.rs (the CcsdOverrideHooks trait — default_energy delegate)
    - crates/pyscf-mp2/src/mp2.rs lines 176-313 (the rmp2_kernel generic-over-hooks shape + ShapeMismatch ?-propagation + the t2=(ia|jb)/Dijab block at 265-273 to copy for init_amps + default_ao2mo building the ovov block from int2e→ao2mo.general)
    - crates/pyscf-mp2/src/lib.rs (the rmp2_kernel + ovov path + Frozen + the 5 MP2-08 helpers to reuse for init_amps/frozen)
    - crates/pyscf-runtime/src/workspace_pool.rs (06-02 reserve/release/try_reserve — the pre-flight refusal contract)
    - UPSTREAM pyscf/cc/ccsd.py lines 44-101 (the kernel iterate to port), 710-746 (energy), 1048-1077 (init_amps/emp2), 920-928 (the VERIFIED class defaults)
    - crates/pyscf-mp2/src/dfmp2.rs / crates/pyscf-py/src/mp.rs:111-114 (how the in-core ovov is built — for default_ao2mo)
    - 06-VALIDATION.md §"Per-Requirement Verification Map" rows CCSD-01/CCSD-03 (the exact test commands)
  </read_first>
  <action>
Port the `ccsd.py:44-101` kernel loop into `ccsd::ccsd_kernel<H: CcsdOverrideHooks>(refr: &CcsdReference, frozen: &Frozen, hooks: &H, pool: &WorkspacePool) -> Result<CcsdResult, PyscfRsError>` (the rmp2_kernel generic-over-hooks shape). Sequence: (1) PRE-FLIGHT `pool.try_reserve(estimate_bytes(nocc,nvir))?` — on `Err(MemoryLimitExceeded)` propagate with no downgrade (D-01/CCSD-11; tell the user to opt into DF/AO-direct). (2) Build eris via `hooks.ao2mo(refr, frozen)?`. (3) `init_amps`: `t1=0`, `t2=(ia|jb)/Dijab` (copy the mp2.rs:265-273 block), compute `emp2`; the `e_hf + emp2` print mirrors upstream. (4) `pool.reserve` the `vvvv`/`Wabef` buffer ONCE here, before the loop. (5) iterate `0..max_cycle`: `(t1new,t2new) = hooks.update_amps(t1,t2,eris)?`; `normt = ||amplitudes_to_vector(new) − vector(old)||`; (DIIS is a NO-OP this plan — 06-04 wires AmplitudeSubspace; for now `t1,t2 = t1new,t2new` directly); `eccsd = hooks.energy(t1,t2,eris)?`; converged when `|eccsd−eold| < conv_tol (1e-7)` AND `normt < conv_tol_normt (1e-5)`. (6) `pool.release` the vvvv buffer. Return `CcsdResult { e_corr, converged, t1, t2, emp2, niter }`.

Implement `default_energy` (port `ccsd.py:710`): `e = 2*einsum('ijab,iajb', t2+tau, eris_ovov) − einsum('jiab,iajb', ...)` as host-loop materialize-then-`oracle_sum`/`oracle_dot`. Implement `default_ao2mo` (mirror `mp2::default_ao2mo` but build the FULL CCSD block set — `oooo`/`ovoo`/`oovv`/`ovov`/`ovvo`/`ovvv`/`vvvv` — from `mol.intor('int2e')` via `pyscf-ao2mo::general`/`full`; `int2e` is real since 05-08). Reuse the Phase-5 `Frozen` enum + the 5 MP2-08 helpers verbatim (`get_nocc`/`get_nmo`/`get_frozen_mask`/`get_e_hf`/`mo_without_core`) — CCSD-10's frozen contract, no re-port. Wire the verified defaults as module constants: `MAX_CYCLE=50`, `CONV_TOL=1e-7`, `CONV_TOL_NORMT=1e-5`, `DIIS_SPACE=6`, `DIIS_START_CYCLE=0`. Define `amplitudes_to_vector` here (or delegate to 06-04's packer; for normt this plan needs only the flat norm — a local pack is acceptable, 06-04 makes it the DIIS storable).

Create `crates/pyscf-ccsd/tests/rccsd_numeric_smoke.rs` (always-on, CCSD-01): build a small RHF reference (H2/STO-3G or H2O/STO-3G via the in-tree SCF path used by mp2_numeric_smoke), run `ccsd_kernel` with `NoCcsdOverrides`, assert `converged` and that `e_corr` matches an in-tree independent reference (the upstream-published small-system CCSD e_corr for that geometry/basis) to ≤1e-6 Hartree (≤1 µHartree). Mirror `crates/pyscf-mp2/tests/mp2_numeric_smoke.rs`. Create `crates/pyscf-ccsd/tests/convergence.rs` (CCSD-03): assert the dual-criterion convergence flag and that re-running under RAYON 1 vs 8 yields the same converged energy (thread-invariant).

NB the upstream byte-identity vs a live PySCF (caffeine and small) is the `workflow_dispatch` human-verify arm (06-08) — this plan's small oracle uses an in-tree published reference value, NOT a live PySCF call (sandbox has no PySCF).
  </action>
  <verify>
    <automated>cargo test -p pyscf-ccsd --test rccsd_numeric_smoke --test convergence --locked -- --test-threads=1 2>&1 | tail -10; cargo test -p pyscf-ccsd --lib --locked 2>&1 | tail -3</automated>
  </verify>
  <done>`cargo test -p pyscf-ccsd --test rccsd_numeric_smoke -- --test-threads=1` passes: small-system in-core RCCSD `e_corr` within 1e-6 Hartree of the published reference; `convergence.rs` proves dual-criterion convergence + RAYON 1==8 energy; `ccsd_kernel` calls `try_reserve` before vvvv allocation and `reserve`s vvvv once before the loop (grep/source assertion); over-budget run returns `MemoryLimitExceeded`; the verified defaults (1e-7/1e-5/50/6) are module constants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hooks.ao2mo/update_amps → kernel | A (future) Python override could return a wrong-shaped ERI/amplitude array; the kernel must validate before indexing. |
| in-core job → memory budget | The vvvv reservation crosses the PYSCF_MAX_MEMORY ceiling (the CCSD-11 hazard). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-SHAPE | Denial of Service / Tampering | ChemistsEris block lengths + hook returns | mitigate | Validate every block length against expected `nocc*nvir*...` before indexing → `ShapeMismatch` `?`-propagation (mp2.rs:191-205); `#![forbid(unsafe_code)]` → no UB on logic error. |
| T-06-03-OOM | Denial of Service | ccsd_kernel pre-flight | mitigate | `pool.try_reserve(estimate)?` HARD-refuses over-budget in-core jobs before any vvvv allocation (D-01); no silent downgrade. |
| T-06-03-FP | Tampering (result integrity) | every contraction / energy reduction | mitigate | materialize-then-`oracle_sum`/`oracle_dot` (no `+=`, no gemm); RAYON 1==8 byte-identity test; `check-no-fma` (pyscf-ccsd now scanned, 06-01). |

No network/auth/session/crypto surface — pure internal contraction.
</threat_model>

<verification>
- `cargo test -p pyscf-ccsd --test rccsd_numeric_smoke -- --test-threads=1` passes (≤1 µHartree).
- `cargo test -p pyscf-ccsd --test convergence` passes (dual criterion + thread-invariant).
- `cargo test -p pyscf-ccsd --lib` passes (rintermediates/update_amps/energy unit tests).
- Per-wave: `cargo test --workspace --locked` + the release-oracle determinism arms (rayon-1 vs rayon-8) stay green.
- grep confirms no `gemm` call and no bare `+=` in the contraction modules.
</verification>

<success_criteria>
- In-core RCCSD converges and matches the small-system reference to ≤1 µHartree (CCSD-01) — the un-gated headline.
- Dual-criterion convergence (|dE|<1e-7 AND normt<1e-5) within max_cycle=50 (CCSD-03).
- The vvvv/Wabef arena tenant is reserved ONCE before the loop; the pre-flight refuses over-budget (CCSD-11 under real math).
- All contractions are host-loop oracle reductions (bit-exact, thread-invariant); verified upstream defaults used.
</success_criteria>

<output>
Create `.planning/phases/06-ccsd/06-03-SUMMARY.md` when done.
</output>
