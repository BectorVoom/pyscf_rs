---
phase: 06-ccsd
plan: 09
type: execute
wave: 6
depends_on: ["06-03"]
files_modified:
  - crates/pyscf-ccsd/src/dfccsd.rs
  - crates/pyscf-ao2mo/src/outcore.rs
  - crates/pyscf-ao2mo/src/lib.rs
  - crates/pyscf-ccsd/tests/dfccsd_spill.rs
autonomous: true
requirements: [CCSD-08]

must_haves:
  truths:
    - "DF-CCSD reuses the in-core ccsd_kernel and swaps the ERI source to the pyscf-df B-tensor (vvL) via the ao2mo hook (the DFRMP2(RMP2) subclass pattern)"
    - "when PYSCF_MAX_MEMORY is exceeded, the Wabef/vvL intermediate spills to an HDF5 temp file via the pyscf-chkfile hdf5 alias (D-07, no new hdf5-metno dep)"
    - "the spill temp file is deleted on drop (RAII); no leftover scratch after the run"
    - "DF-CCSD converges to a DF-accuracy-consistent e_corr on the small system"
  artifacts:
    - path: "crates/pyscf-ccsd/src/dfccsd.rs"
      provides: "DFRCCSD/DFUCCSD ERI swap + dmax/vvblk block sizing + HDF5 spill (port dfccsd.py)"
      contains: "vvL"
    - path: "crates/pyscf-ao2mo/src/outcore.rs"
      provides: "the outcore/semi-incore HDF5-spilling AO->MO surface (Phase-5 D-04 deferral, added here)"
      contains: "outcore"
    - path: "crates/pyscf-ccsd/tests/dfccsd_spill.rs"
      provides: "DF-CCSD spill + no-leftover-scratch (CCSD-08, small always-on)"
      contains: "spill"
  key_links:
    - from: "crates/pyscf-ccsd/src/dfccsd.rs"
      to: "pyscf_df::{cholesky_eri, default_ri}"
      via: "DF B-tensor (vvL) source — un-gated since 05-09"
      pattern: "cholesky_eri|default_ri"
    - from: "crates/pyscf-ccsd/src/dfccsd.rs"
      to: "pyscf_chkfile::hdf5 + pyscf_runtime::WorkspacePool Spilled backend"
      via: "Wabef/vvL HDF5 spill, RAII drop-delete (D-07)"
      pattern: "hdf5|Spill"
---

<objective>
Port DF-CCSD with HDF5 spill (CCSD-08, D-05/D-07/D-08): `DFRCCSD`/`DFUCCSD` subclass the in-core `ccsd_kernel` and swap the ERI/`_add_vvvv` source to the `pyscf-df` B-tensor (`vvL`) via the `CcsdOverrideHooks::ao2mo` mechanism (the exact Phase-5 `DFRMP2(RMP2)` subclass-swaps-ERI pattern). When `PYSCF_MAX_MEMORY` is exceeded, the `Wabef`/`vvL` intermediate spills to an HDF5 temp file through the `pyscf-chkfile` `hdf5` alias (D-07 — NO new `hdf5-metno` dep) via the 06-02 `WorkspacePool` `Spilled` backend; the temp file is RAII drop-deleted (no leftover scratch). Add the outcore/semi-incore HDF5-spilling AO→MO surface to `pyscf-ao2mo` (the Phase-5 D-04 deferral, landing here). DF numeric is un-gated (cintx#11 closed 05-08/09) — only memory-bounded.

Purpose: Land CCSD-08 (DF-CCSD bounded memory + spill). The second user opt-in the D-01 refusal points to; proves the arena's `Spilled` backend end-to-end.
Output: A converging DF-CCSD; a small spill test proving the HDF5 spill file is created+deleted with no leftover scratch; the benzene-dimer constrained-budget spill proof is the 06-08-closeout human-verify arm.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ccsd/06-CONTEXT.md
@.planning/phases/06-ccsd/06-RESEARCH.md
@.planning/phases/06-ccsd/06-PATTERNS.md
@.planning/phases/06-ccsd/06-VALIDATION.md

<interfaces>
<!-- From the codebase + verified upstream. -->

From crate::ccsd (06-03): ccsd_kernel<H: CcsdOverrideHooks> — DF-CCSD impls a DfCcsdHooks whose ao2mo builds blocks from the DF B-tensor.
From crate::hooks (06-01): CcsdOverrideHooks::ao2mo — the swap point (DFRMP2(RMP2) pattern).
From crates/pyscf-mp2/src/dfmp2.rs: DFRMP2 + df_ao2mo — the Phase-5 DF-MP2 subclass template (assemble (ia|jb)=sum_Q B^Q_ia*B^Q_jb via oracle_dot over Q).
From crates/pyscf-py/src/mp.rs:111-114: build_df_integrals (default_ri + cholesky_eri B-tensor assembly).
From crates/pyscf-df/src/lib.rs: DfIntegrals/cholesky_eri/df_metric_fit(robust since 05-09)/default_ri.
From crates/pyscf-runtime/src/workspace_pool.rs (06-02): the Spilled TensorBackend + SpillHandle (RAII drop-delete).
From crates/pyscf-chkfile/src/lib.rs:37: pub use hdf5_metno as hdf5 (Group::new_dataset chunked — D-07, no new dep).

VERIFIED upstream block sizing (dfccsd.py:93-96):
  dmax  = int(min((nvira+3)//4, max(BLKMIN, sqrt(max_memory*.7e6/8/nvirb**2/2))))
  vvblk = int(min((nvira+3)//4, max(BLKMIN, (max_memory*1e6/8 - dmax**2*(nvirb**2*1.5+naux))/naux/naux)))
  eris.feri = lib.H5TmpFile(); eris.vvL = feri.create_dataset('vvL',(nvir_pair,naux),'f8',chunks=...)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the outcore/semi-incore HDF5-spilling AO->MO surface to pyscf-ao2mo (Phase-5 D-04 deferral)</name>
  <files>crates/pyscf-ao2mo/src/outcore.rs, crates/pyscf-ao2mo/src/lib.rs</files>
  <read_first>
    - crates/pyscf-ao2mo/src/transform.rs (the in-core general/full quarter-transform — outcore spills the intermediate to HDF5 instead of holding it)
    - crates/pyscf-ao2mo/src/lib.rs (the module decls + re-exports to extend)
    - crates/pyscf-ao2mo/Cargo.toml (confirm pyscf-chkfile is available for the hdf5 alias; add it if absent — verify no cubecl/libxc enters the tree)
    - crates/pyscf-chkfile/src/lib.rs lines 30-40 (the hdf5 alias + Group::new_dataset chunked layout — D-07)
    - crates/pyscf-runtime/src/workspace_pool.rs (06-02 Spilled backend + SpillHandle RAII — the outcore transform reserves spillable buffers)
    - UPSTREAM pyscf/ao2mo/outcore.py + pyscf/ao2mo/semi_incore.py (the outcore/semi-incore HDF5-spilling AO->MO — the port target, Phase-5 D-04 deferred to here)
  </read_first>
  <action>
Create `crates/pyscf-ao2mo/src/outcore.rs` porting the outcore/semi-incore AO→MO transform (`pyscf/ao2mo/outcore.py` + `semi_incore.py`) — the Phase-5 D-04 deferral now landing. It mirrors the in-core `transform.rs` quarter-transform but spills the half-transformed intermediate to an HDF5 dataset (via the `pyscf_chkfile::hdf5` alias + the 06-02 `WorkspacePool` `Spilled` backend) instead of holding it fully in memory, then completes the second-half transform block-by-block. RAII drop-delete the scratch file. Every contraction host-loop materialize-then-`oracle_sum`/`oracle_dot` (no gemm, no `+=`); doc-comment flat-index offsets; ShapeMismatch-validate. Add `pub mod outcore;` + re-exports to `lib.rs`. Add `pyscf-chkfile` (+ `pyscf-runtime` if needed) to the `pyscf-ao2mo` Cargo deps; verify `cargo tree -p pyscf-ao2mo` pulls no cubecl/libxc.
  </action>
  <verify>
    <automated>cargo test -p pyscf-ao2mo outcore --locked 2>&1 | tail -8; cargo tree -p pyscf-ao2mo 2>/dev/null | grep -ci 'cubecl\|libxc' | grep -qx 0 && echo "AO2MO_TREE_CLEAN"</automated>
  </verify>
  <done>`outcore.rs` ports the spilling AO→MO transform; an always-on test proves outcore == in-core `general` to bit-close tolerance on a synthetic small input AND that the scratch file is deleted after the transform; `cargo tree -p pyscf-ao2mo` pulls no cubecl/libxc (prints AO2MO_TREE_CLEAN).</done>
</task>

<task type="auto">
  <name>Task 2: Port dfccsd.py — DFRCCSD/DFUCCSD ERI swap + dmax/vvblk block sizing + Wabef HDF5 spill + small spill test</name>
  <files>crates/pyscf-ccsd/src/dfccsd.rs, crates/pyscf-ccsd/tests/dfccsd_spill.rs</files>
  <read_first>
    - crates/pyscf-ccsd/src/dfccsd.rs (the 06-01 stub returning NotYetImplemented{wave:4})
    - crates/pyscf-ccsd/src/ccsd.rs (06-03 ccsd_kernel<H> — DF-CCSD reuses it with a DfCcsdHooks ao2mo swap)
    - crates/pyscf-ccsd/src/hooks.rs (06-01 CcsdOverrideHooks — the ao2mo swap point)
    - crates/pyscf-mp2/src/dfmp2.rs (DFRMP2 + df_ao2mo — the Phase-5 subclass-swaps-ERI template: (ia|jb)=sum_Q B^Q_ia*B^Q_jb via oracle_dot over Q)
    - crates/pyscf-py/src/mp.rs lines 111-114 (build_df_integrals — default_ri + cholesky_eri)
    - crates/pyscf-df/src/lib.rs (DfIntegrals/cholesky_eri/default_ri — un-gated since 05-09)
    - crates/pyscf-runtime/src/workspace_pool.rs (06-02 Spilled backend + try_reserve — the spill trigger + RAII)
    - crates/pyscf-ao2mo/src/outcore.rs (Task-1 — the outcore transform DF-CCSD's ovvv/vvvv uses when over budget)
    - UPSTREAM pyscf/cc/dfccsd.py lines 70-194 (RCCSD(ccsd.CCSD) DF subclass, _add_vvvv DF branch, dmax/vvblk block sizing @93-96, lib.H5TmpFile spill @139) + pyscf/cc/dfuccsd.py
    - 06-VALIDATION.md row CCSD-08 (the `cargo test -p pyscf-ccsd dfccsd_spill` command)
  </read_first>
  <action>
Port `dfccsd.py` into `dfccsd.rs`: `DFRCCSD` (and `DFUCCSD`) reuse the in-core `ccsd_kernel` and impl a `DfCcsdHooks: CcsdOverrideHooks` whose `ao2mo` builds the CCSD ERI blocks from the `pyscf-df` B-tensor (`vvL`) — assemble blocks as `sum_Q B^Q*B^Q` via `oracle_dot` over the Q axis (the `df_ao2mo` MATH from dfmp2.rs). The B-tensor comes from `pyscf_df::{cholesky_eri, default_ri}` (aux default = `default_ri`, the mp2fit *-ri — NOT jkfit; un-gated since 05-09). Port the `dmax`/`vvblk` block sizing (`dfccsd.py:93-96`, the exact verified formulas) — these become `WorkspacePool` reservation sizes (D-08). When the `Wabef`/`vvL` reservation exceeds `PYSCF_MAX_MEMORY`, route through the `Spilled` backend (HDF5 temp dataset via the `pyscf_chkfile::hdf5` alias + the Task-1 outcore surface) — the `lib.H5TmpFile()`-equivalent (`dfccsd.py:139`); RAII drop-delete the spill file (no leftover scratch). EVERY reduction host-loop `oracle_dot`/`oracle_sum` (no gemm, no `+=`). The int3c2e_sph DF gate is closed (05-08/09); `?`-propagate any shape surprise (never panic/zero-substitute — the T-05-05-FFI lesson). NO new `hdf5-metno` dep (D-07); NO pyo3.

Create `crates/pyscf-ccsd/tests/dfccsd_spill.rs` (CCSD-08, small always-on): (1) a synthetic-B-tensor DFRCCSD `e_corr` matches a longhand `sum_Q B^Q*B^Q` reference (the DF ERI assembly correctness, the dfmp2 test pattern); (2) the small-system DF-CCSD converges to a DF-accuracy-consistent `e_corr` (close to in-core RCCSD within DF fitting error, the 05-09 DF-MP2-vs-in-core pattern); (3) the SPILL proof: run with a deliberately tiny `PYSCF_MAX_MEMORY` so the `vvL`/`Wabef` reservation forces the `Spilled` backend, assert an HDF5 scratch file is created during the run AND deleted afterward (glob the temp dir before/after — no leftover scratch, the Runtime State Inventory requirement). The benzene-dimer/cc-pVDZ constrained-budget spill proof is the 06-08-closeout `workflow_dispatch` human-verify arm (heavy/upstream — never always-on, the user-memory constraint).
  </action>
  <verify>
    <automated>cargo test -p pyscf-ccsd --test dfccsd_spill --locked -- --test-threads=1 2>&1 | tail -12; grep -q 'vvL' crates/pyscf-ccsd/src/dfccsd.rs && grep -qi 'hdf5\|spill' crates/pyscf-ccsd/src/dfccsd.rs && echo "DF_SPILL_WIRED"</automated>
  </verify>
  <done>`dfccsd.rs` ports DFRCCSD/DFUCCSD (ERI swap via the ao2mo hook + dmax/vvblk sizing + HDF5 spill, prints DF_SPILL_WIRED); the synthetic-B reference + DF-accuracy convergence + the spill-file-created-and-deleted tests pass; no leftover scratch; no new hdf5-metno dep (D-07); `cargo test -p pyscf-ccsd --test dfccsd_spill` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DF B-tensor blocks → contraction | wrong-shaped vvL/aux blocks must be validated before indexing. |
| spill backend → filesystem | the HDF5 spill temp file (predictable path / leak hazard). |
| DF-CCSD job → memory budget | the vvL/Wabef reservation crosses PYSCF_MAX_MEMORY (the spill trigger). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09-LEAK | Information Disclosure | SpillHandle / HDF5 temp file | mitigate | RAII `Drop` deletes the spill file (mirror `lib.H5TmpFile()`); `pyscf-chkfile` secure-temp-path convention; the dfccsd_spill test asserts no leftover scratch. |
| T-06-09-SHAPE | DoS / Tampering | DF B-tensor / aux block lengths | mitigate | ShapeMismatch `?`-propagation before indexing; never panic/zero-substitute (T-05-05-FFI); `#![forbid(unsafe_code)]`. |
| T-06-09-OOM | DoS | DF-CCSD pre-flight / spill trigger | mitigate | `try_reserve` HARD refusal for over-budget in-core; DF spills to HDF5 (the explicit opt-in) rather than OOMing (D-01/D-07). |
| T-06-09-SC | Tampering | cargo deps (pyscf-ao2mo gains pyscf-chkfile) | accept | No registry install; `cargo tree` gate proves only the existing (Phase-3/4-vetted) hdf5-metno via pyscf-chkfile enters — no cubecl/libxc. |
| T-06-09-FP | Tampering (integrity) | DF ERI assembly reductions | mitigate | host-loop `oracle_dot` over Q; RAYON 1==8; check-no-fma scans pyscf-ccsd. |
</threat_model>

<verification>
- `cargo test -p pyscf-ccsd --test dfccsd_spill -- --test-threads=1` passes (synthetic-B reference + DF convergence + spill created/deleted).
- `cargo test -p pyscf-ao2mo outcore` passes (outcore == in-core; scratch deleted).
- `cargo tree -p pyscf-ccsd`/`-p pyscf-ao2mo` pull no new hdf5-metno owner beyond pyscf-chkfile, no cubecl/libxc.
- Per-wave: `cargo test --workspace --locked` + release-oracle determinism arms green.
- The benzene-dimer constrained-budget spill proof is the 06-08-closeout workflow_dispatch arm.
</verification>

<success_criteria>
- DF-CCSD works (CCSD-08): reuses the in-core kernel, swaps ERIs to the DF B-tensor via the ao2mo hook (DFRMP2(RMP2) pattern), with the verified dmax/vvblk block sizing as arena reservations.
- Over-budget Wabef/vvL spills to an HDF5 temp file (the Spilled backend) and the file is RAII drop-deleted — no leftover scratch.
- The outcore/semi-incore AO→MO surface (Phase-5 D-04 deferral) lands in pyscf-ao2mo.
- No new hdf5-metno dep (D-07); all reductions oracle-based, thread-invariant.
</success_criteria>

<output>
Create `.planning/phases/06-ccsd/06-09-SUMMARY.md` when done.
</output>
