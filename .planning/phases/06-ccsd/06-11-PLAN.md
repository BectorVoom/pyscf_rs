---
phase: 06-ccsd
plan: 11
type: execute
wave: 8
depends_on: ["06-03", "06-04", "06-05", "06-06", "06-07", "06-08", "06-09", "06-10"]
files_modified:
  - crates/pyscf-oracle/tests/ccsd_oracle.rs
  - .github/workflows/ci.yml
autonomous: true
requirements: [CCSD-01, CCSD-02, CCSD-05, CCSD-06, CCSD-08, CCSD-11]

must_haves:
  truths:
    - "a ccsd-structural always-on CI job runs the small-system pyscf-ccsd arms on every push (no --features python, no caffeine, no libxc)"
    - "a ccsd-oracle-upstream-manual workflow_dispatch arm runs caffeine byte-identity + DF-CCSD spill + lambda/RDM byte-identity against live PySCF (human-verify only)"
    - "a python3.13t CCSD smoke job (clone of the SCF smoke) gates Pitfall 6 (the heaviest GIL re-validation)"
    - "a heap-alloc-count CI gate runs the dedicated counting-allocator target (CCSD-11)"
    - "no heavy/upstream/libxc-pulling test is ever always-on (user-memory constraint)"
  artifacts:
    - path: "crates/pyscf-oracle/tests/ccsd_oracle.rs"
      provides: "CCSD energy/lambda/RDM fixtures (small always-on; caffeine/DF-spill behind --features python + workflow_dispatch)"
      contains: "ccsd"
    - path: ".github/workflows/ci.yml"
      provides: "ccsd-structural (always-on) + ccsd-oracle-upstream-manual (workflow_dispatch) + python3.13t CCSD smoke + heap-alloc-count gate"
      contains: "ccsd-structural"
  key_links:
    - from: ".github/workflows/ci.yml ccsd-oracle-upstream-manual"
      to: "github.event_name == 'workflow_dispatch'"
      via: "the heavy/upstream arm never auto-triggers (the mp2-oracle-upstream-manual precedent)"
      pattern: "workflow_dispatch"
    - from: ".github/workflows/ci.yml heap-alloc-count gate"
      to: "crates/pyscf-ccsd/tests/heap_alloc_count.rs"
      via: "the CCSD-11 allocate-once assertion (06-02 target)"
      pattern: "heap_alloc_count"
---

<objective>
Close out CCSD validation: add the `pyscf-oracle` CCSD fixtures (small-system always-on; caffeine / DF-spill / λ/RDM byte-identity gated behind `--features python` + `workflow_dispatch`) and the four CI arms — `ccsd-structural` (always-on small, clone `mp2-structural`), `ccsd-oracle-upstream-manual` (`workflow_dispatch`, caffeine + DF-CCSD spill + λ/RDM byte-identity, clone `mp2-oracle-upstream-manual`), the `python3.13t` CCSD smoke (clone the `python3.13t SCF smoke` job — Pitfall 6, the heaviest GIL re-validation), and the heap-alloc-count gate (the dedicated counting-allocator target from 06-02, CCSD-11). Honor the user-memory constraint: small arms always-on; caffeine/DF-spill/upstream-byte-identity/3.13t are `workflow_dispatch`/human-verify ONLY — never always-on, never pull libxc.

Purpose: Land the Nyquist validation contract (06-VALIDATION.md): every CCSD requirement has an automated verify or a tracked human-verify arm; the always-on `cargo test` stays fast/green; the heavy proofs run on demand.
Output: A green always-on CCSD CI surface + the documented `workflow_dispatch` human-verify arms; the phase-gate manual run is the final human-verify step.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ccsd/06-CONTEXT.md
@.planning/phases/06-ccsd/06-RESEARCH.md
@.planning/phases/06-ccsd/06-PATTERNS.md
@.planning/phases/06-ccsd/06-VALIDATION.md

<interfaces>
<!-- The CI arms to clone. From the codebase (verified line ranges). -->

From .github/workflows/ci.yml:
  mp2-structural (416-427):                  needs: [build-default]; cargo test -p pyscf-ccsd --locked -- --test-threads=1; NO --features python
  mp2-oracle-upstream-manual (445-464):      if: github.event_name == 'workflow_dispatch'; pip install "pyscf>=2.5"; cargo test -p pyscf-oracle --features python --locked -- --test-threads=1
  python3.13t SCF smoke (274-323):           3.13t venv + maturin develop --no-default-features --features free-threading (abi3-incompatible with 3.13t) + import-assert + SCF smoke body

From crates/pyscf-oracle/: the oracle_check! macro + the existing MP2 energy fixtures (the shape CCSD fixtures reuse).
From crates/pyscf-ccsd/tests/: rccsd_numeric_smoke / uccsd_smoke / diis_amps / diagnostics / frozen / dfccsd_spill / direct / heap_alloc_count / refusal (all the always-on small arms from 06-02..06-09).

User-memory constraint: NEVER make a heavy/upstream/libxc-pulling test always-on. Heavy arms = workflow_dispatch only (the 02-10/05-08 precedent).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pyscf-oracle CCSD fixtures (small always-on; caffeine/DF-spill/lambda/RDM byte-identity behind --features python)</name>
  <files>crates/pyscf-oracle/tests/ccsd_oracle.rs</files>
  <read_first>
    - crates/pyscf-oracle/tests/ (the existing MP2 energy oracle fixtures — the oracle_check! shape + the --features python gating to mirror)
    - crates/pyscf-oracle/src/lib.rs (the oracle_check! macro + KNOWN_METHODS registry — add the CCSD method names)
    - crates/pyscf-ccsd/tests/rccsd_numeric_smoke.rs + uccsd_smoke.rs (06-03/06-04 — the small-system always-on references already shipped; the oracle fixture cross-references them)
    - 06-VALIDATION.md §"Per-Requirement Verification Map" (the exact `cargo test -p pyscf-oracle --features python -- ccsd_rccsd` command + the small-vs-gated split) + §"Manual-Only Verifications"
  </read_first>
  <action>
Create `crates/pyscf-oracle/tests/ccsd_oracle.rs` adding the CCSD correlation-energy / λ / RDM fixtures. Register the CCSD method names in the `KNOWN_METHODS` registry (mirror the Phase-5 MP2 registry extension — `ccsd_rccsd_energy`, `ccsd_uccsd_energy`, `ccsd_dfccsd_energy`, `ccsd_lambda`, `ccsd_rdm1`, `ccsd_rdm2`; update any len-assert). The SMALL-system arms (H2O/cc-pVDZ-sized) are always-on and assert vs an in-tree published reference (the 06-03/06-04 small references). The HEAVY arms — caffeine/cc-pVDZ byte-identity, DF-CCSD spill, λ/RDM byte-identity vs live PySCF — are `#[cfg(feature = "python")]`-gated (require `--features python` + an installed upstream PySCF), NEVER always-on (user-memory constraint; the 05-08 `mp2-oracle-upstream-manual` precedent). A non-python build of `pyscf-oracle` must NOT pull libxc or attempt a live PySCF call.
  </action>
  <verify>
    <automated>cargo test -p pyscf-oracle --locked 2>&1 | tail -6; cargo tree -p pyscf-oracle 2>/dev/null | grep -ci 'libxc' | grep -qx 0 && echo "ORACLE_NO_LIBXC"</automated>
  </verify>
  <done>`ccsd_oracle.rs` registers the CCSD method names; small-system arms are always-on and green without `--features python`; caffeine/DF-spill/λ/RDM byte-identity arms are `#[cfg(feature="python")]`-gated; default `cargo test -p pyscf-oracle` pulls no libxc (prints ORACLE_NO_LIBXC) and makes no live PySCF call.</done>
</task>

<task type="auto">
  <name>Task 2: Add the four CCSD CI arms (ccsd-structural always-on + ccsd-oracle-upstream-manual + python3.13t CCSD smoke + heap-alloc-count gate)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml lines 416-427 (mp2-structural — clone for ccsd-structural)
    - .github/workflows/ci.yml lines 445-464 (mp2-oracle-upstream-manual — clone for ccsd-oracle-upstream-manual; the `if: github.event_name == 'workflow_dispatch'` gate)
    - .github/workflows/ci.yml lines 274-323 (python3.13t SCF smoke — clone for the CCSD smoke; the free-threading maturin build)
    - crates/pyscf-ccsd/tests/heap_alloc_count.rs (06-02 — the dedicated counting-allocator target the heap-alloc gate runs)
    - 06-VALIDATION.md §"Wave 0 Requirements" (the four CI arms to add) + §"Manual-Only Verifications" (the workflow_dispatch / human-verify arms)
  </read_first>
  <action>
Add four CCSD CI jobs to `.github/workflows/ci.yml`:
1. `ccsd-structural` (always-on) — clone `mp2-structural` (ci.yml:416-427): `needs: [build-default]`, `cargo test -p pyscf-ccsd --locked -- --test-threads=1` (runs all the small always-on arms: rccsd_numeric_smoke, uccsd_smoke, diis_amps, diagnostics, frozen, dfccsd_spill, direct, convergence). NO `--features python`, NO caffeine, NO libxc.
2. `heap-alloc-count` (always-on) — a dedicated `cargo test -p pyscf-ccsd --test heap_alloc_count` arm (the CCSD-11 allocate-once gate; the test target has its OWN counting `#[global_allocator]`, NOT linked to the oracle/determinism binaries — RESEARCH A3). Also run `--test refusal`.
3. `ccsd-oracle-upstream-manual` (`workflow_dispatch` ONLY) — clone `mp2-oracle-upstream-manual` (ci.yml:445-464): `if: github.event_name == 'workflow_dispatch'`, `pip install "pyscf>=2.5"`, `cargo test -p pyscf-oracle --features python --locked -- --test-threads=1` covering caffeine/cc-pVDZ byte-identity + DF-CCSD spill on a deliberately constrained `PYSCF_MAX_MEMORY` (benzene-dimer) + λ/RDM byte-identity. Never auto-triggers.
4. `python3.13t CCSD smoke` (Pitfall 6, heaviest GIL re-validation) — clone the `python3.13t SCF smoke` job (ci.yml:274-323): same 3.13t venv + `maturin develop --no-default-features --features free-threading` (abi3-incompatible with 3.13t) + import-assert, with a CCSD smoke replacing the SCF body (build a small mf, run `mf.CCSD().kernel()`, assert no deadlock + finite e_corr). `workflow_dispatch`/special-runner per the existing 3.13t job's trigger.

CRITICAL: arms 3 and 4 must be `workflow_dispatch`-gated (or the existing 3.13t job's special trigger) so the default-push CI NEVER runs caffeine/upstream/3.13t/libxc (the user-memory "don't freeze the test run" + "never trigger libxc" constraint). Verify the workflow YAML parses.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml')); print('YAML_OK')" 2>&1 | tail -2; grep -c 'ccsd-structural\|ccsd-oracle-upstream-manual\|heap_alloc_count\|3.13t CCSD' .github/workflows/ci.yml</automated>
  </verify>
  <done>The four CCSD CI arms are present and the workflow YAML parses (prints YAML_OK); `ccsd-structural` + `heap-alloc-count` are always-on (no `--features python`); `ccsd-oracle-upstream-manual` + `python3.13t CCSD smoke` are `workflow_dispatch`-gated (never auto-trigger); no heavy/upstream/libxc arm runs on default push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI heavy arms → libxc/upstream | the heavy arms must never be always-on (compile-freeze / test-freeze hazard per the user-memory constraint). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11-SC | Tampering (supply-chain / build integrity) | the `pip install "pyscf>=2.5"` in the manual oracle arm | mitigate | The install runs ONLY in the `workflow_dispatch` arm (never on default push); the default `pyscf-oracle` build pulls no libxc and makes no live PySCF call (Task-1 gate). Honors the user-memory "never trigger libxc compile" constraint. |
| T-06-11-FREEZE | Denial of Service (CI/dev ergonomics) | always-on test matrix | mitigate | Caffeine/DF-spill/upstream/3.13t are `workflow_dispatch` only; only the fast small arms + the isolated heap-alloc-count target run on push (06-VALIDATION user-memory constraint). |
| T-06-11-GIL | Tampering | python3.13t CCSD smoke | mitigate | The 3.13t smoke gate confirms no GIL deadlock under the heaviest compute (Pitfall 6 re-validation); runs on the existing 3.13t special trigger, not default push. |
</threat_model>

<verification>
- `cargo test -p pyscf-oracle --locked` (no `--features python`) passes; small CCSD arms green; no libxc, no live PySCF.
- The workflow YAML parses; the four CCSD arms present with the correct always-on vs workflow_dispatch gating.
- Phase gate (human-verify, per 06-VALIDATION §"Manual-Only"): run `ccsd-oracle-upstream-manual` + the `python3.13t CCSD smoke` once manually — caffeine byte-identity, DF-CCSD spill proof on constrained PYSCF_MAX_MEMORY, λ/RDM byte-identity, no GIL deadlock.
- Set `nyquist_compliant: true` in 06-VALIDATION.md frontmatter once every requirement has an automated verify or a tracked workflow_dispatch arm.
</verification>

<success_criteria>
- Every CCSD requirement has an automated always-on verify or a tracked workflow_dispatch/human-verify arm (Nyquist contract satisfied).
- The always-on `cargo test` surface stays fast/green; caffeine/DF-spill/upstream/3.13t are workflow_dispatch only (user-memory constraint honored).
- The heap-alloc-count gate (CCSD-11) + the refusal gate run always-on; the upstream byte-identity + 3.13t GIL arms are the documented phase-gate human-verify run.
</success_criteria>

<output>
Create `.planning/phases/06-ccsd/06-11-SUMMARY.md` when done.
</output>
