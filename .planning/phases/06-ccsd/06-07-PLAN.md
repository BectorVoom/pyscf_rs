---
phase: 06-ccsd
plan: 07
type: execute
wave: 5
depends_on: ["06-03"]
files_modified:
  - crates/pyscf-ccsd/src/diagnostics.rs
  - crates/pyscf-ccsd/tests/diagnostics.rs
  - crates/pyscf-ccsd/tests/frozen.rs
autonomous: true
requirements: [CCSD-09, CCSD-10]

must_haves:
  truths:
    - "mycc.t1diagnostic() returns the Frobenius-norm T1 diagnostic; mycc.d1diagnostic() returns the D1 diagnostic (eigh of t1.t1^T)"
    - "the diagnostic values match the known definitions on a small-system converged t1/t2"
    - "frozen=int / frozen=list / frozen='auto' on a CCSD run reproduce the MP2 frozen helper behavior (CCSD-10)"
  artifacts:
    - path: "crates/pyscf-ccsd/src/diagnostics.rs"
      provides: "get_t1_diagnostic / get_d1_diagnostic / get_d2_diagnostic (port ccsd.py:748-776)"
      contains: "t1_diagnostic"
    - path: "crates/pyscf-ccsd/tests/diagnostics.rs"
      provides: "diagnostic value checks (CCSD-09)"
      contains: "d1_diagnostic"
    - path: "crates/pyscf-ccsd/tests/frozen.rs"
      provides: "frozen int/list/auto contract (CCSD-10, reuse MP2 helpers)"
      contains: "Frozen"
  key_links:
    - from: "crates/pyscf-ccsd/src/diagnostics.rs"
      to: "pyscf_algebra::eigh + oracle_sum"
      via: "D1/D2 need eigh of t1.t1^T; Frobenius via oracle reduction"
      pattern: "eigh|oracle"
    - from: "crates/pyscf-ccsd/tests/frozen.rs"
      to: "pyscf_mp2::{Frozen, get_nocc, get_frozen_mask}"
      via: "CCSD frozen reuses the MP2 helpers verbatim (CCSD-10)"
      pattern: "Frozen|get_nocc"
---

<objective>
Port the CCSD T1/D1/D2 diagnostics (CCSD-09, `ccsd.py:748-776`) — Frobenius-norm and `eigh`-of-`t1·t1ᵀ` based — exposing `t1diagnostic()` / `d1diagnostic()`. Validate the frozen-core contract (CCSD-10) end-to-end: `frozen=int`/`frozen=list`/`frozen='auto'` on a CCSD run reproduce the Phase-5 MP2 frozen-helper behavior (already contract-tested vs `cc/ccsd.py:35` in Phase 5 — CCSD imports the 5 MP2-08 helpers + the `Frozen` enum verbatim, no new frozen logic).

Purpose: Land CCSD-09 (diagnostics) and CCSD-10 (frozen-core matches MP2). Both are mechanical: diagnostics are Frobenius/eigh on the converged amplitudes; frozen is pure reuse with a contract test.
Output: `t1diagnostic`/`d1diagnostic` matching the known definitions; frozen int/list/auto green.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ccsd/06-CONTEXT.md
@.planning/phases/06-ccsd/06-RESEARCH.md
@.planning/phases/06-ccsd/06-PATTERNS.md
@.planning/phases/06-ccsd/06-VALIDATION.md

<interfaces>
<!-- From the codebase + verified upstream. -->

From crate::ccsd (06-03): ccsd_kernel produces CcsdResult { t1, t2, ... } the diagnostics consume.
From pyscf_algebra: eigh (symmetric eigendecomp — D1/D2 need eigh of t1.t1^T), oracle_sum/oracle_dot (Frobenius).
From pyscf_mp2 (re-exported pyscf-mp2/src/lib.rs:36-37): Frozen enum + get_nocc/get_nmo/get_frozen_mask/get_e_hf/mo_without_core — the EXACT CCSD import contract (cc/ccsd.py:35), already contract-tested in Phase 5.

VERIFIED upstream: get_t1_diagnostic (Frobenius norm of t1 / sqrt(nelec)), get_d1_diagnostic / get_d2_diagnostic (eigh-based) — ccsd.py:748-776.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port get_t1_diagnostic / get_d1_diagnostic / get_d2_diagnostic (ccsd.py:748-776)</name>
  <files>crates/pyscf-ccsd/src/diagnostics.rs, crates/pyscf-ccsd/tests/diagnostics.rs</files>
  <behavior>
    - get_t1_diagnostic(t1, nelec) == ||t1||_F / sqrt(nelec) on a synthetic t1 vs a hand-computed reference.
    - get_d1_diagnostic(t1) == sqrt(max eigenvalue of t1.t1^T) (the D1 definition) on a synthetic t1 vs a hand-computed eigh reference.
    - get_d2_diagnostic(t2) matches its definition on a synthetic t2.
    - the Frobenius reductions are oracle_sum-based (thread-invariant).
  </behavior>
  <read_first>
    - crates/pyscf-ccsd/src/diagnostics.rs (the 06-01 stub returning NotYetImplemented{wave:3})
    - crates/pyscf-ccsd/src/ccsd.rs (06-03 CcsdResult — the t1/t2 the diagnostics consume)
    - crates/pyscf-algebra/src/ (the eigh signature + oracle_sum/oracle_dot — grep for `pub fn eigh`)
    - crates/pyscf-mp2/src/mp2.rs (the Frobenius/oracle-reduction shape — the diagnostics mirror it)
    - UPSTREAM pyscf/cc/ccsd.py lines 748-776 (get_t1_diagnostic / get_d1_diagnostic / get_d2_diagnostic — Lee-Taylor T1, Janssen-Nielsen D1/D2 definitions)
    - 06-VALIDATION.md row CCSD-09 (the `cargo test -p pyscf-ccsd diagnostics` command)
  </read_first>
  <action>
Port `ccsd.py:748-776` into `diagnostics.rs`: `get_t1_diagnostic(t1, nelec) -> f64` (the Lee-Taylor T1: `||t1||_F / sqrt(nelec)`, Frobenius via `oracle_sum` of squares then sqrt), `get_d1_diagnostic(t1) -> f64` (Janssen-Nielsen D1: largest singular value of t1 = sqrt(max eigenvalue of `t1·t1ᵀ`) via `pyscf_algebra::eigh`), `get_d2_diagnostic(t2) -> f64` (the D2 definition via eigh of the appropriate t2 contraction). Build `t1·t1ᵀ` as a host-loop materialize-then-`oracle_sum` (no gemm/`+=`), then `eigh`. Mechanical — no arena needed (diagnostics are small). ShapeMismatch-validate.

RED→GREEN: write the `<behavior>` tests (synthetic t1/t2 + hand-computed Frobenius/eigh references) against the stub, then implement.
  </action>
  <verify>
    <automated>cargo test -p pyscf-ccsd --test diagnostics --locked 2>&1 | tail -8; cargo test -p pyscf-ccsd --lib diagnostics --locked 2>&1 | tail -3</automated>
  </verify>
  <done>`get_t1_diagnostic`/`get_d1_diagnostic`/`get_d2_diagnostic` ported and matching the hand-computed references; Frobenius via `oracle_sum`, D1/D2 via `eigh`; `cargo test -p pyscf-ccsd --test diagnostics` green.</done>
</task>

<task type="auto">
  <name>Task 2: Frozen-core contract test (CCSD-10) — reuse MP2 Frozen + the 5 MP2-08 helpers verbatim</name>
  <files>crates/pyscf-ccsd/tests/frozen.rs</files>
  <read_first>
    - crates/pyscf-ccsd/src/ccsd.rs (06-03 — confirm ccsd_kernel/default_ao2mo already consume the MP2 Frozen enum + helpers per CCSD-10; this plan adds the validating test, the wiring landed in 06-03)
    - crates/pyscf-mp2/src/frozen.rs (the Frozen enum: None/Count/List/Auto/Window)
    - crates/pyscf-mp2/src/helpers.rs (get_nocc/get_nmo/get_frozen_mask/get_e_hf/mo_without_core — the contract cc/ccsd.py:35 imports; already contract-tested in Phase 5's ccsd_import_contract arm)
    - crates/pyscf-mp2/tests/ (the MP2 frozen fixtures, if present — mirror the expected nocc/nvir/mask values)
    - 06-VALIDATION.md row CCSD-10 (the `cargo test -p pyscf-ccsd frozen` command)
  </read_first>
  <action>
Create `crates/pyscf-ccsd/tests/frozen.rs` validating CCSD-10: assert that running `ccsd_kernel` (or its `default_ao2mo` frozen-subset build) with `Frozen::Count(n)`, `Frozen::List(vec)`, and `Frozen::Auto` produces the SAME active-space `nocc`/`nvir` and frozen mask as the Phase-5 MP2 helpers on the same reference — i.e. CCSD's frozen behavior IS the MP2 helper behavior (no new frozen logic, D-Discretion). Concretely: build a small reference, call `pyscf_mp2::{get_nocc, get_nmo, get_frozen_mask, mo_without_core}` directly and confirm `ccsd_kernel`'s frozen-aware active space matches; assert a frozen-core CCSD e_corr differs from the all-electron e_corr in the expected direction (frozen raises e_corr toward zero). If 06-03 did not already route frozen through the kernel, this plan adds the minimal wiring (the kernel already takes `frozen: &Frozen`).

This is the only non-tdd task (a contract/validation test over already-shipped helpers — the MP2-08 helpers are the source of truth, contract-tested in Phase 5).
  </action>
  <verify>
    <automated>cargo test -p pyscf-ccsd --test frozen --locked 2>&1 | tail -8</automated>
  </verify>
  <done>`crates/pyscf-ccsd/tests/frozen.rs` proves CCSD frozen int/list/auto produces the same active space as the MP2 helpers (CCSD-10) and a frozen-core CCSD e_corr behaves correctly; `cargo test -p pyscf-ccsd --test frozen` green; no new frozen logic added (helpers reused verbatim).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure internal diagnostics + a frozen-contract test over already-shipped helpers; no Python boundary, no untrusted input, no file I/O. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-FP | Tampering (integrity) | Frobenius/eigh reductions | mitigate | `oracle_sum` Frobenius + `pyscf_algebra::eigh`; thread-invariant; check-no-fma scans pyscf-ccsd. |
| T-06-07-SHAPE | Tampering | t1/t2 diagnostic inputs | mitigate | ShapeMismatch validation; `#![forbid(unsafe_code)]`. |

No new threat surface — pure internal reduction; the only risk is non-deterministic reduction (mitigated by oracle_sum).
</threat_model>

<verification>
- `cargo test -p pyscf-ccsd --test diagnostics` passes (T1/D1/D2 values).
- `cargo test -p pyscf-ccsd --test frozen` passes (CCSD-10 frozen contract).
- Per-wave: `cargo test --workspace --locked` green.
- Diagnostic byte-identity vs upstream is the 06-08 workflow_dispatch human-verify arm.
</verification>

<success_criteria>
- t1diagnostic/d1diagnostic (and d2) match the known definitions (CCSD-09).
- Frozen int/list/auto reproduce the MP2 helper behavior (CCSD-10) — no new frozen logic.
- All reductions oracle-based, thread-invariant.
</success_criteria>

<output>
Create `.planning/phases/06-ccsd/06-07-SUMMARY.md` when done.
</output>
