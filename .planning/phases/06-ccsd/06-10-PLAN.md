---
phase: 06-ccsd
plan: 10
type: execute
wave: 7
depends_on: ["06-03", "06-04", "06-05", "06-06", "06-07", "06-08", "06-09"]
files_modified:
  - crates/pyscf-py/src/cc.rs
  - crates/pyscf-py/src/lib.rs
  - python/pyscf/cc/__init__.py
  - crates/pyscf-py/tests/cc_bridge.rs
autonomous: true
requirements: [CCSD-01, CCSD-02, CCSD-05, CCSD-06, CCSD-08]

must_haves:
  truths:
    - "cc.RCCSD(mf).kernel() / cc.UCCSD(uhf_mf).kernel() / mf.CCSD().run() / mf.density_fit().CCSD() dispatch correctly through the PyO3 bridge"
    - "a Python subclass override of ao2mo/update_amps/make_rdm1/make_rdm2/energy dispatches via slf.call_method1 (Pitfall 7); the no-override default runs pure-Rust under py.detach (BIND-05)"
    - "the kernel does NOT py.detach at the top (hooks re-enter Python); each hook's default path detaches"
    - "mycc.solve_lambda() / make_rdm1() / make_rdm2() / as_scanner() are exposed"
    - "factory dispatch matches cc/__init__.py: RHF->RCCSD, UHF->UCCSD, with_df->DFCCSD"
  artifacts:
    - path: "crates/pyscf-py/src/cc.rs"
      provides: "PyRCCSD/PyUCCSD/PyDFCCSD + CcsdPyBridge + ccsd_factory + PyCcsdScanner + solve_lambda/make_rdm methods"
      contains: "CcsdPyBridge"
    - path: "python/pyscf/cc/__init__.py"
      provides: "cc submodule overlay + mf.CCSD()/mf.density_fit().CCSD() dispatch"
      contains: "RCCSD"
    - path: "crates/pyscf-py/tests/cc_bridge.rs"
      provides: "structural dispatch + override-detect + scanner-closure (always-on)"
      contains: "RCCSD"
  key_links:
    - from: "crates/pyscf-py/src/cc.rs"
      to: "pyscf_ccsd::{ccsd_kernel, uccsd_kernel, CcsdOverrideHooks, solve_lambda, make_rdm1, make_rdm2}"
      via: "the bridge calls the pyo3-free kernels"
      pattern: "ccsd_kernel|CcsdOverrideHooks"
    - from: "crates/pyscf-py/src/cc.rs"
      to: "Python::detach"
      via: "hook default path detaches; kernel does NOT (BIND-05, Pitfall 6 heaviest)"
      pattern: "detach"
---

<objective>
Build the PyO3 bridge for CCSD (D-09): `pyscf-py::cc` submodule with `PyRCCSD`/`PyUCCSD`/`PyDFCCSD` + `CcsdPyBridge` (impl `CcsdOverrideHooks`) + `ccsd_factory` + `PyCcsdScanner`, plus the `python/pyscf/cc/__init__.py` overlay wiring `mf.CCSD()` / `mf.density_fit().CCSD()` dispatch. Copy `pyscf-py::mp` section-for-section (the second-closest one-to-one analog after `hooks.rs`): eager-snapshot the Python `mf` into a plain-array `CcsdReference`, `is_overridden` `__qualname__` base-class dispatch for the D-09 hook set (`ao2mo`/`update_amps`/`make_rdm1`/`make_rdm2`/`energy`), the kernel does NOT `py.detach` at the top (hooks re-enter Python) while each hook's pure-Rust DEFAULT path wraps compute in `Python::attach(|py| py.detach(|| ...))`. Expose `solve_lambda`/`make_rdm1`/`make_rdm2`/`as_scanner`. `pyscf-ccsd` stays pyo3-free — the bridge is the ONLY pyo3 layer.

Purpose: Surface the drop-in `cc.RCCSD(mf).kernel()` / `mf.CCSD().run()` API (CCSD-01/02 surface, CCSD-05/06 methods, CCSD-08 DF factory). Phase 6 is the HEAVIEST `python3.13t` GIL re-validation (ROADMAP cross-cutting) — the `update_amps` default is the biggest `py.detach` region in the project; the 3.13t smoke gate is the 06-11 CI arm.
Output: A compiling `pyscf-py` cdylib exposing the cc surface; always-on structural dispatch/override/scanner tests green.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ccsd/06-CONTEXT.md
@.planning/phases/06-ccsd/06-RESEARCH.md
@.planning/phases/06-ccsd/06-PATTERNS.md
@.planning/phases/06-ccsd/06-VALIDATION.md

<interfaces>
<!-- The mp.rs section map to copy. From the codebase (verified line ranges). -->

From crates/pyscf-py/src/mp.rs (copy section-for-section, rename Mp2->Ccsd / RMP2->RCCSD):
  register (47-55):   m.add_class::<PyRMP2>() + PyUMP2 + PyDFMP2 + PyMp2Scanner + add_function(mp2_factory)
  snapshot_reference (73-106): pull mf.mol/mo_coeff(F-order)/mo_energy/mo_occ/e_tot(->e_hf)/converged into Mp2Reference
  build_df_integrals (111-114): default_ri + cholesky_eri for the DF class
  is_overridden (130-150): __qualname__ base-class comparison (Pitfall 7 MRO)
  Mp2PyBridge (157-212): hold Py<PyAny> slf + base_classes + default_source; each hook is_overridden? call_method1 : py.detach(default)
  kernel (346-375): "NB we DO NOT py.detach here — hooks re-enter Python" (the load-bearing comment, mp.rs:359)
  as_scanner / PyMp2Scanner (425-439, 772-840): Mole->energy callable that re-runs mf.as_scanner()(mol), re-snapshots, re-runs kernel
  mf_is_uhf / mf_has_df / mp2_factory (849-903): UHF->PyUMP2, with_df->PyDFMP2, else PyRMP2

From pyscf_ccsd (the pyo3-free kernels the bridge calls): ccsd_kernel<H>, uccsd_kernel<H>, CcsdOverrideHooks, NoCcsdOverrides, CcsdReference, solve_lambda, make_rdm1, make_rdm2.

From crates/pyscf-py/src/lib.rs: pub mod mp; (add pub mod cc;) + the _native #[pymodule] (add the cc submodule registration mirroring mp).

VERIFIED upstream factory (cc/__init__.py:83-139): RCCSD resolves to dfccsd.RCCSD if with_df else ccsd.CCSD; UCCSD for UHF. Dispatch order: UHF->UCCSD, with_df->DFCCSD, else RCCSD.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pyscf-py::cc submodule — PyRCCSD/PyUCCSD/PyDFCCSD + CcsdPyBridge + factory + scanner + solve_lambda/make_rdm</name>
  <files>crates/pyscf-py/src/cc.rs, crates/pyscf-py/src/lib.rs</files>
  <read_first>
    - crates/pyscf-py/src/mp.rs (the WHOLE file — copy section-for-section per the interface map above; the load-bearing detach comments at mp.rs:24-25,121,206,359)
    - crates/pyscf-py/src/lib.rs (the pub mod + _native #[pymodule] registration to extend with cc)
    - crates/pyscf-ccsd/src/lib.rs (the re-exported kernels/hooks/types the bridge calls — from 06-01..06-09)
    - crates/pyscf-ccsd/src/hooks.rs (the CcsdOverrideHooks hook set: ao2mo/update_amps/make_rdm1/make_rdm2/energy — the bridge dispatches each)
    - crates/pyscf-ccsd/src/dfccsd.rs (06-09 — PyDFCCSD routes the kernel through the DF ao2mo source)
    - crates/pyscf-ccsd/src/lambda.rs + rdm.rs (06-06 — solve_lambda/make_rdm1/make_rdm2 the Py methods expose)
    - crates/pyscf-runtime/src/workspace_pool.rs (WorkspacePool::from_env — the bridge builds the pool from PYSCF_MAX_MEMORY and passes it to the kernel)
    - UPSTREAM pyscf/cc/__init__.py lines 83-139 (the factory dispatch order)
  </read_first>
  <action>
Create `crates/pyscf-py/src/cc.rs` copying `pyscf-py/src/mp.rs` section-for-section (rename Mp2→Ccsd, RMP2→RCCSD, etc.):
- `register`: add `PyRCCSD`/`PyUCCSD`/`PyDFCCSD`/`PyCcsdScanner` classes + `ccsd_factory` (`#[pyfunction(name="CCSD")]`).
- `snapshot_reference`: pull `mf.mol`/`mo_coeff`(F-order, sign-canonicalized)/`mo_energy`/`mo_occ`/`e_tot`(→`e_hf`)/`converged` into a plain-array `CcsdReference` (verbatim the mp.rs:73-106 snapshot — D-09 eager snapshot).
- `is_overridden` (mp.rs:130-150 verbatim): `__qualname__` base-class comparison; pass `&["RCCSD"]`/`&["UCCSD"]`/`&["DFCCSD"]`.
- `CcsdPyBridge: CcsdOverrideHooks` (mirror `Mp2PyBridge`): hold `Py<PyAny> slf` + `base_classes` + default source; for EACH of the 5 hooks (`ao2mo`/`update_amps`/`make_rdm1`/`make_rdm2`/`energy`), `is_overridden`? → `slf.call_method1(py, "<hook>", args)` (the override, re-enters Python under the GIL) : → `Python::attach(|py| py.detach(|| <pure-Rust default>))` (BIND-05). The `update_amps` default is the biggest `py.detach` region in the project (the per-iteration compute) — copy the mp.rs:204-209 discipline EXACTLY.
- `PyRCCSD`/`PyUCCSD`/`PyDFCCSD` `kernel`: build `WorkspacePool::from_env()`, snapshot config, build the `CcsdPyBridge`, call `ccsd_kernel`/`uccsd_kernel`/(DF: `ccsd_kernel` with the DF ao2mo source) — **the kernel does NOT `py.detach` at the top** (hooks re-enter Python; copy the load-bearing mp.rs:359 comment). `e_corr`/`e_tot` getters. PyDFCCSD builds its B-tensor via `build_df_integrals` (default_ri + cholesky_eri, mp.rs:111-114).
- `solve_lambda`/`make_rdm1`/`make_rdm2` methods (route through the bridge `rdm_via_bridge` shape, mp.rs:390-415; `make_rdm2` accepts `ao_repr` per D-03/06-06).
- `as_scanner`/`PyCcsdScanner` (mp.rs:425-439,772-840): the Mole→energy callable that re-runs `mf.as_scanner()(mol)` → re-snapshot → re-run the kernel (Phase-7 geomopt seam, CCSD analog of SCF-12/MP2-07). A `CcsdKind { Restricted, Unrestricted, DensityFitted }` enum (mp.rs:766-770 `Mp2Kind` analog).
- `ccsd_factory` (mp.rs:888-903): `mf_is_uhf`→PyUCCSD, `mf_has_df`→PyDFCCSD, else PyRCCSD (the verified cc/__init__.py:83-139 dispatch order); `frozen=None/int/list/'auto'`.

Wire `pub mod cc;` + the cc submodule registration into `crates/pyscf-py/src/lib.rs` (mirror the `mp` registration in the `_native` `#[pymodule]`). Confirm `pyscf-py` already deps `pyscf-ccsd` (add it if absent).
  </action>
  <verify>
    <automated>cargo build -p pyscf-py --locked 2>&1 | tail -5; grep -q 'DO NOT' crates/pyscf-py/src/cc.rs && grep -q 'CcsdPyBridge' crates/pyscf-py/src/cc.rs && echo "BRIDGE_OK"</automated>
  </verify>
  <done>`cargo build -p pyscf-py --locked` exits 0; `cc.rs` defines `PyRCCSD`/`PyUCCSD`/`PyDFCCSD` + `CcsdPyBridge` (prints BRIDGE_OK) with the 5-hook is_overridden/call_method1/py.detach dispatch; the kernel does NOT py.detach at the top (the load-bearing comment present); `solve_lambda`/`make_rdm1`/`make_rdm2`/`as_scanner` exposed; `ccsd_factory` dispatch matches cc/__init__.py; the cc submodule is registered in `_native`.</done>
</task>

<task type="auto">
  <name>Task 2: python/pyscf/cc/__init__.py overlay (mf.CCSD()/mf.density_fit().CCSD() dispatch) + always-on structural bridge test</name>
  <files>python/pyscf/cc/__init__.py, crates/pyscf-py/tests/cc_bridge.rs</files>
  <read_first>
    - python/pyscf/mp/__init__.py (the mp overlay — the exact shape to mirror for cc: re-export _native.cc + the factory)
    - crates/pyscf-py/src/cc.rs (Task-1 — the PyRCCSD/PyUCCSD/PyDFCCSD + ccsd_factory the overlay re-exports)
    - crates/pyscf-py/tests/ (the existing mp bridge structural test, if present — the always-on no-maturin test shape)
    - UPSTREAM pyscf/cc/__init__.py lines 83-139 (RCCSD/UCCSD/DFCCSD factory + the cross-module mf.CCSD() dispatch)
    - 06-VALIDATION.md §"Per-Requirement Verification Map" (the CCSD-01/02 structural always-on rows) + §"Manual-Only Verifications" (the live numeric/3.13t arms deferred to 06-11)
  </read_first>
  <action>
Create `python/pyscf/cc/__init__.py` mirroring `python/pyscf/mp/__init__.py`: re-export `pyscf._native.cc.{RCCSD, UCCSD, DFCCSD, CCSD, Scanner}` so `from pyscf import cc` and `cc.RCCSD(mf)` work as upstream (BIND-02), and wire the `mf.CCSD()` / `mf.density_fit().CCSD()` cross-module dispatch (the `cc/__init__.py:83-139` factory order: UHF→UCCSD, with_df→DFCCSD, else RCCSD). Preserve the canonical `pyscf.cc` import path.

Create `crates/pyscf-py/tests/cc_bridge.rs` (always-on, no maturin/upstream — the structural arm): mirror the mp bridge structural test. Cover: (1) the `ccsd_factory` dispatch picks the right class for a mock RHF/UHF/DF reference (the cc/__init__.py order); (2) `is_overridden` correctly detects a subclass override vs the base class; (3) a synthetic `PyCcsdScanner` closure returns the expected energy on a mock Mole (the as_scanner shape). The LIVE numeric CCSD dispatch-parity + the `python3.13t` GIL smoke are Manual-Only / `workflow_dispatch` (06-11 — sandbox has no maturin/PySCF/3.13t; the 05-07 precedent).
  </action>
  <verify>
    <automated>cargo test -p pyscf-py --test cc_bridge --locked 2>&1 | tail -8; test -f python/pyscf/cc/__init__.py && grep -q 'RCCSD' python/pyscf/cc/__init__.py && echo "OVERLAY_OK"</automated>
  </verify>
  <done>`python/pyscf/cc/__init__.py` re-exports the cc surface + wires mf.CCSD() dispatch (prints OVERLAY_OK); `crates/pyscf-py/tests/cc_bridge.rs` proves factory dispatch + override-detect + scanner-closure always-on; `cargo test -p pyscf-py --test cc_bridge` green; live numeric + 3.13t deferred to 06-11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python mf / subclass overrides → bridge | UNTRUSTED: a Python override (ao2mo/update_amps/make_rdm) can return a wrong-shaped array; NumPy inputs may be non-contiguous. |
| free-threaded python3.13t → GIL seam | concurrent calls under the longest compute in the project (the heaviest GIL re-validation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10-SHAPE | DoS / Tampering | hook-returned arrays (ao2mo/update_amps) | mitigate | The pyo3-free kernels ShapeMismatch-validate every hook return before indexing (06-03 boundary); the PyO3 NumPy extraction reuses the vetted Phase-3/5 to_owned()-on-non-standard-layout converters (BIND-04). |
| T-06-10-GIL | Tampering | CcsdPyBridge / Python::detach | mitigate | The kernel does NOT detach at the top (hooks re-enter Python); each hook's default detaches (mp.rs discipline). The `python3.13t` CCSD smoke gate (06-11) confirms no deadlock — the heaviest re-validation (Pitfall 6). |
| T-06-10-PANIC | Tampering | Rust panic crossing FFI | mitigate | BIND-09: Rust errors map to Python exceptions (pyscf_to_py); `#![forbid(unsafe_code)]` + the catch_unwind FFI policy (FOUND-07) — panics never escape. |
| T-06-10-SC | Tampering | cargo deps (pyscf-py gains pyscf-ccsd) | accept | pyscf-py already links all method crates; pyscf-ccsd is workspace-internal (no registry install); the cdylib never links libpython at build (abi3). |
</threat_model>

<verification>
- `cargo build -p pyscf-py --locked` exits 0 with the cc submodule registered.
- `cargo test -p pyscf-py --test cc_bridge` passes (factory dispatch + override-detect + scanner-closure).
- `python/pyscf/cc/__init__.py` re-exports RCCSD/UCCSD/DFCCSD/CCSD + wires mf.CCSD().
- The live numeric CCSD dispatch-parity + `python3.13t` GIL smoke are the 06-11 workflow_dispatch/human-verify arms.
</verification>

<success_criteria>
- The cc surface (PyRCCSD/PyUCCSD/PyDFCCSD + ccsd_factory + scanner + solve_lambda/make_rdm) dispatches correctly; pyscf-ccsd stays pyo3-free (D-09).
- Subclass overrides dispatch via slf.call_method1 (Pitfall 7); the no-override default runs pure-Rust under py.detach; the kernel does NOT detach at the top (BIND-05).
- mf.CCSD()/mf.density_fit().CCSD() cross-module dispatch works; the canonical pyscf.cc import path preserved.
- Always-on structural arm green; live numeric + 3.13t deferred to 06-11.
</success_criteria>

<output>
Create `.planning/phases/06-ccsd/06-10-SUMMARY.md` when done.
</output>
