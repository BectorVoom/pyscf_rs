---
phase: 06-ccsd
plan: 08
type: execute
wave: 6
depends_on: ["06-03"]
files_modified:
  - crates/pyscf-ccsd/src/direct.rs
  - crates/pyscf-ccsd/src/ccsd.rs
  - crates/pyscf-ccsd/tests/direct.rs
autonomous: true
requirements: [CCSD-07]

must_haves:
  truths:
    - "mycc.direct = True runs AO-direct CCSD: the vvvv contraction is done on-the-fly from AO integrals instead of holding the in-memory vvvv tensor"
    - "AO-direct CCSD converges to the SAME e_corr as in-core CCSD on the small system (the equivalence proof)"
    - "AO-direct avoids materializing the full nv^4 vvvv MO block (lower peak arena reservation)"
  artifacts:
    - path: "crates/pyscf-ccsd/src/direct.rs"
      provides: "AO-direct _contract_vvvv_t2 branch (port ccsd.py:473-570)"
      contains: "contract_vvvv"
    - path: "crates/pyscf-ccsd/src/ccsd.rs"
      provides: "direct: bool flag routing the vvvv contraction through direct.rs"
      contains: "direct"
    - path: "crates/pyscf-ccsd/tests/direct.rs"
      provides: "AO-direct == in-core e_corr equivalence (CCSD-07)"
      contains: "direct"
  key_links:
    - from: "crates/pyscf-ccsd/src/direct.rs"
      to: "pyscf_gto::intor int2e"
      via: "on-the-fly AO contraction (shell-sliced if available, else full-tensor-tiled)"
      pattern: "int2e|intor"
    - from: "crates/pyscf-ccsd/src/ccsd.rs"
      to: "crate::direct"
      via: "direct=True routes update_amps' vvvv step through the AO-direct branch"
      pattern: "direct"
---

<objective>
Port AO-direct CCSD (CCSD-07, `mycc.direct=True`): the `_contract_vvvv_t2` AO-direct branch (`ccsd.py:473-570`) that trades the in-memory `vvvv` MO tensor for an on-the-fly AO-integral contraction, reducing peak arena pressure. Wire a `direct: bool` kernel flag that routes `update_amps`' vvvv step through `direct.rs`. Prove equivalence: AO-direct converges to the SAME `e_corr` as in-core on the small system.

Purpose: Land CCSD-07. The AO-direct path is the memory-frugal alternative to in-core for systems where `vvvv ≈ nv⁴` is too large — one of the two explicit user opt-ins (alongside DF) the D-01 refusal points users to.
Output: A converging `direct=True` CCSD matching the in-core energy; no full-vvvv MO materialization on the AO-direct path.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ccsd/06-CONTEXT.md
@.planning/phases/06-ccsd/06-RESEARCH.md
@.planning/phases/06-ccsd/06-PATTERNS.md
@.planning/phases/06-ccsd/06-VALIDATION.md

<interfaces>
<!-- From the codebase + verified upstream. RESEARCH Open Q4 must be resolved here. -->

From crate::ccsd (06-03): ccsd_kernel + the in-core update_amps vvvv step (the thing AO-direct replaces); the arena reserve/release.
From crate::update_amps (06-03): the host-loop _contract_vvvv_t2 'ijcd,acdb->ijab' in-core branch — the AO-direct branch is the alternative.
From pyscf_gto: intor("int2e") returns the FULL arity-4 tensor (verified used whole in mp2.rs:150). RESEARCH Open Q4: there is NO shell-sliced streaming int2e primitive in-tree yet.

Upstream (ccsd.py:473-570 / 538-558): the AO-direct branch uses gto.moleintor.getints4c with shls_slice for on-the-fly shell-pair int2e blocks, contracting t2 into AO integrals then back to MO.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port the AO-direct _contract_vvvv_t2 branch (ccsd.py:473-570) into direct.rs</name>
  <files>crates/pyscf-ccsd/src/direct.rs</files>
  <behavior>
    - the AO-direct vvvv contraction on a small system produces the SAME result block as the in-core _contract_vvvv_t2 (06-03) to bit-close tolerance (the equivalence is the correctness proof — AO-direct is a different contraction ORDER of the same math).
    - the AO-direct path does NOT materialize the full nv^4 vvvv MO tensor (a source assertion / a peak-buffer check).
    - host-loop oracle reductions; RAYON 1==8 invariant.
  </behavior>
  <read_first>
    - crates/pyscf-ccsd/src/direct.rs (the 06-01 stub returning NotYetImplemented{wave:4})
    - crates/pyscf-ccsd/src/update_amps.rs (06-03 — the in-core _contract_vvvv_t2 'ijcd,acdb->ijab' host-loop the AO-direct branch mirrors mathematically)
    - crates/pyscf-ao2mo/src/transform.rs (the host-loop AO<->MO transform — AO-direct contracts t2→AO then back→MO)
    - crates/pyscf-gto/src/lib.rs (the intor("int2e") surface — CONFIRM whether a shell-sliced int2e exists; RESEARCH Open Q4)
    - crates/pyscf-mp2/src/mp2.rs line 150 (how the full int2e tensor is consumed whole — the fallback if no shell-slice exists)
    - UPSTREAM pyscf/cc/ccsd.py lines 473-570 (_contract_vvvv_t2 AO-direct branch) + 538-558 (the getints4c shls_slice streaming)
  </read_first>
  <action>
RESOLVE RESEARCH Open Q4 FIRST: grep `pyscf-gto`'s intor surface for a shell-sliced `int2e` primitive (`shls_slice`-equivalent). (a) IF a shell-sliced primitive exists → port the upstream streaming AO-direct contraction (`ccsd.py:538-558`): walk AO shell-pair blocks, contract t2 (transformed to AO via the MO coeffs) against each on-the-fly int2e block, accumulate back to MO — never holding the full vvvv. (b) IF only the full int2e tensor is exposed (the verified mp2.rs:150 reality) → AO-direct v1 computes the full AO `int2e` ONCE and tiles the `t2`-AO-contraction in MO space WITHOUT ever materializing the full `nv⁴` vvvv MO block (correct + lower peak MO arena than in-core, even if it holds the AO tensor — document this as the v1 AO-direct semantics; it satisfies CCSD-07's `direct=True` contract: the vvvv MO tensor is not materialized). Document the chosen path in the module doc-comment.

EITHER WAY: the AO-direct vvvv contraction is the SAME math as in-core `_contract_vvvv_t2`, just a different contraction order — so the equivalence test (Task-1 behavior #1) is the correctness anchor. Host-loop materialize-then-`oracle_sum`/`oracle_dot` (no gemm, no `+=`). ShapeMismatch-validate.

RED→GREEN: write the equivalence test (AO-direct vvvv block == in-core vvvv block on a small system) + the no-full-vvvv assertion + RAYON invariance against the stub, then implement.
  </action>
  <verify>
    <automated>cargo test -p pyscf-ccsd --lib direct --locked 2>&1 | tail -8</automated>
  </verify>
  <done>`direct.rs` ports the AO-direct vvvv contraction (the Open-Q4 path documented); the AO-direct vvvv block matches the in-core block bit-close on the small system; no full nv⁴ vvvv MO tensor materialized; host-loop oracle reductions only; RAYON 1==8 invariant.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the direct: bool kernel flag + the AO-direct == in-core equivalence integration test</name>
  <files>crates/pyscf-ccsd/src/ccsd.rs, crates/pyscf-ccsd/tests/direct.rs</files>
  <read_first>
    - crates/pyscf-ccsd/src/ccsd.rs (06-03 ccsd_kernel — the vvvv step + the kernel-config struct the direct flag joins; the arena reserve for vvvv)
    - crates/pyscf-ccsd/src/direct.rs (Task-1 AO-direct contraction)
    - crates/pyscf-ccsd/tests/rccsd_numeric_smoke.rs (06-03 — the small-system reference-build pattern to reuse for the equivalence test)
    - UPSTREAM pyscf/cc/ccsd.py (the `self.direct` flag gating _add_vvvv vs the AO-direct branch)
    - 06-VALIDATION.md row CCSD-07 (the `cargo test -p pyscf-ccsd direct` command)
  </read_first>
  <action>
Add a `direct: bool` field to the `ccsd_kernel` config (default false, per `mycc.direct=False`). When `direct==true`, route the `update_amps` vvvv step through `crate::direct` (the AO-direct branch) instead of the in-core `_contract_vvvv_t2`, and SKIP the full-vvvv MO arena reservation (the AO-direct path's whole point — lower peak reservation; the pre-flight `try_reserve` estimate is correspondingly lower). Keep everything else (DIIS, convergence, init_amps) identical.

Create `crates/pyscf-ccsd/tests/direct.rs` (CCSD-07): run the small-system `ccsd_kernel` with `direct=false` and `direct=true`, assert both converge to the SAME `e_corr` (≤1e-9 — AO-direct is the same minimum via a different contraction order) and that the `direct=true` run's pre-flight reservation estimate is LOWER than the in-core run's (the memory-frugality proof). This is the always-on CCSD-07 arm; live upstream `direct=True` byte-identity is the 06-08-closeout workflow_dispatch arm.
  </action>
  <verify>
    <automated>cargo test -p pyscf-ccsd --test direct --locked -- --test-threads=1 2>&1 | tail -10; grep -q 'direct' crates/pyscf-ccsd/src/ccsd.rs && echo "DIRECT_WIRED"</automated>
  </verify>
  <done>`ccsd_kernel` has a `direct: bool` flag routing vvvv through the AO-direct branch (prints DIRECT_WIRED); `cargo test -p pyscf-ccsd --test direct` proves AO-direct e_corr == in-core e_corr (≤1e-9) and a lower peak reservation; in-core path unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AO int2e blocks → contraction | block lengths must be validated before indexing. |
| AO-direct job → memory budget | AO-direct lowers peak vvvv pressure; pre-flight estimate must reflect the lower reservation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08-SHAPE | DoS / Tampering | AO int2e block lengths | mitigate | ShapeMismatch validation before indexing; `#![forbid(unsafe_code)]`. |
| T-06-08-OOM | DoS | AO-direct pre-flight | mitigate | `try_reserve` reflects the lower AO-direct peak; HARD refusal still applies (D-01). |
| T-06-08-FP | Tampering (integrity) | AO-direct vvvv contraction | mitigate | host-loop `oracle_sum`/`oracle_dot`; the AO-direct == in-core equivalence test is the correctness anchor; RAYON 1==8. |

No network/auth/session/crypto surface — pure internal contraction.
</threat_model>

<verification>
- `cargo test -p pyscf-ccsd --test direct -- --test-threads=1` passes (AO-direct == in-core e_corr; lower peak).
- `cargo test -p pyscf-ccsd --test rccsd_numeric_smoke` still passes (in-core path unchanged).
- Per-wave: `cargo test --workspace --locked` + release-oracle determinism arms green.
</verification>

<success_criteria>
- AO-direct CCSD (direct=True) works and converges to the same e_corr as in-core (CCSD-07).
- The AO-direct path does not materialize the full nv⁴ vvvv MO tensor (lower peak reservation).
- The Open-Q4 shell-slice-vs-full-tensor decision is documented; all reductions oracle-based, thread-invariant.
</success_criteria>

<output>
Create `.planning/phases/06-ccsd/06-08-SUMMARY.md` when done.
</output>
