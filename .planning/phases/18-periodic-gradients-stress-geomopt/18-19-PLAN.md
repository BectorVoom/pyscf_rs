---
phase: 18-periodic-gradients-stress-geomopt
plan: 19
type: implementation
wave: 4
depends_on: ["18-05"]
files_modified:
  - crates/pyscf-pbc-grad/src/krhf.rs
  - crates/pyscf-pbc-grad/src/scanner.rs
  - crates/pyscf-pbc-grad/tests/krhf.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "GradientsBase routes get_jk/get_j/get_k to 18-04's *_e1 and INHERITS its named refusal on every non-FFTDF builder"
    - "optimizer(solver='ase') raising is UPSTREAM's behaviour, not an omission (18-CONTEXT §1.7)"
    - "the scanner's side-effect contract is load-bearing: verify_fd consumes this seam and 18-14's optimizer drives it"
    - "Gate C is at the 6 decimals upstream itself asserts, on upstream's own cell - not on this port's fixtures"
  artifacts:
    - path: "crates/pyscf-pbc-grad/src/scanner.rs"
      provides: "SCF_GradScanner - the seam verify_fd and geomopt both consume"
---

<objective>
**The KRHF driver surface, the scanner seam, and the first real gates.**

Split out of 18-05 so the assembly (`grad_elec`, `hcore_generator`, the
no-hoist ruling) is reviewed separately from the surface that wraps it. 18-05
lands the physics; this plan lands the object model and proves it against
upstream.

The scanner is the load-bearing piece: it is the seam **18-02's `verify_fd`
consumes** and the seam **18-14's optimizer drives**, and both depend on the
side-effect contract upstream documents at `krhf.py:311-313` (*"scanner has
side effects ... may change many underlying objects"*).

Gate C is the first comparison in the phase against a number upstream
committed: `-0.9017171774435333`.
</objective>

<context>
@.planning/phases/18-periodic-gradients-stress-geomopt/18-CONTEXT.md   # §1.7, §3.3
@.planning/phases/18-periodic-gradients-stress-geomopt/measurements/README.md  # 18-01's Gate B floor
@pyscf/pbc/grad/krhf.py                                                # :114-115, :149, :219-228, :230-298, :300-343
@pyscf/pbc/grad/test/test_krhf.py                                      # :50 the anchor, :58 test_exxdiv_ewald
@crates/pyscf-pbc-grad/src/verify_fd.rs                                # 18-02's harness
</context>

<tasks>

<task type="auto">
  <name>Task 1: `get_ovlp`, `grad_nuc`, `make_rdm1e`, `GradientsBase`, `get_jk`</name>
  * `get_ovlp` (`:114-115`) — `-pbc_intor('int1e_ipovlp')`, the minus here not
    in `pbc_intor` (18-03 Task 2).
  * `grad_nuc` (`:149-...`) — delegates to 18-03's `ewald_nuc_grad`.
  * `make_rdm1e` (`:224-228`) and `get_veff` (`:219-222`).
  * `GradientsBase` (`:230-298`) — `get_jk`/`get_j`/`get_k` route to 18-04's
    `get_jk_e1`/`get_j_e1`/`get_k_e1`, and inherit its named refusal on every
    non-FFTDF builder.
  * `optimizer(solver='ase')` (`:290-298`) — the refusal 18-02 Task 1 already
    declared; `'geometric'` raises, and that is upstream's behaviour, not an
    omission (`18-CONTEXT §1.7`).
</task>

<task type="auto">
  <name>Task 2: `as_scanner` / `SCF_GradScanner`</name>
  Port `krhf.py:300-343`. The scanner takes a `Cell` (or a geometry) and
  returns `(e_tot, de)`, reusing the previous solution as the initial guess.
  `:338-339` resets `self.grids` when the object has one — that hook exists for
  the KRKS subclass (18-07), so declare it here and leave it inert.

  This is the seam 18-02's `verify_fd` consumes and the seam 18-14's optimizer
  drives; both depend on the side-effect contract `:311-313` documents
  (*"scanner has side effects … may change many underlying objects"*).
</task>

<task type="auto">
  <name>Task 3: Gates B and C for KRHF</name>
  * **Gate B**: `verify_fd` against the scanner from Task 6, at 18-01's
    measured tolerance, on diamond at gamma and 1×1×2.
  * **Gate C**: `lib.fp(g)` against upstream's committed
    `-0.9017171774435333` (`test_krhf.py:50`) on upstream's own cell
    (diamond `gth-szv`/`gth-pade`, `mesh = [13]*3`, `kpts = [1,1,2]`,
    `exxdiv=None`, `conv_tol=1e-10`), at the 6 decimals upstream asserts.
  * Also port `test_krhf.py:58`'s `test_exxdiv_ewald` (upstream issue 2877) —
    it is the only test covering the `exxdiv` interaction with the Ewald term.
</task>

</tasks>

<verification>
* `cargo test -p pyscf-pbc-grad` green.
* **Gate C** reproduces `-0.9017171774435333` to the 6 decimals upstream asserts, on upstream's own cell (diamond `gth-szv`/`gth-pade`, `mesh = [13]*3`, `kpts = [1,1,2]`, `exxdiv=None`, `conv_tol=1e-10`).
* **Gate B**'s `verify_fd` residual is recorded and at or below 18-01's measured floor, on diamond at gamma and 1x1x2.
* `test_exxdiv_ewald` (upstream issue 2877) is ported and passes - the only coverage of the `exxdiv`/Ewald interaction.
* `optimizer(solver='ase')` raises, and the doc comment states this matches upstream rather than marking a gap.
* `GradientsBase::get_jk` on a GDF/MDF/RSDF/AFTDF builder raises 18-04's named refusal.
</verification>
