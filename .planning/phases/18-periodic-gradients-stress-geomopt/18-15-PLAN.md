---
phase: 18-periodic-gradients-stress-geomopt
plan: 15
type: verification
wave: 8
depends_on: ["18-01", "18-02", "18-03", "18-04", "18-05", "18-06", "18-07", "18-08", "18-09", "18-10", "18-11", "18-12", "18-13", "18-14", "18-16", "18-17", "18-18", "18-19", "18-20", "18-21"]
files_modified:
  - .planning/phases/18-periodic-gradients-stress-geomopt/18-VERIFICATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/pbc/PBC-MASTER-PLAN.md
  - FEATURES
autonomous: false
requirements: [GRAD-09, GEOMOPT-01]
must_haves:
  truths:
    - "every gate is reported as RUN/PASS, RUN/FAIL or NOT RUN — never as an inference from another gate"
    - "a SIGKILL (exit 137) is recorded as a measurement, not as a skip"
    - "no gate is loosened to make it pass; a failure is recorded with its number and a defect ID"
    - "FEATURES moves only what a green gate proves, and PROJECT.md Active->Validated follows the same rule"
  artifacts:
    - path: ".planning/phases/18-periodic-gradients-stress-geomopt/18-VERIFICATION.md"
      provides: "the phase rollup: gates A-E per body, defects, deferrals"
      contains: "NOT RUN"
---

<objective>
Close Phase 18: run Gates A–E across everything the phase shipped, record what
passed, what failed with its number, and what was never run — and say which is
which.

`STATE.md`'s Phase-17 rollup is the standard this must meet and the warning it
must heed: 17-12 shipped with *"kernel gates 5/5 GREEN, host-side Gate E
UNVERIFIED — every SCF-bearing host test is SIGKILLed (exit 137) … nothing
observed to fail or pass."* That is the honest report, and it is honest because
it did not convert an unrun gate into a passing one.
</objective>

<context>
@.planning/phases/18-periodic-gradients-stress-geomopt/18-CONTEXT.md          # §2.3, the five gates
@.planning/phases/18-periodic-gradients-stress-geomopt/18-REVIEW.md           # D-PBC-30's seven clauses and D-PBC-31's twelve
@.planning/phases/18-periodic-gradients-stress-geomopt/measurements/README.md # 18-01's measured floors
@.planning/phases/17-ksymm-multigrid/17-13-PLAN.md                            # the rollup shape to copy
</context>

<tasks>

<task type="auto">
  <name>Task 1: the gate matrix</name>
  One row per shipped body × gate. Every cell is **RUN/PASS**, **RUN/FAIL**
  (with the number) or **NOT RUN** (with the reason).

  | body | A | B | C | D | E |
  |---|---|---|---|---|---|
  | `ewald_nuc_grad` (18-03) | — | ✓ | ✓ | — | — |
  | `vpploc_part2_nuc_grad`, `vppnl_nuc_grad` (18-03) | — | — | ✓ | — | — |
  | `get_j_e1_kpts`, `get_k_e1_kpts` (18-04) | — | — | ✓ | — | — |
  | KRHF (18-05) | — | ✓ | ✓ | — | — |
  | KUHF (18-06) | — | ✓ | ✓ | — | — |
  | KRKS / KUKS × LDA, GGA, hybrid (18-07) | — | ✓ | ✓ | — | — |
  | KRKS+U / KUKS+U (18-08) | — | ✓ (5e-6 tier) | ✓ | — | — |
  | multigrid v2 grad entry points (18-09) | — | — | — | — | ✓ |
  | gamma RHF/UHF/RKS/UKS (18-10) | — | — | — | — | ✓ |
  | strain AO kernel (18-11) | ✓ | — | — | — | — |
  | `rks_stress` (18-12) | ✓ | — | — | ✓ | — |
  | `uks`/`krks`/`kuks` stress (18-13) | ✓ | — | — | ✓ | — |
  | geomopt (18-14) | — | — | ✓ | — | — |

  A dash means the gate does not apply to that body; it is **not** a synonym for
  NOT RUN, and the legend says so.
</task>

<task type="auto">
  <name>Task 2: the D-PBC-30 clause audit</name>
  Seven clauses, seven verdicts. For each: **landed** (with the test that pins
  it), **landed but unmeasured** (with what is missing), or **not landed** (with
  why).

  | clause | claim to verify |
  |---|---|
  | 1 | stress blocks sized against both AO arrays; the low-budget test shrinks `blk` |
  | 2 | **SUPERSEDED by D-PBC-31 clause 2** — `get_ovlp`/`get_kin` ship the closed form, not a shared-`Ls` finite difference; verify via that clause's row below, not via a shared-vs-per-cell `Ls` comparison (there is no longer a 36-cell lattice sum to share one across) |
  | 3 | **CORRECTED by D-PBC-31 clause 8** — the AO cache is one tiled residency `m`, not two branches; verify via that clause's row below (a run with `m < nkpts` in CI), not via "CI ran both branches" |
  | 4a | the tagged route allocates strictly less than the untagged one, same answer |
  | 4b | the gradient route refuses `kk_symmetry`, and the refusal is tested |
  | 5 | 18-01 Task 6's number is recorded, and 18-11 fused or did not, consistently with it |
  | 6 | **SUBSUMED by D-PBC-31 clause 7** — `18-05` Task 3 eliminates the per-atom AO/matrix entirely rather than hoisting it; verify via that clause's row below (the `ngrids·nao²` counter does not scale with `natm`), not via "the AO table is hoisted" |
  | 7 | the fused XC contraction is bit-identical across thread counts |

  Then **twelve more, for D-PBC-31** (`18-REVIEW` Part II §10), same three
  verdicts:

  | clause | claim to verify |
  |---|---|
  | 1 | Gate A ran at **three tiers**; no component test is gated at 1e-8 unless it is `get_pp` |
  | 2 | `get_ovlp`/`get_kin` ship the closed form; `grep` finds no `_finite_diff_cells` in the production path; both upstream arrangements agree at 1e-9 |
  | 3 | the strain FD transforms k at fixed fractional coordinates, asserted at `nkpts > 1` on a non-cubic cell |
  | 4 | the `.real` is inside the contraction and the rewrite is bit-identical on a non-symmetric DM |
  | 5 | the FD denominators are realised steps, and 18-01 recorded the effect as **sub-floor** rather than as a floor |
  | 6 | `coulG_1` stores 6 components; `weight_1` is added as a diagonal scalar |
  | 7 | `grad_elec` never materialises the per-atom `hcore`; the `ngrids·nao²` counter does not scale with `natm`; the Parseval residual is at 18-01 Task 5b's number |
  | 8 | the AO cache is one tiled `m`; the AO-evaluation counter matches `nkpts²/m`; a run with `m < nkpts` appears in CI |
  | 9 | the `get_coulG` counter reads `nkpts`, not `nkpts²` |
  | 10 | the `(natm,3)` reduction primitive has one implementation and three call sites |
  | 11 | the identity-phase multiply is skipped; the gate builds 18 cells, not 36 |
  | 12 | `blksize` keeps a **measured** buffer multiplicity and subtracts `mem_now` once |

  Three of D-PBC-31's clauses **supersede or correct** D-PBC-30, and the audit
  must say so rather than reporting both as landed: clause 2 supersedes
  D-PBC-30 clause 2 for `get_ovlp`/`get_kin`, clause 7 subsumes D-PBC-30
  clause 6, and clause 8 corrects D-PBC-30 clause 3's cost model by `nkpts`×.

  A clause whose optimisation shipped without the test that pins it is
  **landed but unmeasured**, and it is recorded as such — an optimisation with
  no test that it *was* taken is one that silently stops being taken.
</task>

<task type="auto">
  <name>Task 3: the scope ledger — what upstream does not have</name>
  Restate, in one place, everything this phase deliberately did not build, each
  with the line that proves upstream does not have it either. These will be
  asked about, and the answer must be findable:

  * **No HF stress** — `ls pyscf/pbc/grad/` has no `rhf_stress`/`khf_stress`.
  * **No lattice / variable-cell optimization** —
    `grep -rn "lattice" pyscf/pbc/geomopt/*.py` hits once, in `__main__`.
  * **No gradient on GDF/MDF/RSDF/AFTDF** — `get_jk_e1` exists only on
    `FFTDF` (`fft.py:324`), and none of the others subclass it.
  * **No meta-GGA or NLC gradient** — `krks.py:110,112`, `kuks.py:116,118`;
    but meta-GGA **is** supported in the stress (`rks_stress.py:176-178`).
  * **No `grid_response`** in the periodic gradient — `krks.py:48-49`,
    `kuks.py:49-50`, even though the molecular port supports it
    (`crates/pyscf-grad/src/rks.rs:106`).
  * **No periodic Hessian** — `pyscf/pbc` has no Hessian module
    (`PBC-MASTER-PLAN §8.13`), so the missing cintx Hessian families block
    nothing here.
  * **No `optimizer(solver='geometric')`** — upstream raises
    (`krhf.py:290-298`); the geomeTRIC path is reachable only via
    `pbc.geomopt.optimize`.
</task>

<task type="auto">
  <name>Task 4: document updates, gated on green</name>
  * `FEATURES` — move only what a **green** gate proves.
  * `.planning/STATE.md` — the Phase-18 rollup, in 17-13's format: what
    shipped, what is gated at what number, what is deferred and to where.
  * `.planning/ROADMAP.md:464` — confirm 18-01 Task 8's gate rewrite and
    18-14 Task 4's lattice strike both landed; the line must contain no
    `1e-14` and no bare-Ha stress tolerance.
  * `.planning/pbc/PBC-MASTER-PLAN.md` — `§8.10`'s seven-plan table replaced by
    the fifteen; `§2.4`'s two "BLOCKS plan 18-01" rows retired (18-03 Task 1);
    `§7`'s Phase-18 gate line matching `ROADMAP` byte-for-byte; **R-10 and
    R-14's Phase-18 entries updated** with what 18-03 Task 1 actually proved.
  * `PROJECT.md` — Active → Validated only for green gates.
</task>

<task type="manual">
  <name>Task 5: the honest failure report</name>
  For every RUN/FAIL and every NOT RUN, write: the number observed (or the
  reason it was not), the leading hypothesis, and a defect ID (`D-18-xx-nn`).

  **Do not loosen a gate to make it pass.** 17-08's `GDF Gate C` was recorded
  at 1.43e-6 against a target it failed, with the hypothesis named — that entry
  is why the failure is still tractable. And 17-08's KUKS gate is `#[ignore]`d
  **on an asserted precondition**, not on a loosened tolerance; if any Phase-18
  gate needs the same treatment, it gets the same treatment.

  If the machine cannot run a gate (exit 137), that is a **measurement of the
  machine**, recorded with the fixture size at which it happened — not a skip,
  and not a pass.
</task>

</tasks>

<verification>
* `18-VERIFICATION.md` exists; every cell of Task 1's matrix is RUN/PASS,
  RUN/FAIL + number, NOT RUN + reason, or an explicitly-defined dash.
* Task 2's seven D-PBC-30 clauses and twelve D-PBC-31 clauses each carry a
  verdict and a test path (or a stated gap), and the three supersessions are
  named as such rather than double-counted.
* `grep -n "1e-14" .planning/ROADMAP.md` returns nothing on the Phase-18 line;
  `grep -n "vol" .planning/ROADMAP.md` finds the stress gate expression.
* The Phase-18 gate text is byte-identical in `ROADMAP.md`,
  `PBC-MASTER-PLAN §7`/`§8.10` and `18-CONTEXT §2.3`.
* `grep -rn "phase: 18" crates/` returns no live refusal outside the
  deliberately-recorded non-ports in Task 3.
* Every `#[ignore]` added by this phase names an asserted precondition, not a
  tolerance that could not be met.
</verification>
