---
phase: 18-periodic-gradients-stress-geomopt
plan: 06
type: implementation
wave: 3
depends_on: ["18-05"]
files_modified:
  - crates/pyscf-pbc-grad/src/kuhf.rs
  - crates/pyscf-pbc-grad/src/lib.rs
  - crates/pyscf-pbc-grad/tests/kuhf.rs
autonomous: true
requirements: [GRAD-09]
must_haves:
  truths:
    - "kuhf.Gradients extends krhf's GradientsBase (kuhf.py:86), not krhf's Gradients"
    - "the hcore term contracts against the SPIN-SUMMED density; vhf contracts spin-resolved (kuhf.py:47-49)"
    - "make_rdm1e is the spin-resolved pair, and dme0 is spin-summed before the overlap term"
    - "verify_fd runs on the gradient this plan ships"
  artifacts:
    - path: "crates/pyscf-pbc-grad/src/kuhf.rs"
      provides: "KUHF k-point analytic nuclear gradient"
      contains: "verify_fd"
---

<objective>
`pyscf/pbc/grad/kuhf.py` (124 l) — the k-point unrestricted Hartree–Fock
gradient. It is `krhf`'s assembly with the spin index threaded through three of
the five terms and not the other two, which is the only thing that makes it its
own plan.
</objective>

<context>
@.planning/phases/18-periodic-gradients-stress-geomopt/18-CONTEXT.md   # §3.3, §3.8
@pyscf/pbc/grad/kuhf.py                                                # the port source
@crates/pyscf-pbc-grad/src/krhf.rs                                     # 18-05's base
@crates/pyscf-grad/src/uhf.rs                                          # the molecular spin-threading precedent
</context>

<tasks>

<task type="auto">
  <name>Task 1: `grad_elec` — which terms are spin-summed and which are not</name>
  Port `kuhf.py:29-72`. The class declaration is
  `class Gradients(krhf_grad.GradientsBase)` (`:86`) — it extends 18-05's
  **base**, not its `Gradients`, so `grad_elec`, `get_veff` and `make_rdm1e`
  are replaced wholesale while `get_hcore`, `hcore_generator`, `get_ovlp`,
  `get_jk`, `grad_nuc` and `as_scanner` are inherited unchanged.

  The spin threading (`:47-49`, mirroring `grad/uhf.py:65-68`):
  * the `h1ao` term contracts against `dm0_sf = dm0[0] + dm0[1]` — **spin
    summed**;
  * the `vhf` term contracts **spin-resolved**, `vhf[:,:,:,p0:p1]` against
    `dm0[:,:,:,p0:p1]`, summed over spin inside the einsum;
  * the `s1` term contracts against `dme0_sf = dme0[0] + dme0[1]` — spin
    summed again.

  Getting the middle one spin-summed too is the classic UHF gradient error: it
  is off by exactly the exchange asymmetry, so it is *correct for a closed-shell
  fixture* and wrong for every open-shell one. Gate it on a spin-polarised
  cell, not on diamond.
</task>

<task type="auto">
  <name>Task 2: `get_veff` and `make_rdm1e`</name>
  * `get_veff` (`:74-78`) — `vj[0] + vj[1] - vk`, the spin-summed Coulomb minus
    the spin-resolved exchange, from 18-04's `get_jk_e1`.
  * `make_rdm1e` (`:80-84`) — the spin-resolved pair, each built by 18-05's
    restricted `make_rdm1e` on that spin's orbitals.
</task>

<task type="auto">
  <name>Task 3: Gates B and C</name>
  * **Gate C**: `lib.fp(g)` against upstream's `-0.9017171774435333`
    (`test_kuhf.py:49`) on upstream's cell — note it is the **same constant** as
    KRHF's, because upstream's KUHF fixture is a closed-shell diamond and the
    two must agree exactly. That makes it a strong test of Task 1's threading
    only in the direction that catches a *restricted* bug, so it is necessary
    and not sufficient.
  * **The sufficient one**: run Gate B (`verify_fd`) on a genuinely
    spin-polarised periodic fixture at 18-01's measured tolerance. Reuse the
    open-shell cell the working tree's
    `crates/pyscf-pbc-dft/tests/gate_openshell.rs` and
    `crates/pyscf-pbc-scf/tests/init_guess_spin.rs` already build, rather than
    inventing a sixth reference system.
</task>

</tasks>

<verification>
* `cargo test -p pyscf-pbc-grad` green.
* Gate C reproduces `-0.9017171774435333` to 6 decimals and equals the KRHF
  number bit-for-bit on the closed-shell fixture.
* Gate B passes on the open-shell fixture; its residual is recorded.
* Determinism at `RAYON_NUM_THREADS=1` and `8` under `release-oracle`.
</verification>
