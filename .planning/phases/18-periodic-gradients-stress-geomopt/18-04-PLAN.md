---
phase: 18-periodic-gradients-stress-geomopt
plan: 04
type: implementation
wave: 1
depends_on: ["18-02"]
files_modified:
  - crates/pyscf-pbc-df/src/fft_jk_grad.rs
  - crates/pyscf-pbc-df/src/fftdf.rs
  - crates/pyscf-pbc-df/src/lib.rs
  - crates/pyscf-pbc-df/src/ao_cache.rs
  - crates/pyscf-pbc-df/tests/fft_jk_grad.rs
  - crates/pyscf-pbc-df/tests/grad_route_refusal.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "get_jk_e1/get_j_e1/get_k_e1 exist ONLY on Fftdf; GDF/MDF/RSDF/AFTDF get a NAMED refusal, never a fallback to another route"
    - "the k-point AO table is ONE budgeted residency m, tiled; m = nkpts is resident and m = 1 is streaming, and CI exercises a small m"
    - "the streaming cost is nkpts^2/m AO evaluations, not nkpts — get_k_e1_kpts is a DOUBLE loop (fft_jk.py:369, :381)"
    - "upstream's blksize subtracts mem_now TWICE (fft_jk.py:362, :363) — that is the defect; the /4 buffer multiplicity is deliberate and is KEPT"
    - "coulG is cached by k-difference index: nkpts^2 builds collapse to nkpts"
    - "the gradient route REFUSES kk_symmetry; the energy path's conjugate identity does not close when the derivative sits on the bra (D-PBC-30 clause 4b)"
    - "the mo_coeff-tagged density collapses the ket index from nao to nocc when present, and the two routes are gated against each other"
  artifacts:
    - path: "crates/pyscf-pbc-df/src/fft_jk_grad.rs"
      provides: "get_j_e1_kpts + get_k_e1_kpts"
      contains: "oracle_sum"
    - path: "crates/pyscf-pbc-df/src/ao_cache.rs"
      provides: "the budgeted k-point AO table shared by 18-04 and 18-05"
      contains: "PYSCF_MAX_MEMORY"
---

<objective>
The two-electron half of every k-point gradient, and the only density-fitting
route in PySCF 2.12.1 that has one.

`grep -rn "def get_jk_e1\|def get_j_e1\|def get_k_e1" pyscf/pbc/df/*.py` returns
three lines, all in `fft.py` (`:324`, `:330`, `:335`), and the class graph shows
`GDF`, `RSGDF`, `MDF` and `AFTDF` are **not** FFTDF subclasses
(`18-CONTEXT §1.2`). So `krhf.GradientsBase.get_jk`'s
`self.base.with_df.get_jk_e1(...)` (`krhf.py:265`) raises `AttributeError` on
every other route, and there is no periodic analytic gradient for GDF/MDF/RSDF
anywhere upstream. This port defaults `KRHF` to FFTDF already
(`crates/pyscf-pbc-scf/src/krhf.rs:46`) — but a user who calls
`.density_fit()` must get a loud, named refusal, not a plausible wrong number
from a substituted route.

This plan also lands **three of the seven D-PBC-30 clauses** (3, 4a, 4b) and
**four of D-PBC-31's twelve** (8, 9, 11, 12), because they are all properties
of this one file. D-PBC-31 clause 8 **corrects** clause 3's cost model and
clause 12 corrects its memory sizing — read Part II §7.2 and §8 before Task 2,
not after.
</objective>

<context>
@.planning/phases/18-periodic-gradients-stress-geomopt/18-CONTEXT.md   # §1.2, §3.2, §3.7
@.planning/phases/18-periodic-gradients-stress-geomopt/18-REVIEW.md    # §3.1, §3.4, §3.5; Part II §7.2, §7.3, §7.5, §8
@crates/pyscf-pbc-df/src/fft_jk.rs                                     # :55 get_j_kpts, :265 get_k_kpts_opts
@pyscf/pbc/df/fft_jk.py                                                # :113 get_j_e1_kpts, :310 get_k_e1_kpts
@crates/pyscf-pbc-df/src/aftdf.rs                                      # :84 PYSCF_MAX_MEMORY precedent
</context>

<tasks>

<task type="auto">
  <name>Task 1: `get_j_e1_kpts`</name>
  Port `fft_jk.py:113-...`. It differentiates the AOs on the real-space grid
  via `eval_ao_kpts(deriv=1)` and reuses the same FFT/`coulG` pipeline as the
  energy — `int2e_ip1` is never called. The port's `get_j_kpts`
  (`fft_jk.rs:55`) already owns that pipeline; the derivative version differs
  only in which AO component sits on the bra and in the leading `(3, …)`
  component axis.

  Build it beside `get_j_kpts` in a sibling module, sharing the `coulG` cache
  the energy path already has (W-01), not re-deriving it. **That cache is keyed
  by the k-difference** — see Task 3's clause-9 ruling; one cache serves both
  entry points.
</task>

<task type="auto">
  <name>Task 2: the budgeted k-point AO table (D-PBC-30 clause 3)</name>
  `fft_jk.py:346-350` builds `ao1_kpts` for **all** k-points at `deriv = 1`
  before the loop — `nkpts · 4 · ngrids · nao` complex, which
  `18-REVIEW §3.1` sizes at **1.02 GiB** for diamond `gth-dzvp` at 3×3×3 — and
  `blksize` (`:363`) guards only the inner `rho1`, not this. The grid cannot be
  blocked (the FFT at `:392` needs it whole), so the only lever is how many
  k-points are resident.

  Ship `ao_cache.rs`: a table sized from `PYSCF_MAX_MEMORY` (the
  `aftdf.rs:84-85` precedent), holding **`m` k-point tables**, with the inner
  `k1` loop tiled in chunks of `m`. Peak `(m + 1) · 4 · ngrids · nao` complex,
  cost `nkpts²/m` AO evaluations. `m = nkpts` **is** the resident case and
  `m = 1` is the worst case — Part I's two branches are the endpoints of this
  one number, not two code paths.

  **Part I's cost model was wrong and this is the correction** (D-PBC-31
  clause 8). `get_k_e1_kpts` is a **double** loop — `:369` over `k2`, `:381`
  over `k1` — so a two-resident cache rebuilds the inner table for every outer
  k: `nkpts²` AO evaluations, not `nkpts`. At 3×3×3 that is 729 full-grid
  `deriv = 1` evaluations against 27, which is not a trade anyone would take
  knowingly. Tiling makes the exchange rate visible and choosable.

  Re-derive `blksize` for the inner `rho1`, and **be precise about which half
  of upstream's formula is the defect** (D-PBC-31 clause 12). `:363` is
  `(max_memory-mem_now)*1e6/16/4/3/ngrids/nao`:
  * the `16` is bytes per complex, the `3` the component axis — transcribe;
  * the **`4` is buffer multiplicity and is deliberate**. `:389-394` holds
    `rho1` and `vG` alive across the forward transform and `vG` and `vR`
    across the inverse, so the inner peak is ~2× the `rho1` figure —
    `18-REVIEW §3.1`'s 755 MiB is really ~1.5 GiB. Keep a multiplicity factor;
    use the one **18-01 Task 5 measured** (2 if the transform runs in place,
    3–4 if not), with the derivation in the doc comment;
  * the **doubled `mem_now` is the defect** — `:362` subtracts it from
    `max_memory` and `:363` subtracts it again (`18-CONTEXT §3.7`). Drop the
    second subtraction and nothing else.

  This module is shared with 18-05 Task 3 (clause 6) — one cache, one budget,
  two consumers. Two independent budgets that each believe they own the machine
  is how a 1 GiB array becomes 2 GiB.
</task>

<task type="auto">
  <name>Task 3: `get_k_e1_kpts`, with the MO factorisation (clause 4a)</name>
  Port `fft_jk.py:310-...`. When the density carries the `mo_coeff`/`mo_occ`
  tag 18-02 Task 3 built, `:357-359` collapses the ket index from `nao` to the
  occupied count. `18-REVIEW §3.1` sizes the inner `rho1` at **755 MiB**
  untagged and **116 MiB** tagged on diamond `gth-dzvp` — a 6.5× cut, and 2×
  even on `gth-szv`.

  **State both sides of the trade** (D-PBC-31 §8.2). `:357-359` materialises a
  *new* `nkpts · nocc · ngrids` complex array — +42 MiB at diamond `gth-dzvp`
  3×3×3 — **beside** the deriv-1 table, which stays alive as `ao1_kpts`
  (`:347-349` makes `ao2_kpts` a **view** into it in the untagged case, so the
  untagged route holds one table and the tagged route holds one table plus the
  MO one). Against a ~1.4 GiB cut in the transient this is an overwhelmingly
  good trade — which is exactly why the assertion below is on **peak**, and why
  the resident term is stated here rather than discovered by a surprised test.

  Two further free ones in this body (D-PBC-31 clauses 9 and 11):
  * **`coulG` is rebuilt `nkpts²` times for `nkpts` distinct arguments.**
    `:384` calls `tools.get_coulG(cell, kpt2-kpt1, ...)` inside the double
    loop; on a Monkhorst–Pack mesh the difference set **is** the mesh. Key
    Task 1's W-01 cache by the k-difference index: `nkpts² → nkpts`, each
    `O(ngrids)` plus the `exxdiv='ewald'` correction that rides on it.
  * **`vR_dm *= expmikr.conj()` (`:402`) runs over the whole
    `(3, nset, nao, ngrids)` array — 30 MiB — even when `expmikr` is
    `np.array(1.)`** (`:386-387`'s `is_zero(kpt1-kpt2)` branch). Skip it on the
    diagonal: `nkpts` full-array complex multiplies, free.

  Both routes ship. Gate them against each other: the tagged and untagged
  results agree at 18-01's measured tolerance, and the tagged one's peak
  allocation is strictly smaller. An optimisation with no test that it *was*
  taken is an optimisation that silently stops being taken.
</task>

<task type="auto">
  <name>Task 4: the named refusal for every non-FFTDF route</name>
  `Fftdf` gains `get_jk_e1` / `get_j_e1` / `get_k_e1`. Every other
  `PeriodicDf` implementor gains the same three methods returning an error that
  **names the route and says why**: PySCF 2.12.1 has no periodic analytic
  gradient for GDF/MDF/RSDF/AFTDF, and the fix is to run the gradient on FFTDF,
  not to substitute a route.

  Test (`grad_route_refusal.rs`): construct a `KRHF` on each of GDF, MDF, RSDF
  and AFTDF, ask for the gradient JK, and assert the error names the route.
  **Assert it is an error, not a number** — a fallback here would produce a
  gradient that is plausible, wrong, and consistent run to run.
</task>

<task type="auto">
  <name>Task 5: refuse `kk_symmetry` on the gradient route (clause 4b)</name>
  `fft_jk.rs:265-330` documents the energy-path identity
  `rho1^{21}[(i,j),g] = conj(rho1^{12}[(j,i),g])`, which halves the transforms
  and which its own doc block warns *"CHANGES THE RESULT … must be
  re-baselined"*. It holds because bra and ket carry the same AO table.

  In `get_k_e1_kpts` the bra is `ao1T[1:,p0:p1]` — the **derivative** AO — and
  the ket is `ao2T`. The `(j,i)` swap moves the derivative onto the ket and the
  identity does not close.

  The gradient entry point therefore takes no `kk_symmetry` flag, and if the
  builder carries one it **refuses** rather than ignoring it. Test the refusal.
  A comment asking implementers not to enable it is not a mechanism, and a
  wrongly enabled flag here moves the last bits of an otherwise-correct
  gradient — the hardest class of error to attribute.
</task>

<task type="auto">
  <name>Task 6: gates</name>
  * `get_j_e1_kpts` and `get_k_e1_kpts` against upstream
    `pyscf.pbc.df.fft_jk` on the §9.2 reference cells at gamma, 1×1×2 (the
    shape `test_krhf.py:37` uses) and 2×2×2, at 18-01's measured tolerance.
  * **A small `m` runs in CI**: one test pins `PYSCF_MAX_MEMORY` low enough to
    force `m < nkpts` and asserts the result matches `m = nkpts` at 18-01's
    measured tolerance, **and** that the chosen `m` and the AO-evaluation count
    are what the budget predicts (`nkpts²/m`). This is 16-01's rule — *"a
    fixture that silently stayed incore fails rather than passes"* — and it is
    the exact failure mode 17-12 hit (`STATE.md`, exit 137). Asserting the
    count, not just the answer, is what keeps clause 8's exchange rate honest.
  * **The `coulG` cache is counted, not assumed**: a test asserts `get_coulG`
    is entered `nkpts` times, not `nkpts²`, over a full `get_k_e1_kpts` call.
  * Determinism at `RAYON_NUM_THREADS=1` and `8` under `release-oracle`.
</task>

</tasks>

<verification>
* `cargo test -p pyscf-pbc-df` green.
* `grad_route_refusal.rs` shows four refusals, each naming its route.
* The AO-cache test log records the chosen `m` and the realised AO-evaluation
  count for each run; a run with `m < nkpts` appears.
* The `coulG` build counter reads `nkpts`.
* `grep -n "mem_now" crates/pyscf-pbc-df/src/fft_jk_grad.rs` shows one
  subtraction, and the buffer-multiplicity factor cites 18-01 Task 5 by value.
* `grep -rn "kk_symmetry" crates/pyscf-pbc-df/src/fft_jk_grad.rs` finds only
  the refusal.
* Peak-RSS assertion in Task 3 shows the tagged route strictly below the
  untagged one on the same fixture.
</verification>
