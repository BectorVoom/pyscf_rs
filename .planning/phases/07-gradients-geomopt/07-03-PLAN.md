---
phase: 07-gradients-geomopt
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - crates/pyscf-grad/src/rhf.rs
  - crates/pyscf-grad/src/lib.rs
  - crates/pyscf-grad/tests/rhf_verify_fd.rs
autonomous: true
requirements: [GRAD-01]
must_haves:
  truths:
    - "mf.nuc_grad_method().kernel() for RHF returns an analytical gradient (natm, 3) array"
    - "grad_elec = sum(hcore_deriv . dm0) + 2*(veff_deriv . dm0) - 2*(s1 . dme0), plus grad_nuc"
    - "make_rdm1e (energy-weighted RDM) and grad_nuc are wired as thin ports over shipped arrays"
    - "rhf_verify_fd passes the FD gate at <=1e-6 Ha/Bohr (D-01) — OR is #[ignore]'d behind the cintx-availability gate per 07-01"
  artifacts:
    - path: "crates/pyscf-grad/src/rhf.rs"
      provides: "RHF grad_elec / hcore_generator / get_ovlp / get_veff / make_rdm1e / grad_nuc"
      contains: "grad_elec"
      min_lines: 80
    - path: "crates/pyscf-grad/tests/rhf_verify_fd.rs"
      provides: "RHF analytical-grad-vs-FD gate (GRAD-01, D-01)"
      contains: "verify_fd"
  key_links:
    - from: "crates/pyscf-grad/src/rhf.rs"
      to: "pyscf-gto gradient intors (int1e_ip*, int2e_ip1)"
      via: "hcore_generator + get_ovlp + get_veff"
      pattern: "int1e_ip|int2e_ip1|hcore"
    - from: "crates/pyscf-grad/src/rhf.rs"
      to: "pyscf_algebra oracle_sum / oracle_dot"
      via: "every einsum reduction"
      pattern: "oracle_sum|oracle_dot"
---

<objective>
Port the RHF analytical gradient body (GRAD-01) — the phase headline — into
`crates/pyscf-grad/src/rhf.rs`, FD-gated via the 07-02 `verify_fd` harness (D-01).

This is the first method body and the base every variational method (UHF/RKS/UKS) and
relaxed-density method (MP2/CCSD) reuses. Per CONTEXT "Claude's Discretion": port
`pyscf/grad/rhf.py` (`grad_elec`: `hcore_generator`, `get_ovlp` derivative →
energy-weighted RDM via `make_rdm1e`, `get_veff` derivative, `grad_nuc`). The
Hellmann-Feynman + Pulay decomposition is mechanical; every reduction routes through
`oracle_sum`/`oracle_dot` (Pitfall 1/2, no bare `+=`).

Per D-04: RHF energy gradients are stationary (the 2n+1 rule) and do NOT call CPHF.
Per D-02 + 07-01: `int2e_ip1` + `int1e_ip{ovlp,kin,nuc,rinv}` + the `with_rinv_at_nucleus`
rinv-origin shift are likely MISSING from cintx — so the rhf_verify_fd NUMERIC arm is
gated (workflow_dispatch / `#[ignore]`) until the cintx workstream lands them, while the
STRUCTURAL wiring (the grad_elec assembly, shapes, atmlst) lands always-on.

Purpose: Ship the headline RHF gradient + prove the FD gate end-to-end on the thinnest
slice. Output: A working `RhfGradients` body + the rhf_verify_fd gate.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gradients-geomopt/07-CONTEXT.md
@.planning/phases/07-gradients-geomopt/07-RESEARCH.md
@.planning/phases/07-gradients-geomopt/07-PATTERNS.md
@.planning/phases/07-gradients-geomopt/07-01-SUMMARY.md
@.planning/phases/07-gradients-geomopt/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port grad_elec + hcore_generator + get_ovlp + get_veff + make_rdm1e + grad_nuc</name>
  <files>crates/pyscf-grad/src/rhf.rs, crates/pyscf-grad/src/lib.rs</files>
  <read_first>
    - pyscf/grad/rhf.py:38-189 (the PRIMARY port target: grad_elec lines 59-76; grad_nuc 92-107; get_hcore 109-117; hcore_generator 121-143; get_ovlp 146; get_veff/get_jk 149-183; make_rdm1e 185-189)
    - crates/pyscf-grad/src/lib.rs (the base Gradients trait + module stubs from 07-02)
    - crates/pyscf-mp2/src/mp2.rs (the structural analog: default_* free-fn + Result<_,Error> kernel module shape — the pyo3-free method-body file shape)
    - crates/pyscf-gto/src/intor.rs (the wired gradient-intor dispatch from 07-01 — how to request int1e_ipovlp/ipkin/ipnuc/iprinv (arity-2 component-leading) and int2e_ip1 (arity-4))
    - 07-01-SUMMARY.md (which gradient families are cintx-ready vs gated — determines whether rhf_verify_fd numeric un-gates now or is #[ignore]'d)
    - 07-PATTERNS.md §"crates/pyscf-grad/src/rhf.rs" (the grad_elec decomposition + bit-exact discipline + the with_rinv_at_nucleus note)
    - 07-RESEARCH.md §"Pattern 1: RHF grad_elec decomposition" + §"Pitfall 4 (F-order/component-leading)"
  </read_first>
  <action>
    Port `grad_elec` from `pyscf/grad/rhf.py:59-76` into `crates/pyscf-grad/src/rhf.rs`. The
    decomposition: `de[k] = einsum('xij,ij->x', hcore_deriv(ia), dm0)
    + 2*einsum('xij,ij->x', vhf[:,p0:p1], dm0[p0:p1])
    - 2*einsum('xij,ij->x', s1[:,p0:p1], dme0[p0:p1])` per atom `ia` over `atmlst`, where
    `p0,p1 = aoslices[ia, 2:]`. Wire the pieces:
    - `s1 = -int1e_ipovlp` (`get_ovlp`, rhf.py:146) — arity-2 component-leading `[3, nao, nao]`.
    - `dm0 = mf.make_rdm1(mo_coeff, mo_occ)` from the SCF reference snapshot.
    - `vhf = get_veff(mol, dm0)` = `get_jk` over `int2e_ip1` (rhf.py:149-183) — arity-4.
    - `dme0 = make_rdm1e(mo_energy, mo_coeff, mo_occ)` — the energy-weighted RDM (rhf.py:185-189),
      a thin pure-linear-algebra port over shipped arrays.
    - `hcore_deriv = hcore_generator(mol)` (rhf.py:121-143): `int1e_ipkin + int1e_ipnuc` (get_hcore)
      plus the per-atom Hellmann-Feynman `int1e_iprinv` with `with_rinv_at_nucleus(atm_id)`
      origin shift scaled by `-atom_charge(atm_id)`.
    - `grad_nuc` (rhf.py:92-107): the nuclear repulsion gradient, a thin index-arithmetic port.
    - `aoslice_by_atom`: derive from `_bas[:,ATOM_OF]` + `ao_loc_nr` (both in pyscf-gto — RESEARCH A4).

    BIT-EXACT DISCIPLINE (Pitfall 1/2): every `einsum`/per-atom reduction materializes then
    `pyscf_algebra::oracle_sum` / `oracle_dot` — NEVER a bare `+=`. All matmuls route through
    `pyscf-algebra` gemm/gemv (algebra wall). Per Pitfall 4: assert every gradient intor returns
    `[3, nao, nao]` (component-leading, axis 0 = x/y/z) — never `[nao, nao, 3]`.

    Per D-04: do NOT call CPHF — RHF energy gradients are stationary. Implement the base
    trait's `grad_elec`/`make_rdm1e` for `RhfGradients`; reuse the shared `grad_nuc`/`get_ovlp`.
    Wire `mf.nuc_grad_method()` → `RhfGradients` at the Rust level (the PyO3 graft lands in 07-09).
  </action>
  <verify>
    <automated>cargo test -p pyscf-grad --locked -- --test-threads=1 rhf</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-grad/src/rhf.rs` contains `grad_elec`, `make_rdm1e`, `hcore_generator` (or `hcore_deriv`), and `get_veff`
    - `grep -nE '\+=' crates/pyscf-grad/src/rhf.rs | grep -vE 'oracle|de\[k\] \+= oracle'` shows no bare-`+=` reductions (reductions go through oracle_sum/oracle_dot)
    - A test asserts `RhfGradients::kernel()` returns a `(natm, 3)` array
    - `cargo test -p pyscf-grad --locked rhf` exits 0 (NO libxc pulled)
  </acceptance_criteria>
  <done>
    RHF grad_elec + hcore_generator + make_rdm1e + grad_nuc compile and produce a (natm,3) gradient;
    all reductions are oracle-ordered; no CPHF call.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire rhf_verify_fd against the 07-02 FD harness (GRAD-01, D-01)</name>
  <files>crates/pyscf-grad/tests/rhf_verify_fd.rs</files>
  <read_first>
    - crates/pyscf-grad/src/verify_fd.rs (the harness from 07-02 — its disp default + tolerance + closure contract)
    - crates/pyscf-grad/src/rhf.rs (the grad_elec body from Task 1)
    - crates/pyscf-scf/src/scanner.rs (the SCF as_scanner energy closure verify_fd consumes)
    - 07-01-SUMMARY.md (the cintx-availability finding — gate-or-un-gate decision for the numeric arm)
    - 07-RESEARCH.md §"Validation Architecture" GRAD-01 row (`cargo test -p pyscf-grad rhf_verify_fd`) + §"Wave 0 Gaps"
    - 07-VALIDATION.md Per-Task Verification Map GRAD-01 row
  </read_first>
  <action>
    Create `crates/pyscf-grad/tests/rhf_verify_fd.rs`: build an RHF reference on a tiny molecule
    (H2 or H2O / minimal basis that does NOT pull libxc), get its `as_scanner` energy closure, and
    call the 07-02 `verify_fd(disp=1e-4)` harness comparing the analytical `RhfGradients::kernel()`
    to the central-difference FD of the energy at `<=1e-6 Ha/Bohr` (D-01).

    GATING (per 07-01-SUMMARY + D-02): if `int2e_ip1` + `int1e_ip*` + the rinv-origin shift are
    cintx-ready (un-gated case), this test runs always-on as the daily gate. If they are MISSING
    (the expected case per RESEARCH), mark the NUMERIC assertion `#[ignore]` with a comment citing
    the cintx workstream prerequisite (the CCSD-D-04-style gated arm) — but keep an always-on
    STRUCTURAL assertion (the grad returns the right shape, atmlst subsets correctly, no
    NotYetImplemented). FD-structural stays always-on regardless (D-01). Document the chosen arm
    in the SUMMARY.
  </action>
  <verify>
    <automated>cargo test -p pyscf-grad --locked -- --test-threads=1 rhf_verify_fd</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-grad/tests/rhf_verify_fd.rs` exists and calls the `verify_fd` harness with `disp=1e-4` and a `1e-6` tolerance
    - `cargo test -p pyscf-grad --locked rhf_verify_fd` exits 0 (the numeric arm either passes or is `#[ignore]`'d per the cintx-availability gate; the structural arm always runs)
    - The SUMMARY records whether the numeric arm is un-gated (cintx ready) or `#[ignore]`'d (cintx workstream pending)
  </acceptance_criteria>
  <done>
    The RHF analytical gradient is FD-gated end-to-end (always-on structural; numeric un-gated or
    gated per the cintx finding) — the headline GRAD-01 slice is proven.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gradient-intor buffer ↔ grad_elec contraction | component-leading layout crosses here; a permutation scrambles x/y/z |
| per-atom einsum reduction ↔ de result | bare `+=` makes the gradient thread-count-dependent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Tampering | grad_elec einsum reductions | mitigate | Every reduction materializes then `oracle_sum`/`oracle_dot` (Pitfall 1/2); FD gate catches any reduction-order drift at the 7th digit |
| T-07-08 | Tampering | component-leading layout in get_ovlp/get_veff/hcore | mitigate | Assert `[3, nao, nao]` on every gradient intor (Pitfall 4 / Pitfall 8 "Phase 7 grad"); FD agreeing on only one axis is the warning sign |
| T-07-09 | Tampering | eigenvector sign flips across the scanner | accept | The SCF reference already applies `canonicalize_signs` (SCF-13, Pitfall 4 re-validation); gradients inherit vendor-stable mo_coeff for free |
| T-07-SC | Tampering | crate deps | mitigate | No new registry packages; dependency-wall lint enforces no cubecl-* |
</threat_model>

<verification>
- `cargo test -p pyscf-grad --locked -- --test-threads=1` green (scoped — NO libxc).
- rhf_verify_fd structural arm always-on; numeric arm un-gated or `#[ignore]`'d per 07-01.
- No bare `+=` in rhf.rs reductions; no CPHF call (D-04).
</verification>

<success_criteria>
- `RhfGradients::kernel()` returns a `(natm, 3)` analytical gradient assembled via the
  Hellmann-Feynman + Pulay decomposition over `oracle`-ordered reductions.
- GRAD-01 is FD-gated (D-01): structural always-on; numeric gated per the cintx-availability finding.
</success_criteria>

<output>
Create `.planning/phases/07-gradients-geomopt/07-03-SUMMARY.md` when done. MUST record the
RhfGradients API, whether rhf_verify_fd numeric un-gated or is gated on the cintx workstream,
and the grad_elec decomposition so UHF/RKS/UKS (07-05) and the geomopt scanner (07-04) reuse it.
</output>
