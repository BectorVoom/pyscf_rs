---
phase: 07-gradients-geomopt
plan: 08
type: execute
wave: 6
depends_on: ["07-07", "07-01"]
files_modified:
  - crates/pyscf-grad/src/ccsd.rs
  - crates/pyscf-grad/src/ecp.rs
  - crates/pyscf-grad/src/lib.rs
  - crates/pyscf-grad/tests/ccsd_verify_fd.rs
  - crates/pyscf-grad/tests/ecp_verify_fd.rs
autonomous: true
requirements: [GRAD-06, GRAD-07]
must_haves:
  truths:
    - "CCSD gradients consume the Phase-6 solve_lambda + make_rdm1/make_rdm2 (incl. ao_repr) directly — NO lambda re-derivation (GRAD-06, D-04)"
    - "CCSD-grad re-enters CPHF ONLY for the orbital-relaxation Z-vector via the one 07-07 solver"
    - "ECP gradients wire ECPscalar_ipnuc (cintx-ready) + ECPscalar_iprinv (gated on the cintx workstream) through the Phase-2 CintxEcpEngine (GRAD-07)"
    - "Both are FD-gated via verify_fd (D-01); numeric arms follow the 07-01 cintx-availability gate"
  artifacts:
    - path: "crates/pyscf-grad/src/ccsd.rs"
      provides: "CCSD grad from Phase-6 lambda + RDMs + orbital-relaxation Z-vector (calls cphf)"
      contains: "solve_lambda"
    - path: "crates/pyscf-grad/src/ecp.rs"
      provides: "ECP gradient hcore term (ECPscalar_ipnuc/iprinv)"
      contains: "ECPscalar"
    - path: "crates/pyscf-grad/tests/ccsd_verify_fd.rs"
      provides: "CCSD analytical-grad-vs-FD gate (GRAD-06)"
      contains: "verify_fd"
  key_links:
    - from: "crates/pyscf-grad/src/ccsd.rs"
      to: "crates/pyscf-ccsd solve_lambda + make_rdm1/make_rdm2 (Phase 6)"
      via: "consume lambda + RDMs directly, NO re-derivation"
      pattern: "solve_lambda|make_rdm1|make_rdm2"
    - from: "crates/pyscf-grad/src/ccsd.rs"
      to: "crates/pyscf-grad/src/cphf.rs solve (07-07)"
      via: "orbital-relaxation Z-vector only"
      pattern: "cphf::solve"
    - from: "crates/pyscf-grad/src/ecp.rs"
      to: "crates/pyscf-gto ecp_engine_cintx (07-01)"
      via: "ECPscalar_ipnuc/iprinv dispatch"
      pattern: "ECPscalar|ecp_ipnuc"
---

<objective>
Complete the gradient method surface (D-09 order: CCSD then ECP last): CCSD gradients via
Λ-equations + orbital-relaxation Z-vector (GRAD-06), and ECP gradients (GRAD-07).

Per D-04 + RESEARCH anti-pattern + the Phase-6 investment: CCSD Λ is ALREADY solved in Phase 6
(`solve_lambda`, 06-06). Phase-7 CCSD-grad CONSUMES λ + `make_rdm1`/`make_rdm2` (incl. `ao_repr`)
directly — it does NOT re-derive λ. It only re-enters CPHF for the orbital-relaxation Z-vector,
via the ONE 07-07 solver (its own `fvind`/RHS). This is the deliberate Phase-6 scope choice
paying off (CONTEXT specifics).

Per D-02 + 07-01: ECP-grad wires `ECPscalar_ipnuc` (cintx-READY) + `ECPscalar_iprinv` (a cintx
workstream item — gated) through the Phase-2 `CintxEcpEngine`, porting the `pyscf/grad/rhf.py`
ECP-gradient term. This closes the GTO-05 arc (Phase 2 wired ECP eval; Phase 7 wires ECP grad).

Both are FD-gated (D-01); numeric arms follow the 07-01 cintx-availability gate.

Purpose: Ship the last two gradient methods. Output: CCSD-grad + ECP-grad bodies + their FD gates.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gradients-geomopt/07-CONTEXT.md
@.planning/phases/07-gradients-geomopt/07-RESEARCH.md
@.planning/phases/07-gradients-geomopt/07-PATTERNS.md
@.planning/phases/07-gradients-geomopt/07-01-SUMMARY.md
@.planning/phases/07-gradients-geomopt/07-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CCSD gradient — consume Phase-6 λ + RDMs + orbital-relaxation Z-vector (GRAD-06)</name>
  <files>crates/pyscf-grad/src/ccsd.rs, crates/pyscf-grad/src/lib.rs, crates/pyscf-grad/tests/ccsd_verify_fd.rs</files>
  <read_first>
    - pyscf/grad/ccsd.py (the Λ-driven CCSD gradient port target + the orbital-relaxation Z-vector) + pyscf/grad/uccsd.py (spin-resolved, if UCCSD grad is in scope)
    - crates/pyscf-ccsd/src/lambda.rs (solve_lambda — CONSUMED, lambda.rs:411 per RESEARCH) + crates/pyscf-ccsd/src/rdm.rs (make_rdm1/make_rdm2 incl. ao_repr — rdm.rs:202,296) — GRAD-06 consumes these DIRECTLY, NO re-derivation
    - crates/pyscf-grad/src/cphf.rs (07-07 — the one solver; CCSD passes its own fvind/RHS for the orbital-relaxation Z-vector)
    - crates/pyscf-grad/src/rhf.rs (07-03 — the grad_elec assembly the relaxed CCSD density plugs into)
    - 07-PATTERNS.md §"crates/pyscf-grad/src/mp2.rs / ccsd.rs (Z-vector methods)" + §"Anti-Patterns" (re-deriving CCSD λ is WRONG) + 07-07-SUMMARY.md (the cphf::solve contract)
    - 07-RESEARCH.md §"Anti-Patterns to Avoid" (Re-deriving CCSD λ in Phase 7 is Wrong) + §"Standard Stack" (pyscf-ccsd shipped Phase 6)
  </read_first>
  <behavior>
    - Test (ccsd structural): CcsdGradients::kernel() returns (natm,3); it CONSUMES the Phase-6 solve_lambda + make_rdm1/make_rdm2 (no second lambda solve in pyscf-grad); the orbital-relaxation Z-vector goes through cphf::solve.
    - Test (ccsd_verify_fd): the analytical CCSD gradient matches FD of the CCSD as_scanner energy at <=1e-6 Ha/Bohr (or #[ignore]'d behind the cintx gate; structural always-on).
  </behavior>
  <action>
    Port `pyscf/grad/ccsd.py` into `crates/pyscf-grad/src/ccsd.rs`: build the gradient from the
    Phase-6 λ (`crates/pyscf-ccsd::solve_lambda`) + `make_rdm1`/`make_rdm2` (incl. `ao_repr`,
    06-06 D-03) — CONSUMED DIRECTLY, NO λ re-derivation (D-04 / RESEARCH anti-pattern). Form the
    orbital-relaxation Z-vector RHS + its `fvind` response operator and solve it through the ONE
    `cphf::solve` (07-07) with its own RHS (default max_cycle=50 unless the upstream ccsd.py
    overrides — match upstream). Assemble the gradient by plugging the CCSD relaxed density into
    the RHF `grad_elec` decomposition (07-03). Every reduction → `oracle_sum`/`oracle_dot`
    (Pitfall 1/2). Create `crates/pyscf-grad/tests/ccsd_verify_fd.rs` per the <behavior> block,
    INCLUDING a structural assertion that no second λ solve exists in pyscf-grad (it consumes the
    Phase-6 one). Numeric arm gated per the 07-01 cintx finding; structural always-on.
  </action>
  <verify>
    <automated>cargo test -p pyscf-grad --locked -- --test-threads=1 ccsd</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-grad/src/ccsd.rs` references `solve_lambda` / `make_rdm1` / `make_rdm2` from `pyscf-ccsd` (consumed, not re-implemented)
    - `crates/pyscf-grad/src/ccsd.rs` does NOT contain a second lambda-equation solver (`grep -ni "solve_lambda\|fn.*lambda" crates/pyscf-grad/src/ccsd.rs` shows only consumption/Z-vector, no re-derivation)
    - The orbital-relaxation Z-vector calls `cphf::solve` (the one 07-07 solver)
    - `cargo test -p pyscf-grad --locked ccsd` exits 0 (NO libxc pulled)
  </acceptance_criteria>
  <done>
    CCSD gradient consumes the Phase-6 λ + RDMs directly, re-enters CPHF only for the
    orbital-relaxation Z-vector via the one solver, returns (natm,3), and is FD-gated (GRAD-06).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ECP gradient hcore term (GRAD-07)</name>
  <files>crates/pyscf-grad/src/ecp.rs, crates/pyscf-grad/src/lib.rs, crates/pyscf-grad/tests/ecp_verify_fd.rs</files>
  <read_first>
    - pyscf/grad/rhf.py:109-143 (get_hcore: + ECPscalar_ipnuc; hcore_deriv: vrinv += ECPscalar_iprinv — the ECP-gradient branches)
    - crates/pyscf-gto/src/ecp_engine_cintx.rs (07-01 — the wired ecp_int1e_ipnuc path; ECPscalar_iprinv gated)
    - crates/pyscf-grad/src/rhf.rs (07-03 — the hcore_generator the ECP term extends)
    - 07-01-SUMMARY.md (which ECP-grad families are cintx-ready: ECPscalar_ipnuc yes, ECPscalar_iprinv gated)
    - 07-PATTERNS.md §"crates/pyscf-grad/src/ecp.rs (ECP gradient)" + 07-RESEARCH.md §"Code Examples → ECP gradient hcore term"
  </read_first>
  <behavior>
    - Test (ecp structural): the ECP-gradient hcore term wires ECPscalar_ipnuc through the engine and contributes to the gradient for an ECP-bearing molecule; ECPscalar_iprinv routes to the gated arm (cintx workstream).
    - Test (ecp_verify_fd): the analytical ECP gradient matches FD of the as_scanner energy at <=1e-6 Ha/Bohr for the ipnuc-only contribution (or #[ignore]'d behind the iprinv cintx gate; structural always-on).
  </behavior>
  <action>
    Port the `pyscf/grad/rhf.py:109-143` ECP-gradient term into `crates/pyscf-grad/src/ecp.rs`:
    the `get_hcore` `+ ECPscalar_ipnuc` contribution (cintx-READY per 07-01) and the `hcore_deriv`
    `+ ECPscalar_iprinv` per-atom contribution (a cintx workstream item — route to the gated arm,
    NOT a panic). Wire both through the Phase-2 `CintxEcpEngine` (the dispatch un-gated in 07-01).
    Fold the ECP term into the RHF `hcore_generator` (07-03) so ECP-bearing molecules get the ECP
    gradient contribution. Every reduction → `oracle_sum`/`oracle_dot`. Create
    `crates/pyscf-grad/tests/ecp_verify_fd.rs` per the <behavior> block (the ipnuc numeric arm
    un-gates now per 07-01; the iprinv-dependent numeric arm is `#[ignore]`'d behind the cintx
    workstream; structural always-on).
  </action>
  <verify>
    <automated>cargo test -p pyscf-grad --locked -- --test-threads=1 ecp</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-grad/src/ecp.rs` references `ECPscalar_ipnuc` (ready) and `ECPscalar_iprinv` (gated arm)
    - The ECP term is folded into the RHF hcore_generator path (ECP-bearing molecules get the contribution)
    - `crates/pyscf-grad/tests/ecp_verify_fd.rs` calls `verify_fd(disp=1e-4)` (ipnuc numeric un-gated per 07-01; iprinv numeric `#[ignore]`'d)
    - `cargo test -p pyscf-grad --locked ecp` exits 0 (NO libxc pulled)
  </acceptance_criteria>
  <done>
    ECP gradient hcore term wired (ipnuc ready, iprinv gated), folded into hcore_generator, and
    FD-gated (GRAD-07) — closing the GTO-05 arc.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-6 λ/RDM ↔ CCSD-grad | re-deriving λ instead of consuming it would diverge from the validated surface |
| ECP iprinv (missing in cintx) ↔ caller | the gated family must surface a clean error, not a panic or silent zero |
| orbital-relaxation Z-vector reductions ↔ de | bare `+=` makes the CCSD gradient thread-dependent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-25 | Tampering | re-deriving CCSD λ in pyscf-grad | mitigate | Consume the Phase-6 `solve_lambda` + RDMs directly (D-04); the structural test forbids a second λ solver |
| T-07-26 | Tampering | CCSD Z-vector / ECP reductions | mitigate | All contractions via `pyscf_algebra` gemm; reductions via `oracle_sum`/`oracle_dot` (Pitfall 1/2); FD gate catches drift |
| T-07-27 | Denial of Service | ECPscalar_iprinv missing in cintx | mitigate | Route the iprinv arm to a clean cintx-availability error (07-01), never a panic; ipnuc numeric un-gates now |
| T-07-28 | Tampering | CCSD-grad reuses the wrong CPHF | mitigate | Re-enter the ONE 07-07 `cphf::solve` for the orbital-relaxation Z-vector only (GRAD-10 single solver) |
| T-07-SC | Tampering | crate deps | mitigate | No new registry packages; dependency-wall lint enforces no cubecl-* |
</threat_model>

<verification>
- `cargo test -p pyscf-grad --locked -- --test-threads=1` green (scoped — NO libxc).
- CCSD-grad consumes Phase-6 λ/RDMs (no re-derivation); ECP-grad wired (ipnuc/iprinv).
- Both FD-gated; numeric arms per the 07-01 cintx finding.
</verification>

<success_criteria>
- CCSD gradient via Λ + orbital-relaxation Z-vector, consuming the Phase-6 surface directly
  (GRAD-06, D-04 — no λ re-derivation), FD-gated.
- ECP gradient (GRAD-07) wired through the Phase-2 ECP engine (ipnuc ready, iprinv gated),
  FD-gated — closing the GTO-05 arc.
</success_criteria>

<output>
Create `.planning/phases/07-gradients-geomopt/07-08-SUMMARY.md` when done. Record the CCSD-grad
+ ECP-grad APIs + which numeric arms are un-gated vs cintx-gated, so the PyO3 bridge (07-09) and
oracle close-out (07-10) wire the right gate structure.
</output>
