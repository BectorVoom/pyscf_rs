---
phase: 07-gradients-geomopt
plan: 05
type: execute
wave: 4
depends_on: ["07-03"]
files_modified:
  - crates/pyscf-grad/src/uhf.rs
  - crates/pyscf-grad/src/rks.rs
  - crates/pyscf-grad/src/uks.rs
  - crates/pyscf-grad/src/lib.rs
  - crates/pyscf-grad/tests/uhf_verify_fd.rs
  - crates/pyscf-grad/tests/rks_verify_fd.rs
  - crates/pyscf-grad/tests/uks_verify_fd.rs
autonomous: true
requirements: [GRAD-02, GRAD-03, GRAD-04]
must_haves:
  truths:
    - "UHF gradients return spin-resolved (natm, 3) analytical gradients (GRAD-02)"
    - "RKS gradients add the XC-potential derivative on the grid; grid_response=True adds the Becke-weight-derivative term (GRAD-03)"
    - "UKS gradients are the spin-resolved KS gradient (GRAD-04)"
    - "grid_response defaults OFF (upstream default) but is fully supported on request; none of UHF/RKS/UKS call CPHF (D-04)"
  artifacts:
    - path: "crates/pyscf-grad/src/uhf.rs"
      provides: "UHF spin-resolved grad_elec"
      contains: "grad_elec"
    - path: "crates/pyscf-grad/src/rks.rs"
      provides: "KS XC-potential derivative + grid_response weight-deriv term"
      contains: "grid_response"
    - path: "crates/pyscf-grad/src/uks.rs"
      provides: "UKS spin-resolved KS gradient"
      contains: "grad_elec"
  key_links:
    - from: "crates/pyscf-grad/src/rks.rs"
      to: "pyscf-grids byte-exact Becke weights + pyscf-dft numint"
      via: "grid_response weight-derivative term"
      pattern: "grid|weight|numint"
    - from: "crates/pyscf-grad/src/uhf.rs"
      to: "the RHF grad_elec decomposition (07-03)"
      via: "spin-resolved alpha/beta reuse"
      pattern: "grad_elec|alpha|beta"
---

<objective>
Broaden the variational-method gradients (D-09 order: UHF → RKS → UKS) by reusing the RHF
`grad_elec` decomposition from 07-03. Per D-04: all four are STATIONARY (the 2n+1 rule) and
do NOT call CPHF — `grid_response` is a grid-weight-derivative term, not a response solve.

Per CONTEXT "Claude's Discretion": port `pyscf/grad/uhf.py` (spin-resolved), `rks.py`/`uks.py`
(the XC-potential derivative on the grid + the `grid_response=True` Becke-weight-derivative
term, reusing the Phase-4 byte-exact `pyscf-grids` weights + `pyscf-dft` numint). `grid_response`
defaults OFF (upstream default) but is fully supported when requested (GRAD-03/04).

Each is FD-gated via the 07-02 `verify_fd` harness (D-01); the numeric arm follows the same
cintx-availability gate as RHF (07-01/07-03 SUMMARY).

Purpose: Complete the variational gradient surface. Output: UHF/RKS/UKS grad bodies + their
FD gates.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gradients-geomopt/07-CONTEXT.md
@.planning/phases/07-gradients-geomopt/07-RESEARCH.md
@.planning/phases/07-gradients-geomopt/07-PATTERNS.md
@.planning/phases/07-gradients-geomopt/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UHF spin-resolved gradient (GRAD-02)</name>
  <files>crates/pyscf-grad/src/uhf.rs, crates/pyscf-grad/src/lib.rs, crates/pyscf-grad/tests/uhf_verify_fd.rs</files>
  <read_first>
    - pyscf/grad/uhf.py (the port target — the spin-resolved grad_elec: dm0 = (dm_a, dm_b); the get_veff/get_jk over both spin channels; make_rdm1e per spin)
    - crates/pyscf-grad/src/rhf.rs (the RHF grad_elec decomposition from 07-03 — the structure UHF reuses)
    - crates/pyscf-mp2/src/ump2.rs (the spin-resolved α/β file-for-file-from-restricted-sibling shape)
    - crates/pyscf-grad/src/verify_fd.rs (the FD harness)
    - 07-PATTERNS.md §"crates/pyscf-grad/src/uhf.rs / rks.rs / uks.rs" + 07-03-SUMMARY.md (RHF grad_elec API + cintx gate status)
  </read_first>
  <behavior>
    - Test (uhf structural): UhfGradients::kernel() returns a (natm, 3) array; the spin-resolved dm0=(dm_a,dm_b) both contribute.
    - Test (uhf_verify_fd): the analytical UHF gradient matches the central-difference FD of the UHF as_scanner energy at <=1e-6 Ha/Bohr (or #[ignore]'d behind the cintx gate, structural always-on).
  </behavior>
  <action>
    Port `pyscf/grad/uhf.py` into `crates/pyscf-grad/src/uhf.rs`, mirroring the RHF `grad_elec`
    decomposition (07-03) but spin-resolved: `dm0 = (dm_a, dm_b)`, the `get_veff`/`get_jk` summed
    over both spin channels, and the energy-weighted RDM `dme0` per spin. The Hellmann-Feynman
    (`hcore_deriv·(dm_a+dm_b)`) + Pulay (overlap term over both spins) structure is identical to
    RHF. Per D-04: NO CPHF call. Every reduction → `oracle_sum`/`oracle_dot` (Pitfall 1/2). Create
    `crates/pyscf-grad/tests/uhf_verify_fd.rs` per the <behavior> block (numeric arm gated per the
    07-01/07-03 cintx finding; structural always-on).
  </action>
  <verify>
    <automated>cargo test -p pyscf-grad --locked -- --test-threads=1 uhf</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-grad/src/uhf.rs` contains a spin-resolved `grad_elec`
    - `cargo test -p pyscf-grad --locked uhf` exits 0 (NO libxc pulled)
    - `crates/pyscf-grad/tests/uhf_verify_fd.rs` calls `verify_fd(disp=1e-4)` with `1e-6` tolerance (numeric arm un-gated or `#[ignore]`'d per the cintx finding)
    - No CPHF call in uhf.rs (`grep -i cphf crates/pyscf-grad/src/uhf.rs` returns nothing)
  </acceptance_criteria>
  <done>UHF spin-resolved gradient compiles, returns (natm,3), and is FD-gated; no CPHF (D-04).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RKS + UKS gradients with grid_response (GRAD-03/04)</name>
  <files>crates/pyscf-grad/src/rks.rs, crates/pyscf-grad/src/uks.rs, crates/pyscf-grad/src/lib.rs, crates/pyscf-grad/tests/rks_verify_fd.rs, crates/pyscf-grad/tests/uks_verify_fd.rs</files>
  <read_first>
    - pyscf/grad/rks.py + pyscf/grad/uks.py (the port targets: the XC-potential derivative on the grid + the grid_response=True Becke-weight-derivative term)
    - crates/pyscf-grad/src/rhf.rs (07-03) + crates/pyscf-grad/src/uhf.rs (Task 1 — the variational base RKS/UKS extend)
    - crates/pyscf-grids/src/lib.rs + crates/pyscf-dft numint (Phase-4 byte-exact Becke weights — the grid_response weight-deriv reuses these)
    - 07-PATTERNS.md §"crates/pyscf-grad/src/uhf.rs / rks.rs / uks.rs" (grid_response off by default; NOT a CPHF consumer — D-04 / Pitfall 5)
    - 07-RESEARCH.md §"Pitfall 5" (CPHF for the wrong methods) + §"Anti-Patterns" (grid_response is a grid-weight term, not a response solve)
  </read_first>
  <behavior>
    - Test (rks structural): RksGradients::kernel() returns (natm,3); grid_response defaults OFF; kernel with grid_response=True adds the weight-derivative term and changes the result.
    - Test (rks_verify_fd): the analytical RKS gradient (grid_response=True) matches FD of the RKS as_scanner energy at <=1e-6 Ha/Bohr (or #[ignore]'d behind the cintx gate; structural always-on). The XC functional under test must NOT pull libxc_rs (use a pure-Rust/xcfun-available functional per the Phase-4 numint surface).
    - Test (uks_verify_fd): analogous spin-resolved.
  </behavior>
  <action>
    Port `pyscf/grad/rks.py` into `crates/pyscf-grad/src/rks.rs`: the variational base (reuse RHF
    `grad_elec`) PLUS the XC-potential derivative evaluated on the DFT grid (via `pyscf-dft` numint
    + `pyscf-grids` byte-exact Becke weights) PLUS the `grid_response=True` Becke-weight-derivative
    term. `grid_response` defaults OFF (upstream default, GRAD-03) — fully supported on request.
    Per D-04 / Pitfall 5: RKS does NOT call CPHF — `grid_response` is a grid-weight-derivative term,
    NOT a response solve. Port `pyscf/grad/uks.py` into `crates/pyscf-grad/src/uks.rs` as the
    spin-resolved sibling. Every reduction → `oracle_sum`/`oracle_dot`. Create
    `crates/pyscf-grad/tests/rks_verify_fd.rs` + `uks_verify_fd.rs` per the <behavior> block —
    using a functional that does NOT compile libxc_rs (scope the test to keep `cargo test -p
    pyscf-grad` off the 6h libxc compile).
  </action>
  <verify>
    <automated>cargo test -p pyscf-grad --locked -- --test-threads=1 rks uks</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-grad/src/rks.rs` contains `grid_response` handling (defaults off) + the XC-potential derivative
    - `crates/pyscf-grad/src/uks.rs` contains the spin-resolved KS gradient
    - `grep -i cphf crates/pyscf-grad/src/rks.rs crates/pyscf-grad/src/uks.rs` returns nothing (D-04 — not CPHF consumers)
    - `cargo test -p pyscf-grad --locked rks uks` exits 0 and does NOT trigger a libxc_rs compile
    - rks_verify_fd / uks_verify_fd call `verify_fd(disp=1e-4)` (numeric un-gated or `#[ignore]`'d per the cintx finding)
  </acceptance_criteria>
  <done>
    RKS + UKS gradients compile with grid_response (off by default, fully supported), reuse the
    byte-exact Phase-4 grid weights, call NO CPHF, and are FD-gated without pulling libxc.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grid weights ↔ grid_response term | a wrong weight-derivative silently mis-computes the KS gradient |
| spin-channel reduction ↔ de result | bare `+=` over α/β makes the gradient thread-dependent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-14 | Tampering | spin-channel + grid reductions | mitigate | Every α/β + grid reduction materializes then `oracle_sum`/`oracle_dot` (Pitfall 1/2); FD gate catches drift |
| T-07-15 | Tampering | CPHF wrongly called for KS grad | mitigate | D-04 / Pitfall 5: assert NO cphf reference in rks/uks/uhf — `grid_response` is a grid-weight term, not a response solve |
| T-07-16 | Denial of Service | a libxc-backed functional in the FD test | mitigate | Scope the KS-grad FD test to a non-libxc functional so `cargo test -p pyscf-grad` never triggers the ~6h libxc compile (user memory) |
| T-07-SC | Tampering | crate deps | mitigate | No new registry packages; dependency-wall lint enforces no cubecl-* |
</threat_model>

<verification>
- `cargo test -p pyscf-grad --locked -- --test-threads=1` green (scoped — NO libxc).
- UHF/RKS/UKS FD gates present (numeric un-gated or `#[ignore]`'d per cintx); no CPHF in any.
- grid_response defaults off, fully supported on request.
</verification>

<success_criteria>
- UHF (GRAD-02), RKS with grid_response (GRAD-03), and UKS (GRAD-04) gradients return
  analytical `(natm,3)` arrays, reuse the RHF decomposition + byte-exact Phase-4 grid weights,
  call NO CPHF (D-04), and are FD-gated (D-01).
</success_criteria>

<output>
Create `.planning/phases/07-gradients-geomopt/07-05-SUMMARY.md` when done. Record the UHF/RKS/UKS
gradient APIs + the confirmed "no CPHF / grid_response is a weight-derivative" decision (D-04).
</output>
