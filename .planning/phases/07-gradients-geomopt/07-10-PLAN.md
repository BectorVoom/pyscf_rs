---
phase: 07-gradients-geomopt
plan: 10
type: execute
wave: 8
depends_on: ["07-09"]
files_modified:
  - crates/pyscf-oracle/src/runner.rs
  - crates/pyscf-oracle/src/grad_oracle.rs
  - .github/workflows/ci.yml
  - .planning/phases/07-gradients-geomopt/07-VALIDATION.md
autonomous: true
requirements: [GRAD-01, GRAD-02, GRAD-03, GRAD-04, GRAD-05, GRAD-06, GRAD-07, GEOMOPT-01, GEOMOPT-07]
must_haves:
  truths:
    - "pyscf-oracle registers nuc_grad_* method names (register-but-defer-dispatch, mirrors MP2/CCSD); byte-identity arms are #[ignore]'d / workflow_dispatch"
    - "CI has always-on grad-structural + geomopt-structural jobs (scoped cargo test -p, no python, no libxc)"
    - "CI has the GEOMOPT-01 no-runtime-dep proof: pip uninstall -y geometric pyberny && python -c import pyscf.geomopt; optimize(mf) (runs in CI per D-05, NOT workflow_dispatch)"
    - "workflow_dispatch arms: upstream byte-identity grad (<=1e-7 Ha/Bohr) + geomopt trajectory parity"
  artifacts:
    - path: "crates/pyscf-oracle/src/grad_oracle.rs"
      provides: "grad oracle fixtures: always-on dispatch-layer arms + #[ignore]'d byte-identity arms"
      contains: "nuc_grad"
    - path: ".github/workflows/ci.yml"
      provides: "grad-structural + geomopt-structural always-on + geomopt-no-runtime-dep + workflow_dispatch upstream/trajectory arms"
      contains: "grad-structural"
  key_links:
    - from: ".github/workflows/ci.yml grad-structural"
      to: "cargo test -p pyscf-grad -p pyscf-geomopt -p pyscf-oracle"
      via: "scoped always-on FD/structural gate (no python, no libxc)"
      pattern: "cargo test -p pyscf-grad"
    - from: ".github/workflows/ci.yml geomopt-no-runtime-dep"
      to: "pip uninstall geometric pyberny"
      via: "GEOMOPT-01 proof in CI"
      pattern: "pip uninstall.*geometric.*pyberny"
---

<objective>
Close out the phase Nyquist contract: register `pyscf-oracle` grad fixtures (register-but-defer-
dispatch, mirroring the MP2/CCSD precedent) and wire the CI gate structure (D-01/D-05).

Per D-01: the always-on daily gate is the FD `verify_fd` (≤1e-6 Ha/Bohr) + the structural arms
(`single_cphf_impl`, `atmlst`, optimizer convergence) — scoped `cargo test -p pyscf-grad
-p pyscf-geomopt -p pyscf-oracle`, NO python, NO libxc. Upstream byte-identity (≤1e-7 Ha/Bohr vs
`pyscf/grad/*`) + geomopt trajectory parity are `workflow_dispatch` arms (the 02-10/05-08/06-11
precedent — the sandbox can't run upstream PySCF). Per D-05 + GEOMOPT-01: the `pip uninstall
geometric pyberny && python -c "import pyscf.geomopt; pyscf.geomopt.optimize(mf)"` no-runtime-dep
proof runs in CI (NOT workflow_dispatch).

Per 07-PATTERNS.md: mirror the `mp2-structural` + `mp2-oracle-upstream-manual` job pair. The
dependency wall (`check_dependency_wall.rs`) needs ZERO edits (denylist auto-scan). Constraints:
never `--features libxc` (6h compile); `--test-threads=1`; `setup-sibling-crates` provides cintx;
`if: github.event_name == 'workflow_dispatch'` on the upstream arms.

Purpose: Lock the phase validation contract + the CI gates. Output: oracle fixtures + the CI jobs
+ `nyquist_compliant: true`.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gradients-geomopt/07-CONTEXT.md
@.planning/phases/07-gradients-geomopt/07-RESEARCH.md
@.planning/phases/07-gradients-geomopt/07-PATTERNS.md
@.planning/phases/07-gradients-geomopt/07-VALIDATION.md
@.planning/phases/07-gradients-geomopt/07-01-SUMMARY.md
@.planning/phases/07-gradients-geomopt/07-08-SUMMARY.md
@.planning/phases/07-gradients-geomopt/07-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pyscf-oracle grad fixtures (register-but-defer-dispatch)</name>
  <files>crates/pyscf-oracle/src/runner.rs, crates/pyscf-oracle/src/grad_oracle.rs</files>
  <read_first>
    - crates/pyscf-oracle/src/runner.rs (KNOWN_METHODS at line 47 — currently 24 entries; the dispatch at line 127; the catalogue test at line 1014 asserting len()==24; the MP2/CCSD register-but-defer-dispatch precedent)
    - 07-RESEARCH.md §"Wave 0 Gaps" (pyscf-oracle grad fixtures — nuc_grad_* method names; byte-identity arms #[ignore]'d / workflow_dispatch) + §"Validation Architecture" Req→Test map
    - STATE.md line 305 (the 06-11 CCSD oracle close-out: 6 method names registered, KNOWN_METHODS=24, ccsd_oracle.rs with always-on dispatch-layer arms + #[cfg(feature=python)]/#[ignore]'d byte-identity arms, ORACLE_NO_LIBXC default pulls no libxc)
    - 07-PATTERNS.md §"CI gate shape" + the mp2/ccsd oracle precedent
  </read_first>
  <action>
    Register the grad oracle method names in `crates/pyscf-oracle/src/runner.rs` `KNOWN_METHODS`
    (mirroring the MP2/CCSD register-but-defer-dispatch precedent): `nuc_grad_rhf`, `nuc_grad_uhf`,
    `nuc_grad_rks`, `nuc_grad_uks`, `nuc_grad_mp2`, `nuc_grad_ccsd`, `nuc_grad_ecp`, plus
    `geomopt_h2o` (the optimizer fixture). Update the catalogue-len test (currently `==24`) to the
    new count. Create `crates/pyscf-oracle/src/grad_oracle.rs` with:
    - ALWAYS-ON dispatch-layer arms (the fixture wiring + shape assertions — no python, no libxc),
      mirroring the MP2/CCSD always-on arms.
    - `#[cfg(feature = "python")]` + `#[ignore]`'d byte-identity arms (≤1e-7 Ha/Bohr vs
      `pyscf/grad/*`) for the `workflow_dispatch` upstream cross-check.
    Keep the default `cargo test -p pyscf-oracle` pulling NO libxc (the `ORACLE_NO_LIBXC` discipline
    from 06-11). For families gated on the cintx workstream (per 07-01-SUMMARY), the corresponding
    numeric fixture arm is `#[ignore]`'d behind that prerequisite.
  </action>
  <verify>
    <automated>cargo test -p pyscf-oracle --locked -- --test-threads=1 grad geomopt</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-oracle/src/runner.rs` `KNOWN_METHODS` contains the `nuc_grad_*` + `geomopt_*` names; the catalogue-len test is updated to the new count
    - `crates/pyscf-oracle/src/grad_oracle.rs` exists with always-on dispatch-layer arms + `#[cfg(feature="python")]`/`#[ignore]`'d byte-identity arms
    - `cargo test -p pyscf-oracle --locked grad geomopt` exits 0 and pulls NO libxc (default ORACLE_NO_LIBXC)
  </acceptance_criteria>
  <done>
    Grad + geomopt oracle fixtures registered (always-on dispatch arms; byte-identity arms gated),
    mirroring the MP2/CCSD register-but-defer-dispatch precedent.
  </done>
</task>

<task type="auto">
  <name>Task 2: CI gate wiring — always-on grad/geomopt structural + GEOMOPT-01 proof + workflow_dispatch upstream/trajectory arms</name>
  <files>.github/workflows/ci.yml, .planning/phases/07-gradients-geomopt/07-VALIDATION.md</files>
  <read_first>
    - .github/workflows/ci.yml (the mp2-structural job at lines 463-475 + mp2-oracle-upstream-manual at 493-513 — the always-on structural + workflow_dispatch upstream precedent; also ccsd-structural 532-569; the oracle-determinism matrix; the setup-sibling-crates action; the python313t-*-smoke workflow_dispatch jobs)
    - 07-PATTERNS.md §".github/workflows/ci.yml (new grad/geomopt gate jobs)" (the three gate groups + the exact job shape + the constraints: never --features libxc, --test-threads=1, setup-sibling-crates, workflow_dispatch guard)
    - 07-RESEARCH.md §"Wave 0 Gaps" (CI arm structure) + §"Validation Architecture" §"Sampling Rate"
    - CONTEXT D-05 (the GEOMOPT-01 pip-uninstall proof runs in CI, NOT workflow_dispatch) + 07-09-SUMMARY.md (the Python entry points the proof exercises)
    - 07-VALIDATION.md §"Manual-Only Verifications" (which arms are workflow_dispatch)
  </read_first>
  <action>
    Add to `.github/workflows/ci.yml` (mirroring the `mp2-structural`/`mp2-oracle-upstream-manual`
    job pair shape — copy verbatim, rename):
    1. ALWAYS-ON `grad-structural` job: `cargo test -p pyscf-grad -p pyscf-geomopt -p pyscf-oracle
       --locked -- --test-threads=1` (the FD `verify_fd` ≤1e-6 + `atmlst` + `single_cphf_impl`
       structural + the H2O→equilibrium self-contained convergence + `bmatrix`/`rfo`/`conv_defaults`).
       `needs: [build-default]`, `setup-sibling-crates` action (provides cintx), NO python, NO libxc.
    2. CI `geomopt-no-runtime-dep` job (GEOMOPT-01, per D-05 — runs in CI, NOT workflow_dispatch):
       `pip uninstall -y geometric pyberny || true` then
       `python -c "import pyscf.geomopt; from pyscf import gto, scf; ...; pyscf.geomopt.optimize(mf)"`
       succeeds — proving NO geometric/pyberny runtime dependency. Build the abi3 wheel / import the
       native module as the existing python smoke jobs do.
    3. `workflow_dispatch` `grad-oracle-upstream-manual` job (`if: github.event_name ==
       'workflow_dispatch'`): `pip install "numpy>=1.26" "pyscf>=2.5"` then `cargo test -p pyscf-oracle
       --features python --locked -- --test-threads=1` (upstream byte-identity ≤1e-7 Ha/Bohr grad +
       geomopt trajectory/stationary-point parity vs geometric_solver).

    Constraints (user memory + precedent): NEVER `--features libxc` (6h compile); `--test-threads=1`;
    mirror the `if: github.event_name == 'workflow_dispatch'` guard on the upstream arms (default
    runners have no upstream PySCF/geomeTRIC). Validate the YAML.

    Finally: update `.planning/phases/07-gradients-geomopt/07-VALIDATION.md` — fill the Task ID
    column in the Per-Task Verification Map, check the Wave 0 Requirements + Validation Sign-Off
    boxes, and set `nyquist_compliant: true` + `wave_0_complete: true` in the frontmatter.
  </action>
  <verify>
    <automated>python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml')); print('YAML_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a `grad-structural` always-on job running `cargo test -p pyscf-grad -p pyscf-geomopt -p pyscf-oracle` with `--test-threads=1` and NO `--features libxc`
    - `.github/workflows/ci.yml` contains a `geomopt-no-runtime-dep` job with `pip uninstall -y geometric pyberny` + a `python -c "import pyscf.geomopt; ...optimize(mf)"` success check (NOT guarded by workflow_dispatch)
    - `.github/workflows/ci.yml` contains a `grad-oracle-upstream-manual` job guarded by `if: github.event_name == 'workflow_dispatch'`
    - `grep -c "features libxc\|--features libxc" .github/workflows/ci.yml` shows NO new libxc-enabled grad/geomopt job
    - The YAML parses (`YAML_OK`)
    - `.planning/phases/07-gradients-geomopt/07-VALIDATION.md` frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`
  </acceptance_criteria>
  <done>
    CI has the always-on grad/geomopt structural gate, the GEOMOPT-01 no-runtime-dep proof, and the
    workflow_dispatch upstream byte-identity + trajectory-parity arms; the phase Nyquist contract is
    closed (`nyquist_compliant: true`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner ↔ cintx sibling crate | the setup-sibling-crates action provides cintx; a missing/wrong cintx breaks the gate |
| CI runner ↔ libxc compile | a workspace-wide cargo test would trigger the ~6h libxc compile |
| upstream-PySCF arm ↔ default runner | default runners lack upstream PySCF/geomeTRIC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-35 | Denial of Service | workspace-wide cargo test triggering libxc | mitigate | Scope every grad/geomopt CI job to `-p pyscf-grad -p pyscf-geomopt -p pyscf-oracle`; NEVER `--features libxc` (user memory — 6h compile freeze) |
| T-07-36 | Tampering | non-deterministic FD/oracle results across threads | mitigate | `--test-threads=1` + the `release-oracle` ordered-reduction discipline (oracle-determinism matrix) |
| T-07-37 | Tampering | upstream arm leaking into the daily gate | mitigate | `if: github.event_name == 'workflow_dispatch'` guard on the upstream byte-identity + trajectory arms (default runners have no upstream PySCF/geomeTRIC) |
| T-07-38 | Tampering | a hidden geometric/pyberny runtime dep | mitigate | The `geomopt-no-runtime-dep` CI job `pip uninstall`s them and proves `pyscf.geomopt.optimize` still works (GEOMOPT-01) |
| T-07-SC | Tampering | CI deps / sibling crate | mitigate | No new registry packages; setup-sibling-crates provides cintx; dependency-wall lint stays green |
</threat_model>

<verification>
- `cargo test -p pyscf-oracle --locked -- --test-threads=1` green (scoped — NO libxc).
- The new CI jobs parse (`YAML_OK`) and never enable libxc for grad/geomopt.
- 07-VALIDATION.md `nyquist_compliant: true`, `wave_0_complete: true`.
</verification>

<success_criteria>
- pyscf-oracle grad + geomopt fixtures registered (always-on dispatch arms; byte-identity arms gated).
- CI: always-on grad/geomopt structural gate + the GEOMOPT-01 no-runtime-dep proof + the
  workflow_dispatch upstream byte-identity (≤1e-7 Ha/Bohr) + trajectory-parity arms.
- The phase Nyquist validation contract is closed.
</success_criteria>

<output>
Create `.planning/phases/07-gradients-geomopt/07-10-SUMMARY.md` when done. Record the registered
oracle method names, the CI job names + their gating (always-on vs workflow_dispatch), and the
final phase-gate human-verify run (the upstream byte-identity + trajectory + pip-uninstall arms).
</output>
