---
phase: 07-gradients-geomopt
plan: 06
type: execute
wave: 4
depends_on: ["07-04"]
files_modified:
  - crates/pyscf-geomopt/src/checkpoint.rs
  - crates/pyscf-geomopt/src/shims.rs
  - crates/pyscf-geomopt/src/lib.rs
  - crates/pyscf-geomopt/tests/checkpoint_resume.rs
  - crates/pyscf-geomopt/tests/shim_parity.rs
autonomous: true
requirements: [GEOMOPT-02, GEOMOPT-03, GEOMOPT-05]
must_haves:
  truths:
    - "HDF5 checkpoint of optimizer state allows resuming a partially-converged optimization (GEOMOPT-05)"
    - "geometric_solver and berny_solver shim entry points both delegate to the ONE native engine (GEOMOPT-02/03)"
    - "The shims mirror the upstream call signatures (conv_params, callback, maxsteps, constraints) and return shapes (kernel -> (conv, mol); optimize -> mol)"
    - "The constraints kwarg raises a clear error, never a silent no-op (D-07, GEOMOPT-EXT-01 deferred)"
  artifacts:
    - path: "crates/pyscf-geomopt/src/checkpoint.rs"
      provides: "HDF5 optimizer-state dump/load via pyscf-chkfile hdf5 alias"
      contains: "hdf5"
    - path: "crates/pyscf-geomopt/src/shims.rs"
      provides: "geometric_solver / berny_solver entry-point parity over the one engine"
      contains: "constraints"
    - path: "crates/pyscf-geomopt/tests/checkpoint_resume.rs"
      provides: "HDF5 checkpoint round-trip / resume (GEOMOPT-05)"
      contains: "resume"
  key_links:
    - from: "crates/pyscf-geomopt/src/checkpoint.rs"
      to: "pyscf_chkfile::hdf5"
      via: "the re-exported hdf5 alias (no new dep)"
      pattern: "pyscf_chkfile::hdf5|hdf5::Group"
    - from: "crates/pyscf-geomopt/src/shims.rs"
      to: "the native optimize() engine (07-04)"
      via: "both shims are thin aliases over the one engine"
      pattern: "optimize|GeometryOptimizer"
---

<objective>
Complete the geomopt API surface around the native engine from 07-04: the HDF5 optimizer-state
checkpoint (GEOMOPT-05), and the `geometric_solver`/`berny_solver` shim entry points
(GEOMOPT-02/03) — both thin aliases over the ONE native engine (D-06), mirroring the upstream
call signatures (D-07).

Per D-07: the shims accept the same kwargs upstream accepts (`conv_params` dict, `callback`,
`maxsteps`, `constraints`) and return the same shapes (`kernel` → `(conv, mol)`; `optimize` →
optimized `Mole`). The `constraints` kwarg is accepted-but-unsupported — a CLEAR ERROR
(GEOMOPT-EXT-01 deferred), never a silent no-op. Per GEOMOPT-05: the HDF5 checkpoint reuses the
`pyscf-chkfile` `hdf5` re-export alias (no new dep — the sole-owner discipline).

NOTE: this is the Rust-side shim/checkpoint layer (in `pyscf-geomopt`, pyo3-free). The Python
`pyscf.geomopt.geometric_solver.optimize` / `berny_solver.optimize` entry points + the
no-runtime-dep proof (GEOMOPT-01) land in the PyO3 bridge plan (07-09).

Purpose: Round out the optimizer API + persistence. Output: checkpoint + shims + their tests.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gradients-geomopt/07-CONTEXT.md
@.planning/phases/07-gradients-geomopt/07-RESEARCH.md
@.planning/phases/07-gradients-geomopt/07-PATTERNS.md
@.planning/phases/07-gradients-geomopt/07-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HDF5 optimizer-state checkpoint + resume (GEOMOPT-05)</name>
  <files>crates/pyscf-geomopt/src/checkpoint.rs, crates/pyscf-geomopt/src/lib.rs, crates/pyscf-geomopt/tests/checkpoint_resume.rs</files>
  <read_first>
    - crates/pyscf-chkfile/src/lib.rs:37 (`pub use hdf5_metno as hdf5;` — name `pyscf_chkfile::hdf5::Group`, no own hdf5-metno dep)
    - crates/pyscf-scf/src/chkfile.rs (the dump/load round-trip shape to mirror)
    - crates/pyscf-geomopt/src/lib.rs (the GeometryOptimizer state from 07-04 — geometry, trust radius, BFGS Hessian, step count)
    - 07-PATTERNS.md §"HDF5 checkpoint (apply to: pyscf-geomopt/src/checkpoint.rs)" + §"crates/pyscf-geomopt/* — Cargo.toml deps (NO own hdf5)"
    - CONTEXT D-07 (HDF5 optimizer-state checkpoint reuses the pyscf-chkfile hdf5 alias — no new dep)
  </read_first>
  <behavior>
    - Test (checkpoint_resume): dump the optimizer state (current geometry, trust radius, BFGS approximate Hessian, accumulated step history, step counter) to an HDF5 group; load it back; assert the loaded state byte-matches the dumped state; resume the optimization from the loaded state and assert it converges to the same stationary point as an uninterrupted run.
  </behavior>
  <action>
    Create `crates/pyscf-geomopt/src/checkpoint.rs`: dump/load the `GeometryOptimizer` state to/from
    an HDF5 group using `pyscf_chkfile::hdf5::Group` (the re-exported alias — NO own `hdf5-metno`
    dep, per GEOMOPT-05 + D-07 sole-owner discipline). Persist: the current Cartesian geometry, the
    trust radius, the BFGS approximate Hessian, the accumulated step/internal-coordinate history,
    and the step counter — enough to resume a partially-converged optimization. Mirror
    `crates/pyscf-scf/src/chkfile.rs` for the dump/load round-trip shape. Wire a `resume_from`
    path into `lib.rs`'s optimize loop. Create `crates/pyscf-geomopt/tests/checkpoint_resume.rs`
    per the <behavior> block (use a tmp HDF5 file; no external package).
  </action>
  <verify>
    <automated>cargo test -p pyscf-geomopt --locked -- --test-threads=1 checkpoint_resume</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-geomopt/src/checkpoint.rs` references `pyscf_chkfile::hdf5` (NOT a direct `hdf5_metno` dep)
    - `grep -E "hdf5-metno|hdf5_metno" crates/pyscf-geomopt/Cargo.toml` returns nothing (routes through pyscf-chkfile)
    - `crates/pyscf-geomopt/tests/checkpoint_resume.rs` asserts a dump→load round-trip + a resume-converges-to-same-point assertion
    - `cargo test -p pyscf-geomopt --locked checkpoint_resume` exits 0
  </acceptance_criteria>
  <done>
    Optimizer state checkpoints to HDF5 via the chkfile alias and resumes a partially-converged run
    to the same stationary point (GEOMOPT-05).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: geometric_solver + berny_solver shims (one engine) + clear-error constraints (GEOMOPT-02/03, D-07)</name>
  <files>crates/pyscf-geomopt/src/shims.rs, crates/pyscf-geomopt/src/lib.rs, crates/pyscf-geomopt/tests/shim_parity.rs</files>
  <read_first>
    - pyscf/geomopt/geometric_solver.py:96-192 (kernel signature: method, constraints=None, callback=None, maxsteps=100, **kwargs; the method dispatch GradScanner/GradientsBase/nuc_grad_method; returns (conv, engine.mol); optimize returns kernel(...)[1])
    - pyscf/geomopt/berny_solver.py (the berny shim API to mirror — a thin alias)
    - crates/pyscf-geomopt/src/lib.rs (the native optimize() engine from 07-04 — both shims delegate to it)
    - 07-PATTERNS.md §"crates/pyscf-geomopt/* — Shim API to mirror (D-07)" + §"Anti-patterns" (no second optimizer for berny; constraints raises a clear error)
    - CONTEXT D-06 (berny is a thin alias over the same engine) + D-07 (mirror call sigs; constraints = clear error, never silent no-op)
  </read_first>
  <behavior>
    - Test (shim_parity): both geometric_solver and berny_solver shim entry points accept (conv_params, callback, maxsteps, constraints) and delegate to the SAME native optimize engine; kernel returns a (conv, mol) shape; optimize returns the optimized mol. There is NO second optimizer implementation (berny is a thin alias).
    - Test (constraints_error): passing a non-None constraints kwarg raises a clear, descriptive error (GEOMOPT-EXT-01 deferred) — never silently ignored.
  </behavior>
  <action>
    Create `crates/pyscf-geomopt/src/shims.rs`: `geometric_solver`-shaped and `berny_solver`-shaped
    entry points that BOTH delegate to the ONE native `optimize` engine (07-04) — NO second
    optimizer (D-06 / anti-pattern). Mirror the upstream `kernel(method, constraints=None,
    callback=None, maxsteps=100, **kwargs)` signature (D-07): dispatch a `GradScanner` /
    `GradientsBase` / `nuc_grad_method()` input; cap/validate `maxsteps` (default 100); thread the
    `conv_params` dict + `callback`; return `(conv, mol)` from `kernel` and the optimized `Mole`
    from `optimize`. The `constraints` kwarg, when non-None, raises a CLEAR error (e.g.
    `GeomoptError::ConstraintsUnsupported` citing GEOMOPT-EXT-01 deferred) — NEVER a silent no-op
    (D-07 / RESEARCH Security Domain T-pattern). Create `crates/pyscf-geomopt/tests/shim_parity.rs`
    per the <behavior> block, including the structural assertion that berny + geometric delegate to
    the same engine (e.g. both call the same `optimize` fn / a single-engine marker).
  </action>
  <verify>
    <automated>cargo test -p pyscf-geomopt --locked -- --test-threads=1 shim_parity constraints</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-geomopt/src/shims.rs` contains both a geometric_solver-shaped and a berny_solver-shaped entry, both delegating to the single native `optimize`
    - A test asserts a non-None `constraints` kwarg raises a clear error (not a no-op)
    - A structural test asserts there is ONE optimizer engine (berny is a thin alias — no second implementation)
    - `cargo test -p pyscf-geomopt --locked shim_parity constraints` exits 0
  </acceptance_criteria>
  <done>
    geometric_solver + berny_solver shims mirror the upstream call signatures, delegate to the one
    native engine (GEOMOPT-02/03), and reject `constraints` with a clear error (D-07).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user kwargs (maxsteps, constraints, conv_params) ↔ shims | unbounded maxsteps or an unsupported constraints kwarg crosses here |
| HDF5 checkpoint file ↔ resume | a corrupt/incompatible checkpoint must fail cleanly, not silently mis-resume |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-17 | Tampering | `constraints` kwarg silently ignored | mitigate | Raise a clear `ConstraintsUnsupported` error (D-07, GEOMOPT-EXT-01 deferred) — never a silent no-op that mis-optimizes |
| T-07-18 | Denial of Service | unbounded `maxsteps` in the shims | mitigate | Default `maxsteps=100`; cap/validate at the shim boundary (RESEARCH Security Domain) |
| T-07-19 | Tampering | a corrupt/incompatible HDF5 checkpoint | mitigate | Validate the loaded state shape/version on resume; fail with a clear error rather than resuming from garbage |
| T-07-20 | Tampering | a second berny optimizer drifting from geomeTRIC | mitigate | berny is a thin ALIAS over the one engine (D-06); the structural shim_parity test forbids a second implementation |
| T-07-SC | Tampering | crate deps | mitigate | No new registry packages; HDF5 via the chkfile alias (no own hdf5-metno); dependency-wall lint enforces no cubecl-* |
</threat_model>

<verification>
- `cargo test -p pyscf-geomopt --locked -- --test-threads=1` green (scoped — NO libxc).
- checkpoint round-trip + resume + shim parity + constraints-error tests pass.
- `grep hdf5-metno crates/pyscf-geomopt/Cargo.toml` empty (routes through pyscf-chkfile).
</verification>

<success_criteria>
- HDF5 optimizer-state checkpoint resumes a partially-converged run (GEOMOPT-05).
- `geometric_solver`/`berny_solver` shims mirror the upstream signatures, delegate to the ONE
  native engine (GEOMOPT-02/03), and reject `constraints` with a clear error (D-07).
</success_criteria>

<output>
Create `.planning/phases/07-gradients-geomopt/07-06-SUMMARY.md` when done. Record the shim entry
signatures + the checkpoint schema so the PyO3 bridge (07-09) wires the Python entry points.
</output>
