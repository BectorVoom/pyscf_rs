---
phase: 07-gradients-geomopt
plan: 04
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - crates/pyscf-geomopt/Cargo.toml
  - crates/pyscf-geomopt/src/lib.rs
  - crates/pyscf-geomopt/src/internals.rs
  - crates/pyscf-geomopt/src/bmatrix.rs
  - crates/pyscf-geomopt/src/rfo.rs
  - crates/pyscf-geomopt/src/backtransform.rs
  - crates/pyscf-geomopt/src/converge.rs
  - crates/pyscf-geomopt/tests/bmatrix.rs
  - crates/pyscf-geomopt/tests/rfo.rs
  - crates/pyscf-geomopt/tests/conv_defaults.rs
  - crates/pyscf-geomopt/tests/h2o_equilibrium.rs
autonomous: true
requirements: [GEOMOPT-04, GEOMOPT-06, GEOMOPT-07]
must_haves:
  truths:
    - "The native BFGS+RFO optimizer drives an RHF GradScanner from a perturbed H2O to its equilibrium geometry (self-contained, no external package)"
    - "Wilson B-matrix construction for redundant internals + RFO step with negative-eigenvalue tracking match geomeTRIC"
    - "The 5 convergence defaults are the geomeTRIC GAU preset (energy 1e-6, grms 3e-4, gmax 4.5e-4, drms 1.2e-3, dmax 1.8e-3)"
    - "Default maxsteps=100, initial trust=0.1, tmax=0.3 are enforced/capped"
  artifacts:
    - path: "crates/pyscf-geomopt/src/bmatrix.rs"
      provides: "Wilson B-matrix, G=BBt, G- pseudo-inverse"
      contains: "Wilson"
    - path: "crates/pyscf-geomopt/src/rfo.rs"
      provides: "RFO step + trust-radius + neg-eigenvalue shift + BFGS update"
      contains: "trust"
    - path: "crates/pyscf-geomopt/src/converge.rs"
      provides: "5-criterion GAU convergence check + locked defaults"
      contains: "1.0e-6"
    - path: "crates/pyscf-geomopt/tests/h2o_equilibrium.rs"
      provides: "self-contained convergence gate (D-05/GEOMOPT-07)"
      contains: "h2o"
  key_links:
    - from: "crates/pyscf-geomopt/src/lib.rs"
      to: "the RHF GradScanner (e_tot, de) seam"
      via: "optimize() drives set_geom -> (E, grad) -> RFO step"
      pattern: "GradScanner|scanner|set_geom"
    - from: "crates/pyscf-geomopt/src/rfo.rs"
      to: "pyscf_algebra eigh_gen / solve_linear"
      via: "RFO generalized eigen-step + B-matrix pseudo-inverse"
      pattern: "eigh_gen|solve_linear"
---

<objective>
De-risk the phase's biggest novelty EARLY (D-08): port the native BFGS+RFO redundant-internal
geometry optimizer from geomeTRIC (D-06) and prove it end-to-end on the RHF `GradScanner`
loop from 07-03 — `pyscf.geomopt.optimize(mf)` on the thinnest slice (H2O → equilibrium).

Per D-05: this is the only phase with NO in-tree algorithm source (upstream `geomopt` only
shims external `geometric`/`pyberny`). The always-on gate is self-contained: drive a perturbed
H2O to its known equilibrium and assert the converged geometry + final gradient norm — no
external package importable. Upstream trajectory parity is a `workflow_dispatch` arm; the
acceptance bar is GEOMOPT-07's "same stationary point within chemical accuracy", not bit-for-bit.

Per CONTEXT "Claude's Discretion": port geomeTRIC's redundant-internal-coordinate generation,
Wilson B-matrix + pseudo-inverse, RFO step + trust-radius + negative-eigenvalue handling, and
internal↔Cartesian back-transformation. The 5 GAU convergence defaults (GEOMOPT-04) and the
Wilson-B + RFO + neg-eig tracking (GEOMOPT-06) are LOCKED by the requirements.

Purpose: Prove the riskiest component at the earliest end-to-end point. Output: A native
optimizer engine + the self-contained H2O-equilibrium gate + B-matrix/RFO/conv unit tests.
This plan does NOT do shims/checkpoint/PyO3 (those land in 07-06/07-09).

Scope note (3 tasks / 11 files — acknowledged, >10-file WARNING threshold): the 11 files map
cleanly onto 3 distinct concerns — (Task 1) crate skeleton + redundant internals + Wilson
B-matrix + back-transform, (Task 2) RFO + trust-radius + neg-eig + BFGS + the 5 GAU convergence
defaults, (Task 3) the optimize() loop wiring + the self-contained H2O-equilibrium gate. NOT
split: 07-04 is Wave 3 (depends_on [07-03]) and 07-06 depends_on [07-04]; splitting would
perturb the validated dependency graph for no decomposition benefit (the de-risk-early intent
of D-08 wants the whole engine landed as one coherent unit).
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gradients-geomopt/07-CONTEXT.md
@.planning/phases/07-gradients-geomopt/07-RESEARCH.md
@.planning/phases/07-gradients-geomopt/07-PATTERNS.md
@.planning/phases/07-gradients-geomopt/07-03-SUMMARY.md

<interfaces>
From crates/pyscf-grad/src/scanner.rs (the seam this optimizer drives):
- GradScanner: on a new geometry returns (e_tot: Energy, de: ndarray (natm,3))

From crates/pyscf-algebra (algebra wall — RFO/B-matrix route here):
- pyscf_algebra::eigh_gen(f, s, n) -> Result<(Vec<f64>, Vec<f64>), AlgebraError>  // RFO generalized eigen-step
- pyscf_algebra::solve_linear(a, b, n) -> Result<Vec<f64>, AlgebraError>          // B-matrix pseudo-inverse / linear solve
- pyscf_algebra::gemm, gemv, oracle_sum, oracle_dot                               // contractions, reductions

geomeTRIC GAU convergence defaults (LOCKED, GEOMOPT-04):
- convergence_energy = 1.0e-6  Eh
- convergence_grms   = 3.0e-4  Eh/Bohr
- convergence_gmax   = 4.5e-4  Eh/Bohr
- convergence_drms   = 1.2e-3  (Bohr — confirm unit at port time, Pitfall 6 / A2)
- convergence_dmax   = 1.8e-3  (same unit caveat)
- trust=0.1 initial, tmax=0.3, maxsteps=100, epsilon (neg-eig shift)=1e-5, BFGS max_updates=100
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optimizer skeleton + redundant internals + Wilson B-matrix + back-transform</name>
  <files>crates/pyscf-geomopt/Cargo.toml, crates/pyscf-geomopt/src/lib.rs, crates/pyscf-geomopt/src/internals.rs, crates/pyscf-geomopt/src/bmatrix.rs, crates/pyscf-geomopt/src/backtransform.rs, crates/pyscf-geomopt/tests/bmatrix.rs</files>
  <read_first>
    - crates/pyscf-geomopt/src/lib.rs + Cargo.toml (the 5-line stubs to fill)
    - crates/pyscf-ccsd/Cargo.toml (the path-dep block shape to copy; deps here = pyscf-{algebra,scf,grad,chkfile,core,runtime}; NO pyo3, NO cubecl-*, NO own hdf5)
    - crates/pyscf-ccsd/src/lib.rs:1-64 (the #![forbid(unsafe_code)] header + one-pub-mod-per-file + flat re-export shape)
    - geomeTRIC published source (FETCH per D-06 — github.com/leeping/geomeTRIC): internal.py (primitive coordinate classes: Distance/Angle/Dihedral, the connectivity graph) + the Wilson B-matrix + G=BBt + G- pseudo-inverse construction
    - 07-RESEARCH.md §"Pattern 3: geomeTRIC RFO + trust-radius step" + §"Recommended Project Structure" (the module split) + §"Assumptions Log" A2/A3 (unit + license confirm)
    - 07-PATTERNS.md §"crates/pyscf-geomopt/* (the native BFGS+RFO optimizer)" + §"No In-Tree Analog"
  </read_first>
  <action>
    FETCH geomeTRIC source (D-06). FIRST, before porting ANY algorithm structure, confirm the
    geomeTRIC license is BSD/Apache-compatible (RESEARCH A3 — expected: BSD-3-Clause). Record the
    confirmed license string in the SUMMARY. If the fetched source is NOT BSD/Apache-compatible,
    STOP and surface a license checkpoint rather than vendoring the algorithm structure.

    Then fill `crates/pyscf-geomopt/Cargo.toml` (path deps =
    `pyscf-{algebra,scf,grad,chkfile,core,runtime}`; NO pyo3/cubecl-*/own-hdf5;
    thiserror+tracing workspace; approx="0.5" dev-dep) and `crates/pyscf-geomopt/src/lib.rs`
    (the forbid-unsafe header + `pub mod internals/bmatrix/rfo/backtransform/converge` (+
    shims/checkpoint declared but bodied in 07-06) + flat re-export + a `GeometryOptimizer` struct
    + an `optimize` entry).

    `internals.rs`: port geomeTRIC's redundant-internal-coordinate generation — bond/angle/dihedral
    primitive classes + the connectivity graph from interatomic distances. `bmatrix.rs`: the Wilson
    B-matrix (∂q/∂x), `G = B Bᵀ`, and `G⁻` pseudo-inverse — routing eigh/linear-solve through
    `pyscf_algebra::eigh_gen`/`solve_linear` (algebra wall). `backtransform.rs`: the internal→Cartesian
    iteration (geomeTRIC's `newCartesian` analog — iterate `dx = G⁻ Bᵀ dq` until the internal
    displacement matches). Per Pitfall 2: every reduction materializes then `oracle_sum`/`oracle_dot`.
  </action>
  <verify>
    <automated>cargo test -p pyscf-geomopt --locked -- --test-threads=1 bmatrix</automated>
  </verify>
  <acceptance_criteria>
    - GATING (precedes the port): the SUMMARY records the confirmed geomeTRIC license (expected BSD-3-Clause per RESEARCH A3) BEFORE any algorithm structure is ported; if the source is NOT BSD/Apache-compatible the port is STOPPED and a license checkpoint surfaced (no algorithm structure vendored without a recorded compatible license)
    - `cargo check -p pyscf-geomopt --locked` exits 0 (NO libxc/pyo3/cubecl in the dep graph)
    - `grep -E "pyo3|cubecl|hdf5-metno" crates/pyscf-geomopt/Cargo.toml` returns nothing
    - `crates/pyscf-geomopt/src/bmatrix.rs` contains the Wilson B-matrix + `G⁻` pseudo-inverse routing through `pyscf_algebra`
    - `cargo test -p pyscf-geomopt --locked bmatrix` exits 0
    - `cargo run -p xtask --bin check_dependency_wall` passes for pyscf-geomopt
  </acceptance_criteria>
  <done>
    The geomeTRIC license is confirmed compatible and recorded BEFORE the port; the optimizer
    crate compiles; redundant internals + Wilson B-matrix + pseudo-inverse + back-transform are
    ported and the bmatrix unit test (vs hand-calc) passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: RFO step + trust-radius + neg-eigenvalue tracking + BFGS + the 5 GAU convergence defaults</name>
  <files>crates/pyscf-geomopt/src/rfo.rs, crates/pyscf-geomopt/src/converge.rs, crates/pyscf-geomopt/tests/rfo.rs, crates/pyscf-geomopt/tests/conv_defaults.rs</files>
  <read_first>
    - crates/pyscf-geomopt/src/bmatrix.rs (from Task 1)
    - geomeTRIC optimize.py (FETCH per D-06): the RFO step, trust_step (Brent), trust-radius update from the quality factor, the neg-eigenvalue shift v0, BFGS update_hessian (max_updates=100)
    - geometric.readthedocs.io/en/latest/options.html (CITED): the GAU preset convergence defaults, trust radius (0.1/0.3), epsilon (1e-5)
    - 07-RESEARCH.md §"Pattern 3" (RFO + trust + neg-eig + the 5-criterion convergence table) + §"Pitfall 6" (Bohr-vs-Angstrom displacement unit — confirm at port time)
    - 07-PATTERNS.md §"crates/pyscf-geomopt/* — Convergence defaults + RFO + trust step"
  </read_first>
  <action>
    `rfo.rs`: port geomeTRIC's RFO step in internals — the augmented-Hessian eigen-problem via
    `pyscf_algebra::eigh_gen`, the trust-radius update
    (`quality = ΔE_actual/ΔE_predicted`; `quality>0.75` → `trust=min(trust*sqrt(2), tmax)`;
    `quality>thre` → keep; else → `trust=max(tmin, min(trust,cnorm)/2)` + may reject), Brent's
    `trust_step` for a step exceeding the trust radius, the negative-eigenvalue shift
    `v0 = (epsilon - Emin) if Emin < epsilon else 0` (epsilon=1e-5) to lift negative Hessian modes
    for MINIMIZATION (GEOMOPT-06 neg-eig tracking), and the BFGS approximate-Hessian update
    (max_updates=100). Per Pitfall 3: the SCF reference's `canonicalize_signs` keeps mo_coeff
    sign-stable across steps so the BFGS history is not corrupted.

    `converge.rs`: the 5-criterion GAU convergence check (ALL must hold) with the LOCKED defaults
    `convergence_energy=1.0e-6`, `convergence_grms=3.0e-4`, `convergence_gmax=4.5e-4`,
    `convergence_drms=1.2e-3`, `convergence_dmax=1.8e-3` (GEOMOPT-04). CONFIRM the displacement
    unit (Bohr vs Angstrom) against geomeTRIC source at port time (Pitfall 6 / A2) — the engine
    internally redefines ang2bohr/bohr2ang; the tabled numerics are what the engine compares.
    The optimizer-level defaults are ALSO locked and must be enforced/capped: `maxsteps=100`
    (cap/validate at the optimize entry), initial `trust=0.1`, `tmax=0.3`. Every reduction goes
    through `oracle_sum`/`oracle_dot`.

    Create `crates/pyscf-geomopt/tests/rfo.rs` (RFO step on a 1-D/2-D model Hessian; assert the
    step direction + the neg-eig shift) and `crates/pyscf-geomopt/tests/conv_defaults.rs` (assert
    the 5 GAU constants exactly equal the preset AND the optimizer-level defaults
    maxsteps==100 / initial trust==0.1 / tmax==0.3).
  </action>
  <verify>
    <automated>cargo test -p pyscf-geomopt --locked -- --test-threads=1 rfo conv_defaults</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-geomopt/src/converge.rs` contains the literal constants `1.0e-6`, `3.0e-4`, `4.5e-4`, `1.2e-3`, `1.8e-3` (GAU preset)
    - The optimizer-level defaults are present and enforced/capped: `maxsteps` default == 100 (validated/capped at the optimize entry), initial `trust` == 0.1, `tmax` == 0.3
    - `crates/pyscf-geomopt/tests/conv_defaults.rs` asserts BOTH the 5 GAU energy/gradient/displacement constants AND `maxsteps==100`, initial `trust==0.1`, `tmax==0.3`
    - `crates/pyscf-geomopt/src/rfo.rs` contains the trust-radius quality-factor update + the neg-eigenvalue shift `v0`
    - `cargo test -p pyscf-geomopt --locked rfo conv_defaults` exits 0
    - The SUMMARY records the confirmed displacement unit (Bohr vs Angstrom) per Pitfall 6
  </acceptance_criteria>
  <done>
    RFO + trust-radius + neg-eig tracking + BFGS + the 5 GAU convergence defaults + the
    optimizer-level defaults (maxsteps=100, trust=0.1, tmax=0.3) are ported and enforced;
    the rfo + conv_defaults unit tests pass (GEOMOPT-04/06).
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire optimize() on the RHF GradScanner + the self-contained H2O-equilibrium gate (D-05/GEOMOPT-07)</name>
  <files>crates/pyscf-geomopt/src/lib.rs, crates/pyscf-geomopt/tests/h2o_equilibrium.rs</files>
  <read_first>
    - crates/pyscf-grad/src/scanner.rs (the GradScanner (e_tot, de) seam this consumes)
    - crates/pyscf-geomopt/src/{lib,internals,bmatrix,rfo,backtransform,converge}.rs (Tasks 1-2)
    - crates/pyscf-scf/src/scanner.rs (the SCF as_scanner the grad scanner wraps; set_geom re-run)
    - 07-RESEARCH.md §"System Architecture Diagram" (the optimize loop: build internals → B-matrix → RFO step → back-transform → set_geom_ → GradScanner → converge) + §"Validation Architecture" GEOMOPT-07 row (`cargo test -p pyscf-geomopt h2o_equilibrium`)
    - 07-VALIDATION.md Per-Task Verification Map GEOMOPT-07 row + §"Wave 0 Requirements" (h2o_equilibrium.rs)
    - CONTEXT D-05 (self-contained convergence is the always-on gate; upstream parity is workflow_dispatch)
  </read_first>
  <action>
    Wire `optimize(scanner, conv_params)` in `lib.rs` as the geomeTRIC outer loop: from the
    current Cartesian geometry, build redundant internals (`internals.rs`), the Wilson B-matrix
    (`bmatrix.rs`), evaluate `(e_tot, de)` via the RHF `GradScanner`, transform the Cartesian
    gradient to internals (`Bᵀ`-projection), take an RFO step + trust-radius update (`rfo.rs`),
    back-transform the internal step to Cartesians (`backtransform.rs`), `set_geom_` the new
    geometry, and check the 5-criterion convergence (`converge.rs`). Cap `maxsteps=100`. The
    `constraints` handling raises a clear error (full shim parity lands in 07-06). Return the
    converged geometry + `conv` flag.

    Create `crates/pyscf-geomopt/tests/h2o_equilibrium.rs` (D-05/GEOMOPT-07 self-contained gate):
    build an RHF on a PERTURBED H2O (bond length/angle off equilibrium, minimal basis — NO libxc),
    construct its grad scanner via the 07-03 `RhfGradients`, run `optimize`, and assert the
    converged geometry matches the known H2O equilibrium (O-H ~0.96 Å, H-O-H ~104.5° within
    chemical accuracy) AND the final gradient RMS norm < `convergence_grms` (3e-4 Eh/Bohr). NO
    external package importable. GATING: if the RHF gradient numeric is gated on the cintx
    workstream (per 07-01/07-03 SUMMARY), this end-to-end numeric gate is `#[ignore]`'d behind the
    same prerequisite while the optimizer STRUCTURE (B-matrix, RFO, convergence, loop wiring) stays
    always-on via Tasks 1-2 unit tests — document the chosen arm in the SUMMARY.
  </action>
  <verify>
    <automated>cargo test -p pyscf-geomopt --locked -- --test-threads=1 h2o_equilibrium</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-geomopt/src/lib.rs` contains `optimize` driving the GradScanner through the RFO loop with a `maxsteps` cap
    - `crates/pyscf-geomopt/tests/h2o_equilibrium.rs` exists and asserts the converged H2O geometry within chemical accuracy + final grms < 3e-4 (or is `#[ignore]`'d behind the cintx-availability gate with the optimizer structure unit-tested always-on)
    - `cargo test -p pyscf-geomopt --locked h2o_equilibrium` exits 0
    - No external `geometric`/`pyberny` package is imported anywhere in the crate
  </acceptance_criteria>
  <done>
    `optimize(mf)` drives the RHF grad scanner to convergence; the self-contained H2O-equilibrium
    gate proves the optimizer end-to-end on the thinnest slice (the phase's riskiest component
    de-risked early per D-08).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user conv_params / maxsteps ↔ optimize() | unbounded maxsteps or a `constraints` kwarg crosses here |
| GradScanner ↔ optimizer loop | a sign-flipped mo_coeff would corrupt the BFGS history |
| B-matrix conditioning ↔ back-transform | a pathological (singular) B-matrix could diverge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Denial of Service | unbounded `maxsteps` from user kwargs | mitigate | Default `maxsteps=100`; cap/validate at the optimize entry (RESEARCH Security Domain) — never an unbounded loop |
| T-07-11 | Tampering | `constraints` kwarg silently ignored | mitigate | Raise a clear error (D-07, GEOMOPT-EXT-01 deferred) — never a silent no-op that mis-optimizes |
| T-07-12 | Tampering | eigenvector sign flip across geometry steps | mitigate | Reuse the SCF `canonicalize_signs` mo_coeff each step (SCF-13, Pitfall 3/4); verify the grad scanner re-uses canonicalized coefficients |
| T-07-13 | Denial of Service | singular/ill-conditioned Wilson B-matrix | mitigate | Pseudo-inverse via `pyscf_algebra` with a Cartesian fallback for pathological conditioning (Claude's-discretion per CONTEXT) |
| T-07-SC | Tampering | geomeTRIC source port | mitigate | No registry package installed (algorithm ported, not vendored as a dep); confirm BSD-license compatibility (A3) BEFORE porting structure and record the confirmed license in the SUMMARY (Task 1 gating criterion) |
</threat_model>

<verification>
- `cargo test -p pyscf-geomopt --locked -- --test-threads=1` green (scoped — NO libxc).
- bmatrix / rfo / conv_defaults unit tests pass; h2o_equilibrium passes (or `#[ignore]`'d per cintx).
- `cargo run -p xtask --bin check_dependency_wall` passes (no cubecl-*/pyo3 in pyscf-geomopt).
- No external `geometric`/`pyberny` import anywhere.
</verification>

<success_criteria>
- The geomeTRIC license is confirmed compatible and recorded BEFORE the algorithm structure is ported.
- The native BFGS+RFO redundant-internal optimizer converges an RHF GradScanner from a
  perturbed H2O to its equilibrium (self-contained, GEOMOPT-07) — or is gated on the cintx
  workstream with the optimizer structure (B-matrix/RFO/convergence) proven always-on.
- Wilson B-matrix + RFO + neg-eig tracking + the 5 GAU convergence defaults + the optimizer-level
  defaults (maxsteps=100, trust=0.1, tmax=0.3) match geomeTRIC (GEOMOPT-04/06).
</success_criteria>

<output>
Create `.planning/phases/07-gradients-geomopt/07-04-SUMMARY.md` when done. MUST record the
confirmed geomeTRIC license (Task 1 gating criterion), the `optimize`/`GeometryOptimizer` API,
the confirmed displacement unit (Pitfall 6), and whether the h2o_equilibrium numeric gate is
un-gated or gated on the cintx workstream — so the shims (07-06) and PyO3 bridge (07-09) wire
against the same engine.
</output>
