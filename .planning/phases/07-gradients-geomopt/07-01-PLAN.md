---
phase: 07-gradients-geomopt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pyscf-gto/src/intor.rs
  - crates/pyscf-gto/src/layout_table.rs
  - crates/pyscf-gto/src/ecp_engine_cintx.rs
  - crates/pyscf-gto/tests/grad_intor_smoke.rs
autonomous: false
requirements: [GRAD-07]
must_haves:
  truths:
    - "int2e_ip1 component-leading arity-4 dispatch returns a [3, nao, nao] shaped buffer (or a cintx-availability error) — never NotYetImplemented{phase:7}"
    - "int1e_ip{ovlp,kin,nuc,rinv} arity-2 component-leading intors return [3, nao, nao] via the existing evaluate_arity2 path"
    - "ECPscalar_ipnuc (int1e_ecp_ipnuc) dispatches through the cintx ECP engine and returns [3, nao, nao]"
    - "The two cintx-ready families (int3c2e_ip1, int1e_ecp_ipnuc) round-trip against the cintx kernel"
  artifacts:
    - path: "crates/pyscf-gto/src/intor.rs"
      provides: "int2e_ip1 component-leading arity-4 dispatch; NotYetImplemented{phase:7} arity-4 guard removed"
      contains: "ComponentLeadingFOrder"
    - path: "crates/pyscf-gto/src/ecp_engine_cintx.rs"
      provides: "ECP-gradient dispatch (ecp_ipnuc ready; ecp_iprinv gated arm)"
      contains: "ipnuc"
    - path: "crates/pyscf-gto/tests/grad_intor_smoke.rs"
      provides: "cintx grad-intor round-trip smoke for the two ready families"
  key_links:
    - from: "crates/pyscf-gto/src/intor.rs"
      to: "evaluate_arity2 component-leading stitch"
      via: "shared stitch_block / block_component_leading path"
      pattern: "stitch_block|block_component_leading"
    - from: "crates/pyscf-gto/src/ecp_engine_cintx.rs"
      to: "cintx OperatorId::INT1E_ECP_IPNUC"
      via: "SessionRequest dispatch"
      pattern: "ecp_ipnuc|INT1E_ECP_IPNUC"
---

<objective>
Wave-0 risk-buy-down (D-02): wire the gradient integrals into `pyscf-gto`, remove the
`NotYetImplemented{phase:7}` dispatch guards, confirm the layout table, and prove the
cintx grad-intor round-trip for the families cintx already ships.

This is the cross-repo gating hinge. Per 07-RESEARCH.md §"Gradient-Integral Availability
Matrix", cintx ships only **2 of 8** needed families today (`int3c2e_ip1`,
`int1e_ecp_ipnuc`=`ECPscalar_ipnuc`). The other 6 (`int2e_ip1`, `int1e_ip{ovlp,kin,nuc,rinv}`,
`ECPscalar_iprinv`, + the `with_rinv_at_nucleus` rinv-origin shift) require a **cintx
workstream** that is a HARD blocking prerequisite for analytical-grad numeric un-gating.
This plan does the pyscf-gto-side structural wiring (always lands) and tracks the cintx
workstream as a coordination checkpoint (hence `autonomous: false`).

Purpose: Unblock every downstream gradient method body — the dispatch must return a
shaped buffer (or a clean cintx-availability error), never `NotYetImplemented{phase:7}`.
Output: Wired `pyscf-gto` gradient-intor dispatch + a cintx round-trip smoke test.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gradients-geomopt/07-CONTEXT.md
@.planning/phases/07-gradients-geomopt/07-RESEARCH.md
@.planning/phases/07-gradients-geomopt/07-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm the cintx gradient-integral workstream branch + availability</name>
  <read_first>
    - .planning/phases/07-gradients-geomopt/07-RESEARCH.md §"Gradient-Integral Availability Matrix" + §"Open Questions" 1-2 + §"Assumptions Log" A1
    - ~/Documents/workspace/cintx/crates/cintx-ops/src/generated/api_manifest.rs (grep the symbol_name column for: int2e_ip1, int1e_ipovlp, int1e_ipkin, int1e_ipnuc, int1e_iprinv, int3c2e_ip1, int1e_ecp_ipnuc / ECPscalar_ipnuc, ECPscalar_iprinv)
    - User memory: project_int2e_general_contraction_workstream — the analogous cintx workstream tracking model
  </read_first>
  <what-built>
    A grep-confirmed re-verification of the RESEARCH availability matrix against the LIVE cintx
    manifest (the research is valid-until 2026-06-09; the cintx grad workstream is the fast-moving
    variable). The executor greps the cintx api_manifest for each of the 8 families and records
    which are present today.
  </what-built>
  <how-to-verify>
    1. Run: `grep -nE "int2e_ip1|int1e_ipovlp|int1e_ipkin|int1e_ipnuc|int1e_iprinv|int3c2e_ip1|int1e_ecp_ipnuc|ECPscalar_ipnuc|ECPscalar_iprinv" ~/Documents/workspace/cintx/crates/cintx-ops/src/generated/api_manifest.rs`
    2. Expected (per RESEARCH, confirm): only `int3c2e_ip1` and `int1e_ecp_ipnuc` (=`ECPscalar_ipnuc`) present; the other 6 absent.
    3. Confirm whether a cintx branch (e.g. extending `fix/general-contraction-nctr-1e`) is scheduled to land the 6 missing families + the `with_rinv_at_nucleus` rinv-origin-shift parameter.
    4. Record the branch name / availability status in the SUMMARY so downstream plans know which method-numeric arms stay gated (workflow_dispatch) vs un-gate now.
  </how-to-verify>
  <action>
    Re-verify the D-02 availability matrix against live cintx. Per D-02 + RESEARCH Pitfall 1: do
    NOT assume cintx ships the core families. The two ready families (`int3c2e_ip1`,
    `int1e_ecp_ipnuc`) un-gate their numeric now; the 6 missing families ride the CCSD-D-04-style
    gated/workflow_dispatch arm until the cintx workstream lands them — but FD-structural (D-01)
    stays always-on regardless. Document the cintx branch/timeline. This task is a coordination
    gate: the pyscf-gto structural wiring in Tasks 2-3 lands no matter what cintx ships; only the
    numeric un-gating in downstream plans is gated on this finding.
  </action>
  <acceptance_criteria>
    - SUMMARY records, per family, present-in-cintx-today (yes/no) from the live api_manifest grep
    - SUMMARY records the cintx workstream branch name (or "not yet scheduled — gate all 6 missing families")
    - The two-ready / six-missing split from RESEARCH is either confirmed or the deviation is documented
  </acceptance_criteria>
  <resume-signal>Type "approved" with the confirmed family availability + cintx branch, or describe deviations from the RESEARCH matrix.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Remove the arity-4 NotYetImplemented{phase:7} guard and wire int2e_ip1 component-leading dispatch</name>
  <files>crates/pyscf-gto/src/intor.rs</files>
  <read_first>
    - crates/pyscf-gto/src/intor.rs (the WHOLE file — esp. the live arity-4 guard near line 444 matching `NotYetImplemented { phase: 7` inside the ComponentLeadingFOrder match, the doc comment near line 428, and the `evaluate_arity2` component-leading stitch path with `stitch_block`/`block_component_leading` for the layout repack template)
    - crates/pyscf-gto/src/layout_table.rs (the int2e_ip1_sph / int1e_ip*_sph ComponentLeadingFOrder entries already declared)
    - 07-PATTERNS.md §"crates/pyscf-gto/src/intor.rs (remove arity-4 component-leading guard)" — the exact guard text + the arity-2 analog
    - pyscf/gto/moleintor.py (the component layout convention — comp=3, component-leading)
  </read_first>
  <action>
    In `crates/pyscf-gto/src/intor.rs`, locate the LIVE arity-4 guard (match on the
    `NotYetImplemented { phase: 7, what: "component-leading arity-4 integrals (int2e_ip1/ip2
    gradients) — Phase 7" }` literal inside the `IntorLayout::ComponentLeadingFOrder` arm of
    `evaluate_arity4`; do NOT trust the line number — it has drifted per PATTERNS). Replace the
    guard with the real `int2e_ip1` component-leading dispatch, mirroring the arity-2
    component-leading stitch (`evaluate_arity2`'s `block_component_leading` + `components > 1`
    branch via `stitch_block`). The result MUST be a `[3, nao, nao]` (component-leading F-order)
    buffer for `int2e_ip1`. Per Pitfall 4 (Pitfall 8 re-validation, "Phase 7 grad"): assert the
    returned shape is `[3, nao, nao]` (axis 0 = x/y/z component), reusing the existing
    `ComponentLeadingFOrder { components: 3 }` layout path — never repack as `[nao, nao, 3]`.
    Per D-02: this guard removal is STRUCTURAL — if cintx lacks `int2e_ip1` (the expected case per
    Task 1), the dispatch returns a clean cintx-availability error from the kernel call, NOT a
    `NotYetImplemented{phase:7}`. Leave the arity-2 1e gradient intors as-is (they already dispatch
    via `evaluate_arity2`'s component-leading path).
  </action>
  <verify>
    <automated>cargo test -p pyscf-gto --locked -- --test-threads=1 intor</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "NotYetImplemented { phase: 7" crates/pyscf-gto/src/intor.rs | grep -v '^[0-9]*://'` no longer matches the arity-4 int2e_ip1 guard (the doc-comment near line 428 may remain, but the live guard arm is gone)
    - `cargo test -p pyscf-gto --locked intor` exits 0 (no libxc pulled)
    - A test asserts `int2e_ip1` dispatch returns shape `[3, nao, nao]` OR a cintx-availability error — never `PyscfRsError::NotYetImplemented { phase: 7, .. }`
  </acceptance_criteria>
  <done>
    The arity-4 int2e_ip1 guard is removed; the component-leading arity-4 dispatch is wired
    through the existing layout path; `cargo test -p pyscf-gto intor` is green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire the ECP-gradient guard + confirm layout table + cintx round-trip smoke</name>
  <files>crates/pyscf-gto/src/ecp_engine_cintx.rs, crates/pyscf-gto/src/layout_table.rs, crates/pyscf-gto/tests/grad_intor_smoke.rs</files>
  <read_first>
    - crates/pyscf-gto/src/ecp_engine_cintx.rs (the WHOLE file — esp. the live guard near line 75 matching `core != "int1e_ecp" && core != "ECPscalar"` returning `NotYetImplemented { phase: 7, what: "ECP derivative/gradient integrals ..." }`, and the scalar arity-2 `ecp_int1e` SessionRequest → F-order buffer loop near lines 51-222)
    - crates/pyscf-gto/src/layout_table.rs (confirm the catalogue_meets_phase_2_floor ≥20-entry test stays green; the int1e_ip*/int2e_ip1/int3c2e_ip1 entries are already ComponentLeadingFOrder{components:3})
    - 07-PATTERNS.md §"crates/pyscf-gto/src/ecp_engine_cintx.rs (wire the ECP-gradient guard)" + §"crates/pyscf-gto/src/layout_table.rs (confirm/extend)"
    - ~/Documents/workspace/cintx/crates/cintx-cubecl/src/kernels/ecp.rs (the ecp_ipnuc kernel, Plan 19-07 per RESEARCH)
    - pyscf/grad/rhf.py:109-143 (get_hcore + hcore_deriv ECP branches: ECPscalar_ipnuc in get_hcore; ECPscalar_iprinv in hcore_deriv)
  </read_first>
  <action>
    In `crates/pyscf-gto/src/ecp_engine_cintx.rs`, wire the guard near line 75 (match the
    `NotYetImplemented { phase: 7, what: "ECP derivative/gradient integrals (int1e_ecp_ipnuc/iprinv,
    ECPscalar_ip* — Phase 7 GRAD-07)" }` literal). Add an `ecp_int1e_ipnuc` path that resolves the
    `OperatorId::INT1E_ECP_IPNUC_*` operator (manifest positions per the file's doc-comment, ~26-29)
    and returns a component-leading `[3, nao, nao]` buffer, mirroring the scalar arity-2 `ecp_int1e`
    SessionRequest → F-order loop but with `components = 3`. Per D-02 + Task 1 finding:
    `ECPscalar_ipnuc` (=`int1e_ecp_ipnuc`) is READY in cintx — un-gate its numeric now; route
    `ECPscalar_iprinv` to a clean cintx-availability error (it is a cintx workstream item — do NOT
    return `NotYetImplemented{phase:7}`). Then create `crates/pyscf-gto/tests/grad_intor_smoke.rs`:
    a round-trip smoke (the 02-01 cintx-smoke pattern) for the TWO cintx-ready families
    (`int3c2e_ip1`, `int1e_ecp_ipnuc`) on a tiny molecule (H2 or He), asserting the
    `[3, nao, nao]` shape and finite values. Confirm `layout_table.rs` `catalogue_meets_phase_2_floor`
    (≥20 entries) stays green; add ECP-gradient names to the table only if the ECP engine path
    requires a table entry (per PATTERNS, ECPscalar routes through the ECP engine, not this table —
    so likely no edit).
  </action>
  <verify>
    <automated>cargo test -p pyscf-gto --locked -- --test-threads=1 grad_intor_smoke layout</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-gto/src/ecp_engine_cintx.rs` contains an `ecp_int1e_ipnuc` (or equivalently-named) path resolving `INT1E_ECP_IPNUC`
    - `grep -n "NotYetImplemented { phase: 7" crates/pyscf-gto/src/ecp_engine_cintx.rs | grep -v '^[0-9]*://'` no longer matches the ECPscalar_ipnuc family (the ecp_iprinv arm may remain as a cintx-availability gate, but NOT as NotYetImplemented{phase:7})
    - `crates/pyscf-gto/tests/grad_intor_smoke.rs` exists and asserts `[3, nao, nao]` for int3c2e_ip1 + int1e_ecp_ipnuc
    - `cargo test -p pyscf-gto --locked grad_intor_smoke layout` exits 0; `catalogue_meets_phase_2_floor` passes
  </acceptance_criteria>
  <done>
    ECP-gradient dispatch wired (ipnuc ready, iprinv gated as cintx-availability error); layout
    table confirmed ≥20 entries; cintx round-trip smoke green for the two ready families.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cintx kernel ↔ pyscf-gto dispatch | integral-buffer shape/layout crosses here; a wrong repack scrambles x/y/z components |
| missing-cintx-family ↔ caller | an absent family must surface a clean error, not a panic or silent zero |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | int2e_ip1 component-leading repack (intor.rs) | mitigate | Reuse the existing `ComponentLeadingFOrder{components:3}` layout path; assert `[3, nao, nao]` shape on return (Pitfall 4 / Pitfall 8 re-validation) — never a `[nao,nao,3]` permutation |
| T-07-02 | Denial of Service | missing cintx family dispatch | mitigate | Return a clean cintx-availability `Err` (not a panic, not `NotYetImplemented{phase:7}`); the FFI boundary is in pyscf-py, but pyscf-gto must never panic on an absent op |
| T-07-03 | Information disclosure | silent zero buffer for an unimplemented op | accept | A zero buffer would silently corrupt gradients; mitigated by T-07-01's shape assertion + the FD gate downstream catching any wrong numeric |
| T-07-SC | Tampering | cintx sibling-crate dependency | mitigate | No new registry packages (RESEARCH Package Legitimacy Audit: N/A — sibling path dep). cintx availability re-verified in Task 1; numeric stays gated until the workstream lands |
</threat_model>

<verification>
- `cargo test -p pyscf-gto --locked -- --test-threads=1` is green (scoped — pulls NO libxc_rs).
- No `NotYetImplemented{phase:7}` remains in the live arity-4 (int2e_ip1) or ECPscalar_ipnuc dispatch arms.
- The cintx round-trip smoke asserts `[3, nao, nao]` for the two ready families.
- The dependency-wall lint (`cargo run -p xtask --bin check_dependency_wall`) still passes (no cubecl-* added to pyscf-gto).
</verification>

<success_criteria>
- The gradient-intor dispatch returns a shaped `[3, nao, nao]` buffer (or a clean cintx-availability error) for every gradient family — never `NotYetImplemented{phase:7}` for int2e_ip1 / int1e_ip* / ECPscalar_ipnuc.
- The cintx workstream status for the 6 missing families is documented in the SUMMARY (which downstream numeric arms stay gated).
- GRAD-07's ECP-gradient dispatch seam is wired (ipnuc ready, iprinv gated).
</success_criteria>

<output>
Create `.planning/phases/07-gradients-geomopt/07-01-SUMMARY.md` when done. MUST record:
the live cintx family-availability split, the cintx workstream branch/timeline, and which
downstream method-numeric arms therefore stay gated (workflow_dispatch) vs un-gate now.
</output>
