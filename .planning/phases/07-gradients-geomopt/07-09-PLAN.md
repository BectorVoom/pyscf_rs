---
phase: 07-gradients-geomopt
plan: 09
type: execute
wave: 7
depends_on: ["07-03", "07-04", "07-05", "07-06", "07-07", "07-08"]
files_modified:
  - crates/pyscf-py/src/grad.rs
  - crates/pyscf-py/src/geomopt.rs
  - crates/pyscf-py/src/lib.rs
  - python/pyscf/grad/__init__.py
  - python/pyscf/geomopt/__init__.py
autonomous: true
requirements: [GEOMOPT-01, GEOMOPT-02, GEOMOPT-03]
must_haves:
  truths:
    - "mf.nuc_grad_method().kernel() is callable from Python for RHF/UHF/RKS/UKS/MP2/CCSD and returns the analytical gradient"
    - "pyscf.geomopt.optimize(mf) runs the native Rust optimizer with NO Python geometric/pyberny runtime dependency (GEOMOPT-01)"
    - "pyscf.geomopt.geometric_solver.optimize(mf) and pyscf.geomopt.berny_solver.optimize(mf) are drop-in shims delegating to the native engine (GEOMOPT-02/03)"
    - "Method crates stay pyo3-free; ALL pyo3 lives in pyscf-py; the bridge uses eager-snapshot + is_overridden + call_method1 + Python::detach"
  artifacts:
    - path: "crates/pyscf-py/src/grad.rs"
      provides: "PyGradients / per-method grad classes + mf.nuc_grad_method factory + grad scanner"
      contains: "nuc_grad_method"
    - path: "crates/pyscf-py/src/geomopt.rs"
      provides: "geomopt submodule: optimize + geometric_solver + berny_solver entry points"
      contains: "optimize"
    - path: "python/pyscf/grad/__init__.py"
      provides: "grad overlay + _graft_nuc_grad_onto_scf"
      contains: "nuc_grad_method"
    - path: "python/pyscf/geomopt/__init__.py"
      provides: "geomopt overlay: optimize + geometric_solver/berny_solver re-exports"
      contains: "optimize"
  key_links:
    - from: "crates/pyscf-py/src/grad.rs"
      to: "the pyscf-grad method bodies (RHF..CCSD)"
      via: "eager SCF snapshot + call_method1 override dispatch + py.detach"
      pattern: "is_overridden|call_method1|detach"
    - from: "python/pyscf/geomopt/__init__.py"
      to: "_native.geomopt"
      via: "re-export optimize + geometric_solver/berny_solver shims"
      pattern: "_native.geomopt|geometric_solver|berny_solver"
---

<objective>
Build the PyO3 bridge so the gradient + optimizer surface is callable from Python exactly as
upstream (D-07): `mf.nuc_grad_method().kernel()` for every in-scope method, and
`pyscf.geomopt.optimize(mf)` / `geometric_solver.optimize(mf)` / `berny_solver.optimize(mf)`
— with NO Python `geometric`/`pyberny` runtime dependency (GEOMOPT-01).

Per CONTEXT "Claude's Discretion" PyO3 bridge shape: `PyGradients`/per-method grad classes +
the `geomopt` submodule + `python/pyscf/grad` + `python/pyscf/geomopt` overlays, following the
Phase-5/6 `mp.rs`/`cc.rs` pattern (eager SCF snapshot + `is_overridden` `__qualname__` MRO check
+ `call_method1` override-dispatch + `Python::detach` + `as_scanner` re-run + factory + the
cross-module graft). Method crates stay pyo3-free; ALL pyo3 lives here.

NOTE: `python/pyscf/grad` and `python/pyscf/geomopt` are NET-NEW overlay dirs (only `cc`/`mp`/
`scf`/`dft` exist today); `cc/__init__.py` is the template.

Purpose: Make the phase's deliverables drop-in from Python. Output: the grad + geomopt bridge
+ the two overlays. (The no-runtime-dep CI proof + oracle fixtures land in 07-10.)
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gradients-geomopt/07-CONTEXT.md
@.planning/phases/07-gradients-geomopt/07-RESEARCH.md
@.planning/phases/07-gradients-geomopt/07-PATTERNS.md
@.planning/phases/07-gradients-geomopt/07-03-SUMMARY.md
@.planning/phases/07-gradients-geomopt/07-04-SUMMARY.md
@.planning/phases/07-gradients-geomopt/07-06-SUMMARY.md
@.planning/phases/07-gradients-geomopt/07-07-SUMMARY.md
@.planning/phases/07-gradients-geomopt/07-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PyGradients bridge + mf.nuc_grad_method factory + grad scanner + python/pyscf/grad overlay</name>
  <files>crates/pyscf-py/src/grad.rs, crates/pyscf-py/src/lib.rs, python/pyscf/grad/__init__.py</files>
  <read_first>
    - crates/pyscf-py/src/cc.rs (the WHOLE ~930-line file — the closest one-to-one analog: snapshot_reference cc.rs:82-115; is_overridden cc.rs:154-174; override dispatch cc.rs:202-291; PyCcsdScanner as_scanner re-run cc.rs:551-822; ccsd_factory cc.rs:889-906)
    - crates/pyscf-py/src/lib.rs:85-88 (the cc_mod PyModule::new + register + add_submodule block to mirror)
    - python/pyscf/cc/__init__.py (the WHOLE 63-line overlay — from pyscf._native.X import ... + _graft_ccsd_onto_scf)
    - pyscf/grad/rhf.py:248-262 (SCF_GradScanner — the grad scanner returns (e_tot, de) tuple) + scf/hf.py:2484 (def nuc_grad_method — the graft target)
    - crates/pyscf-grad/src/{rhf,uhf,rks,uks,mp2,ccsd,ecp,scanner}.rs (the method bodies the bridge wraps — from 07-03/05/07/08)
    - 07-PATTERNS.md §"crates/pyscf-py/src/grad.rs + geomopt submodule (PyO3 bridge)" + §"python/pyscf/grad/__init__.py" + §"PyO3 bridge" + §"Cross-module graft"
  </read_first>
  <action>
    Create `crates/pyscf-py/src/grad.rs` by copying the `cc.rs` mechanisms verbatim (rename
    `Ccsd`→`Grad`):
    - Eager SCF snapshot (`cc.rs:82-115`): pull `mo_coeff`/`mo_energy`/`mo_occ`/`e_tot`/`converged`/
      `mol` from the Python `mf` into plain Rust arrays at construction (D-09; reuse the
      CcsdReference/Mp2Reference snapshot shape).
    - `is_overridden` MRO check (`cc.rs:154-174`): `__qualname__` split against base-class names;
      missing attr → not overridden; no `__qualname__` → conservatively overridden.
    - Override dispatch (`cc.rs:202-291`): if overridden → `slf.bind(py).call_method1("<hook>", args)`;
      else the pure-Rust default under `Python::attach(|py| py.detach(|| ...))` (BIND-05). The
      `kernel` itself does NOT `py.detach` at the top (hooks re-enter Python).
    - The grad SCANNER (`cc.rs:551-822` PyCcsdScanner analog): wrap `mf.as_scanner()`, re-run SCF
      at the new geom, re-snapshot, re-run the grad kernel under `py.detach` — but return
      `(e_tot, de)` (a TUPLE) instead of a scalar (rhf.py:248-262).
    - Factory + cross-module graft (`cc.rs:889-906` ccsd_factory analog): a `grad_factory`
      dispatching RHF→RhfGradients / UHF→UhfGradients / RKS→RksGradients / UKS→UksGradients /
      MP2→Mp2Gradients / CCSD→CcsdGradients (+ ECP folded into the HF path).
    - NumPy boundary (BIND-04): return C-contiguous `(natm,3)` arrays; `to_owned()` on
      non-standard-layout inputs. Wrap the FFI body in catch_unwind (panic→exception, BIND-09).

    Register the module in `crates/pyscf-py/src/lib.rs` (mirror the `cc_mod` block at lines 85-88):
    add a `grad_mod = PyModule::new(py, "grad")?; crate::grad::register(py, &grad_mod)?;
    m.add_submodule(&grad_mod)?;`.

    Create `python/pyscf/grad/__init__.py` (NET-NEW dir; template = `python/pyscf/cc/__init__.py`):
    re-export from `pyscf._native.grad` + a `_graft_nuc_grad_onto_scf()` that grafts
    `mf.nuc_grad_method()` onto the Rust `_native.scf.{RHF,UHF,GHF}` pyclasses (upstream
    scf/hf.py:2484), guarded by `if getattr(cls, "nuc_grad_method", None) is None` (subclass
    override wins).
  </action>
  <verify>
    <automated>cargo check -p pyscf-py --locked && cargo test -p pyscf-py --locked -- --test-threads=1 grad</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-py/src/grad.rs` contains the eager-snapshot, `is_overridden`, `call_method1` dispatch, the grad scanner returning a tuple, and a `grad_factory`
    - `crates/pyscf-py/src/lib.rs` contains a `grad_mod` registration block mirroring `cc_mod`
    - `python/pyscf/grad/__init__.py` exists and contains `_graft_nuc_grad_onto_scf` grafting `nuc_grad_method`
    - `cargo check -p pyscf-py --locked` exits 0 (pyscf-grad stays pyo3-free — `grep pyo3 crates/pyscf-grad/Cargo.toml` returns nothing)
  </acceptance_criteria>
  <done>
    `mf.nuc_grad_method().kernel()` is callable from Python for every in-scope method via the
    eager-snapshot + override-dispatch + scanner bridge; the grad overlay grafts onto the SCF classes.
  </done>
</task>

<task type="auto">
  <name>Task 2: geomopt submodule bridge + python/pyscf/geomopt overlay (GEOMOPT-01/02/03)</name>
  <files>crates/pyscf-py/src/geomopt.rs, crates/pyscf-py/src/lib.rs, python/pyscf/geomopt/__init__.py</files>
  <read_first>
    - crates/pyscf-py/src/grad.rs (Task 1 — the grad scanner the optimizer drives) + crates/pyscf-py/src/cc.rs (the as_scanner re-run + py.detach + factory patterns)
    - crates/pyscf-geomopt/src/{lib,shims}.rs (07-04/07-06 — the native optimize + geometric_solver/berny_solver shim entry points to expose)
    - pyscf/geomopt/geometric_solver.py:96-192 (optimize/kernel return shapes: kernel -> (conv, mol); optimize -> mol) + pyscf/geomopt/berny_solver.py (the berny entry shape)
    - python/pyscf/cc/__init__.py (the overlay template) + crates/pyscf-py/src/lib.rs:85-88 (submodule registration)
    - 07-PATTERNS.md §"python/pyscf/grad/__init__.py + python/pyscf/geomopt/__init__.py" + §"crates/pyscf-py/src/grad.rs + geomopt submodule"
    - 07-RESEARCH.md §"Pattern 3 — Shim API to mirror (D-07)" + CONTEXT D-07
  </read_first>
  <action>
    Create `crates/pyscf-py/src/geomopt.rs`: expose the native optimizer (07-04) + the
    `geometric_solver`/`berny_solver` shims (07-06) to Python. Mirror the upstream `kernel(method,
    constraints=None, callback=None, maxsteps=100, **kwargs)` signature (D-07): dispatch a
    `GradScanner` / `GradientsBase` / `nuc_grad_method()`-bearing `method` (use the Task-1 grad
    factory + scanner), thread `conv_params`/`callback`, cap `maxsteps` (default 100). Return
    `(conv, mol)` from `kernel` and the optimized `Mole` from `optimize`. The `constraints` kwarg,
    when non-None, raises a clear error (D-07 — surfaced from the 07-06 `ConstraintsUnsupported`).
    Wrap the long compute under `py.detach` (BIND-05) and the FFI body in catch_unwind. Register a
    `geomopt_mod` (with `geometric_solver` + `berny_solver` as nested submodules or attributes)
    in `crates/pyscf-py/src/lib.rs`, mirroring the `cc_mod` block.

    Create `python/pyscf/geomopt/__init__.py` (NET-NEW dir; template = `python/pyscf/cc/__init__.py`):
    re-export `optimize` + the `geometric_solver` and `berny_solver` submodule shims from
    `pyscf._native.geomopt`. The CRITICAL contract (GEOMOPT-01): NO `import geometric` / `import
    pyberny` anywhere — the optimizer is fully native. (The `pip uninstall geometric pyberny` CI
    proof lands in 07-10.)
  </action>
  <verify>
    <automated>cargo check -p pyscf-py --locked && cargo test -p pyscf-py --locked -- --test-threads=1 geomopt</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-py/src/geomopt.rs` exposes `optimize` + `geometric_solver`/`berny_solver` shim entries delegating to the native 07-04/07-06 engine, returning the upstream shapes (kernel→(conv,mol), optimize→mol)
    - `crates/pyscf-py/src/lib.rs` contains a `geomopt_mod` registration block
    - `python/pyscf/geomopt/__init__.py` exists, re-exports `optimize` + `geometric_solver` + `berny_solver` from `_native.geomopt`, and contains NO `import geometric` / `import pyberny` (`grep -nE "import (geometric|pyberny)" python/pyscf/geomopt/__init__.py` returns nothing)
    - `crates/pyscf-py/src/geomopt.rs` raises a clear error for a non-None `constraints` kwarg (no silent no-op)
    - `cargo check -p pyscf-py --locked` exits 0
    - `cargo test -p pyscf-py --locked -- --test-threads=1 geomopt` exits 0 (matches the verify block; catches a regression in the Task-1 grad.rs integration path this bridge calls — keep it scoped to `-p pyscf-py`, NEVER workspace-wide)
  </acceptance_criteria>
  <done>
    `pyscf.geomopt.optimize(mf)` + `geometric_solver.optimize` + `berny_solver.optimize` are
    callable from Python via the native engine, with NO geometric/pyberny import (GEOMOPT-01/02/03).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python mf object ↔ Rust snapshot | a malformed/non-contiguous NumPy mo_coeff crosses the FFI here |
| GIL ↔ long grad/optimize compute | a callback re-entering Python under a held GIL deadlocks |
| Rust panic ↔ Python exception | a panic escaping the FFI is a DoS |
| user kwargs (maxsteps, constraints) ↔ geomopt bridge | unbounded/unsupported kwargs cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-29 | Denial of Service | Rust panic escaping the FFI | mitigate | `catch_unwind` at the PyO3 boundary (BIND-09, Pitfall 14); panic→exception, never abort the interpreter |
| T-07-30 | Tampering | malformed/non-contiguous NumPy mo_coeff/mol | mitigate | `to_owned()` on non-standard-layout inputs; return C-contiguous arrays (BIND-04) |
| T-07-31 | Denial of Service | GIL re-entrancy in scanner/callback | mitigate | `Python::detach` on long compute; callbacks reacquire the GIL cleanly (BIND-05); kernel does NOT detach at top (hooks re-enter Python) |
| T-07-32 | Denial of Service | unbounded `maxsteps` from Python kwargs | mitigate | Default `maxsteps=100`; cap/validate at the bridge boundary |
| T-07-33 | Tampering | `constraints` kwarg silently ignored | mitigate | Surface the 07-06 clear error (D-07) — never a silent no-op |
| T-07-34 | Tampering | subclass override not dispatched | mitigate | `is_overridden` `__qualname__` MRO check + `call_method1` (Pitfall 7, Phase-3 precedent); subclass override wins the graft guard |
| T-07-SC | Tampering | crate deps | mitigate | No new registry packages; method crates stay pyo3-free; dependency-wall lint enforces no cubecl-* |
</threat_model>

<verification>
- `cargo check -p pyscf-py --locked` + `cargo test -p pyscf-py --locked -- --test-threads=1` green (scoped — NO libxc).
- `python/pyscf/geomopt/__init__.py` contains NO geometric/pyberny import (GEOMOPT-01 contract).
- Method crates (pyscf-grad/pyscf-geomopt) stay pyo3-free.
- `cargo run -p xtask --bin check_dependency_wall` passes.
</verification>

<success_criteria>
- `mf.nuc_grad_method().kernel()` is callable from Python for RHF/UHF/RKS/UKS/MP2/CCSD.
- `pyscf.geomopt.optimize(mf)` + `geometric_solver.optimize` + `berny_solver.optimize` run the
  native engine with NO geometric/pyberny import (GEOMOPT-01/02/03).
- The bridge follows the eager-snapshot + is_overridden + call_method1 + Python::detach pattern;
  method crates stay pyo3-free.
</success_criteria>

<output>
Create `.planning/phases/07-gradients-geomopt/07-09-SUMMARY.md` when done. Record the Python entry
points + the overlay grafts so the oracle/CI close-out (07-10) wires the no-runtime-dep proof +
the structural/upstream CI arms.
</output>
