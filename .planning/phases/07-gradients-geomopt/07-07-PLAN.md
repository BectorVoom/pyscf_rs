---
phase: 07-gradients-geomopt
plan: 07
type: execute
wave: 5
depends_on: ["07-03"]
files_modified:
  - crates/pyscf-grad/src/cphf.rs
  - crates/pyscf-grad/src/mp2.rs
  - crates/pyscf-grad/src/lib.rs
  - crates/pyscf-grad/tests/cphf.rs
  - crates/pyscf-grad/tests/mp2_verify_fd.rs
autonomous: true
requirements: [GRAD-05, GRAD-10]
must_haves:
  truths:
    - "ONE generic matrix-free Krylov CPHF/CPKS solver lives in pyscf-grad::cphf (D-03); a structural test asserts exactly one implementation (GRAD-10)"
    - "The solver takes a method-supplied aop (response operator) + RHS; never materializes the dense A-matrix"
    - "cphf::solve defaults max_cycle=50, tol=1e-9, level_shift=0; MP2 grad overrides max_cycle=30"
    - "MP2 gradients via the relaxed-density Lagrangian + Z-vector through the one CPHF solver match (GRAD-05)"
  artifacts:
    - path: "crates/pyscf-grad/src/cphf.rs"
      provides: "the single matrix-free Krylov CPHF/CPKS solver"
      contains: "pub fn solve"
      min_lines: 50
    - path: "crates/pyscf-grad/src/mp2.rs"
      provides: "MP2 relaxed-density Lagrangian + Z-vector (calls cphf)"
      contains: "max_cycle"
    - path: "crates/pyscf-grad/tests/cphf.rs"
      provides: "single-CPHF-implementation structural assertion (GRAD-10)"
      contains: "single_cphf_impl"
  key_links:
    - from: "crates/pyscf-grad/src/mp2.rs"
      to: "crates/pyscf-grad/src/cphf.rs solve"
      via: "Z-vector RHS + fvind response operator, max_cycle=30"
      pattern: "cphf::solve|cphf::solve"
    - from: "crates/pyscf-grad/src/cphf.rs"
      to: "pyscf_algebra gemm / oracle_dot"
      via: "matrix-free aop contractions"
      pattern: "gemm|oracle_dot"
---

<objective>
Build the ONE generic matrix-free Krylov CPHF/CPKS solver (D-03 / GRAD-10) in a new `cphf`
module inside `pyscf-grad`, then consume it for the first non-variational method gradient:
MP2 via the relaxed-density Lagrangian + Z-vector (GRAD-05, D-04).

Per D-03: ONE solver, not N — a new `cphf` module inside `pyscf-grad` (not a new crate, not in
`pyscf-scf`). Per D-04: the consumers are MP2-grad (this plan) and CCSD-grad (07-08) ONLY —
the variational HF/KS gradients are stationary and never call it. A single CI/structural test
asserts exactly one CPHF implementation (the GRAD-10 success criterion).

Per RESEARCH §"Pattern 2" + §"Don't Hand-Roll": NO matrix-free Krylov exists in `pyscf-algebra`
(only dense `solve_linear`). This is a genuine BUILD — port `pyscf/scf/cphf.py:solve` +
`lib.krylov` (Pople 1979). Exact upstream defaults: `max_cycle=50, tol=1e-9, hermi=False,
level_shift=0`; the MP2-grad caller OVERRIDES `max_cycle=30` (`mp2.py:280`). NEVER build the
dense A-matrix (O(nocc²nvir²) memory).

Purpose: Ship the single response-equation solver + the first Z-vector method. Output: the
cphf solver + MP2-grad body + the single-CPHF structural gate + the MP2 FD gate.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gradients-geomopt/07-CONTEXT.md
@.planning/phases/07-gradients-geomopt/07-RESEARCH.md
@.planning/phases/07-gradients-geomopt/07-PATTERNS.md
@.planning/phases/07-gradients-geomopt/07-03-SUMMARY.md

<interfaces>
From crates/pyscf-algebra (the only sanctioned contraction/reduction surface — algebra wall):
- pyscf_algebra::gemm, gemv, oracle_sum, oracle_dot
- pyscf_algebra::solve_linear(a, b, n)  // DENSE LU — NOT the CPHF Krylov; the Krylov is BUILT here

Exact upstream CPHF defaults to port (pyscf/scf/cphf.py:29-31):
  solve(fvind, mo_energy, mo_occ, h1, s1=None, max_cycle=50, tol=1e-9, hermi=False, level_shift=0)
  - s1 is None  -> solve_nos1 (MP2/CCSD Z-vector — the Phase-7 path)
  - s1 is Some  -> solve_withs1 (field-dependent; Hessian future, NOT Phase 7)
Matrix-free aop (cphf.py:73-79): vind_vo(mo1) = (fvind(mo1) [- mo1*level_shift]) * e_ai
  mo1 = lib.krylov(vind_vo, mo1base, tol=tol, max_cycle=max_cycle, hermi=hermi)
MP2 caller override (pyscf/grad/mp2.py:280): dvo = cphf.solve(fvind, mo_energy, mo_occ, Xvo, max_cycle=30)[0]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The single matrix-free Krylov CPHF/CPKS solver (D-03, GRAD-10)</name>
  <files>crates/pyscf-grad/src/cphf.rs, crates/pyscf-grad/src/lib.rs, crates/pyscf-grad/tests/cphf.rs</files>
  <read_first>
    - pyscf/scf/cphf.py:29-148 (solve / solve_nos1 / solve_withs1 — the exact defaults max_cycle=50, tol=1e-9, hermi=False, level_shift=0; the vind_vo matrix-free aop)
    - pyscf/lib/linalg_helper.py:1221 (lib.krylov — the Pople 1979 non-symmetric Krylov algorithm to port)
    - crates/pyscf-algebra/src/solve_linear.rs (the CALLER-SHAPE analog only — this is dense LU; the Krylov is built here over gemm/oracle_dot)
    - 07-PATTERNS.md §"crates/pyscf-grad/src/cphf.rs (D-03 — the single CPHF/CPKS solver, GRAD-10)" + §"No In-Tree Analog"
    - 07-RESEARCH.md §"Pattern 2: ONE matrix-free Krylov CPHF" + §"Pitfall 5" (CPHF for the right methods/defaults) + §"Anti-Patterns" (never build the dense A)
  </read_first>
  <behavior>
    - Test (krylov convergence): for a small known linear system (1+A)z=b with A applied via an aop callback, cphf::solve converges to the dense-solve_linear reference solution within tol.
    - Test (defaults): cphf::solve uses max_cycle=50, tol=1e-9, level_shift=0 by default; the caller can override max_cycle.
    - Test (single_cphf_impl, GRAD-10): a structural assertion that there is exactly ONE CPHF solve implementation in the crate (e.g. a grep-style or symbol-count assertion that no second solve exists).
  </behavior>
  <action>
    Create `crates/pyscf-grad/src/cphf.rs`: port `pyscf/scf/cphf.py:solve` + `lib.krylov`
    (Pople 1979). Signature: `solve(fvind, mo_energy, mo_occ, h1, s1=None, max_cycle=50, tol=1e-9,
    hermi=false, level_shift=0)` — the EXACT upstream defaults. For the Phase-7 path `s1=None` →
    the `solve_nos1` branch (MP2/CCSD Z-vector). The matrix-free `aop` (`vind_vo`): reshape `mo1`
    to `(nvir, nocc)`, apply the caller-supplied `fvind` response operator, optionally subtract
    `mo1*level_shift`, scale by `e_ai = 1/(e_a - e_i)`, reshape back. Run the Krylov iteration to
    `tol`/`max_cycle`. NEVER materialize the dense A-matrix (O(nocc²nvir²); RESEARCH anti-pattern).
    All contractions route through `pyscf_algebra` gemm/gemv; every reduction → `oracle_sum`/
    `oracle_dot` (Pitfall 1/2). Port `solve_withs1` only as a stub/`unimplemented`-with-clear-error
    (it is the Hessian-future field-dependent branch, NOT Phase 7). Create
    `crates/pyscf-grad/tests/cphf.rs` per the <behavior> block — INCLUDING the `single_cphf_impl`
    structural assertion (GRAD-10).
  </action>
  <verify>
    <automated>cargo test -p pyscf-grad --locked -- --test-threads=1 cphf single_cphf_impl</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-grad/src/cphf.rs` contains `pub fn solve(` with the exact defaults `max_cycle=50, tol=1e-9, level_shift=0` (`grep -n "max_cycle" crates/pyscf-grad/src/cphf.rs` shows the 50 default)
    - cphf.rs contains NO dense-A-matrix construction (matrix-free aop only — `grep -ni "dense.*a.*matrix\|materialize.*a" ` finds no full-A build)
    - `crates/pyscf-grad/tests/cphf.rs` contains a `single_cphf_impl` test (GRAD-10) and a krylov-converges-to-solve_linear-reference test
    - `cargo test -p pyscf-grad --locked cphf single_cphf_impl` exits 0
  </acceptance_criteria>
  <done>
    The single matrix-free Krylov CPHF/CPKS solver compiles, converges on a known system, uses the
    exact upstream defaults, and the GRAD-10 single-implementation structural test passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MP2 gradient — relaxed-density Lagrangian + Z-vector (GRAD-05)</name>
  <files>crates/pyscf-grad/src/mp2.rs, crates/pyscf-grad/src/lib.rs, crates/pyscf-grad/tests/mp2_verify_fd.rs</files>
  <read_first>
    - pyscf/grad/mp2.py:268-280 (_response_dm1: the fvind response operator + dvo = cphf.solve(fvind, mo_energy, mo_occ, Xvo, max_cycle=30)[0]) + the full mp2.py grad_elec (relaxed-density Lagrangian)
    - pyscf/grad/ump2.py (the spin-resolved MP2 grad — if UMP2 grad is in-scope this phase, else note RHF-MP2 first)
    - crates/pyscf-grad/src/cphf.rs (Task 1 — the solve signature MP2 calls with max_cycle=30)
    - crates/pyscf-mp2/src/lib.rs + crates/pyscf-mp2/src/rdm.rs (MP2 amplitudes + as_scanner (MP2-07) — the Z-vector RHS source + the relaxed-density assembly shape)
    - crates/pyscf-grad/src/rhf.rs (07-03 — the grad_elec assembly the relaxed density plugs into)
    - 07-PATTERNS.md §"crates/pyscf-grad/src/mp2.rs / ccsd.rs (Z-vector methods)" + §"MP2 Z-vector call" + 07-03-SUMMARY.md (cintx gate status)
  </read_first>
  <behavior>
    - Test (mp2 structural): Mp2Gradients::kernel() returns (natm,3); the Z-vector solve goes through cphf::solve with max_cycle=30 (NOT 50).
    - Test (mp2_verify_fd): the analytical MP2 gradient matches FD of the MP2 as_scanner energy at <=1e-6 Ha/Bohr (or #[ignore]'d behind the cintx gate; structural always-on).
  </behavior>
  <action>
    Port `pyscf/grad/mp2.py` into `crates/pyscf-grad/src/mp2.rs`: build the relaxed-density
    Lagrangian from the Phase-5 MP2 amplitudes (`pyscf-mp2` + `pyscf-ao2mo` general/full transform),
    form the Z-vector RHS `Xvo` + the method-specific `fvind` response operator, and solve the
    orbital-response Z-vector via `cphf::solve(fvind, mo_energy, mo_occ, Xvo, max_cycle=30)` —
    OVERRIDING the default `max_cycle=50` to `30` (Pitfall 5 / mp2.py:280). Assemble the gradient
    by plugging the relaxed density into the RHF `grad_elec` decomposition (07-03). Every reduction
    → `oracle_sum`/`oracle_dot`. Create `crates/pyscf-grad/tests/mp2_verify_fd.rs` per the
    <behavior> block (numeric arm gated per the 07-01/07-03 cintx finding; structural always-on,
    INCLUDING the assertion that the Z-vector solve uses max_cycle=30).
  </action>
  <verify>
    <automated>cargo test -p pyscf-grad --locked -- --test-threads=1 mp2</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-grad/src/mp2.rs` calls `cphf::solve` with `max_cycle=30` (`grep -n "max_cycle" crates/pyscf-grad/src/mp2.rs` shows 30, NOT 50)
    - `crates/pyscf-grad/src/mp2.rs` builds the relaxed-density Lagrangian from the Phase-5 MP2 amplitudes
    - `crates/pyscf-grad/tests/mp2_verify_fd.rs` calls `verify_fd(disp=1e-4)` (numeric un-gated or `#[ignore]`'d per the cintx finding)
    - `cargo test -p pyscf-grad --locked mp2` exits 0 (NO libxc pulled)
  </acceptance_criteria>
  <done>
    MP2 gradient via the relaxed-density Lagrangian + Z-vector through the ONE cphf solver
    (max_cycle=30) compiles, returns (natm,3), and is FD-gated (GRAD-05).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user/caller max_cycle ↔ cphf::solve | an unbounded max_cycle would loop forever |
| aop response operator ↔ Krylov iteration | a wrong fvind silently converges to a wrong Z-vector |
| Z-vector reductions ↔ relaxed density | bare `+=` makes the MP2 gradient thread-dependent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-21 | Denial of Service | unbounded `max_cycle` in cphf::solve | mitigate | Default `max_cycle=50` (MP2 override 30); cap/validate; a non-converged solve returns a clear error, never an infinite loop |
| T-07-22 | Tampering | dense A-matrix memory blow-up | mitigate | Matrix-free `aop` only (RESEARCH anti-pattern); never materialize O(nocc²nvir²) — bounds memory |
| T-07-23 | Tampering | CPHF reduction / aop drift | mitigate | All contractions via `pyscf_algebra` gemm; reductions via `oracle_sum`/`oracle_dot` (Pitfall 1/2); FD gate catches drift |
| T-07-24 | Tampering | N CPHF copies (GRAD-10 violation) | mitigate | ONE solver in `pyscf-grad::cphf` (D-03); the `single_cphf_impl` structural test forbids a second implementation |
| T-07-SC | Tampering | crate deps | mitigate | No new registry packages; dependency-wall lint enforces no cubecl-* |
</threat_model>

<verification>
- `cargo test -p pyscf-grad --locked -- --test-threads=1` green (scoped — NO libxc).
- `single_cphf_impl` (GRAD-10) passes; MP2 Z-vector uses max_cycle=30; MP2 FD gate present.
- No dense A-matrix construction in cphf.rs.
</verification>

<success_criteria>
- ONE matrix-free Krylov CPHF/CPKS solver (D-03) with the exact upstream defaults; the GRAD-10
  single-implementation structural test passes.
- MP2 gradient via the relaxed-density Lagrangian + Z-vector through that one solver (max_cycle=30),
  FD-gated (GRAD-05).
</success_criteria>

<output>
Create `.planning/phases/07-gradients-geomopt/07-07-SUMMARY.md` when done. MUST record the
`cphf::solve` signature + defaults, the aop/RHS contract, and the MP2 Z-vector wiring so CCSD-grad
(07-08) consumes the SAME solver with its own fvind/RHS.
</output>
