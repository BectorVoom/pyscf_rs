---
phase: 13-ft-ao-aftdf
plan: 08
type: verify
wave: 6
depends_on: [13-06, 13-07]
files_modified:
  - .planning/phases/13-ft-ao-aftdf/13-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [PBC-ORACLE-02]
must_haves:
  truths:
    - "the Gate-2 upstream convergence table is MEASURED, not asserted from the roadmap's 1e-13"
    - "if upstream itself does not reach 1e-13, the measured upstream value becomes the gate and the roadmap text is corrected"
    - "every gate number in 13-VERIFICATION.md is a measurement with the command that produced it"
  artifacts:
    - path: ".planning/phases/13-ft-ao-aftdf/13-VERIFICATION.md"
      provides: "the phase's closing evidence"
      contains: "Gate"
---

<objective>
Close Phase 13: measure the three gates, write `13-VERIFICATION.md`, tick the
ROADMAP, advance `STATE.md`.

**The authoritative task body is `.planning/pbc/PBC-MASTER-PLAN.md` §8.5, plan 13-08**,
which carries the gate table, the Gate-2 convergence-law wording and the
measurement script.
</objective>

<context>
@.planning/pbc/PBC-MASTER-PLAN.md   # §8.5 plan 13-08, §9 testing strategy, §10 risks R-08/R-15/R-16
@.planning/phases/13-ft-ao-aftdf/13-CONTEXT.md
@.planning/phases/12-periodic-dft/12-VERIFICATION.md   # the format, and §1d's precedent for retiring an unreachable gate
</context>

<tasks>

<task type="auto">
  <name>Task 0: run this BEFORE plan 13-01 starts, not here</name>
  The upstream `|E_AFTDF − E_FFTDF|` convergence table over meshes
  `[21, 31, 41, 55, 71]` is the phase's pre-recorded target (Phase-9 precedent).
  If it has not already been measured and pasted into `13-VERIFICATION.md` §1,
  measure it now and say plainly, in §1, that it was recorded late.
</task>

<task type="auto">
  <name>Task 1: measure the three gates</name>
  Gate 1 (`ft_aopair[G=0] == int1e_ovlp`, 1e-10, no oracle);
  Gate 2 (AFTDF vs FFTDF KRHF, the convergence law against the pre-recorded target);
  Gate 3 (upstream 2.12.1 oracle, 1e-11, on `get_nuc`/`get_pp`/`get_jk`/`get_eri`
  and the converged KRHF energy). Record the exact command for each.
</task>

<task type="auto">
  <name>Task 2: roll up what the phase learned</name>
  The `get_pp` cross-builder table from 13-04 (isolates `ft_aopair` from the SCF);
  the `FtKernel::build` and `ft_loop` wall-clock numbers, so Phase 14's D-PBC-21
  decision about the BvK bucket contraction has a number behind it; the measured
  size of the R-15 exxdiv term; and the green/red status of
  `check-orphan-modules`, `check_dependency_wall`, `check_no_fma`.
</task>

<task type="auto">
  <name>Task 3: correct the record</name>
  If Gate 2's 1e-13 turned out to be unreachable, correct the ROADMAP's Phase 13
  line to the measured value and explain why in `13-VERIFICATION.md` §1 — the same
  thing Phase 12 §1d did for the proposed 1e-15 gate. Do not quietly ship against
  a different number than the roadmap claims.
</task>

</tasks>

<verification>
```bash
cargo test --workspace
PYSCF_ORACLE_VENV=1 cargo test -p pyscf-pbc-df -p pyscf-pbc-scf -- --include-ignored
cargo run -p xtask --bin check_orphan_modules
cargo run -p xtask --bin check_dependency_wall
```
`13-VERIFICATION.md` written, ROADMAP Phase 13 ticked, `STATE.md` advanced to
Phase 14.
</verification>
