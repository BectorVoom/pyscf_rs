---
phase: 13-ft-ao-aftdf
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pyscf-gto/src/ft_ao.rs
  - crates/pyscf-gto/src/lib.rs
  - crates/pyscf-gto/tests/ft_ao.rs
  - crates/pyscf-pbc-df/src/ft_ao/mod.rs
  - crates/pyscf-pbc-df/src/ft_ao/fake_nuc.rs
autonomous: true
requirements: [PBC-DF-05]
must_haves:
  truths:
    - "the angular factor calls mcmurchie::e_coefficients from plan 13-01 — there is ONE recursion in the workspace"
    - "_fake_nuc is a line-by-line port of aft.py:247-274, including eta = 0.5/rloc^2 and eta = 1e16"
    - "tests live in crates/*/tests/ — never inside a source file (AGENTS.md §2)"
  artifacts:
    - path: "crates/pyscf-gto/tests/ft_ao.rs"
      provides: "numeric acceptance gate for this plan"
      contains: "assert"
---

<objective>
The single-centre AO Fourier transform `ft_ao` — molecular, plus the two-line
periodic wrapper — and the fake nuclear cell `_fake_nuc` that the all-electron
`get_nuc` branch runs on.

**The authoritative task body is `.planning/pbc/PBC-MASTER-PLAN.md` §8.5, plan 13-02.**
</objective>

<context>
@.planning/pbc/PBC-MASTER-PLAN.md   # §8.5 plan 13-02
@.planning/phases/13-ft-ao-aftdf/13-CONTEXT.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: read the upstream source</name>
  `pyscf/gto/ft_ao.py` (the `ft_ao` entry point), `pyscf/pbc/df/ft_ao.py:93-100`
  (the periodic wrapper — note there is NO lattice sum in it), and
  `pyscf/pbc/df/aft.py:247-274` (`_fake_nuc`).
</task>

<task type="auto">
  <name>Task 2: implement the STEPS</name>
  STEPS 1–3 of §8.5 plan 13-02. The angular factor is the `j = 0` special case of
  plan 13-01's `E` table — CALL `mcmurchie::e_coefficients(l, 0, alpha, 0, 0, 0)`.
  Writing a second recursion is a plan violation: one recursion, one place to be
  wrong.
</task>

<task type="test">
  <name>Task 3: tests/ft_ao.rs</name>
  The five assertions in the plan's TEST block. Test 5 — the `_fake_nuc`
  charge-weighted `ft_ao` reproducing `get_gth_vlocG_part1 × SI` to 1e-12 — is the
  identity that makes 13-04's two `get_nuc` branches consistent; do not skip it.
</task>

</tasks>

<verification>
```bash
cargo test -p pyscf-gto --test ft_ao
cargo clippy -p pyscf-gto -p pyscf-pbc-df --all-targets --all-features -- -D warnings
```
Then write `13-02-SUMMARY.md`.
</verification>
