---
phase: 13-ft-ao-aftdf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pyscf-kernels/src/pbc/ft_aopair.rs
  - crates/pyscf-kernels/src/pbc/mod.rs
  - crates/pyscf-kernels/tests/pbc_ft_aopair.rs
  - crates/pyscf-pbc-df/src/ft_ao/mod.rs
  - crates/pyscf-pbc-df/src/ft_ao/mcmurchie.rs
  - crates/pyscf-pbc-df/src/lib.rs
  - crates/pyscf-pbc-df/Cargo.toml
autonomous: true
requirements: [PBC-DF-04]
must_haves:
  truths:
    - "every function is a line-by-line port of the upstream Python named in the PORT block"
    - "tests live in crates/*/tests/ — never inside a source file (AGENTS.md §2)"
    - "the cubecl kernel is generic over F: Float and launched via dispatch_backend!"
    - "scalar math inside the #[cube] body is cube-math with MathConfig::EXACT — never f64::exp/sin/cos"
    - "the McMurchie–Davidson E-coefficient recursion runs on the HOST; the device does no recursion"
  artifacts:
    - path: "crates/pyscf-kernels/tests/pbc_ft_aopair.rs"
      provides: "numeric acceptance gate for this plan, including Gate 1 of the phase"
      contains: "assert"
---

<objective>
The `ft_aopair` McMurchie–Davidson cubecl kernel (K-15) — the largest new kernel
in v2.0 — plus the host E-coefficient recursion and the `q = 0`, single-k,
`aosym = 's1'` driver.

**The authoritative task body for this plan is `.planning/pbc/PBC-MASTER-PLAN.md`
§8.5, plan 13-01.** It carries the exact MD recursion, the host/device split, the
kernel design, the ordered STEPS, the TEST assertions and the DONE command. Open
it and follow it literally. Do not improvise an algorithm — R-08 in the risk
register is precisely "the recursion is wrong at `l ≥ 2`".
</objective>

<context>
@.planning/pbc/PBC-MASTER-PLAN.md   # §0 rules, §3 D-PBC-21, §6 K-15, §8.5 plan 13-01, §11 formula appendix
@.planning/phases/13-ft-ao-aftdf/13-CONTEXT.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: read the upstream source and the manual</name>
  Run `sed -n '48,90p' pyscf/pbc/df/ft_ao.py` and `sed -n '744,790p' pyscf/pbc/df/ft_ao.py`
  and read them before writing any Rust. Read the CubeCL manual index at
  `/home/user/Documents/workspace/cubecl_manual/manual/manual/Cubecl/INDEX.md`
  (AGENTS.md §3) and re-read `crates/pyscf-kernels/src/pbc/struct_factor.rs` — it is
  the shape this kernel must copy (comptime BLOCK, `launch_unchecked`, core launch on
  resident handles, `Runtime` never in a public signature).
  Run `codegraph_explore` on `cart2sph_coeff`, `get_lattice_ls`, `pbc_intor`,
  `dispatch_backend` so you edit with the blast radius in view (RULE 3).
</task>

<task type="auto">
  <name>Task 2: dependency wiring</name>
  Add `pyscf-kernels = { path = "../pyscf-kernels" }` to `crates/pyscf-pbc-df/Cargo.toml`.
  `pyscf-pbc-gto` already depends on it, so no new exemption is needed — run
  `cargo run -p xtask --bin check_dependency_wall` and confirm it stays green.
</task>

<task type="auto">
  <name>Task 3: implement the STEPS</name>
  Execute STEPS 1–7 from PBC-MASTER-PLAN §8.5 plan 13-01 in order:
  `estimate_rcut` (both refinement passes), `mcmurchie::e_coefficients`, the record
  table, the device kernel, the launch, cart→sph, and the `q = 0`/single-k/`s1`
  restriction. Keep upstream variable names in comments so a reviewer can diff
  against the Python. Any branch this plan defers returns
  `PbcDfError`/`PyscfRsError::NotYetImplemented { phase: 13, what: "..." }` naming
  plan 13-03 — never a silently wrong answer (D-PBC-20).
</task>

<task type="test">
  <name>Task 4: tests/pbc_ft_aopair.rs</name>
  Write exactly the five assertions listed in the plan's TEST block, tier-1 first:
  the s-s closed form (1e-14), the dense-grid numerical FT (1e-6, `l ≤ 2`),
  **Gate 1** — `ft_aopair[G=0] == pbc_intor("int1e_ovlp")` to 1e-10 — the
  `conj(ft[νμ, −G])` hermiticity identity (1e-13), and the oracle-gated
  `ft_ao.ft_aopair` comparison (1e-10) behind `PYSCF_ORACLE_VENV`.
  Gate 1 must pass unconditionally, with no oracle.
</task>

</tasks>

<verification>
```bash
cargo test -p pyscf-kernels --test pbc_ft_aopair
cargo test -p pyscf-kernels --all-features
cargo clippy -p pyscf-kernels -p pyscf-pbc-df --all-targets --all-features -- -D warnings
cargo run -p xtask --bin check_dependency_wall
cargo run -p xtask --bin check_orphan_modules
```
Then write `13-01-SUMMARY.md`: what shipped, the green test command, the measured
Gate 1 residual, deviations, carry-overs.
</verification>
