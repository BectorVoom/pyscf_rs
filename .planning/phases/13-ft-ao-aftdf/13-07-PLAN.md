---
phase: 13-ft-ao-aftdf
plan: 07
type: execute
wave: 5
depends_on: [13-05]
files_modified:
  - crates/pyscf-pbc-df/src/fftdf.rs
  - crates/pyscf-pbc-df/src/traits.rs
  - crates/pyscf-pbc-scf/src/krhf.rs
  - crates/pyscf-pbc-scf/src/kuhf.rs
  - crates/pyscf-pbc-scf/src/krohf.rs
  - crates/pyscf-pbc-scf/src/kghf.rs
  - crates/pyscf-pbc-scf/src/gamma.rs
  - crates/pyscf-pbc-dft/src/lib.rs
  - crates/pyscf-py/src/bridge.rs
  - crates/pyscf-pbc-scf/tests/df_swap.rs
autonomous: true
requirements: [PBC-DF-10, PBC-SCF-09]
must_haves:
  truths:
    - "with_df is Box<dyn PeriodicDf> — NOT a generic parameter D: PeriodicDf (D-PBC-22)"
    - "the FFTDF path is BIT-IDENTICAL to what it produced before this plan — assert_eq!, not a tolerance"
    - "KRHF::new still constructs an Fftdf by default, matching upstream"
  artifacts:
    - path: "crates/pyscf-pbc-scf/tests/df_swap.rs"
      provides: "proof the boxing changed no number, and that AFTDF is a drop-in"
      contains: "assert_eq!"
---

<objective>
Make every k-point driver builder-agnostic (D-PBC-22). **Without this plan the
phase's own Gate 2 cannot be measured** — `KRHF { pub with_df: Fftdf }` and
`pyscf_pbc_df::get_{nuc,pp,hcore}(df: &Fftdf, …)` mean nothing downstream can be
handed an AFTDF.

**The authoritative task body is `.planning/pbc/PBC-MASTER-PLAN.md` §8.5, plan 13-07.**
</objective>

<context>
@.planning/pbc/PBC-MASTER-PLAN.md   # §3 D-PBC-22, §8.5 plan 13-07
@.planning/phases/13-ft-ao-aftdf/13-CONTEXT.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: map the blast radius before touching anything</name>
  `codegraph_explore` on `Fftdf`, `PeriodicDf`, `get_hcore`, `with_df`. Every site
  that names the concrete `Fftdf` must be enumerated first — this plan crosses four
  crates and the PyO3 bridge, and a missed site is a compile error at best and a
  silent second code path at worst.
</task>

<task type="auto">
  <name>Task 2: implement the STEPS</name>
  STEPS 1–5 of §8.5 plan 13-07. STEP 3 is conditional: widen `PeriodicDf` to
  `Send + Sync` only if the drivers are actually used across threads today — check
  before widening the bound.
</task>

<task type="test">
  <name>Task 3: tests/df_swap.rs</name>
  The three assertions in the plan's TEST block. Tests 1 and 2 are `assert_eq!` on
  the energy bits: this plan is a refactor and must move no number.
</task>

</tasks>

<verification>
```bash
cargo test -p pyscf-pbc-scf --test df_swap
cargo test -p pyscf-pbc-scf -p pyscf-pbc-dft -p pyscf-pbc-df
cargo test --workspace
cargo clippy --workspace --all-targets --all-features -- -D warnings
```
Then write `13-07-SUMMARY.md`, listing every call site that changed.
</verification>
