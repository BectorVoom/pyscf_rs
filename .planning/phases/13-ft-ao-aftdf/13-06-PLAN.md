---
phase: 13-ft-ao-aftdf
plan: 06
type: execute
wave: 4
depends_on: [13-04]
files_modified:
  - crates/pyscf-pbc-df/src/fft_ao2mo.rs
  - crates/pyscf-pbc-df/src/aft_ao2mo.rs
  - crates/pyscf-pbc-df/src/lib.rs
  - crates/pyscf-pbc-df/tests/pbc_ao2mo.rs
autonomous: true
requirements: [PBC-DF-09]
must_haves:
  truths:
    - "fft_ao2mo ships in this plan so aft_ao2mo has an independent same-phase cross-check"
    - "ao2mo_7d's shape and index order are a downstream contract for Phases 15/16 — stated in module docs AND asserted in a test"
    - "momentum conservation goes through pyscf_pbc_lib::get_kconserv, never a hand-rolled table"
  artifacts:
    - path: "crates/pyscf-pbc-df/tests/pbc_ao2mo.rs"
      provides: "numeric acceptance gate for both builders' ERI paths"
      contains: "assert"
---

<objective>
`fft_ao2mo` and `aft_ao2mo`: `get_ao_pairs_G`, `get_mo_pairs_G`, `get_eri`,
`general`, `ao2mo_7d`.

**The authoritative task body is `.planning/pbc/PBC-MASTER-PLAN.md` §8.5, plan 13-06**,
including the rationale for pulling `fft_ao2mo` (skipped in Phase 11) into this phase.
</objective>

<context>
@.planning/pbc/PBC-MASTER-PLAN.md   # §8.5 plan 13-06, §8.7 (the Phase-15 consumer)
@.planning/phases/13-ft-ao-aftdf/13-CONTEXT.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: read the upstream source</name>
  `pyscf/pbc/df/aft_ao2mo.py` (all 434 lines) and `pyscf/pbc/df/fft_ao2mo.py`.
  Then run `codegraph_explore` on `get_kconserv` and on the Phase-15 §8.7 plan
  entry, so `ao2mo_7d`'s index order is fixed once against its actual consumer.
</task>

<task type="auto">
  <name>Task 2: implement the STEPS</name>
  STEPS 1–4 of §8.5 plan 13-06, `fft_ao2mo` first so the cross-check exists before
  `aft_ao2mo` is written.
</task>

<task type="test">
  <name>Task 3: tests/pbc_ao2mo.rs</name>
  The five assertions in the plan's TEST block. Test 2 (`general` with identity MO
  coefficients reproducing `get_eri`) is `assert_eq!` on the buffers — an identity
  transform must not perturb a bit.
</task>

</tasks>

<verification>
```bash
cargo test -p pyscf-pbc-df --test pbc_ao2mo
cargo clippy -p pyscf-pbc-df --all-targets --all-features -- -D warnings
cargo run -p xtask --bin check_orphan_modules
```
Then write `13-06-SUMMARY.md`, stating `ao2mo_7d`'s shape contract explicitly.
</verification>
