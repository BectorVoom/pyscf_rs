---
phase: 13-ft-ao-aftdf
plan: 03
type: execute
wave: 2
depends_on: [13-01, 13-02]
files_modified:
  - crates/pyscf-pbc-df/src/ft_ao/mod.rs
  - crates/pyscf-pbc-df/src/ft_ao/kernel.rs
  - crates/pyscf-pbc-df/tests/ft_ao.rs
autonomous: true
requirements: [PBC-DF-06]
must_haves:
  truths:
    - "the G-major output layout is stated ONCE in the module docs and never re-derived at a call site"
    - "block evaluation is BIT-IDENTICAL to a whole-mesh call — assert_eq!, not a tolerance"
    - "the BvK bucket contraction is deferred (D-PBC-21) and the deviation is recorded in the module docs"
  artifacts:
    - path: "crates/pyscf-pbc-df/tests/ft_ao.rs"
      provides: "numeric acceptance gate, including Gate 1 at k-resolution"
      contains: "assert"
---

<objective>
`ft_aopair_kpts`, the `FtKernel` object that replaces upstream's `gen_ft_kernel`
closure, `aosym` ∈ {`s1`, `s2`, `s1hermi`}, `shls_slice`, the `q` shift, and
G-blocking — everything `ft_loop`/`pw_loop` need in plan 13-04.

**The authoritative task body is `.planning/pbc/PBC-MASTER-PLAN.md` §8.5, plan 13-03.**
</objective>

<context>
@.planning/pbc/PBC-MASTER-PLAN.md   # §3 D-PBC-21, §8.5 plan 13-03
@.planning/phases/13-ft-ao-aftdf/13-CONTEXT.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: read the upstream source</name>
  `sed -n '63,90p' pyscf/pbc/df/ft_ao.py` (`ft_aopair_kpts`) and
  `sed -n '102,250p' pyscf/pbc/df/ft_ao.py` (`gen_ft_kernel` + the inner closure).
  Port the SHAPE and the `aosym` handling — not the `libpbc` ctypes plumbing.
  Read upstream's `s1hermi` comment at `:176-181`: it mirrors WITHOUT conjugation
  because at `q = k = 0` the pair density is real-symmetric.
</task>

<task type="auto">
  <name>Task 2: implement the STEPS</name>
  STEPS 1–7 of §8.5 plan 13-03. STEP 2 is the load-bearing deviation: one kernel
  launch per k-point with the Bloch phase folded into the per-image record on the
  host, INSTEAD of upstream's `bvk_ncells` bucket + `expLk` contraction. Record it
  in the module docs with the measured cost, per D-PBC-21.
</task>

<task type="test">
  <name>Task 3: tests/ft_ao.rs</name>
  The six assertions in the plan's TEST block. Test 1 is Gate 1 at k-resolution and
  must pass with no oracle. Tests 2 and 4 are `assert_eq!` on the raw buffers, not
  tolerance comparisons — `s2` unpacking and G-blocking must not perturb a bit.
</task>

</tasks>

<verification>
```bash
cargo test -p pyscf-pbc-df --test ft_ao
cargo clippy -p pyscf-pbc-df --all-targets --all-features -- -D warnings
cargo run -p xtask --bin check_orphan_modules
```
Then write `13-03-SUMMARY.md`, including the `FtKernel::build` and per-`ft_loop`-pass
wall-clock numbers plan 13-08 will roll up.
</verification>
