---
phase: 13-ft-ao-aftdf
plan: 05
type: execute
wave: 4
depends_on: [13-04]
files_modified:
  - crates/pyscf-pbc-df/src/aft_jk.rs
  - crates/pyscf-pbc-df/src/aftdf.rs
  - crates/pyscf-pbc-df/src/lib.rs
  - crates/pyscf-pbc-df/tests/aft_jk.rs
  - crates/pyscf-pbc-lib/src/kpts_helper.rs
  - crates/pyscf-pbc-lib/tests/kpts_helper.rs
autonomous: true
requirements: [PBC-DF-08, PBC-LIB-02]
must_haves:
  truths:
    - "get_k_kpts is ported STATEMENT BY STATEMENT with upstream's identifiers kept as Rust identifiers"
    - "the exxdiv='ewald' divergence from FFTDF is intentional (R-15) and is documented at the call site, not 'fixed'"
    - "_format_dms/_format_jks/_format_kpts_band are REUSED from crate::df_jk, never duplicated"
    - "the four alternative _update_vk* variants return NotYetImplemented { phase: 14 } rather than silently taking another route"
  artifacts:
    - path: "crates/pyscf-pbc-df/tests/aft_jk.rs"
      provides: "numeric acceptance gate, including the cross-builder J/K convergence table"
      contains: "assert"
    - path: "crates/pyscf-pbc-lib/tests/kpts_helper.rs"
      provides: "tier-1 gate for kk_adapted_iter / group_by_conj_pairs"
      contains: "assert"
---

<objective>
`aft_jk`: `get_j_kpts`, `get_k_kpts`, the two `*_for_bands` entry points,
`get_jk`, and the two `kpts_helper` functions they need and which
`pyscf-pbc-lib` does not have yet.

**The authoritative task body is `.planning/pbc/PBC-MASTER-PLAN.md` §8.5, plan 13-05.**
</objective>

<context>
@.planning/pbc/PBC-MASTER-PLAN.md   # §8.5 plan 13-05, §10 risk R-15
@.planning/phases/13-ft-ao-aftdf/13-CONTEXT.md
@.planning/phases/11-fft-fftdf-periodic-hf/11-VERIFICATION.md   # the 2.14 exxdiv trap, measured
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: read the upstream source — all of it, before writing anything</name>
  `pyscf/pbc/df/aft_jk.py` lines 41-94, 96-133, 135-293, 295-364, 366-418, 641-753,
  and `pyscf/pbc/lib/kpts_helper.py:170-268`. Then read
  `pyscf/pbc/tools/pbc.py:480-484` and `crates/pyscf-pbc-gto/src/coulg.rs:196-205`
  so the exxdiv divergence described in §8.5 plan 13-05 is understood BEFORE
  `get_k_kpts` is written. This is risk R-15: the difference is real, documented,
  and must not be "fixed".
</task>

<task type="auto">
  <name>Task 2: port kk_adapted_iter and group_by_conj_pairs</name>
  Into `crates/pyscf-pbc-lib/src/kpts_helper.rs`, with their own tier-1 tests:
  every k-point is covered exactly once; conjugate pairs are mutual; a gamma-only
  k-mesh yields the trivial partition.
</task>

<task type="auto">
  <name>Task 3: implement the STEPS</name>
  STEPS 1–6 of §8.5 plan 13-05. Keep upstream's identifiers (`vkR`, `vkI`,
  `wcoulG`, `kpti_idx`, `swap_2e`, `k_to_compute`, `t_rev_pairs`) as Rust
  identifiers — the same instruction plan 11-06 followed for `fft_jk.get_k_kpts`,
  and for the same reason: a reviewer must be able to diff against the Python line
  for line.
</task>

<task type="test">
  <name>Task 4: tests/aft_jk.rs</name>
  The five assertions in the plan's TEST block. Test 4 is the cross-builder J/K
  convergence table plus the `exxdiv=None` control; the control is what proves the
  R-15 divergence is confined to the `G+k = 0` term.
</task>

</tasks>

<verification>
```bash
cargo test -p pyscf-pbc-lib --test kpts_helper
cargo test -p pyscf-pbc-df --test aft_jk
cargo clippy -p pyscf-pbc-df -p pyscf-pbc-lib --all-targets --all-features -- -D warnings
```
Then write `13-05-SUMMARY.md` with the vj/vk convergence tables and the measured
size of the exxdiv term.
</verification>
