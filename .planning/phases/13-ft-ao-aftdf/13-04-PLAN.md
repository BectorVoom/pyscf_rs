---
phase: 13-ft-ao-aftdf
plan: 04
type: execute
wave: 3
depends_on: [13-03]
files_modified:
  - crates/pyscf-pbc-df/src/aftdf.rs
  - crates/pyscf-pbc-df/src/lib.rs
  - crates/pyscf-pbc-df/tests/aftdf.rs
autonomous: true
requirements: [PBC-DF-07]
must_haves:
  truths:
    - "get_pp reuses pyscf_pbc_gto::pseudo::{get_pp_loc_part2, get_pp_nl} — AFTDF and FFTDF differ ONLY in part 1"
    - "weighted_coulG calls the EXISTING pyscf_pbc_gto::get_coulg; there is no second exxdiv path"
    - "upstream's real/imaginary bookkeeping in _get_pp_loc_part1 is kept literally, including the is_zero(kpts[k]) guard"
  artifacts:
    - path: "crates/pyscf-pbc-df/tests/aftdf.rs"
      provides: "numeric acceptance gate, including the cross-builder convergence table"
      contains: "assert"
---

<objective>
`AFTDF`: the struct, `PeriodicDf` impl, `build`, `ft_loop`, `pw_loop`,
`weighted_coulG`, `estimate_ke_cutoff`, `_get_pp_loc_part1`, `get_nuc`, `get_pp`.

**The authoritative task body is `.planning/pbc/PBC-MASTER-PLAN.md` §8.5, plan 13-04.**
</objective>

<context>
@.planning/pbc/PBC-MASTER-PLAN.md   # §8.5 plan 13-04
@.planning/phases/13-ft-ao-aftdf/13-CONTEXT.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: read the upstream source</name>
  `pyscf/pbc/df/aft.py` lines 104-165, 186-234, 236-245, 247-274, 276-301,
  421-551, 585-770. Also re-read `crates/pyscf-pbc-df/src/fftdf.rs` — `Aftdf` is
  the second implementor of a trait that already has one, and the two must line up.
</task>

<task type="auto">
  <name>Task 2: implement the STEPS</name>
  STEPS 1–7 of §8.5 plan 13-04. Confirm `KE_SCALING` against the upstream import
  before hard-coding it. STEP 7 is the phase's structural payoff: `get_pp` is
  part 1 (new, analytic) + `get_pp_loc_part2` + `get_pp_nl` (both already shipped
  in Phase 10), so any `get_pp` deviation from FFTDF isolates `ft_aopair`.
</task>

<task type="test">
  <name>Task 3: tests/aftdf.rs</name>
  The four assertions in the plan's TEST block. Test 2 — the cross-builder
  convergence table over meshes `[21,31,41,55]` with a **monotonicity** assertion —
  is the phase's early-warning system: a deviation that stops falling is an
  `ft_aopair` bug, not a mesh effect. Record the table in the summary.
</task>

</tasks>

<verification>
```bash
cargo test -p pyscf-pbc-df --test aftdf
cargo clippy -p pyscf-pbc-df --all-targets --all-features -- -D warnings
```
Then write `13-04-SUMMARY.md` with the get_pp/get_nuc convergence tables.
</verification>
