---
phase: 14-gdf-mdf-rsdf-rsjk
plan: 09
type: verify
wave: 7
depends_on: [14-08]
files_modified:
  - .planning/phases/14-gdf-mdf-rsdf-rsjk/14-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/pbc/PBC-MASTER-PLAN.md
autonomous: true
requirements: [PBC-ORACLE-03]
must_haves:
  truths:
    - "every gate number in 14-VERIFICATION.md is a measurement, with the command that produced it"
    - "the roadmap's '1e-15 across builders' is CORRECTED in ROADMAP.md, not quietly shipped against"
    - "the D-PBC-23 deferral is priced in Ha and carried forward as ONE named carry-over"
  artifacts:
    - path: ".planning/phases/14-gdf-mdf-rsdf-rsjk/14-VERIFICATION.md"
      provides: "the phase's closing evidence"
      contains: "Gate"
---

<objective>
Close Phase 14: measure the five gates, write `14-VERIFICATION.md`, correct the
ROADMAP, advance `STATE.md`, and record D-PBC-23 in the master plan's decision
register.
</objective>

<context>
@.planning/phases/14-gdf-mdf-rsdf-rsjk/14-CONTEXT.md
@.planning/phases/14-gdf-mdf-rsdf-rsjk/measurements/README.md
@.planning/phases/13-ft-ao-aftdf/13-VERIFICATION.md   # the format, and its precedent for retiring an unreachable gate
@.planning/phases/12-periodic-dft/12-VERIFICATION.md  # §1d, the same precedent one phase earlier
</context>

<tasks>

<task type="auto">
  <name>Task 0: this was run BEFORE plan 14-01, not here</name>
  The pre-implementation measurements are already committed at
  `measurements/` (`_cells.py`, `smoke.py`, `params.py`, `rscell.py`,
  `ddblock.py`, `memory.py`, `builders.py`, `ccdf.py`, `mdfladder.py`) with
  results in `README.md`. **Re-run them, do not re-derive them.** If 14-07's
  `omega.py` was not added at the time, say so plainly in §1.
</task>

<task type="auto">
  <name>Task 1: measure the five gates</name>
  Record the exact command for each.
  * **Gate 1** — He-fcc/`sto-3g` 2×2×2, the all-electron control where
    `exclude_dd_block` is provably inert: `j2c`, `j3c`, `cderi`, `get_nuc`,
    `get_pp`, `vj`, `vk`, `get_eri` and the converged `KRHF` energy vs upstream
    2.12.1 at **1e-11**.
  * **Gate 1b** — diamond 2×2×2 and gamma: the same quantities at **1e-11** vs
    upstream with `exclude_dd_block = False`, and at **≤ 3e-8** vs upstream's
    default. Report BOTH; the second alone is not evidence.
  * **Gate 2** — the MDF mesh ladder against `measurements/mdfladder.out`.
  * **Gate 3** — `|E(GDF) − E(RSDF)|` against the upstream floor
    (1.353e-08 / 4.566e-09 / 1.113e-10), and additionally `|E(CC route) −
    E(RS route)|` against upstream's own 5.960e-07 / 4.502e-06 / 5.222e-10.
  * **Gate 4** — `_cderi` bytes vs the FFTDF AO table at mesh `[40,40,40]`,
    diamond **2×2×2** (upstream 6.17 %), with the 3×3×3 number (20.95 %) beside
    it and the reason the k-mesh is pinned.
</task>

<task type="auto">
  <name>Task 2: roll up what the phase learned</name>
  * The **D-PBC-23** deferral, priced: 1.835e-8 (diamond 2×2×2), 2.900e-8
    (gamma), 0 (He-fcc), and the fact that closing it is ONE piece of work —
    `_RangeSeparatedCell` + `_int_dd_block` — that also closes Phase 13's
    remaining `ft_aopair` screening residual (5.121e-10) and feeds Phase 17.
    One carry-over, not three.
  * The wall-clock of `aux_e2`, `make_j3c`, `get_k_kpts` and a full `KRHF` for
    each builder, beside upstream's (6.4 s GDF / 13.5 s RSDF / 16.9 s MDF /
    30.0 s FFTDF / 450.6 s AFTDF on diamond 2×2×2). GDF being the *fastest*
    upstream builder is the phase's whole point; if the port inverts that
    ordering, say so and say why.
  * `_cderi` file sizes vs `measurements/memory.py`.
  * The green/red status of `check-orphan-modules`, `check_dependency_wall`
    (including the D-07 / `hdf5-metno` resolution from 14-03 Task 0) and
    `check_no_fma`.
  * Every defect the phase's own tests caught, with what it was worth in Ha —
    the section Phases 11, 12 and 13 each ended with, and the one that makes the
    verification document worth re-reading.
</task>

<task type="auto">
  <name>Task 3: correct the record</name>
  The ROADMAP's Phase-14 line says **"every DF builder gives the same KRHF
  energy to 1e-15 with GDF under 20% of FFTDF memory."** Both halves are wrong
  and `measurements/README.md` says why:
  * GDF is an *approximation*; `FFTDF − GDF` is **1.222e-03** on diamond and
    that is the DF fitting error, not an implementation error. Upstream's own
    two GDF builders disagree by up to **4.502e-06**. Rewrite the line as the
    five gates above.
  * The memory ratio is **k-mesh dependent** — 6.17 % at 2×2×2, 20.95 % at
    3×3×3 — so the gate must name its k-mesh.
  Do the same thing Phase 12 §1d and Phase 13 did: correct the ROADMAP text,
  and explain the correction in `14-VERIFICATION.md` §1. **Do not quietly ship
  against a different number than the roadmap claims.**
  Also add **D-PBC-23** to `PBC-MASTER-PLAN.md` §3's decision register, and
  correct §8.6's plan table where this phase's plans diverged from it.
</task>

<task type="auto">
  <name>Task 4: advance the state</name>
  `STATE.md`: Phase 14 status, the carry-overs (D-PBC-23; the `rsjk` MPI
  variants; `_prefer_ccdf`'s flip; anything 14-07 blocked on in cintx), and the
  next phase. Confirm Phase 15's two prerequisites are now met: `ao2mo_7d`'s
  index order (14-05) and a GDF that KMP2 can read (`sr_loop`, 14-03).
</task>

</tasks>

<verification>
```bash
cargo test --workspace
PYSCF_ORACLE_VENV=1 cargo test -p pyscf-pbc-df -p pyscf-pbc-scf -p pyscf-pbc-dft -- --include-ignored
cargo run -p xtask --bin check_orphan_modules
cargo run -p xtask --bin check_dependency_wall
cargo run -p xtask --bin check_no_fma
```
`14-VERIFICATION.md` written, ROADMAP Phase 14 ticked and its gate line
corrected, `PBC-MASTER-PLAN.md` §3 carrying D-PBC-23, `STATE.md` advanced to
Phase 15.
</verification>
