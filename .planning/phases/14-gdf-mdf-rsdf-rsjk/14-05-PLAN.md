---
phase: 14-gdf-mdf-rsdf-rsjk
plan: 05
type: execute
wave: 4
depends_on: [14-04]
files_modified:
  - crates/pyscf-pbc-df/src/df_ao2mo.rs
  - crates/pyscf-pbc-df/src/outcore.rs
  - crates/pyscf-pbc-df/src/pbc_ao2mo.rs
  - crates/pyscf-pbc-df/src/lib.rs
  - crates/pyscf-pbc-df/tests/df_ao2mo.rs
autonomous: true
requirements: [PBC-DF-09, PBC-AO2MO-01]
must_haves:
  truths:
    - "ao2mo_7d's index order is a CONTRACT with Phase 15's KMP2 — it is fixed here and documented, not guessed"
    - "13-06's deferred general/get_mo_pairs_G for FFTDF and AFTDF close in this plan, not in Phase 15"
    - "tests live in crates/*/tests/ — never inside a source file (AGENTS.md §2)"
  artifacts:
    - path: "crates/pyscf-pbc-df/tests/df_ao2mo.rs"
      provides: "the cross-builder ERI agreement and the ao2mo_7d shape contract"
      contains: "assert"
---

<objective>
MO-basis integrals from `cderi`, and the out-of-core 3-index driver.

Port target: `pyscf/pbc/df/df_ao2mo.py` — `get_eri` (:32), `general` (:107),
`ao2mo_7d` (:210), `_mo_as_complex` (:276), `_dtrans` (:285), `_ztrans` (:294),
`warn_pbc2d_eri` (:307) — and `pyscf/pbc/df/outcore.py` — `aux_e1` (:29),
`_aux_e2` (:147).

**This plan also closes Phase 13's carry-over.** Plan 13-06 shipped `get_eri`
and `get_ao_pairs_G` for FFTDF and AFTDF but deliberately did not ship
`general`, `get_mo_pairs_G` or `ao2mo_7d`, on the stated grounds that
`ao2mo_7d`'s index order is a contract with Phase 15's KMP2 and should not be
guessed. It is settled here, against upstream, for all three builders at once —
which is also what gives the shape a cross-builder test instead of a lone
assertion.
</objective>

<context>
@.planning/phases/14-gdf-mdf-rsdf-rsjk/14-CONTEXT.md
@.planning/phases/13-ft-ao-aftdf/13-VERIFICATION.md   # the 13-06 carry-over this plan closes
@crates/pyscf-pbc-df/src/pbc_ao2mo.rs                # what 13-06 did ship
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: read the upstream source and the Phase-13 carry-over</name>
  `sed -n '32,310p' pyscf/pbc/df/df_ao2mo.py`, `sed -n '29,250p' pyscf/pbc/df/outcore.py`,
  and `pyscf/pbc/ao2mo/eris.py` for the k-quadruple conventions.
  Re-read `13-VERIFICATION.md`'s §13-06 section and
  `crates/pyscf-pbc-df/src/pbc_ao2mo.rs` so the new code extends the shared
  contraction 13-06 built rather than adding a second one.
</task>

<task type="auto">
  <name>Task 2: df_ao2mo::get_eri and general</name>
  * `get_eri(mydf, kpts, compact)` — PORT `:32-106`. `(pq|rs)` at a k-quadruple
    from `Σ_P cderi[k1,k2][P,pq]* · cderi[k3,k4][P,rs]`, with the momentum
    conservation check `k1 - k2 + k3 - k4 ≈ 0` (mod a reciprocal vector) that
    upstream raises on.
  * `general(mydf, mo_coeffs, kpts, compact)` — PORT `:107-209`, through
    `_dtrans` / `_ztrans`. The real (gamma, real MOs) and complex paths are
    separate in upstream because the real one halves the flops; port both,
    and assert they agree at gamma.
  * `warn_pbc2d_eri` — the 2-D truncated-Coulomb warning. Keep it as a
    `tracing::warn!`, not a silent pass.
</task>

<task type="auto">
  <name>Task 3: ao2mo_7d — the Phase-15 contract</name>
  PORT `:210-275`. Output is `(nkpts, nkpts, nkpts, nmo, nmo, nmo, nmo)`:
  `eri[ki, kj, ka][i, j, a, b]` with `kb = kconserv[ki, ka, kj]`, matching the
  KMP2 formula in `PBC-MASTER-PLAN.md` §8.7. **Write that index order into the
  module doc comment, name Phase 15 as the consumer, and assert it in the
  test** — this is the thing 13-06 refused to guess, and the reason it refused
  was that a wrong guess is silent until KMP2's correlation energy is wrong in
  the fourth digit.
  Ship it for `GDF` here and, in the same pass, for `FFTDF` and `AFTDF`
  (closing 13-06) so the three can be cross-checked against each other.
</task>

<task type="auto">
  <name>Task 4: outcore</name>
  PORT `outcore.py:29-250`: `aux_e1` and `_aux_e2`, the blocked out-of-core
  drivers over 14-01's `aux_e2`, writing into 14-03's `CderiStore`. These exist
  so a large cell never materialises the full 3-index tensor; keep the blocking
  parameters (`max_memory`, `blksize`) as explicit inputs with the same
  defaults, not as hidden constants.
</task>

<task type="test">
  <name>Task 5: tests/df_ao2mo.rs</name>

  1. **8-fold symmetry at gamma, no oracle.** `(pq|rs) = (qp|rs) = (pq|sr) =
     (rs|pq)` to 1e-12. Phase 13 used exactly this residue as a screening probe
     for `ft_aopair`; here it tests the `cderi` contraction.
  2. **`compact` ↔ `s1`, no oracle**, bit-identical after packing.
  3. **`general` with identity MO coefficients == `get_eri`, no oracle**, 1e-13.
  4. **The `ao2mo_7d` shape contract, no oracle.** Shape, and
     `eri7d[ki,kj,ka] == general(mo[ki], mo[kj], mo[ka], mo[kb])` with
     `kb = kconserv[ki,ka,kj]`, to 1e-13, for every `(ki,kj,ka)` on diamond
     2×2×2. Assert against `pyscf_pbc_lib::kpts_helper::get_kconserv`, not
     against a hand-written table.
  5. **Cross-builder, no oracle.** `ao2mo_7d(GDF)` vs `ao2mo_7d(AFTDF)`:
     record the number; it is the DF fitting error (~1e-3 on the energy scale),
     so do NOT gate on it. `ao2mo_7d(FFTDF)` vs `ao2mo_7d(AFTDF)` gates at the
     Phase-13 floor.
  6. **Oracle (`PYSCF_ORACLE_VENV`).** `df_ao2mo.get_eri`, `.general` and
     `.ao2mo_7d` vs upstream at **1e-11** (He-fcc) and, with
     `exclude_dd_block = False`, **1e-11** (diamond).
</task>

</tasks>

<verification>
```bash
cargo test -p pyscf-pbc-df --test df_ao2mo --test pbc_ao2mo
PYSCF_ORACLE_VENV=1 cargo test -p pyscf-pbc-df -- --include-ignored
cargo clippy -p pyscf-pbc-df --all-targets --all-features -- -D warnings
cargo run -p xtask --bin check_orphan_modules
```
Then write `14-05-SUMMARY.md`, and state explicitly that Phase 13's `ao2mo_7d`
carry-over is CLOSED, with the index order written out, so Phase 15 can start
against a fixed contract.
</verification>
