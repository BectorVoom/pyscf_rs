---
phase: 05-mp2
plan: 05
type: execute
wave: 5
depends_on: [05-04]
files_modified:
  - crates/pyscf-mp2/src/dfmp2.rs
  - crates/pyscf-mp2/src/lib.rs
  - crates/pyscf-mp2/tests/dfmp2_structural.rs
autonomous: true
requirements: [MP2-04]
must_haves:
  truths:
    - "DFRMP2 reuses the RMP2 base and swaps the ERI source to the pyscf-df B-tensor (D-06 conventional path)"
    - "(ia|jb) = Σ_Q B^Q_ia · B^Q_jb assembled from the MO-transformed DF B-tensor"
    - "DF-MP2 uses the *-ri (mp2fit) aux default via pyscf_df::default_ri, NOT default_jkfit"
    - "The DF int3c2e_sph gating is propagated as an error; structural tests are always-on"
  artifacts:
    - path: "crates/pyscf-mp2/src/dfmp2.rs"
      provides: "DFRMP2/DFUMP2 conventional path + DF ao2mo hook"
      contains: "default_ri"
  key_links:
    - from: "crates/pyscf-mp2/src/dfmp2.rs"
      to: "pyscf_df::DfIntegrals"
      via: "B-tensor ERI source"
      pattern: "DfIntegrals|b_uvq|cholesky_eri"
    - from: "crates/pyscf-mp2/src/dfmp2.rs"
      to: "pyscf_df::default_ri"
      via: "mp2fit aux default"
      pattern: "default_ri"
---

<objective>
Add the conventional DF-MP2 path (MP2-04, D-06 primary): `DFRMP2`/`DFUMP2` reuse the RMP2/UMP2 base (plans 05-03/05-04) and provide a different `Mp2OverrideHooks::ao2mo` implementation that assembles the `(ia|jb)` block from the `pyscf-df` 3-tensor instead of the full AO ERI. This is the drop-in `mp.DFMP2(mf)` / `mf.density_fit().MP2()` contract target.

Purpose: MP2-04 conventional half. Upstream `DFRMP2` subclasses `mp2.RMP2` (dfmp2.py:124), so this is "swap the ERI source," not a parallel rewrite. The DF B-tensor assembly reuses `pyscf-df::cholesky_eri`/`DfIntegrals` (Phase-3 D-10, in-memory). DF numeric parity is CI-gated behind the same cintx#11 `int3c2e_sph` merge as DF-HF; the structural code + always-on tests ship now and need no change when cintx lands (D-05).
Output: A conventional DF-MP2 path (structural tests always-on; numeric oracle cintx-gated).
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mp2/05-RESEARCH.md
@.planning/phases/05-mp2/05-PATTERNS.md
@.planning/phases/05-mp2/05-03-SUMMARY.md
@.planning/phases/05-mp2/05-04-SUMMARY.md

<interfaces>
<!-- Concrete contracts. -->

From plans 05-03/05-04: `pyscf_mp2::{Mp2Reference, ChemistsEris, rmp2_kernel, ump2_kernel, Mp2OverrideHooks, NoMp2Overrides, get_nocc, get_nmo, Frozen}`.

pyscf-df surface (crates/pyscf-df/src/lib.rs, verified):
- `DfIntegrals { pub b_uvq: Vec<f64>, pub naux: usize, pub nao: usize }` — `b_uvq` is ROW-MAJOR `[nao,nao,naux]`: element `(mu,nu,q)` at `b_uvq[mu*nao*naux + nu*naux + q]`.
- `cholesky_eri(mol, auxbasis_name) -> Result<DfIntegrals, _>` — `auxbasis_name` is the basis NAME.
- `default_ri(basis) -> &'static str` (auxbasis.rs:76) — resolves the `*-ri`/mp2fit aux (e.g. `cc-pvdz` → `cc-pvdz-ri`). A2 CONFIRMED: DF-MP2 aux default IS `*-ri`, NOT `default_jkfit`. Upstream dfmp2.py:136 uses `make_auxbasis(mol, mp2fit=True)`.
- `cholesky_eri` already documents that `int3c2e_sph` returns the cintx-gated error/zero-buffer until cintx lands (cholesky_eri.rs:104-108) — DF-MP2 needs NO change when cintx merges (D-05).

DF-MP2 contraction (D-06): `(ia|jb) = Σ_Q B^Q_ia · B^Q_jb` where `B^Q_ia` = ao2mo-transform of `b_uvq` into the (occ,vir) MO block; the Q-sum via `oracle_dot`. Upstream uses the C driver `libmp.MP2_contract_d`; port the MATH as an `oracle_sum`/`oracle_dot` loop (NO C dep).

Upstream `DFRMP2(mp2.RMP2)` (dfmp2.py:124); `scf.hf.RHF.DFMP2 = lib.class_as_method(DFMP2)` (dfmp2.py:192); `DFUMP2` (dfump2.py).

oracle reductions: oracle_sum/oracle_dot. gemm NotYetImplemented{phase:2}. ao2mo transform from plan 05-02: `pyscf_ao2mo::general`.

dfmp2_structural.rs does NOT exist yet — create it (the 05-01 scaffold covered rmp2/ump2; this plan adds the dfmp2 test file).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DFRMP2/DFUMP2 conventional path (swap ERI source to DF B-tensor)</name>
  <files>crates/pyscf-mp2/src/dfmp2.rs, crates/pyscf-mp2/src/lib.rs</files>
  <read_first>
    - crates/pyscf-mp2/src/dfmp2.rs (the stub from 05-01)
    - crates/pyscf-mp2/src/mp2.rs (rmp2_kernel + ChemistsEris + default_ao2mo — the base this swaps the ERI source on)
    - crates/pyscf-mp2/src/ump2.rs (ump2_kernel — DFUMP2 reuses it)
    - crates/pyscf-mp2/src/hooks.rs (Mp2OverrideHooks — DF provides a different ao2mo impl)
    - crates/pyscf-df/src/cholesky_eri.rs (DfIntegrals layout + the int3c2e_sph gating doc at 104-108)
    - crates/pyscf-df/src/auxbasis.rs (default_ri — the mp2fit aux default at line 76)
    - pyscf/mp/dfmp2.py (DFRMP2:124, kernel:39-122, _make_df_eris:215, class_as_method:192)
    - pyscf/mp/dfump2.py (DFUMP2 open-shell)
    - crates/pyscf-ao2mo/src/incore.rs (general — for the per-Q MO transform of b_uvq)
  </read_first>
  <action>
    Implement `pub fn df_ao2mo(refr: &Mp2Reference, frozen: &Frozen, df: &DfIntegrals) -> Result<ChemistsEris, PyscfRsError>` in `dfmp2.rs`: transform the DF B-tensor `b_uvq` (AO basis, row-major `[nao,nao,naux]`) into the MO `(occ,vir)` block per Q — `B^Q_ia = Σ_{mu,nu} C_mu^i · b_uvq[mu,nu,Q] · C_nu^a` (use `pyscf_ao2mo::general` on the per-Q AO slice or an explicit two-index transform, materialize-then-`oracle_sum`). Then assemble `(ia|jb) = Σ_Q B^Q_ia · B^Q_jb` via `oracle_dot` over the Q axis for each `(ia,jb)` pair, packing into a `ChemistsEris { ovov, nocc, nvir }`. Doc-comment the exact flat-index of `b_uvq`, the MO `B^Q`, and the output `ovov` (Pitfall 3/8). Port the upstream `libmp.MP2_contract_d` MATH as the oracle_dot Q-loop — NO C dependency.

    Define `pub struct DFRMP2` (the conventional path) that holds an `Mp2Reference` + a `DfIntegrals` (assembled via `pyscf_df::cholesky_eri(mol, pyscf_df::default_ri(basis))` — A2: use `default_ri`, the mp2fit aux, NOT `default_jkfit`). Implement `Mp2OverrideHooks` for `DFRMP2` (or a `DfMp2Overrides` wrapper) whose `ao2mo` delegates to `df_ao2mo` (swap-the-source pattern, D-06); the kernel reuses `rmp2_kernel` verbatim with this hooks impl. Provide `pub fn dfrmp2_kernel(refr: &Mp2Reference, frozen: &Frozen, df: &DfIntegrals, with_t2: bool) -> Result<Mp2Result, PyscfRsError>` that wires `df_ao2mo` into `rmp2_kernel`.

    Define `pub struct DFUMP2` analogously for the open-shell conventional path (dfump2.py), reusing `ump2_kernel` with per-spin DF `ao2mo`. Provide `pub fn dfump2_kernel(...)`.

    Document the cintx#11 gating (D-05): `cholesky_eri` propagates the `int3c2e_sph` error today; `df_ao2mo` and the DF kernels `?`-propagate it (never panic, never substitute). When cintx lands, the same code returns the numeric result with no change. Re-export `DFRMP2`, `DFUMP2`, `dfrmp2_kernel`, `dfump2_kernel`, `df_ao2mo` from `lib.rs`.
  </action>
  <verify>
    <automated>cargo build -p pyscf-mp2 --locked && grep -q 'default_ri' crates/pyscf-mp2/src/dfmp2.rs && grep -q 'pub fn df_ao2mo' crates/pyscf-mp2/src/dfmp2.rs</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-mp2/src/dfmp2.rs` contains `pub fn df_ao2mo`, `pub struct DFRMP2`, `pub struct DFUMP2`, `pub fn dfrmp2_kernel`, `pub fn dfump2_kernel`
    - `dfmp2.rs` references `pyscf_df::default_ri` (the mp2fit aux) and does NOT reference `default_jkfit`
    - The `(ia|jb) = Σ_Q B^Q_ia·B^Q_jb` Q-sum uses `oracle_dot`/`oracle_sum` (no `acc +=` reduction)
    - `dfmp2.rs` does NOT panic or substitute a zero buffer on the int3c2e_sph gate — it `?`-propagates
    - `cargo build -p pyscf-mp2 --locked` exits 0
  </acceptance_criteria>
  <done>Conventional DF-MP2 (R+U) swaps the ERI source to the DF B-tensor via df_ao2mo, reuses the RMP2/UMP2 base, uses the *-ri aux default, and propagates the int3c2e_sph gate.</done>
</task>

<task type="auto">
  <name>Task 2: DF-MP2 structural/wiring tests (always-on, cintx-gated numeric separate)</name>
  <files>crates/pyscf-mp2/tests/dfmp2_structural.rs</files>
  <read_first>
    - crates/pyscf-mp2/src/dfmp2.rs (the surface from Task 1)
    - crates/pyscf-mp2/tests/rmp2_structural.rs (the always-on structural test shape from 05-03)
    - crates/pyscf-df/src/cholesky_eri.rs (DfIntegrals — for building a synthetic B-tensor in the test)
    - crates/pyscf-ao2mo/tests/transform_roundtrip.rs (the synthetic-input always-on test idiom from 05-02)
  </read_first>
  <action>
    Create `crates/pyscf-mp2/tests/dfmp2_structural.rs` with ALWAYS-ON tests (no cintx, no live intor):
    (1) Feed `df_ao2mo` a hand-built synthetic `DfIntegrals { b_uvq, naux, nao }` (small nao/naux) and a toy `Mp2Reference`; assert the resulting `(ia|jb)` block equals an independent longhand `Σ_Q B^Q_ia·B^Q_jb` reference computed inline (proves the DF contraction math un-gated, mirroring the 05-02 synthetic-ERI approach).
    (2) Assert `dfrmp2_kernel` on the synthetic DF block returns a finite e_corr matching a hand-computed closed-form value.
    (3) Structural: assert that `cholesky_eri` against a real Mole propagates the `int3c2e_sph` gate as an `Err` (does NOT panic) — the numeric DF-MP2 energy oracle is cintx-gated, but the error-propagation shape is always-on.
    Mark only the live-PySCF numeric parity assertions (none here — those live in the `dfmp2_energy` oracle arm) as out-of-scope; everything in this file is always-on synthetic.
  </action>
  <verify>
    <automated>cargo test -p pyscf-mp2 --locked dfmp2 2>&1 | grep -qE 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-mp2/tests/dfmp2_structural.rs` builds a synthetic `DfIntegrals` and asserts `df_ao2mo` matches a longhand `Σ_Q B^Q_ia·B^Q_jb` reference
    - A test asserts `dfrmp2_kernel` returns the hand-computed e_corr on the synthetic DF block
    - A test asserts `cholesky_eri` on a real Mole returns `Err` (no panic) on the int3c2e_sph gate
    - No test in this file is `#[ignore]`'d for the synthetic arms (always-on)
    - `cargo test -p pyscf-mp2 --locked dfmp2` exits 0
  </acceptance_criteria>
  <done>DF-MP2 structural + synthetic-contraction tests pass always-on; the int3c2e_sph gate error-propagation is asserted; numeric upstream parity stays in the cintx-gated oracle arm.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → df_ao2mo/dfrmp2_kernel | B-tensor shape (nao/naux), nocc/nvir untrusted; mismatch must error |
| pyscf-df::cholesky_eri → DF-MP2 | int3c2e_sph currently errors; the error must propagate, never be swallowed/substituted |
| (no Python boundary) | pyscf-mp2 pyo3-free; FFI exercised in 05-07 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-FFI | Denial of Service | df_ao2mo / dfrmp2_kernel / dfump2_kernel | mitigate | `?`-propagate the int3c2e_sph gate; no `unwrap()` in numeric paths (clippy-gated); never substitute a zero B-tensor (the Phase-4 CR-02 silent-substitution lesson) |
| T-05-05-FP | Tampering | Σ_Q B^Q·B^Q contraction | mitigate | Q-sum through `oracle_dot`; no `+=` accumulation (Pitfall 1/2, FOUND-06) |
| T-05-05-AUX | Tampering | DF aux default | mitigate | Use `pyscf_df::default_ri` (mp2fit `*-ri`), NOT `default_jkfit` (A2) — a wrong aux silently shifts the DF energy; pinned by the acceptance grep gate |
| T-05-05-LAYOUT | Tampering | b_uvq / B^Q / ovov flat-index | mitigate | Doc-comment every tensor's flat-index at every boundary (Pitfall 3/8); validated by the synthetic-contraction longhand-reference test |
| T-05-05-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages added (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `cargo test -p pyscf-mp2 --locked` green (dfmp2 structural + synthetic always-on).
- `cargo clippy -p pyscf-mp2 -- -D warnings` clean; `cargo fmt --check` clean.
- `xtask check-no-fma` clean; `xtask check-dependency-wall` clean (no cubecl).
- Grep confirms `default_ri` used, `default_jkfit` NOT used in dfmp2.rs.
</verification>

<success_criteria>
- Conventional `DFRMP2`/`DFUMP2` swap the ERI source to the DF B-tensor and reuse the RMP2/UMP2 base (D-06).
- `(ia|jb) = Σ_Q B^Q_ia·B^Q_jb` assembled via oracle_dot from the MO-transformed B-tensor.
- DF-MP2 uses the `*-ri` (mp2fit) aux via `default_ri`.
- The int3c2e_sph gate is propagated as an error; structural + synthetic-contraction tests are always-on.
</success_criteria>

<output>
Create `.planning/phases/05-mp2/05-05-SUMMARY.md` when done.
</output>
