---
phase: 05-mp2
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - crates/pyscf-mp2/src/mp2.rs
  - crates/pyscf-mp2/src/helpers.rs
  - crates/pyscf-mp2/src/frozen.rs
  - crates/pyscf-mp2/src/hooks.rs
  - crates/pyscf-mp2/src/lib.rs
  - crates/pyscf-mp2/tests/ccsd_import_contract.rs
  - crates/pyscf-mp2/tests/rmp2_structural.rs
autonomous: true
requirements: [MP2-01, MP2-03, MP2-06, MP2-08]
must_haves:
  truths:
    - "rmp2_kernel computes the closed-form MP2 correlation energy from an Mp2Reference + transformed (ia|jb) ERIs"
    - "The five MP2-08 helpers export with upstream semantics and the CCSD import contract test passes always-on"
    - "frozen=int/list/'auto'/window all resolve a frozen mask matching upstream defaults"
    - "SCS-MP2 ss/os factors split correctly; default factors reproduce plain MP2"
    - "Mp2OverrideHooks trait declared pyo3-free with a NoMp2Overrides default impl"
  artifacts:
    - path: "crates/pyscf-mp2/src/mp2.rs"
      provides: "Mp2Reference struct + rmp2_kernel closed-form energy + SCS energy split"
      contains: "pub fn rmp2_kernel"
    - path: "crates/pyscf-mp2/src/helpers.rs"
      provides: "MP2-08 helpers (get_nocc/get_nmo/get_frozen_mask/get_e_hf/mo_without_core)"
      contains: "pub fn get_nocc"
    - path: "crates/pyscf-mp2/src/frozen.rs"
      provides: "Frozen enum + chemcore 'auto' table + mask resolution"
      contains: "pub enum Frozen"
    - path: "crates/pyscf-mp2/src/hooks.rs"
      provides: "Mp2OverrideHooks trait + NoMp2Overrides (D-08)"
      contains: "pub trait Mp2OverrideHooks"
  key_links:
    - from: "crates/pyscf-mp2/src/mp2.rs"
      to: "pyscf_ao2mo::general"
      via: "ao2mo (ia|jb) block"
      pattern: "ao2mo|general"
    - from: "crates/pyscf-mp2/src/mp2.rs"
      to: "pyscf_algebra::oracle_sum"
      via: "energy reduction"
      pattern: "oracle_sum|oracle_dot"
    - from: "crates/pyscf-mp2/src/hooks.rs"
      to: "crates/pyscf-mp2/src/mp2.rs"
      via: "NoMp2Overrides::ao2mo delegates to default_ao2mo"
      pattern: "default_ao2mo"
---

<objective>
Fill the in-core RMP2 headline deliverable: the closed-form MP2 correlation-energy kernel (`rmp2_kernel`), the five MP2-08 helper functions that Phase-6 CCSD imports verbatim, frozen-core handling (`int`/`list`/`'auto'`/window), SCS-MP2 factors, and the `Mp2OverrideHooks` trait + `NoMp2Overrides` default (D-08). All pyo3-free.

Purpose: This is the core of MP2-01/03/06/08. The kernel asks `pyscf-ao2mo` (plan 05-02) for the `(ia|jb)` block, divides by `εi+εj−εa−εb` denominators, and reduces the energy through `oracle_sum`. The MP2-08 helpers are a HARD CCSD interface contract — they must export with the exact semantics `cc/ccsd.py:35` imports. Numeric energy parity is CI-gated behind cintx#11 (arity-4 `int2e`); the algorithm + always-on structural/helper tests ship now.
Output: A complete in-core RMP2 path (structural + helper tests always-on; energy oracle cintx-gated).
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mp2/05-RESEARCH.md
@.planning/phases/05-mp2/05-PATTERNS.md
@.planning/phases/05-mp2/05-02-SUMMARY.md

<interfaces>
<!-- Concrete contracts extracted this planning session. -->

Mp2Reference (D-07 snapshot struct — pyo3-free, takes plain arrays):
```
pub struct Mp2Reference {
    pub mo_coeff: MOCoefficients,   // F-order, sign-canonicalized (SCF-13)
    pub mo_energy: Vec<f64>,
    pub mo_occ: Vec<f64>,
    pub e_hf: f64,
    pub converged: bool,
    pub mol: Mole,                  // snapshot from mf.mol — needed by default_ao2mo for intor call
}
```
Consumes pyscf-scf::ScfResult fields (kernel.rs:52-57: e_tot/mo_coeff/mo_energy/mo_occ/converged) + `mf.mol` extracted by the pyscf-py bridge (05-07 T1). Storing `mol` on the reference keeps `default_ao2mo` and `Mp2OverrideHooks::ao2mo` free of a `&Mole` parameter — the mol is always available through `refr.mol`.

PostScf trait (crates/pyscf-core/src/traits.rs:42-51): `pub trait PostScf: Method { type Reference: Scf; fn reference(&self)->&Self::Reference; fn e_correlation(&self)->Result<Energy, PyscfRsError>; }`. Supertrait `Method` (traits.rs:12-18): `fn kernel(&mut self)->Result<Energy,_>; fn mol(&self)->&Mole;`.

ao2mo surface (plan 05-02): `pyscf_ao2mo::general(eri_ao, nao, [&MOCoefficients;4]) -> Result<Vec<f64>, Ao2moError>`. MP2 calls with `(co, cv, co, cv)` to get the `(ia|jb)` block (upstream mp2.py:_make_eris).

oracle reductions (crates/pyscf-algebra/src/oracle.rs): `oracle_sum(&[f64])->f64`, `oracle_dot(&[f64],&[f64])->f64`. `gemm` is NotYetImplemented{phase:2}.

MOCoefficients (crates/pyscf-core/src/mo.rs): `data` column-major; `C[:, :nocc]` = first nocc columns = `data[0..nocc*nao]`.

MP2-08 CCSD import contract (pyscf/cc/ccsd.py:35 — the test mimics VERBATIM):
`from pyscf.mp.mp2 import get_nocc, get_nmo, get_frozen_mask, get_e_hf, _mo_without_core`
Upstream semantics (pyscf/mp/mp2.py):
- get_nocc (line 351): `count_nonzero(mo_occ>0)` minus frozen-occ count
- get_nmo (line 371): `len(mo_occ)` minus frozen count
- get_frozen_mask (line 383): bool array, False at frozen indices
- get_e_hf (line 402): `mp._scf.e_tot` for the converged ref
- _mo_without_core (line 732): `mo[:, get_frozen_mask(mp)]`

MP2 closed-form math (pyscf/mp/mp2.py:47-76, canonical branch):
`eia = mo_energy[occ] - mo_energy[vir]`; for each i: `t2_ijab = (ia|jb)/(εi+εj−εa−εb)`; `edi = 2*Σ(ia|jb)·t2`, `exi = -Σ(ib|ja)·t2`; `emp2_ss += edi*0.5 + exi`, `emp2_os += edi*0.5`.

SCS-MP2 (mp2.py:117-126, 597-599): default factors ss=os=1.0 reproduce plain MP2; SCS defaults emp2_scs = e_ss*(1/3) + e_os*1.2. User factors via emp2_ss_factor/emp2_os_factor.

Frozen-core 'auto' (A1, mp2.py:575 → cc/ccsd.py set_frozen): chemcore element→core-count table. Static-table shape model: crates/pyscf-df/src/auxbasis.rs:15-58 (`static TABLE: OnceLock<HashMap>` + get_or_init + fallback).

Mp2Error + helpers/frozen/hooks/mp2 module stubs created in 05-01. helpers.rs already exports the five fn SIGNATURES (returning todo!()).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MP2-08 helpers + frozen-core resolution + CCSD import contract</name>
  <files>crates/pyscf-mp2/src/helpers.rs, crates/pyscf-mp2/src/frozen.rs, crates/pyscf-mp2/src/lib.rs, crates/pyscf-mp2/tests/ccsd_import_contract.rs</files>
  <read_first>
    - crates/pyscf-mp2/src/helpers.rs + frozen.rs (the stubs from 05-01)
    - crates/pyscf-mp2/tests/ccsd_import_contract.rs (the scaffold from 05-01)
    - pyscf/mp/mp2.py (get_nocc:351, get_nmo:371, get_frozen_mask:383, get_e_hf:402, _mo_without_core:732, _mo_splitter:206, set_frozen:575)
    - pyscf/cc/ccsd.py (the chemcore set_frozen 'auto' table source — locate the element→core-count data, A1)
    - crates/pyscf-df/src/auxbasis.rs (the OnceLock<HashMap> static-table shape for the 'auto' core table)
    - crates/pyscf-core/src/mo.rs (MOCoefficients — for mo_without_core column slicing)
  </read_first>
  <action>
    Define `pub enum Frozen` in `frozen.rs` with variants `None`, `Count(usize)`, `List(Vec<usize>)`, `Auto`, and a `Window` form matching upstream `frozen=int/list/'auto'/window`. Implement `pub fn frozen_mask(frozen: &Frozen, mo_occ: &[f64], elements: &[u32]) -> Result<Vec<bool>, Mp2Error>` returning the bool active-orbital mask (True = active, False = frozen), mirroring upstream `get_frozen_mask` semantics. For `Frozen::Auto`, port the chemcore element→core-count table from upstream `cc/ccsd.py` set_frozen into a `static CHEMCORE: OnceLock<HashMap<u32, usize>>` (the auxbasis.rs OnceLock shape) with the exact upstream per-element core-electron counts; the auto frozen count is `sum(per-atom core electrons) / 2`. Document the upstream table source line in a comment. Defaults must match upstream on the corpus.

    Implement the five MP2-08 helpers in `helpers.rs` with the exact upstream semantics (replace the 05-01 todo!() bodies):
    - `pub fn get_nocc(mo_occ: &[f64], frozen: &Frozen) -> Result<usize, Mp2Error>` — count of `mo_occ > 0` minus frozen-occupied count.
    - `pub fn get_nmo(mo_occ: &[f64], frozen: &Frozen) -> Result<usize, Mp2Error>` — `mo_occ.len()` minus frozen count.
    - `pub fn get_frozen_mask(mo_occ: &[f64], frozen: &Frozen) -> Result<Vec<bool>, Mp2Error>` — delegates to `frozen::frozen_mask` (active-orbital mask).
    - `pub fn get_e_hf(reference_e_tot: f64) -> f64` — returns the reference SCF e_tot (the converged-ref case).
    - `pub fn mo_without_core(mo: &MOCoefficients, frozen: &Frozen) -> Result<MOCoefficients, Mp2Error>` — returns `mo[:, active_mask]` (the column subset; upstream `_mo_without_core`). Doc-map the Rust name `mo_without_core` to the Python `_mo_without_core`.
    Re-export all five from `lib.rs` (already declared in 05-01) and add `pub use frozen::Frozen;`.

    Complete `crates/pyscf-mp2/tests/ccsd_import_contract.rs` (MP2-08, ALWAYS-ON): the test imports all five symbols `use pyscf_mp2::{get_nocc, get_nmo, get_frozen_mask, get_e_hf, mo_without_core};` (mirroring `cc/ccsd.py:35`), then asserts numeric semantics on a toy reference: `mo_occ = [2.0, 2.0, 0.0, 0.0]`, `Frozen::None` → `get_nocc==2`, `get_nmo==4`, `get_frozen_mask==[true,true,true,true]`; `Frozen::Count(1)` → `get_nocc==1`, `get_nmo==3`, mask `[false,true,true,true]`. Always-on (no cintx, no integrals).
  </action>
  <verify>
    <automated>cargo test -p pyscf-mp2 --locked ccsd_import_contract 2>&1 | grep -qE 'test result: ok' && cargo test -p pyscf-mp2 --locked frozen 2>&1 | grep -qE 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-mp2/src/helpers.rs` contains `pub fn get_nocc`, `pub fn get_nmo`, `pub fn get_frozen_mask`, `pub fn get_e_hf`, `pub fn mo_without_core`
    - `crates/pyscf-mp2/src/frozen.rs` contains `pub enum Frozen` with `Count`, `List`, `Auto` variants and a `static` chemcore table via `OnceLock`
    - `crates/pyscf-mp2/tests/ccsd_import_contract.rs` imports the exact five-symbol set and asserts `get_nocc==2`/`get_nmo==4` on `mo_occ=[2,2,0,0]` with `Frozen::None`, and the `Frozen::Count(1)` mask is `[false,true,true,true]`
    - The contract test is NOT `#[ignore]`'d (always-on)
    - `cargo test -p pyscf-mp2 --locked ccsd_import_contract` and `... frozen` exit 0
  </acceptance_criteria>
  <done>The five MP2-08 helpers export with upstream semantics; frozen=int/list/'auto'/window resolve correctly; the verbatim CCSD import contract passes always-on.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RMP2 closed-form kernel + SCS energy + Mp2OverrideHooks</name>
  <files>crates/pyscf-mp2/src/mp2.rs, crates/pyscf-mp2/src/hooks.rs, crates/pyscf-mp2/src/lib.rs, crates/pyscf-mp2/tests/rmp2_structural.rs</files>
  <behavior>
    - Given a toy Mp2Reference (nao=2, nocc=1, nvir=1) and a synthetic (ia|jb) block fed via a test hooks impl, rmp2_kernel returns a finite e_corr matching a hand-computed closed-form value (always-on, synthetic ERIs — no intor).
    - With emp2_ss_factor=emp2_os_factor=1.0 the SCS energy equals plain e_corr = e_ss + e_os.
    - With SCS defaults (ss=1/3, os=1.2) the energy equals e_ss/3 + e_os*1.2.
    - default_ao2mo propagates the int2e NotYetImplemented error (does NOT panic) when called against a real Mole reference.
  </behavior>
  <read_first>
    - crates/pyscf-mp2/src/mp2.rs + hooks.rs (the stubs from 05-01)
    - crates/pyscf-mp2/tests/rmp2_structural.rs (the scaffold from 05-01)
    - pyscf/mp/mp2.py (kernel:33-76 closed-form, energy:117-126 SCS, class RMP2:692, MP2Base:463)
    - crates/pyscf-dft/src/numint.rs (nr_rks_inner lines ~491-566: the closed-form-energy-via-oracle_sum reduction discipline)
    - crates/pyscf-scf/src/hooks.rs (the OverrideHooks trait + NoOverrides default-impl shape — lines 13-107)
    - crates/pyscf-dft/src/hooks.rs (KsOverrideHooks + NoKsOverrides — the smaller-hook-set precedent)
    - crates/pyscf-core/src/{traits.rs, amplitudes.rs, energy.rs} (PostScf, Amplitudes, Energy types)
    - crates/pyscf-ao2mo/src/incore.rs (the general() the kernel calls)
    - crates/pyscf-gto/src/lib.rs (the intor signature — intor returns Result, int2e is arity-4 NotYetImplemented{phase:2})
  </read_first>
  <action>
    Declare `pub trait Mp2OverrideHooks` in `hooks.rs` (pyo3-free, D-08, smaller set than SCF): methods `ao2mo(&self, refr: &Mp2Reference, frozen: &Frozen) -> Result<ChemistsEris, PyscfRsError>` (the main one), `make_rdm1` + `make_rdm2` (declared here with `Density`-returning signatures; bodies fleshed in plan 05-04), and `energy(&self, eris: &ChemistsEris, refr: &Mp2Reference, frozen: &Frozen, ss_factor: f64, os_factor: f64) -> Result<Energy, PyscfRsError>`. Define `pub struct ChemistsEris { pub ovov: Vec<f64>, pub nocc: usize, pub nvir: usize }` (the flat `(occ,vir|occ,vir)` block; doc-comment its flat-index formula). Provide `pub struct NoMp2Overrides;` impl where `ao2mo` delegates to `crate::mp2::default_ao2mo(refr, frozen)` and `energy` delegates to a `crate::mp2::default_energy(...)` free fn — mirroring `pyscf-scf/src/hooks.rs::NoOverrides` (each method delegates to a `crate::*::default_*` free fn).

    In `mp2.rs`: define `pub struct Mp2Reference { pub mo_coeff: MOCoefficients, pub mo_energy: Vec<f64>, pub mo_occ: Vec<f64>, pub e_hf: f64, pub converged: bool, pub mol: Mole }` — `mol` is snapshotted by the pyscf-py bridge from `mf.mol` (05-07 T1). Implement `pub fn default_ao2mo(refr: &Mp2Reference, frozen: &Frozen) -> Result<ChemistsEris, PyscfRsError>` that builds `co`/`cv` MO column subsets (occupied/virtual, frozen-aware via the helpers) and calls `pyscf_ao2mo::general(eri_ao, nao, [&co, &cv, &co, &cv])` where `eri_ao = pyscf_gto::intor(&refr.mol, "int2e")?`. CRITICAL: `intor("int2e")` returns `NotYetImplemented{phase:2}` today; propagate with `?` (do NOT panic, do NOT substitute a zero buffer). Doc-comment that numeric flips on when cintx#11 lands, no code change (D-05). The `Mole` is always available via `refr.mol` — no extra parameter needed on `Mp2OverrideHooks::ao2mo`.

    Implement `pub fn rmp2_kernel<H: Mp2OverrideHooks>(refr: &Mp2Reference, frozen: &Frozen, hooks: &H, with_t2: bool) -> Result<Mp2Result, PyscfRsError>` where `pub struct Mp2Result { pub e_corr: f64, pub e_ss: f64, pub e_os: f64, pub t2: Option<Amplitudes> }`. Port the closed-form math (mp2.py:47-76): compute `nocc`/`nmo` via the helpers, get `eris = hooks.ao2mo(refr, frozen)?`, build `eia` denominators from `mo_energy` (exact subtraction), and for each i form `t2_ijab = (ia|jb)/(εi+εj−εa−εb)`, accumulate `edi = 2·Σ(ia|jb)·t2` and `exi = -Σ(ib|ja)·t2` with EVERY sum materialized into a `Vec` then reduced via `oracle_sum`/`oracle_dot` (NO `+=` reduction). Aggregate `e_ss += edi*0.5 + exi`, `e_os += edi*0.5`; final `e_corr = oracle_sum(per_i_contributions)`. `with_t2` default true (upstream WITH_T2); store `t2` in `Amplitudes` when true.

    Implement `pub fn scs_energy(e_ss: f64, e_os: f64, ss_factor: f64, os_factor: f64) -> f64` = `e_ss*ss_factor + e_os*os_factor` (mp2.py:117-126); document SCS defaults (1/3, 1.2) and that ss=os=1.0 reproduces plain MP2. Re-export `Mp2Reference`, `Mp2Result`, `rmp2_kernel`, `scs_energy`, `ChemistsEris`, `Mp2OverrideHooks`, `NoMp2Overrides` from `lib.rs`.

    Add behavior tests in `rmp2_structural.rs`: (1) ALWAYS-ON — a test-only hooks impl returns a hand-built `ChemistsEris` (bypassing `intor`); assert `rmp2_kernel` returns the hand-computed closed-form e_corr. (2) ALWAYS-ON — `scs_energy` factor tests (1.0/1.0 reproduces plain; 1/3,1.2 the SCS split). (3) ALWAYS-ON structural — assert `default_ao2mo` propagates the `int2e` `NotYetImplemented` error (matches `Err(PyscfRsError::NotYetImplemented{..})`, does NOT panic) when called with a real Mole reference (the numeric energy is cintx-gated; the error-propagation shape is always-on).
  </action>
  <verify>
    <automated>cargo test -p pyscf-mp2 --locked rmp2 2>&1 | grep -qE 'test result: ok' && grep -q 'pub fn rmp2_kernel' crates/pyscf-mp2/src/mp2.rs && grep -q 'pub trait Mp2OverrideHooks' crates/pyscf-mp2/src/hooks.rs</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-mp2/src/mp2.rs` contains `pub fn rmp2_kernel`, `pub struct Mp2Reference`, `pub fn default_ao2mo`, `pub fn scs_energy`
    - `crates/pyscf-mp2/src/hooks.rs` contains `pub trait Mp2OverrideHooks`, `pub struct ChemistsEris`, `pub struct NoMp2Overrides`
    - The closed-form kernel uses `oracle_sum`/`oracle_dot` for every energy reduction (grep: `grep -v '^[[:space:]]*//' crates/pyscf-mp2/src/mp2.rs | grep -c 'oracle_sum\|oracle_dot'` returns ≥ 1, and the same filtered file contains no reduction accumulator `acc +=`)
    - The always-on rmp2 hand-computed-energy test passes (synthetic ChemistsEris, no intor)
    - The scs_energy tests assert `1.0/1.0` reproduces `e_ss+e_os` and `1/3, 1.2` gives the SCS split
    - The structural test asserts `default_ao2mo` returns `Err(NotYetImplemented)` (no panic) against a real Mole
    - `cargo test -p pyscf-mp2 --locked rmp2` exits 0
  </acceptance_criteria>
  <done>rmp2_kernel computes the closed-form energy through oracle_sum from a synthetic (ia|jb); SCS factors split correctly; Mp2OverrideHooks + NoMp2Overrides declared pyo3-free; int2e error propagates cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → kernel | `mo_occ`/`mo_energy`/frozen indices are untrusted shape inputs; mismatch must error, not panic |
| pyscf-gto::intor → kernel | `int2e` currently errors (NotYetImplemented); the error must propagate, never be swallowed or substituted |
| (no Python boundary) | pyscf-mp2 is pyo3-free; FFI exercised in 05-07 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-FFI | Denial of Service | rmp2_kernel / default_ao2mo / helpers | mitigate | `?`-propagate every `Result`; NO `unwrap()` in numeric paths (clippy::unwrap_used warn-gated); the `int2e` NotYetImplemented error propagates (never panics) — FOUND-07 / BIND-09 by construction (panic→exception bridge is in pyscf-py) |
| T-05-03-FP | Tampering | energy reductions | mitigate | Every closed-form sum through `oracle_sum`/`oracle_dot` (materialize-then-reduce); no `+=` accumulation — bit-exact + thread-count invariant (Pitfall 1/2, FOUND-06) |
| T-05-03-FROZEN | Tampering | frozen mask / get_nocc / get_nmo | mitigate | Validate frozen indices in-range against `mo_occ.len()`; return `Mp2Error` on out-of-range — wrong nocc/nmo → wrong energy (A1); chemcore table ported verbatim from upstream |
| T-05-03-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages added (RESEARCH Package Legitimacy Audit). `approx` dev-dep was added in 05-01 from the already-pinned workspace set; no new registry fetch. |
</threat_model>

<verification>
- `cargo test -p pyscf-mp2 --locked` green (ccsd_import_contract + frozen + rmp2 always-on).
- `cargo clippy -p pyscf-mp2 -- -D warnings` clean (no unwrap in numeric paths); `cargo fmt --check` clean.
- `xtask check-no-fma` clean on pyscf-mp2 (oracle_sum reductions, FMA-free).
- `xtask check-dependency-wall` clean (no cubecl).
- Grep: no `pyo3` symbol referenced in pyscf-mp2.
</verification>

<success_criteria>
- The five MP2-08 helpers export with upstream-matching semantics; the verbatim CCSD import contract test passes always-on.
- `rmp2_kernel` computes the closed-form MP2 correlation energy via oracle_sum from a transformed `(ia|jb)` block.
- frozen=int/list/'auto'/window resolve the active-orbital mask matching upstream defaults.
- SCS factors split ss/os; default (1.0,1.0) reproduces plain MP2.
- `Mp2OverrideHooks` + `NoMp2Overrides` declared pyo3-free; `int2e` error propagates (numeric energy cintx-gated).
</success_criteria>

<output>
Create `.planning/phases/05-mp2/05-03-SUMMARY.md` when done.
</output>
