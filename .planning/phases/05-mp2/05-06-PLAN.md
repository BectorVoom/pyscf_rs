---
phase: 05-mp2
plan: 06
type: execute
wave: 6
depends_on: [05-05]
files_modified:
  - crates/pyscf-mp2/src/dfmp2_native.rs
  - crates/pyscf-mp2/src/lib.rs
  - crates/pyscf-mp2/tests/dfmp2_native_structural.rs
autonomous: true
requirements: [MP2-04]
must_haves:
  truths:
    - "The native RI-MP2 fast path is exposed via its own module path (dfmp2_native), distinct from the default factory"
    - "ints3c_cholesky + emp2_rhf port the upstream native contraction without a C dependency"
    - "The native path gates behind the same cintx#11 int3c2e_sph merge; structural tests are always-on"
  artifacts:
    - path: "crates/pyscf-mp2/src/dfmp2_native.rs"
      provides: "Native RI-MP2 fast path (RHF + UHF)"
      contains: "pub fn emp2_rhf"
  key_links:
    - from: "crates/pyscf-mp2/src/dfmp2_native.rs"
      to: "pyscf_df::cholesky_eri"
      via: "3-center Cholesky B-tensor"
      pattern: "cholesky_eri|DfIntegrals"
    - from: "crates/pyscf-mp2/src/dfmp2_native.rs"
      to: "pyscf_algebra::oracle_dot"
      via: "RI contraction reduction"
      pattern: "oracle_dot|oracle_sum"
---

<objective>
Add the native RI-MP2 fast path (MP2-04, D-06 additional target) as a distinct module `dfmp2_native` (own path `pyscf.mp.dfmp2_native`), NOT the default factory. Port upstream `pyscf/mp/dfmp2_native.py`'s `ints3c_cholesky` + `emp2_rhf` (and `dfump2_native.py` for UHF) as a pure-Rust contraction, reusing `pyscf-df`'s 3-center Cholesky B-tensor. This is the deliberate scope expansion the user chose ("Both" DF-MP2 variants — D-06).

Purpose: MP2-04 native half. Sequenced as a follow-on after the conventional path (05-05) proves out, per D-06. Both DF paths gate behind the same cintx#11 `int3c2e_sph` merge; the structural code + always-on tests ship now. The optional CPHF-based relaxed-RDM path (`solve_cphf_rhf`) reuses `pyscf_algebra::solve_linear` (already wired) — keep it minimal (energy fast-path is the core deliverable; the relaxed-RDM may be staged behind a status marker if its surface warrants).
Output: A native RI-MP2 path (structural tests always-on; numeric oracle cintx-gated via the `dfmp2_native_energy` arm registered in 05-01).
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mp2/05-RESEARCH.md
@.planning/phases/05-mp2/05-PATTERNS.md
@.planning/phases/05-mp2/05-05-SUMMARY.md

<interfaces>
<!-- Concrete contracts. -->

From 05-03/05-05: `pyscf_mp2::{Mp2Reference, ChemistsEris, Frozen, df_ao2mo, DFRMP2}`.

pyscf-df: `DfIntegrals { b_uvq, naux, nao }` (row-major); `cholesky_eri(mol, name)`; `default_ri(basis)` (the mp2fit aux). int3c2e_sph cintx-gated (cholesky_eri.rs:104-108).

pyscf-algebra: `solve_linear` (lib.rs:56, wired) for the CPHF relaxed-RDM path; `eigh_gen` (lib.rs:49); `oracle_sum`/`oracle_dot`. gemm NotYetImplemented{phase:2}.

Upstream native (pyscf/mp/dfmp2_native.py): `class DFRMP2(StreamObject)` (distinct from the conventional `DFRMP2(mp2.RMP2)`!), `ints3c_cholesky` (3c Cholesky → B-tensor), `emp2_rhf` (the RI-MP2 energy contraction), `solve_cphf_rhf` (relaxed-RDM CPHF). `dfump2_native.py` is the UHF analog. Module aliases within its own namespace: `MP2 = RMP2 = DFMP2 = DFRMP2`.

The `dfmp2_native_energy` oracle arm was registered in plan 05-01 KNOWN_METHODS.

dfmp2_native.rs is a stub from 05-01. dfmp2_native_structural.rs does NOT exist — create it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Native RI-MP2 energy fast path (emp2_rhf + ints3c_cholesky reuse)</name>
  <files>crates/pyscf-mp2/src/dfmp2_native.rs, crates/pyscf-mp2/src/lib.rs</files>
  <read_first>
    - crates/pyscf-mp2/src/dfmp2_native.rs (the stub from 05-01)
    - crates/pyscf-mp2/src/dfmp2.rs (df_ao2mo + DFRMP2 — the conventional path this parallels; reuse the B-tensor MO-transform helper)
    - crates/pyscf-mp2/src/mp2.rs (Mp2Reference, Mp2Result)
    - crates/pyscf-df/src/cholesky_eri.rs (DfIntegrals + cholesky_eri — the 3c Cholesky source `ints3c_cholesky` maps to)
    - crates/pyscf-df/src/auxbasis.rs (default_ri)
    - crates/pyscf-algebra/src/{solve_linear.rs, eigh_gen.rs, oracle.rs} (the wired CPHF/reduction surface)
    - pyscf/mp/dfmp2_native.py (DFRMP2(StreamObject), ints3c_cholesky, emp2_rhf, solve_cphf_rhf)
    - pyscf/mp/dfump2_native.py (UHF native analog)
  </read_first>
  <action>
    Implement the native RI-MP2 path in `dfmp2_native.rs` as a DISTINCT module surface (own path; document that `pyscf.mp.dfmp2_native` is the upstream module — NOT the default `mp.DFMP2` factory, which is the conventional path from 05-05). Map upstream `ints3c_cholesky` to `pyscf_df::cholesky_eri(mol, default_ri(basis))` (reuse the shipped 3c Cholesky B-tensor — do NOT build a second Cholesky; Don't-Hand-Roll). Implement `pub fn emp2_rhf(refr: &Mp2Reference, frozen: &Frozen, df: &DfIntegrals) -> Result<f64, PyscfRsError>`: the RI-MP2 energy contraction over the MO-transformed B-tensor, every reduction through `oracle_sum`/`oracle_dot` (port the upstream native contraction MATH; NO C dep). Reuse the per-Q MO transform helper from `dfmp2.rs` (`df_ao2mo` or a shared `b_mo` helper) rather than duplicating it.

    Define `pub struct NativeDFRMP2` (the native fast path) holding `Mp2Reference` + `DfIntegrals`, with a `pub fn kernel(...) -> Result<Mp2Result, PyscfRsError>` wrapping `emp2_rhf`. Add the UHF native analog `pub fn emp2_uhf(...)` + `pub struct NativeDFUMP2` (dfump2_native.py). 

    For the CPHF relaxed-RDM path (`solve_cphf_rhf`): wire a `pub fn solve_cphf_rhf(...)` that uses `pyscf_algebra::solve_linear` for the response linear system IF the corpus needs the relaxed RDM; otherwise stub it returning `NotYetImplemented{phase:5}` behind a clearly-documented status marker (the energy fast-path is the core MP2-04 deliverable; the relaxed RDM is the optional native extra — D-06 "may stage behind a status marker"). Document the decision in a comment.

    Propagate the int3c2e_sph cintx gate with `?` (never panic/substitute). Re-export `NativeDFRMP2`, `NativeDFUMP2`, `emp2_rhf`, `emp2_uhf` from `lib.rs` under a clearly-namespaced re-export (or `pub mod dfmp2_native` re-export) so the Python overlay can expose `pyscf.mp.dfmp2_native` separately from the default factory.
  </action>
  <verify>
    <automated>cargo build -p pyscf-mp2 --locked && grep -q 'pub fn emp2_rhf' crates/pyscf-mp2/src/dfmp2_native.rs && grep -q 'cholesky_eri\|DfIntegrals' crates/pyscf-mp2/src/dfmp2_native.rs</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-mp2/src/dfmp2_native.rs` contains `pub fn emp2_rhf`, `pub fn emp2_uhf`, `pub struct NativeDFRMP2`, `pub struct NativeDFUMP2`
    - The native energy contraction uses `oracle_sum`/`oracle_dot` (no `acc +=` reduction)
    - `dfmp2_native.rs` reuses `pyscf_df::cholesky_eri` (does NOT define a second Cholesky)
    - `dfmp2_native.rs` references `default_ri` (mp2fit aux), not `default_jkfit`
    - The int3c2e_sph gate is `?`-propagated (no panic/substitution); `solve_cphf_rhf` is wired via `solve_linear` OR a documented status-marker stub
    - `cargo build -p pyscf-mp2 --locked` exits 0
  </acceptance_criteria>
  <done>The native RI-MP2 energy fast path (R+U) ports the upstream native contraction through oracle_dot, reuses the shipped 3c Cholesky, lives on a distinct module path, and propagates the cintx gate.</done>
</task>

<task type="auto">
  <name>Task 2: Native DF-MP2 structural tests (always-on synthetic, numeric cintx-gated)</name>
  <files>crates/pyscf-mp2/tests/dfmp2_native_structural.rs</files>
  <read_first>
    - crates/pyscf-mp2/src/dfmp2_native.rs (the surface from Task 1)
    - crates/pyscf-mp2/tests/dfmp2_structural.rs (the conventional DF synthetic-test idiom from 05-05)
    - crates/pyscf-df/src/cholesky_eri.rs (DfIntegrals — for the synthetic B-tensor)
  </read_first>
  <action>
    Create `crates/pyscf-mp2/tests/dfmp2_native_structural.rs` with ALWAYS-ON tests (no cintx, synthetic B-tensor):
    (1) Feed `emp2_rhf` a hand-built synthetic `DfIntegrals` + toy `Mp2Reference`; assert it returns the hand-computed RI-MP2 energy (and that it AGREES with the conventional `dfrmp2_kernel` result on the SAME synthetic B-tensor — the two DF paths must give the same energy for the same integrals; this cross-checks the native math against the conventional path un-gated).
    (2) Assert `emp2_uhf` on a synthetic open-shell B-tensor returns a finite hand-computed energy.
    (3) Structural: assert the native kernel `?`-propagates the int3c2e_sph gate as an `Err` (no panic) against a real Mole.
    All synthetic arms are always-on; the live-PySCF numeric parity lives in the cintx-gated `dfmp2_native_energy` oracle arm.
  </action>
  <verify>
    <automated>cargo test -p pyscf-mp2 --locked dfmp2_native 2>&1 | grep -qE 'test result: ok'</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-mp2/tests/dfmp2_native_structural.rs` asserts `emp2_rhf` on a synthetic B-tensor equals the conventional `dfrmp2_kernel` result on the same B-tensor (cross-path agreement, un-gated)
    - A test asserts `emp2_uhf` returns a finite hand-computed energy on a synthetic open-shell block
    - A test asserts the native kernel returns `Err` (no panic) on the int3c2e_sph gate
    - No synthetic arm is `#[ignore]`'d (always-on)
    - `cargo test -p pyscf-mp2 --locked dfmp2_native` exits 0
  </acceptance_criteria>
  <done>Native and conventional DF-MP2 agree on synthetic integrals (always-on cross-check); the cintx gate propagates; live parity stays in the cintx-gated oracle arm.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → emp2_rhf/emp2_uhf | B-tensor shapes, nocc/nvir untrusted; mismatch must error |
| pyscf-df::cholesky_eri → native path | int3c2e_sph currently errors; must propagate |
| (no Python boundary) | pyscf-mp2 pyo3-free; FFI exercised in 05-07 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-FFI | Denial of Service | emp2_rhf / emp2_uhf / solve_cphf_rhf | mitigate | `?`-propagate the int3c2e_sph gate; no `unwrap()` (clippy-gated); CPHF `solve_linear` errors propagate, never panic |
| T-05-06-FP | Tampering | RI contraction reductions | mitigate | Q-sum + energy through `oracle_dot`/`oracle_sum`; no `+=` accumulation (Pitfall 1/2, FOUND-06) |
| T-05-06-XCHECK | Tampering | native vs conventional energy | mitigate | The always-on synthetic test asserts native == conventional on identical B-tensor — catches a divergent native math port un-gated |
| T-05-06-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages added (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `cargo test -p pyscf-mp2 --locked` green (dfmp2_native synthetic always-on).
- `cargo clippy -p pyscf-mp2 -- -D warnings` clean; `cargo fmt --check` clean.
- `xtask check-no-fma` clean; `xtask check-dependency-wall` clean (no cubecl).
- The native vs conventional cross-check passes on synthetic integrals.
</verification>

<success_criteria>
- The native RI-MP2 fast path (R+U) lives on a distinct module path, NOT the default factory (D-06).
- `emp2_rhf`/`emp2_uhf` port the upstream native contraction through oracle_dot, reusing the shipped 3c Cholesky (no second Cholesky).
- Native and conventional DF-MP2 agree on synthetic integrals (always-on cross-check).
- The cintx#11 int3c2e_sph gate propagates; numeric parity is CI-gated.
</success_criteria>

<output>
Create `.planning/phases/05-mp2/05-06-SUMMARY.md` when done.
</output>
