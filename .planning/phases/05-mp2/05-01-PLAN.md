---
phase: 05-mp2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/pyscf-ao2mo/Cargo.toml
  - crates/pyscf-ao2mo/src/lib.rs
  - crates/pyscf-ao2mo/src/error.rs
  - crates/pyscf-ao2mo/tests/transform_roundtrip.rs
  - crates/pyscf-mp2/Cargo.toml
  - crates/pyscf-mp2/src/lib.rs
  - crates/pyscf-mp2/tests/ccsd_import_contract.rs
  - crates/pyscf-mp2/tests/rmp2_structural.rs
  - crates/pyscf-mp2/tests/ump2_structural.rs
  - crates/pyscf-oracle/src/runner.rs
  - .github/workflows/ci.yml
  - .planning/ROADMAP.md
autonomous: true
requirements: [MP2-01, MP2-02, MP2-04, MP2-05, MP2-08]
must_haves:
  truths:
    - "cargo build --workspace compiles with pyscf-ao2mo as a registered member"
    - "pyscf-mp2 declares pyscf-ao2mo / pyscf-df / pyscf-scf deps and stays pyo3-free + cubecl-free"
    - "Every MP2 numeric oracle method name is registered in KNOWN_METHODS"
    - "Wave-0 test files exist and compile (failing/ignored as appropriate) so later waves have targets"
  artifacts:
    - path: "crates/pyscf-ao2mo/Cargo.toml"
      provides: "AO→MO crate manifest (core+algebra+gto deps, no pyo3/cubecl)"
      contains: "pyscf-ao2mo"
    - path: "crates/pyscf-ao2mo/src/lib.rs"
      provides: "AO→MO crate root with forbid(unsafe_code) + pub module surface"
      contains: "general"
    - path: "crates/pyscf-mp2/Cargo.toml"
      provides: "MP2 crate manifest wired with ao2mo/df/scf/gto/algebra deps"
      contains: "pyscf-ao2mo"
    - path: "crates/pyscf-oracle/src/runner.rs"
      provides: "MP2 oracle method names registered in KNOWN_METHODS"
      contains: "mp2_rmp2_energy"
  key_links:
    - from: "Cargo.toml"
      to: "crates/pyscf-ao2mo"
      via: "workspace members list"
      pattern: "crates/pyscf-ao2mo"
    - from: "crates/pyscf-mp2/Cargo.toml"
      to: "crates/pyscf-ao2mo"
      via: "path dependency"
      pattern: "pyscf-ao2mo"
---

<objective>
Scaffold the Phase 5 MP2 substrate: create the new `pyscf-ao2mo` workspace crate (D-01, the 20th `pyscf-*` member), register it in `Cargo.toml`, wire `pyscf-mp2`'s dependency block, and stand up every Wave-0 test target + oracle method name + CI job so the implementation waves (05-02..05-07) have concrete, compiling targets to fill in.

Purpose: Per RESEARCH Wave 0 Gaps and 05-VALIDATION Wave 0 Requirements, nothing in Phase 5 can be tested until the new crate exists, the workspace lockfile is regenerated, the oracle `KNOWN_METHODS` table carries the MP2 arms, and the CI gating (structural always-on + numeric cintx#11-gated) is in place. This plan is pure scaffolding + bookkeeping — it ships NO compute.
Output: A buildable 20-`pyscf-*`-crate workspace with `pyscf-ao2mo` skeleton, `pyscf-mp2` deps wired, failing/structural test scaffolds, oracle arms, and CI jobs.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mp2/05-CONTEXT.md
@.planning/phases/05-mp2/05-RESEARCH.md
@.planning/phases/05-mp2/05-PATTERNS.md

<interfaces>
<!-- Concrete contracts the executor needs. Extracted from the codebase this planning session. -->

The new pyscf-ao2mo crate MUST mirror crates/pyscf-df exactly. The df Cargo.toml dep triple (the template):
```
[dependencies]
pyscf-core    = { path = "../pyscf-core" }
pyscf-algebra = { path = "../pyscf-algebra" }
pyscf-gto     = { path = "../pyscf-gto" }
thiserror     = { workspace = true }
tracing       = { workspace = true }
```
NO pyo3, NO cubecl (D-01/D-03 algebra+pyo3 wall).

Workspace Cargo.toml members block currently lists 19 pyscf-* crates + xtask (the last grids comment line reads "Workspace grows 18 → 19."). `crates/pyscf-ao2mo` is genuinely ABSENT and must be added; `crates/pyscf-mp2` and `crates/pyscf-ccsd` are already present.

pyscf-oracle KNOWN_METHODS (crates/pyscf-oracle/src/runner.rs:47) is a `const KNOWN_METHODS: &[&str]` array currently length 13. A test at runner.rs:969 asserts `KNOWN_METHODS.len() == 13` — this assertion MUST be updated to the new length when arms are added. run_oracle_check returns PythonFeatureNotEnabled for known methods when the `python` feature is off.

pyscf-mp2/src/lib.rs is currently a 5-line stub: `#![forbid(unsafe_code)]` only. pyscf-mp2/Cargo.toml has an empty `[dependencies]` block.

The CCSD import contract (the MP2-08 test must mimic this verbatim — pyscf/cc/ccsd.py:35):
`from pyscf.mp.mp2 import get_nocc, get_nmo, get_frozen_mask, get_e_hf, _mo_without_core`

The DFT-01 oracle arms (`rks_energy`/`uks_energy`) are the model for cintx-gating: they live in KNOWN_METHODS and their numeric assertions are CI-only behind `--features python`; structural tests are always-on. The libxc CI job in .github/workflows/ci.yml uses `if: false` as its disable gate (line 345) — model the cintx#11 numeric job's disabled state on this.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyscf-ao2mo crate skeleton + register in workspace</name>
  <files>Cargo.toml, crates/pyscf-ao2mo/Cargo.toml, crates/pyscf-ao2mo/src/lib.rs, crates/pyscf-ao2mo/src/error.rs, .planning/ROADMAP.md</files>
  <read_first>
    - Cargo.toml (the workspace members block, lines 1-30, to insert the new member)
    - crates/pyscf-df/Cargo.toml (the EXACT dep-triple template to copy)
    - crates/pyscf-df/src/lib.rs (the lib-root shape: module doc + forbid(unsafe_code) + warn(clippy::unwrap_used) + pub mod/pub use)
    - crates/pyscf-scf/src/error.rs (the thiserror enum + From<E> for PyscfRsError bridge template)
    - .planning/ROADMAP.md (the Overview paragraph and Phase 4 line that mention the member count, to update 19→20)
  </read_first>
  <action>
    Create `crates/pyscf-ao2mo/Cargo.toml` by copying `crates/pyscf-df/Cargo.toml` verbatim, changing only `name = "pyscf-ao2mo"` and `description = "AO→MO integral transformation (general/full). CCSD-reusable (D-01). Phase 5."`. Dep triple is `pyscf-core`/`pyscf-algebra`/`pyscf-gto` path-deps + `thiserror`/`tracing` workspace deps. Add NO pyo3, NO cubecl, NO numpy.

    Create `crates/pyscf-ao2mo/src/lib.rs` with `#![forbid(unsafe_code)]` and `#![warn(clippy::unwrap_used)]`, a module doc citing D-01/D-02 and upstream `pyscf/ao2mo/`, and declare `pub mod incore; pub mod transform; pub mod error;` with `pub use error::Ao2moError; pub use incore::{full, general};`. For THIS plan, create only `error.rs` with the real content; create `incore.rs` and `transform.rs` as one-line stub modules each containing a single doc-comment and `// Body filled in plan 05-02.` plus a placeholder `pub fn` signature that returns `Err(Ao2moError::NotYetImplemented)` so lib.rs compiles. Specifically `incore.rs` exports `pub fn general(...)` and `pub fn full(...)` stubs returning the NotYetImplemented error; `transform.rs` is `// Body filled in plan 05-02.` only.

    Create `crates/pyscf-ao2mo/src/error.rs` by mirroring `crates/pyscf-scf/src/error.rs`: a `#[derive(Debug, Error)] pub enum Ao2moError` with `Algebra(#[from] pyscf_algebra::AlgebraError)`, `Core(#[from] pyscf_core::CoreError)`, a `ShapeMismatch { expected: usize, got: usize }` variant, and a `NotYetImplemented` unit variant. Add `impl From<Ao2moError> for pyscf_core::PyscfRsError` routing through `Core(InvalidMolecule(format!("{}", e)))` exactly as scf/error.rs does.

    Register the crate in workspace `Cargo.toml`: insert `"crates/pyscf-ao2mo",` into the `members` list (place it before `crates/pyscf-mp2` so the dependency layering reads top-down) with a comment `# Phase 5 addition (D-01): AO→MO transformation crate (CCSD-reusable). Workspace grows 19 → 20.`

    Update `.planning/ROADMAP.md`: in the Overview paragraph and the Phase 5 line, update the workspace member count from 19 to 20 `pyscf-*` crates, citing the `pyscf-ao2mo` addition (D-01). Use the same counting convention the prose already uses (pyscf-* crates, not counting xtask).
  </action>
  <verify>
    <automated>cargo build -p pyscf-ao2mo --locked && cargo metadata --format-version=1 --no-deps | grep -q '"name":"pyscf-ao2mo"'</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-ao2mo/Cargo.toml` contains `name = "pyscf-ao2mo"` and the `pyscf-core`/`pyscf-algebra`/`pyscf-gto` path deps, and contains NO occurrence of `pyo3`, `numpy`, or `cubecl`
    - `crates/pyscf-ao2mo/src/lib.rs` contains `#![forbid(unsafe_code)]` and `pub use incore::{full, general};`
    - `crates/pyscf-ao2mo/src/error.rs` contains `pub enum Ao2moError` and `impl From<Ao2moError> for pyscf_core::PyscfRsError`
    - Workspace `Cargo.toml` members list contains the line `crates/pyscf-ao2mo`
    - `cargo build -p pyscf-ao2mo --locked` exits 0
    - `.planning/ROADMAP.md` no longer states the workspace has 19 `pyscf-*` crates in the Phase 5 / Overview context (states 20)
  </acceptance_criteria>
  <done>pyscf-ao2mo builds clean as a registered 20th pyscf-* member; error type bridges to PyscfRsError; ROADMAP member count updated.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pyscf-mp2 deps + module skeleton + Wave-0 test scaffolds</name>
  <files>crates/pyscf-mp2/Cargo.toml, crates/pyscf-mp2/src/lib.rs, crates/pyscf-mp2/tests/ccsd_import_contract.rs, crates/pyscf-mp2/tests/rmp2_structural.rs, crates/pyscf-mp2/tests/ump2_structural.rs, crates/pyscf-ao2mo/tests/transform_roundtrip.rs</files>
  <read_first>
    - crates/pyscf-mp2/Cargo.toml (current empty deps block)
    - crates/pyscf-mp2/src/lib.rs (current 5-line stub)
    - crates/pyscf-df/Cargo.toml (dep wiring template)
    - crates/pyscf-scf/src/lib.rs (re-export surface shape)
    - crates/pyscf-core/src/traits.rs (the PostScf trait MP2 will implement, lines 42-51)
    - .planning/phases/05-mp2/05-RESEARCH.md (the Recommended Project Structure module list)
  </read_first>
  <action>
    Wire `crates/pyscf-mp2/Cargo.toml` `[dependencies]`: path-deps `pyscf-core`, `pyscf-algebra`, `pyscf-ao2mo` (new), `pyscf-scf` (reference types), `pyscf-df` (DF-MP2), `pyscf-gto`, `pyscf-runtime`; workspace deps `thiserror`, `tracing`. Add a comment `# pyo3 is FORBIDDEN here (D-07/D-08 algebra+pyo3 wall) — bridge lives in pyscf-py.` Add NO pyo3, NO cubecl. Add `[dev-dependencies]` `approx = "0.5"` for test assertions.

    Replace `crates/pyscf-mp2/src/lib.rs` stub: keep `#![forbid(unsafe_code)]`, add `#![warn(clippy::unwrap_used)]`, add a module doc citing the Phase 5 deliverable + D-01/D-07/D-08. Declare the module skeleton per RESEARCH structure: `pub mod mp2; pub mod ump2; pub mod dfmp2; pub mod dfmp2_native; pub mod helpers; pub mod frozen; pub mod rdm; pub mod hooks; pub mod error;` — but for THIS plan create each module file as a stub (a doc-comment + `// Body filled in plan 05-0X.`). The `helpers.rs` stub MUST already export the five MP2-08 function SIGNATURES (`get_nocc`, `get_nmo`, `get_frozen_mask`, `get_e_hf`, `_mo_without_core` — name the Rust fn `mo_without_core` with a `#[doc]` noting the Python `_mo_without_core` mapping) returning `todo!()` or `Err(...)` so the contract test can reference them and compile. The `error.rs` stub MUST contain the real `Mp2Error` enum + `From<Mp2Error> for PyscfRsError` bridge (mirror ao2mo/error.rs from Task 1). Re-export from lib.rs: `pub use error::Mp2Error; pub use helpers::{get_nocc, get_nmo, get_frozen_mask, get_e_hf, mo_without_core};`.

    Create `crates/pyscf-mp2/tests/ccsd_import_contract.rs` (MP2-08, the verbatim CCSD import contract): a `#[test]` that `use pyscf_mp2::{get_nocc, get_nmo, get_frozen_mask, get_e_hf, mo_without_core};` and references all five symbols (e.g. takes their function pointers / calls them on a toy `mo_occ = vec![2.0, 2.0, 0.0, 0.0]` once they are implemented). For THIS plan, the test body asserts the symbols are importable and callable; mark assertions on RESULTS with `#[ignore = "numeric body lands in plan 05-03"]` on a SEPARATE numeric test fn, keeping the import-existence test always-on. The always-on portion proves the exact `cc/ccsd.py:35` symbol set is exported.

    Create `crates/pyscf-mp2/tests/rmp2_structural.rs` and `crates/pyscf-mp2/tests/ump2_structural.rs`: always-on tests that build a toy `Mp2Reference`-shaped input and assert the kernel call SHAPE + error-propagation (NOT the numeric energy). For THIS plan, scaffold them with `#[ignore = "wired in plan 05-03/05-04"]` on any test that needs the not-yet-implemented kernel, plus at least one always-on test that asserts module/type existence so the file compiles and is discovered.

    Create `crates/pyscf-ao2mo/tests/transform_roundtrip.rs`: scaffold the always-on synthetic-ERI transform correctness test (the one numeric assertion that ships un-gated this phase, per RESEARCH Validation Architecture). For THIS plan, mark the numeric assertion `#[ignore = "transform body lands in plan 05-02"]`; include an always-on compile-smoke that references `pyscf_ao2mo::general`.
  </action>
  <verify>
    <automated>cargo test -p pyscf-mp2 -p pyscf-ao2mo --locked --no-run && cargo test -p pyscf-mp2 --locked ccsd_import_contract 2>&1 | grep -qE 'test result: ok|running'</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-mp2/Cargo.toml` `[dependencies]` contains `pyscf-ao2mo`, `pyscf-scf`, `pyscf-df`, and contains NO `pyo3`/`cubecl`
    - `crates/pyscf-mp2/src/lib.rs` contains `pub use helpers::{get_nocc, get_nmo, get_frozen_mask, get_e_hf, mo_without_core};`
    - `crates/pyscf-mp2/tests/ccsd_import_contract.rs` contains `use pyscf_mp2::{get_nocc, get_nmo, get_frozen_mask, get_e_hf, mo_without_core}` (the exact CCSD import symbol set)
    - `cargo test -p pyscf-mp2 -p pyscf-ao2mo --locked --no-run` exits 0 (all test files compile)
    - `crates/pyscf-ao2mo/tests/transform_roundtrip.rs` references `pyscf_ao2mo::general`
  </acceptance_criteria>
  <done>pyscf-mp2 deps wired pyo3-free; module + test scaffolds compile; the MP2-08 CCSD import-contract test passes its always-on existence arm.</done>
</task>

<task type="auto">
  <name>Task 3: Register MP2 oracle arms + CI structural/gated jobs</name>
  <files>crates/pyscf-oracle/src/runner.rs, .github/workflows/ci.yml</files>
  <read_first>
    - crates/pyscf-oracle/src/runner.rs (KNOWN_METHODS array lines 47-78 and the len assertion at 969)
    - crates/pyscf-oracle/Cargo.toml (the `python` feature gating model)
    - .github/workflows/ci.yml (the libxc `if: false` gated-job pattern at line 345; the existing oracle/test job structure)
    - .planning/phases/05-mp2/05-VALIDATION.md (the Per-Task Verification Map + Wave 0 Requirements for exact oracle arm names)
  </read_first>
  <action>
    Add the four MP2 numeric oracle method names to `KNOWN_METHODS` in `crates/pyscf-oracle/src/runner.rs`: `"mp2_rmp2_energy"`, `"mp2_ump2_energy"`, `"dfmp2_energy"`, `"mp2_rdm"` (and `"dfmp2_native_energy"` for the native path — 5 arms total). Add a block comment citing MP2-01/02/04/05 and that the fixture name encodes the system as `<base>` (e.g. `h2o_ccpvdz`) per the rks_energy precedent, and that all MP2 numeric arms are CI-gated behind cintx#11 (both arity-4 `int2e` for in-core and arity-3 `int3c2e_sph` for DF). Update the `KNOWN_METHODS.len() == 13` assertion at runner.rs:969 to the new length (18) and add `assert!(KNOWN_METHODS.contains(&"mp2_rmp2_energy"));` plus the other four.

    In `.github/workflows/ci.yml`, add an always-on MP2 structural job named `mp2-structural` that runs `cargo test -p pyscf-mp2 -p pyscf-ao2mo --locked` (no `--features python`) — this asserts the structural/wiring + the always-on synthetic ao2mo transform test + the MP2-08 import contract. Add a SECOND job named `mp2-oracle-cintx-gated` that runs `cargo test -p pyscf-oracle --features python --locked` for the MP2 arms, gated with `if: false` and a comment block citing cintx#11 (mirror the libxc `if: false` disabled-job pattern at ci.yml:345): the numeric MP2 oracle stays disabled until cintx ships arity-3/4 `int2e`/`int3c2e_sph`, exactly as DF-HF/DFT-01 are gated. Document the enable condition in the comment (flip `if: false` once cintx#11 merges).
  </action>
  <verify>
    <automated>cargo test -p pyscf-oracle --locked known_methods 2>&1 | grep -qE 'test result: ok' && grep -q 'mp2_rmp2_energy' crates/pyscf-oracle/src/runner.rs && grep -q 'mp2-structural' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-oracle/src/runner.rs` `KNOWN_METHODS` contains `"mp2_rmp2_energy"`, `"mp2_ump2_energy"`, `"dfmp2_energy"`, `"dfmp2_native_energy"`, `"mp2_rdm"`
    - The `KNOWN_METHODS.len()` assertion in runner.rs matches the new array length (the known-methods test passes)
    - `cargo test -p pyscf-oracle --locked` exits 0 (default feature build, no libpython needed)
    - `.github/workflows/ci.yml` contains a job `mp2-structural` running `cargo test -p pyscf-mp2 -p pyscf-ao2mo`
    - `.github/workflows/ci.yml` contains a job for MP2 numeric oracle gated `if: false` with a cintx#11 enable-condition comment
  </acceptance_criteria>
  <done>MP2 oracle arms registered + len-assert updated; CI carries an always-on structural MP2 job and a cintx#11-gated numeric job mirroring DF-HF/DFT-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build system → workspace | A new crate member changes the dependency graph; the algebra/pyo3 wall must hold |
| (no runtime boundary this plan) | Pure scaffolding — no compute, no Python FFI exercised here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-DEP | Tampering | pyscf-ao2mo / pyscf-mp2 dependency declarations | mitigate | NO `cubecl-*` (check_dependency_wall denylist auto-passes — A5); NO `pyo3`/`numpy` (review-discipline + acceptance_criteria grep gate) — the algebra+pyo3 wall every prior phase established |
| T-05-01-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO external packages (RESEARCH Package Legitimacy Audit: "no new external packages"). All deps are internal workspace path-crates + already-pinned workspace deps. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `cargo build --workspace --locked` succeeds with 20 `pyscf-*` members.
- `cargo test -p pyscf-mp2 -p pyscf-ao2mo --locked --no-run` compiles all scaffolded test files.
- `cargo test -p pyscf-oracle --locked` passes (KNOWN_METHODS len assert updated).
- `xtask check-dependency-wall` passes (no new cubecl consumer).
- `cargo clippy -p pyscf-ao2mo -p pyscf-mp2 -- -D warnings` clean; `cargo fmt --check` clean.
- Grep confirms no `pyo3`/`cubecl` in either new crate's Cargo.toml.
</verification>

<success_criteria>
- `pyscf-ao2mo` is a registered, building 20th `pyscf-*` workspace member with an error type bridging to `PyscfRsError`.
- `pyscf-mp2` deps wired (ao2mo/df/scf/gto/algebra/runtime), pyo3-free + cubecl-free, with a compiling module + test skeleton.
- The MP2-08 CCSD import contract test passes its always-on symbol-existence arm.
- All five MP2 oracle method names registered; CI carries an always-on structural job + a cintx#11-gated numeric job.
- ROADMAP member count updated 19→20.
</success_criteria>

<output>
Create `.planning/phases/05-mp2/05-01-SUMMARY.md` when done.
</output>
