---
phase: 05-mp2
plan: 07
type: execute
wave: 7
depends_on: [05-04, 05-05, 05-06]
files_modified:
  - crates/pyscf-py/src/mp.rs
  - crates/pyscf-py/src/lib.rs
  - python/pyscf/mp/__init__.py
  - crates/pyscf-py/tests/mp2_scanner.rs
autonomous: true
requirements: [MP2-01, MP2-02, MP2-04, MP2-05, MP2-06, MP2-07]
user_setup: []
must_haves:
  truths:
    - "PyRMP2/PyUMP2/PyDFMP2 eagerly snapshot the SCF reference from the Python mf into plain Rust arrays (D-07)"
    - "Subclass overrides (ao2mo/make_rdm1/make_rdm2/energy) dispatch via slf.call_method1 (D-08, Pitfall 7)"
    - "mf.MP2() returns RMP2 for RHF refs / UMP2 for UHF refs; mf.density_fit().MP2() returns conventional DFMP2"
    - "mp2.as_scanner() returns a Mole→energy callable (re-run mf → re-snapshot → MP2 kernel)"
    - "from pyscf import mp works via the python/pyscf/mp/__init__.py overlay re-exporting _native.mp"
  artifacts:
    - path: "crates/pyscf-py/src/mp.rs"
      provides: "PyRMP2/PyUMP2/PyDFMP2 + Mp2 bridge + as_scanner + factory dispatch"
      contains: "PyRMP2"
    - path: "python/pyscf/mp/__init__.py"
      provides: "mp overlay re-exporting from _native.mp + MP2 factory"
      contains: "from pyscf._native.mp"
  key_links:
    - from: "crates/pyscf-py/src/mp.rs"
      to: "pyscf_mp2::rmp2_kernel"
      via: "pyo3-free kernel call on snapshotted arrays"
      pattern: "rmp2_kernel|ump2_kernel|dfrmp2_kernel"
    - from: "crates/pyscf-py/src/mp.rs"
      to: "slf.call_method1"
      via: "subclass override dispatch (Pitfall 7)"
      pattern: "call_method1"
    - from: "crates/pyscf-py/src/lib.rs"
      to: "crates/pyscf-py/src/mp.rs"
      via: "mp submodule registration"
      pattern: "mp::register|mp_mod"
---

<objective>
Wire the PyO3 bridge for MP2 in `pyscf-py` — the ONLY pyo3 layer (D-07/D-08). Add `PyRMP2`/`PyUMP2`/`PyDFMP2` (+ native variant) that eagerly snapshot the SCF reference from the Python `mf` into plain Rust arrays and call the pyo3-free `pyscf-mp2` kernels; the `Mp2OverrideHooks` bridge routing subclass overrides through `slf.call_method1`; the `mf.MP2()`/`mf.density_fit().MP2()` factory dispatch; `as_scanner` (MP2-07); the `_native.mp` submodule registration; and the `python/pyscf/mp/__init__.py` overlay.

Purpose: This makes every MP2 entry point user-callable from a Python script (MP2-01/02/04/05/06/07). The bridge mirrors `PyRHF` (scf.rs) exactly: eager snapshot, per-hook `py.detach`, NumPy `to_owned()`, `call_method1` for overrides, scanner closure. The MP2 method crates stay pyo3-free; the bridge holds the `Py<PyAny>` for `mf` and the scanner (D-07). Live cross-module dispatch parity is a Manual-Only/CI item (env lacks maturin/pyscf — Phase-4 precedent); the Rust-side structural tests + the PyO3 build are always-on.
Output: A complete, building MP2 PyO3 surface + Python overlay (structural scanner test always-on; live `mf.MP2().run()` parity is Manual-Only/CI per 05-VALIDATION).
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mp2/05-RESEARCH.md
@.planning/phases/05-mp2/05-PATTERNS.md
@.planning/phases/05-mp2/05-VALIDATION.md
@.planning/phases/05-mp2/05-04-SUMMARY.md
@.planning/phases/05-mp2/05-05-SUMMARY.md
@.planning/phases/05-mp2/05-06-SUMMARY.md

<interfaces>
<!-- Concrete contracts. -->

pyscf-mp2 surface (now complete from 05-03..06): `Mp2Reference`, `Mp2Result`, `ChemistsEris`, `rmp2_kernel`, `ump2_kernel`, `UmpReference`, `UmpResult`, `dfrmp2_kernel`, `dfump2_kernel`, `df_ao2mo`, `NativeDFRMP2`, `NativeDFUMP2`, `emp2_rhf`, `make_rdm1`, `make_rdm2`, `scs_energy`, `Mp2OverrideHooks`, `NoMp2Overrides`, `Frozen`, MP2-08 helpers. ALL pyo3-free.

pyscf-py module registration (crates/pyscf-py/src/lib.rs:45-66): `#[pymodule] fn _native` registers submodules via `let X_mod = PyModule::new(py, "X")?; crate::X::register(py, &X_mod)?; m.add_submodule(&X_mod)?;` (scf and dft already wired). Add `mp` the same way: `let mp_mod = PyModule::new(py, "mp")?; crate::mp::register(py, &mp_mod)?; m.add_submodule(&mp_mod)?;` and `pub mod mp;`.

PyRHF template (crates/pyscf-py/src/scf.rs):
- `#[pyclass(subclass, name="RHF", module="pyscf._native.scf")] pub struct PyRHF { ... }`
- `#[new] fn new(py, mol: Py<PyAny>)` — eager snapshot (extract_mole_from_pyany at scf.rs:62)
- `kernel` (scf.rs:320) does NOT `py.detach` at top (hooks re-enter Python); per-hook DEFAULT methods DO `py.detach(|| default_*(...))` (scf.rs:472,484,498)
- `run` = kernel then return self (scf.rs:399-402)
- `as_scanner` (scf.rs:694-699) + `PyScfScanner` pyclass (scf.rs:844-882)
- NumPy converters (scf.rs:31-33): `crate::numpy_io::{to_density, to_mo_coeff, density_to_pyarray, mo_coeff_to_pyarray, slice_to_pyarray1}` — `to_owned()` non-standard-layout (BIND-04)

Bridge dispatch (crates/pyscf-py/src/bridge.rs:35-72): `PyOverrideBridge` + `call_hook` → `slf.bind(py).call_method1(method, args).map_err(py_to_pyscf)?` (Pitfall 7 immune — resolves Python MRO).

Factory dispatch (pyscf/mp/__init__.py:27-65): `MP2(mf)` → UMP2 if `mf.istype('UHF')`, else RMP2; DFMP2 if `getattr(mf,'with_df',None)`.

python overlay model (python/pyscf/scf/__init__.py): `from pyscf._native.scf import RHF, UHF, GHF` + `__all__`. python/pyscf/mp/ does NOT exist — create the overlay dir + __init__.py.

mp.rs does NOT exist — create it (model: scf.rs). mp2_scanner.rs test does NOT exist — create it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PyRMP2/PyUMP2/PyDFMP2 + Mp2OverrideHooks bridge + factory + as_scanner</name>
  <files>crates/pyscf-py/src/mp.rs, crates/pyscf-py/src/lib.rs</files>
  <read_first>
    - crates/pyscf-py/src/scf.rs (PyRHF/PyUHF: #[new] eager snapshot, kernel/run, per-hook py.detach, as_scanner, PyScfScanner — the verbatim template)
    - crates/pyscf-py/src/bridge.rs (PyOverrideBridge + call_hook → call_method1 — the override-dispatch template)
    - crates/pyscf-py/src/numpy_io.rs (to_density/to_mo_coeff/density_to_pyarray converters — BIND-04)
    - crates/pyscf-py/src/dft.rs (PyRKS/PyUKS + KsOverrideHooks bridge — the closest recent precedent for a method bridge)
    - crates/pyscf-py/src/lib.rs (the _native pymodule registration block, lines 45-66)
    - crates/pyscf-mp2/src/lib.rs (the complete pyscf-mp2 surface to call)
    - crates/pyscf-mp2/src/hooks.rs (Mp2OverrideHooks — the trait the bridge impls)
    - pyscf/mp/__init__.py (the MP2/RMP2/UMP2 factory + istype/with_df dispatch lines 27-65)
  </read_first>
  <action>
    Create `crates/pyscf-py/src/mp.rs` modeling `scf.rs`:

    `#[pyclass(subclass, name="RMP2", module="pyscf._native.mp")] pub struct PyRMP2 { inner: Mp2Reference, py_mf: Py<PyAny>, frozen: Frozen, ss_factor: f64, os_factor: f64, ... }`. `#[new] fn new(py, mf: Py<PyAny>)` eagerly snapshots `mo_coeff`/`mo_energy`/`mo_occ`/`e_tot`(→e_hf)/`converged`/`mol` from the Python `mf` into the plain-array `Mp2Reference` (mirror scf.rs:62 but pull `mf` attributes via NumPy converters — `to_mo_coeff` for `mf.mo_coeff`, `slice_to_*` for energies/occ, `extract_mole_from_pyany` for `mf.mol`). **`mf.mol` → `Mp2Reference.mol` is required**: `default_ao2mo` calls `pyscf_gto::intor(&refr.mol, "int2e")` and needs the Mole without a separate parameter. Hold `py_mf: Py<PyAny>` for as_scanner re-run + hook dispatch (D-07).

    Define the `Mp2OverrideHooks` bridge impl (the pyo3 side): a `Mp2PyBridge { slf: Py<PyAny> }` whose `ao2mo`/`make_rdm1`/`make_rdm2`/`energy` each do `Python::attach` → build args → `slf.bind(py).call_method1("<hook>", args)` → extract via numpy_io (mirror bridge.rs:57-72). The DEFAULT (non-overridden) path: when the Python class does NOT override a hook, the bridge falls back to `NoMp2Overrides` semantics wrapped in `py.detach(|| pyscf_mp2::default_*(...))` (mirror scf.rs:472 per-hook detach around pure-Rust compute). Document that `kernel` itself does NOT `py.detach` at the top (hooks re-enter Python — scf.rs:345-350).

    `kernel`/`run`: `kernel` calls `pyscf_mp2::rmp2_kernel(&self.inner, &self.frozen, &bridge, with_t2)` and stores `e_corr`/`e_ss`/`e_os`/`t2`; expose `e_corr`/`e_tot`(= e_hf + e_corr)/`emp2_scs` (via `scs_energy` with the set factors — MP2-06) as `#[getter]`s; `set(emp2_ss_factor=, emp2_os_factor=)` setters (MP2-06). `run` = kernel then return self. Expose `make_rdm1`/`make_rdm2` methods routing through the bridge (MP2-05).

    `PyUMP2` analogous (calls `ump2_kernel`); `PyDFMP2` analogous (calls `dfrmp2_kernel`/`dfump2_kernel`, assembling `DfIntegrals` via the snapshotted `mf.with_df`/basis); optionally a native variant under a `dfmp2_native` namespace.

    `as_scanner` (MP2-07): a `PyMp2Scanner` pyclass holding a `Py<PyAny>` mf-scanner; `__call__(mol)` re-runs `mf(mol)` at the new geometry, re-snapshots into a fresh `Mp2Reference`, runs the MP2 kernel, returns the energy (mirror scf.rs:694-699 + PyScfScanner at scf.rs:844-882).

    Factory: a module-level `MP2(py, mf)` fn (registered in `register`) that returns `PyUMP2` if `mf.istype('UHF')` (or the to_uhf/to_rhf SCF-11 check), `PyDFMP2` if `getattr(mf,'with_df',None)`, else `PyRMP2` — mirror `pyscf/mp/__init__.py:27-65`.

    Add `pub fn register(py, m: &Bound<PyModule>) -> PyResult<()>` adding `PyRMP2`/`PyUMP2`/`PyDFMP2`/`PyMp2Scanner` + the `MP2` factory fn to the module.

    In `crates/pyscf-py/src/lib.rs`: add `pub mod mp;` and register the `mp` submodule in `_native` (mirror the scf/dft registration block at lines 56-64): `let mp_mod = PyModule::new(py, "mp")?; crate::mp::register(py, &mp_mod)?; m.add_submodule(&mp_mod)?;`.
  </action>
  <verify>
    <automated>cargo build -p pyscf-py --locked && grep -q 'PyRMP2' crates/pyscf-py/src/mp.rs && grep -q 'call_method1' crates/pyscf-py/src/mp.rs && grep -q 'crate::mp::register' crates/pyscf-py/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
    - `crates/pyscf-py/src/mp.rs` contains `PyRMP2`, `PyUMP2`, `PyDFMP2`, `PyMp2Scanner`, and an `MP2` factory fn dispatching on `istype('UHF')`/`with_df`
    - The bridge uses `slf.bind(py).call_method1` for override dispatch (Pitfall 7) and `py.detach` around the pure-Rust default-hook compute
    - `crates/pyscf-py/src/lib.rs` contains `pub mod mp;` and `crate::mp::register` in the `_native` pymodule
    - The PyMP2 classes hold a `Py<PyAny>` for `mf` (eager snapshot into `Mp2Reference` — D-07) and call the pyo3-free `pyscf_mp2` kernels
    - `cargo build -p pyscf-py --locked` exits 0
    - `xtask check-forbidden-paths` clean (no out-of-scope imports)
  </acceptance_criteria>
  <done>PyRMP2/PyUMP2/PyDFMP2 + scanner + factory wired with eager snapshot, call_method1 override dispatch, per-hook py.detach, and NumPy to_owned; the mp submodule is registered in _native.</done>
</task>

<task type="auto">
  <name>Task 2: python/pyscf/mp overlay + as_scanner structural test</name>
  <files>python/pyscf/mp/__init__.py, crates/pyscf-py/tests/mp2_scanner.rs</files>
  <read_first>
    - python/pyscf/scf/__init__.py (the re-export overlay template)
    - crates/pyscf-py/src/mp.rs (the PyMp2Scanner + factory from Task 1)
    - pyscf/mp/__init__.py (the upstream factory dispatch the overlay mirrors — istype/with_df branches)
    - crates/pyscf-py/tests/ (existing pyscf-py test shape for a structural Rust test)
  </read_first>
  <action>
    Create `python/pyscf/mp/__init__.py` (the pyscf-rs `_native` re-export overlay — DISTINCT from the vendored upstream `pyscf/mp/`): re-export `RMP2`, `UMP2`, `MP2`, `DFMP2` from `pyscf._native.mp` (mirror `python/pyscf/scf/__init__.py`), with `__all__ = ["MP2", "RMP2", "UMP2", "DFMP2"]`. Add a module docstring noting the overlay re-exports `_native.mp` (BIND-02) and that `MP2(mf)` is the factory dispatch (RHF→RMP2, UHF→UMP2, with_df→DFMP2). Expose the native fast path under `pyscf.mp.dfmp2_native` if it was namespaced separately in Task 1 (a small re-export). Create the `python/pyscf/mp/` directory.

    Create `crates/pyscf-py/tests/mp2_scanner.rs` — an ALWAYS-ON STRUCTURAL Rust test (MP2-07, per 05-VALIDATION the scanner test is `cargo test -p pyscf-py mp2_scanner (structural)`): assert the `PyMp2Scanner`/`as_scanner` plumbing exists and the scanner-closure shape compiles + behaves (a Rust-side structural assertion that does NOT require a live Python `mf` — e.g. construct the scanner over a snapshotted `Mp2Reference` and assert the Mole→energy callable signature). Do NOT assert live energy parity here — that is the Manual-Only/CI item below.

    Document in the test file (and surface in the SUMMARY) the two Manual-Only verifications from 05-VALIDATION: (a) `mf.MP2().run().e_corr == mp.RMP2(mf).kernel()[0]` cross-module dispatch parity (live Python + fully-wired PyO3), and (b) `mf.density_fit().MP2()` routes to DFMP2 (live + cintx#11 gate) — both run in CI with libpython + upstream pyscf, not in the executor sandbox (Phase-4 precedent).
  </action>
  <verify>
    <automated>cargo test -p pyscf-py --locked mp2_scanner 2>&1 | grep -qE 'test result: ok|running' && test -f python/pyscf/mp/__init__.py && grep -q 'from pyscf._native.mp' python/pyscf/mp/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - `python/pyscf/mp/__init__.py` contains `from pyscf._native.mp import` and `__all__` listing `MP2`/`RMP2`/`UMP2`/`DFMP2`
    - `crates/pyscf-py/tests/mp2_scanner.rs` exists and its structural arm is NOT `#[ignore]`'d (always-on)
    - The two Manual-Only verifications are documented (test-file comment + SUMMARY)
    - `cargo test -p pyscf-py --locked mp2_scanner` exits 0
  </acceptance_criteria>
  <done>The mp overlay re-exports _native.mp with the MP2 factory; the as_scanner structural test passes always-on; live cross-module dispatch parity is documented as the Manual-Only/CI item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python mf → PyRMP2::new | `mf.mo_coeff`/`mo_energy`/`mo_occ` are untrusted NumPy arrays crossing the FFI boundary |
| Python subclass → bridge | a subclass override is untrusted Python code invoked via call_method1 |
| Rust kernel → Python | a Rust panic must surface as a Python exception, never a process abort |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-NUMPY | Tampering | PyRMP2::new / bridge NumPy inputs | mitigate | BIND-04 (V5): `to_owned()` non-`is_standard_layout()` arrays via `crate::numpy_io::to_*`; validate `nao`/`nmo` shapes against the snapshotted reference before contraction (RESEARCH Security Domain) |
| T-05-07-PANIC | Denial of Service | PyRMP2::kernel / hook dispatch | mitigate | BIND-09: Rust panics never escape FFI — `catch_unwind` at the pyo3 boundary (existing pyscf-py errors.rs); `?`-propagate the `int2e`/`int3c2e_sph` gate to a Python exception with the error chain preserved |
| T-05-07-GIL | Denial of Service | per-hook compute | mitigate | BIND-05: `py.detach(\|\| default_*(...))` around pure-Rust compute (kernel itself does NOT detach — hooks re-enter Python); tested under python3.13t in CI (Phase-8 free-threaded build) |
| T-05-07-OVERRIDE | Spoofing | subclass override dispatch | mitigate | BIND-07/Pitfall 7: dispatch via `slf.call_method1` (resolves the Python MRO) so a subclass override is invoked transparently — never a Rust-side MRO bypass |
| T-05-07-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages added (RESEARCH Package Legitimacy Audit). pyscf-py's pyo3/numpy are already-pinned (0.28.3/0.28.0). |
</threat_model>

<verification>
- `cargo build -p pyscf-py --locked` succeeds (the full MP2 PyO3 surface compiles).
- `cargo test -p pyscf-py --locked mp2_scanner` green (always-on structural).
- `xtask check-forbidden-paths` clean; `xtask check-dependency-wall` clean (pyscf-py is the pyo3 layer, never a direct cubecl consumer).
- `cargo clippy -p pyscf-py -- -D warnings` clean; `cargo fmt --check` clean.
- Live `mf.MP2().run()` parity + `mf.density_fit().MP2()` routing are Manual-Only/CI (libpython + upstream pyscf + cintx#11) per 05-VALIDATION.
</verification>

<success_criteria>
- `PyRMP2`/`PyUMP2`/`PyDFMP2` snapshot the SCF reference eagerly (D-07) and call the pyo3-free `pyscf-mp2` kernels.
- Subclass overrides dispatch via `slf.call_method1` (D-08, Pitfall 7); per-hook `py.detach`; NumPy `to_owned()`.
- `mf.MP2()` factory dispatches RHF→RMP2 / UHF→UMP2 / with_df→DFMP2; `as_scanner` returns a Mole→energy callable.
- `from pyscf import mp` works via the overlay; the scanner structural test is always-on.
</success_criteria>

<output>
Create `.planning/phases/05-mp2/05-07-SUMMARY.md` when done.
</output>
