# Audit-Fix Classification — 2026-06-01

**Source:** `audit-uat` (+ source-level NYI sweep, scoped to the "implement not implemented yet" directive)
**Severity threshold:** medium · **Max:** 5
**Outcome:** 1/1 auto-fixable fixed (F-13), pipeline completed cleanly (0 failures)

---

## Classification table

| #    | Finding (NYI marker)                                   | Sev  | Classification | Reason |
|------|--------------------------------------------------------|------|----------------|--------|
| **F-01** | **`eval_gto` `GTOval_ip`/`GTOval_ig` (grad/magnetic)** | med  | **partially auto-fixable → ip-arm FIXED** | `GTOval_ip_sph` reclassified: byte-identical to `GTOval_sph_deriv1[1:4]` (verified vs upstream, no sign flip) — in-tree, NOT cintx-gated. Fixed. `GTOval_ip_cart` deferred (F-02 cart surface); `GTOval_ig` magnetic-GIAO stays manual-only (distinct kernel). See note below. |
| F-02 | `eval_gto` `GTOval_cart_deriv1`/`_deriv2`              | low  | manual-only    | Needs cart `ao_loc`/`nao` kernel surface — architectural change |
| F-03 | `intor` spinor representation                          | low  | **SCAFFOLDED** | Relativistic complex feature — scaffold + plan landed; see note below |
| **F-04** | **`intor` single-mol arity-3 (`int3c2e`)**         | low  | **auto-fixable → FIXED** | Reclassified from not-a-bug: upstream `mol.intor('int3c2e_sph')` is valid with no auxmol — `(μν\|P)` over mol's own basis. Implemented as the `auxmol==mol` specialisation; in-tree oracle vs byte-verified DF wrapper. See note below. |
| F-05 | `int1e_ecp_ipnuc` (ECP gradient)                       | med  | manual-only    | cintx-gated (Phase 7 GRAD-07) |
| F-06 | MP2/DFMP2 cross-spin αβ + plan-5 paths                 | med  | manual-only    | cintx#11 cross-spin `ao2mo` pending (external) |
| F-07 | Open-shell UCCSD `ulambda`/`urdm` + wave-3 hooks       | med  | manual-only    | Documented Phase-7 open-shell deferral (06-CONTEXT D-03) |
| F-08 | grad wave-3 hooks (`pyscf-grad`)                       | med  | manual-only (reason updated) | **Integral gate substantially lifted**: `int1e_ipovlp` + `int2e_ip1` now evaluate in-tree (probed OK); only `int1e_iprinv` per-atom origin plumbing remains. But the hook *bodies* (`get_veff` 2e-response, `hcore_generator`, `get_ovlp`) are multi-wave analytic-gradient scope (07-03..07-08), not an audit-fix. **Now ready for those waves.** See note below. |
| F-09 | DFT `xc_backend` libxc UKS arm                         | low  | won't-fix      | libxc_rs ≈ 6h compile, forbidden; xcfun is default path |
| **F-10** | **`init_guess_by_chkfile` basis projection (nao mismatch)** | **med** | **auto-fixable → FIXED** | `intor_cross` is proven in-tree (minao guess); not cintx-blocked. (Correction — see note below) |
| F-11 | `Mole::build` direct call                              | low  | won't-fix      | Intentional FOUND-02 constraint (zero-compute-deps redirect to `pyscf_gto::M`) |
| F-12 | `format_atom` callable atom form (GTO-01.5)            | low  | manual-only    | Needs Rust-closure API design decision |
| **F-13** | **`format_atom` Z-matrix atom form**              | **med** | **auto-fixable → FIXED** | Pure-Rust deterministic parser; in-tree oracle available |
| F-14 | All Phase 2/3/4 UAT "pending" items                    | —    | manual-only    | Need maturin + live PySCF runtime (byte-identity); no code change possible in-sandbox |
| F-15 | Phase-1 algebra primitive stubs                        | —    | resolved       | Already wired in Phase 2 (`cubecl` dispatch) |

**Tally (after follow-up pass 2, 2026-06-01):** 15 distinct findings — 4 fixed
(F-13, F-10, F-04, F-01 ip-arm; F-01 ip-cart + `GTOval_ig` remain), 1 scaffolded
(F-03, since superseded — cintx now ships validated spinor 1e+2e), 7 manual-only,
2 won't-fix, 1 already-resolved.

> Note: the stale `xfail` markers flagged in `03-VERIFICATION.md` (`test_scf_chkfile.py`) were
> already removed in-tree — verified during this sweep, no longer outstanding.

> **Correction (follow-up pass):** F-10 was initially classified manual-only on the assumption
> that the cross-basis overlap needed an external/cintx path. That was wrong — `intor_cross` is
> proven in-tree (the default `minao` guess uses it and byte-matches upstream). F-10 was
> therefore reclassified auto-fixable and resolved (commit `e1360c3`). Its exact cross-basis
> numbers remain invariant-verified, not live-PySCF-oracle-verified, in-sandbox.

> **Scaffold (follow-up pass):** F-03 (spinor representation) is a relativistic complex-valued
> feature, not an auto-fix — it needs a complex output type, the `sph→spinor` CG transform, and
> a live-PySCF byte oracle (no oracle-free invariant is strong enough). It was scaffolded
> (commit `5f9a3e8`): `crates/pyscf-gto/src/spinor.rs` (complex `IntorOutputComplex` + tested
> `n2c_per_shell` AO-count formula + honest `intor_spinor`/`sph2spinor_coeff` stubs + `#[ignore]`d
> oracle-contract tests) and a full task DAG in
> `.planning/features/F-03-spinor-integrals-PLAN.md` (T1 nao_2c → T2 CG tables → T3 congruence →
> T4 live-PySCF oracle gate → T5 PyO3). Implementation tasks T1–T5 remain open.

---

## Fixed

| #    | Finding                                  | Status   | Commit    |
|------|------------------------------------------|----------|-----------|
| F-13 | Z-matrix atom form                       | ✅ Fixed | `8d828eb` |
| F-10 | Cross-basis chkfile init-guess projection | ✅ Fixed | `e1360c3` |
| F-04 | Single-mol arity-3 `int3c2e` (μν\|P)      | ✅ Fixed | `c3ec974` |
| F-01 | `GTOval_ip_sph` gradient (ip-arm)        | ✅ Fixed (partial) | `46844dd` |

### F-01 detail (follow-up pass 2 — 2026-06-01)

Originally classified manual-only / cintx-gated. Re-examination split the finding:
`GTOval_ip_sph` (`nabla |AO>`, 3 components) is **byte-identical** to the gradient
block `GTOval_sph_deriv1[1:4]` of the already-shipped Phase-4 deriv1 kernel —
confirmed empirically against the upstream venv
(`mol.eval_gto('GTOval_ip_sph') == mol.eval_gto('GTOval_sph_deriv1')[1:4]`, max
diff `0.0`, **no sign flip**). So the spherical gradient is purely in-tree, not
cintx-gated.

Resolution (`crates/pyscf-gto/src/eval_gto.rs`): evaluate `GTOval_sph_deriv1`,
drop the leading value block, return `[3, ngrids, nao]`. The alias resolver was
extended to suffix bare `GTOval_ip`/`GTOval_ig` by `mol.cart` (mirrors upstream
`_get_intor_and_comp`).

**Still deferred (not part of this fix):**
- `GTOval_ip_cart` — `NotYetImplemented{phase:4}`, same cart-`ao_loc` deriv1
  blocker as `GTOval_cart_deriv1` (F-02).
- `GTOval_ig*` (magnetic-gauge GIAO) — manual-only; a distinct kernel, not a
  slice of deriv1.

**Verification:** in-tree oracle (`tests/eval_gto_smoke.rs::ip_sph_equals_deriv1_gradient_block`):
`GTOval_ip_sph == deriv1[1:4]` (`<1e-14`) + bare-alias routing. Replaces the
stale `ip_returns_not_yet_implemented_phase_7` test. Gates:
`cargo +nightly test -p pyscf-gto --locked` full suite green; rustfmt clean;
clippy no findings on new code.

### F-08 update (follow-up pass 2 — 2026-06-01) — reason corrected, still manual-only

The `pyscf-grad` wave-3 hooks (`get_veff`, `hcore_generator`, `get_ovlp`) were
classified *cintx-gated (land when cintx ships the missing integral families)*.
That reason is now **stale**: probing the live cintx via `intor()` shows
`int1e_ipovlp_sph` → `OK [3,nao,nao]` and `int2e_ip1` → `OK [3,nao,nao,nao,nao]`
**evaluate today**. Only `int1e_iprinv_sph` still errors — and only because the
plain `intor()` path doesn't set the per-atom rinv origin (cintx *does* expose
`with_rinv_origin`; pyscf-gto's `intor` just needs to plumb it). The 07-RESEARCH
"2 of 8 grad families" availability matrix (quoted in `grad_intor_smoke.rs`) is
out of date.

**Still manual-only** — but for a different reason: the hook *bodies* are
multi-wave analytic-gradient implementations (the planned waves 07-03..07-08) —
the J/K gradient response contraction over `int2e_ip1`, the energy-weighted
overlap-derivative assembly over `int1e_ipovlp`, and the per-atom `hcore`
generator — each requiring byte-identity against upstream PySCF and spanning the
grad subsystem. That is out of audit-fix (minimal single-file change) range.
**Actionable:** F-08 is no longer integral-blocked for the overlap-derivative
and 2e-response arms; it is ready for the 07-03..07-08 implementation waves
(remaining integral task: plumb `rinv_origin` through `intor` for the `hcore`
`iprinv` arm).

### F-04 detail (follow-up pass 2026-06-01)

Originally classified *not-a-bug* on the assumption that arity-3 always needs a
distinct auxiliary basis. That was wrong: upstream `mol.intor('int3c2e_sph')`
called with no auxmol is a legitimate operation — all three centers range over
`mol`'s own basis, yielding `[nao, nao, nao]`. The dispatcher returned
`NotYetImplemented{phase:3}` for it.

Resolution — `evaluate_arity3_single_mol` (`crates/pyscf-gto/src/intor.rs`): the
`auxmol == mol` specialisation of the byte-verified `evaluate_int3c2e_with_auxmol`.
It iterates every shell triple `(i,j,k)` over mol's own cintx `BasisSet` and
stitches the per-triple F-order `[ni,nj,nk]` block into a global F-order
`[nao,nao,nao]` buffer, using the same `OperatorId`/`SessionRequest` path the DF
wrapper uses (so it inherits libcint byte-identity at the cintx source). Scalar
arity-3 only; component-leading arity-3 derivatives (`int3c2e_ip1` …) stay
`NotYetImplemented` (grad-path, cintx-gated — F-01/F-08).

**Verification** (in-sandbox, no live PySCF):
- **Rigorous in-tree oracle** (`tests/int3c2e_auxmol.rs::int3c2e_single_mol_equals_self_auxmol`):
  `intor(mol, "int3c2e_sph")` is bit-identical (`<1e-12`) to
  `intor_with_auxmol(mol, "int3c2e_sph", mol)` — the latter is itself
  libcint-byte-verified at the cintx source, so equality pins the single-mol
  path to the same numbers. Shape `[nao,nao,nao]`, finite, non-zero asserted.

**Gates:** `cargo +nightly test -p pyscf-gto --locked` → full suite green (new
oracle test passes; pyscf-gto dep graph excludes libxc, no ~6h build).
`rustfmt --edition 2024 --check` → clean. `cargo +nightly clippy -p pyscf-gto`
→ no findings on the new code.

### F-13 detail

The `String`/`FilePath` atom forms previously returned `NotYetImplemented` for Z-matrix
input. The old per-line `tokens < 4` guard could not even detect a Z-matrix block (the 3rd
atom onward has ≥ 5 tokens and was misread as Cartesian).

Resolution — faithful port of upstream `pyscf/gto/mole.py::from_zmatrix`
(+ `pyscf.symm.geom::rotation_mat`), Apache-2.0:

- **Block-level format detection** matching upstream exactly: after stripping blank/comment
  lines, a first atom line with `< 4` whitespace tokens selects the Z-matrix path; otherwise
  Cartesian (unchanged behaviour).
- **Per-line construction** selected by token count (origin → +x bond → bond+angle →
  bond+angle+dihedral), with the Rodrigues axis-angle rotation matrix and the
  colinear-dihedral fallback reproduced verbatim.
- **Unit/origin/axes** post-processing reuses the existing Cartesian path, as upstream does.

**Verification**

- Pinned to PySCF's published output via the `from_zmatrix` docstring oracle (4-H example,
  byte-identity to 1e-6).
- 6 tests total: docstring oracle, water internals (bond lengths + H–O–H angle), Cartesian
  path regression, `;` separator, Angstrom→Bohr unit conversion, out-of-range reference
  rejection.
- Full `pyscf-gto` suite green: 59 lib tests + integration tests, 0 failures.

**Gates**

- `cargo +nightly test -p pyscf-gto --locked` → exit 0.
  - Nightly required: the workspace manifest does not parse on stable because
    `pyscf-dft → libxc_rs` uses the `profile-rustflags` Cargo feature. `pyscf-gto` does **not**
    pull `libxc` into its dep graph, so the ~6h libxc build was **not** triggered.
- `rustfmt --edition 2024 --check` → clean.
- `cargo +nightly clippy -p pyscf-gto` → no findings for the new code (remaining warnings are
  pre-existing cintx-dependency noise).

**Intentional deviation:** PySCF's `eval()` expression path (`DISABLE_EVAL=false`) is not
supported — Z-matrix coordinates must be plain numbers (equivalent to upstream
`DISABLE_EVAL=True`).

### F-10 detail

`init_guess_by_chkfile` returned `NotYetImplemented` when the prior chkfile's basis differed
from the current basis (`prior.nao != current nao`). Resolution — faithful port of
`project_mo_nr2nr` (`pyscf/scf/addons.py`):

```text
C2 = S22⁻¹ · (S21 · C1),   S22 = <cur|cur> = int1e_ovlp(cur),
                           S21 = <cur|prior> = intor_cross(cur, prior)
```

then `D[μν] = Σ_i occ_i·C2[μ,i]·C2[ν,i]`. The prior Mole/basis is reconstructed from the `mol`
JSON already stored in the chkfile (new `chkfile::load_mol_json_from_file` +
`pyscf_gto::loads`). The layout/solve idiom mirrors `init_guess_by_minao` (itself
`project_mo_nr2nr(ano, I, mol)`): F-order overlaps in `IntorOutput.values`, per-column
`solve_linear` against S22, `oracle_sum` density reduction. The byte-verified same-basis fast
path is preserved (extracted to `density_from_mos`).

**Verification scope** (in-sandbox, no live PySCF):
- **Same-basis identity invariant** (rigorous anchor): projecting onto the same basis yields
  `C2 == C1`, so the projected density equals the direct same-basis RDM1 — exercises the full
  `S21·C1` contraction + per-column S22 solve and proves it collapses to identity.
- **E2E cross-basis** (sto-3g → 6-31g) through the real chkfile read + `loads` path: seed shaped
  to the current basis, finite, symmetric.
- **Graceful error** when the stored prior mol cannot be reconstructed.
- **Residual risk:** exact cross-basis numbers are NOT oracle-verified against upstream
  in-sandbox. The `intor_cross` machinery itself is byte-verified via the default minao guess.

**Gates:** `cargo +nightly test -p pyscf-scf --locked` → exit 0 (5 chkfile/projection tests +
full suite green; SCF dep graph excludes libxc). `rustfmt --edition 2024 --check` → clean.
`cargo +nightly clippy -p pyscf-scf` → no findings for the new code.

---

## Manual-only findings (require developer attention)

**Partially resolved / reason corrected (follow-up pass 2):**
- F-01 — `GTOval_ip_sph` FIXED in-tree (`46844dd`); only `GTOval_ip_cart` (F-02
  blocker) + `GTOval_ig` (magnetic GIAO) remain.
- F-08 — integral gate substantially lifted (`int1e_ipovlp` + `int2e_ip1`
  evaluate today); remaining work is the 07-03..07-08 analytic-gradient
  implementation waves + plumbing `rinv_origin` through `intor` for the `iprinv`
  `hcore` arm. Not an audit-fix.

**cintx-gated (external crate — land when cintx ships the missing integral families):**
- F-05 `int1e_ecp_ipnuc`
- F-06 MP2/DFMP2 cross-spin αβ

**Live-PySCF environment (need maturin + upstream PySCF; no in-sandbox code change):**
- F-14 all Phase 2/3/4 UAT byte-identity "pending" items

**Design decisions:**
- F-02 cart-deriv kernel surface
- F-03 spinor representation
- F-12 callable atom form
- F-07 open-shell UCCSD (Phase-7 deferral)

**Won't-fix (intentional / forbidden):**
- F-09 libxc UKS arm — forbidden ~6h build; xcfun is the default path
- F-11 `Mole::build` direct call — intentional FOUND-02 redirect

---

## Pipeline notes

- Auto-fixable findings were processed sequentially; the single one (F-13) passed tests and
  committed atomically with its finding ID. No test failure, so no revert/halt occurred.
- F-13 was implemented inline (not via a spawned executor) because it is a single,
  numerically-delicate port where byte-identity matters; the full upstream algorithm + oracle
  were gathered first, then applied directly for tighter control.

### Follow-up pass 2 (2026-06-01) — F-01, F-08, F-04 targeted re-run

- **F-04** was already fixed in the prior follow-up (`c3ec974`) — no action.
- **F-01** ip-arm fixed inline (`46844dd`): `GTOval_ip_sph` is a slice of the
  byte-verified deriv1 kernel; the sign convention was confirmed empirically
  against the upstream venv before coding (no guessing on a numerically-delicate
  relationship). Implemented inline for the same reason as F-13.
- **F-08** stays manual-only but its blocking reason was corrected: cintx has
  since shipped `int1e_ipovlp` + `int2e_ip1` (probed live), so the integral gate
  is mostly lifted; the remaining work is multi-wave analytic-gradient
  implementation, out of audit-fix range. No code change — surfaced for the
  07-03..07-08 waves.
- No test failure occurred, so no revert/halt. Each fix committed atomically
  with its finding ID; logs saved under `log/`.
