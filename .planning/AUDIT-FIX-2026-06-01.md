# Audit-Fix Classification — 2026-06-01

**Source:** `audit-uat` (+ source-level NYI sweep, scoped to the "implement not implemented yet" directive)
**Severity threshold:** medium · **Max:** 5
**Outcome:** 1/1 auto-fixable fixed (F-13), pipeline completed cleanly (0 failures)

---

## Classification table

| #    | Finding (NYI marker)                                   | Sev  | Classification | Reason |
|------|--------------------------------------------------------|------|----------------|--------|
| **F-01** | **`eval_gto` `GTOval_ip`/`GTOval_ig` (grad/magnetic)** | med  | **partially auto-fixable → ip-arm FIXED** | `GTOval_ip_sph` reclassified: byte-identical to `GTOval_sph_deriv1[1:4]` (verified vs upstream, no sign flip) — in-tree, NOT cintx-gated. Fixed. `GTOval_ip_cart` deferred (F-02 cart surface); `GTOval_ig` magnetic-GIAO stays manual-only (distinct kernel). See note below. |
| **F-02** | **`eval_gto` `GTOval_cart_deriv1`/`_deriv2`**      | low  | **auto-fixable → FIXED (deriv1)** | Reclassified: the cart `ao_loc`/`nao` surface is the same `make_loc` formula `make_env` already uses, and the deriv1 host kernel computes cartesian values *before* the c2s transform. `GTOval_cart_deriv1` + `GTOval_ip_cart` (= cart F-01 arm) FIXED via a `spherical` flag + cart-loc build; in-tree c2s-congruence oracle (commit `00e9e11`). `_deriv2` stays deferred (no deriv2 kernel exists). See note below. |
| **F-03** | **`intor` spinor representation**                  | low  | **scaffold → RESOLVED (segmented)** | Architecture pivoted: cintx ships a libcint-parity cart→spinor transform, so `intor_spinor` (1e + `int2e_spinor`) routes through cintx. T1 + T2/T3 (obsoleted) + T4 byte-identity (atol 1e-10) + T5 PyO3 `complex128` + T6 2e all landed for SEGMENTED bases. See note below. |
| **F-04** | **`intor` single-mol arity-3 (`int3c2e`)**         | low  | **auto-fixable → FIXED** | Reclassified from not-a-bug: upstream `mol.intor('int3c2e_sph')` is valid with no auxmol — `(μν\|P)` over mol's own basis. Implemented as the `auxmol==mol` specialisation; in-tree oracle vs byte-verified DF wrapper. See note below. |
| **F-05** | **`int1e_ecp_ipnuc` + `int1e_ecp_iprinv` (ECP gradient)** | med  | **auto-fixable → FIXED (both integral arms)** | `int1e_ecp_ipnuc` shipped earlier (`73e9dcc`, GRAD-07 07-01). The `int1e_ecp_iprinv` per-atom `hcore_deriv` arm — previously "genuinely MISSING from cintx, no scheduled workstream" — is now **un-gated**: cintx **21-07 shipped native `ecp_iprinv`** (`dc9c0fc`/`84a5b77`, byte-identity parity vs vendor `nr_ecp_deriv`), so the pass-3 "no scheduled workstream" reason went STALE. Quick task `260601-rhc` un-gated it end-to-end (new `EcpEngine::ecp_int1e_iprinv` per-atom rinv-origin path + `pyscf-grad::hcore_deriv_ecp` returns the real `[3,nao,nao]` buffer). Only the multi-wave end-to-end analytic ECP-gradient FD assembly (F-08 / 07-03..07-08) + spinor iprinv remain out of scope. See pass-7 note below. |
| **F-06** | **MP2/DFMP2 cross-spin αβ (conventional layout)**  | med  | **manual-only → FIXED (cross-spin layout, oracle-certified)** | The pass-5 "unverifiable in-sandbox" premise was **disproven**. All three enumerated sub-tasks landed after pass 5 (`260601-nfb`/`pbk`): (a) `cross_spin_ao2mo` + F→C repack in `default_ao2mo` (`d7e7fad`); (b) the 2 PyO3 αβ sites un-gated + `dfump2_kernel` wired (`8540566`); (c) the open-shell live-PySCF oracle (`ump2_open_shell_oracle.py`). The oracle pass-5 deemed impossible **PASSES**: open-shell OH doublet (nocc_α ≠ nocc_β, real F→C repack) **conventional UMP2 = live PySCF 2.12.1 to `\|Δe_corr\|=2.3e-10`** (tol 1e-9). DFUMP2 residual `2.6e-5` = separate DF metric-fit-inverse method gap (NOT the cross-spin layout, NOT F-06's gate). See note below. |
| F-07 | Open-shell UCCSD `ulambda`/`urdm` + wave-3 hooks       | med  | manual-only    | Documented Phase-7 open-shell deferral (06-CONTEXT D-03) |
| F-08 | grad wave-3 hooks (`pyscf-grad`)                       | med  | manual-only (reason updated) | **Integral gate substantially lifted**: `int1e_ipovlp` + `int2e_ip1` now evaluate in-tree (probed OK); only `int1e_iprinv` per-atom origin plumbing remains. But the hook *bodies* (`get_veff` 2e-response, `hcore_generator`, `get_ovlp`) are multi-wave analytic-gradient scope (07-03..07-08), not an audit-fix. **Now ready for those waves.** See note below. |
| F-09 | DFT `xc_backend` libxc UKS arm                         | low  | won't-fix      | libxc_rs ≈ 6h compile, forbidden; xcfun is default path |
| **F-10** | **`init_guess_by_chkfile` basis projection (nao mismatch)** | **med** | **auto-fixable → FIXED** | `intor_cross` is proven in-tree (minao guess); not cintx-blocked. (Correction — see note below) |
| F-11 | `Mole::build` direct call                              | low  | won't-fix      | Intentional FOUND-02 constraint (zero-compute-deps redirect to `pyscf_gto::M`) |
| F-12 | `format_atom` callable atom form (GTO-01.5)            | low  | manual-only    | Needs Rust-closure API design decision |
| **F-13** | **`format_atom` Z-matrix atom form**              | **med** | **auto-fixable → FIXED** | Pure-Rust deterministic parser; in-tree oracle available |
| F-14 | All Phase 2/3/4 UAT "pending" items                    | —    | manual-only    | Need maturin + live PySCF runtime (byte-identity); no code change possible in-sandbox |
| F-15 | Phase-1 algebra primitive stubs                        | —    | resolved       | Already wired in Phase 2 (`cubecl` dispatch) |

**Tally (after follow-up pass 7, 2026-06-01):** 15 distinct findings — 7 fixed
(F-13, F-10, F-04, F-01 ip-sph+ip-cart, F-02 cart-deriv1, F-03 spinor via cintx
route-through, **F-06 cross-spin αβ MP2 — layout now live-PySCF-oracle-certified
at 2.3e-10**; only `GTOval_ig` + `GTOval_*_deriv2` remain on the eval_gto
surface), 5 manual-only
(F-05 + F-08 reason-corrected: the `ipnuc`/`ip` integral arms ship in-tree
and cintx#11 is closed — only the cintx-missing `iprinv` arms and the multi-wave
grad assembly remain), 2 won't-fix, 1 already-resolved. The F-06 DFUMP2 residual
(`2.6e-5`, DF metric-fit-inverse method) is a separate non-gating item.

> Note: the stale `xfail` markers flagged in `03-VERIFICATION.md` (`test_scf_chkfile.py`) were
> already removed in-tree — verified during this sweep, no longer outstanding.

> **Correction (follow-up pass):** F-10 was initially classified manual-only on the assumption
> that the cross-basis overlap needed an external/cintx path. That was wrong — `intor_cross` is
> proven in-tree (the default `minao` guess uses it and byte-matches upstream). F-10 was
> therefore reclassified auto-fixable and resolved (commit `e1360c3`). Its exact cross-basis
> numbers remain invariant-verified, not live-PySCF-oracle-verified, in-sandbox.

> **Scaffold → RESOLVED (follow-up pass 6, 2026-06-01):** F-03 (spinor representation) was first
> scaffolded (commit `5f9a3e8`/`28876f4`) on the premise that cintx shipped no spinor operators,
> which would have required hand-transcribing the libcint CG `sph→spinor` transform. That premise
> was wrong — cintx ships a **libcint-parity-validated cart→spinor transform** — so the
> implementation **pivoted to route through cintx** rather than transform-from-spherical.
> `intor_spinor` builds a `Representation::Spinor`-tagged cintx `BasisSet`, evaluates each shell
> tuple, and stitches the complex `n2c`-unit blocks into `IntorOutputComplex`. Landed:
> **T1** real `mol.nao_2c` from shells (`79061a3`); **T2/T3** obsoleted-via-cintx (`e3ac82e`);
> **T4** byte-identity to upstream PySCF 2.12.1 at atol 1e-10 (1e ovlp/kin/nuc on H2O/STO-3G
> ≤2e-14, int2e on H2/STO-3G 6e-16); **T5** PyO3 `complex128` bridge (`c8d0696`); **T6** arity-4
> `int2e_spinor` ERIs (`f860ebf`). **Limitation:** SEGMENTED bases only (`nctr==1`) — general
> contraction errors cleanly; multi-shell-same-`l` global ordering differs (eigenvalue-identical).
> See `.planning/features/F-03-spinor-integrals-PLAN.md` §3a + §3b for the full record.

---

## Fixed

| #    | Finding                                  | Status   | Commit    |
|------|------------------------------------------|----------|-----------|
| F-13 | Z-matrix atom form                       | ✅ Fixed | `8d828eb` |
| F-10 | Cross-basis chkfile init-guess projection | ✅ Fixed | `e1360c3` |
| F-04 | Single-mol arity-3 `int3c2e` (μν\|P)      | ✅ Fixed | `c3ec974` |
| F-01 | `GTOval_ip_sph` gradient (ip-arm)        | ✅ Fixed (partial) | `46844dd` |
| F-02 | `GTOval_cart_deriv1` + `GTOval_ip_cart`  | ✅ Fixed (deriv1) | `00e9e11` |
| F-03 | `intor` spinor (1e + `int2e_spinor`)     | ✅ Fixed (segmented) | `c8d0696` |
| F-05 | `int1e_ecp_iprinv` ECP-grad arm (ipnuc was `73e9dcc`) | ✅ Fixed (integral arms; FD assembly = F-08) | `e384188` |
| F-06 | Cross-spin αβ UMP2 (conventional layout) | ✅ Fixed (oracle-certified) | `8540566` |

### F-02 detail (follow-up pass — 2026-06-01)

Originally classified manual-only ("needs cart `ao_loc`/`nao` kernel surface —
architectural change"). The architectural concern turned out to be modest:

- The cartesian `ao_loc`/`nao` is the **same `make_loc` formula** `make_env`
  already applies (`((l+1)(l+2)/2)·nctr` per shell) — built fresh from `_bas` in
  the wrapper, independent of `mol.cart`, mirroring upstream
  `make_loc(bas, 'GTOval_cart_deriv1')`.
- The deriv1 host kernel already computes the cartesian values/gradients
  **before** applying the c2s transform. Cartesian output = those pre-transform
  values, so no new numeric stencil is needed.

Resolution:
- `pyscf-kernels` (`eval_gto.rs`): `eval_gto_sph_deriv1_cpu` gains a `spherical`
  flag; when `false` it writes the `ncart(l)` cartesian components directly (no
  c2s) at the cartesian `ao_loc` offsets. New `pub eval_gto_cart_deriv1`
  (host-only — the device deriv1 launcher emits spherical AOs). No `l` ceiling on
  the cart path (never calls `c2s_coeff`).
- `pyscf-gto` (`eval_gto.rs`): route `GTOval_cart_deriv1` (build cart loc, call
  the new kernel, shape `[4,ngrids,nao_cart]`) and `GTOval_ip_cart`
  (= `cart_deriv1[1:4]`, slice off the value block → `[3,ngrids,nao_cart]`).

This also closes the **`GTOval_ip_cart` arm of F-01** (the only remaining
cartesian gradient gap there).

**Verification** (in-sandbox, no live PySCF):
- **Congruence oracle** (`tests/eval_gto_cart_deriv1_oracle.rs`):
  `c2s(GTOval_cart_deriv1) == GTOval_sph_deriv1` (all 4 components, `<1e-10`) on
  H2O/cc-pVDZ (s,p,d — exercises l=2 where `ncart=6 ≠ nsph=5`). The spherical
  side is itself byte-verified against an independent longhand reference
  (`eval_gto_deriv1_oracle.rs`), so equality transitively pins the cartesian
  output. Device-sph vs host-cart is also a cross-implementation check.
- **Finite-difference** check on the cart gradient (independent of c2s *and* the
  analytic-derivative algebra).
- `GTOval_ip_cart == GTOval_cart_deriv1[1:4]` bit-for-bit.
- Replaces the stale `deriv1_cart_returns_not_yet_implemented_phase_4` smoke test.

**Still deferred:** `GTOval_*_deriv2` (no deriv2 kernel exists — `GTOval_sph_deriv2`
is itself NYI); `GTOval_ig*` (distinct GIAO kernel). **Residual risk:** absolute
cartesian numbers are congruence-/FD-verified in-tree, not live-PySCF-oracle-
verified.

**Gates:** `cargo +nightly test -p pyscf-gto` + `-p pyscf-kernels --locked` → full
suites green; `rustfmt --check` clean; clippy no findings on new code.

### F-01 detail (follow-up pass 2 — 2026-06-01)

Originally classified manual-only / cintx-gated. Re-examination split the finding:
`GTOval_ip_sph` (`nabla |AO>`, 3 components) is **byte-identical** to the gradient
block `GTOval_sph_deriv1[1:4]` of the already-shipped Phase-4 deriv1 kernel —
confirmed empirically against the upstream venv
(`mol.eval_gto('GTOval_ip_sph') == mol.eval_gto('GTOval_sph_deriv1')[1:4]`, max
diff `0.0`, **no sign flip**). So the spherical gradient is purely in-tree, not
cintx-gated.

Resolution (`crates/pyscf-gto/src/eval_gto.rs`): evaluate `GTOval_sph_deriv1`,
drop the leading value block, return `[3, ngrids, nao]`. The alias resolver was
extended to suffix bare `GTOval_ip`/`GTOval_ig` by `mol.cart` (mirrors upstream
`_get_intor_and_comp`).

**Still deferred (not part of this fix):**
- `GTOval_ip_cart` — was deferred here on the same cart-`ao_loc` deriv1 blocker
  as `GTOval_cart_deriv1`; **now FIXED under F-02** (`00e9e11`).
- `GTOval_ig*` (magnetic-gauge GIAO) — manual-only; a distinct kernel, not a
  slice of deriv1.

**Verification:** in-tree oracle (`tests/eval_gto_smoke.rs::ip_sph_equals_deriv1_gradient_block`):
`GTOval_ip_sph == deriv1[1:4]` (`<1e-14`) + bare-alias routing. Replaces the
stale `ip_returns_not_yet_implemented_phase_7` test. Gates:
`cargo +nightly test -p pyscf-gto --locked` full suite green; rustfmt clean;
clippy no findings on new code.

### F-08 update (follow-up pass 2 — 2026-06-01) — reason corrected, still manual-only

The `pyscf-grad` wave-3 hooks (`get_veff`, `hcore_generator`, `get_ovlp`) were
classified *cintx-gated (land when cintx ships the missing integral families)*.
That reason is now **stale**: probing the live cintx via `intor()` shows
`int1e_ipovlp_sph` → `OK [3,nao,nao]` and `int2e_ip1` → `OK [3,nao,nao,nao,nao]`
**evaluate today**. Only `int1e_iprinv_sph` still errors — and only because the
plain `intor()` path doesn't set the per-atom rinv origin (cintx *does* expose
`with_rinv_origin`; pyscf-gto's `intor` just needs to plumb it). The 07-RESEARCH
"2 of 8 grad families" availability matrix (quoted in `grad_intor_smoke.rs`) is
out of date.

**Still manual-only** — but for a different reason: the hook *bodies* are
multi-wave analytic-gradient implementations (the planned waves 07-03..07-08) —
the J/K gradient response contraction over `int2e_ip1`, the energy-weighted
overlap-derivative assembly over `int1e_ipovlp`, and the per-atom `hcore`
generator — each requiring byte-identity against upstream PySCF and spanning the
grad subsystem. That is out of audit-fix (minimal single-file change) range.
**Actionable:** F-08 is no longer integral-blocked for the overlap-derivative
and 2e-response arms; it is ready for the 07-03..07-08 implementation waves
(remaining integral task: plumb `rinv_origin` through `intor` for the `hcore`
`iprinv` arm).

### F-05 update (follow-up pass 3 — 2026-06-01) — reason corrected, still manual-only

The ECP-gradient finding was classified *cintx-gated (Phase 7 GRAD-07)*. Direct
re-examination shows that reason is **stale and conflates two distinct arms**:

1. **`int1e_ecp_ipnuc` (the `get_hcore` ECP-gradient term) — already resolved
   in-tree, NOT cintx-gated.** cintx ships `int1e_ecp_ipnuc_{cart,sph}` (manifest
   IDs 28..=29; `cintx-ops/src/resolver.rs:532-533`, `cintx-compat/src/raw.rs:167-168`).
   `CintxEcpEngine::ecp_int1e_ipnuc` (`crates/pyscf-gto/src/ecp_engine_cintx.rs:258-423`)
   evaluates it and stitches the component-leading `[3, nao, nao]` F-order buffer
   (`data[comp + p*3 + q*3*nao] = ∂/∂R_comp ⟨p|V_ECP|q⟩`), and it is wired into
   `pyscf-grad::get_hcore_ecp` (`crates/pyscf-grad/src/ecp.rs:69`). This landed in
   commit **`73e9dcc`** (`feat(07-01): wire ECP-gradient ipnuc dispatch`), so it is
   **not a fresh audit-fix** — the auto-fixable arm already shipped.

2. **`int1e_ecp_iprinv` (the per-atom `with_rinv_at_nucleus` `hcore_deriv` term) —
   genuinely cintx-gated.** It is MISSING from every cintx branch: the manifest rows
   exist (`INT1E_ECP_IPRINV_{CART,SPH,SPINOR}`) but have **no `ALL_CINT/ALL_CINT1E`
   wrapper in libcint 6.1.3** and resolve to `None`
   (`cintx-ops/src/lib.rs:25`, `cintx-ops/src/resolver.rs:328`,
   `cintx-compat/src/legacy.rs:285-287`). `pyscf-grad::hcore_deriv_ecp`
   (`crates/pyscf-grad/src/ecp.rs:120-141`) routes it to a clean cintx-availability
   error — correctly **not** `NotYetImplemented{phase:7}`. No code change can lift
   this in-sandbox; it un-gates only when a cintx workstream lands `ECPscalar_iprinv`
   (none is scheduled).

**Why no fix this pass:** the only auto-fixable arm (`ipnuc`) was already implemented
and wired (`73e9dcc`); the remainder is genuinely cintx-gated (`iprinv`) plus the
multi-wave end-to-end analytic ECP-gradient FD assembly (the same base-integral
contraction scope as F-08, waves 07-03..07-08). Both are out of audit-fix
(minimal single-file change) range. **Status corrected, not fixed.**

**Verification (in-sandbox, no live PySCF; logs under `log/`):**
- `cargo +nightly test -p pyscf-gto --locked --test grad_intor_smoke --test ecp_int1e_oracle`
  → green: `ecp_int1e_oracle` 3/3 (incl. `cu_lanl2dz_int1e_ecp_ipnuc_is_not_silently_scalar`),
  `grad_intor_smoke` 5/5 (incl. `int1e_ecp_ipnuc_returns_component_leading_3_nao_nao`,
  `ecpscalar_ipnuc_alias_resolves_like_int1e_ecp_ipnuc`, and
  `ecp_iprinv_is_clean_cintx_availability_error_not_phase_7`).
- `cargo +nightly test -p pyscf-grad --locked --test ecp_verify_fd`
  → 3 passed, 1 **correctly** ignored (`ecp_verify_fd_numeric` — gated on the full base
  assembly + `iprinv`, not on the ipnuc term). pyscf-grad dep graph excludes
  libxc (`cargo tree -p pyscf-grad` → 0 libxc rows), so no ~6h build was triggered.

### F-05 resolution (follow-up pass 7 — 2026-06-01) — iprinv arm FIXED (cintx 21-07 shipped `ecp_iprinv`)

The pass-3 conclusion ("`int1e_ecp_iprinv` genuinely MISSING from cintx — no
libcint 6.1.3 wrapper, no scheduled workstream") is now **STALE**. cintx
workstream **21-07 landed a native `ecp_iprinv`** kernel
(`cintx-cubecl/src/kernels/ecp.rs::launch_ecp` per-nucleus selector, commits
`dc9c0fc` + `84a5b77`, byte-identity parity vs vendor `nr_ecp_deriv`). It does
NOT go through the legacy libcint-FFI macro path (which still resolves
`int1e_ecp_iprinv` → `None` — a *different*, unused dispatch path); it routes
through the native `SessionRequest → query_workspace → evaluate → launch_ecp`
path, the same one the `ipnuc` arm already uses. The rinv-origin plumbing
(`ExecutionOptions.rinv_orig` → `operator_env_params`, `with_rinv_origin`,
validator) was also already present.

Quick task **`260601-rhc`** un-gated the arm end-to-end (NOT a multi-wave job —
the integral now simply evaluates):
- `pyscf-core`: new `EcpEngine::ecp_int1e_iprinv(&self, mol, name, rinv_origin)`
  trait method (default `EcpEngineNotAvailable`).
- `pyscf-gto` (`ecp_engine_cintx.rs`): `CintxEcpEngine::ecp_int1e_iprinv` resolves
  the iprinv `OperatorId` via `Resolver::descriptor_by_symbol("int1e_ecp_iprinv_sph").id`
  (no `OperatorId` const exists), sets `ExecutionOptions { rinv_orig: Some(mol.atom_coord(ia)) }`
  (Bohr), and stitches the per-atom component-leading `[3, nao, nao]` buffer
  (same layout as `ecp_int1e_ipnuc`). The hardcoded "MISSING from every cintx
  branch" rejection is removed.
- `pyscf-grad` (`ecp.rs`): `hcore_deriv_ecp` now calls `ecp_int1e_iprinv(...)` and
  returns the real per-atom buffer instead of a hardcoded availability error.
- Tests: flipped the 3 stale gated-behavior tests to un-gated
  (`grad_intor_smoke.rs`, `ecp_verify_fd.rs`); the scalar-path WR-01
  `InvalidMolecule` rejection in `ecp_engine_stub.rs` is correctly RETAINED
  (it rejects iprinv as a *gradient name on the scalar path*, not for absence).

**Verification (in-sandbox, logs under `log/`):**
- `cargo +nightly test -p pyscf-gto -p pyscf-grad --locked` → 0 failures, incl.
  `ecp_iprinv_evaluates_real_per_atom_buffer`, `ecp_iprinv_at_cu_equals_ipnuc_single_nucleus`
  (an in-tree **self-consistency** smoke at atol 1e-12 — the single-ECP-atom
  degeneracy where per-atom rinv selection coincides with all-slot accumulation;
  the EXTERNAL byte-identity lives in cintx's own `ecp_iprinv_parity.rs`, not
  here), and `ecp_iprinv_origin_matching_no_atom_is_all_zeros`.
- F-05's diff is clean under the **real CI fmt gate**
  (`git ls-files '*.rs' | xargs rustfmt --edition 2024 --check`).

**Still out of scope (NOT this fix):** the multi-wave end-to-end analytic
ECP-gradient FD assembly (consuming the per-atom `hcore_deriv` buffer into nuclear
forces — F-08 / waves 07-03..07-08), and the spinor iprinv path (cintx fails it
closed with `UnsupportedApi`; pyscf-gto keeps its spinor arm erroring). **Status:
both integral arms FIXED; FD assembly remains F-08 territory.**

### F-06 resolution (follow-up pass 7 — 2026-06-01) — FIXED, live-PySCF-oracle-certified

**This supersedes the pass-5 note below.** Pass 5 left F-06 manual-only on the
premise that the cross-spin `(o_α v_α | o_β v_β)` layout was *"unverifiable
in-sandbox by any oracle-free invariant"* and needed *"open-shell byte-identity
against live PySCF ... beyond a minimal change + cargo test."* **That premise was
disproven.** The live-PySCF oracle exists and passes, and all three enumerated
sub-tasks had already landed (in `260601-nfb`/`260601-pbk`, *after* pass 5 wrote
the finding off):

1. **(a) cross-spin ao2mo wrapper + F→C layout — DONE.** `cross_spin_ao2mo`
   (`crates/pyscf-mp2/src/mp2.rs:214`) + the F→C repack in `default_ao2mo`
   (commit `d7e7fad`). The very F↔C transpose hazard pass 5 flagged is now pinned
   by three always-on in-tree tests (`crates/pyscf-mp2/tests/ump2_cross_spin.rs`):
   Test A (non-palindromic F→C repack, layout-pinning), Test B (closed-shell
   `UMP2==RMP2` guard, necessary-not-sufficient), Test C (bra-ket symmetry, nvir>1).
2. **(b) un-gate the 2 PyO3 sites + wire `dfump2_kernel` — DONE** (commit
   `8540566`). `pyscf-py/src/mp.rs` no longer returns `NotYetImplemented{plan:4}`
   for the αβ block; the unrestricted PyO3 scanner routes `dfump2_kernel`.
3. **(c) open-shell byte-identity vs live PySCF — DONE & PASSING.** Harness
   `crates/pyscf-py/tests/ump2_open_shell_oracle.py` (the two-venv `.npz`
   cross-compare idiom my memory flags as in-sandbox-available): `.upstream-venv`
   PySCF 2.12.1 UHF→UMP2 reference vs the rs `.venv` PyO3 bridge, on a **real
   open-shell doublet (OH radical, spin=1, STO-3G) where nocc_α ≠ nocc_β** — so
   the F→C repack is genuinely exercised and the palindromic-symmetry masking that
   makes the closed-shell guard insufficient does **not** apply.

**Oracle result (this pass):**

```text
[upstream] UMP2  e_corr   = -0.015810842704
[rs]       UMP2  e_corr   = -0.015810842935
[delta]    UMP2   |Δe_corr| = 2.307e-10  (tol 1e-09)   → PASS  (cross-spin layout gate)
[delta]    DFUMP2 |Δe_corr| = 2.629e-05  (tol 1e-09)   → KNOWN GAP (separate, non-gating)
```

The conventional cross-spin layout is therefore **certified correct against live
PySCF** — exactly the sufficient gate pass 5 said could not be run. Logs:
`log/f06-open-shell-oracle.log`, `log/f06-mp2-tests.log`.

**Residual (out of F-06 scope, separate item):** the DFUMP2 `2.629e-5` delta is
the DF metric-fit-inverse **method** difference (rs rank-revealing `eigh`+lindep
vs upstream Cholesky), NOT the cross-spin layout and NOT the d-shell integral bug
(DF-01, the `n*b00` mixed-recurrence factor, already FIXED in cintx `55bf984` —
DFUMP2 fell from 1.78e-3 to ~2.6e-5). Closing it to 1e-9 needs matching upstream's
exact metric-inverse method; tracked separately, does not gate F-06.

**No source change this pass** — F-06's code was complete; this pass ran the
acceptance oracle, confirmed PASS, and corrected the stale classification.

---

### F-06 update (follow-up pass 5 — 2026-06-01) — reason corrected, still manual-only [SUPERSEDED by pass 7 above]

The MP2/DFMP2 cross-spin finding was classified *cintx#11 cross-spin `ao2mo`
pending (external)*. Direct re-examination shows that reason is **stale and
misattributes the blocker**:

1. **Not externally cintx-gated.** cintx#11 is **CLOSED** (05-08): `intor("int2e")`
   returns a real arity-4 tensor and RMP2 + DF-RMP2 are numeric-green in-tree
   (`crates/pyscf-mp2/tests/mp2_numeric_smoke.rs`). The AO→MO `ao2mo` transform is
   an **in-tree contraction** (`crates/pyscf-ao2mo/src/incore.rs::general`, which
   already accepts 4 distinct MO blocks), **not** a cintx feature — cintx is an
   AO-integral library and ships no `ao2mo`.

2. **The kernel + same-spin path already exist and are safely gated.** The αβ
   `opposite_spin_channel` and αα/ββ `same_spin_channel` are implemented and
   structurally tested (`crates/pyscf-mp2/src/ump2.rs`, `ump2_structural.rs`); the
   DF cross-spin assembler (`dfmp2.rs::assemble_cross_spin` + `dfump2_kernel`)
   exists with a synthetic-UHF structural test. The conventional αβ block is
   **safely** gated at both PyO3 sites with `NotYetImplemented{plan:4}`
   (`crates/pyscf-py/src/mp.rs:545-553` PyUMP2.kernel, `:806-819` PyMp2Scanner) —
   it refuses rather than reusing the αα block as αβ, so there is **no silent
   wrong open-shell energy**.

**Why this is NOT an audit-fix (the real blocker):**

- **Layout-delicate.** `ao2mo::general` returns **F-order** `[n0,n1,n2,n3]`; the
  kernel reads `eris_ab` in **C-order** `((i·nvir_a+a)·nocc_b+J)·nvir_b+B`. The
  proven RMP2 path (`default_ao2mo` → `[co,cv,co,cv]`) only works because the
  palindromic αα shape + the `(ia|jb)=(jb|ia)` particle-exchange symmetry absorb
  the F↔C reorder. **The distinct α/β spaces break that symmetry**, so the
  cross-spin wrapper must get the coefficient ordering *and* the F→C transpose
  exactly right — a guessy, numerically-delicate change.
- **Unverifiable in-sandbox by any oracle-free invariant.** The only in-tree
  invariant available — closed-shell `UMP2 == RMP2` — is **necessary but NOT
  sufficient**: a transposed-but-symmetric-equivalent layout bug is *invisible* in
  the α==β case (exactly the symmetry that masks it in RMP2), so it cannot certify
  the cross-spin layout. True correctness needs **open-shell byte-identity against
  live PySCF** (a doublet radical), i.e. the F-14 live-PySCF verification path
  (`.upstream-venv` + maturin two-venv `.npy` cross-compare) — beyond a minimal
  change + `cargo test`. Shipping an unverifiable cross-spin energy via an executor
  is exactly the plausible-but-wrong risk the pipeline guards against (the same
  reason **F-03** was first *scaffolded* rather than ship-unverified — it was only
  resolved once the cintx route-through gave a byte oracle; F-06 still lacks one).

**No code change this pass.** Remaining work, surfaced for a dedicated wave:
(a) the cross-spin `(o_α v_α | o_β v_β)` ao2mo wrapper with the correct F→C
layout; (b) un-gate the two PyO3 sites + wire `dfump2_kernel` into the
unrestricted PyO3 scanner; (c) open-shell byte-identity verification vs live
PySCF (UMP2 + DFUMP2). **Status corrected, not fixed.**

**Verification touched this pass (read-only ground-truthing; no tests run):**
the gate sites, kernel layout, `ao2mo::general` F-order contract, and cintx#11
closure were confirmed by source inspection (paths + lines above).

### F-04 detail (follow-up pass 2026-06-01)

Originally classified *not-a-bug* on the assumption that arity-3 always needs a
distinct auxiliary basis. That was wrong: upstream `mol.intor('int3c2e_sph')`
called with no auxmol is a legitimate operation — all three centers range over
`mol`'s own basis, yielding `[nao, nao, nao]`. The dispatcher returned
`NotYetImplemented{phase:3}` for it.

Resolution — `evaluate_arity3_single_mol` (`crates/pyscf-gto/src/intor.rs`): the
`auxmol == mol` specialisation of the byte-verified `evaluate_int3c2e_with_auxmol`.
It iterates every shell triple `(i,j,k)` over mol's own cintx `BasisSet` and
stitches the per-triple F-order `[ni,nj,nk]` block into a global F-order
`[nao,nao,nao]` buffer, using the same `OperatorId`/`SessionRequest` path the DF
wrapper uses (so it inherits libcint byte-identity at the cintx source). Scalar
arity-3 only; component-leading arity-3 derivatives (`int3c2e_ip1` …) stay
`NotYetImplemented` (grad-path, cintx-gated — F-01/F-08).

**Verification** (in-sandbox, no live PySCF):
- **Rigorous in-tree oracle** (`tests/int3c2e_auxmol.rs::int3c2e_single_mol_equals_self_auxmol`):
  `intor(mol, "int3c2e_sph")` is bit-identical (`<1e-12`) to
  `intor_with_auxmol(mol, "int3c2e_sph", mol)` — the latter is itself
  libcint-byte-verified at the cintx source, so equality pins the single-mol
  path to the same numbers. Shape `[nao,nao,nao]`, finite, non-zero asserted.

**Gates:** `cargo +nightly test -p pyscf-gto --locked` → full suite green (new
oracle test passes; pyscf-gto dep graph excludes libxc, no ~6h build).
`rustfmt --edition 2024 --check` → clean. `cargo +nightly clippy -p pyscf-gto`
→ no findings on the new code.

### F-13 detail

The `String`/`FilePath` atom forms previously returned `NotYetImplemented` for Z-matrix
input. The old per-line `tokens < 4` guard could not even detect a Z-matrix block (the 3rd
atom onward has ≥ 5 tokens and was misread as Cartesian).

Resolution — faithful port of upstream `pyscf/gto/mole.py::from_zmatrix`
(+ `pyscf.symm.geom::rotation_mat`), Apache-2.0:

- **Block-level format detection** matching upstream exactly: after stripping blank/comment
  lines, a first atom line with `< 4` whitespace tokens selects the Z-matrix path; otherwise
  Cartesian (unchanged behaviour).
- **Per-line construction** selected by token count (origin → +x bond → bond+angle →
  bond+angle+dihedral), with the Rodrigues axis-angle rotation matrix and the
  colinear-dihedral fallback reproduced verbatim.
- **Unit/origin/axes** post-processing reuses the existing Cartesian path, as upstream does.

**Verification**

- Pinned to PySCF's published output via the `from_zmatrix` docstring oracle (4-H example,
  byte-identity to 1e-6).
- 6 tests total: docstring oracle, water internals (bond lengths + H–O–H angle), Cartesian
  path regression, `;` separator, Angstrom→Bohr unit conversion, out-of-range reference
  rejection.
- Full `pyscf-gto` suite green: 59 lib tests + integration tests, 0 failures.

**Gates**

- `cargo +nightly test -p pyscf-gto --locked` → exit 0.
  - Nightly required: the workspace manifest does not parse on stable because
    `pyscf-dft → libxc_rs` uses the `profile-rustflags` Cargo feature. `pyscf-gto` does **not**
    pull `libxc` into its dep graph, so the ~6h libxc build was **not** triggered.
- `rustfmt --edition 2024 --check` → clean.
- `cargo +nightly clippy -p pyscf-gto` → no findings for the new code (remaining warnings are
  pre-existing cintx-dependency noise).

**Intentional deviation:** PySCF's `eval()` expression path (`DISABLE_EVAL=false`) is not
supported — Z-matrix coordinates must be plain numbers (equivalent to upstream
`DISABLE_EVAL=True`).

### F-10 detail

`init_guess_by_chkfile` returned `NotYetImplemented` when the prior chkfile's basis differed
from the current basis (`prior.nao != current nao`). Resolution — faithful port of
`project_mo_nr2nr` (`pyscf/scf/addons.py`):

```text
C2 = S22⁻¹ · (S21 · C1),   S22 = <cur|cur> = int1e_ovlp(cur),
                           S21 = <cur|prior> = intor_cross(cur, prior)
```

then `D[μν] = Σ_i occ_i·C2[μ,i]·C2[ν,i]`. The prior Mole/basis is reconstructed from the `mol`
JSON already stored in the chkfile (new `chkfile::load_mol_json_from_file` +
`pyscf_gto::loads`). The layout/solve idiom mirrors `init_guess_by_minao` (itself
`project_mo_nr2nr(ano, I, mol)`): F-order overlaps in `IntorOutput.values`, per-column
`solve_linear` against S22, `oracle_sum` density reduction. The byte-verified same-basis fast
path is preserved (extracted to `density_from_mos`).

**Verification scope** (in-sandbox, no live PySCF):
- **Same-basis identity invariant** (rigorous anchor): projecting onto the same basis yields
  `C2 == C1`, so the projected density equals the direct same-basis RDM1 — exercises the full
  `S21·C1` contraction + per-column S22 solve and proves it collapses to identity.
- **E2E cross-basis** (sto-3g → 6-31g) through the real chkfile read + `loads` path: seed shaped
  to the current basis, finite, symmetric.
- **Graceful error** when the stored prior mol cannot be reconstructed.
- **Residual risk:** exact cross-basis numbers are NOT oracle-verified against upstream
  in-sandbox. The `intor_cross` machinery itself is byte-verified via the default minao guess.

**Gates:** `cargo +nightly test -p pyscf-scf --locked` → exit 0 (5 chkfile/projection tests +
full suite green; SCF dep graph excludes libxc). `rustfmt --edition 2024 --check` → clean.
`cargo +nightly clippy -p pyscf-scf` → no findings for the new code.

---

## Manual-only findings (require developer attention)

**Partially resolved / reason corrected (follow-up pass 2):**
- F-01 — `GTOval_ip_sph` FIXED in-tree (`46844dd`); `GTOval_ip_cart` now also
  FIXED under F-02 (`00e9e11`); only `GTOval_ig` (magnetic GIAO) remains.
- F-02 — `GTOval_cart_deriv1` + `GTOval_ip_cart` FIXED (`00e9e11`); only
  `GTOval_*_deriv2` deferred (no deriv2 kernel). No longer a design decision.
- F-08 — integral gate substantially lifted (`int1e_ipovlp` + `int2e_ip1`
  evaluate today); remaining work is the 07-03..07-08 analytic-gradient
  implementation waves + plumbing `rinv_origin` through `intor` for the `iprinv`
  `hcore` arm. Not an audit-fix.
- F-05 — `int1e_ecp_ipnuc` integral gate **already lifted** (shipped + wired +
  tested, commit `73e9dcc` / GRAD-07 07-01); only the genuinely-cintx-missing
  `int1e_ecp_iprinv` `hcore_deriv` arm + the multi-wave end-to-end FD assembly
  (F-08 territory) remain. Not an audit-fix.
- F-06 — **FIXED (pass 7)**, no longer manual-only. The cross-spin ao2mo wrapper
  + F→C layout, PyO3 un-gating, and `dfump2_kernel` wiring all landed
  (`d7e7fad`/`8540566`), and the open-shell live-PySCF oracle pass 5 deemed
  impossible **PASSES** (conventional UMP2 |Δe_corr|=2.3e-10, tol 1e-9). See the
  F-06 resolution note above. Residual DFUMP2 `2.6e-5` = separate DF
  metric-fit-inverse method item.

**cintx-gated (external crate — land when cintx ships the missing integral families):**
- F-05 `int1e_ecp_iprinv` (the `hcore_deriv` per-atom arm; `ipnuc` arm already done)

**Live-PySCF environment (need maturin + upstream PySCF; no in-sandbox code change):**
- F-14 all Phase 2/3/4 UAT byte-identity "pending" items

**Separate non-gating items (not the original finding):**
- F-06 DFUMP2 `2.6e-5` residual — the DF metric-fit-inverse method difference (rs
  rank-revealing `eigh`+lindep vs upstream Cholesky), not the cross-spin layout
  and not DF-01 (already fixed, cintx `55bf984`). Closing to 1e-9 needs matching
  upstream's exact metric-inverse method.

**Design decisions:**
- F-12 callable atom form
- F-07 open-shell UCCSD (Phase-7 deferral)

**Won't-fix (intentional / forbidden):**
- F-09 libxc UKS arm — forbidden ~6h build; xcfun is the default path
- F-11 `Mole::build` direct call — intentional FOUND-02 redirect

---

## Pipeline notes

- Auto-fixable findings were processed sequentially; the single one (F-13) passed tests and
  committed atomically with its finding ID. No test failure, so no revert/halt occurred.
- F-13 was implemented inline (not via a spawned executor) because it is a single,
  numerically-delicate port where byte-identity matters; the full upstream algorithm + oracle
  were gathered first, then applied directly for tighter control.

### Follow-up pass 7 (2026-06-01, quick `260601-re6`) — F-06 RESOLVED

- **F-06 → FIXED.** The pass-5 "manual-only / unverifiable in-sandbox"
  classification was **disproven** by running the acceptance oracle pass 5 said
  could not be run. All three sub-tasks had already landed after pass 5
  (`260601-nfb`/`pbk`): cross-spin `cross_spin_ao2mo` + F→C repack (`d7e7fad`),
  PyO3 αβ un-gate + `dfump2_kernel` wiring (`8540566`), and the open-shell oracle
  harness (`ump2_open_shell_oracle.py`).
- **Acceptance gate run this pass:** two-venv `.npz` cross-compare on a real
  open-shell doublet (OH, spin=1, STO-3G, nocc_α ≠ nocc_β) — conventional UMP2
  **matches live PySCF 2.12.1 to `|Δe_corr|=2.3e-10`** (tol 1e-9) → **PASS**. The
  palindromic-symmetry masking that makes the closed-shell guard insufficient does
  not apply here, so the cross-spin F→C layout is certified. Logs:
  `log/f06-open-shell-oracle.log`, `log/f06-mp2-tests.log` (`pyscf-mp2` full suite
  green incl. the 3 `ump2_cross_spin.rs` layout tests).
- **No source change** — F-06's code was complete; this pass executed the oracle,
  confirmed PASS, and corrected the stale record. The DFUMP2 `2.6e-5` residual is
  a separate DF metric-fit-inverse method item (non-gating).
- `pyscf-mp2` and `pyscf-py` dep graphs both exclude libxc (`cargo tree` → 0 rows),
  so no ~6h build was triggered.

### Follow-up pass 5 (2026-06-01) — F-06 targeted re-run [SUPERSEDED by pass 7]

- **F-06** stays manual-only but its blocking reason was corrected (mirrors the
  F-05/F-08 treatment): the "cintx#11 external" framing is **stale** — cintx#11 is
  closed (arity-4 `int2e` real; RMP2/DF-RMP2 numeric-green) and `ao2mo` is an
  in-tree contraction, not a cintx feature. The αβ `opposite_spin_channel` kernel +
  same-spin blocks exist and the αβ block is safely gated (`NotYetImplemented{plan:4}`
  at `pyscf-py/src/mp.rs:545,806` — no silent wrong energy).
- **Why no code change:** the cross-spin block is layout-delicate (`ao2mo::general`
  is F-order, the kernel reads C-order; the RMP2 path only works because αα
  palindromic symmetry + `(ia|jb)=(jb|ia)` absorb the reorder — distinct α/β spaces
  break that), and its correctness is **unverifiable by any oracle-free in-tree
  invariant** (closed-shell `UMP2==RMP2` is necessary-but-not-sufficient — a
  transposed-but-symmetric-equivalent layout bug survives it). True correctness
  needs open-shell byte-identity vs live PySCF. Shipping an unverifiable cross-spin
  energy via an executor is the plausible-but-wrong risk the pipeline guards
  against (cf. F-03, which was held at scaffold until the cintx route-through
  supplied a byte oracle, then resolved). Surfaced for a dedicated wave:
  cross-spin ao2mo wrapper + PyO3 un-gate/`dfump2` wiring + live-PySCF verification.
- Ground-truthing was read-only (gate sites, kernel layout, `ao2mo::general`
  F-order contract, cintx#11 closure confirmed by source inspection); no tests run,
  so no revert/halt.

### Follow-up pass 3 (2026-06-01) — F-05 targeted re-run

- **F-05** stays manual-only but its blocking reason was corrected (mirrors the
  F-08 treatment): the `int1e_ecp_ipnuc` integral arm is **not** cintx-gated — it
  ships on cintx (`INT1E_ECP_IPNUC_{CART,SPH}`), is implemented in
  `CintxEcpEngine::ecp_int1e_ipnuc`, and is already wired into
  `pyscf-grad::get_hcore_ecp` (commit `73e9dcc`, GRAD-07 07-01). The only
  genuinely cintx-gated piece is `int1e_ecp_iprinv` (no libcint 6.1.3 wrapper,
  resolves to `None`, no scheduled cintx workstream); the rest is the multi-wave
  end-to-end ECP-gradient FD assembly (F-08 territory). No code change — both
  remaining arms are out of audit-fix range.
- Verified the ipnuc arm green before correcting the reason (no guessing on a
  numerically-delicate gradient relationship): pyscf-gto `ecp_int1e_oracle` +
  `grad_intor_smoke` and pyscf-grad `ecp_verify_fd` all pass (the end-to-end FD
  test is correctly `#[ignore]`d on the missing base assembly + iprinv, not on
  the ipnuc term). Logs under `log/`.
- No test failure, so no revert/halt.

### Follow-up pass 2 (2026-06-01) — F-01, F-08, F-04 targeted re-run

- **F-04** was already fixed in the prior follow-up (`c3ec974`) — no action.
- **F-01** ip-arm fixed inline (`46844dd`): `GTOval_ip_sph` is a slice of the
  byte-verified deriv1 kernel; the sign convention was confirmed empirically
  against the upstream venv before coding (no guessing on a numerically-delicate
  relationship). Implemented inline for the same reason as F-13.
- **F-08** stays manual-only but its blocking reason was corrected: cintx has
  since shipped `int1e_ipovlp` + `int2e_ip1` (probed live), so the integral gate
  is mostly lifted; the remaining work is multi-wave analytic-gradient
  implementation, out of audit-fix range. No code change — surfaced for the
  07-03..07-08 waves.
- No test failure occurred, so no revert/halt. Each fix committed atomically
  with its finding ID; logs saved under `log/`.
