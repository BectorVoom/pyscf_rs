---
phase: quick-260529-oj6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pyscf-algebra/src/host_fallback.rs
  - crates/pyscf-algebra/tests/host_fallback_oracle.rs
autonomous: true
requirements: [ALG-05]
must_haves:
  truths:
    - "host_fallback::eigh returns real eigenvalues+eigenvectors via faer (no NotYetImplemented)"
    - "host_fallback::cholesky returns a real lower-triangular L via faer"
    - "host_fallback::qr returns real Q and R factors via faer"
    - "host_fallback::svd returns real U, singular values, and V via faer"
    - "Each decomposition is validated by a CPU oracle test asserting its defining property"
    - "Each decomposition has a #[cfg(feature=\"rocm\")] oracle test mirroring transpose_oracle.rs"
    - "host_fallback never names a cubecl::* type (ALG-06 wall); device I/O goes through device_buffer::download/upload"
  artifacts:
    - path: "crates/pyscf-algebra/src/host_fallback.rs"
      provides: "Real faer-on-host implementations of eigh/cholesky/qr/svd over the Tensor surface"
      contains: "device_buffer::download"
    - path: "crates/pyscf-algebra/tests/host_fallback_oracle.rs"
      provides: "CPU-always + ROCm-cfg-gated oracle differential tests for all four decompositions"
      contains: "fn eigh_matches_oracle_on_cpu"
  key_links:
    - from: "crates/pyscf-algebra/src/host_fallback.rs"
      to: "crates/pyscf-algebra/src/device_buffer.rs"
      via: "download::<f64> / upload::<f64> round-trip"
      pattern: "device_buffer::(download|upload)"
    - from: "crates/pyscf-algebra/tests/host_fallback_oracle.rs"
      to: "crates/pyscf-algebra/src/host_fallback.rs"
      via: "upload -> host_fallback::fn -> download device path"
      pattern: "pyscf_algebra::\\{?.*(eigh|cholesky|qr|svd)"
---

<objective>
Refactor the four `host_fallback` decompositions (eigh, cholesky, qr, svd) from
`NotYetImplemented` stubs into real implementations via the faer-0.24 host
round-trip (ALG-05 discipline), and add ROCm oracle differential tests for each.

Purpose: ALG-05 mandates that dense decompositions route to faer on host. The
signatures are locked and re-exported from `lib.rs`; only the bodies and the
module doc comment change. The device-buffer bridge keeps every cubecl `Runtime`
type inside `device_buffer` (ALG-06 wall) — `host_fallback` only ever names
`device_buffer::download`/`upload`, `faer::*`, and `AlgebraError`.

Output:
- `crates/pyscf-algebra/src/host_fallback.rs` — four real faer-backed bodies + updated module doc.
- `crates/pyscf-algebra/tests/host_fallback_oracle.rs` — CPU-always + ROCm-cfg-gated oracle tests for all four.
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

# THE four stubs to replace (signatures LOCKED — do NOT change; they are re-exported from lib.rs:63)
@crates/pyscf-algebra/src/host_fallback.rs

# Closest analog — faer round-trip, Mat::from_fn row-major load, SelfAdjointEigen, error mapping, F-order output convention
@crates/pyscf-algebra/src/eigh_gen.rs

# Sibling FullPivLu solve — flat-slice wrapper discipline, Mat::from_fn, post-solve singular detection, no unwrap
@crates/pyscf-algebra/src/solve_linear.rs

# The Tensor<->host bridge: download::<f64>(client, &tensor) -> Vec<f64>, upload::<f64>(client, &flat, shape) -> Tensor
@crates/pyscf-algebra/src/device_buffer.rs

# Tensor<T=f64> generic; bare Tensor == Tensor<f64>; placeholder vs from_buffer
@crates/pyscf-algebra/src/tensor.rs

# Sibling refactored from NYI stub — doc-comment style with quick-id provenance line, backend-dispatch shape
@crates/pyscf-algebra/src/scal.rs

# THE oracle-test template — copy structure EXACTLY: Lcg RNG, random_matrix, check_case, CPU-always + #[cfg(feature="rocm")] gfx1152 test, direct client construction (no select_backend)
@crates/pyscf-algebra/tests/transpose_oracle.rs

# AlgebraError variants available for mapping (ShapeMismatch, Singular, CubeclRuntime(String))
@crates/pyscf-algebra/src/error.rs

<interfaces>
<!-- Contracts the executor must implement against. Do NOT change these signatures — they are re-exported at lib.rs:63 `pub use host_fallback::{cholesky, eigh, qr, svd};`. Bare `Tensor` == `Tensor<f64>`. -->

From crates/pyscf-algebra/src/host_fallback.rs (LOCKED signatures):
```rust
pub fn eigh(client: &AlgebraClient, matrix: &Tensor) -> Result<(Vec<f64>, Tensor), AlgebraError>;
pub fn cholesky(client: &AlgebraClient, matrix: &Tensor) -> Result<Tensor, AlgebraError>;
pub fn qr(client: &AlgebraClient, matrix: &Tensor) -> Result<(Tensor, Tensor), AlgebraError>;
pub fn svd(client: &AlgebraClient, matrix: &Tensor) -> Result<(Tensor, Vec<f64>, Tensor), AlgebraError>;
```

From crates/pyscf-algebra/src/device_buffer.rs (the bridge — use these, never a cubecl type):
```rust
pub fn download<F: DeviceScalar>(client: &AlgebraClient, t: &Tensor<F>) -> Result<Vec<F>, AlgebraError>;
pub fn upload<F: DeviceScalar>(client: &AlgebraClient, data: &[F], shape: Vec<usize>) -> Result<Tensor<F>, AlgebraError>;
```
`download`/`upload` are also re-exported at the crate root (`pyscf_algebra::{download, upload, release}`), so tests can call them directly.

faer 0.24 API (VERIFIED via context7 — give to executor; confirm only the qr/svd accessor spellings at compile):
```text
use faer::prelude::*;
use faer::{Mat, Side};
use faer::linalg::solvers::SelfAdjointEigen;   // proven in eigh_gen.rs

// load row-major flat slice (element (i,j) at data[i*n + j]):
let m = Mat::<f64>::from_fn(n, n, |i, j| data[i * n + j]);

// eigh (self-adjoint): eigenvalues ASCENDING
let evd = SelfAdjointEigen::new(m.as_ref(), Side::Lower)?;   // map_err -> CubeclRuntime
let evals = evd.S();   // diagonal; index evals[k]
let evecs = evd.U();   // index evecs[(i, j)]

// cholesky LLT (positive-definite only): returns Result
let llt = m.as_ref().llt(Side::Lower);   // Result<_, _> ; .L() -> lower-triangular factor Mat

// qr (infallible): .Q() and .R() factor Mats  (CONFIRM accessor names at compile)
let qr = m.as_ref().qr();

// svd: singular values DESCENDING and >= 0
let svd = m.as_ref().svd()?;   // .U(), .S() (use .S().column_vector()[k] to index), .V()

// faer Mat is COLUMN-MAJOR; element access mat[(i, j)] or mat.get(i, j).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement eigh + cholesky bodies (atomic commit 1)</name>
  <files>crates/pyscf-algebra/src/host_fallback.rs</files>
  <action>
Replace the `eigh` and `cholesky` NotYetImplemented stubs with real faer-0.24
bodies (ALG-05). Follow the eigh_gen.rs / solve_linear.rs wrapper discipline
exactly: download the host Vec, validate square, build the faer Mat row-major,
decompose, read factors back to a flat Vec, upload the result Tensor(s). The
crate already imports faer in solve_linear.rs/eigh_gen.rs, so the dep exists.

Add the imports `use faer::prelude::*;`, `use faer::{Mat, Side};`, and
`use faer::linalg::solvers::SelfAdjointEigen;` to the module. Keep
`use crate::{AlgebraClient, AlgebraError, Tensor};` and add `use crate::device_buffer;`.

SHARED helper (define once, used by all four bodies — add it now): a private
`fn square_n(matrix: &Tensor) -> Result<usize, AlgebraError>` that reads
`matrix.shape`, requires rank 2 with `shape[0] == shape[1]`, returns `n`, else
`AlgebraError::ShapeMismatch { expected: "square n*n matrix".into(), actual: format!("shape={:?}", matrix.shape) }`. Also guard the downloaded
`data.len() == n*n` after `device_buffer::download::<f64>(client, matrix)?`
(belt-and-suspenders, mirroring eigh_gen.rs lines 53-64) returning ShapeMismatch
on disagreement.

eigh body:
- `let n = square_n(matrix)?;`
- `let data = device_buffer::download::<f64>(client, matrix)?;` then the length guard.
- `let m = Mat::<f64>::from_fn(n, n, |i, j| data[i * n + j]);`
- `let evd = SelfAdjointEigen::new(m.as_ref(), Side::Lower).map_err(|e| AlgebraError::CubeclRuntime(format!("eigh failed: {e:?}")))?;` (reuse CubeclRuntime exactly as eigh_gen.rs:70 does).
- eigenvalues: `let eigenvalues: Vec<f64> = (0..n).map(|k| evd.S()[k]).collect();` (ASCENDING, as faer returns — DOCUMENT this in the doc comment).
- eigenvectors back to a flat Vec. DOCUMENT the layout convention in the doc comment and STAY consistent: use **column-major / F-order** to match eigh_gen.rs (`c_out[i + j*n] = U[(i,j)]`). Build `let mut evecs = vec![0.0_f64; n*n]; for j in 0..n { for i in 0..n { evecs[i + j*n] = evd.U()[(i, j)]; } }`.
- `let evec_tensor = device_buffer::upload::<f64>(client, &evecs, vec![n, n])?;`
- return `Ok((eigenvalues, evec_tensor))`.

cholesky body:
- `let n = square_n(matrix)?;` + download + length guard + `Mat::from_fn` (same as eigh).
- `let llt = m.as_ref().llt(Side::Lower).map_err(|e| AlgebraError::CubeclRuntime(format!("cholesky failed (matrix not positive-definite?): {e:?}")))?;` — non-PD input is the documented failure mode; map it to CubeclRuntime with that message (the existing error enum has no dedicated NotPositiveDefinite variant; do NOT add one — reuse CubeclRuntime, consistent with eigh_gen). Confirm `.llt(..)` returns a `Result` at compile; if faer 0.24 spells the accessor differently, use the compiler error to find the LLT solver entry point and its `.L()` factor accessor — do NOT invent.
- Read L back ROW-MAJOR into the output Tensor and DOCUMENT that convention: `let l = llt.L(); let mut out = vec![0.0_f64; n*n]; for i in 0..n { for j in 0..n { out[i*n + j] = l[(i, j)]; } }` (faer L is column-major Mat; we read element (i,j) and store row-major). Upper triangle of `l` is zero, so `out` is lower-triangular — the test will assert this.
- `Ok(device_buffer::upload::<f64>(client, &out, vec![n, n])?)`.

Update the MODULE doc comment (currently lines 1-8 saying "bodies are
NotYetImplemented stubs"): replace with a description that the four
decompositions now route to faer 0.24 on host via a `device_buffer` Vec<f64>
round-trip (ALG-05 + RESEARCH §9 + faer-ext Pitfall 3), naming the documented
layout conventions (eigenvectors F-order ascending eigenvalues; cholesky L
row-major lower-triangular; and forward-reference qr/svd from Task 2). Add a
`quick-260529-oj6:` provenance line in the doc comment matching scal.rs's
`quick-260529-skl:` style (scal.rs line 4).

Do NOT touch `qr` / `svd` in this task — they stay NotYetImplemented until Task 2
so this is a clean atomic commit. NO `unwrap`/`expect` in non-test code
(workspace lints `clippy::unwrap_used`).

Commit boundary: `git add crates/pyscf-algebra/src/host_fallback.rs` then commit
`feat(algebra): implement host_fallback eigh + cholesky via faer round-trip` after the verify gate passes.
  </action>
  <verify>
    <automated>cargo fmt -p pyscf-algebra && cargo clippy -p pyscf-algebra --lib -- -D warnings 2>&1 | tee log/oj6_t1_clippy.log; grep -c "NotYetImplemented" crates/pyscf-algebra/src/host_fallback.rs</automated>
  </verify>
  <done>eigh + cholesky bodies compile with `-D warnings`; `grep -c NotYetImplemented` on host_fallback.rs returns 2 (only qr + svd remain); module doc updated with quick-260529-oj6 provenance and layout conventions. cargo scoped to `-p pyscf-algebra` only.</done>
</task>

<task type="auto">
  <name>Task 2: Implement qr + svd bodies (atomic commit 2)</name>
  <files>crates/pyscf-algebra/src/host_fallback.rs</files>
  <action>
Replace the remaining `qr` and `svd` NotYetImplemented stubs, reusing the
`square_n` helper and download/length-guard pattern from Task 1.

NOTE on shape: qr and svd are valid for general (possibly non-square) matrices,
but the LOCKED signature carries no separate (rows, cols) — the Tensor's
`shape` does. Read `n` via `square_n` for now (the oracle tests use square
inputs and the existing call sites in later phases pass square matrices). If a
future caller needs rectangular qr/svd, that is a separate change; keep this
plan square-only and DOCUMENT that the host_fallback qr/svd currently require a
square Tensor (matching eigh/cholesky), returning ShapeMismatch otherwise.

qr body:
- `let n = square_n(matrix)?;` + download + length guard + `Mat::from_fn`.
- `let qr = m.as_ref().qr();` (infallible per faer 0.24 — no `?`). Confirm `.Q()` and `.R()` accessor spellings at compile; if they differ, read the compiler error / faer `Qr` solver to find the factor accessors — do NOT guess. Both are `n×n` Mats for square input.
- Read Q ROW-MAJOR into `q_out` and R ROW-MAJOR into `r_out` (same `out[i*n+j] = mat[(i,j)]` convention as cholesky; DOCUMENT it). R is upper-triangular.
- `let q_t = device_buffer::upload::<f64>(client, &q_out, vec![n, n])?;`
- `let r_t = device_buffer::upload::<f64>(client, &r_out, vec![n, n])?;`
- return `Ok((q_t, r_t))`.

svd body:
- `let n = square_n(matrix)?;` + download + length guard + `Mat::from_fn`.
- `let svd = m.as_ref().svd().map_err(|e| AlgebraError::CubeclRuntime(format!("svd failed: {e:?}")))?;` (svd returns Result in faer 0.24).
- singular values DESCENDING and >= 0: `let singular: Vec<f64> = (0..n).map(|k| svd.S().column_vector()[k]).collect();` — confirm the `.S().column_vector()[k]` indexing at compile (the verified API block notes this is how faer 0.24 exposes the singular value column); if the diagonal accessor differs, use the compiler error to find it. DOCUMENT that singular values are returned descending.
- U back ROW-MAJOR into `u_out`, V back ROW-MAJOR into `v_out` (both `n×n`, same convention). Confirm `.U()` / `.V()` accessor names at compile.
- `let u_t = device_buffer::upload::<f64>(client, &u_out, vec![n, n])?;`
- `let v_t = device_buffer::upload::<f64>(client, &v_out, vec![n, n])?;`
- return `Ok((u_t, singular, v_t))`.

Update the module doc comment forward-references from Task 1 so qr/svd are
described as implemented (qr Q/R row-major, svd U/V row-major + descending
singular values). NO `unwrap`/`expect` in non-test code.

Commit boundary: `git add crates/pyscf-algebra/src/host_fallback.rs` then commit
`feat(algebra): implement host_fallback qr + svd via faer round-trip` after the verify gate passes.
  </action>
  <verify>
    <automated>cargo fmt -p pyscf-algebra && cargo clippy -p pyscf-algebra --lib -- -D warnings 2>&1 | tee log/oj6_t2_clippy.log; grep -c "NotYetImplemented" crates/pyscf-algebra/src/host_fallback.rs</automated>
  </verify>
  <done>All four bodies compile with `-D warnings`; `grep -c NotYetImplemented` on host_fallback.rs returns 0; module doc describes all four implementations and their layout conventions. cargo scoped to `-p pyscf-algebra` only.</done>
</task>

<task type="auto">
  <name>Task 3: Oracle differential tests for all four decompositions (atomic commit 3)</name>
  <files>crates/pyscf-algebra/tests/host_fallback_oracle.rs</files>
  <action>
Create `crates/pyscf-algebra/tests/host_fallback_oracle.rs` mirroring
`tests/transpose_oracle.rs` structure EXACTLY: copy the `Lcg` LCG struct
(Knuth/MMIX constants, `next_f64()` -> [-1,1)) and the `random_matrix` helper
verbatim. Add a module doc comment in the transpose_oracle.rs style with a
`quick-260529-oj6:` provenance line, citing docs/rust_crate_test_guideline.md
(differential testing vs a trusted reference, randomized inputs, reproducible
seed). Per CLAUDE.md, the test guideline lives at the path noted in the project
conventions — follow it for test design.

Imports: `use cubecl::Runtime;` (brings `::client` into scope for the concrete
runtimes) and `use pyscf_algebra::{AlgebraClient, cholesky, download, eigh, qr, svd, upload};`.

These are FLOATING decompositions, NOT exact permutations — assert the DEFINING
PROPERTY with a numeric tolerance, NOT bitwise equality. `const TOL: f64 = 1e-9;`
(document that this is intentionally non-zero, unlike the exact transpose
permutation in transpose_oracle.rs:79).

Helpers (host-side, plain Vec<f64> row-major n×n math — no faer in the test, so
the assertion is independent of the implementation's faer call):
- `fn matmul(a: &[f64], b: &[f64], n: usize) -> Vec<f64>` — row-major n×n * n×n.
- `fn transpose(a: &[f64], n: usize) -> Vec<f64>` — row-major n×n transpose.
- `fn max_abs_diff(x: &[f64], y: &[f64]) -> f64` — max elementwise abs diff.
- `fn make_spd(rng: &mut Lcg, n: usize) -> Vec<f64>` — build A = MᵀM + n·I (SPD): draw random M (n*n), `let mt_m = matmul(&transpose(&m, n), &m, n);` then add `n as f64` to each diagonal `mt_m[i*n + i]`. SPD + symmetric — valid input for eigh and cholesky.
- `fn make_general(rng: &mut Lcg, n: usize) -> Vec<f64>` — just `random_matrix(rng, n*n)` (general input for qr and svd).

Device path per decomposition (upload -> host_fallback::fn -> download): build
the input Vec, `let t = upload::<f64>(client, &input, vec![n, n]).expect("upload");`,
call the decomposition, `download::<f64>(client, &tensor).expect("download")`
each returned Tensor back to a flat Vec, then assert the property. Release is
optional (registry persists to process exit; transpose_oracle doesn't release).

Property assertions (recall the documented layouts from Tasks 1-2:
eigenvectors F-order/column-major; cholesky L, qr Q/R, svd U/V all row-major):
- eigh: input = make_spd. `(evals, u_t) = eigh(client, &t)?`; download U (F-order:
  element (i,j) at `u[i + j*n]`). Reconstruct A_rec = U·diag(λ)·Uᵀ and assert
  `max_abs_diff(&A_rec, &input) <= TOL`. To multiply with F-order U, convert U to
  row-major first (`u_rm[i*n+j] = u[i + j*n]`) then use `matmul`/`transpose`.
  Also assert eigenvalues ascending (`evals[k] <= evals[k+1] + TOL`).
- cholesky: input = make_spd. `l_t = cholesky(client, &t)?`; download L (row-major).
  Assert L lower-triangular: every `l[i*n + j]` for `j > i` is `<= TOL` in abs.
  Reconstruct `matmul(&l, &transpose(&l, n), n)` ≈ input within TOL (L·Lᵀ = A).
- qr: input = make_general. `(q_t, r_t) = qr(client, &t)?`; download Q, R (row-major).
  Assert `matmul(&q, &r, n)` ≈ input within TOL (Q·R = A) and `matmul(&transpose(&q,n), &q, n)` ≈ identity within TOL (Qᵀ·Q = I).
- svd: input = make_general. `(u_t, s, v_t) = svd(client, &t)?`; download U, V (row-major).
  Build diag(s) row-major (n×n). Assert `U·diag(s)·Vᵀ` ≈ input within TOL, and
  singular values descending (`s[k] >= s[k+1] - TOL`) and non-negative (`s[k] >= -TOL`).

Sizes: drive a small set of `n` values, e.g. `const SIZES: &[usize] = &[1, 2, 3, 5, 8];`
(square only — host_fallback requires square inputs per Task 2). Use distinct
per-size seeds derived from a base seed (`base_seed.wrapping_add(i as u64)`),
exactly like transpose_oracle's `run_all`.

Structure four `run_*` driver fns (one per decomposition iterating SIZES) and
EIGHT `#[test]` fns total:
- `eigh_matches_oracle_on_cpu`, `cholesky_matches_oracle_on_cpu`, `qr_matches_oracle_on_cpu`, `svd_matches_oracle_on_cpu` — always run, CPU client built directly:
  `AlgebraClient::Cpu(cubecl_cpu::CpuRuntime::client(&cubecl_cpu::CpuDevice))`.
- `eigh_matches_oracle_on_rocm`, `cholesky_matches_oracle_on_rocm`, `qr_matches_oracle_on_rocm`, `svd_matches_oracle_on_rocm` — each `#[cfg(feature = "rocm")]`, ROCm client built directly:
  `AlgebraClient::Rocm(cubecl_hip::HipRuntime::client(&cubecl_hip::AmdDevice::default()))`, with the
  `assert!(matches!(client, AlgebraClient::Rocm(_)), "...")` guard exactly as transpose_oracle.rs:126-129.
Construct clients directly (NOT via select_backend) so CPU and ROCm tests never
race on the process-global PYSCF_BACKEND env var (transpose_oracle.rs:16-17 note).

The ROCm path is identical to CPU except the client and a distinct base seed —
factor the per-decomposition logic into the `run_*` fns so both the CPU and ROCm
`#[test]` fns just call `run_eigh(&client, seed, "CPU"|"ROCm")` etc.

Commit boundary: `git add crates/pyscf-algebra/tests/host_fallback_oracle.rs`
then commit `test(algebra): oracle differential tests for host_fallback eigh/cholesky/qr/svd` after the verify gate passes.
  </action>
  <verify>
    <automated>cargo fmt -p pyscf-algebra && cargo clippy -p pyscf-algebra --all-targets -- -D warnings 2>&1 | tee log/oj6_t3_clippy.log && cargo test -p pyscf-algebra 2>&1 | tee log/oj6_test.log; grep -E "test result|matches_oracle_on_cpu" log/oj6_test.log</automated>
  </verify>
  <done>`cargo test -p pyscf-algebra` passes including the four `*_matches_oracle_on_cpu` tests; clippy clean with `-D warnings` over `--all-targets`; ROCm tests compile under `#[cfg(feature="rocm")]` (executor MAY additionally run `cargo test -p pyscf-algebra --features rocm` as best-effort if hardware present, logging to log/oj6_rocm.log). NEVER run a workspace-wide cargo command — every invocation scoped to `-p pyscf-algebra` (libxc_rs compile is ~6h; pyscf-algebra is a leaf crate that does NOT pull it in).</done>
</task>

</tasks>

<verification>
- `grep -c "NotYetImplemented" crates/pyscf-algebra/src/host_fallback.rs` returns 0.
- `host_fallback.rs` contains `device_buffer::download` and `device_buffer::upload`, and names NO `cubecl::*` type (ALG-06 wall): `grep -c "cubecl" crates/pyscf-algebra/src/host_fallback.rs` returns 0.
- `cargo clippy -p pyscf-algebra --all-targets -- -D warnings` is clean.
- `cargo test -p pyscf-algebra` passes; the four `*_matches_oracle_on_cpu` tests appear in log/oj6_test.log with `test result: ok`.
- Module doc comment carries a `quick-260529-oj6:` provenance line and documents each decomposition's output layout convention.
</verification>

<success_criteria>
- All four host_fallback decompositions return real faer-backed results (no NotYetImplemented).
- eigh: A ≈ U·diag(λ)·Uᵀ, eigenvalues ascending, validated CPU + (cfg) ROCm.
- cholesky: L·Lᵀ ≈ A, L lower-triangular, validated CPU + (cfg) ROCm.
- qr: Q·R ≈ A, Qᵀ·Q ≈ I, validated CPU + (cfg) ROCm.
- svd: U·diag(s)·Vᵀ ≈ A, singular values descending and ≥ 0, validated CPU + (cfg) ROCm.
- ALG-06 wall intact: host_fallback never names a cubecl type; device I/O via device_buffer only.
- Three atomic commits (eigh+cholesky, qr+svd, tests). Every cargo invocation scoped to `-p pyscf-algebra`.
</success_criteria>

<output>
Create `.planning/quick/260529-oj6-refactor-host-fallback-to-cubecl-faer-ho/260529-oj6-SUMMARY.md` when done.
</output>
