---
phase: quick-260601-si2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/pyscf-ccsd/src/uccsd.rs
  - crates/pyscf-ccsd/src/ulambda.rs
  - crates/pyscf-ccsd/src/urdm.rs
  - crates/pyscf-ccsd/src/lib.rs
  - crates/pyscf-py/src/cc.rs
  - crates/pyscf-py/tests/ucc_open_shell_oracle.py
autonomous: false
requirements: [F-07, CCSD-05, CCSD-06]
must_haves:
  truths:
    - "A Python user runs cc.UCCSD(uhf_mf).run().make_rdm1() on an open-shell (nocc_α≠nocc_β) molecule and gets the α/β spin-block 1-RDMs back (no NotYetImplemented{wave:3})."
    - "make_rdm1 on an OH doublet (STO-3G) byte-matches live PySCF 2.12.1 UCCSD make_rdm1 per spin block to ≤1e-7."
    - "Tr(dm1a)+Tr(dm1b) == nelec for the open-shell reference (always-on, oracle-free)."
    - "For an α==β (closed-shell) reference, solve_ulambda/umake_rdm1 collapse to the validated closed-shell solve_lambda/make_rdm1 results."
    - "For an α==β reference, umake_rdm2(ao_repr=true) AO-basis 2-RDM equals the validated closed-shell rdm.rs::make_rdm2(ao_repr=true) within ~1e-9 (oracle-free; pins the pyscf_ao2mo::general C→F/F→C reorder, Pitfall 3)."
    - "Spin-orbital l2 is antisymmetric: l2[i,j,a,b] == -l2[j,i,a,b] == -l2[i,j,b,a]."
    - "update_ulambda is bit-identical across RAYON_NUM_THREADS=1 vs 8 (thread-count invariance)."
  artifacts:
    - path: "crates/pyscf-ccsd/src/uccsd.rs"
      provides: "UccsdResult surfaces converged spin-orbital so_t1/so_t2/so_eris + per-spin counts so λ/RDM can consume them"
      contains: "so_t1"
    - path: "crates/pyscf-ccsd/src/ulambda.rs"
      provides: "solve_ulambda + update_ulambda (spin-orbital GCCSD λ port) + ULambdaAmplitudes"
      contains: "pub fn solve_ulambda"
      min_lines: 120
    - path: "crates/pyscf-ccsd/src/urdm.rs"
      provides: "umake_rdm1 + umake_rdm2 + gamma intermediates + pack_rdm1/pack_rdm2 spin-block recovery"
      contains: "pub fn umake_rdm1"
      min_lines: 120
    - path: "crates/pyscf-py/src/cc.rs"
      provides: "PyUCCSD caches spin-orbital amps+eris and exposes solve_lambda/make_rdm1/make_rdm2 open-shell entry points"
      contains: "solve_ulambda"
    - path: "crates/pyscf-py/tests/ucc_open_shell_oracle.py"
      provides: "Two-venv live-PySCF acceptance gate on OH doublet"
      contains: "make_rdm1"
  key_links:
    - from: "crates/pyscf-ccsd/src/uccsd.rs"
      to: "UccsdResult.so_t1/so_t2/so_eris"
      via: "uccsd_kernel return surfaces t1_cur/t2_cur/eris instead of dropping them"
      pattern: "so_t1|so_eris"
    - from: "crates/pyscf-ccsd/src/urdm.rs"
      to: "crates/pyscf-ccsd/src/ulambda.rs"
      via: "umake_rdm1 consumes (l1,l2) from solve_ulambda"
      pattern: "l1.*l2"
    - from: "crates/pyscf-py/src/cc.rs"
      to: "pyscf_ccsd::solve_ulambda / umake_rdm1"
      via: "PyUCCSD.make_rdm1 calls the open-shell entry points on cached so amps+eris"
      pattern: "umake_rdm1|solve_ulambda"
    - from: "crates/pyscf-py/tests/ucc_open_shell_oracle.py"
      to: "live PySCF 2.12.1 cc.UCCSD"
      via: "two-venv .npz cross-compare on OH doublet"
      pattern: "UCCSD"
---

<objective>
Close audit finding **F-07**: implement the open-shell UCCSD Λ-equations + reduced
density matrices and wire the two wave-3 `make_rdm1`/`make_rdm2` hooks (currently
`NotYetImplemented{wave:3}`). The in-tree UCCSD amplitude solver is internally
SPIN-ORBITAL (`SpinOrbitalEris`, `uccsd.rs`), so this ports PySCF's spin-orbital
`gccsd_lambda.py` (219 lines) + `gccsd_rdm.py` (297 lines) — the clean single-tensor
1:1 analogs of the validated closed-shell `lambda.rs`/`rdm.rs` — NOT the spin-block
`uccsd_lambda.py`/`uccsd_rdm.py`.

Purpose: Phase-7 open-shell CCSD gradients (GRAD-06) and any open-shell response
density consumer need a complete, validated λ + RDM surface. The closed-shell RDM
surface shipped in 06-06; this is the documented open-shell deferral (06-CONTEXT D-03).

Output: a converged-amplitude-surfacing `UccsdResult`; filled `ulambda.rs`/`urdm.rs`;
wired PyUCCSD `solve_lambda`/`make_rdm1`/`make_rdm2`; and a live-PySCF acceptance
oracle that certifies open-shell `make_rdm1` byte-identity against PySCF 2.12.1.

THE plan is NOT done until Task 6's live oracle PASSES. A plausible-but-wrong RDM is a
FAILURE, not a partial success (the F-03/F-06 discipline).
</objective>

<execution_context>
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/pyscf_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260601-si2-implement-open-shell-uccsd-lambda-and-rd/260601-si2-RESEARCH.md
@.planning/AUDIT-FIX-2026-06-01.md
@CLAUDE.md
@docs/rust_crate_test_guideline.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. No exploration required. -->

From crates/pyscf-ccsd/src/uintermediates.rs — the spin-orbital eris λ/RDM read directly:
  pub struct SpinOrbitalEris {
      pub oovv: Vec<f64>,   // <ij||ab> [no,no,nv,nv]  ((i*no+j)*nv+a)*nv+b
      pub oooo: Vec<f64>,   // <ij||kl> [no,no,no,no]
      pub vvvv: Vec<f64>,   // <ab||cd> [nv,nv,nv,nv]  (the nv⁴ arena tenant)
      pub ovvo: Vec<f64>,   // <ia||bj> [no,nv,nv,no]
      pub ooov: Vec<f64>,   // <ij||ka> [no,no,no,nv]
      pub ovvv: Vec<f64>,   // <ia||bc> [no,nv,nv,nv]
      pub fock: Vec<f64>,   // [nmo,nmo] (occ first); diagonal = mo_energy for canonical ref
      pub mo_energy: Vec<f64>, // [nmo] occ first then vir
      pub no: usize,        // nocc_a + nocc_b
      pub nv: usize,        // nvir_a + nvir_b
  }
  // pub(crate) index helpers: oovv_idx/oooo_idx/vvvv_idx/ovvo_idx/ooov_idx/ovvv_idx

From crates/pyscf-ccsd/src/uccsd.rs — the kernel that currently DROPS t1+eris:
  pub fn uccsd_kernel(refr:&UccsdReference, frozen:&Frozen, pool:&WorkspacePool)
      -> Result<UccsdResult, PyscfRsError>;
  // inside: builds `eris: SpinOrbitalEris`, iterates to `t1_cur: Vec<f64>` (no*nv),
  //         `t2_cur: Vec<f64>` (no*no*nv*nv); then pack_amplitudes(&t2_cur,...) → spin-block.
  //         t1_cur + eris are DROPPED at the Ok(...) return (only e_corr + spin-block t2 survive).
  pub struct UccsdResult { e_corr, e_aa, e_ab, e_bb, converged, niter, emp2,
                           amplitudes: UccsdAmplitudes }
  // UccsdAmplitudes { nocc_a, nvir_a, nocc_b, nvir_b, t2aa, t2ab, t2bb } — NO t1, spin-block only.
  // pack_amplitudes(&t2, no, nv, no_a, nv_a, no_b, nv_b) slices by spin label:
  //   α-occ = 0..no_a, β-occ = no_a..no, α-vir = 0..nv_a, β-vir = nv_a..nv.

From crates/pyscf-ccsd/src/lambda.rs (the closed-shell mirror to follow VERBATIM):
  pub struct LambdaAmplitudes { pub l1, pub l2, pub converged: bool, pub niter: usize }
  pub fn solve_lambda(t1:&[f64], t2:&[f64], eris:&ChemistsEris, pool:&WorkspacePool)
      -> Result<LambdaAmplitudes, PyscfRsError>;
  // seed l1=t1,l2=t2; HARD pool.try_reserve on wvvvv≈nv⁴ (NO downgrade); dual-criterion
  // CONV_TOL_NORMT/MAX_CYCLE; reserve nv⁴ scratch ONCE; ShapeMismatch (not panic) on bad seed.

From crates/pyscf-ccsd/src/rdm.rs (the closed-shell mirror to follow VERBATIM):
  pub fn make_rdm1(t1,t2,l1,l2, eris:&ChemistsEris, ao_repr:bool, mo_coeff:&MOCoefficients)
      -> Result<Density, PyscfRsError>;
  pub fn make_rdm2(...) -> Result<Density, PyscfRsError>;
  pub fn gamma1_intermediates(t1,t2,l1,l2,no,nv) -> Result<Gamma1, _>; // Gamma1 { doo,dov,dvo,dvv }
  // make_rdm1_trace_equals_nelec test: nelec = 2.0*no; |trace - nelec| < 1e-10.
  // make_rdm2 ao_repr=true: VALIDATED (byte-checked) closed-shell AO 2-RDM via the
  //   pyscf_ao2mo::general C→F/F→C reorder at rdm.rs:418-449 — the round-trip oracle for Task 3.

From crates/pyscf-py/src/cc.rs — the bridge sites:
  pub struct PyUCCSD { inner: UccsdReference, py_mf, frozen: Frozen, e_corr: Option<f64> }
  // PyUCCSD.kernel currently stores ONLY e_corr — the result is otherwise dropped.
  // PyRCCSD.solve_lambda/make_rdm1/make_rdm2 (lines ~434-540) are the closed-shell template:
  //   borrow cached (t1,t2), build WorkspacePool::from_env(), default_ao2mo→ChemistsEris,
  //   solve_lambda → ccsd_make_rdm1(...,ao_repr,&refr.mo_coeff). is_overridden(...,"make_rdm1",..)
  //   routes subclass overrides via call_method1.

Upstream port targets (this repo): pyscf/cc/gccsd_lambda.py (make_intermediates l.34-99,
update_lambda l.103-168), pyscf/cc/gccsd_rdm.py (_gamma1 l.26-43, _gamma2 l.49-104,
_make_rdm1 l.138-176, _make_rdm2 l.178-254). The RESEARCH.md §2 table maps each upstream
block to its in-tree analog.

Template oracle: crates/pyscf-py/tests/ump2_open_shell_oracle.py (the proven F-06 two-venv idiom).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface the converged spin-orbital amplitudes + eris on UccsdResult</name>
  <files>crates/pyscf-ccsd/src/uccsd.rs, crates/pyscf-ccsd/src/lib.rs</files>
  <action>
Per RESEARCH §3 item 1 (the single biggest plumbing gap): `uccsd_kernel` builds the
converged spin-orbital `t1_cur` (no*nv), `t2_cur` (no*no*nv*nv) and `eris: SpinOrbitalEris`
but DROPS `t1_cur` + `eris` at the `Ok(UccsdResult{...})` return (only `e_corr` + the
spin-block `pack_amplitudes(&t2_cur,...)` survive; `UccsdAmplitudes` has no t1 at all).
λ/RDM consume all three.

Add fields to `UccsdResult`: `so_t1: Vec<f64>`, `so_t2: Vec<f64>`, `so_eris: SpinOrbitalEris`,
and the per-spin counts `no_a: usize, nv_a: usize, no_b: usize, nv_b: usize` (these are
already computed as `channel_counts` locals at the top of `uccsd_kernel`). Populate them at
the existing `Ok(...)` return by moving `t1_cur`, `t2_cur`, `eris` into the result (they are
otherwise dropped on the same line — reorder so `pack_amplitudes` borrows `&t2_cur` BEFORE
the move, or clone the spin-block pack input). Do NOT change the energy/convergence/amplitude
math — this is pure surfacing. Keep `SpinOrbitalEris` already re-exported from lib.rs (it is).
Document the new fields the same way the existing ones are.
  </action>
  <verify>
    <automated>cargo +nightly test -p pyscf-ccsd --locked uccsd 2>&1 | tee log/si2-task1.log | tail -20</automated>
  </verify>
  <done>UccsdResult carries so_t1/so_t2/so_eris + no_a/nv_a/no_b/nv_b; existing uccsd_smoke + energy tests still pass; e_corr/spin-block amplitudes unchanged (no numeric regression).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port the spin-orbital GCCSD Λ-equations into ulambda.rs</name>
  <files>crates/pyscf-ccsd/src/ulambda.rs, crates/pyscf-ccsd/src/lib.rs</files>
  <behavior>
    - Seed l1=t1, l2=t2; for an α==β closed-shell reference, solve_ulambda collapses to the validated closed-shell solve_lambda l1/l2 (within CONV_TOL_NORMT).
    - Converged l2 is antisymmetric: l2[i,j,a,b] == -l2[j,i,a,b] == -l2[i,j,b,a] (gccsd_lambda.py:198-199 self-check).
    - update_ulambda is bit-identical across RAYON_NUM_THREADS=1 vs 8.
    - A truncated/mis-shaped t1/t2 seed returns ShapeMismatch, never panics.
  </behavior>
  <action>
Replace the `ulambda.rs` stub with a faithful port of `pyscf/cc/gccsd_lambda.py`, mirroring
`lambda.rs` discipline VERBATIM per spin-orbital tensor (RESEARCH §2a). Public API:
  pub struct ULambdaAmplitudes { pub l1: Vec<f64>, pub l2: Vec<f64>, pub converged: bool, pub niter: usize }
  pub fn solve_ulambda(t1:&[f64], t2:&[f64], eris:&SpinOrbitalEris, pool:&WorkspacePool) -> Result<ULambdaAmplitudes, PyscfRsError>;
  pub fn update_ulambda(t1,t2,l1,l2, eris:&SpinOrbitalEris, wvvvv:&mut[f64]) -> Result<(Vec<f64>,Vec<f64>), CcsdError>;

Port `make_intermediates` (gccsd_lambda.py l.34-99: v1/v2/v3/v4/v5/w3/woooo/wovvo/wovoo/wvvvo)
into a new `ULambdaImds` struct built from `(t1,t2,SpinOrbitalEris)`. Reuse the shared
`uintermediates.rs` blocks where they coincide (make_tau, f_vv/f_oo/f_ov, w_oooo, w_ovvo,
w_vvvv); add the λ-only v1/v2/v5/w3/wovoo/wvvvo. Apply the energy-diagonal removal:
`v1 -= diag(mo_e_v)`, `v2 -= diag(mo_e_o)` (l.110-111), exactly as lambda.rs:107-111 does for
Loo/Lvv. `mba = ½ einsum('klca,klcb->ba',l2,t2)`, `mij = ½ einsum('kicd,kjcd->ij',l2,t2)`
(l.116-117). The `m3` vvvv tenant: `m3 += ½ einsum('ijcd,cdab->ijab',l2,vvvv)` (l.125) reuses
the nv⁴ `wvvvv` scratch.

Port `update_lambda` (l.103-168): CRITICAL — the symmetrizer is the SIGNED spin-orbital
ANTISYMMETRIZER `tmp - tmp.transpose(1,0,2,3)` then `- tmp.transpose(0,1,3,2)` (l.132-133,138,143),
NOT lambda.rs's closed-shell SYMMETRIC `tmp + tmp.transpose(1,0,3,2)` (RESEARCH Pitfall 2 — the
most likely silent bug; pin it with the l2-antisymmetry behavior test). Divide by eia / (eia+jb)
(l.163-165) using `eris.mo_energy` (occ first then vir).

`solve_ulambda` mirrors `lambda.rs::solve_lambda` structure VERBATIM: validate seed shapes →
ShapeMismatch (T-06-06-SHAPE, NOT panic); HARD `pool.try_reserve` pre-flight on the spin-orbital
`wvvvv ≈ nv⁴` tenant (NO downgrade — note nv = nv_a+nv_b is double a single channel so nv⁴ is 16×;
RESEARCH Pitfall 5); reserve the nv⁴ scratch ONCE before the loop; seed l1=t1,l2=t2; dual-criterion
`||Δl|| < CONV_TOL_NORMT` within `MAX_CYCLE` (reuse the existing lambda.rs constants — do NOT invent
tolerances, Pitfall 7). ALGEBRA WALL (HARD, RESEARCH Pitfall 1): every einsum → host loop that
MATERIALIZES the contracted-axis products into a Vec then reduces via `pyscf_algebra::oracle_sum`
(or oracle_dot). NO bare `+=` across a contracted axis. NO `pyscf_algebra::gemm` (it is a
NotYetImplemented{phase:2} stub). `#![forbid(unsafe_code)]` already at crate root.

Re-export `solve_ulambda`/`update_ulambda`/`ULambdaAmplitudes` from lib.rs. Remove the
`#![allow(dead_code)]` + the DEFERRED doc note. Place the three behavior tests in `ulambda.rs`
(α==β collapse vs lambda.rs, l2-antisymmetry, RAYON 1==8 + a ShapeMismatch-not-panic test mirroring
lambda.rs:558-588).
  </action>
  <verify>
    <automated>cargo +nightly test -p pyscf-ccsd --locked ulambda 2>&1 | tee log/si2-task2.log | tail -25</automated>
  </verify>
  <done>solve_ulambda/update_ulambda live + re-exported; α==β collapse, l2-antisymmetry, RAYON 1==8, and ShapeMismatch-not-panic tests pass; no bare += across contracted axes (grep -v '^//' src/ulambda.rs | grep -c 'gemm' == 0).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Port the spin-orbital GCCSD RDMs into urdm.rs (+ spin-block recovery)</name>
  <files>crates/pyscf-ccsd/src/urdm.rs, crates/pyscf-ccsd/src/lib.rs</files>
  <behavior>
    - umake_rdm1 produces a spin-orbital dm1 whose Tr == nelec (= no, since the spin-orbital occ count is the electron count) — the always-on oracle-free invariant.
    - For an α==β reference, umake_rdm1 collapses to the validated closed-shell make_rdm1.
    - pack_rdm1 slices the spin-orbital dm1 into (dm1a, dm1b) using the same spin labels pack_amplitudes uses (α-occ 0..no_a, β-occ no_a..no, α-vir 0..nv_a, β-vir nv_a..nv).
    - make_rdm2 energy reconstruction: ½·einsum('pqrs,pqrs', eris, dm2) + e1 == e_tot (gccsd_rdm.py:292-295) — catches an omitted final transpose.
    - ao_repr=true α==β round-trip (the REQUIRED Pitfall-3 fix): for an α==β (closed-shell-equivalent) reference, umake_rdm2(ao_repr=true) AO-basis 2-RDM equals the VALIDATED closed-shell rdm.rs::make_rdm2(ao_repr=true) on the same RHF reference within ~1e-9. Exercises the exact pyscf_ao2mo::general C→F/F→C reorder (rdm.rs:418-449), oracle-free. KNOWN LIMITATION (state in the test's own comment): α==β cannot catch a spin-block-SPECIFIC transpose inside the AO repack (only the OH oracle could, and make_rdm2 byte-identity is not gated this task) — but it DOES catch a wholesale-wrong C→F/F→C reorder, the dominant F-06 failure mode.
  </behavior>
  <action>
Replace the `urdm.rs` stub with a faithful port of `pyscf/cc/gccsd_rdm.py`, mirroring `rdm.rs`
discipline VERBATIM per spin-orbital tensor (RESEARCH §2b). Public API:
  pub fn umake_rdm1(t1,t2,l1,l2, eris:&SpinOrbitalEris, ao_repr:bool, mo_coeff_a:&MOCoefficients, mo_coeff_b:&MOCoefficients) -> Result<.., PyscfRsError>;
  pub fn umake_rdm2(t1,t2,l1,l2, eris:&SpinOrbitalEris, ao_repr:bool, mo_coeff_a, mo_coeff_b, pool:&WorkspacePool) -> Result<.., PyscfRsError>;
plus `pack_rdm1`/`pack_rdm2` helpers (mirroring `uccsd.rs::pack_amplitudes`'s spin-label slicing).
Return type: choose a spin-block container — return the spin-orbital dm1/dm2 AND the packed
(dm1a,dm1b)/(dm2aa,dm2ab,dm2bb) so the bridge can hand PySCF-shaped tuples (RESEARCH §2b
"spin-orbital → spin-block recovery"). Mirror the closed-shell `Density` return shape where it fits.

Port `_gamma1_intermediates` (l.26-43) → (doo,dov,dvo,dvv), single-tensor spin-orbital:
`doo = -einsum('ie,je->ij',l1,t1) - ½ einsum('imef,jmef->ij',l2,t2)`; dvv/dvo analogous; dov=l1
(simpler than the closed-shell theta form). Port `_make_rdm1` (l.138-176): `dm1[:no,:no]=doo+doo.T`,
`[:no,no:]=dov+dvo.T`, `[no:,no:]=dvv+dvv.T`, `*=.5`, then `+1` on the occupied diagonal (l.155-161).
**trace=nelec invariant.** ao_repr: `einsum('pi,ij,qj->pq', mo, dm1, mo)` (l.175) with the
spin-orbital mo_coeff = α/β stacked (gccsd_rdm.py:281 `mo_a+mo_b`).

Port `_gamma2_intermediates` (l.49-104) → (dovov,dvvvv,doooo,doovv,dovvo,dvvov,dovvv,dooov):
miajb, goovv/gvvvv/goooo/gooov/govvo/govvv then the transpose antisymmetrizations (l.92-104).
Port `_make_rdm2` (l.178-254): block placement (l.192-217) + the with_dm1 separable correction
(l.229-244) + **the final `dm2.transpose(1,0,3,2)` (l.250)** (RESEARCH Pitfall 8 — easy to omit;
the energy-reconstruction behavior test catches it). ao_repr via the in-tree
`pyscf_ao2mo::general` back-transform — the EXACT path rdm.rs uses (rdm.rs:418-449): reorder C→F
before `general`, reorder the result F→C after (RESEARCH Pitfall 3 — the F-06 F-order/C-order trap).
This ao_repr=true nmo⁴→nao⁴ back-transform is the single most F-06-prone line in the plan; it MUST
ship with coverage — pin it with the REQUIRED ao_repr=true α==β round-trip behavior test below
(NOT deferred — the path ships, so verify it).

Frozen-core: ACTIVE-ONLY this task (RESEARCH Open Question 2 / Pitfall 6 — match the rdm.rs
CCSD-10 deferral precedent; document it, do NOT silently embed). ALGEBRA WALL identical to Task 2:
every einsum → host-loop materialize-then-oracle_sum, NO bare += across contracted axes, NO gemm.
HARD `pool.try_reserve` (NO downgrade) for the `gvvvv ≈ nv⁴` γ2 tenant, reserved ONCE (Pitfall 5,
nv = nv_a+nv_b → 16× a single channel). Re-export from lib.rs; remove the stub's allow(dead_code)
+ DEFERRED note.

Behavior tests in urdm.rs:
  1. Tr=nelec (mirror rdm.rs:526-546).
  2. α==β collapse of umake_rdm1 vs make_rdm1.
  3. make_rdm2 energy-reconstruction `½·⟨eri,dm2⟩+e1==e_tot` (MO basis).
  4. REQUIRED — ao_repr=true α==β round-trip: build a UccsdReference with alpha==beta (closed-shell-
     equivalent, same RHF reference the collapse tests use), call umake_rdm2(ao_repr=true), pack the
     αα spin-block AO 2-RDM, and assert it equals the VALIDATED closed-shell
     rdm.rs::make_rdm2(ao_repr=true) result on that RHF reference within ~1e-9. This exercises the
     EXACT pyscf_ao2mo::general C→F/F→C reorder (rdm.rs:418-449) and pins Pitfall 3 against the
     already-byte-validated closed-shell AO path. Add a comment in the test body stating the KNOWN
     LIMITATION: α==β cannot catch a spin-block-SPECIFIC transpose inside the AO repack (only the OH
     oracle could, and make_rdm2 byte-identity is out of scope this task) — but it DOES catch a
     wholesale-wrong C→F/F→C reorder, the dominant F-06 failure mode.
  </action>
  <verify>
    <automated>cargo +nightly test -p pyscf-ccsd --locked urdm 2>&1 | tee log/si2-task3.log | tail -25</automated>
  </verify>
  <done>umake_rdm1/umake_rdm2 + pack_rdm1/pack_rdm2 live + re-exported; Tr=nelec, α==β umake_rdm1 collapse, make_rdm2 MO energy-reconstruction, AND the REQUIRED ao_repr=true α==β round-trip invariant (umake_rdm2(ao_repr=true) == validated rdm.rs::make_rdm2(ao_repr=true) within ~1e-9) all pass; grep -v '^//' src/urdm.rs | grep -c 'gemm' == 0.</done>
</task>

<task type="auto">
  <name>Task 4: Wire the wave-3 hooks + PyUCCSD open-shell λ/RDM entry points</name>
  <files>crates/pyscf-py/src/cc.rs</files>
  <action>
Wire the open-shell λ/RDM surface into the PyO3 bridge. Per RESEARCH §3 item 4 + Open Question 1
(recommend DIRECT-IN-BRIDGE, NOT through CcsdOverrideHooks — that trait is RHF-shaped on
CcsdReference and cannot carry UccsdReference; the trait-override seam is a v1.x concern). Leave
the `hooks.rs` `make_rdm1`/`make_rdm2` `NotYetImplemented{wave:3}` trait DEFAULTS as-is (they are
the closed-shell RHF seam; open-shell does not route through them) — the wave-3 closure is the
PyUCCSD entry points below being live, not the trait default body.

(a) Extend `PyUCCSD` to cache the spin-orbital amplitudes + eris after kernel(): add a field
`so: Option<(Vec<f64>, Vec<f64>, SpinOrbitalEris, /*no_a*/usize, /*nv_a*/usize, /*no_b*/usize, /*nv_b*/usize)>`
(or a small named struct). In `PyUCCSD::kernel`, after `uccsd_kernel(...)`, store
`result.so_t1/so_t2/so_eris` + the per-spin counts (currently the result is dropped except e_corr).

(b) Add `PyUCCSD::solve_lambda` — borrow the cached `so`, build `WorkspacePool::from_env()`, call
`pyscf_ccsd::solve_ulambda(&so_t1, &so_t2, &so_eris, &pool)`, return the convergence flag (mirror
the closed-shell PyRCCSD::solve_lambda shape at cc.rs:434; error cleanly if kernel() not run).

(c) Add `PyUCCSD::make_rdm1(ao_repr=False)` and `PyUCCSD::make_rdm2(ao_repr=False)` — solve_ulambda
then call `umake_rdm1`/`umake_rdm2`, returning the (dm1a, dm1b) / (dm2aa, dm2ab, dm2bb) spin-block
tuples as NumPy arrays via the existing converters (the α/β mo_coeff come from
`me.inner.alpha.mo_coeff` / `me.inner.beta.mo_coeff`). Mirror PyRCCSD::make_rdm1/make_rdm2
(cc.rs:455-540) for the borrow/pool/is_overridden pattern; route subclass overrides via
`is_overridden(..., "make_rdm1", &["UCCSD"]) → call_method1` exactly as the closed-shell path does.
Import `solve_ulambda`/`umake_rdm1`/`umake_rdm2`/`SpinOrbitalEris` from `pyscf_ccsd`. Do NOT
py.detach where hooks may re-enter Python (the established kernel-doesn't-detach rule).
  </action>
  <verify>
    <automated>cargo +nightly test -p pyscf-py --locked cc 2>&1 | tee log/si2-task4.log | tail -25</automated>
  </verify>
  <done>PyUCCSD caches spin-orbital amps+eris; solve_lambda/make_rdm1/make_rdm2 entry points compile + are exposed; no NotYetImplemented surfaces on the open-shell make_rdm path; pyscf-py structural tests green.</done>
</task>

<task type="auto">
  <name>Task 5: Build the OH-doublet live-PySCF oracle harness</name>
  <files>crates/pyscf-py/tests/ucc_open_shell_oracle.py</files>
  <action>
Create `ucc_open_shell_oracle.py` by mirroring `ump2_open_shell_oracle.py` (the proven F-06
two-venv idiom) per RESEARCH §4a and `docs/rust_crate_test_guideline.md`. State in the docstring:
specification source = PySCF 2.12.1 (the byte-identity oracle); verified scope = open-shell
UCCSD λ/RDM on OH/STO-3G (SEGMENTED); tolerance + molecule explicit; do NOT claim unverified
scope (larger bases where cintx ordering differs).

System: OH radical, `spin=1`, STO-3G (nocc_α=5, nocc_β=4 → nocc_α≠nocc_β; the SAME molecule the
F-06 oracle uses) — the non-palindromic spin slice that EXERCISES the spin-orbital↔spin-block
transpose (α==β masks it, the F-06 trap; RESEARCH §1/§5).

Stage 1 (`.upstream-venv`, PySCF 2.12.1): `mf=scf.UHF(mol).run()`; `mycc=cc.UCCSD(mf).run()`;
`mycc.solve_lambda()`; `dm1=mycc.make_rdm1()` (spin-block (dm1a,dm1b)); also dump `e_corr`,
α/β `mo_coeff`, and the mol spec to a temp `.npz`.

Stage 2 (`.venv`, rs PyO3): rebuild the mol via rs `gto.M`, wrap the upstream-converged α/β MOs
in a minimal UHF-shaped shim (exactly as the ump2 oracle does), run `cc.UCCSD(shim).kernel()`
then `.make_rdm1()`.

Compare + tolerances: `e_corr` ≤1e-7 (sanity, already validated by uccsd_smoke); **`make_rdm1`
block byte-identity** `|dm1a_rs - dm1a_up|, |dm1b_rs - dm1b_up| ≤ 1e-7` (THE primary λ-correctness
gate); the **trace invariant** `Tr(dm1a)+Tr(dm1b) == 9` (oracle-free). Do NOT gate on make_rdm2
byte-identity (RESEARCH Open Question 3 — dm1 byte-identity is the gate). OPTIONAL BONUS (Open
Question 3 / per the revision note): if the OH/STO-3G budget trivially permits it, ALSO assert a
make_rdm2 slice (e.g. dm2ab, or the ao_repr=False dm2ab-slice) ≤1e-7 as a non-gating bonus — this
is the only thing that could catch a spin-block-SPECIFIC AO-repack transpose that the in-tree
α==β round-trip (Task 3) cannot. The REQUIRED ao_repr coverage is the Task 3 in-tree round-trip;
this oracle dm2 assertion is optional and must NOT be the sole make_rdm2 coverage relied upon.
The script re-invokes the rs `.venv` for stage 2 itself (the ump2 oracle pattern). This task
CREATES the harness; Task 6 RUNS it as the gate.
  </action>
  <verify>
    <automated>test -f crates/pyscf-py/tests/ucc_open_shell_oracle.py && $REPO/.upstream-venv/bin/python -c "import ast,sys; ast.parse(open('crates/pyscf-py/tests/ucc_open_shell_oracle.py').read()); print('parse OK')" 2>&1 | tee log/si2-task5.log; [ "${REPO:=/home/user/Documents/workspace/pyscf_rs}" ] && /home/user/Documents/workspace/pyscf_rs/.upstream-venv/bin/python -c "import ast; ast.parse(open('crates/pyscf-py/tests/ucc_open_shell_oracle.py').read()); print('parse OK')"</automated>
  </verify>
  <done>ucc_open_shell_oracle.py exists, parses, mirrors the ump2 oracle two-venv structure, states spec source/scope/tolerance per the test guideline, and gates on make_rdm1 block byte-identity + trace=9.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: ACCEPTANCE GATE — run the live-PySCF oracle (make_rdm1 byte-identity)</name>
  <what-built>
The full open-shell UCCSD λ + RDM surface (Tasks 1-4) and the OH-doublet two-venv oracle
harness (Task 5). This task RUNS the oracle — the SUFFICIENT gate for F-07. A
plausible-but-wrong RDM is a FAILURE (the F-03/F-06 discipline): the α==β in-tree collapse
(Tasks 2-3) is necessary-but-not-sufficient; only this OH oracle (nocc_α≠nocc_β) catches a
spin-orbital↔spin-block transpose/antisymmetry bug.
  </what-built>
  <how-to-verify>
First confirm the maturin .venv reflects the Task-4 bridge, then run the oracle:

  1. REPO=/home/user/Documents/workspace/pyscf_rs
  2. Confirm no libxc in the dep graph (must print 0):
       cargo tree -p pyscf-py 2>/dev/null | grep -ci libxc
  3. Rebuild the rs .venv extension so the new PyUCCSD entry points are live:
       cd $REPO && $REPO/.venv/bin/maturin develop -m crates/pyscf-py/Cargo.toml --release 2>&1 | tee log/si2-task6-maturin.log
  4. Run the acceptance oracle (it self-invokes the .venv for stage 2):
       $REPO/.upstream-venv/bin/python $REPO/crates/pyscf-py/tests/ucc_open_shell_oracle.py 2>&1 | tee log/si2-task6-oracle.log

  Expected PASS criteria (all must hold):
    - [upstream] UCCSD e_corr  ≈ [rs] UCCSD e_corr   (|Δ| ≤ 1e-7)
    - |dm1a_rs - dm1a_up| ≤ 1e-7  AND  |dm1b_rs - dm1b_up| ≤ 1e-7   ← THE λ-correctness gate
    - Tr(dm1a) + Tr(dm1b) == 9   (OH nelec; oracle-free)
    - the script prints an explicit PASS line

  If make_rdm1 byte-identity FAILS: the spin-orbital antisymmetrizer sign/index (Task 2,
  Pitfall 2) or the F-order/C-order ao_repr repack (Task 3, Pitfall 3) is wrong — fix and re-run.
  Do NOT mark F-07 closed on a failing oracle.

  Also confirm the always-on in-tree suite stays green (no live PySCF needed), INCLUDING the
  Task-3 ao_repr=true α==β round-trip invariant (the required in-tree coverage for the
  make_rdm2 ao_repr back-transform):
       cargo +nightly test -p pyscf-ccsd --locked 2>&1 | tee log/si2-task6-intree.log | tail -15
  </how-to-verify>
  <resume-signal>Type "approved" once the oracle prints PASS (make_rdm1 byte-identity ≤1e-7 + trace=9) and the in-tree suite is green (incl. the Task-3 ao_repr=true α==β round-trip); or describe the delta/failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python `mf` → PyUCCSD snapshot | User-supplied UHF reference crosses into Rust; already validated by the existing snapshot_uccsd_reference path. |
| spin-orbital ↔ spin-block index map | The internal antisymmetry/transpose boundary where a silent (α==β-invisible) correctness bug can hide. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-si2-01 | Tampering | spin-orbital λ antisymmetrizer + spin-block recovery (Pitfall 2, Tasks 2-3) | mitigate | l2-antisymmetry behavior test + the OH oracle (Task 6, make_rdm1 ao_repr=False, nocc_α≠nocc_β) — the OH oracle genuinely catches the antisymmetrizer sign/index and the spin-orbital→spin-block recovery. α==β collapse alone is insufficient (the F-06 trap). |
| T-si2-02 | Information disclosure | make_rdm2 ao_repr=true F-order/C-order repack (Pitfall 3, Task 3) | mitigate | (a) Mirror rdm.rs:418-449 C→F/F→C reorder exactly. (b) REQUIRED in-tree coverage: the Task-3 ao_repr=true α==β round-trip invariant — umake_rdm2(ao_repr=true) == VALIDATED closed-shell rdm.rs::make_rdm2(ao_repr=true) within ~1e-9 — catches a wholesale-wrong C→F/F→C reorder, the dominant F-06 failure mode. SCOPE: this mitigation covers Pitfall 3 (the AO back-transform) ONLY; it does NOT cover Pitfall 2 (the antisymmetrizer + spin-block recovery) — that is T-si2-01's OH oracle. RESIDUAL (honest, documented — NOT silently presented as validated): a spin-block-SPECIFIC transpose INSIDE the AO repack is NOT gated, because make_rdm2 byte-identity is out of scope this task (the OH oracle gates make_rdm1 only). The optional non-gating make_rdm2 dm2-slice assertion in Task 5 is the only thing that could catch it; it is a bonus, not a gate. |
| T-si2-03 | Denial of service | nv⁴ wvvvv/gvvvv arena tenant (Tasks 2-3) | mitigate | HARD pool.try_reserve pre-flight, NO downgrade (RESEARCH Pitfall 5); nv=nv_a+nv_b → 16× a single channel. |
| T-si2-SC | Tampering | npm/pip/cargo installs | accept | NO new dependencies added; pyscf-ccsd + pyscf-py dep graphs confirmed 0 libxc rows (RESEARCH §0; cargo tree). |

**Coverage note (OH oracle scope, Task 6):** The OH oracle gates `make_rdm1` with `ao_repr=False` (MO basis). It genuinely catches Pitfall 2 (the spin-orbital antisymmetrizer + spin-block recovery — T-si2-01). It does NOT touch the make_rdm2 `ao_repr=True` C→F/F→C path — that path is covered by the Task-3 in-tree α==β round-trip (T-si2-02), with the documented residual above. The in-tree energy-reconstruction test (½⟨eri,dm2⟩+e1==e_tot) is MO-basis and likewise does not touch the ao_repr path.
</threat_model>

<verification>
- `cargo +nightly test -p pyscf-ccsd --locked` → full suite green (ulambda + urdm behavior tests, uccsd surfacing, no regression), INCLUDING the Task-3 ao_repr=true α==β round-trip invariant (umake_rdm2(ao_repr=true) == validated rdm.rs::make_rdm2(ao_repr=true) within ~1e-9 — the required in-tree coverage for the make_rdm2 ao_repr back-transform, Pitfall 3).
- `cargo +nightly test -p pyscf-py --locked` → bridge structural tests green.
- Live: `.upstream-venv/bin/python crates/pyscf-py/tests/ucc_open_shell_oracle.py` → PASS (make_rdm1 block byte-identity ≤1e-7 + Tr=9).
- `cargo tree -p pyscf-ccsd` and `-p pyscf-py` → 0 libxc rows (no ~6h build triggered; CLAUDE.md).
- Algebra wall: `grep -v '^//' crates/pyscf-ccsd/src/{ulambda,urdm}.rs | grep -c 'gemm'` == 0; every contraction via oracle_sum/oracle_dot.
- Full cargo output saved under `log/` before investigating any build issue (CLAUDE.md).
</verification>

<success_criteria>
- F-07 closed: open-shell UCCSD λ (ulambda.rs) + RDMs (urdm.rs) implemented, wave-3 make_rdm1/make_rdm2 path live via the PyUCCSD open-shell entry points.
- The OH-doublet live-PySCF oracle PASSES: make_rdm1 (dm1a, dm1b) byte-identical to PySCF 2.12.1 ≤1e-7, Tr(dm1a)+Tr(dm1b)=9.
- In-tree invariants hold: Tr=nelec, α==β collapse to validated closed-shell λ/RDM, l2 antisymmetry, RAYON 1==8 bit-identity, AND the ao_repr=true α==β round-trip (umake_rdm2(ao_repr=true) == validated rdm.rs::make_rdm2(ao_repr=true) within ~1e-9 — the make_rdm2 ao_repr back-transform ships with real coverage, no longer unverified).
- Algebra-wall discipline preserved (oracle_sum/oracle_dot, no bare +=, no gemm); no new deps; 0 libxc rows.
- Each task atomically committable.
</success_criteria>

<output>
Create `.planning/quick/260601-si2-implement-open-shell-uccsd-lambda-and-rd/260601-si2-SUMMARY.md` when done.
</output>
