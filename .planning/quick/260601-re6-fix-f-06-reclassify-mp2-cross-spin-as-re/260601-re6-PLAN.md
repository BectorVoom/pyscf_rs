---
quick_id: 260601-re6
description: "fix F-06: reclassify MP2 cross-spin αβ as RESOLVED (live-PySCF oracle-certified)"
date: 2026-06-01
status: in-progress
---

# Quick Task 260601-re6 — fix F-06 (MP2/DFMP2 cross-spin αβ)

## Finding

`F-06` in `.planning/AUDIT-FIX-2026-06-01.md` was classified **manual-only** in
follow-up pass 5 with the rationale that the cross-spin `(o_α v_α | o_β v_β)`
ao2mo layout is *"unverifiable in-sandbox by any oracle-free invariant"* and
needs *"open-shell byte-identity against live PySCF ... beyond a minimal change +
cargo test."*

## What ground-truthing revealed

That rationale is **stale**. After pass 5, the `260601-nfb`/`260601-pbk` quick
tasks landed all three of F-06's enumerated sub-tasks, and the live-PySCF oracle
pass 5 deemed impossible **exists and passes**:

1. **(a) cross-spin ao2mo wrapper + F→C layout** — `cross_spin_ao2mo`
   (`crates/pyscf-mp2/src/mp2.rs:214`) + F→C repack in `default_ao2mo`
   (commit `d7e7fad`). In-tree layout-pinning tests green
   (`ump2_cross_spin.rs`: Test A F→C repack, Test B closed-shell guard, Test C
   bra-ket symmetry).
2. **(b) un-gate 2 PyO3 sites + wire `dfump2_kernel`** — commit `8540566`. The
   `pyscf-py/src/mp.rs` αβ sites no longer return `NotYetImplemented{plan:4}`.
3. **(c) open-shell byte-identity vs live PySCF** — harness
   `crates/pyscf-py/tests/ump2_open_shell_oracle.py` (OH doublet, spin=1,
   STO-3G; nocc_α ≠ nocc_β so the F→C repack is genuinely exercised).

## Acceptance gate result (this task)

Ran `.upstream-venv` (PySCF 2.12.1) → rs `.venv` PyO3 two-venv `.npy`
cross-compare. **Conventional open-shell UMP2 matches live PySCF to
`|Δe_corr| = 2.307e-10` (tol 1e-9) → PASS.** The cross-spin layout is certified.

DFUMP2 residual = `2.629e-5`: the separate DF metric-fit-inverse method gap
(rs rank-revealing eigh+lindep vs upstream Cholesky), explicitly **not** the
cross-spin layout and **not** F-06's gate (DF-01 d-shell integral bug already
fixed, cintx `55bf984`).

## Tasks

1. **No source change.** F-06's code is complete and now live-PySCF-certified.
   The DFUMP2 metric-method residual is out of F-06 scope (separate item).
2. **Reclassify F-06** in `.planning/AUDIT-FIX-2026-06-01.md`: manual-only →
   **FIXED (cross-spin layout, oracle-certified 2.3e-10)**; update the table row,
   the tally, the F-06 note (record the oracle result + the refuted pass-5
   premise), add a Fixed-table row, and move the DFUMP2 metric gap to the
   residual/separate-item list. Mirrors the F-01/F-03/F-04 reclassification
   pattern.
3. Update STATE.md "Quick Tasks Completed"; atomic commit with the finding ID.

## Verify / done

- `cargo +nightly test -p pyscf-mp2 --locked` → green (logged `log/f06-mp2-tests.log`).
- `ump2_open_shell_oracle.py` → `RESULT: PASS` at 2.3e-10 (logged
  `log/f06-open-shell-oracle.log`).
- AUDIT-FIX F-06 row reads FIXED, with the oracle evidence recorded.
