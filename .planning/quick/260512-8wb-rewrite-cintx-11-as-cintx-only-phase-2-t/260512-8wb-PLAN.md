---
quick_id: 260512-8wb
type: quick
description: "Rewrite cintx#11 as cintx-only Phase 2 task list (drop pyscf_rs framing)"
status: complete
date: 2026-05-11
---

# Quick Task 260512-8wb: Plan

## Context

Issue [BectorVoom/cintx#11](https://github.com/BectorVoom/cintx/issues/11) was
filed yesterday (quick task 260512-8jv) titled *"pyscf_rs Phase 2 follow-ups:
int1e_ecp operators, arity ≥3 safe-API dispatch, real-integral evaluation"*.
The body was framed entirely from pyscf_rs's perspective ("blocks pyscf_rs
plan X-Y", links into pyscf_rs `.planning/` paths, references pyscf_rs file
anchors).

User asked for a cintx-only issue describing the same three tasks from
cintx's perspective — what cintx needs to build, with no pyscf_rs framing.

User chose **"Edit #11 in place"** (vs. close-and-replace or duplicate) and
**"All three asks"** for scope.

## Approach

Single GitHub API call — `gh issue edit 11 --repo BectorVoom/cintx`. The work
is one well-bounded artifact (issue body markdown), so no executor subagent is
needed. The technical content is already grounded in the prior task's
verification work (260512-8jv); the task here is reframing, not new research.

## Tasks

### 1. Verify cintx state hasn't moved since 260512-8jv

Re-check the three signal anchors before rewriting:

- **Ask 1:** `gh search code --repo BectorVoom/cintx 'int1e_ecp'` — should still return zero matches.
- **Ask 3:** Fetch `crates/cintx-rs/src/api.rs` from cintx master, confirm `fill_staging_values` still has the synthetic pattern (`(idx + 1) as f64`).

If either has moved, scope shrinks. If both unchanged, all three asks remain valid.

**Verify:** both anchors confirmed unchanged ✓

### 2. Draft cintx-only issue body

Rewrite the three asks from cintx's perspective:

- No "blocks pyscf_rs plan X-Y" lines.
- No links into `pyscf_rs/.planning/` paths.
- No references to pyscf_rs file anchors (`crates/pyscf-gto/...`).
- Title reframed from "pyscf_rs Phase 2 follow-ups" → "Safe API: extend SessionRequest with ..."
- Add a complexity-ordering table (smallest → largest) since the cross-project blocking ordering is gone.
- Keep all cintx-internal file pointers (`crates/cintx-rs/src/api.rs`, `cintx-ops/...`, `cintx-compat/...`).

Draft to `/tmp/cintx-11-body.md`.

**Verify:** body file contains zero `pyscf_rs` mentions.

### 3. Edit issue #11 in place

```
gh issue edit 11 --repo BectorVoom/cintx \
  --title "Safe API: extend SessionRequest with ECP operators, arity ≥3 dispatch, and real-integral evaluation" \
  --body-file /tmp/cintx-11-body.md
```

**Verify:** `gh issue view 11 --repo BectorVoom/cintx --json title` reflects the new title; issue state still `OPEN`; label `enhancement` preserved (gh issue edit leaves labels alone unless `--add-label` / `--remove-label` is passed).

## must_haves

- truths:
  - Issue #11 still references the three real, verified-as-unfixed cintx tasks.
  - New body contains zero `pyscf_rs` mentions.
  - Issue state preserved (OPEN, label `enhancement`).
- artifacts:
  - PLAN.md, SUMMARY.md under `.planning/quick/260512-8wb-.../`
  - STATE.md updated with quick task row.
- key_links:
  - https://github.com/BectorVoom/cintx/issues/11
  - cintx `crates/cintx-rs/src/api.rs` (Ask 3 anchor)
  - cintx `crates/cintx-ops/src/generated/api_manifest.csv` (Ask 2 anchor)

## Out of scope

- No bumping of the cintx pin in pyscf_rs `Cargo.toml`.
- No closing or merging of cintx#11 — issue remains OPEN tracking the work.
- No changes to pyscf_rs code or to other cintx issues.
- No new pyscf_rs planning artifacts (only the quick-task ledger).
